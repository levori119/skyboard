# מעקב נסיעה חי - אפיון

> ניהול נסיעות, שלב ההפעלה: מרגע שהנהג לוחץ **"הפעל נסיעה"** ועד שהוא מסיים.
> הנהג רואה את הנתיב ואת מה שחוסם אותו, והמגדל רואה את הרכב נוסע ומקבל התרעה
> כשהוא סוטה או מתקרב לאלמנט שסוגר את הדרך.

## 0. מה המסמך הזה מכריע

| שאלה | הכרעה | מקור |
|---|---|---|
| איזו מפה רואה הנהג | **גם וגם** - מפת השדה של המגדל **ו**-Google Maps, עם מעבר ביניהן. הנתיב מצויר בשתיהן | אורי, 2026-09-13 |
| מתי להתריע לנהג על אלמנט | **רק כשהוא סוגר את הדרך** (רמזור אדום, מחסום סגור, STOP BAR דולק). אלמנט פתוח מוצג על המפה בלי התרעה | אורי, 2026-09-13 |
| מרחקי התרעה לנהג | מסלול טיסה **150 מ'** · מסלול הסעה **100 מ'** · אלמנט סוגר **50 מ'** | אורי, 2026-09-13 |
| סטייה במגדל | **200 מ'**, ורק כשהיא **נמשכת ~10 שניות** (שתי קריאות GPS רצופות) | אורי, 2026-09-13 |

## 1. שלושה עקרונות

1. **ההשוואה היא מול הנתיב שהמגדל אישר, לא מול חישוב מחדש.** הנתיב נשמר
   ברגע שהפקח בוחר אותו. חישוב מחדש בזמן הנסיעה עלול להחזיר דרך אחרת (רשת
   הדרכים השתנתה) - ואז "סטייה" נמדדת מול משהו שאיש לא אישר.
2. **ההערכה במגדל נעשית בשרת.** שני מגדלים שפתוחים על אותו שדה חייבים לראות
   את אותה התרעה באותו רגע. חישוב בכל לקוח בנפרד הוא שתי תשובות.
3. **ההתרעות לנהג מחושבות אצלו, מקומית.** נתוני המפה נטענים פעם אחת בהפעלה,
   וההתרעה לא תלויה בקליטה. נהג שנכנס לאזור בלי קליטה ליד מסלול טיסה חייב
   לקבל את ההתרעה.

## 2. מקור אמת יחיד לגאומטריה (`shared/`)

הקוד הקיים מחזיק **שלושה** עותקי haversine, **שני** עותקי המרת נ"צ↔אחוזי מפה
(לקוח TS ושרת JS, בלי הופכי בשרת), ו**שלושה** עותקים שאינם עקביים של "אלמנט
חוסם נתיב". מעקב חי צריך את שלושתם בשרת, באפליקציית הנהג ובמגדל - ולכן הם
נכתבים **פעם אחת** ב-`shared/`, שכבר נטען משלושת הצדדים (`shared/driverLogic.js`).

הכל במודול **אחד**, `shared/tripTracking.js`, שדף הנהג טוען מ-`/driver/tracking.js`
(כמו `driverLogic.js` מ-`/driver/logic.js`) - נתיב אחד, יבוא אחד.

| חלק | מה |
|---|---|
| גאומטריה | `anchorFrom` · `pctToLatLon` / `latLonToPct` (עוגן שתי נקודות, לינארי - אותה מתמטיקה כמו `src/utils/geo.ts`) · `metersToSegment` · `metersToPolyline` |
| חסימה | `effectiveBlockingStatuses` (עם ברירות המחדל לקטגוריה) · `isElementBlocking` · `roadControlElements` · `compactWaypoints` |
| התרעות | הספים · `nextDeviationStreak` / `isDeviating` · `findHazards` · `nextAlertState` · `isFixStale` |

מקובע ב-54 בדיקות (`shared/tripTracking.test.js`), כולל מרחק ניצב מול haversine
עצמאי בדיוק של פחות ממטר.

**לא נכלל כאן:** איחוד שלושת העותקים הקיימים ב-`GroundView` וב-`GroundVehiclePanel`.
הם משרתים את אנימציית הרכב המדומה (`elemNavData`), מבוססי מזהי `airfield_routes`,
ויעברו ל-`shared/elementBlocking.js` בשינוי נפרד.

## 3. מודל הנתונים

### 3.1 הנתיב שאושר - בתוך `route_options`

כל אפשרות נתיב (`RouteOption`) נושאת מעתה את ה-`waypoints` שחזרו מ-`/api/route-plan`,
בצורה מצומצמת: `{lat, lon, xPct, yPct, routeType, isCrossing}`. הנתיב שאושר הוא
האפשרות ש-`route_ids` שלה שווה (כקבוצה) ל-`selected_route_ids`.

> **נסיעה ישנה בלי `waypoints`** (אושרה לפני השינוי): המפה מציגה מוצא, יעד
> ותחנות **בלי** קו נתיב, זיהוי סטייה **כבוי**, והמסך אומר זאת במפורש. בחירת
> הנתיב מחדש בטופס מתקנת.

### 3.2 טבלאות חדשות (שתיהן **תפעוליות** - `OPERATIONAL_TABLES`)

**`entry_permit_trip_gps`** - היסטוריית קריאות GPS:
`id, trip_id → entry_permit_trips CASCADE, lat, lng, accuracy_m, heading, speed_kmh, recorded_at TIMESTAMPTZ`.
נחתך ל-**500** הקריאות האחרונות לנסיעה.

**`entry_permit_trip_live`** - מצב חי אחד לנסיעה (PK = `trip_id`):
`lat, lng, accuracy_m, heading, speed_kmh, fix_at, deviation_m, deviation_streak, blocking_element_id, blocking_distance_m, updated_at`.

> **למה לא עמודות על `entry_permit_trips`:** שורת הנסיעה היא רשומה שהמפעיל
> עורך, ויומן הביטול עוקב אחריה. עדכון כל 5 שניות היה מערבב טלמטריה ברשומה
> תפעולית. שתי הטבלאות ברשימת החסימה של הביטול - קריאת GPS אינה פעולת מפעיל.

## 4. API

| נתיב | מי | מה |
|---|---|---|
| `GET /api/driver-trips/:id/live` | נהג (בעלות) | כל נתוני המפה בקריאה אחת: עוגן, `map_id`, נתיב, תחנות, אלמנטים רלוונטיים עם מצבם, מסלולי טיסה, מסלולי הסעה |
| `POST /api/driver-trips/:id/gps` | נהג (בעלות) | קריאת GPS. השרת שומר, מחשב סטייה וחסימה, ומחזיר את המצב |
| `POST /api/driver-trips/:id/end` | נהג (בעלות) | סיום נסיעה (`ended_at`). בלעדיו המעקב לא נגמר והמגדל רואה רכב רפאים |
| `GET /api/trips/live?airfield_id=` | מגדל | נסיעות פעילות: מיקום אחרון, גיל הקריאה, סטייה, אלמנט חוסם, והנתיב |

**בעלות:** כל נתיבי הנהג עוברים דרך `driverScope` + `ownsTrip` (כמו `/start`).
ה-`/api/vehicle-gps` הישן **אינו** נבדק לבעלות, ולכן אינו משמש כאן.

## 5. כללי ההתרעה

### 5.1 אלמנטי השליטה בתנועה, ומי מהם על הנתיב
**כל** אלמנט בשדה שיכול לסגור דרך (רמזור, מחסום, STOP BAR, או סוג עם סטטוסים) מוצג
במפה - גם רחוק מהנתיב וגם בנסיעה בלי נתיב (`roadControlElements`). מצלמות, כלי רכב
ואלמנט בלי מצב סוגר לא נכללים, וגם **אלמנט שרלוונטי רק למטוסים** (`relevant_for`, 2026-09-14) -
הוא לא מוצג לנהג ולא חוסם אותו. `on_route` - מרחק מקו הנתיב **≤ 40 מ'**
(`ROUTE_CORRIDOR_M`): מוצג מלא ועם תווית כשהוא סוגר; שאינו על הנתיב - קטן ושקוף, בלי תווית.

**למה לא רק שעל הנתיב (2026-09-13, תקלה מהשדה):** בשדה אמיתי הרמזורים והמחסומים עמדו
228 מ' ויותר מהנתיב שנשמר (הנתיב הוא צמתי כבישים, והרמזור מצויר ליד הצומת), והפרוזדור
השאיר את הנהג בלי אף אלמנט על המפה.

**הסמל - כמו במפת המגדל (2026-09-14):** שתי המפות של הנהג מציירות את האלמנט ב-`elementMarkerSvg`
(`shared/elementSymbols.js`) - אותו סמל SVG של הסוג, אותו כלל סגור/פתוח/עצור/עבור/כבוי, סיבוב,
הבהוב ו-X לאלמנט לא כשיר. אלמנט שסוגר את הדרך מקבל בנוסף הילה אדומה. במפת Google הסמל נטען
כתמונה עם ההבהוב מוטמע, והסמן `optimized:false` - סמן מאופטם מצויר על קנבס וההבהוב נקפא.

**התרעת אלמנט (D9, T7) נמדדת מהרכב**, על כל אלמנטי השליטה: רכב שסטה ונתקל במחסום
סגור - המחסום סוגר את הדרך שלו.

### 5.2 אלמנט סוגר את הדרך
המצב האפקטיבי (`display_state` ← תווית: close=סגור, stop=עצור, blink=מנצנץ ...)
או סטטוס הכשירות נמצא ב-`blocking_statuses` של האלמנט. ריק = ברירת המחדל
לקטגוריה (`STOP BAR`/`רמזורים`=מנצנץ, `מחסומים`=סגור), ואחריה הסטטוס המותר
הראשון. **אלמנט "לא שמיש" אינו סוגר** - מחסום שבור אינו יכול להיסגר.
זה הכלל של `/api/live-runway-conflicts` ושל חלון הניווט במגדל, מאוחד.

### 5.3 הספים
| התרעה | היכן | סף | השתקה |
|---|---|---|---|
| התקרבות למסלול טיסה | נהג | ≤ 150 מ' מקו האמצע | 60 ש' לכל מסלול |
| התקרבות למסלול הסעה | נהג | ≤ 100 מ' | 60 ש' לכל מסלול |
| התקרבות לאלמנט סוגר | נהג | ≤ 50 מ' | 60 ש' לכל אלמנט, **או עד שהוא נפתח** |
| סטייה מהנתיב | מגדל | > 200 מ', **2 קריאות רצופות** | נעלמת כשחוזר לנתיב |
| התקרבות לאלמנט סוגר נתיב | מגדל | ≤ 50 מ', אלמנט רלוונטי לנתיב | נעלמת כשנפתח או כשהרכב עבר |

קריאה עם דיוק גרוע מ-**100 מ'** נשמרת ומוצגת, אך **אינה** נספרת לסטייה ולחסימה.

**פעימה - הקריאה האחרונה נשלחת שוב כל 5 ש'**, כל עוד היא טרייה (עד `STALE_FIX_MS`).
טלפון עומד כמעט לא מפיק קריאות GPS חדשות. בלי הפעימה, רכב שעצר 300 מ' מהנתיב שלח
קריאה אחת בלבד - והשנייה, שמאשרת סטייה נמשכת (T5), לא הגיעה: המגדל לא קיבל התרעה
על רכב שחנה במקום הלא נכון, ורכב חונה סומן "אות אבד" אחרי דקה (T3). מעבר ל-60 ש'
הפעימה נעצרת, כדי שאובדן אות אמיתי ייראה במגדל כאובדן אות. **נמצא באימות קצה-לקצה
בדפדפן** - בדיקות היחידה לא יכלו לתפוס את זה.

**על מפת השדה, תווית רק לאלמנט שסוגר.** אלמנט פתוח מוצג בסמל ובטבעת ירוקה בלבד.
תוויות לכולם נערמו זו על זו ליד מחסומים סמוכים ואף אחת לא נקראה.

**התרעה לנהג עולה ב*כניסה* לאזור, לא כל עוד הרכב בתוכו** (`nextAlertState`).
בלי הכלל הזה נתיב שהמגדל אישר **לאורך** מסלול הסעה היה מתריע כל דקה לכל אורך
הנסיעה, והנהג לומד לסגור את ההתרעה בלי לקרוא. יציאה וכניסה חוזרת מתריעות שוב,
בכפוף להשתקה. רמזור שהאדים כשהרכב כבר קרוב - הוא לא היה סכנה קודם, ולכן זו כניסה.

## 6. מטריצת המקרים

### נהג
| # | מקרה | צפוי |
|---|---|---|
| D1 | הפעלה, נתיב שמור | מסך נסיעה חי: מפת שדה, הנתיב, התחנות, האלמנטים הרלוונטיים במצבם, נקודת GPS |
| D2 | הפעלה, נסיעה ישנה בלי נקודות נתיב | השרת מחשב מחדש **באותו מתכנן** (`planRoute`) עם ההרשאות של האפשרות שנבחרה. אותם מקטעים שאושרו - הקו מוצג ונשמר על הנסיעה (פעם אחת). מקטעים אחרים (רשת הדרכים השתנתה) - מוצא, יעד ותחנות + "לא נשמר נתיב לנסיעה... אפשר לבקש מהמגדל לבחור נתיב מחדש". האלמנטים מוצגים בכל מקרה |
| D3 | המפה אינה מעוגנת | הנתיב מצויר, נקודת ה-GPS מוסתרת + "המפה אינה מעוגנת - המיקום לא מוצג על מפת השדה". Google עדיין עובד |
| D4 | אין מפתח Google | מתג Google כבוי + הסיבה. **בייצור (Railway, שירות SKY KING) `GOOGLE_MAPS_API_KEY` לא מוגדר** נכון ל-2026-09-13 - נדרש מפתח Maps JavaScript API |
| D4b | מפתח Google לא תקין (Google דוחה אחרי טעינה) | `gm_authFailure`: "מפתח Google בשרת אינו תקין", מתג כבוי "(מפתח לא תקין)", חזרה למפת השדה. **כך היה בייצור ב-2026-09-14** - הערך ב-Railway אינו מפתח API (Google: `REQUEST_DENIED`) |
| D5 | אין הרשאת מיקום | "אין הרשאת מיקום" - המפה מוצגת, אין שידור ואין התרעות |
| D6 | דיוק גרוע (> 100 מ') | הנקודה מוצגת עם עיגול דיוק, התרעות לא מופעלות ממנה |
| D7 | ≤ 150 מ' ממסלול טיסה | התרעה מלאה + צליל + הקראה, 60 ש' השתקה |
| D8 | ≤ 100 מ' ממסלול הסעה | כנ"ל |
| D9 | ≤ 50 מ' מאלמנט **סוגר** | "רמזור אדום לפניך - עצור" |
| D10 | ≤ 50 מ' מאלמנט **פתוח** | אין התרעה; האלמנט מוצג |
| D11 | האלמנט נפתח בזמן הנסיעה | המפה מתעדכנת תוך 5 ש', ההתרעה לא חוזרת |
| D12 | "סיים נסיעה" | השידור נעצר, הרכב יורד מהמגדל |
| D13 | הטלפון ננעל / האפליקציה ברקע | דף אינטרנט לא מקבל GPS ברקע (מגבלת דפדפן, אין לעקוף). ביציאה נשלח `app-state: background` (keepalive), והמגדל מציג "📱 האפליקציה ברקע אצל הנהג (מ-HH:MM)" - גם לפני 60 ש', ואחריהן "אות אבד - האפליקציה ברקע". קריאה שהדפדפן בכל זאת נותן ברקע נשלחת מיד. בחזרה: ה-GPS מתחדש, מיקום נשלח מיד, והנהג רואה כמה שניות לא שודר. **פתרון מלא (שידור אמיתי ברקע) - רק באפליקציה עטופה (Capacitor), פיצ'ר נפרד** |
| D14 | אין קליטה | התרעות מקומיות ממשיכות; קריאות לא נשלחות ומתחדשות בחזרה |
| D15 | מעבר בין המפות | אותו נתיב, אותם אלמנטים, אותה נקודה |

### מגדל
| # | מקרה | צפוי |
|---|---|---|
| T1 | נסיעה פעילה עם קריאות | סמל רכב במקומו, כיוון, שם, והנתיב |
| T2 | הופעלה, עוד אין קריאה | הרכב במוצא עם "ממתין למיקום" |
| T3 | קריאה אחרונה לפני > 60 ש' | סמל אפור "אות אבד" |
| T4 | > 200 מ', קריאה אחת | **אין** התרעה |
| T5 | > 200 מ', שתי קריאות רצופות | "סטייה מהנתיב - X מ'" |
| T6 | חזר לנתיב | ההתרעה נעלמת |
| T7 | ≤ 50 מ' מאלמנט סוגר על הנתיב | "מתקרב ל[אלמנט] - סגור" |
| T8 | האלמנט נפתח | ההתרעה נעלמת |
| T9 | נסיעה בלי נתיב שמור | אין זיהוי סטייה, והסיבה מוצגת |
| T10 | מפה לא מעוגנת | אין סמל רכב על המפה, והסיבה מוצגת |
| T11 | שני מגדלים | אותה התרעה (מחושבת בשרת) |
| T12 | הנסיעה הסתיימה | הסמל יורד |

### אבטחה
| # | מקרה | צפוי |
|---|---|---|
| S1 | קריאת GPS לנסיעה של נהג אחר | 404 |
| S2 | לנסיעה שלא הופעלה | 409 |
| S3 | לנסיעה שהסתיימה | 409 |
| S4 | נ"צ לא חוקי | 400 |
| S5 | הצפה | היסטוריה נחתכת ל-500 לנסיעה |

## 7. Google Maps - השלכת אבטחה

המפה של Google דורשת לפתוח את ה-CSP של **אפליקציית הנהג בלבד** (`DRIVER_CSP`)
ל-`maps.googleapis.com` ו-`maps.gstatic.com`. שתי השלכות שיש לדעת:

- **מיקום הרכב נשלח לספק חיצוני** כל עוד מפת Google פתוחה (טעינת האריחים חושפת
  את האזור). מפת השדה אינה שולחת דבר החוצה.
- **ה-CSP של המגדל אינו משתנה.**

ברירת המחדל בפתיחה היא **מפת השדה**. Google נטענת רק כשהנהג עובר אליה.

## 8. שלבים

| שלב | תכולה |
|---|---|
| **1 - תשתית** ✅ | `shared/` (גאומטריה, חסימה, התרעות) · שמירת הנתיב ב-`route_options` · שתי הטבלאות · 4 נתיבי ה-API · בדיקות |
| **2 - אפליקציית הנהג** ✅ | מסך נסיעה חי, שתי המפות, שידור GPS, התרעות מקומיות, "סיים נסיעה" |
| **3 - מגדל** ✅ | שכבת רכב חי ב-GroundView, הנתיב, התרעות סטייה וחסימה ב-TripAlertsLayer |

## 9. במגדל - הכרעות תצוגה

| נושא | הכרעה | למה |
|---|---|---|
| **עיגן הסמל** | מרכז **העיגול** על המיקום, התווית תלויה מתחתיו | מירכוז העיגול והתווית יחד העלה את העיגול 17 פיקסלים מעל המיקום האמיתי - עשרות מטרים בזום, מספיק כדי להציג רכב על מסלול טיסה כשהוא לא עליו. **נמצא בבדיקת דפדפן** |
| **שכבה לפי חומרה** | חסימה > סטייה > אות אבד > ממתין > תקין | רכבים סמוכים (שיירה) חופפים; רכב מתריע אסור שיוסתר מאחורי רכב תקין |
| **תווית רכב תקין** | שם בלבד; המהירות בתיאור הצף | תווית קטנה מכסה פחות במקום צפוף |
| **אות אבד גובר** | על סטייה וחסימה | רכב שאיבד אות בזמן שסטה - המיקום כבר לא ידוע, והצגתו כ"סוטה" היא ודאות שאין |
| **נסיעה שהופעלה** | יורדת משכבת הנסיעות הקרובות | אחרת אותו רכב פעמיים - תקוע בשער ובשטח |
| **התרעה שנסגרה** | חוזרת כשהתנאי חלף וחזר (`pruneDismissedLive`) | בלי זה: הפקח סגר סטייה, הרכב חזר לנתיב וסטה שוב - ואין התרעה |
| **סקר משותף** | ערוץ אחד לכל שדה עם ספירת מנויים (`sharedPolling`) | המפה וההתרעות חייבות לראות את **אותה** תמונה - שני סקרים נפרדים חלוקים עד 5 ש'. ו-`pollingRegistry.register` **מחליף** משימה באותו מפתח, כך ששני צרכנים ישירים היו משאירים אחד קפוא |

**נבדק בדפדפן אמיתי:** `e2e/trip-live-tower.spec.ts` על המתקן `e2e/fixtures/trip-live.tsx` -
הרכיבים וההוקים האמיתיים, רשת מדומה, **בלי DB**. 8 בדיקות, כולל מיקום גאומטרי, חזרת
התרעה שנסגרה, ושכבה לפי חומרה (אומתה שהיא **נכשלת** בלי הדירוג).

> ⚠️ `playwright.config.ts` מעלה `node server.js` - מול ה-DB של הייצור. הבדיקה הזו
> צריכה רק את vite. להרצה מבודדת: קונפיג עם שרת vite בלבד (ראה REFACTOR_LOG 2026-09-13).
