# SKY-KING - Claude Code Project Instructions

> קובץ זה נקרא אוטומטית בתחילת כל סשן Claude Code.
> עדכון שפה מקצועית: רק כשהצוות מצהיר במפורש "מעכשיו X".

---

## מסמכי ייחוס (לקרוא לפני עבודה)

| מסמך | תוכן |
|------|------|
| [SHARED_LANGUAGE.md](SHARED_LANGUAGE.md) | **שפה משותפת** - 19 השירותים בשם עסקי (העברות עמדה, התראות בד"ח...) + מיפוי לקוד |
| [SERVICES.md](SERVICES.md) | **קטלוג טכני** - כל מודול: שם, מיקום, תפקיד, מה מייצא + כל 485 ה-endpoints |
| [README.md](README.md) | התקנה, הרצה, tech stack |
| [DEV_GUIDE.md](DEV_GUIDE.md) | onboarding, מילון מונחים, conventions, FAQ |
| [ARCHITECTURE.md](ARCHITECTURE.md) | מבנה המערכת, זרימת נתונים, מפת תלויות |
| [REFACTOR_LOG.md](REFACTOR_LOG.md) | לוג כל שינוי + QA |
| [data-model.md](data-model.md) | מבנה ה-DB |
| [CIV_STRIP_CONTROLS.md](CIV_STRIP_CONTROLS.md) | **פקדים על הסטריפ** - חמשת הסוגים, ב"מ, היקף (פנימי ללוח / גלובלי לפ"מ), עיצוב מותנה ומטריצת המקרים |
| [UNDO_SPEC.md](UNDO_SPEC.md) | **ביטול פעולה (CTRL+Z)** - יומן הביטול, רשימת החסימה (העברות ו-GAPI), זיהוי התנגשות בין עמדות ומטריצת המקרים |
| [USER_STORIES.md](USER_STORIES.md) | סטוריות משתמש |

> **לפני שמחפשים איפה קוד נמצא - לבדוק ב-SERVICES.md.** הקוד מודולרי: server.js → server/routes/*, App.tsx → src/components/*.
> **כשמדברים על פיצ'ר - להשתמש בשם מ-SHARED_LANGUAGE.md** (שפה משותפת לצוות).

---

## מי אנחנו

**SKY-KING** מפותח על ידי **אורי לב** ו**אורי אלימלך** - **בקרי טיסה בחיל האוויר**.

אנחנו מכירים את שולחן הבקרה ואת עמדות הפיקוח מבפנים, ומפתחים מערכת שמחליפה כלי עבודה פיזיים יומיומיים.

---

## מה זה SKY-KING

**SKY-KING** הוא **דesk אלקטרוני חכם** לרישום, ניהול ותצוגה של **מידע שדה**
בשדה האווירי והקרקעי - **באזורים המבצעיים** (מסלולי טיסה וסביבתם).

המערכת מחליפה את **הסדק הפלסטיק** - לוח הרישום שבו כותבים ב**צ'ינו**
ומוחקים עם **פלנלית**.

---

## למי המערכת משרתת

| עמדה | מפעיל | תפקיד |
|------|--------|--------|
| **שולחן בקרה** (CTRL) | **בקר** | ניהול ורישום מידע שדה בשולחן הבקרה |
| **מגדל** (TWR) | **פקח** | ניהול שדה אווירי וקרקעי באזורים המבצעיים |

> שים לב: **עמדות שונות → מסכים שונים**.
> אותם רכיבים, אותה לוגיקה - לא duplicate code/UI.

---

## מה המערכת מנהלת

- **שדה אווירי** - מסלולי טיסה, אזורי תנועה אווירית, מגבלות
- **שדה קרקעי** - תנועה קרקעית, נקודות, מצבים תפעוליים
- **אזורים מבצעיים** - רק מה שרלוונטי לתפעול בזמן אמת

---

## עקרונות UX (חובה)

SKY-KING חייבת להיות **ממוקדת משתמש** - לא "מערכת טכנית עם מסך".

1. **מהירות תפעולית** - פחות צעדים מהסדק הפיזי
2. **קריאות** - פונטים גדולים, ניגודיות, מינימום רעש
3. **עקביות** - אותו רכיב = אותה התנהגות בכל מסך
4. **שקיפות** - המשתמש תמיד יודע מה השתנה, מי שינה, ומתי
5. **דו-לשוניות (i18n)** - עברית **ברירת מחדל**, אנגלית נבחרת. כל טקסט UI חדש דרך `t('ns.key')` (i18next) - **לא** מחרוזת מקודדת. בורר שפה ב-LOGIN. פירוט: [DEV_GUIDE.md](DEV_GUIDE.md) §i18n
6. **Dark mode** - ברירת מחדל לחדר בקרה / מגדל
7. **קיצורי מקלדת** - לפעולות תכופות
8. **אישור לפני פעולה קריטית** - לא modal מיותר, אבל לא מחיקה שקטה
9. **מגע ועט לפני עכבר** - העמדה בפועל היא מסך מגע (Wacom Cintiq 24 Touch + Pro Pen 3). **כל** גרירה במערכת חייבת לעבוד באצבע ובעט, לא רק בעכבר. פירוט: §גרירה למטה
10. **מסגרת חלון בצבע** - **לכל** חלון צף יש מסגרת, וצבעה אומר מה החלון עושה. פירוט: §מסגרת חלון למטה

> לפני כל feature חדש: "האם בקר/פקח יכול לעשות את זה מהר יותר מהסדק?"

---

## מסגרת חלון - קוד צבע (חובה)

על המסך של הפקח/בקר פתוחים עשרות חלונות צפים בו-זמנית. **צבע המסגרת אומר מה
החלון עושה**, כדי לזהות אותו במבט אחד בלי לקרוא את הכותרת.

### הכלל

**לכל** חלון צף יש מסגרת `2px`, בצבע לפי סוגו:

| צבע | סוג חלון | מתי | דוגמאות |
|-----|----------|-----|---------|
| 🟠 **כתום** | **עריכה** | החלון משנה הגדרות, תוכן או תצורה | עורך אזורי מפה, סרגל ציור, עריכת שאילתא של חלון נתונים |
| 🔵 **תורכיז** | **צפייה ותפעול** | החלון מציג מצב שדה ומאפשר פעולה תפעולית שוטפת | הודעות שלי, מסלולים בשימוש, תמונה אווירית, דסק חופשי |

### המימוש (חובה)

```tsx
// ✅ נכון - מקור אמת יחיד, מותאם לשלוש התמות
import { windowFrame } from '../../utils/windowFrame';
style={{ background: C.panel, ...windowFrame('view', themeMode, 8) }}   // או 'edit'

// ❌ אסור - צבע מסגרת מקודד בחלון עצמו
style={{ border: '1px solid #334155', borderRadius: 8 }}
```

הצבעים המדויקים ב-[`src/utils/windowFrame.ts`](src/utils/windowFrame.ts) (שלוש
התמות; ocean היא תמה **כהה** ולכן צבעיה בהירים כמו dark).

### חריג: חלון שצבעו מזהה ישות

חלון שהצבע שלו נושא משמעות **פר-ישות** שומר על צבעו, כי שם הצבע עונה על "*איזו*
ישות זו" ולא על "*איזה סוג* חלון זה":

- **נקודת הצטרפות** (`JoiningPointPanel`) - צבע הנקודה כפי שהוגדר לה
- **חלון נתונים** (`DataWindowLayer`) - הצבע שנבחר לחלון בניהול
- **פתקים** (`StickyNotesLayer`) - צבע הפתק, כמו פתק פיזי

> שים לב: **מודל העריכה** של חלון כזה כן מקבל כתום (למשל עריכת השאילתא של חלון
> נתונים), כדי שיהיה ברור מיד שנמצאים במצב עריכה.

---

## קונטיינר החלונות

על המסך צפים עשרות חלונות בו-זמנית, וכל אחד מכסה חלק מהמפה. ה**קונטיינר** הוא
עמודה בצד (בין הפ"מים לעזרים) שאליה הפקח/בקר דוחף חלון צף: החלון יוצא
מהערבוביה, מקבל משבצת קבועה, ונשאר בה עד שגוררים אותו החוצה.

| | |
|---|---|
| **הפעלה** | `workstation_presets.show_window_container` בניהול העמדה קובע אם היכולת **קיימת**; המתג בתפריט "תצוגה" פותח וסוגר בפועל |
| **סידור** | `localStorage` פר-עמדה (לא DB) - הסידור שווה משהו גם אחרי רענון |
| **גודל** | **הרוחב קובע**: כל חלון מוקטן/מוגדל כיחידה אחת לרוחב הקונטיינר (`FitScaleBox mode="width"`), והגובה נגזר |
| **פריסה** | החלונות **נארזים למעלה** ולא נמתחים לגובה העמודה - חלון בודד לא מתנפח. כשהכל לא נכנס, הקונטיינר נגלל |
| **כמה בשורה** | מעל `DOCK_COL_WIDTH` (240) לכל עמודה - החלונות נעמדים **אחד ליד השני** (CSS Grid, עד 4 עמודות) במקום להימתח לרוחב מלא |
| **מיקום** | בורר ויזואלי שנפתח מהמשולש שבשורת "קונטיינר חלונות" (מקופל כברירת מחדל): הכי שמאלי · צמוד למפה · בין הפ"מים לעזרים · הכי ימני. ה-`order` מגיע מ-`DOCK_POSITION_ORDER`, ועמודות השורה מקבלות ערכים אי-זוגיים (נקודות=1ż3 · מפה=2 · פ"מים=5 · עזרים=7) |
| **חלון חדש** | נכנס ל**ראש** הרשימה כשמשחררים אותו בשטח הריק; שחרור **על** משבצת מכבד את המקום שכוונו אליו |

### להפוך חלון צף לבר-עגינה

שלושה שינויים בחלון עצמו, ותו לא ([`useDockableWindow`](src/hooks/useDockableWindow.ts)):

```tsx
const dock = useDockableWindow('signalBoard', tr('dock.winMessages'), {
  // בלעדיהם הגרירה פנימה דורסת את המיקום הצף, ובשחרור החלון "נעלם" מעבר לקצה
  setFloatingPos: (x, y) => setPos({ x, y }),
  floatingPos: () => pos,
});
// 1. שורש החלון - rootStyle **אחרון**, כדי שיבטל את position/left/top הצפים
<div style={{ position:'fixed', left, top, ...dock.rootStyle }}>
  // 2. ידית הגרירה - לצד ההתנהגות הקיימת, לא במקומה
  <div onPointerDown={e => { dock.onHeaderPointerDown(e); ...הקיים... }}>
// 3. עטיפת ההחזרה
return dock.render(win);
```

חלון שנולד ב-`map()` (פתקיות, חלונות נתונים) נעטף ב-`<DockableWindow>` -
hook אי אפשר לקרוא בתוך לולאה. חלון שכבר מרונדר ב-`createPortal` מחליף רק את
היעד: `createPortal(win, dock.slotEl ?? document.body)`.

> **מה לא מעוגן, בכוונה:** חלונות **עריכה** שקשורים לבחירה רגעית ונסגרים
> ב-Esc (סרגל הציור, עורך אזורי המפה, טבלת נקודות הכוונון). הם לא "חלונות
> שמחזיקים על המסך", ועגינה שלהם היא מלכודת ולא פיצ'ר.

---

## גרירה - מגע ועט (חובה)

עמדת היעד היא **מסך מגע** (Wacom Cintiq 24 Touch + Pro Pen 3). הפקח/בקר עובד
ב**עט** וב**אצבע** - העכבר הוא מקרה הקצה, לא ההפך.

### הכלל

**כל** אינטראקציית גרירה - ללא יוצא מן הכלל - חייבת לעבוד בעט ובאצבע:

| מה נגרר | דוגמאות |
|---------|---------|
| **חלונות צפים** | דסק חופשי, פתקים, חלונות נתונים, פאנל מסלולים בשימוש |
| **ספליטרים / שינוי גודל** | רוחב עמודות בתצוגה קלאסית, גובה סטריפ, פאנל עזרים, ידית ⇲ |
| **פ"מים** | גרירת סטריפ בין חלונות, גרירה למפה, גרירה לנקודת העברה |
| **מפות** | pan, גרירת אזור/נקודה בעורך, גרירת אייקון פ"מ |
| **עורכים** | סידור כפתורים בלוח, גרירת שדות בעורך תצוגה, סידור מחדש ברשימות |

### המימוש (חובה)

```tsx
// ✅ נכון - Pointer Events מכסים עכבר + עט + אצבע במימוש אחד
onPointerDown={e => { el.setPointerCapture(e.pointerId); ... }}
style={{ touchAction: 'none', userSelect: 'none' }}

// ❌ אסור - onMouseDown / window.addEventListener('mousemove')
```

ארבע המלכודות (כולן פתורות ב-[`useDragPosition`](src/hooks/useDragPosition.ts)):

1. **`touchAction: 'none'`** על ידית הגרירה - בלעדיו הדפדפן תופס את התנועה כגלילה
   ואירועי `pointermove` **לא נשלחים בכלל** באצבע. זו הסיבה #1 ש"לא מצליח לגרור בטאץ'".
2. **`setPointerCapture(e.pointerId)`** - בלעדיו הגרירה נקטעת כשהמצביע עובר מעל
   iframe (סרגל הצצה) או יוצא מהאלמנט.
3. **חלוקה ב-`--s`** - `#root` תחת `zoom: var(--s)`, ולכן `clientX/clientY` מגיעים
   בפיקסלים אמיתיים בעוד `left/top` ביחידות מוגדלות. בלי חלוקה החלון זז פי 1.65 מהיד.
4. **בלי `preventDefault` על `pointerdown`** - בעט/מגע הוא מבטל את אירועי העכבר
   התואמים ותפריטים שנסגרים ב-`mousedown` נתקעים פתוחים. סימון טקסט נמנע ב-`userSelect`.

> **לפני מימוש גרירה חדשה:** לבדוק אם [`useDragPosition`](src/hooks/useDragPosition.ts)
> מתאים (חלון צף) - ורק אם לא, לכתוב Pointer Events ידנית לפי ארבעת הסעיפים.
> `onMouseDown` לגיטימי רק ל**לא-גרירה** (למשל `e.preventDefault()` על כפתור
> שמונע איבוד פוקוס מ-input).

---

## עקרון הרכיבים המשותפים (DRY)

### כלל

רכיבים **זהים** (למשל: **נקודות העברה**) חולקים:

- אותה **פונקציונליות**
- אותה **לוגיקה**
- אותו **עיצוב**

### שינוי ברכיב

אם רכיב מוגדר כ**זהה** - שינוי אחד **חל על כל המסכים** שמשתמשים בו.

### חריג

רק אם נאמר **במפורש** שב**סוג פעולה / תהליך מסוים**
הרכיב מתנהג **שונה** - אז מותר divergence נקודתי.

תבנית לתיעוד חריג:

```markdown
### חריג: [שם הרכיב] ב-[סוג תהליך]
- רכיב: [שם]
- תהליך: [TBD]
- התנהגות שונה: [TBD]
- סיבה: [TBD]
```

### מסכים vs רכיבים

| | |
|---|---|
| **מסך** | שונה לפי סוג עמדה / משתמש |
| **רכיב** | משותף - לא לשכפל |

> Claude: לפני יצירת רכיב חדש - לבדוק אם כבר קיים רכיב זהה.

---

## שפה מקצועית (X) - מתעדכנת

השפה המקצועית של SKY-KING נבנית בהדרגה.
קטגוריות עיקריות (ימולאו):

| קטגוריה | סטטוס |
|---------|--------|
| **סוגי רכיבים** | 🔜 יוגדר |
| **סוגי עמדות** | 🔜 יוגדר |
| **סוגי תצוגות** | 🔜 יוגדר |
| *(נוספות)* | 🔜 |

### כלל עדכון

כשהצוות אומר **"מעכשיו X"** - לעדכן קובץ זה:

1. להוסיף/לשנות את המונח בטבלת המונחים
2. לעדכן את כל ההפניות בקובץ
3. **לא** להמציא מונחים שלא הוגדרו

### סוגי עמדות - טיוטה ראשונית

| סוג עמדה | קוד | מסך ייעודי |
|-----------|-----|------------|
| בקר טיסה | CTRL | [TBD] |
| מגדל פיקוח | TWR | [TBD] |
| דסק משימה כללי | mission_desk | MissionDeskView |

### סוגי רכיבים - מה שידוע

| רכיב | משותף? | הערות |
|-------|--------|-------|
| נקודות העברה | ✅ כן | אותה פונקציונליות בכל מסך |
| נקודות הצטרפות (STAR) | ⚠️ חריג | חולקת את **מנגנון ההעברות** עם נקודת ההעברה, אבל **התצוגה שונה** (טבלת בלוקי גבהים ולא פאנל מוסר/מקבל). רלוונטית רק לעמדת **שדה** |
| [TBD] | | |

### חריג: נקודת הצטרפות מול נקודת העברה
- רכיב: נקודת הצטרפות (`ground/JoiningPointPanel.tsx`)
- תהליך: קליטת מטוסים מצטרפים בעמדת שדה
- התנהגות שונה: הנקודה נפרסת ל**טבלת בלוקי גבהים** ופ"מ יושב בבלוק לפי גובהו, במקום פאנל מוסר/מקבל
- סיבה: נאמר במפורש באפיון - "דומה לנקודות העברה **אך תצוגה שונה**". המנגנון (accept, כרטיס ההעברה) נשאר משותף

### מונחים ידועים (legacy → SKY-KING)

| Legacy | משמעות |
|--------|---------|
| **סדק** | לוח פיזי - מה ש-SKY-KING מחליף |
| **צ'ינו** | סימון על הסדק |
| **פלנלית** | מחיקה על הסדק |
| **מידע שדה** | מידע תפעולי על מצב השדה |
| **נקודות העברה** | רכיב משותף - פונקציונליות זהה בכל מסך |
| **בקר** | מפעיל עמדת CTRL (שולחן בקרה) |
| **מגדל** | עמדת TWR - מפת שדה קרקעי (GroundView) |
| **פקח** | מפעיל עמדת המגדל (TWR) |
| **תמונ"א** (תמונה אווירית) | ה**מטוס הפיזי בשמיים**, כפי שהמאגר החיצוני מודד אותו. קיים רק כל עוד הוא באוויר |
| **פ"מ** | ה**רישום** של המטוס - לפני שהמריא, אחרי שהמריא, אחרי שנחת. יושב ב-DB של SKY-KING |

> **מעכשיו (2026-06-22):** עמדת ה-TWR נקראת **מגדל** (לא "מגד"), והמפעיל שלה הוא **פקח**.

> **מעכשיו (2026-08-07):** **תמונ"א ≠ פ"מ.** תמונה אווירית היא המטוס הפיזי בשמיים;
> פ"מ הוא הרישום שלו לאורך כל חייו. שתי שכבות מידע נפרדות שלא מתערבבות -
> ראה [AIR_PICTURE_SPEC.md](AIR_PICTURE_SPEC.md) §0.

---

## פונקציות ליבה (MVP)

- [ ] לוח מידע שדה דיגיטלי (מרכזי)
- [ ] רישום / עדכון / ביטול רשומות
- [ ] סטטוסים ויזואליים (צבעים תפעוליים)
- [ ] היסטוריית שינויים (Event Log)
- [ ] מסכים שונים: בקר vs מגדל פיקוח
- [ ] רכיבים משותפים (נקודות העברה וכו')
- [ ] עדכון בזמן אמת בין עמדות
- [ ] התחברות משתמשים + audit

> רשימה זו תתעדכן ככל שהשפה X מתפתחת.

---

## Tech Stack

- **Frontend:** React 18 + TypeScript + Vite + Tailwind CSS + Framer Motion
- **Backend:** Node.js (ESM) + Express 5
- **DB:** PostgreSQL (Neon) דרך `pg`
- **OCR:** Tesseract.js | **מפות:** Leaflet + pdfjs-dist | **Desktop:** Electron

> פירוט מלא ב-[README.md](README.md) ו-[SERVICES.md](SERVICES.md).

---

## Important: Do NOT

- ❌ לא לשכפל רכיבים - לבדוק קודם אם קיים רכיב זהה
- ❌ לא לשנות התנהגות רכיב משותף בלי לציין שזה חריג מפורש
- ❌ לא להמציא מונחים מקצועיים - רק מה שהוגדר ב-X
- ❌ לא לעצב כמו אפליקציית consumer
- ❌ לא לבנות רכיב תצוגה עם צבעים קשיחים (לא מותאם תמה) או שלא מתכווץ לפי גודל מסך - חובה `/ui-adapt` (פרט לצבעי סטטוס)
- ❌ לא לתת לחלון צף מסגרת בצבע משלו - תמיד `windowFrame('edit' | 'view', themeMode)` מ-[`src/utils/windowFrame.ts`](src/utils/windowFrame.ts). ראה §מסגרת חלון
- ❌ לא לממש גרירה ב-`onMouseDown` / `mousemove` / `mouseup` - **הן לא נשלחות באצבע**. כל גרירה (חלון, ספליטר, פ"מ, מפה, סידור מחדש) ב-Pointer Events + `touchAction:'none'` + `setPointerCapture`. ראה §גרירה - מגע ועט
- ❌ **לא לקמט ולדחוף לגיט בלי `npm run version:bump` קודם** - **כל** בקשת "תקמט ותעלה לגיט" מתחילה ב-bump, ו-`src/version.ts` נכנס **לאותו קומיט**. הזרימה: `/ship`. הגרסה והתאריך במסך הכניסה ובחלון העזרה חייבים לשקף את מה שנדחף. ראה §גרסת המערכת
- ❌ **לא לעבוד שני סוכנים באותו worktree** - **לכל סוכן עץ (worktree) משלו** (`/worktree`, ובפיצ'ר `/feature`). שני סוכנים על אותו עץ דורסים זה את זה **בשקט**, ומקמטים זה את עבודתו של זה. ראה §worktree נפרד לכל סוכן
- ❌ לא לקמט קבצים שאינם שלך - `git status` לפני קימוט; קובץ זר בעץ = לדווח למשתמש, לא "לנקות"
- ❌ **לא להוסיף טבלה בלי לסווג אותה ב-[`server/db/env-tables.js`](server/db/env-tables.js)** - `checkTableClassification` מפיל את **כל שרשרת העלייה** על טבלה לא מסווגת ב-public, ואיתה `syncAllEnvSchemas` ו**כל העבודות המחזוריות** (קבלה אוטומטית, ניקוי פ"מים, ניקוי נקודות זמניות) - הן רצות אחריה ב-`server.js`. הסימפטום מטעה: השרת חי ועונה, ורק `/api/health` מראה `phase:"failed"`. קרה בייצור (REFACTOR_LOG #045). זה חל גם על טבלה שענף **אחר** יצר: כל ה-worktrees חולקים DB אחד
- ❌ **לא להוסיף טבלה רגישה בלי לבדוק את רשימת החסימה של הביטול** - טבלה חדשה מקבלת CTRL+Z **אוטומטית** בעלייה הבאה (הטריגר מותקן בסריקת קטלוג). אם ביטול שלה מסוכן תפעולית או בלתי הפיך - להוסיפה ל-`UNDO_DENYLIST` ב-[`server/db/undoJournal.js`](server/db/undoJournal.js) עם נימוק כתוב. ראה [UNDO_SPEC.md](UNDO_SPEC.md) §4
- ❌ **לא לתת לפקד להידלק בלי שקורה משהו** - כפתור שמשנה state אבל תנאי רינדור נוסף מונע את התוצאה נראה למפעיל בדיוק כמו פיצ'ר שבור, והוא מחפש את התקלה במקום הלא נכון. אם התנאי הנוסף הכרחי - להציג *למה* (כמו `ctrl.peekNoneConfigured` מול `ctrl.peekNonePermitted`), ואם לא - להסיר אותו (ראה `shouldRenderPattern3D`)
- ❌ לא לממש עגינה ידנית לחלון צף - `useDockableWindow` מ-[`src/hooks/useDockableWindow.ts`](src/hooks/useDockableWindow.ts). ראה §קונטיינר החלונות
- ❌ לא למחוק היסטוריה
- ❌ לא לcommit secrets
- ❌ לא להתייחס ל-AeroZone (טבלאות `az_*`) - פרויקט ישן, לא רלוונטי
- ❌ לא ליצור עמודות `TIMESTAMP` (בלי tz) - תמיד `TIMESTAMPTZ`. עמודה בלי tz נקראת מוסטת כשהשרת לא ב-UTC (שבר את התראות בד"ח, ראה REFACTOR_LOG #016)
- ❌ לא לקודד מחרוזות UI קשיח - דרך `t('ns.key')` (i18next). ולא `marginLeft/Right`/`textAlign:'left'` - תמיד CSS logical properties (`marginInlineStart/End`, `textAlign:'start'`) כדי שאנגלית (LTR) תעבוד. `dir` מנוהל **רק** ב-root דרך `useDirection`
- ❌ לא להשתמש במקף ארוך `—` (em-dash) או בינוני `–` (en-dash) בטקסט שמוצג למשתמש (i18n, כותרות, tooltips, הודעות) - תמיד מקף רגיל `-`. זה "המקף של AI" ולא כותבים אותו. חריג יחיד: בקוד כשהמקף נושא לוגיקה (regex טווחי גובה `[-–]`, סנטינל `'—'`, `split('—')`) - שם משאירים כמו שהוא

---

## סקילים - זרימת עבודה חובה

| מתי | סקיל | מה עושה |
|-----|------|---------|
| **בתחילת כל סוכן / סשן חדש - לפני השורה הראשונה של קוד** | `/worktree` | **עץ (worktree) נפרד לכל סוכן** - הקמה, בדיקת בידוד, פורטים, ניקוי. זה כלל ולא המלצה |
| **בתחילת כל פיצ'ר** | `/feature` | worktree משלו + workflow מלא (pm → arch → before → TDD → QA) |
| **כל בקשה "תקמט" / "תעלה לגיט" / "תדחוף"** | `/ship` | **קימוט ודחיפה** - `npm run version:bump` **אוטומטית ראשון**, ואז add/commit/push. נאכף ב-hook |
| לפני כל feature חדש | `/pm` | סטוריית משתמש + acceptance criteria |
| לפני כתיבת קוד | `/before` | Gate - checklist חובה |
| לפני החלטה טכנית | `/arch` | תכנית טכנית + DRY check |
| לפני שינוי DB | `/migrate` | מיגרציה + עדכון data-model.md |
| לפני "done" | `/qa` | בדיקה מול עקרונות SKY-KING |
| כל שינוי בהעברות | `/transfer-logic` | context קריטי של מנגנון ההעברות |
| עבודה על עמדת מגדל | `/ground-view` | context עמדת TWR |
| עבודה על עמדת בקר | `/ctrl-view` | context עמדת CTRL |
| עיצוב/שינוי מסך או רכיב תצוגה | `/atc-ux` | ביקורת UX לפי הנדסת אנוש של ATC (FAA/EUROCONTROL/ICAO) |
| **כל רכיב תצוגה חדש/שונה** | `/ui-adapt` | **חובה: התאמה לתמה (אור/שחור/כחול) + סקייל גודל מסך (15.6/16/18/24). כולל מלכודת portal→body** |
| בדיקת מצב | `/status` | דו"ח MVP + git log |
| תמחור תכולות / הצעת מחיר / אומדן | `/pricing` | **תמחור** - שעות נטו + ימי עבודה (יום = 4-5 ש' אפקטיביות), שני בסיסי תמחור, PERT/FP/מדידת קוד |
| עבודה על real-time | `/realtime` | playbook WebSocket |
| שינוי נתוני seed | `/seed` | ניהול נתוני אתחול |
| **לפני הצגה ללקוח ביטחוני / שחרור גרסה / אחרי שינוי בשרת או בתלויות** | `/security-audit` | **ביקורת אבטחה** - סקר אבטחת מידע, אמינות ושרשרת אספקה מול OWASP/NIST/ISO + ספריות ומדינות מקור + מסמך HTML (`SECURITY_AUDIT.html`) |
| **בקשת תשתית מחיל האוויר / שינוי היקף פריסה / הצגת דרישות לגורם מתקצב** | `/hardware-spec` | **מפרט ביצועים וחומרה** - מדידת עומס/גודל/השהיה בפועל, סייזינג שרת·DB·עמדה לפי היקפי VMP/בט"ש/לחימה, ומסמך HTML להגשה (`HARDWARE_SPEC.html`) |
| **אחרי כל 3-5 שינויי קוד** | `/sync-docs` | **סנכרון כל התיעוד עם הקוד בפועל** |
| **אחרי כל בקשת משתמש - חובה, נאכף ב-hook** | `/requirements-tracker` | **רישום הבקשה ל-project-requirements.xlsx (ממוין לפי תאריך ושעה), לפני המימוש** |

> Claude לא מתחיל לכתוב קוד בלי `/before`. לא מסמן done בלי `/qa`.
> **אחרי כל batch שינויים - להפעיל `/sync-docs`** כדי שהתיעוד לא יתיישן.

---

## worktree נפרד לכל סוכן (חובה)

**כל סוכן עובד על עץ (worktree) משלו. לא שניים באותו עץ, גם לא "רק לרגע".**

> 🔧 **הסקיל:** [`/worktree`](.claude/skills/worktree/SKILL.md) - הזרימה המלאה
> (בדיקת בידוד → הקמה → פורטים → מיגרציות → קימוט → ניקוי) + המלכודות.
> להפעיל אותו **בתחילת עבודה של כל סוכן/סשן**, לפני השורה הראשונה של קוד.
> `/feature` מקים worktree כחלק מ-workflow הפיצ'ר המלא.

### הכלל

בתחילת עבודה, סוכן שאינו לבדו על הריפו מקים לעצמו worktree:

```bash
git worktree add ../skyboard-<agent-or-feature> -b feature/<name>
cp .env ../skyboard-<agent-or-feature>/.env
```

ואז עובד **רק שם** - קורא, כותב, מריץ בדיקות ומקמט.

**איך יודעים אם אני לבד:** `git worktree list` (מי עוד פרוס) + `git status --short`
(האם יש בעץ שינויים שאינם שלי). כל תשובה שאינה "worktree יחיד ועץ נקי" = להקים worktree.

**סוכני משנה:** `Agent(..., isolation: "worktree")` - worktree אוטומטי לכל סוכן.

### למה - זה קרה בפועל

כששני סוכנים עבדו על אותו working tree באותו ענף:

| מה קרה | התוצאה |
|---|---|
| סוכן א' ערך `managers.tsx`, סוכן ב' כתב את אותו קובץ | השינויים של א' **נמחקו בשקט**. התגלה רק בקומיט, במקרה |
| סוכן א' ערך את `CLAUDE.md`, וסשן אחר שחזר את הקובץ | הקומיט יצא **ריק מהשינוי** - התוכן נעלם בין העריכה ל-`git add` |
| סוכן א' קימט "הכל" | הקומיט נשא איתו קוד של סוכן ב' באמצע עבודה, כי הקבצים שזורים ואי-אפשר להפריד ברמת קובץ |
| סוכן ב' הוסיף JOIN שגוי ל-route של סוכן א' | `GET` נפל ב-500, הפיצ'ר "לא עבד", ואיש לא ידע של**מי** השבר |
| סוכן ב' הוסיף שדה לטופס בלי להשלים אותו | `tsc` נשבר בעץ המשותף, וכל סוכן אחר "ירש" בנייה אדומה |

הנזק אינו רק התנגשות: הוא **אובדן שקט**. `git` אינו מתריע כששני תהליכים
כותבים לאותו קובץ בזה אחר זה - הכתיבה האחרונה מנצחת.

### כללי עזר

- **פורטים:** worktree שמריץ `npm run dev` במקביל צריך פורטים משלו -
  `PORT` ב-`.env` ו-`vite.config.ts` (או `SKYKING_API_PORT`). ראשי: 3001/5000.
- **`node_modules`** מקושר אוטומטית דרך `.claude/settings.json` - אין `npm install`.
- **DB משותף:** ה-worktrees חולקים DB אחד. מיגרציה שסוכן אחד מריץ משפיעה על כולם,
  ולכן `ADD COLUMN IF NOT EXISTS` בלבד ואף פעם לא `DROP`.
- **לפני קימוט:** `git status` - אם יש בעץ קבצים שאינם שלך, **אל תקמט אותם**.
  לדווח למשתמש ולהמתין. קומיט של עבודה של מישהו אחר אינו "ניקיון".
- **אחרי עריכת מסמך:** לוודא שהשינוי **על הדיסק** לפני הקומיט (`grep`), כי סשן
  מקביל יכול היה לדרוס אותו בינתיים.
- **סיום:** `git worktree remove ../skyboard-<name>` אחרי מיזוג.

### אכיפה אוטומטית (Hooks)
`.claude/settings.json` מגדיר שלושה hooks:

1. **PreToolUse hook** (`Bash|PowerShell`) - לפני כל פקודת מעטפת:
   אם הפקודה מקמטת או דוחפת לגיט ו-`src/version.ts` **לא בומפ** (לא בעץ ולא בקומיטים
   שטרם נדחפו) - מוזרקת ל-Claude תזכורת מחייבת להריץ `npm run version:bump` תחילה.
   הסקריפט: [`.claude/hooks/version-bump-reminder.mjs`](.claude/hooks/version-bump-reminder.mjs).
   דילוג מכוון: `SKIP_VERSION_BUMP=1` בתוך הפקודה. ראה §גרסת המערכת.
2. **UserPromptSubmit hook** - בכל הודעת משתמש מוזרקת ל-Claude תזכורת מחייבת:
   לרשום את הבקשה ל-`project-requirements.xlsx` דרך `/requirements-tracker` **לפני** המימוש.
   כך הרישום עובד **בכל בקשה** - לא תלוי בזיכרון של המודל.
   הקובץ מנוהל **לפי תאריך ושעה** (הסקריפט ממיין כרונולוגית אחרי כל הוספה).
3. **Stop hook** - בסוף כל turn: אם קוד ב-`src/`/`server/` השתנה אך מסמכי התיעוד לא -
   מוצגת תזכורת אוטומטית להריץ `/sync-docs`.
   התזכורת נעלמת ברגע שמסמך תיעוד עודכן באותו working tree.

---

## גרסת המערכת - bump בכל קימוט ודחיפה (חובה)

> 🔧 **הסקיל:** [`/ship`](.claude/skills/ship/SKILL.md) - הזרימה המלאה של קימוט ודחיפה.

**כל בקשה של המשתמש לקמט ולהעלות לגיט** ("תקמט", "תעלה לגיט", "תדחוף") מתחילה
**אוטומטית** בעדכון מספר הגרסה + התאריך והשעה - לפני ה-`git add`:

```bash
npm run version:bump          # bump patch + חותמת זמן נוכחית (1.0.26 -> 1.0.27)
npm run version:bump 1.1.0    # גרסה מפורשת (minor / major)
```

ואז `src/version.ts` נכנס **לאותו קומיט** יחד עם הקוד, כדי שההיסטוריה תראה איזה
קוד יצא באיזו גרסה:

```bash
git add <הקבצים שלי> src/version.ts
git commit -m "<type>(<scope>): <תיאור>"
git push origin feature/i18n-bilingual:main
```

הסקריפט מעדכן את [`src/version.ts`](src/version.ts) - **מקור-אמת יחיד לגרסה**:

| קבוע | איפה מוצג |
|------|-----------|
| `APP_VERSION` | **מסך הכניסה** (פוטר, מתחת ללוגו LEO) + **חלון העזרה** בעמדה ("גרסה נוכחית") |
| `APP_VERSION_DATE` | אותם שני מקומות - פורמט `YYYY-MM-DD HH:MM` |

### אכיפה אוטומטית - שתי שכבות

| שכבה | איפה | מתי | מה עושה |
|---|---|---|---|
| **PreToolUse hook** | [`.claude/hooks/version-bump-reminder.mjs`](.claude/hooks/version-bump-reminder.mjs) | לפני כל `git commit` / `git push` שהסוכן מריץ | אם `src/version.ts` לא בומפ - מזריק ל-Claude תזכורת מחייבת להריץ `npm run version:bump`. **לא חוסם** |
| **git pre-push hook** | [`.githooks/pre-push`](.githooks/pre-push) | בכל דחיפה ל-main, גם מהטרמינל | מבמפ, מקמט `chore(version)`, ו**עוצר את הדחיפה** |

שתי השכבות מדלגות כשמופיע `SKIP_VERSION_BUMP=1` בפקודה.

#### git pre-push hook

[`.githooks/pre-push`](.githooks/pre-push) רץ **בכל דחיפה ל-main**, גם כשדוחפים ידנית
מהטרמינל (לא רק כש-Claude דוחף). אם `src/version.ts` לא השתנה בקומיטים שנדחפים -
ההוק מבמפ, יוצר קומיט `chore(version): vX.Y.Z`, ו**עוצר את הדחיפה**:

```
  SKY-KING: הגרסה לא עודכנה בקומיטים שנדחפים ל-main.
  ✅ נוצר קומיט: chore(version): v1.0.5
  ⛔ הדחיפה בוטלה בכוונה - הרץ אותה שוב כדי לכלול את קומיט הגרסה
```

**הדחיפה השנייה עוברת חלק.** הביטול הוא הכרחי: git מקבע את ה-SHA לדחיפה *לפני*
שההוק רץ, ולכן קומיט שנוצר בתוך ההוק לא יכול להצטרף לאותה דחיפה.

| | |
|---|---|
| **הפעלה בקלון חדש** | `git config core.hooksPath .githooks` (חד-פעמי - ההוק מנוהל בגיט, לא ב-`.git/hooks`) |
| **עקיפה חד-פעמית** | `SKIP_VERSION_BUMP=1 git push origin feature/i18n-bilingual:main` |
| **טריגר** | רק `refs/heads/main` ביעד. דחיפה לענף פיצ'ר לא מפעילה כלום |

> `.gitattributes` מקבע `.githooks/** text eol=lf` - עם `core.autocrlf=true` ב-Windows
> סקריפט עם CRLF פשוט לא ירוץ.

### הזרימה המלאה (ידנית, בלי להסתמך על ההוקים)

1. `git status --short` - לוודא שהעץ שלך (ראה §worktree נפרד לכל סוכן)
2. `npm run version:bump`
3. `git add <הקבצים שלי> src/version.ts` + commit
4. `git push origin feature/i18n-bilingual:main`
5. אימות: `git log origin/main -1 --oneline` + `grep APP_VERSION src/version.ts`

> ❌ **לא לקודד מספר גרסה קשיח ב-JSX.** כל מקום שמציג גרסה צורך
> `import { APP_VERSION, APP_VERSION_DATE } from '.../version'` - אחרת המסכים
> מציגים גרסאות שונות (מסך הכניסה הראה 1.0.3 בזמן שחלון העזרה הראה 1.0.0).

---

## QA Checklist

- [ ] רכיב חדש - האם כבר קיים רכיב זהה?
- [ ] שינוי ברכיב משותף - האם חל על כל המסכים?
- [ ] UX - האם מהיר יותר מהסדק?
- [ ] כל UI בעברית
- [ ] Event Log לכל שינוי סטטוס

---

## כלל עבודה: VERIFY + TDD (לא להמציא - לחקור online)

לכל משימה, לפי best practices של Anthropic/הקהילה:
0. **עץ משלי** - `/worktree` בתחילת כל סוכן/סשן, לפני השורה הראשונה של קוד.
1. **תכנון לפני קוד** - `/pm` + `/arch` (non-negotiable).
2. **TDD** - בדיקות קודם → לוודא שנכשלות → commit → לממש עד ירוק בלי לשנות בדיקות.
3. **VERIFY לפי best practice עדכני** - לחקור online את שיטת האימות הנכונה לסוג השינוי (DB/API/UI/ביצועים), לא רק tsc/build. תמיד: tsc + tests + build; לפי הסוג: smoke test / נתונים אמיתיים / `/verify` / עומס.
4. **פיצ'ר חדש** → `/feature` (**worktree משלו לכל סוכן** - ראה §worktree נפרד לכל סוכן).
5. **קימוט ודחיפה** → `/ship` (`npm run version:bump` ראשון, תמיד).
