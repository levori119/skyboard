# נקודות הצטרפות (STAR) - אפיון ותכנית טכנית

> תוצר `/pm` + `/arch` לפיצ'ר **נקודות הצטרפות**. מקור: מסמך האפיון של הצוות (05.08.2026).
> שם השירות בשפה המשותפת: **נקודות הצטרפות** (מקביל ל**נקודות העברה**, תצוגה שונה).

---

## 1. מה זה

**נקודת הצטרפות** היא נקודת כניסה לשדה (STAR) שבה מטוסים מצטרפים לתנועת השדה.
היא **דומה לנקודת העברה** - מקבלת פ"ממים מעמדה אחרת - אבל **התצוגה שונה**:
במקום פאנל מוסר/מקבל, הנקודה נפרסת ל**טבלת בלוקי גבהים**, ופ"מ יושב בבלוק
לפי הגובה שקיבל.

**רלוונטית רק לעמדה מסוג "שדה"** (`preset_type = 'ground'`).

---

## 2. שאלת הסדק

על הסדק הפיזי, הפקח מצייר בצ'ינו טבלת גבהים ליד ה-STAR וכותב את הפ"מ בשורת הגובה.
כדי לשנות גובה - מוחק בפלנלית וכותב מחדש; כדי לראות מי בקונפליקט - סורק בעיניים.

ב-SKY-KING: **גרירה אחת** משנה גובה, הקונפליקט **מסמן את עצמו באדום**, והמטוסים
נפרסים בלחיצה על `+` במקום לכתוב אותם בצד. **מהיר יותר מהסדק.** ✅

---

## 3. סטוריות משתמש

### סטוריה 1 - הגדרה (מנהל)
> כ**מנהל**, אני רוצה **להגדיר נקודת הצטרפות על השדה** (שם STAR, טווח גבהים,
> הפרשי גבהים, חיבור לנקודת מעבר, ומיקום בדקירה על המפה), כדי ש**כל עמדות השדה
> יראו את אותה נקודה בלי הגדרה חוזרת בכל משמרת**.

### סטוריה 2 - שיבוץ לגובה (פקח)
> כ**פקח**, אני רוצה **לקבל פ"מ לנקודת ההצטרפות ולשבץ אותו לבלוק גובה**
> (בקבלה עם טופס גובה, בגרירה לבלוק, או בגרירה מרשימת הפ"ממים שבצד),
> כדי ש**התמונה האנכית של ההצטרפות תהיה גלויה ברגע אחד**.

### סטוריה 3 - ניהול מטוסים והקפה (פקח)
> כ**פקח**, אני רוצה **לפרוס פ"מ למטוסים בודדים, לשייך לכל מטוס מסלול נחיתה,
> ולשים אותו בהקפה**, כדי ש**אדע מי נכנס לאיזה מסלול ומי כבר בהקפה בלי לזכור בעל פה**.

---

## 4. Acceptance Criteria

### הגדרה (ניהול - יישות שדה תעופה)
1. בטופס השדה יש מקטע **"נקודות הצטרפות"** עם רשימה + טופס הוספה/עריכה/מחיקה.
2. לכל נקודה: **שם STAR** (חובה), **גובה מ**, **גובה עד**, **נקודת מעבר מקושרת**
   (סקטור מתוך `sectors`, אופציונלי + תת-נקודה), **צבע**.
3. **הפרשי גבהים**: רשימת טווחים, לכל טווח `מ`-`עד`-`הפרש` (למשל 040-070 כל 1000,
   070-100 כל 500). טווח שלא כוסה נופל להפרש ברירת המחדל של הנקודה.
4. הטווחים **לא חופפים**; חפיפה נחסמת בשמירה עם הודעה.
5. **דקירה במפה**: כפתור "📍 מקם" פותח את מפת השדה; לחיצה על המפה קובעת
   `x_pct`/`y_pct`. נקודה בלי מיקום לא מוצגת על המפה אבל כן ברשימה.
6. **דריסת עמדה**: עמדת שדה יכולה לדרוס מיקום ומצב תצוגה של נקודה בודדת;
   "⟲ החזר לברירת מחדל" מוחק את הדריסה.

### עמדה - הנקודה על המפה
7. הנקודה מוצגת על מפת השדה במיקום שהוגדר, עם שם ה-STAR.
8. **ניתן להזיז אותה** בגרירה. ההזזה **זמנית** (state בלבד) - כמו נקודות העברה -
   וכפתור ⟲ מחזיר למיקום הקבוע.
9. **בעלייה** (לחיצה/מצב `full`) הנקודה נפרסת ל**טבלת בלוקי גבהים**.

### עמדה - טבלת הבלוקים
10. כותרת הטבלה: **שם הנקודה/STAR + טווח הגבהים** (למשל `4-10 - STAR X`).
11. שורות הטבלה = בלוקי גובה מ-`alt_max` ל-`alt_min` (מלמעלה למטה), לפי ההפרשים
    שהוגדרו. תצוגת הגובה **תלת-ספרתית במאות רגל** (`040` = 4000 רגל).
12. **שורה עליונה** = פ"ממים שמועברים אליי מ**נקודת המעבר המקושרת**
    (`strip_transfers` בסטטוס `pending`/`acknowledged` לאותו סקטור).
13. על פ"מ בשורה העליונה:
    - **קבל** - אותו `accept` של מנגנון ההעברות, ואז נפתח **טופס גובה** שמציע
      **רק גבהים מטווח הנקודה**.
    - **גרירה לבלוק** - קבלה + הגובה נקבע לבלוק שאליו נגרר, בלי טופס.
14. **גרירה מרשימת הפ"ממים שבצד** אל בלוק גובה משבצת פ"מ שכבר שלי לאותו בלוק.
15. **קונפליקט**: שני פ"ממים באותו בלוק → הבלוק **אדום** + התראה. כפתור
    **"מתואם"** מסיר את האדום ומשאיר סימון תיאום; ההסרה נרשמת ב-`activity_log`.
16. פ"מ בבלוק מוצג **מצומצם**: `או"ק / טייסת (מספר מטוסים)` + **הערת תקלה** אם יש.

### עמדה - פריסת מטוסים
17. על פ"מ בטבלה יש **`+`**; לחיצה פורסת שורה לכל מטוס בפ"מ (רביעייה → 4 שורות).
18. בכל שורת מטוס: **או"ק ומספר במבנה**, **דת"ק**, ו**בחירת מסלול** -
    תפריט שמציג **רק מסלולים פעילים לנחיתות** (`runway_end_use.in_landing`).
19. **גרירת מטוס להקפה** על המפה בוחרת אוטומטית את המסלול של אותה הקפה.
20. מטוס שנגרר להקפה מסומן **במרכז צלע "עם הרוח"** ב**מסגרת מקווקוות** (זמני).
21. **"שים בהקפה"** → המסגרת **קבועה**, והמטוס **נמחק מטבלת ההצטרפות**.
22. כאשר **כל** מטוסי הפ"מ בהקפה - **כל הפ"מ נעלם** מנקודת ההצטרפות.
23. על מטוס ניתן לדווח **"ירוקים" / "אישור לנחות" / "נחיתה"** - עובר ל**סטטוס המטוס**
    (`strip_aircraft.flight_status`) ונראה גם מחוץ לנקודת ההצטרפות.

### רוחבי
24. כל הטקסטים דרך `t('ns.key')` (i18next) - אין מחרוזת מקודדת.
25. הרכיב עובר `/ui-adapt`: תמות אור/שחור/כחול + סקייל 15.6"/16"/18"/24".
26. `TIMESTAMPTZ` בלבד; כל טבלה חדשה מסווגת ב-`server/db/env-tables.js`.

---

## 5. השפעה על עמדות

| עמדה | מושפעת? | הערה |
|---|---|---|
| בקר טיסה (CTRL) | **לא** | הנקודה מוגדרת על שדה תעופה ומוצגת רק ב-`preset_type='ground'` |
| מגדל / שדה (TWR) | **כן** | פאנל חדש על מפת השדה + טבלת בלוקים |
| ניהול קרקעי (`ground_mgmt`) | לא | אין פ"ממים בעמדה |
| ניהול (Admin) | **כן** | מקטע חדש ביישות שדה תעופה |

---

## 6. רכיבים משותפים (DRY)

| קיים | איך משתמשים | לא לשכפל |
|---|---|---|
| **מנגנון ההעברות** (`/api/transfers/:id/accept`) | "קבל" בשורה העליונה = אותו endpoint | ❌ לא ליצור accept מקביל |
| `DraggableNeighborPanel` | **לא** מורחב - התצוגה שונה מהותית (טבלת בלוקים ולא פאנל מוסר/מקבל) | הכרטיסים עצמם (`TransferCards`) כן משותפים לשורה העליונה |
| `TrafficPatternLayer` | שכבת ההקפות הקיימת - מוסיפים לה **שכבת מטוסים** | ❌ לא לצייר הקפה חדשה |
| `runway_end_use` | מקור המסלולים הפעילים לנחיתות | ❌ לא לשמור רשימת מסלולים משלנו |
| `strip_aircraft` | מקור המטוסים הבודדים (דת"ק, כיפה) | ❌ לא ליצור טבלת מטוסים חדשה |
| `map_transfer_points` | **תבנית** למודל ברירת-מחדל/דריסה | טבלה נפרדת - הישות שונה |
| `airfield_elements` | **תבנית** לדקירה `x_pct`/`y_pct` על מפת השדה | |

> **חריג מתועד:** נקודת הצטרפות **אינה** רכיב זהה לנקודת העברה. הן חולקות את
> **מנגנון ההעברות** ואת **כרטיס ההעברה**, אך התצוגה (טבלת בלוקים מול פאנל
> מוסר/מקבל) שונה - כפי שנאמר במפורש באפיון: "דומה לנקודות העברה אך תצוגה שונה".

---

## 7. תכנית טכנית

### 7.1 DB - טבלאות חדשות

**קונפיג (`public` בלבד, שייך לשדה):**

| טבלה | תפקיד |
|---|---|
| `airfield_joining_points` | הנקודה: שם, טווח גבהים, סקטור מקושר, מיקום, צבע |
| `joining_point_alt_steps` | הפרשי גבהים לפי טווח |
| `joining_point_preset_overrides` | דריסת עמדה: מיקום + מצב תצוגה |

**תפעולי (מבודד פר-סביבה):**

| טבלה | תפקיד |
|---|---|
| `joining_point_strips` | איזה פ"מ יושב בנקודה + דגל תיאום קונפליקט |
| `joining_point_aircraft` | מצב מטוס בודד: מסלול, הקפה, סטטוס נחיתה |

**עמודה נוספת:** `strip_aircraft.flight_status VARCHAR(20) DEFAULT 'none'`
(`none` / `greens` / `cleared_to_land` / `landed`) - "זה עובר לסטטוס מטוס".

> **גובה הפ"מ אינו משוכפל**: השיבוץ לבלוק כותב ל-`strips.alt` (הגובה קיים ומוצג
> בכל המערכת). `joining_point_strips` מחזיק **שיוך** בלבד. כך אין שני מקורות אמת.

### 7.2 Backend - `server/routes/joiningPoints.js` (מודול חדש)

| Method | Path | תפקיד |
|---|---|---|
| GET | `/api/joining-points?airfield_id=&preset_id=` | הנקודות **האפקטיביות** (ברירת מחדל + דריסות), כולל `steps` |
| POST | `/api/joining-points` | יצירה |
| PUT | `/api/joining-points/:id` | עדכון (כולל `steps` בטרנזקציה) |
| DELETE | `/api/joining-points/:id` | מחיקה |
| PUT | `/api/joining-points/:id/override` | דריסת עמדה (upsert) |
| DELETE | `/api/joining-points/:id/override/:presetId` | ביטול דריסה |
| GET | `/api/joining-point-state?airfield_id=` | מצב חי: פ"ממים + מטוסים |
| POST | `/api/joining-points/:id/assign` | שיבוץ פ"מ לבלוק (`strip_id`, `alt`) |
| DELETE | `/api/joining-points/:id/strips/:stripId` | הסרת פ"מ מהנקודה |
| PUT | `/api/joining-points/:id/strips/:stripId/coordinate` | אישור קונפליקט מתואם |
| PUT | `/api/joining-point-aircraft/:stripId/:idx` | מסלול / הקפה / סטטוס למטוס |

### 7.3 Frontend

| קובץ | שינוי |
|---|---|
| `src/components/ground/JoiningPointPanel.tsx` | **חדש** - טבלת הבלוקים, שורה עליונה, פריסת מטוסים |
| `src/components/ground/JoiningPointPin.tsx` | **חדש** - הסמן על המפה (מכווץ/פרוס) |
| `src/components/views/GroundView.tsx` | props חדשים + רינדור הסמנים/הפאנל בשכבת המפה |
| `src/components/map/TrafficPatternLayer.tsx` | שכבת מטוסים על צלע "עם הרוח" |
| `src/components/admin/JoiningPointsSection.tsx` | **חדש** - מקטע ניהול ביישות שדה |
| `src/components/admin/ManagementPage.tsx` | הזרקת המקטע לטופס השדה |
| `src/App.tsx` | state + fetch + handlers, מועברים כ-props ל-GroundView |
| `src/utils/joiningPoints.ts` | **לוגיקה טהורה** - בניית הבלוקים, זיהוי קונפליקט (נבדק ב-vitest) |
| `src/types/joiningPoints.ts` | טיפוסים |
| `src/i18n/registry/joining.json` | מחרוזות |

### 7.4 סיכונים

| סיכון | מניעה |
|---|---|
| נגיעה במנגנון ההעברות (ליבה קריטית) | **לא משנים** את ה-accept הקיים; קוראים לו כמו שהוא, ומוסיפים שיבוץ **אחריו** |
| טבלה חדשה לא מסווגת → זליגת תרגול↔אמת | סיווג ב-`env-tables.js` באותו commit; `checkTableClassification` מפיל boot |
| `SectorDashboard.tsx` הוא מונוליט 17.7k שורות | הפאנל בקובץ **נפרד**; ב-GroundView רק רינדור ו-props |
| חפיפת טווחי הפרשים → בלוקים כפולים | ולידציה בשרת + `buildBlocks` טהור עם בדיקות |
| גרירה על Cintiq | `touchAction:'none'` + `setPointerCapture` בכל גרירה (זיכרון חומרה) |

### 7.5 Event Log

| אירוע | מתי |
|---|---|
| `joining_point_assign` | פ"מ שובץ לבלוק גובה |
| `joining_point_conflict` | שני פ"ממים באותו בלוק (severity: `critical`) |
| `joining_point_coordinated` | קונפליקט אושר כמתואם |
| `joining_point_pattern` | מטוס נכנס להקפה |
| `aircraft_flight_status` | ירוקים / אישור לנחות / נחיתה |

### 7.6 אומדן

**מורכב (~יום+)** - 5 טבלאות, 11 endpoints, 3 רכיבי UI חדשים ושכבת מפה.

---

## 8. סדר ביצוע

1. DB + סיווג סביבות + `data-model.md`
2. `src/utils/joiningPoints.ts` + בדיקות (TDD - נכשלות קודם)
3. `server/routes/joiningPoints.js` + חיבור ל-`server.js`
4. מקטע ניהול ביישות שדה + דקירה במפה
5. `JoiningPointPanel` + `JoiningPointPin` + חיבור ל-GroundView
6. שכבת מטוסים על ההקפה + סטטוסי מטוס
7. `/ui-adapt` + `/atc-ux` + `/qa` + `/sync-docs`
