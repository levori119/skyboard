# ניהול אמצעים למשימת הגנ"ש - אפיון

> **הגנ"ש** (הגנת שמי המדינה) = משימה שבה מערכות **גילוי** (מכ"מים) ומערכות **אש**
> (מטוסים וטק"א) מכסות מרחב. SKY-KING מנהל את ה**אמצעים**: מה יש לנו, איפה הוא
> פרוס, לאן הוא רואה ויורה, ומה **חסר** מול אופי האיום שהוגדר למרחב בזמן אמת.
>
> זהו כלי **החלטה והצפת פערים** - לא כלי שליטה באמצעים. המערכת לא מפעילה מכ"ם
> ולא משגרת טיל: היא מציירת את הכיסוי בפועל, מסמנת חורים, וממליצה. ההכרעה
> נשארת אצל הבקר, בדיוק כמו ב**אזור מוגבל** וב**הלאמת אזור זמני**.

**מקור:** "הגדרת מנגנון ניהול אמצעים למשימת הגנת שמי המדינה" (PDF שהתקבל 2026-08-31).
**סטטוס:** **שלב א מומש** (קטלוג - §2, ראה REFACTOR_LOG #046). שלבים ב-ו לא נכתבו.

---

## 0. למה זה לא אזור מפה ולא הלאמת אזור

| | אזור מפה (`map_zones`) | הלאמת אזור זמני | **הגנ"ש** |
|---|---|---|---|
| **הישות המרכזית** | פוליגון אזור טיסה | פוליגון שנולד בציור | **אמצעי** (מכ"ם / סוללה / מטוס) בנ"צ |
| **מה מצויר** | פוליגון | פוליגון | **טווח + גזרה** (טבעת/יתד) המחושבים מנתוני האמצעי |
| **מקור הצורה** | הבקר צייר | הבקר צייר | **חישוב** מ-PTL, מפתח זווית וטווח |
| **מה השאלה** | "מי יושב באזור" | "מי תפס את המרחב" | **"מה לא מכוסה"** |
| **מחזור חיים** | קונפיגורציה קבועה | אירוע קצוב בזמן | קטלוג קבוע + **פריסה** שמשתנה במשמרת |

**המשותף:** מקור האמת הגיאומטרי הוא **נ"צ** (`lat/lon`), והצורה מוקרנת לכל מפה
דרך עוגני המפה (`geoToImagePct` ב-[`src/utils/geo.ts`](src/utils/geo.ts)) - אותו
כלל של הלאמת אזור. מכאן ש**תצוגת הגנ"ש דורשת מפה מעוגנת**, וכל מפה בעמדה מקרינה
את אותם אמצעים במקום הנכון שלה.

---

## 1. שלוש השכבות

הדרישה מפרידה בין שלוש שכבות, וההפרדה הזו היא מבנה הפיצ'ר כולו:

| # | שכבה | איפה מוגדר | מי נוגע | מה יש בה |
|---|---|---|---|---|
| 1 | **קטלוג** - "מהי מערכת נשק/גילוי" | **מסך ניהול טכני** | טכני/אדמין | דגם: טווח, סוג טיל, מפתחות זווית, **טבלת יעילות** מול סוגי איום |
| 2 | **פריסה** - "מה יש לנו בשטח עכשיו" | **מסך ניהול מבצעי** (מוד הגנ"ש) | מבצעי | נ"צ, שם אמצעי, איזו מערכת, סטטוס, PTL, טווח בפועל |
| 3 | **תצוגה והכרעה** - "מה זה אומר לי" | **בעמדה** (מפה + דסק) | בקר/פקח | גזרות על המפה, אזור אירוע, פערים והמלצות |

> **למה זה חשוב:** דגם מכ"ם אחד פרוס בחמישה מקומות. הטווח ומפתח הזווית הם של
> **הדגם**, ה-PTL והסטטוס הם של **הפריסה**, ו"מה חסר" נגזר משניהם מול האירוע.
> ערבוב השכבות היה מחייב הקלדה חוזרת של אותם נתונים בכל פריסה - ומבטיח שיסתרו.

---

## 2. השכבה הטכנית - הקטלוג

### 2.1 טבלת `ad_threat_types` - סוגי איום

**החלטה:** סוג האיום הוא **ישות משלו** ולא מחרוזת חופשית, כי אותו ערך מופיע
בשלושה מקומות (יעילות אש, יעילות גילוי, טופס אזור אירוע). רשימה חופשית שם
הייתה מבטיחה ש"כטב"מ" ו"כטב״מ" לא ייפגשו לעולם, והחישוב היה מחזיר אפס בשקט.

| עמודה | סוג | תיאור |
|---|---|---|
| `id` | SERIAL PK | מזהה |
| `name` | VARCHAR(100) | שם האיום. ערכי הפתיחה מהאפיון: מהיר · איטי · כטב"מ · מטוס · מסוק · לא ידוע |
| `sort_order` | INT | סדר תצוגה |
| `enabled` | BOOLEAN | האם מוצע בטפסים |

### 2.2 טבלת `ad_weapon_systems` - מערכות אש (קטלוג)

| עמודה | סוג | תיאור |
|---|---|---|
| `id` | SERIAL PK | מזהה |
| `name` | VARCHAR(120) | שם המערכת |
| `kind` | VARCHAR(20) | `air` (מטוס) · `ground` (טק"א) |
| `range_nm` | NUMERIC | טווח (ימ"י) - **ברירת מחדל** לפריסה |
| `missile_type` | VARCHAR(120) | סוג טיל |
| `guidance` | VARCHAR(20) | `radar` · `ir` (מכ"ם / חום) |
| `sector_from_deg`, `sector_to_deg` | NUMERIC | גזרת אש יחסית ל-PTL, או NULL = 360 |
| `alt_min`, `alt_max` | INTEGER | **טווח הגובה שהמערכת מסוגלת לו** - רצפה ותקרה ב**רום טיסה** (מאות רגל). ראה §2.5 |
| `enabled` | BOOLEAN | |

> **`guidance` (מכ"ם/חום) הוא של הטיל ולא של המערכת** באפיון המקורי ("טילים
> מסוימים לטווח ויכולות - מכ"ם / חום"). בשלב א' מערכת = תצורת חימוש אחת, ומטוס
> עם שני סוגי טילים מיוצג בשתי שורות קטלוג. ראה §10.2 שאלה 1.

### 2.3 טבלת `ad_sensor_systems` - מערכות גילוי (קטלוג)

| עמודה | סוג | תיאור |
|---|---|---|
| `id` | SERIAL PK | מזהה |
| `name` | VARCHAR(120) | שם המערכת |
| `kind` | VARCHAR(20) | `air` · `ground` |
| `range_nm` | NUMERIC | טווח (ימ"י) - ברירת מחדל לפריסה |
| `detect_from_deg`, `detect_to_deg` | NUMERIC | **מפתח זווית גילוי** יחסית ל-PTL. שניהם NULL = **360** |
| `track_from_deg`, `track_to_deg` | NUMERIC | **מפתח זווית עקיבה** יחסית ל-PTL. שניהם NULL = 360 |
| `alt_min`, `alt_max` | INTEGER | **טווח הגובה שהמערכת רואה בו** - רצפה ותקרה ב**רום טיסה** (מאות רגל). ראה §2.5 |
| `enabled` | BOOLEAN | |

> **המפתח נשמר יחסית ל-PTL, לא כאזימוט מוחלט.** האפיון מבקש בפריסה **חישוב** של
> תחילת/סיום זווית גילוי ועקיבה - וחישוב מניח שהקלט הוא מפתח (רוחב) והפלט הוא
> אזימוט. מכ"ם שמסתובב (`360`) לא מקבל PTL כלל.

### 2.4 טבלאות היעילות

שתי טבלאות תואמות, אחת לכל צד. השורה היא (מערכת × איום):

`ad_weapon_effectiveness` | `ad_sensor_effectiveness`

| עמודה | סוג | תיאור |
|---|---|---|
| `id` | SERIAL PK | |
| `system_id` | INT → הקטלוג | CASCADE |
| `threat_type_id` | INT → `ad_threat_types` | CASCADE |
| `quality_pct` | SMALLINT | **איכות עמידה במשימה - באחוזים, 0-100** (הכרעת הצוות 2026-09-01) |
| `note` | TEXT | הערה חופשית |

**סולם האיכות - אחוזים 0-100:**

| טווח | משמעות | שימוש בחישוב |
|---|---|---|
| `0` | **לא מתמודד** | האמצעי אינו נספר ככיסוי לאיום הזה |
| `1-49` | **התמודדות חלקית** | נספר, אך **מסומן** ומחייב אמצעי משלים |
| `50-100` | **מתמודד** | נספר ככיסוי מלא |

- הסף בין "חלקי" ל"מתמודד" הוא **קבוע בעל שם** - `AD_FULL_COVER_PCT = 50` -
  ולא מספר קסם פזור בקוד. שינוי מדיניות = שורה אחת. ראה §10.2 שאלה 3.
- הטופס מקבל **מספר שלם בלבד** ב-0-100; ערך מחוץ לטווח נחסם עם נימוק.
- ההצגה במסך הפריסה ובטבלת הפערים היא **באחוזים** (`72%`), לא מילולית - זה מה
  שהוזן ואין סיבה לתרגם אותו למילה שמאבדת מידע.

> **ברירת המחדל היא `0`.** צמד (מערכת, איום) שלא הוזן נחשב "לא מתמודד" ולא
> "לא ידוע": כיסוי שלא הוגדר במפורש אינו כיסוי. אותו היגיון של
> [`zoneRestriction.ts`](src/utils/zoneRestriction.ts) - ברירת המחדל הבטוחה היא
> זו שמתריעה, לא זו ששותקת.

### 2.5 טווח הגובה של אמצעי (הכרעת הצוות 2026-09-01)

לכל מערכת - אש וגילוי - **רצפה ותקרה**. בלי זה, אירוע שהוגדר לו גובה היה נספר
ככיסוי גם מול מכ"ם שאינו רואה נמוך וגם מול טק"א שתקרתו מתחת לאיום, וזה בדיוק
הפער השקט שהפיצ'ר נועד להציף.

| | |
|---|---|
| **היחידה** | **רום טיסה** - מאות רגל (`140` = FL140 = 14,000 רגל), כמו `zone_altitude_ranges` ו-`strips.alt`. התמונ"א היא החריג היחיד ועוברת ב-`blockAltFeet` |
| **איפה נקבע** | ב"מ ב**קטלוג** (תכונת הדגם), **ניתן לדריסה** בפריסה - בדיוק כמו `range_nm` |
| **ריק** | `alt_min` ריק = מרצפת השטח · `alt_max` ריק = ללא תקרה. **שניהם ריקים = כל הגבהים** |
| **תפקיד בחישוב** | תנאי 4 ב-§7.1: חייבת **חפיפה** בין טווח האירוע לטווח האמצעי |

> **חפיפה, לא הכלה.** מכ"ם שרואה 000-200 מכסה אירוע שהוגדר 150-300 - חלקית.
> ההכרעה "חלקית" נרשמת ומוצגת (`חפיפת גובה 150-200`), ולא נבלעת ל"מכסה"/"לא מכסה".

### 2.6 איפה זה יושב במסך

מסך הניהול ([`ManagementPage.tsx`](src/components/admin/ManagementPage.tsx))
מקבל קטגוריה חדשה בתפריט הצד: **"הגנ"ש"**, ובה שני טאבים -
`ad_weapon_systems` ו-`ad_sensor_systems` (ועורך סוגי האיום בתוכם). התבנית היא
של `managers.tsx`: רשימה + טופס, בלי רכיב חדש.

---

## 3. השכבה המבצעית - הפריסה (מוד הגנ"ש)

### 3.1 טבלת `ad_sensor_assets` - פריסת אמצעי גילוי

| עמודה | סוג | תיאור |
|---|---|---|
| `id` | SERIAL PK | |
| `name` | VARCHAR(120) | **שם האמצעי** (הפריסה, לא הדגם) |
| `system_id` | INT → `ad_sensor_systems` | RESTRICT - אין אמצעי בלי מערכת |
| `lat`, `lon` | NUMERIC | **נ"צ** |
| `status` | VARCHAR(20) | `operational` מבצעי · `deploying` בפריסה · `faulty` תקול · `maintenance` אחזקות |
| `is_360` | BOOLEAN | מכ"ם מסתובב |
| `ptl_deg` | NUMERIC | **PTL** = מרכז הגזרה. נדרש כאשר `is_360=false`, ריק אחרת |
| `range_nm` | NUMERIC | **טווח בפועל** - ב"מ מהקטלוג, ניתן לדריסה |
| `alt_min`, `alt_max` | INTEGER | **גובה בפועל** - ב"מ מהקטלוג, ניתן לדריסה (§2.5) |
| `notes` | TEXT | |
| `created_at`, `updated_at` | TIMESTAMPTZ | |

**ארבעת החישובים** שהאפיון מבקש **אינם עמודות** - הם נגזרים ב-
`src/utils/airDefense.ts` (ליבה טהורה, נבדקת ב-vitest) ומוצגים בטבלה לקריאה:

```
detectStart = norm360(ptl + detect_from)      detectEnd = norm360(ptl + detect_to)
trackStart  = norm360(ptl + track_from)       trackEnd  = norm360(ptl + track_to)
```

> **למה נגזר ולא נשמר:** שינוי PTL בשדה אחד חייב להזיז את ארבעתם. עמודות שמורות
> היו מתיישנות בשקט ברגע שמישהו מעדכן PTL דרך ה-API.

### 3.2 טבלת `ad_weapon_assets` - פריסת אמצעי אש

אותו מבנה, בלי מפתחות הגילוי:

| עמודה | סוג | תיאור |
|---|---|---|
| `id` | SERIAL PK | |
| `name` | VARCHAR(120) | שם אמצעי האש |
| `system_id` | INT → `ad_weapon_systems` | RESTRICT |
| `lat`, `lon` | NUMERIC | נ"צ |
| `status` | VARCHAR(20) | מבצעי / בפריסה / תקול / אחזקות |
| `is_360` | BOOLEAN | |
| `ptl_deg` | NUMERIC | מרכז גזרת האש (טק"א) |
| `range_nm` | NUMERIC | טווח בפועל - ב"מ מהקטלוג |
| `alt_min`, `alt_max` | INTEGER | **גובה בפועל** - ב"מ מהקטלוג, ניתן לדריסה (§2.5) |
| `missile_count` | INT | **מלאי טילים** - נדרש לשורה "כמה טילים יש" בפער האש (§7.3) |
| `notes` | TEXT | |
| `created_at`, `updated_at` | TIMESTAMPTZ | |

> **מטוס בפריסה סטטית?** אמצעי אש `kind='air'` שיושב כשורה עם נ"צ קבוע הוא
> **כוננות בשדה**, לא מטוס באוויר. מטוס באוויר מגיע מהפ"מ/תמונ"א ולא מהטבלה
> הזו - ראה §7.2.

### 3.3 סיווג הטבלאות (חובה - `env-tables.js`)

| טבלה | סיווג | נימוק |
|---|---|---|
| `ad_threat_types`, `ad_weapon_systems`, `ad_sensor_systems`, שתי טבלאות היעילות | **קונפיגורציה** (`public` בלבד) | קטלוג. אינו משתנה בתרגול |
| `ad_sensor_assets`, `ad_weapon_assets`, `ad_events` | **תפעולי** (פר-סביבה) | פריסה וסטטוס משתנים חי בעמדה; תרגול לא ישנה את הפריסה האמיתית |

> ❗ **בלי הסיווג הזה כל שרשרת העלייה נופלת** (`checkTableClassification`) ואיתה
> העבודות המחזוריות - CLAUDE.md, REFACTOR_LOG #045. זה הצעד הראשון במימוש, לא האחרון.

**רשימת החסימה של הביטול:** `ad_events` נכנסת ל-`UNDO_DENYLIST`
([`server/db/undoJournal.js`](server/db/undoJournal.js)) - CTRL+Z שמחזיר לחיים
אירוע הגנ"ש שנסגר, או מוחק אירוע פעיל, הוא שינוי תמונה מבצעית בלי שאיש ביקש.
טבלאות הפריסה **כן** מקבלות ביטול (טעות הקלדה ב-PTL היא בדיוק המקרה שהוא נועד לו).

---

## 4. הגיאומטריה - מה מצויר על המפה

### 4.1 היתד (הגזרה)

לכל אמצעי מחושב פוליגון בנ"צ, ומוקרן למפה דרך העוגנים:

| קלט | מקור |
|---|---|
| מרכז | `lat`, `lon` של האמצעי |
| רדיוס | `range_nm` (טווח בפועל) |
| אזימוט התחלה/סיום | החישוב מ-§3.1; `is_360` → טבעת מלאה |

- הקשת נבנית בדגימה כל **2°** (`arcPoints`), והנקודות מומרות ב-`geoToImagePct`.
  דגימה ולא קשת SVG: המפה מוקרנת דרך עוגנים ולעתים במרקטור, וקשת "אמיתית"
  בפיקסלים הייתה יוצאת מעוותת דווקא במפות התמונ"א.
- **הרחקה בנ"צ** נעשית ב-`destinationPoint(lat, lon, bearingDeg, nm)` - פונקציה
  חדשה ב-[`src/utils/eta.ts`](src/utils/eta.ts) לצד `haversineNm` הקיים
  (ההפכית שלה). לא לכתוב עוד חישוב מרחק במקום אחר.
- **גילוי מול עקיבה:** שתי גזרות לאותו מכ"ם. העקיבה מצוירת מלאה יותר (מילוי),
  הגילוי כמתאר. גזרה שמפתחה זהה - מצוירת פעם אחת.

### 4.2 עיצוב מותנה לפי סטטוס

| סטטוס | הצגה |
|---|---|
| **מבצעי** | מילוי מלא בצבע האמצעי |
| **בפריסה** | מתאר מקווקו, מילוי חלש |
| **תקול** | אפור + סימן ⚠ על האייקון |
| **אחזקות** | מתאר בלבד, שקוף |

**הצבעים ניתנים לשינוי** (דרישה מפורשת): צבע ברירת מחדל פר-מערכת בקטלוג, דריסה
פר-אמצעי בפריסה, ודריסה **פר-עמדה** בהגדרת התצוגה. בדיוק כמו שאזור מפה נושא
`color` משלו.

> **הגזרות אינן "עוד אזור".** הן שכבה נפרדת (`AirDefenseLayer`) מתחת לשכבת
> הפ"מים ומעל תמונת המפה, ואינן נכנסות ל-`map_zones` - אחרת שיוך פ"מ, בלוקי גובה,
> סגירת אזור ומנוע `zoneWatch` היו מנסים לעבוד עליהן.

---

## 5. אינטגרציה עם דסק המשימה

הדרישה: אמצעי הגנ"ש ייערכו ויוצגו כ**טבלה חכמה** בדסק, והטבלה תדע להשליך למפה.

### 5.1 טבלה חכמה קשורת-ישות

`MDTableConfig` ([`src/types/missionDesk.ts`](src/types/missionDesk.ts)) מקבל:

```ts
entity?: 'ad_sensor_assets' | 'ad_weapon_assets'   // מקור השורות; ריק = טבלה חופשית (הקיימת)
showOnMap?: boolean                                 // דגל "הצג במפה"
mapServiceId?: number | null                        // לאיזה סקשן מפה בדסק (יכולים להיות כמה)
```

| כלל | נימוק |
|---|---|
| `showOnMap` נדלק **רק** כשה-`entity` מספק נ"צ | דרישה מפורשת: "רק כאלו שיש להם נ"צ" |
| כשיש בדסק יותר ממפה אחת - בחירת מפה **חובה** | אחרת הטבלה נדלקת ולא קורה כלום, וזה כלל "לא לתת לפקד להידלק בלי שקורה משהו" |
| עמודות הטבלה נבחרות מ**שדות הישות** | שם, מערכת, סטטוס, PTL, טווח, ארבעת האזימוטים המחושבים |
| שורות הישות **אינן** נערכות כתאים חופשיים | מקור האמת הוא `ad_*_assets`; עריכה בטבלה כותבת ל-API של הישות |

### 5.2 בחירה בהגדרת העמדה

בהגדרת העמדה, לצד בחירת המפה לכל חלון (`MDPresetMapSettings`), נבחר **אילו
אמצעים** מוצגים ואיך: רשימת אמצעים (או "הכל"), האם להציג גזרות, האם להציג טווח,
ודריסת צבע. השדה נשמר ב-`workstation_presets.air_defense_config` (JSONB) לצד
`mission_desk_map_config` הקיים.

---

## 6. אזור אירוע הגנ"ש

### 6.1 היצירה

פעולה חדשה בתפריט היצירה על המפה (לצד "הלאמת אזור זמני"), במנגנון הציור הקיים
של [TEMP_ZONE_SEIZURE_SPEC.md](TEMP_ZONE_SEIZURE_SPEC.md) §2 - **אותו רכיב ציור,
לא שכפול**: קודקודים בעט/אצבע, מינימום 3, שמירה ב-`polygon_geo`.

### 6.2 טופס האירוע - `ad_events`

| שדה | עמודה | ב"מ / התנהגות |
|---|---|---|
| שם | `name` | **חובה** |
| סוג איום | `threat_type_id` | תפריט מ-`ad_threat_types` |
| כמות כוח אש נדרש | `required_fire` | מספר שלם ≥ 0 |
| כמות כוח גילוי נדרש | `required_detect` | מספר שלם ≥ 0 (הפענוח אושר - §10.1) |
| גובה אזור | `alt_min`, `alt_max` | **רום טיסה** (מאות רגל), כמו `zone_altitude_ranges`. ריק = כל הגבהים |
| הפוליגון | `polygon_geo` | נ"צ, כמו הלאמת אזור |
| מצב | `status` | `active` · `closed` |
| חותמות | `created_at`, `closed_at`, `created_by_preset_id` | TIMESTAMPTZ |

> **הגובה ברום טיסה ולא ברגל** - זו המוסכמה בכל המערכת (`strips.alt`,
> `zone_altitude_ranges`). התמונ"א היא החריג היחיד, ולכן כל השוואה מולה עוברת
> ב-`blockAltFeet` ([`src/airPicture/zoneWatch.ts`](src/airPicture/zoneWatch.ts)).

---

## 7. מנגנון החישוב - פערים והמלצות

הלב של הפיצ'ר. **ליבה טהורה** ב-`src/utils/airDefense.ts`, בלי DOM ובלי רשת,
נבדקת ב-vitest - זו הלוגיקה שנשברת בשקט ואי אפשר לראות אותה ב-tsc.

### 7.1 כיסוי אמצעי מול אירוע

אמצעי **מכסה** אירוע כאשר **כל** התנאים מתקיימים:

| # | תנאי | הערה |
|---|---|---|
| 1 | `status = 'operational'` | "בפריסה"/"תקול"/"אחזקות" אינם כיסוי, ומוצגים כ**פוטנציאל** נפרד |
| 2 | הפוליגון של האירוע **חותך** את היתד | חיתוך, לא הכלה: כיסוי חלקי הוא נתון בפני עצמו |
| 3 | `quality_pct ≥ 1` מול סוג האיום של האירוע | `0` = לא מתמודד. מתחת ל-`AD_FULL_COVER_PCT` (50) - נספר ומסומן "חלקי" |
| 4 | **חפיפת גובה** בין טווח האירוע לטווח האמצעי (§2.5) | חפיפה חלקית = כיסוי חלקי, עם הצגת מקטע החפיפה |

**מדד הכיסוי** = אחוז שטח האירוע שנמצא בתוך היתד (דגימת רשת, לא חישוב אנליטי):
`מלא` (≥95%) · `חלקי` · `אין`. אותה שפה של צביעת האזורים בהלאמת אזור
(כתום=חלקי, אדום=כולו).

> **שלושה "חלקי" נפרדים, ואסור למזג אותם למספר אחד:** חלקיות ב**שטח** (היתד חותך
> רק חלק מהאירוע), ב**גובה** (חפיפה ולא הכלה) וב**איכות** (`quality_pct` נמוך).
> הבקר צריך לדעת *מה* חלקי כדי לדעת מה לעשות - מכ"ם נוסף, גובה אחר, או מערכת
> אחרת. שקלול לציון יחיד הופך את זה ללא-שמיש.

### 7.2 פ"מ באוויר בתוך אירוע

כשפ"מ נמצא באוויר ובאזור שבו יש אירוע הגנ"ש פעיל - הוא **נכנס לחישוב כאמצעי אש**:

| מה | מאיפה |
|---|---|
| הימצאות באירוע | שיוך האזור (`strip_zone_assignments`), ובתמונ"א דלוקה - מיקום הרכיב האווירי |
| מה יש עליו | `strip_aircraft_armaments` (שם חימוש + כמות) - **קיים היום** |
| איזו מערכת אש זו | התאמת `armament_name` ל-`ad_weapon_systems.missile_type` |
| כמה זמן נשאר | **שלושה מקורות בסדר עדיפות** - §7.2.1 |

הפלט לטבלה: אילו אמצעים יש על הפ"מ, כמה, ולכמה זמן. ולצדו **שכבת הא"א הקרקעי**
עם מלאי הטילים (`missile_count`).

#### 7.2.1 הזמן - שלושה מקורות, בסדר עדיפות (הכרעת הצוות 2026-09-01)

`adTimeAvailable(strip, event, ctx)` מחזיר **דקות + מקור**, ולא רק מספר. המקור
מוצג לצד הערך, כי בקר שרואה "24 דק'" חייב לדעת אם זה נמדד, חושב או הוקלד:

| # | מקור | מתי נבחר | החישוב | תווית |
|---|---|---|---|---|
| 1 | **ידני** | קיים ערך ידני על הפ"מ | הערך כפי שהוזן | `ידני` |
| 2 | **תמונ"א** | התמונ"א דלוקה **והפ"מ משודך** לרכיב אווירי | לפי מיקומו ומהירותו בפועל של הרכיב האווירי | `תמונ"א` |
| 3 | **מתוכנן** | יש **שדה המראה** ו**שעת המראה** | `haversineNm(שדה ההמראה → מרכז אזור האירוע) / 450 קשר`, מנוכה מהזמן שחלף מההמראה | `מחושב` |
| - | **אין** | אף אחד מהשלושה | לא מוצג מספר. מוצג "**זמן לא ידוע**" | - |

- **הידני דורס את הכל** - גם תמונ"א חיה. זו הכרעה מפורשת: הבקר יודע דברים
  שהמערכת אינה יודעת, ותצוגה שמתעלמת ממנו שווה פחות מכלום. הערך הידני יושב על
  הפ"מ (`strips.ad_time_min` + `ad_time_set_at`) ולא על האירוע - הוא תכונה של
  המטוס, לא של המרחב.
- **המהירות היא קבוע בעל שם:** `AD_TRANSIT_SPEED_KT = 450`.
  ⚠️ **שים לב שזו מהירות אחרת** מ-`SPEED_FIGHTER_KT = 350` שב-
  [`src/utils/eta.ts`](src/utils/eta.ts), ששם היא **שיוט** לחישוב ETA להעברה.
  450 היא מהירות **גיחה לאירוע**, ולכן קבוע נפרד ולא שינוי של הקיים - שינוי
  הקבוע הקיים היה מזיז את כל חישובי ההעברות בשקט.
- **`ROUTE_FACTOR`** (1.1, מקדם עקיפה קיים) - ראה §10.2 שאלה 5: להחיל גם כאן, או
  קו אווירי ישר?
- הפונקציה **טהורה ונבדקת ב-vitest**, כולל שלוש נפילות-לאחור והמקרה הריק.
- **"אין" אינו אפס.** אפס דקות פירושו "נגמר הזמן"; היעדר נתון פירושו שלא יודעים.
  זה בדיוק סוג הכשל השקט שהפיצ'ר נועד למנוע.

> **מה בדיוק נמדד** - זמן **הגעה** לאירוע או זמן **שהייה** שנותר - טרם הוכרע
> (§10.2 שאלה 4). המבנה כאן זהה לשתי המשמעויות: אותם שלושה מקורות, אותו סדר
> עדיפות, אותה תווית. ההכרעה משנה את **נוסחת** מקור 3 בלבד, לא את המנגנון.

### 7.3 הצפת הפערים

| פער | מתי | ההצגה |
|---|---|---|
| **פער אש** | מספר אמצעי האש המכסים < `required_fire` | "חסר N אמצעי אש" |
| **פער גילוי** | מספר אמצעי הגילוי המכסים < `required_detect` | "חסר N אמצעי גילוי" |
| **חור גילוי** | שטח באירוע שאינו בגזרת אף מכ"ם מבצעי | השטח **מוצלל** על המפה |
| **התמודדות חלקית** | הכיסוי קיים אך כולו `quality = 1` | "כיסוי חלקי מול <איום>" |
| **אמצעי לא מבצעי בטווח** | אמצעי שהיה מכסה אלמלא הסטטוס | "תקול - היה סוגר את הפער" |

### 7.4 ההמלצות

**החלטה: מנוע כללים, לא ניקוד.** ההמלצה חייבת להיות מנומקת בפני הבקר, וניקוד
משוקלל אינו ניתן להסבר בזמן אמת:

| כלל | ההמלצה |
|---|---|
| קיים מכ"ם **גזרתי מבצעי** שסיבוב PTL שלו היה מכסה את החור, בלי לפתוח חור חדש | "שנה PTL של \<שם\> ל-\<אזימוט\>" - עם ה**מחיר**: מה יוצא מכיסוי |
| החור אינו נסגר בשום PTL של הפרוסים | "העלה עיטם (מטוס מכ"ם)" |
| פער אש והפער נסגר בטק"א שסטטוסו "בפריסה" | "האץ פריסת \<שם\>" |
| פער אש ואין קרקעי בטווח | "נדרש כיסוי אווירי" |

> ההמלצה היא **טקסט + הדגשת האמצעי במפה**, לא כפתור שמבצע. המערכת אינה מזיזה
> PTL בעצמה גם כשהיא בטוחה.

---

## 8. מטריצת מקרים

| # | מקרה | התנהגות נדרשת |
|---|---|---|
| 1 | מכ"ם `360` | טבעת מלאה; שדה PTL **מוסתר**, לא מושבת-ומבלבל |
| 2 | גזרה שחוצה את `0°` (למשל 350→020) | היתד נמשך דרך הצפון. `norm360` בכל השוואה; **נבדק ב-vitest** |
| 3 | `detect_to - detect_from = 0` | גזרה מנוונת - נחסם בטופס הקטלוג עם נימוק |
| 4 | טווח בפועל ריק | נופל ל-`range_nm` של הקטלוג; אם גם הוא ריק - האמצעי מצויר כאייקון בלבד, בלי יתד, ומסומן "ללא טווח" |
| 5 | מערכת נמחקת מהקטלוג ויש לה אמצעים פרוסים | **נחסם** (`RESTRICT`) עם הודעה כמה אמצעים תלויים בה |
| 6 | סוג איום נמחק ויש אירוע פעיל שמצביע עליו | נחסם באותו אופן |
| 7 | אמצעי בלי נ"צ | לא ניתן לשמור. הנ"צ הוא כל הישות |
| 8 | המפה בעמדה **אינה מעוגנת** | שכבת הגנ"ש לא מוצגת, וכפתור התצוגה **מנומק** ("המפה אינה מעוגנת") - לא נעלם ולא נדלק לריק |
| 9 | אירוע פעיל ואין אף אמצעי מבצעי בטווח | פער מלא: "אין כיסוי" באדום, לא "0 אמצעים" בשקט |
| 10 | סטטוס אמצעי משתנה בזמן שאירוע פעיל | הכיסוי מחושב מחדש והפער מתעדכן בפולינג הבא (5 שניות, כמו `map-zones/operational`) |
| 11 | שתי עמדות פותחות אירוע על אותו מרחב | שני אירועים נפרדים, שניהם מוצגים. אין מיזוג אוטומטי |
| 12 | פ"מ באירוע בלי חימושים רשומים | נספר כנוכח, **לא** נספר ככוח אש; מסומן "חימוש לא ידוע" |
| 13 | חימוש על הפ"מ שאינו מוכר בקטלוג | אותו טיפול, עם שם החימוש בהערה |
| 14 | אירוע ללא טווח גובה | חל על כל הגבהים (כמו סגירה גורפת באזור סגור) |
| 15 | אמצעי נמחק בזמן שהוא מוצג בטבלה חכמה בדסק | השורה נעלמת בפולינג; אין שגיאה בעמדה |
| 16 | תמונ"א כבויה | §7.2 נשען על שיוך האזור בלבד. הפער עדיין מחושב |
| 17 | סביבת תרגול | פריסה ואירועים מבודדים; הקטלוג משותף (§3.3) |
| 18 | עמדה בלי הרשאת הגנ"ש | לא רואה את השכבה, לא יוצרת אירוע. **קבלה**/צפייה באירוע של אחר אינה דורשת הרשאה |
| 19 | אמצעי שגובהו 000-200, אירוע ב-150-300 | **כיסוי חלקי בגובה** - מוצג `חפיפת גובה 150-200`, לא "מכסה" ולא "לא מכסה" |
| 20 | אמצעי שגובהו 000-200, אירוע ב-250-300 | **אינו מכסה כלל** - אין חפיפה, גם אם הוא בטווח ובגזרה |
| 21 | לאמצעי אין גובה בקטלוג ולא בפריסה | כל הגבהים. תנאי הגובה תמיד מתקיים - וזה **מוצג** ("ללא הגבלת גובה"), לא נבלע |
| 22 | `alt_min > alt_max` בטופס | נחסם עם נימוק. אותה בדיקה בקטלוג ובפריסה |
| 23 | `quality_pct` נמוך (למשל 20%) והוא **הכיסוי היחיד** | נספר, אך הפער מוצג: "כיסוי חלקי בלבד - 20% מול \<איום\>" |
| 24 | פ"מ עם ערך זמן **ידני** ותמונ"א דלוקה שסותרת | **הידני מנצח**, והתווית אומרת `ידני`. אין תיקון שקט לפי התמונ"א |
| 25 | פ"מ בלי שדה המראה ובלי תמונ"א | "זמן לא ידוע" - **לא** 0 ולא "-" (§7.2.1) |
| 26 | הפ"מ המריא לפני יותר זמן מזמן ההגעה המחושב | הזמן אינו יורד מתחת ל-0; מוצג "בטווח" ולא מספר שלילי |

---

## 9. מה נגזר מכללי הפרויקט (לא לשכוח במימוש)

| כלל | החלה כאן |
|---|---|
| **i18n** | כל טקסט דרך `t('airDefense.*')`. שמות מערכות ואיומים הם **נתונים** ולא עוברים תרגום |
| **מסגרת חלון** | טופס אזור אירוע וסרגל הציור = **כתום** (`windowFrame('edit')`); טבלת הפערים = **תורכיז** (`view`) |
| **גרירה** | גרירת אמצעי על המפה (אם תותר) ב-Pointer Events + `touchAction:'none'` + `setPointerCapture` + חלוקה ב-`--s` |
| **`/ui-adapt`** | שלוש תמות + ארבעה גדלי מסך. צבעי סטטוס האמצעי הם חריג צבע-סטטוס מותר |
| **`TIMESTAMPTZ`** | בכל החותמות. אין `TIMESTAMP` |
| **`env-tables.js`** | §3.3 - הצעד הראשון |
| **`UNDO_DENYLIST`** | `ad_events` |
| **מקפים** | מקף רגיל בלבד בכל טקסט מוצג |
| **DRY** | ציור הפוליגון = הרכיב של הלאמת אזור. מרחק/אזימוט = `eta.ts`. הקרנה = `geo.ts`. טבלה = `SmartTable` |

---

## 10. הכרעות ושאלות פתוחות

### 10.1 הוכרע (2026-09-01)

| השאלה | ההכרעה | איפה במסמך |
|---|---|---|
| סולם "איכות עמידה במשימה" | **אחוזים 0-100** | §2.4 |
| "כמות כחג יולי נדרש" | **"כמות כוח גילוי נדרש"** - הפענוח אושר | §6.2 |
| גובה לאמצעים | **להוסיף** - רצפה ותקרה לקטלוג, עם דריסה בפריסה | §2.5 |
| מקור הזמן של פ"מ | **שלושה מקורות בעדיפות:** ידני (דורס הכל) → תמונ"א → חישוב מבסיס ב-**450 קשר** | §7.2.1 |

### 10.2 עדיין פתוח

| # | השאלה | ההנחה שלי בינתיים |
|---|---|---|
| 1 | מטוס עם **שני סוגי טילים** (מכ"ם + חום) | שלב א': שתי שורות קטלוג. האם צריך "תצורה" - מערכת אחת עם כמה טילים? |
| 2 | **מי מגדיר את אופי האיום למרחב** - האפיון אומר "לכל מרחב בזמן אמת" | כרגע: האיום יושב על **אירוע ההגנ"ש**. אם הכוונה לאיום קבוע פר-**אזור מפה** - זו עמודה על `map_zone_operational_state` |
| 3 | **סף הכיסוי המלא** - מאיזה אחוז מערכת נחשבת "מתמודדת" ולא "חלקית"? | `AD_FULL_COVER_PCT = 50`. קבוע אחד, שינוי בשורה |
| 4 | הזמן ב-§7.2.1 מודד זמן **הגעה** לאירוע או זמן **שהייה** שנותר? | המנגנון זהה לשתיהן; ההכרעה משנה רק את נוסחת מקור 3 |
| 5 | האם להחיל `ROUTE_FACTOR = 1.1` (מקדם עקיפה קיים) על חישוב ה-450 קשר? | כרגע **קו אווירי ישר**, בלי מקדם |
| 6 | **טווח - ימ"י או ק"מ?** | ימ"י (המערכת כולה ב-NM, `haversineNm`) |
| 7 | האם פריסת האמצעים מגיעה בעתיד מ**מקור חיצוני** (GAPI/שו"ב) | כרגע הזנה ידנית. אם כן - `source` ו-`external_id` על טבלאות הפריסה כבר עכשיו |

---

## 11. הצעת שלבים

| שלב | תכולה | תוצר נראה |
|---|---|---|
| **א** ✅ | קטלוגים (§2) + סיווג ב-`env-tables` + טבלאות היעילות | מסך ניהול טכני עובד |
| **ב** | פריסה (§3) + ליבת הגיאומטריה `airDefense.ts` + vitest | טבלאות פריסה עם ארבעת האזימוטים המחושבים |
| **ג** | `AirDefenseLayer` על המפה (§4) + הגדרת עמדה (§5.2) | גזרות וטבעות על המפה, צבע וסטטוס |
| **ד** | טבלה חכמה קשורת-ישות בדסק (§5.1) | הצגה במפה מתוך הדסק |
| **ה** | אזור אירוע (§6) + חישוב כיסוי ופערים (§7.1, §7.3) | "חסר N אמצעי אש", חור גילוי מוצלל |
| **ו** | פ"מ באוויר בחישוב (§7.2) + המלצות (§7.4) | המלצת שינוי PTL / העלאת עיטם |

> שלבים א-ב אינם מציגים דבר לפקח, ולכן **שלב ג הוא נקודת ההדגמה הראשונה**.
