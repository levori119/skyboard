# תמונ"א על הדסק - תכנית טכנית

> סטטוס: **תכנון** (טרם מומש). מקור: אפיון "תמונ"א על הדסק", 2026-08-07.
> מסמכי ייחוס: [CLAUDE.md](CLAUDE.md) · [ARCHITECTURE.md](ARCHITECTURE.md) · [data-model.md](data-model.md) · [SERVICES.md](SERVICES.md)

---

## 0. שפה - מטוס מול פ"מ

> **הבחנה קובעת (הצוות, 2026-08-07):**
> **תמונה אווירית** היא **מטוס פיזי בשמיים**.
> **פ"מ** הוא ה**רישום** שלו - לפני שהמריא, אחרי שהמריא, אחרי שנחת.

| | תמונ"א | פ"מ |
|---|---|---|
| מה זה | המטוס עצמו, באוויר, עכשיו | הרשומה |
| מקור | חיישני המאגר החיצוני | נרשם בעמדה, יושב ב-DB של SKY-KING |
| מתי קיים | **רק בזמן שהוא באוויר** | לאורך כל חיי המשימה - לפני, תוך כדי, ואחרי |
| ניתן לעריכה | לא. מדידה | כן. זו העבודה של הבקר/פקח |
| נעלם כש- | הרדאר מפסיק לראות אותו | הפ"מ נסגר |

**מכאן שתי מסקנות שמעצבות את כל המסמך:**

1. **אלה שתי שכבות מידע נפרדות, לא שכפול.** לכן הן לא מתערבבות: שכבת המטוסים
   מצוירת בקנבס נפרד, לא נכתבת ל-DB, ולא נוגעת ב-state של הרשומות. מטוס
   שנעלם מהתמונ"א **לא** אומר שהפ"מ נסגר, ולהפך.
2. **השדה "שם פ"מ" הוא הגשר.** המאגר מחזיר בכל מטוס את שם הפ"מ שלו - כלומר את
   המפתח שמקשר את המטוס הפיזי לרשומה שלו בעמדה. זה מה שיאפשר בעתיד לקשר
   ביניהם (§12).

לאורך המסמך: **מטוס** = ישות בתמונ"א. **פ"מ** = רשומה ב-SKY-KING. בלי ערבוב.

---

## 1. מה נדרש

הזרמת **תמונה אווירית** ממאגר חיצוני אל העמדה, והצגתה מעל המפה המעוגנת.

| # | דרישה | מקור |
|---|---|---|
| 1 | תמונ"א נכנסת רק לעמדות שהוגדרו לכך | אפיון §2 |
| 2 | רלוונטי **רק** לעמדות עם **מפה מעוגנת** | אפיון §3 |
| 3 | תמונ"א + מפה לא מעוגנת → **התראה בניהול עמדה** | אפיון §4 |
| 4 | 8 שדות: שם פ"מ · גובה · נ"צ · מהירות · אחראיות · כיוון · סיווג · סוג מטוס | אפיון §5 |
| 5 | API בשם **AirTrafficAPI**, כיוון אחד בלבד (מאגר ← עמדה) | אפיון §6 |
| 6 | חיבור **ישיר לעמדה**, בלי מאגר SKY-KING | אפיון §7 |
| 7 | רענון כל **2 שניות** | אפיון §8 |
| 8 | מניפולציות על המידע בעמדה | אפיון §9 |
| 9 | עמדת סימולטור שמדמה את ה-FRONT של המאגר | אפיון §10 |
| 10 | בסימולטור: מפה, יצירת רכיב עם מסלול/מהירות/נק' שינוי גובה, הרצה מיידית או בשעה, מחיקה/עדכון/שכפול | אפיון §11 |
| 11 | בעמדה: הדלקה/כיבוי · הגדלה/הקטנה · אייקונים ברקע בבהירות שלא תפריע | אפיון §12 |

---

## 2. עקרון העל - התמונ"א לא נוגעת ב-SKY-KING

זו לא החלטה סגנונית אלא **הדרישה התפעולית שמגנה על העמדה**.

התמונ"א היא מידע **חיצוני, קריא-בלבד, בקצב גבוה**. מידע SKY-KING הוא **פנימי,
כתיב, ומקור אמת**. ערבוב שלהם הוא הסיכון האמיתי כאן: מטוס תמונ"א שנכנס ל-DB היה
מייצר 300 UPDATE כל 2 שניות, ומטוס תמונ"א שנכנס ל-state של `SectorDashboard`
(17,894 שורות) היה מרנדר מחדש את **כל** מסך הבקר כל 2 שניות.

לכן:

| | תמונ"א | מידע SKY-KING |
|---|---|---|
| **מקור** | מאגר חיצוני | PostgreSQL (Neon) |
| **DB** | **אפס נגיעה** - לא נכתב ולא נקרא | מקור האמת |
| **State ב-React** | store חיצוני (`useSyncExternalStore`) | state של הרכיב |
| **רינדור** | `<canvas>` יחיד | SVG / DOM |
| **הרשאה** | קריאה בלבד | קריאה/כתיבה |
| **שרידות נתק** | נעלם (עם חיווי "לא מעודכן מאז ...") | offline cache (IndexedDB) |
| **סמכות ויזואלית** | משני - עמום, מתחת לשכבות SKY-KING | ראשי |

> **הכלל:** אם התמונ"א נופלת לגמרי, אף פונקציה של SKY-KING לא נפגעת. אם SKY-KING
> נופל, התמונ"א חסרת משמעות ולכן נעלמת איתו. תלות חד-כיוונית, לא מעגלית.

---

## 3. תרשים הזרימה

```
┌──────────────────────┐                                ┌──────────────────────┐
│  מאגר תמונ"א (חי)     │                                │  ATSIM - סימולטור    │
│  מערכת חיצונית        │                                │  אפליקציה נפרדת      │
└──────────┬───────────┘                                │  (תבנית מיראז')      │
           │                                            └──────────┬───────────┘
           │            AirTrafficAPI (HTTPS, GET בלבד)            │
           └────────────────────────┬─────────────────────────────┘
                                    │  אותו חוזה בדיוק -
                                    │  העמדה לא יודעת מי מהם
                                    ▼
        ┌────────────────────────────────────────────────────┐
        │  שרת העמדה (electron/stationServer.cjs)             │
        │  proxy ל-/air-picture, מזריק את הטוקן               │
        │  → same-origin, הטוקן לא מגיע ל-renderer            │
        └────────────────────────┬───────────────────────────┘
                                 ▼  fetch כל 2 שניות (poller יחיד לעמדה)
        ┌────────────────────────────────────────────────────┐
        │  airPictureStore  -  מחוץ ל-React                   │
        │  snapshot אחרון + חותמת + מצב חיבור                 │
        └────────────────────────┬───────────────────────────┘
                    useSyncExternalStore (מנוי, לא state)
                                 ▼
        ┌────────────────────────────────────────────────────┐
        │  <AirPictureLayer/>  -  canvas יחיד, לולאת rAF      │
        │  dead reckoning + מטמון תוויות + סינון גיאוגרפי      │
        └────────────────────────────────────────────────────┘
                                 │
                    ✗ אין קשר ל-SectorDashboard state
                    ✗ אין קשר ל-PostgreSQL
```

**מה שהתרשים אומר:** בין הרשת לבין הפיקסל אין אף `setState` ברכיב-אב. זה כל
הסיפור של הביצועים.

---

## 4. חוזה AirTrafficAPI

אנחנו מגדירים את החוזה (האפיון: "צריך לייצר API מתאים שיקרא AirTrafficAPI"), ולכן
הוא נכתב פעם אחת ב-`shared/airTrafficApi.js` ומשמש **שלושה** צרכנים: העמדה,
הסימולטור, וה-validator. אותה תבנית של [`shared/sanitizeHtml.js`](shared/sanitizeHtml.js) -
מקור אמת אחד, בלי שכפול.

### `GET /air-picture`

```jsonc
{
  "t": 1786108800000,          // חותמת המאגר (ms epoch, UTC) - לא של העמדה
  "seq": 41827,                // מונה עולה. קפיצה לא-רציפה = היו דגימות שאבדו
  "tracks": [
    {
      "id": "TRK-9F2A",        // מזהה יציב לאורך זמן היותו באוויר. חובה.
      "cs":  "אפיק 21",         // שם הפ"מ שהמטוס הזה שייך לו - הגשר לרשומה (§0)
      "lat": 32.0853, "lon": 34.7818,
      "alt": 18500,            // רגל
      "spd": 420,              // קשר (ground speed)
      "hdg": 275,              // מעלות, 0-359
      "cls": "friend",         // עמית | hostile טורף | unknown בלמ"ז | civil אזרח
      "typ": "f16",            // מפתח מ-AircraftIconType הקיים
      "resp": "צפון"           // אחראיות
    }
  ]
}
```

**החלטות בחוזה, וההצדקה לכל אחת:**

| החלטה | למה |
|---|---|
| `id` נפרד מ-`cs` | שם הפ"מ משתנה תוך כדי טיסה, והמטוס הפיזי לא. בלי מזהה יציב ה-dead reckoning "מקפיץ" מטוס בכל שינוי שם, והמטמון מתאפס. זו בדיוק ההבחנה של §0: המטוס אחד, הרישום שלו יכול להשתנות |
| `seq` מונוטוני | הדרך היחידה לזהות **דגימות שאבדו** מול "המאגר לא זז". בלי זה אי אפשר להבחין בין רשת תקועה למאגר ריק |
| `t` של המאגר, לא של העמדה | שעון העמדה עלול לסטות; החיווי "מעודכן לפני X" חייב להימדד מול המקור |
| שדות מקוצרים | 300 מטוסים × 8 שדות. שמות מלאים מוסיפים ~35% למטען. בקצב 0.5Hz זה מצטבר |
| `cls` כקוד ולא כטקסט מתורגם | i18n בעמדה (`t('airPicture.cls.hostile')`), לא במאגר. אותה החלטה כמו `debriefs.severity` |
| `typ` ממופה ל-`AircraftIconType` | האייקונים כבר קיימים ב-[`src/utils/aircraft.ts`](src/utils/aircraft.ts). DRY - לא ציור אייקונים חדש |
| **אין** רשימת מטוסים שנעלמו | הסנאפשוט הוא התמונה **המלאה**. מטוס שאינו ברשימה כבר לא באוויר ולכן נעלם מהמסך. delta-sync מוסיף מצב שצריך לתקן בפועל, ותמונה מלאה של 45KB היא זולה |

### כותרות ותגובות

| | |
|---|---|
| `ETag` / `If-None-Match` | `304 Not Modified` בגוף ריק כשהתמונה לא זזה. חוסך parse וציור |
| `Accept-Encoding: gzip` | 45KB → ~10KB. חובה, לא אופציה |
| `429` / `503` | העמדה נכנסת ל-backoff מעריכי (2s → 4 → 8 → 16 → 30, תקרה 30) |
| `Cache-Control: no-store` | תמונה תפעולית, אסור proxy cache |

### `GET /air-picture/stream` (אופציונלי, מומלץ)

SSE שדוחף את אותו מבנה. אם המאגר יתמוך - העמדה עוברת אליו אוטומטית ונופלת חזרה
ל-polling של 2 שניות אחרי כישלון. חוסך ~1,800 בקשות לשעה לעמדה.

> **החלטה:** ה-polling של 2 שניות הוא **המסלול המחייב**; SSE הוא שדרוג אופציונלי.
> אנחנו לא שולטים במאגר האמיתי ואסור לתלות בו את הפיצ'ר.

---

## 5. הארכיטקטורה בעמדה - זה החלק שקובע ביצועים

חמש שכבות, כל אחת פותרת בעיית ביצועים מוגדרת. ללא אחת מהן העמדה נפגעת.

### 5.1 שכבת הרשת - `src/airPicture/poller.ts`

```
setTimeout שרשרתי  (לא setInterval)
```

| כלל | הבעיה שהוא מונע |
|---|---|
| `setTimeout` שרשרתי, לא `setInterval` | `setInterval` בקצב 2s מול רשת שמשיבה ב-3s מייצר **תור בקשות חופפות** שגדל בלי גבול. השרשרת מתזמנת רק אחרי שהתשובה חזרה |
| `AbortController` עם timeout של 1.8s | בקשה שנתקעה לא מחזיקה חיבור ולא מעכבת את הבאה |
| `document.hidden` → השהיה | עמדה ממוזערת/מוסתרת לא צורכת רשת ולא מציירת |
| backoff מעריכי בכישלון | נתק רשת לא הופך ל-1,800 בקשות כושלות בשעה |
| **poller אחד לעמדה**, מודול-סינגלטון | דו-מפה (`dual_map_mode`) לא מכפיל תעבורה. שתי המפות קוראות מאותו store |

**עומס נטו על העמדה:** 0.5 req/s. העמדה כבר ב-~2.8 req/s ([זיכרון LOADRUNNER](project-loadrunner)),
כלומר תוספת של ~18%. הבקשה **לא נוגעת ב-DB ולא בשרת SKY-KING**, ולכן העלות למערכת
המרכזית היא **אפס** - זו התוצאה הישירה של דרישה §7 באפיון.

### 5.2 שכבת המצב - `src/airPicture/store.ts`

```ts
// חתימה בלבד - לא מימוש
subscribe(fn): () => void
getSnapshot(): { t, seq, tracks: Track[], status: 'live'|'stale'|'down' }
```

**זו ההחלטה החשובה ביותר במסמך.** אילו הסנאפשוט היה יושב ב-`useState` בתוך
`SectorDashboard`, כל דגימה הייתה מרנדרת מחדש 17,894 שורות של רכיב - כולל
הסטריפים, לוחות ההעברה וטבלאות הבד"ח - **30 פעם בדקה**. במקום זה:

- ה-store הוא מודול רגיל מחוץ ל-React.
- רק `<AirPictureLayer/>` נרשם אליו, דרך `useSyncExternalStore` (התבנית כבר
  בשימוש ב-[`src/offline/useNetStatus.ts`](src/offline/useNetStatus.ts) - DRY).
- `SectorDashboard` **לא מקבל props של תמונ"א ולא מחזיק state שלה**. הוא רק
  מרנדר `<AirPictureLayer .../>` פעם אחת.

> **הכלל שאסור להפר:** אף מטוס מהתמונ"א לא נכנס ל-state של רכיב-אב. הוא נגמר בקנבס.

### 5.3 שכבת החישוב - `src/airPicture/track.ts`

פונקציות טהורות, נבדקות ב-vitest, בלי React ובלי DOM:

| פונקציה | תפקיד |
|---|---|
| `positionAt(track, tMs)` | **Dead reckoning** - מיקום משוער מהדגימה האחרונה לפי `hdg`+`spd` |
| `bboxFilter(tracks, anchor, margin)` | סינון גיאוגרפי - מי בכלל בתחומי המפה |
| `applyFilters(tracks, ui)` | המניפולציות בעמדה (סיווג, טווח גבהים, אחראיות) |
| `capNearest(tracks, center, MAX_TRACKS)` | **אכיפת קאפ 300** (§6) - חותך את הרחוקים ממרכז המפה. הפונקציה האחרונה בשרשרת |

הסדר קבוע: `bboxFilter` → `applyFilters` → `capNearest`. הקאפ אחרון בכוונה -
אחרת מטוס שהפקח סינן החוצה היה "תופס מקום" ודוחק מטוס רלוונטי.

**למה dead reckoning ולא קפיצה כל 2 שניות:** בלעדיו המטוסים "מנתרים" - חוויה שנקראת
כתקלה, ובסביבת ATC היא פוגעת באמון בתמונה. איתו הם גולשים ברציפות. העלות היא
**אפס בקשות רשת נוספות** - זה חישוב מקומי בלולאת הציור שכבר רצה. הפער בין המיקום
המשוער למדוד מתוקן ב-lerp קצר בכל דגימה חדשה, ולא בקפיצה.

### 5.4 שכבת הציור - `src/airPicture/render.ts` + `AirPictureLayer.tsx`

**`<canvas>` אחד. לא SVG.**

| | SVG (כמו השכבות הקיימות) | Canvas (הבחירה) |
|---|---|---|
| 300 מטוסים | ~1,500 צמתי DOM שמשתנים | 0 צמתי DOM |
| עלות עדכון | style + layout + paint על 1,500 צמתים | ציור ישיר לביטמאפ |
| הערכה | עשרות ms לכל עדכון | יעד: < 3ms |

השכבות הקיימות (אזורים, הקפות, נקודות העברה) הן SVG - ובצדק: הן **סטטיות** ומעטות.
תמונ"א היא ההפך: מאות ישויות שכולן זזות. זה בדיוק המקרה שבו קנבס עדיף, ולכן זו
**אינה סטייה מהתבנית** אלא בחירה נכונה לסוג המידע.

**ארבע אופטימיזציות בשכבת הציור:**

1. **מטמון ביטמאפ לתוויות.** `fillText` יקר (~10-20µs); 300 מטוסים × 3 שורות תווית
   = ~900 קריאות = יותר מדי. במקום זה כל תווית מצוירת פעם אחת ל-canvas זעיר,
   ומצוירת בכל פריים ב-`drawImage` (~2-5µs). התווית מצוירת מחדש **רק כשהטקסט
   משתנה** - כלומר לכל היותר פעם ב-2 שניות. חיסכון של פי 4-5.
   מפתח המטמון: `cs|alt|spd|cls`. פינוי LRU בגבול **400** ערכים - `MAX_TRACKS`
   (300) ועוד מרווח לתחלופה, ולא מספר שרירותי.

2. **קצב ציור 10fps, לא 60.** תמונה שמתעדכנת כל 2 שניות לא צריכה 60fps. 10fps
   מספיק לגלישה חלקה ומוריד את עלות הציור פי 6. הלולאה היא `requestAnimationFrame`
   עם דילוג פריימים - כך היא נעצרת אוטומטית כשהחלון מוסתר.

3. **הקנבס יושב מחוץ ל-`transform` של המפה.** שכבות המפה נעות תחת
   `translate(...) scale(zoom)` ([`mapLayerTransform`](src/utils/mapPan.ts#L35)).
   ביטמאפ שנמתח ב-`scale` מטשטש - ובעמדת 24" זה בדיוק הטקסט שהפקח צריך לקרוא.
   לכן הקנבס נשאר ללא transform, ופאן/זום מוחלים **במתמטיקה של הציור**. הטקסט
   נשאר חד בכל רמת זום. כדי לא להישבר מול ה-`transition: transform 0.15s` של
   המפה, הלולאה מבצעת lerp על (pan, zoom) באותו קצב.

4. **גודל הביטמאפ = CSS × devicePixelRatio × cssZoom.** `#root` נמצא תחת
   `zoom: var(--s)` (1.65 בעמדת 24"). קנבס שלא מכפיל ב-`measureCssZoom`
   ([`mapPan.ts`](src/utils/mapPan.ts#L76)) יוצא מטושטש בדיוק בעמדת היעד. זו אותה
   מלכודת שמתועדת ב-CLAUDE.md §גרירה סעיף 3, בלבוש אחר.

### 5.5 שכבת השליטה - `src/airPicture/AirPictureControls.tsx`

הבקרות מאפיון §12. **כולן מקומיות לעמדה** - הן משנות `ref` בלולאת הציור, לא
`state` שמפעיל render.

| בקרה | ברירת מחדל | איפה נשמר |
|---|---|---|
| הדלקה/כיבוי | לפי הגדרת העמדה | `sessionStorage` |
| גודל אייקון/תווית (0.6-1.6) | 1.0 | `sessionStorage` |
| בהירות (opacity 0.15-1.0) | **0.45** | `sessionStorage` |
| רק אייקון / אייקון+תווית | אייקון+תווית | `sessionStorage` |
| סינון סיווג · טווח גובה · אחראיות | הכל | `sessionStorage` |

**למה `sessionStorage` ולא DB:** בדיוק התבנית של `data_windows` שמתועדת ב-
[data-model.md](data-model.md) - "זו ברירת המחדל של העמדה; הפקח מזיז/מכבה/עורך
בסשן שלו (sessionStorage) בלי לשנות אותה". כתיבה ל-DB בכל הזזת סליידר הייתה
מייצרת עשרות UPDATE בדקה על `workstation_presets`.

**עמדת הפאנל:** מחזור [`useDragPosition`](src/hooks/useDragPosition.ts) - לא מימוש
גרירה חדש (CLAUDE.md §גרירה). הפאנל חייב להיגרר בעט ובאצבע.

---

## 6. תקציב ביצועים

> **קבוע התכנון (הצוות, 2026-08-07): `MAX_TRACKS = 300`.**
> 300 מטוסים בו-זמנית באוויר הוא המקרה הגרוע. **כל** מספר במסמך מכויל אליו.
>
> זהו **קאפ נאכף בקוד ולא הנחה**. השכבה מציירת לכל היותר 300 מטוסים, גם אם
> המאגר מחזיר יותר. הקאפ מוחל **אחרי** סינון ה-bbox ולפי מרחק ממרכז המפה -
> כלומר מה שנחתך הוא תמיד הרחוק ביותר, לא "300 הראשונים ברשימה". חריגה
> נרשמת ל-console פעם אחת ומוצגת כחיווי בפאנל הבקרות.
>
> **למה קאפ ולא רק תקציב:** ההנחה כאן היא היחידה שמישהו מחוץ ל-SKY-KING יכול
> להפר. מאגר שיחזיר יום אחד 900 מטוסים יפיל את העמדה מתחת לפקח בזמן אמת. הקאפ
> הופך תקלה במאגר לאובדן תצוגה של המטוסים הרחוקים - במקום לעמדה איטית.

זהו **תקציב מוצהר עם שער בדיקה**, לא מדידה שבוצעה. המספרים הם יעדים שמאמתים
לפני "done".

| משאב | תקציב | איך נמדד |
|---|---|---|
| בקשות רשת | 0.5 req/s (+~18% על העמדה, 0% על שרת SKY-KING) | LOADRUNNER |
| מטען לדגימה | < 15KB דחוס (300 מטוסים) | DevTools Network |
| `JSON.parse` + טרנספורם | < 1ms לדגימה | `performance.mark` |
| ציור פריים (300 מטוסים) | **< 3ms** | Performance panel |
| CPU בפועל | < 4% מליבה | Task Manager, 5 דק' רצופות |
| זיכרון יציב | < 25MB, **בלי צמיחה** ב-30 דק' | Heap snapshot ×3 |
| רינדורים של `SectorDashboard` בגלל תמונ"א | **0** | React DevTools Profiler |
| FPS של גרירת מפה עם תמונ"א דלוקה | ללא שינוי מהיום | מדידה לפני/אחרי |

**שער חובה:** אם ציור פריים חורג מ-3ms - השכבה **מורידה את עצמה אוטומטית**
ל-2fps (ציור רק בדגימה חדשה, בלי dead reckoning) ומציגה חיווי. עדיף תמונה
מקפצת על עמדה איטית. תפעולית, עמדה שמגיבה לאט לעט היא תקלה; תמונ"א שמנתרת היא
אי-נוחות.

---

## 7. הגדרת עמדה - DB, שרת, ואדמין

### 7.1 שינוי DB (דורש `/migrate`)

**`workstation_presets`** - שתי עמודות:

```sql
ALTER TABLE workstation_presets
  ADD COLUMN IF NOT EXISTS air_picture_enabled  BOOLEAN DEFAULT false;
ALTER TABLE workstation_presets
  ADD COLUMN IF NOT EXISTS air_picture_defaults JSONB   DEFAULT '{}';
```

`air_picture_defaults`: `{ opacity, scale, labels, classes[], altMin, altMax }` -
ברירות המחדל של העמדה, שהפקח דורס בסשן שלו.

**`air_picture_config`** - טבלת קונפיגורציה, **שורה אחת גלובלית**:

| עמודה | סוג | תיאור |
|---|---|---|
| `id` | SERIAL PK | |
| `base_url` | TEXT | כתובת המאגר |
| `auth_token` | TEXT | טוקן. **נקרא רק בשרת/בשרת העמדה, לא נשלח ל-renderer** |
| `poll_ms` | INT DEFAULT 2000 | קצב הרענון |
| `enabled` | BOOLEAN DEFAULT false | מתג ראשי |
| `updated_at` | TIMESTAMPTZ | חותמת (TIMESTAMPTZ, לא TIMESTAMP - CLAUDE.md) |

> **החלטת הצוות (2026-08-07): ללא בידוד סביבות בשלב זה.** קונפיגורציה אחת
> חלה על **כל** הסביבות. הטבלה נרשמת ב-`IGNORED_EXACT` של
> [`server/db/env-tables.js`](server/db/env-tables.js#L164) - בדיוק כמו
> `gapi_env_config` ו-`environments` עצמה - ולכן היא **לא** משוכפלת פר-סביבה.
>
> המשמעות: כשמפנים את הקונפיג לסימולטור, **כל** העמדות בכל הסביבות מקבלות
> ממנו. זה מכוון: בשלב זה יש **מאגר תמונ"א אחד** שהעמדות קוראות ממנו, ולא
> "מאגר תרגול" ו"מאגר אמת" נפרדים.
>
> אם בעתיד תידרש הפרדה - המעבר הוא הוצאת הטבלה מ-`IGNORED_EXACT`, בלי שינוי
> בחוזה ה-API ובלי נגיעה בשכבת התצוגה.

### 7.2 שרת - `server/routes/airPicture.js` (חדש, ~120 שורות)

| Route | תפקיד |
|---|---|
| `GET /api/air-picture/config` | הקונפיג **בלי הטוקן** - העמדה צריכה לדעת רק אם דלוק ובאיזה קצב |
| `PUT /api/air-picture/config` | עדכון מהאדמין (כולל טוקן) |
| `GET /api/air-picture/relay` | **מסלול גיבוי בלבד** - ריליי לעמדות דפדפן שאין להן שרת Electron |

> `/relay` מסומן מפורשות כגיבוי. עמדת Electron - כלומר עמדת היעד - **לא עוברת בו**,
> בהתאם לדרישה §7 באפיון. הוא קיים כדי שפיתוח בדפדפן ימשיך לעבוד.

### 7.3 שרת העמדה - `electron/stationServer.cjs`

הרחבת `shouldProxy()` ל-`/air-picture` עם יעד נפרד. שלושה רווחים:

1. הטוקן מוזרק בשרת העמדה ולא מגיע ל-renderer.
2. הבקשה נשארת **same-origin** - אפס בעיות CORS ותעודות, בדיוק ההיגיון שכתוב
   בראש הקובץ לגבי `/api`.
3. כשל **מהיר** במקום timeout - העמדה נכנסת ל-`stale` תוך שבריר שנייה.

### 7.4 אדמין - ההתראה על מפה לא מעוגנת (דרישה §3)

**היכן:** [`ManagementPage.tsx`](src/components/admin/ManagementPage.tsx), טופס העמדה.

**הבדיקה כבר קיימת בקוד:** `buildGeoAnchor(map)` ב-[`src/utils/geo.ts`](src/utils/geo.ts#L6)
מחזירה `null` בדיוק כשהמפה לא מעוגנת, ואותו דפוס כבר בשימוש
(`afIsCalibrated`, ManagementPage:6356). **לא כותבים בדיקה חדשה.**

התנהגות נדרשת:

| מצב | תגובה |
|---|---|
| `air_picture_enabled` + `map_id` לא מעוגנת | התראה אדומה מתמדת ליד המתג: "המפה שנבחרה אינה מעוגנת - תמונ"א לא תוצג" |
| דו-מפה, רק אחת מעוגנת | התראה שמציינת **איזו** מפה. התמונ"א תוצג על המעוגנת בלבד |
| שתיהן לא מעוגנות | **חסימת שמירה** (לא רק אזהרה) |
| המפה עוגנה מאוחר יותר | ההתראה נעלמת בטעינה הבאה של הטופס |

**החלטה - למה חסימה ולא רק אזהרה כששתיהן לא מעוגנות:** עמדה שנשמרה עם תמונ"א
דלוקה ואפס מפות מעוגנות היא עמדה ש**מבטיחה מידע ולא נותנת אותו**. פקח שמניח
שהתמונ"א דלוקה ורואה מפה ריקה יסיק "אין תנועה" במקום "התצוגה מנוטרלת". זה מצב
מטעה ולכן נחסם בהגדרה. במצב חלקי (מפה אחת מעוגנת) יש תמונה אמיתית על מפה אחת -
אזהרה מספיקה.

---

## 8. שילוב בעמדות - היקף ההשפעה

| מסך | מושפע | מה בדיוק |
|---|---|---|
| `SectorDashboard` (בקר) | ✅ | הוספת `<AirPictureLayer/>` ליד שכבות המפה + `<AirPictureControls/>`. **אפס state חדש ברכיב** |
| `GroundView` (מגדל) | ✅ אותו רכיב | אותה שכבה בדיוק, אותן בקרות. DRY מלא |
| `VerticalView` | ❌ | אין מפה מעוגנת |
| `ClassicView` | ❌ | תצוגת טבלה |
| `MissionDeskView` | ❌ | לא מפה |
| `ManagementPage` | ✅ | מתג + התראת עיגון |
| `MapZoneEditor` | ❌ | עורך קונפיגורציה, לא תצוגה חיה |

**שכבת התמונ"א היא רכיב משותף אחד** בין `SectorDashboard` ל-`GroundView`, לפי עקרון
הרכיבים המשותפים ב-CLAUDE.md. אין חריג ואין divergence.

**מיקום z-index:** מתחת לשכבות SKY-KING (פ"ממים, נקודות העברה, אזורים) ומעל תמונת
המפה. הצדקה: התמונ"א היא **מודעות מצבית, לא כלי עבודה** - היא מראה מה קורה בשמיים,
ואילו הפ"מ הוא מה שהבקר/פקח **עובד עליו**. פ"מ של SKY-KING חייב
להישאר לחיץ וקריא גם כשהתמונ"א עמוסה. זה גם מה שהאפיון מבקש - "לשים ברקע את
האייקונים, בהירות שלא יפריעו".

**`pointerEvents: 'none'` על הקנבס.** אחרת הוא היה בולע כל גרירה, לחיצה וציור על
המפה - כלומר שובר את כל העמדה. "hook" של מטוס בתמונ"א (אם יידרש) ימומש כ-hit-test
מתמטי בשכבת הגרירה הקיימת, לא בשכבת הקנבס.

---

## 9. ATSIM - כלי בניית מאגר התמונ"א (אפיון §10-11)

> **תיחום (הצוות, 2026-08-07): ATSIM נועד רק לבניית מאגר תמונ"א.**
> הוא **לא** כלי סביבות ולא מנגנון תרגול. הוא ה-FRONT שדרכו מזריקים מטוסים
> למאגר, וה-API שהעמדה קוראת ממנו הוא בדיוק אותו `AirTrafficAPI` של המאגר האמיתי.
>
> נובע מכך: **מופע אחד, ללא הפרדת סביבות.** מי שמופנה אליו בקונפיג (§7.1) -
> כל העמדות בכל הסביבות - רואה את אותה תמונה. אין "תרחיש של סביבה א'"
> מול "תרחיש של סביבה ב'".

### 9.1 תבנית - מיראז'

**לא ממציאים תבנית חדשה.** [`mirage/`](mirage/) הוא כבר בדיוק זה: אפליקציית Express
נפרדת באותו repo, פורט משלה (7300), אחסון משלה, `admin.html` משלה, `npm run mirage`,
ופריסה כשירות שני ב-Railway. ATSIM משכפל את המבנה הזה:

```
atsim/
  server.js          הרצה (פורט 7400)
  app.js             createAtsimApp() - Express
  store.js           תרחישים ומטוסים (JSON, בלי DB, בלי סביבות)
  sim.js             מנוע התנועה - פונקציות טהורות
  admin.html         ה-FRONT: מפה + עורך + רשימה
  data.example.json
  sim.test.js
```

### 9.2 מנוע התנועה - החלטה מרכזית

**המיקום מחושב על-פי-קריאה, לא על-פי-טיק.**

```
positionAt(scenarioTrack, tNow)  →  { lat, lon, alt, spd, hdg }
```

ATSIM **לא** מריץ `setInterval` שמזיז מטוסים ושומר מצב. הוא שומר את **הגדרת**
המסלול (נקודות דרך + מהירות + נקודות שינוי גובה + זמן התחלה), וכשמגיעה בקשה הוא
מחשב איפה כל מטוס נמצא **עכשיו**, כפונקציה טהורה של הזמן.

| למה זה נכון | |
|---|---|
| **אפס CPU במנוחה** | מאגר שאיש לא קורא לו לא צורך כלום |
| **בלי drift** | טיקים שמצטברים סוטים; חישוב מ-`t0` תמיד מדויק |
| **עמיד לאתחול** | הפעלה מחדש של השרת לא מאבדת מיקומים - הם נגזרים מהזמן |
| **"הרצה בשעה מסוימת"** מגיעה בחינם | `t0` עתידי. המטוס פשוט לא נמצא באוויר עד שהזמן מגיע |
| **נבדק ב-vitest** | פונקציה טהורה. `positionAt(track, t0 + 600_000)` - בלי שרת, בלי שעון |

### 9.3 מודל הנתונים

```jsonc
{
  "id": "sc-1", "name": "תרחיש צפון",
  "startAt": 1786108800000,          // null = ידני
  "loop": false,
  "tracks": [{
    "id": "t-1", "cs": "אפיק 21",
    "cls": "friend", "typ": "f16", "resp": "צפון",
    "legs": [                         // נקודות דרך; שינוי גובה בין הנקודות
      { "lat": 32.9, "lon": 35.1, "alt": 5000,  "spd": 300 },
      { "lat": 32.5, "lon": 34.9, "alt": 22000, "spd": 450 },
      { "lat": 32.1, "lon": 34.8, "alt": 22000, "spd": 450 }
    ]
  }]
}
```

נקודת שינוי גובה = נקודת דרך שה-`alt` שלה שונה מקודמתה. הטיפוס משתנה ליניארית
בין השתיים. **מודל אחד** לניווט ולגובה, במקום שתי רשימות שצריך לסנכרן.

### 9.4 ה-FRONT

| יכולת (אפיון §11) | מימוש |
|---|---|
| מפה | אותה תמונת מפה מעוגנת של SKY-KING + `imagePctToGeo` הקיימת. לחיצה על המפה = נקודת דרך |
| יצירת רכיב (= מטוס) | טופס: שם פ"מ, סוג מטוס, סיווג, אחראיות + נקודות דרך על המפה |
| מהירות / שינוי גובה | עמודות בטבלת נקודות הדרך |
| הרצה מיידית | `startAt = now` |
| הרצה בשעה מסוימת | `startAt = <ts>`; המטוס מופיע בתמונה רק בזמנו |
| מחיקה / עדכון / שכפול | CRUD; שכפול = deep clone + `id` חדש + היסט קטן בנ"צ |

**HTML+JS פשוט, כמו `mirage/admin.html`.** לא React, לא build step. זה כלי בדיקה,
ואסור שיגרור את שרשרת הבנייה של SKY-KING לתוכו.

### 9.5 מה ATSIM נותן לנו

זה לא "נחמד שיהיה". בלעדיו אי אפשר:

- לבדוק את שער הביצועים של §6 (צריך 300 מטוסים מציאותיים).
- לפתח את השכבה לפני שהמאגר האמיתי זמין.
- לכתוב e2e דטרמיניסטי - תרחיש עם `startAt` קבוע מייצר **בדיוק** את אותה תמונה
  בכל ריצה. (Playwright כבר ב-repo.)
- להדגים ללקוח בלי מאגר.

---

## 10. סיכונים ואמצעי מניעה

| # | סיכון | מניעה |
|---|---|---|
| 1 | **התמונ"א מאטה את העמדה** | store מחוץ ל-React + קנבס + מטמון תוויות + שער 3ms עם הורדת-רמה אוטומטית (§6) |
| 2 | **דליפת זיכרון** - 300 אובייקטים כל 2 שניות, 540K לשעה | סנאפשוט יחיד (בלי היסטוריה), מטמון תוויות LRU חסום, בדיקת heap ×3 ב-30 דק' |
| 3 | **המאגר איטי/נופל ותולה את העמדה** | `AbortController` 1.8s, backoff מעריכי, `status: 'down'` - השכבה נעלמת, העמדה ממשיכה |
| 4 | **תמונ"א ישנה מוצגת כאילו היא חיה** - סיכון תפעולי אמיתי | `t` של המאגר; מעל 6s → תווית "לא מעודכן מאז HH:MM:SS" + עמעום. **אף פעם לא מציגים תמונה ישנה בלי חיווי** |
| 5 | **בלבול בין מטוס בתמונ"א לפ"מ ב-SKY-KING** - שתי ישויות שונות (§0) שנראות דומה על אותה מפה | סמכות ויזואלית שונה (§8), `pointerEvents:'none'`, סמל מובחן. הפ"מ נשאר הישות היחידה שאפשר לגעת בה |
| 6 | **הטוקן דולף ל-renderer** | הטוקן חי רק בשרת העמדה / שרת SKY-KING. `GET /config` לא מחזיר אותו |
| 7 | **מפה לא מעוגנת → קריסה** | `buildGeoAnchor` מחזירה `null` → השכבה לא מרונדרת כלל. + חסימה באדמין (§7.4) |
| 8 | **טשטוש בעמדת 24"** | `dpr × cssZoom` + קנבס מחוץ ל-transform (§5.4) |
| 9 | **הקנבס בולע גרירות** | `pointerEvents: 'none'`, נאכף ב-e2e |
| 10 | **סטיית שעונים** מעוותת dead reckoning | מסתמכים על `t` של המאגר בלבד; ההיסט נמדד פעם אחת ומוחל |
| 11 | **המאגר מחזיר יותר מ-300 מטוסים** - ההנחה היחידה שגורם חיצוני יכול להפר | `capNearest` (§5.3, §6): נחתכים הרחוקים ממרכז המפה, לא "מהסוף". חיווי בפאנל + רישום ל-console. תקלה במאגר = אובדן תצוגה של הרחוקים, לא עמדה איטית |

**דגל צהוב לתשומת לב:** `SectorDashboard.tsx` הוא כבר 17,894 שורות. התכנית הזו
מוסיפה לו **שתי שורות JSX ואפס state** - כל השאר יושב ב-`src/airPicture/`. זה
מכוון: כל חריגה מזה היא סימן שהתכנית הופרה.

---

## 11. אבני דרך

| # | שלב | תוצר | תלוי ב- |
|---|---|---|---|
| 0 | חוזה + טיפוסים | `shared/airTrafficApi.js` + בדיקות | - |
| 1 | **ATSIM** | סימולטור + FRONT, מגיש את החוזה | 0 |
| 2 | Store + poller | `src/airPicture/{store,poller,track}.ts` + בדיקות | 0 |
| 3 | שכבת קנבס | `AirPictureLayer.tsx` על מפה אחת | 1, 2 |
| 4 | **שער ביצועים** | מדידת §6 מול 300 מטוסים מ-ATSIM | 3 |
| 5 | בקרות בעמדה | `AirPictureControls.tsx` + sessionStorage | 3 |
| 6 | DB + אדמין + התראה | `/migrate`, `airPicture.js`, ManagementPage | - |
| 7 | פרוקסי שרת העמדה | `stationServer.cjs` | 6 |
| 8 | GroundView | אותה שכבה בעמדת מגדל | 3, 5 |
| 9 | i18n + `/ui-adapt` + `/atc-ux` | תמות, סקייל, ביקורת HMI | 5 |
| 10 | e2e + `/qa` + `/sync-docs` | Playwright מול ATSIM | הכל |

**שלב 4 הוא שער ולא צ'קפוינט:** אם התקציב נחרג, עוצרים ומתקנים לפני שממשיכים.
פיצ'ר תצוגה שמאט את העמדה גרוע מפיצ'ר שלא קיים - העמדה מחליפה סדק פיזי שלא
מאט לעולם.

---

## 12. שאלות פתוחות

> נסגרו ב-2026-08-07: **המונח** (מטוס מול פ"מ - §0) · **בידוד הסביבות**
> (אין, קונפיג אחד לכל הסביבות - §7.1, §9) · **סדר הגודל**
> (`MAX_TRACKS = 300`, נאכף בקוד - §6).
>
> סגירת סדר הגודל מייתרת שתי תכולות ששקלתי: **פורמט טורי** ו-**Web Worker**.
> ב-300 מטוסים ה-parse הוא פחות ממילישנייה והציור נכנס בתקציב על ה-main thread.
> שתיהן היו מוסיפות מורכבות אמיתית בלי להחזיר דבר, ולכן הן **מחוץ לתכולה**.

1. **"מניפולציות" (אפיון §9).** מה שנספר כאן: סינון (סיווג/גובה/אחראיות), גודל,
   בהירות, הצגת תווית. **לא** נכלל: **קישור מטוס לפ"מ שלו** דרך השדה "שם פ"מ" -
   זו תכולה נפרדת שדורשת `/pm`, וההבחנה של §0 היא בדיוק מה שהופך אותה למעניינת
   (למשל: פ"מ שרשום כממריא אך אין לו מטוס בתמונ"א, או להפך).
3. **רשימת סוגי המטוסים.** האם המאגר מחזיר את אותם מפתחות של
   [`aircraft.ts`](src/utils/aircraft.ts), או שנדרשת טבלת מיפוי?
4. **"אחראיות"** - טקסט חופשי או קוד סגור מול הסקטורים ב-SKY-KING?
