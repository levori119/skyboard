# הקלטת פעולות במסך - אפיון

> **מה זה:** הקלטת **כל התצוגה** של העמדה לקובץ וידאו, לצורך תחקיר.
> **איפה נשמר:** נתיב (PATH) שנקבע בניהול הטכני **לכל בסיס ויב"א** בנפרד.
> **מי מפעיל:** קופסה שחורה אוטומטית + כפתור בתפריט העמדה.

---

## 0. ההכרעות שמהן נגזר הכל

| הכרעה | מה נבחר | למה זה משנה |
|---|---|---|
| **מתי מקליטים** | גם אוטומטי וגם כפתור | הקופסה השחורה נותנת תחקיר גם כשאיש לא חשב להקליט; הכפתור נותן שליטה כשהפקח יודע שעכשיו קורה משהו |
| **מה נכנס לקובץ** | **כל המסך הפיזי** | תמונת מצב אמיתית של מה שהיה מול העיניים, כולל אפליקציה זרה שפתוחה מעל העמדה |
| **מי כותב לדיסק** | **העמדה עצמה** | אפס עומס רשת על השרת, עובד גם בנתק, ונתיב הרשת של הבסיס נגיש ישירות מהעמדה |
| **איפה מוגדר ה-PATH** | `aviation_bases` (בסיסים **ויב"אות** באותה טבלה) | ה-PATH הוא הכרעה של הניהול הטכני של אותו בסיס, וחל על כל עמדותיו |

> **דרישה נגררת:** הקלטה עובדת **רק באפליקציית העמדה** (Electron). בדפדפן אין
> דרך לכתוב לנתיב בדיסק, ולכן התפריט מציג במפורש "הקלטה אפשרית רק באפליקציית
> העמדה" ולא כפתור דומם.

---

## 1. חלוקת העבודה בין השכבות

```
┌─ העמדה (renderer) ──────────────────────────────────────────┐
│ getDisplayMedia  →  MediaRecorder  →  נתחים של שנייה         │
│ src/utils/screenRecording.ts · src/hooks/useScreenRecorder.ts │
└──────────────────────────┬──────────────────────────────────┘
                           │ IPC: rec:start / rec:chunk / rec:rotate / rec:keep / rec:stop
                           │ (מזהה בסיס + אסימון. **לא נתיב**)
┌──────────────────────────▼──────────────────────────────────┐
│ תהליך ה-Electron - מחזיק את הנתיב ואת הקובץ                  │
│ electron/screenRecorder.cjs                                  │
│   • שואב תצורה מ-/api/screen-recording/config/:baseId        │
│   • מאמת נתיב · בונה שם קובץ · כותב · מוחק לפי תקופת שמירה    │
└──────────────────────────┬──────────────────────────────────┘
                           │  קובץ
                     PATH של הבסיס  (D:\SKYKING\REC או \\srv\share\rec)
```

**הלוגיקה הטהורה** יושבת ב-[`shared/screenRecording.js`](shared/screenRecording.js) -
מקור אמת אחד לשרת, לתהליך הראשי, לעמדה ולמסך הניהול.

### למה העמוד אינו מוסר נתיב

הכלל שבראש [`electron-preload.cjs`](electron-preload.cjs): *"אין ערוץ שמקבל נתיב
קובץ מהעמוד"*. העמוד נטען מכתובת מרוחקת, ולכן ערוץ שמקבל נתיב היה נותן לכל עמוד
שיגיע לחלון לכתוב לכל מקום בדיסק. כאן העמוד מוסר **מזהה בסיס**, והתהליך הראשי
שואב את הנתיב מהשרת בעצמו. במקרה הגרוע עמוד עוין יכול להפעיל הקלטה לתיקייה
שהניהול הטכני הגדיר - ולא יותר.

גם **שם הבסיס** בשם הקובץ מגיע מתשובת השרת ולא מהעמוד.

---

## 2. התצורה - ניהול טכני, לשונית "בסיסים"

[`src/components/admin/ScreenRecordingSection.tsx`](src/components/admin/ScreenRecordingSection.tsx),
מוצג ל**מנהל טכני** בלבד (`isAdmin`).

| שדה | עמודה ב-`aviation_bases` | ברירת מחדל | טווח |
|---|---|---|---|
| הקלטה אוטומטית | `recording_enabled` | `false` | - |
| **נתיב שמירה (PATH)** | `recording_path` | ריק | נתיב מוחלט או UNC |
| אורך קטע (דקות) | `recording_segment_minutes` | 15 | 1-120 |
| ימי שמירה | `recording_retention_days` | 7 | 0-365 (**0 = ללא הגבלה**) |
| פריימים לשנייה | `recording_fps` | 5 | 1-30 |
| איכות | `recording_quality` | `medium` | low / medium / high |

**5 fps כברירת מחדל** כי מסך עמדה כמעט סטטי: זה קריא לתחקיר וחוסך פי כמה דיסק.
**אומדן הנפח מוצג ליד ההגדרה** (`estimateRecordingBytes`) - מי שקובע נתיב על דיסק
רשת צריך לראות באותו מסך שהוא הזמין לשם 25GB לשבוע, ולא לגלות זאת מהדיסק.

### אימות הנתיב

`isSafeRecordingPath` נאכף **גם בשרת בשמירה וגם בעמדה לפני הכתיבה**:

| מאושר | נדחה |
|---|---|
| `D:\SKYKING\REC` · `D:/SKYKING/REC` | נתיב יחסי (`REC\video`) |
| `\\srv01\skyking$\rec` (כולל שיתוף מוסתר) | `..` בכל מקום בנתיב |
| `/var/skyking/rec` (פיתוח) | `%APPDATA%` / `${VAR}` |
| | `< > " \| ? *` ותווי בקרה |

---

## 3. הקובץ

```
SKYKING_<בסיס>_<עמדה>_<תאריך>_<שעה>[_manual].<webm|mp4>
SKYKING_בחא-8_תל-נוף-אווירי_2026-09-16_143205.mp4
```

- **שעה מקומית** ולא UTC: מי שמחפש "מה קרה ב-14:32" מחפש לפי השעון שעל הקיר.
- שם בסיס ועמדה עוברים `sanitizeFileToken` - גרשיים נמחקים (`בח"א 8` → `בחא-8`),
  תווים אסורים ומפרידי נתיב הופכים ל-`-`.
- `_manual` = הקלטה שהמפעיל התחיל בעצמו.
- **הקודק:** MP4/H264 מועדף על WebM. בעמדה על רשת מבודדת זה ההבדל בין תחקיר
  שנפתח בנגן המובנה של Windows לתחקיר ש"לא נפתח".

### תקופת שמירה ומחיקה

- סריקה בתחילת הקלטה ואחת לשעה.
- נמחק **רק** קובץ שעונה על `isRecordingFile` (התחילית + חותמת הזמן). חומר
  תחקיר שמישהו שם באותה תיקייה לא נעלם.
- `keep/` - תיקיית משנה שהמחיקה לא נוגעת בה כלל.
- הקובץ שנכתב כרגע לא נמחק.

### "שמור את הקטע הזה"

לחיצה בתפריט מסמנת את **הקטע הנוכחי ואת הקודם** (המפעיל לוחץ *אחרי* שהאירוע
קרה), והם עוברים ל-`keep/` ולא יימחקו.

---

## 4. מטריצת המקרים

| מקרה | מה קורה |
|---|---|
| עמדה עולה, הקלטה דולקת לבסיס, נתיב תקין | ההקלטה מתחילה לבד, חיווי אדום מהבהב בכותרת |
| הקלטה **כבויה** לבסיס | אין הקלטה אוטומטית. בתפריט: "ההקלטה כבויה לבסיס הזה בניהול הטכני". **הכפתור הידני עדיין עובד** - מה שנדרש לו הוא הנתיב |
| לבסיס **לא הוגדר נתיב** | אין הקלטה, לא אוטומטית ולא ידנית. בתפריט: "לא הוגדר נתיב שמירה לבסיס" |
| הנתיב מוגדר אך פסול (נשמר לפני האימות) | "נתיב השמירה של הבסיס אינו תקין" |
| הנתיב תקין אך לא נגיש (שיתוף נפל) | "נתיב השמירה אינו נגיש מהעמדה" - **נתפס בהתחלה**, לא בדיעבד |
| העמדה אינה משויכת לבסיס (`parent_base_id` ריק) | "העמדה אינה משויכת לבסיס" |
| ריצה בדפדפן ולא באפליקציית העמדה | "הקלטה אפשרית רק באפליקציית העמדה" |
| הדיסק נופל **באמצע** הקלטה | החיווי בכותרת הופך לאדום מלא: "הכתיבה לדיסק נכשלה - ההקלטה אינה נשמרת" |
| סוף קטע (למשל 15 דקות) | הקובץ נסגר, נפתח קובץ חדש. **פער של עשרות אלפיות בין קטעים - מכוון** (ראה §5) |
| שתי החלפות באותה שנייה | חותמת הזמן מקודמת בשנייה עד שהשם פנוי. קובץ **אינו** נדרס |
| רענון דף / נפילת העמוד | הקטע נסגר מסודר ונשאר נגין; העמדה מתחילה הקלטה חדשה כשהדף חוזר |
| יציאה מהאפליקציה | `before-quit` סוגר את הקובץ |
| המפעיל עצר את השיתוף מחיווי מערכת ההפעלה | ההקלטה נעצרת מסודר |
| שינוי ההגדרה בניהול הטכני | נתפס תוך 5 דקות (`CONFIG_POLL_MS`) בלי לרענן את העמדה |
| שתי עמדות על אותו בסיס | שני קבצים באותה תיקייה; שם העמדה בשם הקובץ מפריד ביניהן |

---

## 5. מה מכוון ואינו באג

1. **פער בין קטעים.** הכותרת של WebM/MP4 יושבת בנתח הראשון של ה-MediaRecorder,
   ולכן החלפת קטע היא **מקליט חדש** - אחרת הקובץ השני היה חסר כותרת ולא נפתח
   בשום נגן. הפער הוא עשרות אלפיות שנייה.
2. **אין שמע.** הקלטת קשר היא הכרעה תפעולית נפרדת, ולא תופעת לוואי של הקלטת מסך.
3. **אין רישום ב-DB.** ההכרעה הייתה "העמדה כותבת ישירות ל-PATH" - חיפוש הקלטה
   נעשה בתיקייה לפי שם הקובץ, ולא ממסך התחקירים.
4. **המפעיל יכול לעצור את הקופסה השחורה.** זו הכרעה: הפקח הוא בעל הבית על העמדה.
   בתמורה, **החיווי גלוי כל זמן שההקלטה רצה** - מסך שמוקלט בלי שהיושב בו יודע
   הוא בעיה, לא פיצ'ר.

---

## 6. הקבצים

| קובץ | תפקיד |
|---|---|
| [`shared/screenRecording.js`](shared/screenRecording.js) | לוגיקה טהורה: שם קובץ, אימות נתיב, תקופת שמירה, בחירת קודק, אומדן נפח |
| [`electron/screenRecorder.cjs`](electron/screenRecorder.cjs) | כתיבה לדיסק, החלפת קטעים, keep, מחיקה |
| [`electron-main.cjs`](electron-main.cjs) | חמשת ערוצי ה-IPC + בורר מקור המסך (`setDisplayMediaRequestHandler`) - **בלי דיאלוג בחירה** |
| [`electron-preload.cjs`](electron-preload.cjs) | הגשר לעמוד: `recStatus / recStart / recChunk / recRotate / recKeep / recStop` |
| [`src/utils/screenRecording.ts`](src/utils/screenRecording.ts) | צילום, קידוד ותור הנתחים |
| [`src/hooks/useScreenRecorder.ts`](src/hooks/useScreenRecorder.ts) | קופסה שחורה + כפתור, וקריאת התצורה |
| [`server/routes/screenRecording.js`](server/routes/screenRecording.js) | שלוש נקודות הקצה של התצורה |
| [`src/components/admin/ScreenRecordingSection.tsx`](src/components/admin/ScreenRecordingSection.tsx) | ההגדרה בניהול הטכני |

### נקודות קצה

| שיטה | נתיב | הרשאה | מי קורא |
|---|---|---|---|
| GET | `/api/screen-recording/bases` | ADMIN | מסך הניהול הטכני |
| PUT | `/api/screen-recording/bases/:id` | ADMIN | מסך הניהול הטכני |
| GET | `/api/screen-recording/config/:baseId` | USER | העמדה ותהליך ה-Electron שלה |

ה-PATH **אינו** חלק מ-`GET /api/aviation-bases`: אותה רשימה נטענת בכל כניסה לעמדה
ובכל מסך, ושם שרת ושיתוף ברשת הבסיס אינם צריכים להיות שם.
