# FLOW של פ"מ - אפיון

> בקשה (2026-09-15, אורי לב): "בכל עמדה לראות את ה-FLOW ואיפה נמצא עכשיו כל פ"מ ופ"מ,
> בפירוט טבלאי של כל מטוס - מה עשה עם המגדל. בננה4 - בחא 8: יצא מהדת"ק 11:00 ->
> הסיעה 11:15 -> המריא על 33 -> נקודת העברה פלמח -> עמדה 305 -> בת"ק עזה -> עמדה 306.
> נמצא בעמדה = **קיבל** את המטוס, לא שמישהו גרר לעמדה. קבל יש רק אחת במערכת.
> בקליק - FLOW של פ"מ. וגם FLOW מרוכז בשאילתא."

## 0. מונחים

| מונח | משמעות |
|---|---|
| **FLOW** | רצף האירועים הכרונולוגי של פ"מ, מהיצירה ועד הנחיתה |
| **שלב** | אירוע אחד ב-FLOW (הסעה, המראה, נקודת העברה, קבלה בעמדה...) |
| **נמצא עכשיו** | המיקום הנוכחי של הפ"מ, מחושב בשרת מהמצב החי |
| **נמצא בעמדה** | עמדה ש**קיבלה** את הפ"מ (קבל / קבלה למפה / קבלה אוטומטית). שליחה לנקודת העברה **אינה** נוכחות בעמדה |

## 1. סטוריות

1. כ**פקח/בקר**, אני רוצה ללחוץ על פ"מ ולראות את כל ה-FLOW שלו בטבלה עם שעות, כדי לדעת מאיפה הגיע, מי החזיק בו ומתי - בלי להתקשר לעמדות אחרות.
2. כ**פקח/בקר**, אני רוצה טבלה מרוכזת של כל הפ"מים עם "נמצא עכשיו" ושרשרת השלבים, מסוננת בשאילתא, כדי לראות את תמונת הזרימה כולה במבט אחד.

## 2. Acceptance Criteria

1. כל אחד מהאירועים הבאים נרשם **בשרת** לטבלת `strip_flow_events` עם `occurred_at` (TIMESTAMPTZ), העמדה והמשתמש:
   - שינוי סטטוס קרקעי של מטוס: `taxi` / `lineup` / `takeoff` (עם מסלול ההמראה)
   - מעבר מטוס מנקודה לנקודה על מפת השדה (יציאה מדת"ק = נקודה מסוג `datk`)
   - שליחת העברה (עם שם נקודת ההעברה ועמדת היעד)
   - **קבלה** - מכל המסלולים: קבל, קבלה למפה, קבלה אוטומטית. נרשמת העמדה **שקיבלה בפועל** ואופן הקבלה
   - דחייה (עם ההערה) וביטול העברה
   - באוויר (`airborne`) ונחיתה של מטוס (`flight_status = landed`)
   - פיצול (חלק חדש) ומיזוג
2. אירוע שלא השתנה לא נרשם (שליחת אותו סטטוס שוב = אפס אירועים).
3. כשל ברישום האירוע **אינו מפיל** את הפעולה התפעולית (קבלה, הסעה...).
4. `GET /api/strips/:id/flow` מחזיר: פרטי הפ"מ, `current` (נמצא עכשיו), ו-`events` כרונולוגיים. "נוצר" נגזר מ-`strips.created_at` (עובד גם לפ"מים ותיקים).
5. פ"מ שנולד בפיצול יורש את אירועי המקור **עד רגע הפיצול**; פ"מ שמוזג אליו חלק מקבל גם את אירועי החלק הממוזג. אין כפילויות.
6. היסטוריה שורדת מחיקת פ"מ ומיזוג (אין FK עם CASCADE).
7. `GET /api/strip-flows?ids=...` מחזיר את אותו מבנה לכמה פ"מים בבקשה אחת (עד 500).
8. **"נמצא עכשיו"**: נחת / בנקודת העברה X (ממתין לעמדה Y) / במגדל - סטטוס קרקעי / בעמדה (העמדה שקיבלה) / לא ידוע.
9. בקליק ימני על פ"מ (חלון הפ"מים, שורת טבלה, תפריט מהיר במגדל) - פריט **FLOW** פותח את החלון בפירוט הפ"מ.
10. תפריט "תצוגה" - **FLOW פ"מים** פותח את הטבלה המרוכזת: או"ק · נמצא עכשיו · שרשרת שלבים עם שעות · עדכון אחרון. חיפוש לפי או"ק + שאילתא (QueryBuilder) + "לפי השאילתא של העמדה". קליק על שורה = פירוט.
11. החלון: מסגרת `windowFrame('view')` (תורכיז), גרירה ב-Pointer Events, עגינה לקונטיינר, שלוש תמות, סקייל, i18n עברית/אנגלית, CSS logical properties, בלי em-dash.
12. החלון מתעדכן כל 5 שניות כשהוא פתוח.

## 3. השפעה על עמדות

| עמדה | מושפעת? | הערה |
|---|---|---|
| בקר (CTRL) | כן | חלון FLOW + פריט בתפריט הפ"מ |
| מגדל (TWR) | כן | כאן נוצרים אירועי הקרקע; חלון FLOW + פריט בתפריט המהיר |
| קלאסית / ניהול שדה | כן | אותו חלון מתפריט "תצוגה" |

## 4. רכיבים משותפים

- `shared/ContextMenu.tsx` - מקבל `onFlow` אופציונלי (בלי prop - ללא שינוי).
- `acceptTransferTx` - **ליבת הקבלה**: נוסף רישום אירוע באותה טרנזקציה (SAVEPOINT - כשל ברישום לא מבטל קבלה). אין שינוי ב-state machine של ההעברות.
- חלון חדש אחד `strips/StripFlowWindow.tsx` - מופע יחיד ב-SectorDashboard, נפתח מכל מקום דרך `openStripFlow(id)`.

## 5. ארכיטקטורה

### DB - `strip_flow_events` (תפעולית, פר-סביבה)

| עמודה | סוג | תיאור |
|---|---|---|
| `id` | BIGSERIAL PK | |
| `strip_id` | INT NOT NULL | **בלי FK** - ההיסטוריה שורדת מחיקה ומיזוג |
| `kind` | VARCHAR(32) | `taxi` · `lineup` · `takeoff` · `ground_point` · `transfer_sent` · `accepted` · `rejected` · `cancelled` · `airborne` · `landed` · `split` · `merged` |
| `callsign` | VARCHAR(64) | או"ק ברגע האירוע |
| `preset_id` / `preset_name` | INT / VARCHAR(128) | העמדה שבה קרה האירוע (בקבלה - **העמדה המקבלת**) |
| `point_label` | VARCHAR(128) | נקודת העברה / נקודה בשדה / מסלול |
| `crew_member_id` / `crew_member_name` | INT / VARCHAR(128) | מ-`req.user` (לא מגוף הבקשה - SK-18) |
| `details` | JSONB | `aircraft`, `runway`, `fromPresetName`, `toPresetName`, `mode`, `note`, `fromPoint`, `fromStripId`, `sourceStripId`... |
| `occurred_at` | TIMESTAMPTZ DEFAULT NOW() | |

אינדקס `(strip_id, occurred_at)`. בסיווג `OPERATIONAL_TABLES`. ב-`UNDO_DENYLIST` - יומן שניתן לבטלו אינו יומן.

### שרת
- `server/utils/stripFlow.js` - **טהור**: `diffAircraftPositions`, `flowCurrent`, `collectFlowStripIds` / סינון ירושה.
- `server/db/stripFlowEvents.js` - `recordFlowEvent(db, ev, { inTx })` + `loadFlows(db, ids)`.
- `server/routes/stripFlow.js` - `GET /api/strips/:id/flow`, `GET /api/strip-flows`.
- נקודות רישום: `strips.js` (`PUT /aircraft`, `PUT /:id` airborne, `partial-create`, `ground-single-transfer`), `transfers.js` (send, transfer-to-preset, accept core, accept-to-map, reject, cancel, move), `joiningPoints.js` (flight-status landed).

### לקוח
- `src/utils/stripFlow.ts` - **טהור**: `flowChain(events)` (שלבים עיקריים לשרשרת), `flowEventText`, `flowCurrentText`.
- `src/utils/stripFlowBus.ts` - `openStripFlow(id?)` / `subscribeStripFlow`.
- `src/components/strips/StripFlowWindow.tsx` - החלון.

## 6. מקרי קצה

| מקרה | התנהגות |
|---|---|
| פ"מ ותיק בלי אירועים | "נוצר" + "נמצא עכשיו" בלבד |
| פ"מ נמחק | לא מופיע ברשימה המרוכזת (היא נגזרת מהפ"מים החיים); האירועים נשארים ב-DB |
| קבלה עם מיזוג לאח | `accepted` נרשם על הנכנס + `merged` על האח עם `sourceStripId` - ה-FLOW של האח מציג את שניהם |
| קבלה אוטומטית | `accepted` עם `mode: auto`, בלי משתמש |
| חלק מהמטוסים מסיעים | אירוע אחד עם `aircraft: [1,2]` |
| סביבת תרגול | הכל פר-סכמה אוטומטית (`pool`) |
| מצב צפייה (peek) | קריאה בלבד - עובד |
