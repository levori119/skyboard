# ביטול פעולה (CTRL+Z) - אפיון

> **מעכשיו:** **פעולה** = בקשת כתיבה אחת של מפעיל אל השרת. זו יחידת הביטול.
> **יומן הביטול** = הרישום של מה שהשתנה ב-DB בעקבות פעולה, לצורך החזרתה.

---

## 1. מה הפיצ'ר עושה

הפקח/בקר לוחץ **CTRL+Z**. המערכת מציגה **מה** עומד להתבטל, **מתי** נעשה
ו**מה הסיכון**; אחרי אישור - הביטול מתבצע. לחיצה נוספת מבטלת את הפעולה
שלפניה, וכן הלאה, עד גבול **5 דקות** אחורה.

זהו התחליף הדיגיטלי ל"מחקתי בפלנלית משהו שלא התכוונתי" - הפעולה היחידה
שבסדק הפיזי הייתה **בלתי הפיכה לחלוטין**.

---

## 2. שלוש ההחלטות שנקבעו

| החלטה | הנבחר | למה |
|---|---|---|
| **היקף** | **רק הפעולות שלי, מהעמדה שלי** | בקר לא יכול לדרוס בשקט פעולה של הפקח. המחסנית היא פר-**מושב עמדה** (`station_sessions`), לא פר-משתמש: אותו אדם בשתי עמדות מקבל שתי מחסניות נפרדות |
| **רשימת חסימה** | **העברות עמדה + GAPI חסומות** | אי אפשר "לבטל שידור" שכבר יצא לשו"ב החיצוני, וביטול העברה שבוצעה עלול להשאיר מטוס בלי בקר או עם שניים |
| **תצוגה אישית** | **לא נכללת** | גרירת חלון או שינוי רוחב עמודה לא "מזהמים" את המחסנית. CTRL+Z תמיד מכוון לפעולה התפעולית האחרונה |

---

## 3. איך זה עובד - שלוש שכבות

```
    לקוח                         שרת                          DB
┌──────────────┐        ┌────────────────────┐        ┌──────────────────┐
│ יירוט fetch  │─POST──▶│ middleware פעולה   │        │  טריגר גנרי      │
│ (כמו אימות/  │        │ • מזהה פעולה (uuid)│        │  על כל טבלה      │
│  סביבה/נתק)  │        │ • תווית קריאה      │        │  ↓                │
│              │        │ • רשימת חסימה      │        │  undo_journal    │
│ מחסנית מקומית│◀─hdr───│                    │        │  before/after    │
│              │        │ pool: SET LOCAL    │───────▶│  לכל שורה        │
│ CTRL+Z       │        │   app.action_id    │        │                  │
│    ↓         │        │                    │        │                  │
│ חלון אישור   │─POST──▶│ מנוע הביטול        │───────▶│  היפוך בסדר הפוך │
└──────────────┘  undo  └────────────────────┘        └──────────────────┘
```

### 3.1 למה טריגר ב-DB ולא קוד בכל endpoint

במערכת **~340 endpoints שכותבים**. מימוש "לכל פעולה יש פעולה הפוכה" היה מחייב
לגעת בכולם, ולהשאיר את מי שנשכח **בלי ביטול ובלי שאיש ידע**. אותו שיקול בדיוק
שהוביל ליירוט ה-fetch באימות (`utils/authToken.ts`), בסביבה (`utils/environment.ts`)
ובנתק (`offline/apiFetch.ts`) - **נקודת חנק אחת, לא מאות אתרי קריאה**.

הטריגר תופס גם את מה ש-endpoint לא יודע שהוא עשה: `ON DELETE CASCADE`, עדכון
גורף, טריגר אחר. אלה בדיוק המקרים שבהם ביטול ידני היה משאיר שאריות.

**התקדים קיים:** `server/db/versionedTables.js` כבר מתקין פונקציית plpgsql
ב-`public` ומחיל אותה על כל סכמות התרגול. יומן הביטול הולך באותו תלם.

### 3.2 למה `app.action_id` ולא `txid`

פעולה אחת = בקשת HTTP אחת, אבל handler טיפוסי מריץ **כמה** `pool.query` -
כל אחת בטרנזקציה משלה. `txid_current()` היה מפצל פעולה אחת לחמישה ביטולים
נפרדים. `SET LOCAL app.action_id` נושא את **אותו** מזהה על פני כל השאילתות
של אותה בקשה, והטריגר קורא אותו ב-`current_setting('app.action_id', true)`.

**אין הקשר פעולה → אין רישום ביומן.** זה מה שמשאיר את המסלול המהיר נקי:
קליטת תמונ"א, polling של GAPI, `initDb`, `seedDb` ומיגרציות אינם כותבים
ליומן ואינם משלמים דבר.

### 3.3 מלכודת ה-pooler (קריטי)

`DATABASE_URL` מצביע על ה-pooler של Neon (pgbouncer, transaction mode). לכן:

- `SET` רגיל לא שורד בין טרנזקציות → **חייב** `SET LOCAL` בתוך `BEGIN`.
- החלפת `client.query` מוציאה את ה-bookkeeping של node-postgres מסנכרון
  וגורמת ל-`SET LOCAL` **לדלוף** לרמת ה-server connection. זה כבר קרה כאן עם
  `search_path` (ראה ההערה ב-`server/db/pool.js`), ולכן:
  **`wrapClientForSchema` נשאר העטיפה היחידה** - היא מקבלת גם את מזהה הפעולה,
  ואין עטיפה שנייה.
- העטיפה מציבה את המזהה **תמיד** - ערך אמיתי או מחרוזת ריקה. כך שורה שנכתבת
  ב-connection שהחזיק ערך ישן לא תיתפס בטעות לפעולה של מישהו אחר.

---

## 4. רשימת החסימה

מוקד אחד, `UNDO_DENYLIST` ב-`server/db/undoJournal.js`, בסגנון טבלת ה-RULES של `middleware/auth.js`:
**נימוק כתוב לכל שורה**. פעולה חסומה אינה נרשמת ביומן מלכתחילה.

| קבוצה | טבלאות | נימוק |
|---|---|---|
| **בטיחות תפעולית** | `strip_transfers`, `provisional_transfer_points`, `position_merges` | ביטול העברה שבוצעה משנה מי מחזיק במטוס. דורש `/transfer-logic` ואישור מפורש |
| **בטיחות תפעולית** | `temp_zone_seizures`, `temp_zone_seizure_targets` | ביטול שקט של **הלאמת אזור זמני** מחזיר לאוויר מרחב שעמדות כבר אישרו שנתפס, וביטול אישור הוא אישור שלא היה |
| **יצא החוצה** | `gapi_outbox`, `gapi_inbound_events`, `gapi_env_config` | שידור שכבר יצא לשו"ב החיצוני אינו ניתן להשבה |
| **יומן ביקורת** | `activity_log`, `undo_actions`, `undo_journal` | יומן שניתן לבטלו אינו יומן |
| **זהות ומושב** | `station_sessions`, `workstation_session_roles`, `crew_member_workstations` | כניסה/יציאה מעמדה אינה "פעולה על מידע שדה" |
| **תצוגה אישית** | `workstation_personal_filters`, `strip_window_layouts`, `strip_window_columns`, `strip_window_cells` | ההחלטה בסעיף 2 |
| **למידה/מטמון** | `learned_digits`, `learned_strokes`, `air_picture_config` | נתוני אימון ותצורת מאגר חיצוני, לא מידע שדה |

> `strip_window_*` נחסמות **כטבלאות** ולא כמסך: אותה טבלה משמשת גם את מסך
> הניהול. אם יתברר שרוצים ביטול לעריכת פריסה במסך הניהול - זו הסרה של שורה
> אחת מהרשימה, לא שינוי ארכיטקטורה.

---

## 5. מטריצת המקרים

| # | מקרה | התנהגות |
|---|---|---|
| 1 | CTRL+Z והמחסנית ריקה | הודעה קצרה: "אין פעולה לביטול" |
| 2 | פעולה בת פחות מ-5 דק' | חלון אישור → ביצוע → הודעת הצלחה |
| 3 | פעולה בת יותר מ-5 דק' | אינה במחסנית. פגה - היומן שלה כבר נמחק |
| 4 | הפוקוס בשדה טקסט / textarea | **לא מיירטים.** ה-undo של הדפדפן בשדה גובר |
| 5 | הפוקוס בקנבס ציור (סרגל ציור, InkPad) | ה-undo המקומי של הציור גובר - הוא כבר קיים |
| 6 | מפעיל אחר שינה את אותה שורה אחרי הפעולה | **אזהרת התנגשות** בחלון האישור, עם שם המשנה והשעה. אישור = דריסה מודעת, ונרשמת ב-`activity_log` |
| 7 | השורה נמחקה בינתיים | ביטול UPDATE על שורה שאיננה - מדווח "הרשומה כבר לא קיימת", הפעולה יורדת מהמחסנית |
| 8 | הפעולה נגעה גם בטבלה חסומה | הפעולה כולה אינה ניתנת לביטול. חלון האישור מציג את הנימוק |
| 9 | שני ביטולים ברצף | כל אחד עם אישור נפרד. הביטול השני מכוון לפעולה שלפני |
| 10 | פעולה שנגעה בכמה טבלאות | הכל בטרנזקציה **אחת**, בסדר הפוך (בן נמחק לפני אב) |
| 11 | הביטול נכשל באמצע | `ROLLBACK` מלא. הפעולה **נשארת** במחסנית, מוצגת שגיאה |
| 12 | עבודה בנתק | פעולה שיושבת ב-outbox אינה ניתנת לביטול עד שסונכרנה. מוצג "ממתין לסנכרון" |
| 13 | סביבת תרגול | תרגול לא רואה ולא מבטל פעולות של סביבה טסה - אבל **לא** בזכות סכמה נפרדת: היומן יושב ב-`public` ונושא עמודת `env`, והמחסנית מסננת לפיה (`WHERE a.env = $1` ב-`server/routes/undo.js`). לכן שתי הטבלאות מוחרגות מסקופ הסביבות ב-`env-tables.js` - שכפול ל-`env_NN` היה מפצל את היומן לשני עותקים (REFACTOR_LOG #045) |
| 14 | אותו אדם בשתי עמדות | שתי מחסניות נפרדות (המפתח הוא מושב העמדה) |
| 15 | הביטול עצמו | נרשם ב-`activity_log` (`event_type = 'undo'`) - **מי** ביטל **מה** ומתי |
| 16 | ריענון הדף / נפילת עמדה | המחסנית נטענת מהשרת (`GET /api/undo/stack`) ולא הולכת לאיבוד |

---

## 6. מודל הנתונים

```
undo_actions                          undo_journal
─────────────                         ────────────
id            TEXT PK  (uuid)         id         BIGSERIAL PK
created_at    TIMESTAMPTZ             action_id  TEXT  → undo_actions
station_key   TEXT      ← המחסנית     table_name TEXT
crew_member_id INTEGER                pk         JSONB   מפתח ראשי
crew_name     TEXT                    op         CHAR(1) I/U/D
method        TEXT                    before     JSONB   NULL ב-INSERT
path          TEXT                    after      JSONB   NULL ב-DELETE
label_key     TEXT      ← מפתח i18n   at         TIMESTAMPTZ
label_params  JSONB
status        TEXT      active/undone/expired
undone_at     TIMESTAMPTZ
kind          TEXT      action/undo
```

**שתי טבלאות ולא אחת:** התווית, הזהות והסטטוס שייכים ל**פעולה**; ה-before/after
שייכים ל**שורה**. פעולה אחת נוגעת בדרך כלל בכמה שורות.

**5 דקות:** ניקוי עצל - כל כתיבת פעולה מוחקת קודם את מה שעבר זמנו. אין cron,
אין תהליך רקע, ואין יומן שגדל בלי גבול.

---

## 7. התווית שהמשתמש רואה

`server/undo/labels.js` ממפה `METHOD + path` לתווית קריאה, בדיוק כמו שטבלת
ה-RULES ב-`auth.js` ממפה נתיב להרשאה:

```js
{ m: 'DELETE', p: /^\/api\/strips\/\d+$/, key: 'undo.deleteStrip' }
```

התווית היא **מפתח i18n** ולא מחרוזת - היא מוצגת בעברית ובאנגלית, וניתנת
לשינוי בלי build. נתיב שאין לו תווית נופל ל-`undo.genericChange` עם שם
הישות שנגזר מהנתיב. **פעולה בלי תווית עדיין ניתנת לביטול** - היא לא נעלמת.

---

## 8. מה מחוץ להיקף (מכוון)

- **CTRL+Y / חזרה קדימה (redo)** - לא נתבקש. הביטול עצמו רץ **בלי** הקשר פעולה
  ולכן אינו נרשם ביומן: אחרת כל ביטול היה פעולה חדשה שאפשר לבטלה, וזו כבר
  חזרה קדימה במסווה. מה שכן נשמר הוא שורת ה-`activity_log` (מי ביטל מה),
  ו-`before`/`after` של הפעולה המקורית - די בשניהם כדי לבנות redo כשיתבקש.
- **ביטול פעולה של מפעיל אחר** - נחסם בהחלטה שבסעיף 2.
- **ביטול חוצה-סביבות** - היומן פר-סכמה.
