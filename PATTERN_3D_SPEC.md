# הקפה תלת מימד - אפיון טכני

> מקור: מסמך "הקפה תלת מימד" (אורי לב). עמדת יעד: **מגדל (TWR)** - `GroundView`.
> ענף: `feature/pattern-3d` · worktree: `../skyboard-pattern-3d`

---

## 0. למה בכלל, ומה מסוכן בזה

הפקח רואה היום את ההקפה **מלמעלה** - מלבן שטוח. הגובה קיים רק כמספר בטבלת
בלוקי הגבהים של נקודת ההצטרפות. השאלה התפעולית "מי מעל מי, ומי נכנס לאן"
נענית בראש של הפקח, לא במסך.

התצוגה התלת מימדית עונה עליה במבט אחד - אבל **מחקר הנדסת אנוש של ATC מזהיר
שתצוגת 3D היא דו-משמעית**: אדם אינו מסוגל לאמוד מרחק וגובה מהיטל פרספקטיבי
בלי רמזי עומק. לכן ארבעת אלה אינם קישוט אלא **תנאי לתקינות התצוגה**:

| רמז עומק | למה חובה |
|---|---|
| **צל ההקפה על הקרקע** | מראה את המיקום האופקי האמיתי, בלי הטיית הגובה (וזה גם מה שמופיע בתמונת הייחוס) |
| **קווי הורדה אנכיים** (מטוס → קרקע) | הופך "איפה זה באוויר" לשאלה של שתי נקודות ידועות |
| **רשת קרקע** | סרגל מרחקים; בלעדיה הטיה נראית כמו הזזה |
| **ציר גבהים עם תוויות ברגל** | הגובה נקרא כמספר, לא נאמד בעין |

**כלל על:** התלת מימד הוא **תצוגה נוספת**, לא מחליף. כל מספר שקריטי לפעולה
(גובה, או"ק) מופיע כטקסט - לעולם לא רק כמיקום במרחב.

---

## 1. הכרעת מימוש: SVG עם היטל משלנו, לא three.js

| | SVG + היטל ידני (**נבחר**) | three.js / R3F |
|---|---|---|
| תלות חדשה | **אפס** | ~600KB + שרשרת אספקה לביקורת אבטחה |
| טקסט (או"ק, גבהים) | `<text>` מקורי - חד, `tr()`, מותאם תמה | דורש `CSS2DRenderer` או sprites |
| התאמת תמה/סקייל | אותו מנגנון של שאר השכבות | עולם צבעים נפרד |
| בדיקות | vitest על פונקציות טהורות | דורש WebGL headless |
| הסתרה (visibility) | סדר ה-DOM **הוא** אלגוריתם הצייר | z-buffer בחומרה |
| מתאים ל | סצנה של עשרות פוליגונים | אלפי משולשים, תאורה, טקסטורות |

הסצנה כאן היא קו הקפה, ערימת לוחות, וכמה מטוסים - **עשרות פוליגונים**.
אלגוריתם הצייר (מיון לפי עומק, ציור מהרחוק לקרוב) מספיק לחלוטין, וסדר
האלמנטים ב-SVG מיישם אותו בחינם.

**היטל אורתוגרפי ולא פרספקטיבי** - בפרספקטיבה אותו מרווח גבהים נראה גדול
מקדימה וקטן מאחור, וזו בדיוק הטעות שהמחקר מזהיר מפניה. אורתוגרפי שומר על
קנה מידה קבוע, ולכן שני בלוקים במרחק 1000 רגל נראים תמיד באותו מרחק.

---

## 2. מתמטיקה - `src/utils/pattern3d.ts` (מודול טהור, נבדק)

### מרחב העולם

| ציר | משמעות | יחידה |
|---|---|---|
| `x`, `y` | המישור האופקי, **מרחב iso** של `trafficPattern.ts` | אחוז מ**גובה** התמונה (`x_iso = x_pct * aspect`) |
| `z` | גובה | **רגל AGL** (0 = פני השדה) |

`z` מומר ליחידות עולם ב-`zScale` (ראה §2.3), כדי ש-`x/y/z` יהיו באותו קנה מידה.

### 2.1 המצלמה

```ts
export interface Camera3D {
  /** סיבוב סביב הציר האנכי, מעלות. 0 = צפון למעלה. */
  yaw: number;
  /** גובה המצלמה מעל מישור הקרקע, מעלות. 90 = מבט על (מפה), 0 = גובה עיניים. */
  tilt: number;
  /** זום. 1 = הסצנה ממלאת את המסגרת. */
  zoom: number;
}

export const DEFAULT_CAMERA: Camera3D = { yaw: 0, tilt: 55, zoom: 1 };
export const TILT_MIN = 5, TILT_MAX = 90, TILT_STEP = 5;
export const ROT_STEP = 15;
export const ZOOM_MIN = 0.4, ZOOM_MAX = 4;
```

`tilt = 90` **חייב** להחזיר בדיוק את מבט-העל המוכר - זה מה שהופך את המעבר
לתלת מימד לרציף ולא למסך זר.

### 2.2 ההיטל

```ts
export interface Projected { x: number; y: number; near: number }

export function project(p: Vec3, cam: Camera3D): Projected {
  const yr = cam.yaw * RAD, tr_ = cam.tilt * RAD;
  const cy = Math.cos(yr), sy = Math.sin(yr);
  const rx = p.x * cy - p.y * sy;          // סיבוב סביב הציר האנכי
  const ry = p.x * sy + p.y * cy;
  const st = Math.sin(tr_), ct = Math.cos(tr_);
  return {
    x: rx,
    y: ry * st - p.z * ct,   // y במסך גדל כלפי מטה, ולכן גובה מחסיר
    near: p.z * st - ry * ct, // גדול = קרוב למצלמה. מיון עולה = ציור מהרחוק
  };
}
```

**אימות (חובה בבדיקות):**
- `tilt=90` → `{x: rx, y: ry}` = בדיוק מבט-על. `near = z` (הגבוה קרוב יותר) ✓
- `tilt=0` → `{x: rx, y: -z}` = חתך אנכי, הגובה כלפי מעלה במסך ✓
- `yaw=0, tilt=90` → פונקציית הזהות על `x,y` ✓

### 2.3 קנה מידה אנכי ומסגור

```ts
/** פרישת הגובה ביחידות עולם. הסצנה נבנית כך שכל הגובה תופס את זה. */
export const VERT_SPAN = 45;
/** zScale = VERT_SPAN / maxAltFt - הגובה הגבוה בסצנה נוגע בתקרה. */
export function zScaleFor(maxAltFt: number): number;
```

**המסגור לא מחושב מחדש בכל סיבוב.** מחשבים פעם אחת מרכז הסצנה (`centroid`)
ורדיוס (`max |p - center|`), ואז `viewBox` הוא ריבוע ברוחב `2R / zoom` סביב
היטל המרכז. כך סיבוב לעולם אינו משנה את הקנה מידה - אחרת הסצנה "נושמת" בכל
תזוזה של היד וזה בלתי נסבל למעקב.

```ts
export function sceneBounds(pts: Vec3[]): { center: Vec3; radius: number };
export function viewBoxFor(center: Vec3, radius: number, cam: Camera3D, pan: Pt): string;
```

### 2.4 פרופיל הגבהים של ההקפה

`patternPoints()` מחזיר 6 נקודות בסדר הטיסה. הפרופיל התלת מימדי מוסיף **נקודה
שביעית**: נקודת תחילת הנמכה על ה"עם הרוח", **מול הסף** - שם מתחילה ההנמכה
בטיסה אמיתית ("abeam the numbers"), ולכן שם היא צריכה להתחיל גם בציור.

| # | נקודה | גובה |
|---|---|---|
| 0 | קצה המסלול (המראה) | `0` |
| 1 | סוף אחרי-המראה | `downwindAlt * 0.5` (טיפוס) |
| 2 | סוף צולבת = תחילת עם-הרוח | `downwindAlt` |
| 2b | **מול הסף** על ה"עם הרוח", בשבר `(rwyLen + upwind) / (rwyLen + upwind + baseExt)` | `downwindAlt` |
| 3 | סוף עם-הרוח | `baseAlt` |
| 4 | סוף בסיס | `baseAlt` |
| 5 | סוף פיינל = הסף | `0` |

```ts
export interface PatternAltProfile { downwindAlt: number; baseAlt: number }
export const DEFAULT_ALT_PROFILE: PatternAltProfile = { downwindAlt: 3000, baseAlt: 1500 };

/** צומתי ההקפה התלת מימדית, בסדר הטיסה. */
export function patternPath3D(g: PatternGeometry, aspect: number, prof: PatternAltProfile): Vec3[];

/** גובה AGL של מטוס שיושב על צלע נתונה בשבר נתון - לפי אותו פרופיל. */
export function altOnLeg(g: PatternGeometry, prof: PatternAltProfile, leg: LegKey, frac: number): number;
```

`altOnLeg` **חייב** להיגזר מאותו פרופיל שמצייר את הקו, אחרת המטוס מרחף מעל
הקו או שוקע מתחתיו - וזה נראה כמו תקלת נתונים.

---

## 3. שינויי DB

### 3.1 גבהי ההקפה - על ההקפה, לא על השדה

האפיון אומר "פרמטר **בהקפה** בניהול שדה תעופה". לשדה יש כמה הקפות (מסלול לכל
כיוון, הקפת ימין והקפת שמאל), ולכל אחת גובה משלה. לכן העמודות על
`airfield_patterns`:

```sql
ALTER TABLE airfield_patterns ADD COLUMN IF NOT EXISTS downwind_alt_ft INTEGER;
ALTER TABLE airfield_patterns ADD COLUMN IF NOT EXISTS base_alt_ft INTEGER;
```

`NULL` = לא הוגדר → נופל לברירת המחדל **3000 / 1500 רגל AGL** בקוד
(`DEFAULT_ALT_PROFILE`), לא ב-DDL. ברירת מחדל בקוד מאפשרת לשנות אותה בלי
מיגרציה, ומבדילה "לא הוגדר" מ"הוגדר במקרה לאותו ערך".

### 3.2 גובה פני השדה

בלוקי הגבהים של נקודת ההצטרפות הם **גובה מוחלט** (`alt_min_ft`/`alt_max_ft`,
מוצג "040"), וגבהי ההקפה הם **מעל פני השדה**. בלי גובה השדה אי אפשר לשים את
שניהם על אותו ציר.

```sql
ALTER TABLE airfields ADD COLUMN IF NOT EXISTS elev_ft INTEGER;
```

`NULL` → 0. הפונקציה `aglOf(altFt, elevFt) = altFt - (elevFt ?? 0)`.

> `TIMESTAMPTZ` בלבד, `ADD COLUMN IF NOT EXISTS` בלבד, **אף פעם לא `DROP`** -
> ה-worktrees חולקים DB אחד.

### 3.3 נקודות מגע בשרת

| קובץ | שורות | מה |
|---|---|---|
| `server/db/init.js` | אחרי `:1424` | שתי העמודות של `airfield_patterns` |
| `server/db/init.js` | אחרי `:534` | `airfields.elev_ft` |
| `server/routes/airfield.js` | `:757` POST, `:770` PUT, `:795` duplicate | `downwind_alt_ft`, `base_alt_ft` |
| `server/routes/airfield.js` | `:66/:78` POST, `:138` PUT, `:222` duplicate | `elev_ft` |
| GET-ים | - | `SELECT *` - זורם לבד |

---

## 4. ניהול שדה תעופה - שדות הקלט

### 4.1 בעורך ההקפה (`src/components/admin/PatternsSection.tsx`)

שני שדות מספריים: **גובה עם הרוח (רגל AGL)** ו**גובה בסיס (רגל AGL)**.
`placeholder` = `3000` / `1500` כדי שברירת המחדל תהיה **גלויה** ולא סוד -
שדה ריק שמתנהג כ-3000 בלי לומר זאת הוא כשל שקט.
עדכון `bodyOf()` `:66` ו-`savePattern()` `:75`.

### 4.2 בטופס השדה (`src/components/admin/ManagementPage.tsx`)

שדה **גובה פני השדה (רגל)**, ליד שם השדה. נקודות: `airfieldForm` `:322`,
`saveAirfield` `:4622`, האיפוסים `:4627`/`:4848`, השכפול `:4652`, ה-JSX `:4813`.

אימות: מספר שלם, `0..15000`, ריק מותר.

---

## 5. UI - מה נבנה

### 5.1 כפתור 3D בסרגל

בפאנל השכבות של המפה (`GroundView.tsx:2409`), **בשורת פקדי הזום** (`:2514`) -
כי זו בקרת **מבט**, אחות של הזום, ולא שכבת תוכן. כפתור `data-testid="pattern-3d-toggle"`,
מסומן כשפעיל (`data-active`).

מצב: `const [show3D, setShow3D] = useState(false)` ב-`GroundView`.

### 5.2 סצנת התלת מימד - `src/components/ground/Pattern3DScene.tsx`

מכסה את שטח המפה כשהמצב פעיל, רקע אטום (שמיים/קרקע לפי תמה). **התצלום
האווירי אינו מוטה** - עיוות ראסטר בפרספקטיבה הופך תוויות לבלתי קריאות, ובשביל
ההתמצאות מספיקים מתאר המסלול, צל ההקפה ורשת הקרקע.

סדר הציור (= סדר ה-DOM = אלגוריתם הצייר), מהרחוק לקרוב:

1. **רקע** - גרדיאנט שמיים/קרקע לפי תמה
2. **רשת קרקע** `z=0`, קווים דקים, שקופים
3. **המסלול** - מרובע מ-`airfield_runways` (`start_*_pct`/`end_*_pct`) ב-`z=0`
4. **צל ההקפה** - מתאר ההקפה ב-`z=0`, כהה ומקווקו
5. **קווי הורדה** מכל צומת של ההקפה אל הצל שלו
6. **ההקפה התלת מימדית** - `polyline` דרך צומתי `patternPath3D`, בצבע ההקפה,
   עם תוויות הצלעות (`LEG_LABEL_KEYS`)
7. **נקודות הצטרפות** - ראה §5.3
8. **קווי חיבור** מטוס-בבלוק → מרכז ה"עם הרוח" שלו
9. **מטוסים** - ממוינים לפי `near` עולה
10. **ציר גבהים** - סרגל אנכי בקצה עם תוויות רגל, ו**חץ צפון**

כל האלמנטים שמעורבבים בעומק (7-9) ממוינים יחד לפי `near` לפני הציור.

### 5.3 נקודת הצטרפות בתלת מימד

- **תורן אנכי** מהקרקע עד הבלוק העליון + עיגול על הקרקע (העוגן)
- כל בלוק מ-`buildBlocks(point)` = **לוח אופקי** (מרובע מוטל) בגובה
  `aglOf(blockFt, elev)`
- **מאויש מול פנוי** אינו נבדל בצבע בלבד: פנוי = מתאר בלבד, קווקו, שקוף;
  מאויש = מילוי מלא + מסגרת + תווית. (FAA HF-STD-001: צבע לעולם לא ערוץ יחיד)
- בבלוק מאויש: **או"ק + מספר במבנה**, ואם יש - הטייסת. אותו `label` שמחושב
  היום ל-`PatternAircraftRow`
- **צבע הנקודה** (`point.color`) נשמר - זה חריג "צבע שמזהה ישות" מ-CLAUDE.md
- קונפליקט (`conflictBlocks`) - אותו אדום ואותו ⚠ שבטבלה. **אותה לוגיקה,
  לא לשכפל**: לייבא מ-`utils/joiningPoints.ts`

### 5.4 מטוסים

| מצב | מקור | מיקום |
|---|---|---|
| **על צלע** (`in_pattern`, `flight_status`) | `legForFlightStatus` + `pattern_frac` + `spreadFracs` | `legPoint` אופקית, `altOnLeg` אנכית |
| **בבלוק בנקודת הצטרפות** | `formationsInBlocks` | על לוח הבלוק |

**חובה לייבא את `legForFlightStatus`, `legPoint`, `spreadFracs`, `FONT`
מ-`PatternAircraftLayer.tsx` ואת `buildBlocks`/`formationsInBlocks`/`conflictBlocks`
מ-`utils/joiningPoints.ts`.** אסור לשכפל את הלוגיקה - מטוס חייב לשבת על אותה
צלע ובאותו בלוק בשתי התצוגות, אחרת התלת מימד "משקר" מול הטבלה.

לכל מטוס: קו הורדה אנכי אל הקרקע + תווית או"ק שתמיד מקבילה למסך (לא מסובבת).

### 5.5 קו החיבור לעם הרוח

לפי האפיון: ממטוס שיושב בבלוק, **קו אל מרכז ה"עם הרוח" של ההקפה שנבחרה לו**
(`joining_point_aircraft.pattern_id`). מקווקו ובצבע ההקפה - זה "לאן הוא הולך",
לא "איפה הוא נמצא", והקווקו הוא שמבדיל.

אין `pattern_id` → אין קו. **לא לנחש** הקפה.

### 5.6 חלון הפקדים - `src/components/ground/Pattern3DControls.tsx`

חלון צף נגרר, `windowFrame('view', themeMode)` (תורכיז - צפייה ותפעול).

| פקד | פעולה | testid |
|---|---|---|
| TILT ▲ / ▼ | `tilt ± 5°`, נעצר ב-`5..90` | `tilt-up` / `tilt-down` |
| סיבוב ◀ / ▶ | `yaw ∓ 15°` | `rot-left` / `rot-right` |
| **מרכוז** | `DEFAULT_CAMERA` + `pan = 0` | `recenter` |
| זום + / − | `zoom × 1.25` | `zoom-in` / `zoom-out` |
| חץ צפון | תצוגה בלבד, מסתובב לפי `yaw`/`tilt` | `north-arrow` |
| סגירה | מכבה את מצב ה-3D | `close` |

**חץ הצפון** מצויר מהיטל הווקטור `(0,-1,0)` - הוא גם מסתובב עם `yaw` וגם
מתקצר עם `tilt`, וזה בדיוק הרמז שאומר "כמה שטוח אני מסתכל".

הצגת המספרים: `TILT 55°` / `HDG 015°` כטקסט. פקח לא אומד זווית בעין.

### 5.7 גרירה - Pointer Events בלבד

- גרירה על הסצנה = **סיבוב** (`yaw` מ-Δx, `tilt` מ-Δy)
- Shift+גרירה = הזזה (`pan`)
- גלגלת = זום

**`touchAction: 'none'` + `setPointerCapture` + חלוקה ב---s** - שלוש המלכודות
מ-CLAUDE.md §גרירה. לא `onMouseDown`, לא `mousemove` - הם לא נשלחים באצבע,
והעמדה היא Cintiq בעט.

---

## 6. i18n

מרחב שמות חדש `src/i18n/registry/pattern3d.json` (he+en), רישום ב-
`src/i18n/registry.ts` (`:9-32` + `:37-40`) וב-`src/i18n/index.ts:22-24`.

מפתחות: `title, tiltUp, tiltDown, rotLeft, rotRight, recenter, zoomIn, zoomOut,
north, occupied, vacant, altAxis, downwindAlt, baseAlt, fieldElev, agl, ft, blocks`.

תוויות צלעות - **לעשות שימוש חוזר** ב-`pattern.leg*` הקיימות. גבהים/מצבים -
שימוש חוזר ב-`joining.*`.

> `tr('ns.key')` בלבד · CSS logical properties בלבד · **מקף רגיל `-`, לא `—`**

---

## 7. תמה וסקייל

`themeMode: 'light' | 'dark' | 'ocean'` - **ocean היא תמה כהה**. אובייקט `C`
מקומי בדיוק כמו `JoiningPointPanel.tsx:114`. צבעי סטטוס (קונפליקט אדום, מאויש)
**קבועים בשלוש התמות** - הם נושאים משמעות.

סקייל: `useMapLabelScale()` לתוויות בתוך הסצנה, `useToolbarScale()`/`tbPx`
לכפתורים בחלון הפקדים. פונטים ועובי קו כפולה של `sz` כמו בשאר השכבות.

---

## 8. בדיקות (TDD - קודם אדום)

**`src/utils/pattern3d.test.ts`** (ליבה):
- `project` ב-`tilt=90` = מבט-על מדויק; `tilt=0` = חתך אנכי
- מיון `near` - הגבוה קרוב יותר במבט-על; הקרוב למצלמה קרוב יותר בחתך
- `patternPath3D` - 7 צומתים, מתחיל ומסתיים ב-`z=0`, שיא ב-`downwindAlt`,
  צלע הבסיס **מפולסת** ב-`baseAlt`
- ברירת מחדל 3000/1500 כשאין פרמטרים; ערך שהוגדר גובר
- `altOnLeg` עקבי עם `patternPath3D` בכל שבר
- `viewBoxFor` - הרוחב **אינו משתנה** עם `yaw`/`tilt` (מבחן ה"נשימה")
- `aglOf` עם/בלי `elev_ft`

**`Pattern3DScene.test.tsx`**: בלוק מאויש מקבל תווית, פנוי לא; מטוס בלי
`pattern_id` לא מקבל קו חיבור; קונפליקט מסומן.

**אימות סופי:** `npx tsc --noEmit` + `npm test` + `npm run build`.

---

## 9. הגבולות - מה לא נכנס

- אין הטיה של התצלום האווירי (§5.2)
- אין עריכה מתוך התלת מימד - **תצוגה בלבד**. הזזת מטוס בין בלוקים נשארת
  בטבלה, מקום אחד לפעולה
- אין פרספקטיבה, אין תאורה, אין אנימציית מעבר בין 2D ל-3D
- אין שינוי בסכמת `strip_aircraft` / `joining_point_aircraft`
