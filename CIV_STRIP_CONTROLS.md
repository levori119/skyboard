# פקדים בפ"מ סטריפ אזרחי - אפיון מלא

> **מקור:** "עדכון פיצר פמ סטריפים אזרחי" (אפיון הצוות).
> **מסמך זה** מתרגם את האפיון למודל נתונים, למטריצת מקרים ולמקרי קצה - **לפני** המימוש.
> אם יש סתירה בין המסמך לקוד - הקוד קובע, והמסמך מתעדכן.

---

## 0. שלוש ההכרעות שנקבעו לפני התכנון

| # | השאלה | ההכרעה |
|---|---|---|
| 1 | איפה הפקדים יושבים בכרטיס האזרחי? | **עורך פריסה כמו בפ"מ הקלאסי** - הכרטיס האזרחי מקבל עץ `layout_json` (SGCell/SGSplit), ותא = "משבצת" שיכולה להחזיק כמה פקדים |
| 2 | מהו "חלון" לצורך פקד פנימי? | **הלוח האזרחי של עמדה** (`workstation_presets`). מפתח הערך: `(פ"מ, עמדה)` |
| 3 | לאן מגיע ערך של פקד גלובלי? | **לכל המערכת** - שאילתות, עיצוב מותנה, תצוגה קלאסית, מוד טבלה ומעבר בין עמדות |

**ב"מ** במסמך זה = **ברירת מחדל** (הערך שהפקד מציג כשלא נקבע לו ערך).

---

## 1. מהו פקד

**פקד** הוא רכיב על הסטריפ שהמנהל מגדיר, והפקח/בקר משנה בלחיצה. הפקד נושא **ערך**,
והערך הוא שמשפיע על השאילתות, על העיצוב המותנה ועל המעבר בין עמדות.

```
הגדרה (מנהל)            ערך (עמדה)                השפעה
──────────────         ──────────────            ──────────────
סוג, טקסט/ערכים   →    לחיצה משנה ערך      →    שאילתות
ב"מ, היקף              (ציקלי/תפריט/מתג)         עיצוב מותנה
עיצוב מותנה                                       מעבר בין עמדות
```

### 1.1 הגדרה אחת בקטלוג, הצבות רבות

**ההגדרה חיה בקטלוג ב-DB** (`strip_field_defs`) ולא בתוך התבנית. משבצת בסטריפ
ועמודה במוד הטבלה **מפנות** לאותה הגדרה:

```
        strip_field_defs  (הגדרה אחת: סוג, ערכים, ב"מ, היקף, עיצוב מותנה)
                 ▲                    ▲                      ▲
       משבצת בסטריפ         עמודה במוד הטבלה         שדה בשאילתא
    (SGCell.controls[])    (table_modes.columns[])   (ctl__<key>)
```

מכאן: שדה שהוגדר בעורך הסטריפ **נבחר גם בתצוגת הטבלה**, ושדה שהוגדר בטבלה נבחר
בעורך הסטריפ - עם כל מאפייני סוגו. שינוי הגדרה במקום אחד חל בכל המקומות.

**המפתח (`key`) נוצר בשרת ואינו נחשף למנהל.** הוא מזהה טכני (`fld_7`), ומה שהמנהל
רואה ועורך הוא ה**תווית**. רצף ייעודי ב-DB מבטיח ייחודיות בלי מרוץ בין שתי עמדות
שמוסיפות שדה באותו רגע.

**מה כן מקומי להצבה:** רוחב יחסי, גודל גופן והדגשה - כדי שאותו שדה יוכל להיות רחב
במשבצת אחת וצר בשנייה, בלי לשכפל הגדרה.

---

## 2. חמשת סוגי הפקדים

| סוג | ערך מאוחסן | לחיצה בעמדה | מה מגדירים |
|---|---|---|---|
| **כפתור** (`button`) | מחרוזת | הערך הבא ברשימה, מחזורית (הערך האחרון → הראשון) | רשימת ערכים, ב"מ |
| **שדה** (`field`) | מחרוזת, או `data:image/png` בכתב יד | פותח עריכה: מקלדת / כתב יד / שניהם | מצב קלט, ב"מ |
| **דגל** (`flag`) | בוליאני | הופך `TRUE`↔`FALSE` | ב"מ (ריק = `FALSE`) |
| **תפריט יחיד** (`select`) | מחרוזת | תפריט, ערך אחד, **תמיד כולל ערך ריק** | רשימת ערכים, ב"מ |
| **תפריט מרובה** (`multiselect`) | מערך מחרוזות | תפריט, ערך אחד או כמה, **תמיד כולל ערך ריק** (מנקה הכל) | רשימת ערכים, ב"מ |

### 2.1 כפתור - מחזוריות

- **רשימה בת ערך אחד** = כפתור סטטי (לחיצה לא משנה דבר). כך ממומשת הדרישה "ניתן לקבוע טקסט".
- **ערך נוכחי שאינו ברשימה** (המנהל ערך את הרשימה אחרי שנקבע ערך) → הלחיצה הבאה קופצת ל**ערך הראשון**.
- **רשימה ריקה** → הכפתור מוצג עם ה-ב"מ (או ריק) והלחיצה אינה עושה דבר.
- הערך הריק **אינו** נכנס למחזור אוטומטית. מנהל שרוצה מצב ריק במחזור מוסיף ערך ריק לרשימה במפורש.
  (בשונה מתפריט, שם האפיון דורש ערך ריק תמיד.)

### 2.2 שדה - כתב יד

- **מקלדת** - עריכה במקום, בתוך המשבצת.
- **כתב יד** - לחיצה פותחת משטח כתיבה ב**50% מגודל המסך**; שמירה מכווצת חזרה לגודל המשבצת ומציגה את הדיו; לחיצה נוספת פותחת שוב **עם הדיו הקיים** לעריכה.
- **גם וגם** - שתי האפשרויות זמינות באותו פקד.
- כתב יד נשמר כתמונת `data:image/png` בערך עצמו - אותה מוסכמה שכבר קיימת בשדות המותאמים ובהערת העמדה.

### 2.3 תפריט מרובה

- הערך הוא **מערך**. תצוגה: הערכים מופרדים ב-`, `.
- **ערך ריק** בתפריט = ניקוי כל הבחירות (מערך ריק).
- לצורך שאילתות ועיצוב מותנה, כלל ההתאמה הוא **הכלה**: תנאי על ערך `X` מתקיים אם `X` נמצא בין הנבחרים.

---

## 3. ב"מ וכלל "אין NULL"

**אין ערך שאינו קיים.** לכל פקד יש ערך אפקטיבי בכל רגע:

```
ערך אפקטיבי = ערך שמור   →   אם אין: ב"מ   →   אם אין: אפס-הסוג
```

| סוג | אפס-הסוג |
|---|---|
| כפתור / שדה / תפריט יחיד | `''` (מחרוזת ריקה) |
| דגל | `FALSE` (**לעולם לא `NULL`** - דרישה מפורשת באפיון) |
| תפריט מרובה | `[]` (מערך ריק) |

**ה-ב"מ אינו מחומר.** הוא נפתר בזמן קריאה, ולכן:

- שינוי ה-ב"מ בניהול משנה **מיד** את התצוגה של כל פ"מ שלא נגעו בו.
- פ"מ שנגעו בו שומר את ערכו גם אם ה-ב"מ השתנה.
- "מתאפס לב"מ" = **אין שורת ערך**, ולא "נכתב ערך שווה ל-ב"מ".

---

## 4. היקף (scope)

| היקף | איפה הערך יושב | מפתח | מה רואים בחלון אחר |
|---|---|---|---|
| **פנימי לחלון** (`window`) | טבלה `strip_control_values` | `(strip_id, preset_id, control_key)` | **ב"מ** - אין שורה, ולכן נופלים לברירת המחדל |
| **גלובלי לפ"מ** (`global`) | `strips.custom_fields[key]` | `(strip_id, control_key)` | **אותו ערך** בדיוק |

`strips.custom_fields` הוא שדה **פנימי** במפת הפ"מ ([STRIP_MODEL.md](STRIP_MODEL.md) §1.3):
GAPI אינו כותב אליו, ו-upsert נכנס אינו נוגע בו. לכן ערכי הפקדים אינם דולפים לשו"ב
ואינם נדרסים על ידו.

### 4.1 מה נגזר מכל היקף

| | פנימי לחלון | גלובלי |
|---|---|---|
| שאילתות מערכתיות (Query Builder) | ❌ לא - הערך חסר משמעות מחוץ ללוח שלו | ✅ כן, כשדה לכל דבר |
| עיצוב מותנה של הפקד עצמו | ✅ | ✅ |
| תנאי `conditions_json` של הלוח האזרחי | ✅ (בהקשר אותו לוח) | ✅ |
| תצוגה קלאסית / מוד טבלה / מעבר בין עמדות | ❌ | ✅ |

---

## 5. עיצוב מותנה

לכל פקד רשימת כללי עיצוב, **על ערכו של הפקד עצמו**:

| התאמה | משמעות |
|---|---|
| ערך מפורש (`TXI`) | הערך שווה לו (בתפריט מרובה: נמצא בין הנבחרים) |
| `''` | ריק |
| `*` | כל ערך שאינו ריק |
| `true` / `false` | דגל בלבד |

כל כלל נושא: רקע, צבע טקסט, מודגש, הבהוב (צבע וקצב). **הכלל הראשון שמתאים - מנצח**,
כדי שסדר הכללים בעורך יהיה גם סדר העדיפות.

> העיצוב המותנה של הפקד **אינו** מחליף את `conditions_json` של הסטריפ. תנאי סטריפ
> (שאילתא מלאה על הפ"מ) ממשיך לצבוע תא וכרטיס, ופקד גלובלי זמין בתוכו כשדה. שתי
> השכבות מצטברות: תנאי הסטריפ צובע את התא, וכלל הפקד צובע את הפקד שבתוכו.

---

## 6. פריסה: משבצת, כמה פקדים, גרירה

הכרטיס האזרחי מקבל את **אותו** עורך פריסה של הפ"מ הקלאסי
([`StripGridEditor`](src/components/admin/managers.tsx)):

- פיצול אופקי/אנכי → עץ `SGSplit`/`SGCell`.
- **משבצת** = `SGCell`. תא יכול להחזיק **שדה** (כמו היום בקלאסי) **וגם רשימת פקדים**.
- **מיקום הפקד במשבצת נקבע בגרירה שלו בתוך התא בעורך** - x/y באחוזי התא (לפי מרכז הפקד), ואותו חישוב בדיוק מצייר את הכרטיס בעמדה. פקד שלא נגרר יושב בשורת הפקדים לפי סדרו, ו"החזר לשורה" מבטל מיקום חופשי.
- **גודל הפקד** נקבע בגרירת הפינה שלו בעורך, או בהקלדת רוחב וגובה ב**פיקסלים**. ריק = הפקד נמדד לפי תוכנו.
- **שם השדה הוא פריט נפרד מהפקד.** הפקד עצמו מציג **ערך בלבד** (הערך השמור, ובהיעדרו ה-ב"מ, ובהיעדרו ריק); שם השדה מוצג רק אם הודלק (`showLabel`), יושב כברירת מחדל **מעל** הפקד, ונגרר למקומו בנפרד. כותרת בתוך הפקד הייתה דוחקת את הערך מחוץ לתא, וכך לחיצה שכן עבדה נראתה כאילו לא קרה כלום.
- הגרירה בעורך ובכרטיס - **Pointer Events** בלבד (`touchAction:'none'` + `setPointerCapture`),
  כי העמדה היא מסך מגע. ראה CLAUDE.md §גרירה.

> **חוב שנסגר אגב:** הכרטיס האזרחי היום גורר ב-HTML5 `draggable` ובצבעים קשיחים
> (`#c8d8e4`) שאינם מותאמי תמה. המעבר לעץ הפריסה מיישר אותו עם שאר המערכת.

---

## 7. מודל הנתונים

### 7.0 קטלוג השדות - `strip_field_defs`

**מקור האמת היחיד להגדרה.** טבלת **קונפיג** (חיה ב-`public`, משותפת לכל הסביבות),
בעוד ה**ערכים** תפעוליים ומבודדים פר-סביבה.

| עמודה | תיאור |
|---|---|
| `key` | מזהה טכני, נוצר בשרת מרצף `strip_field_key_seq` (`fld_7`). לא נחשף למנהל |
| `label` | מה שהמנהל רואה ועורך |
| `type` | `button` / `field` / `flag` / `select` / `multiselect` |
| `input_mode` | לשדה בלבד: `keyboard` / `handwriting` / `both` |
| `scope` | `window` (פנימי ללוח) / `global` (על הפ"מ) |
| `values_json`, `default_value`, `styles_json` | ערכים, ב"מ, וכללי העיצוב המותנה |

**מיגרציה שרצה פעם אחת** (`migrateFieldDefsToCatalog`, אידמפוטנטית) העבירה לקטלוג
את מה שהיה מוגדר בשני מקומות: פקדים שהוגדרו בתוך `layout_json` (התא הומר להפניה),
ו"שדה חופשי" של מוד טבלה - **במפתח הקיים שלו**, ולכן ערכים שכבר נשמרו ב-
`strips.custom_fields` ממשיכים להיקרא בדיוק כמו קודם.

### 7.1 תבנית הסטריפ האזרחי

**שימוש חוזר** ב-`classic_strip_tables`, שכבר נושאת `layout_json`, `conditions_json`,
`strip_height` ו-`mode`. תבנית **גריד** (`mode='civil'`, "מוד אזרחי" בבורר התבניות)
כבר קיימת ונערכת ב-`StripGridEditor` - ולכן **לא נוסף מוד חדש**:

| שינוי | פירוט |
|---|---|
| `SGCell.controls[]` | הפקדים שבמשבצת, בתוך `layout_json` הקיים |
| `workstation_presets.civilian_strip_table_id` | `INTEGER REFERENCES classic_strip_tables(id) ON DELETE SET NULL` - איזו תבנית גריד הלוח האזרחי מציג. ריק = הכרטיס הקבוע הישן |

### 7.2 ערכי פקדים פנימיים לחלון

```sql
CREATE TABLE IF NOT EXISTS strip_control_values (
  strip_id    INTEGER NOT NULL REFERENCES strips(id) ON DELETE CASCADE,
  preset_id   INTEGER NOT NULL REFERENCES workstation_presets(id) ON DELETE CASCADE,
  control_key VARCHAR(64) NOT NULL,
  value       JSONB,
  updated_at  TIMESTAMPTZ DEFAULT NOW(),
  PRIMARY KEY (strip_id, preset_id, control_key)
);
```

`JSONB` ולא `TEXT`, כי הערך הוא מחרוזת, בוליאני **או מערך** לפי סוג הפקד.
`TIMESTAMPTZ` ולא `TIMESTAMP` - כלל הפרויקט.

### 7.3 ערכי פקדים גלובליים

`strips.custom_fields` (JSONB, כבר קיים). מפתח = `key` של הפקד. אין מיגרציה.

---

## 8. מטריצת מקרים ומקרי קצה

### 8.1 קריאת ערך

| # | מצב | התוצאה |
|---|---|---|
| 1 | יש ערך שמור | הערך השמור |
| 2 | אין ערך, יש ב"מ | ה-ב"מ |
| 3 | אין ערך, אין ב"מ | אפס-הסוג (`''` / `FALSE` / `[]`) |
| 4 | פקד פנימי, פ"מ מוצג בלוח אחר | ב"מ (מקרה 2/3) |
| 5 | ערך שמור שאינו ברשימת הערכים (הרשימה נערכה) | הערך מוצג כמות שהוא; הלחיצה הבאה קופצת לערך הראשון |
| 6 | דגל עם ערך שמור `null` (נתון היסטורי) | `FALSE` |
| 6א | **ערך שנוקה במפורש** (דגל שכובה, תפריט שנבחר בו "ריק") | הערך הריק **נשמר** ואינו נופל חזרה ל-ב"מ. רק "לא נקבע מעולם" נופל ל-ב"מ - אחרת דגל שכיבו היה נדלק שוב בכל רענון |

### 8.2 שינויים בהגדרה (מנהל) מול ערכים קיימים

| # | השינוי | מה קורה לערכים |
|---|---|---|
| 7 | שינוי ב"מ | פ"מ בלי ערך שמור מציג מיד את החדש; פ"מ עם ערך שמור לא מושפע |
| 8 | הוספת ערך לרשימה | אין השפעה על ערכים קיימים |
| 9 | **הסרת ערך מהרשימה** | פ"מ שהחזיק אותו ממשיך להציג אותו (מקרה 5) - **אין מחיקה שקטה של מידע תפעולי** |
| 10 | **החלפת היקף** `window`↔`global` | הערכים **אינם עוברים** בין המחסנים. הפקד מתחיל להציג ב"מ. העורך מזהיר על כך לפני שמירה |
| 11 | שינוי **סוג** שדה שכבר יש לו ערכים | הערכים מנורמלים בקריאה לסוג החדש (`normalizeControlValue`): "TXI" בדגל נקרא `FALSE`, `['A','B']` בשדה טקסט נקרא "A, B". שום ערך לא נמחק |
| 12 | **הסרת ההצבה** מהמשבצת / העמודה | ההגדרה נשארת בקטלוג והערכים נשארים ב-DB. הסרה היא החלטה על התצוגה, לא על התוכן |
| 13 | **מחיקת השדה מהקטלוג** | ההגדרה נמחקת, הערכים **נשארים**. משבצת שמפנה אליו מציגה "שדה שנמחק" בעורך ואינה מציירת דבר בעמדה - עדיף תא חסר על פני פקד שאיש אינו יודע מה הוא |

### 8.3 מחזור חיים של הפ"מ ושל הלוח

| # | האירוע | התוצאה |
|---|---|---|
| 14 | פ"מ נמחק | `ON DELETE CASCADE` מוחק את ערכי החלון שלו |
| 15 | עמדה נמחקת | `ON DELETE CASCADE` מוחק את ערכי החלון של אותה עמדה |
| 16 | פ"מ הוסר מהלוח והוחזר | הערך הפנימי **נשמר** - השיוך הוא מיקום, לא מצב |
| 17 | פ"מ נגרר לעמודה אחרת באותו לוח | הערך הפנימי נשמר (החלון הוא הלוח, לא העמודה) |
| 18 | פ"מ הועבר לעמדה אחרת | ערכים גלובליים עוברים איתו; ערכים פנימיים נשארים בלוח המקורי |
| 19 | upsert נכנס מ-GAPI | אינו נוגע ב-`custom_fields` ולא ב-`strip_control_values` |

### 8.4 עמדה ותפעול

| # | המצב | ההתנהגות |
|---|---|---|
| 20 | שתי עמדות משנות **פקד גלובלי** בו-זמנית | אחרון-כותב-מנצח על ה-`key` בלבד (עדכון נקודתי ב-JSONB, לא דריסת כל ה-`custom_fields`) |
| 21 | שתי עמדות משנות **פקד פנימי** באותו לוח | אותו כלל, על שורת הערך |
| 22 | לחיצה בזמן שהרשת נופלת | הערך חוזר למצב הקודם בתצוגה, והשגיאה מדווחת - **בלי לשקר למשתמש** שהמצב השתנה |
| 23 | לחיצה על פקד בתוך כרטיס שנגרר | הלחיצה אינה מתחילה גרירה (`stopPropagation` על הפקד) |
| 24 | כתב יד: שמירה בלי ציור | הערך מתאפס לריק (ולא נשמרת תמונה לבנה) |

---

## 9. מה **לא** בתכולה הזו

| לא נכלל | למה |
|---|---|
| עריכת פקדים בכרטיסי **העברה** | כרטיס העברה הוא תצלום של פ"מ שנמסר (`transferToSynth`) ולא הפ"מ עצמו - אין לו מזהה לכתוב אליו, ולכן הפקדים בו לקריאה בלבד. בכל שאר המקומות - לוח אזרחי, פ"מ קלאסי, חלון סטריפים ומוד טבלה - הם לחיצים. בתצוגה המקדימה שבעורך הם לחיצים אך **הערכים אינם נשמרים** (שולחן ניסוי) |
| החלפת הגרירה של הכרטיסים בלוח האזרחי | הלוח גורר כרטיסים ב-HTML5 `draggable` (מאז שנבנה), שאינו עובד באצבע. זה **חוב קיים** שאינו נובע מהפיצ'ר הזה, ותיקונו הוא שינוי נפרד. הגרירה ה**חדשה** (סידור פקדים בעורך) ממומשת ב-Pointer Events כנדרש |
| היסטוריית שינויים פר-פקד (Event Log) | לא נדרש באפיון. הערך נושא `updated_at` בלבד |
| שאילתות על פקד **פנימי לחלון** | חסר משמעות מחוץ ללוח שלו (§4.1) |
| סנכרון פקדים ל-GAPI | `custom_fields` הוא שדה פנימי במכוון ([STRIP_MODEL.md](STRIP_MODEL.md) §1.3) |

---

## 10. איפה משנים

| השינוי | הקובץ |
|---|---|
| סוג פקד חדש | [`src/types/stripControls.ts`](src/types/stripControls.ts) |
| לוגיקת ערך (ב"מ, מחזור, התאמת עיצוב) | [`src/utils/stripControls.ts`](src/utils/stripControls.ts) + הבדיקות שלו |
| תצוגת פקד בעמדה | [`src/components/classic/StripControl.tsx`](src/components/classic/StripControl.tsx) |
| הגדרת פקד בעורך | [`src/components/admin/managers.tsx`](src/components/admin/managers.tsx) (`StripGridEditor`) |
| אחסון ערך פנימי | [`server/routes/civilian.js`](server/routes/civilian.js) |
| חשיפה לשאילתות | [`src/utils/queryBuilder.ts`](src/utils/queryBuilder.ts) |
