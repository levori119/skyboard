# SKY-KING — לוג ארגון מחדש (Refactor Log)

> קובץ זה עוקב אחר כל שלב בארגון הקוד מחדש.
> כל שינוי מתועד: מה נעשה, למה, ותוצאת QA.
> עדכון אחרון: 2026-08-31

---

## 2026-08-31 - הלאמת אזור זמני: ליטוש הקו, הצבע והטופס

סבב שני מול המפעיל.

| מה | מה עכשיו | למה |
|----|-----------|-----|
| ב"מ צבע | **אדום** `#ef4444` (היה כתום) | הצבע הראשון שהפקח רואה צריך לומר "נתפס". הוא עדיין ניתן לשינוי - מרחבים שונים באירוע אחד צריכים להיבדל |
| תבנית הקו | **שלוש נקודות ומקף** `···—···—` (היה קו-נקודה) | תבנית שאין לה אח על המפה: גבול אזור `2,1`, אזור סגור `2.5,1.5`, ושניהם קווקו אחיד |
| עובי הקו | `0.9` → **`0.5`** | המרחב מכסה שטח גדול, וקו עבה סביבו מתחרה באזורים ובפ"מים שבתוכו. התבנית והצבע נושאים את המשמעות, לא העובי. גם המילוי הוחלש (`22` → `14`) |
| טופס ההגדרה | **portal ל-body**, מעל הכל | ראה למטה |

### "מעל הכל" אינו מספר גדול יותר

זה השיעור השני מאותו שורש כמו סרגל הציור: אלמנט שקבור בתוך הקשר ערימה של
הורה נצבע מול **אחיו בלבד**, ולא מול העולם. `z-index: 9750` בתוך מכולה שההקשר
שלה נמוך אינו מנצח סרגל שיושב מחוצה לה. הדרך היחידה ש**מבטיחה** "מעל הכל" היא
לצאת מהעץ - `createPortal` ל-`body`, וזו גם התבנית של כל דיאלוג אחר במסך.

### המלכודת שנפתרה תוך כדי: גרירה מחוץ לזום

`#root` נושא `zoom: var(--s)` (1.00 ב-15.6" ועד 1.65 ב-24"), וחלון שיוצא
ב-portal ל-body **אינו** מוגדל. `useDragPosition` ו-`startPointerDrag` מחלקים
ב---s כי הם מניחים שהאלמנט בפנים - ולכן חלון פורטלי היה זז פי `1/1.65` מהיד.
הטופס עבר לגרירה בפיקסלים אמיתיים (הכפלה חוזרת ב---s), ומאותה סיבה `maxHeight`
שלו הוא `86vh` ולא `calc(86vh / var(--s))`.

**זה בדיוק סוג הבאג שאי אפשר לראות בפיתוח:** ב-15.6" הסקייל הוא 1, והכל נראה
תקין. הוא מופיע רק על ה-Cintiq של העמדה.

### QA

| בדיקה | תוצאה |
|-------|--------|
| `tsc --noEmit` | ✅ נקי |
| `vitest run` | ✅ 2,355 עוברות |
| `npm run build` | ✅ |

---

## 2026-08-31 - הלאמת אזור זמני: שבעה תיקונים מהשטח

סבב ראשון מול המפעיל. שבע נקודות, וכולן מאותה משפחה: **המרחב היה מצויר אבל לא
היה אפשר לדבר איתו.**

| # | מה היה | מה עכשיו |
|---|--------|-----------|
| 1 | קו המרחב נראה כמו כל קו מקווקו אחר על המפה | **קו-נקודה** (`—·—·—`). גבול אזור הוא `2,1`, אזור סגור `2.5,1.5`, ומרחב מולאם אינו גבול קבוע אלא אירוע חולף - ההבחנה במבט אחד |
| 2 | "סגור פוליגון" | **"אשר"** |
| 3 | הטופס נפל מתחת לתפריטים ולסרגלי המפה | z-index מעל התפריטים לחלונות הנגררים; ההתראה עברה ל-`portal` ל-body כמו יתר הדיאלוגים |
| 3א | **סרגל הציור** עדיין נפל מתחת לסרגל כלי המפה | לא z-index: הסרגל הועבר לרמת הקינון של סרגל המפה. ראה למטה |
| 4 | לפוליגון לא היה מה לומר | לחיצה על **הקו** או על התווית פותחת את **פרטי ההלאמה** |
| 5 | סגרת את טופס האישורים - נגמר | נפתח מחדש מתוך חלון הפרטים |
| 6 | בטופס האישורים לא היה מושג כמה זמן זה נמשך | **שעון רץ** מרגע ההלאמה |
| 7 | פ"מ נכנס למרחב מולאם בשקט | **התראה לפני הביצוע**: "שייך בכל זאת" / "בטל" |

### למה z-index לא פתר את סרגל הציור

סרגל כלי המפה (זום/איפוס/ניווט) הוא **אח** של מכולת המפה ויושב אחריה ב-DOM.
לכן הוא נצבע מעל **כל** מה שבתוך המכולה - בלי קשר ל-`z-index` של הילדים, כי
הם מסודרים בהקשר הערימה של המכולה ולא מולו. סרגל ציור עם `z-index: 221` בתוך
המכולה נעלם מתחת לסרגל עם `z-index: 100` שמחוצה לה.

הפתרון אינו מספר גדול יותר אלא **אותה רמת קינון**: `SeizureDrawToolbar` הופרד
מ-`SeizureDrawLayer` ומרונדר ליד סרגל המפה ב-`MAP_TOOLBAR_GAP`, בדיוק כמו
פאנל הסגירות. מכאן שהפוליגון שבציור עלה למצב של העמדה - שני הרכיבים צופים בו.

### הקו נשאר פקד, ולא הפך למלכודת

הפיתוי היה להפוך את כל הפוליגון ללחיץ. זה היה **חוסם גרירת פ"מ** אל המרחב -
ובדיוק זה אסור: ההלאמה מתריעה ואינה חוסמת, וגרירה אליה היא פעולה לגיטימית.
לכן `pointer-events` יושב על ה**קו בלבד** (`stroke`) ועל התווית - אותה הכרעה
של תפריט האזור (CLAUDE.md §אזור סגור), עם קו תפיסה שקוף ורחב לאצבע.

### ההתראה לפני הביצוע, ולא אחריו

"מתריעה ואינה חוסמת" אינו "שותקת". השאלה נשאלת ב-`doFzSave` **לפני** הכתיבה,
כי אחריה זו הודעה על מה שכבר קרה ולא הזדמנות לעצור. ההכרעה נשארת של הפקח
("שייך בכל זאת"), והמערכת רק מוודאת שהוא יודע. `isNew` נמדד מול השיוך הקודם
כמו באזור סגור/מוגבל, אחרת כל הזזה קטנה בתוך אותו אזור הייתה שואלת שוב -
והתראה שנשאלת בלי סיבה היא התראה שלוחצים עליה בלי לקרוא.

### מה יצא מהרכיבים אל הליבה

`vertexAt` / `tapAction` (ההכרעה בין נגיעה לגרירה) ו-`elapsedLabel` (השעון
הרץ) עברו ל-`tempZoneSeizure.ts` ונבדקים ב-vitest. `SeizureAlert` פוצל ל-
`SeizureAlertCard` (רכיב רגיל) ולעטיפת ה-portal, כי `createPortal` אינו נתמך
ברינדור לשרת - ובלי ההפרדה אי אפשר היה לבדוק את ההתראה כלל.

### QA

| בדיקה | תוצאה |
|-------|--------|
| `tsc --noEmit` | ✅ נקי |
| `vitest run` | ✅ 2,342 עוברות (116 קבצים) |
| `npm run build` | ✅ |

---

## 2026-08-31 - הלאמת אזור זמני

**הבקשה:** "יש מקרים שצריך להלאים אזור ולהעביר את המידע בין כל העמדות."

מרחב ש**נולד בציור** על המפה, מקבל טופס הגדרה, ומופץ לעמדות - עם התראה
מתפרצת, אישור פר-עמדה, צביעת האזורים המושפעים והבהוב הפ"מים שבתוכם.
אפיון מלא + מטריצת 28 המקרים: [TEMP_ZONE_SEIZURE_SPEC.md](TEMP_ZONE_SEIZURE_SPEC.md).

### שתי הכרעות שקבעו את המימוש

| השאלה | ההכרעה | למה |
|-------|---------|-----|
| חוסמת שיוך או מתריעה? | **מתריעה בלבד** | זה כלי התרעה. חסימה שגויה במרחב מולאם משתקת עמדה שלמה, וההכרעה שייכת לפקח - כמו ב"אזור מוגבל" |
| מה קורה כשחלף זמן הסיום המשוער? | **התראה ליוצר: הארך / סיים** | הלאמה שנעלמת מעצמה בזמן שהאירוע נמשך היא כשל בטיחותי. הזמן מזכיר, הפקח מכריע |

### למה נ"צ ולא אחוזי תמונה

המרחב נולד כדי להיות מוצג על מפות **אחרות**, ואחוזי תמונה של מפה אחת חסרי
משמעות על מפה שנייה. לכן `polygon_geo` (lat/lon) הוא מקור האמת, כל עמדה מקרינה
אותו למפה שלה ב-`geoToImagePct` - וזו הסיבה שהפיצ'ר דורש **מפה מעוגנת**. ציור על
מפה לא מעוגנת היה נשלח לעמדה אחרת ומופיע במקום הלא נכון: כשל שקט.
`polygon` (אחוזי המפה של היוצר) נשמר לחלון "פתח מפה" בלבד.

### למה השרת אינו מחשב גיאומטריה

"האם המרחב חותך אזור שמוצג בעמדה X" תלוי במפה של X, בעוגנים שלה ובאזורים שעליה.
`GET /candidates` מגיש חומר גולמי וההכרעה נעשית ב-`src/utils/tempZoneSeizure.ts` -
**אותה** פונקציה שמייצרת את הרשימה החכמה בטופס, שצובעת את האזורים בעמדה המקבלת,
ושמחליטה אילו פ"מים מהבהבים. מימוש שני בשרת היה נשבר בשקט ברגע שאחד מהם משתנה.
מאותה סיבה ספירת הפ"מים מגיעה מהעמדה עצמה (`PATCH /:id/report`).

### קבצים

| קובץ | מה |
|------|-----|
| `TEMP_ZONE_SEIZURE_SPEC.md` | **חדש** - האפיון ומטריצת המקרים |
| `src/utils/tempZoneSeizure.ts` + `.test.ts` | **חדש** - הליבה הטהורה, 52 בדיקות |
| `src/components/seizure/*` | **חדש** - hook + 6 רכיבים (ציור, טופס, התראה, אישורים, חלון מפה, שכבת SVG) |
| `server/routes/tempZoneSeizures.js` + `.test.js` | **חדש** - 9 endpoints, 26 בדיקות מול Postgres אמיתי |
| `server/db/init.js` | `temp_zone_seizures`, `temp_zone_seizure_targets`, `workstation_presets.can_seize_zone` |
| `server/db/env-tables.js` | שתי הטבלאות **תפעוליות** - מבודדות פר-סביבת תרגול |
| `server/db/undoJournal.js` | שתיהן ב-`UNDO_DENYLIST` - ביטול שקט של הלאמה או של אישור אסור |
| `src/components/views/SectorDashboard.tsx` | חיווט: תפריט יצירה, שכבת הציור, צביעת האזורים, הבהוב הפ"מים |
| `src/components/admin/ManagementPage.tsx` | הרשאת `can_seize_zone` בניהול העמדה (ב"מ **לא**) |
| `src/i18n/registry/seizure.json` | **חדש** - namespace מלא (he+en) |

### QA

| בדיקה | תוצאה |
|-------|--------|
| `tsc --noEmit` | ✅ נקי |
| `vitest run` | ✅ 2,230 עוברות (52 חדשות בליבה, 26 חדשות ב-route) |
| `npm run build` | ✅ |
| עליית שרת מלאה מול ה-DB | ✅ `phase:"ready"` - כלומר `checkTableClassification` ו-`syncAllEnvSchemas` עברו עם הטבלאות החדשות |
| בידוד סביבות (אינטגרציה, Neon) | ✅ 8/8 |

---

## 2026-08-31 - היקף ההגבלה ברשימה אחת, חסימה ברמת הרצועה, והתראה קופצת

**הבקשה:** "החל על בלוק ובלוקים פעילים זה אותו דבר - צריך לאחד... וניתן לבחור
מספר בלוקים. במוד אזורים מפוצל לפי גבהים על מפה - אם גורר לאזור בלוק גובה סגור
אז לא לאפשר גרירה ולהוציא התראה. אם מוגבל - להוציא התראה שים לב גררת לאזור
מוגבל עם הגבהים הפתוחים והערת מגבלה. ההתראה צריכה להיות התראה קופצת ולא
ההתראות שבמעלה המסך."

### 1. רשימת בלוקים אחת, בחירה מרובה

בתפריט האזור היו **שתי** רשימות בלוקים: "החל על בלוק" ו"בלוקים (גבהים) פעילים".
הן שאלו את אותה שאלה בשני ניסוחים **הפוכים** - מה סגור מול מה פתוח - וזה מה
שהפך אותן לבלתי קריאות. עכשיו רשימה אחת שבה מסמנים את מה ש**סגור**, עם השם
והגבהים, ובבחירה מרובה.

| | |
|---|---|
| **למה עמודה חדשה** | כמה בלוקים **לא רציפים** אינם ניתנים לביטוי בטווח מספרי אחד, ולכן נוסף `restriction_range_ids` (מערך מזהי `zone_altitude_ranges`) |
| **סימון גובר על טווח** | כשהפקח סימן בלוקים, זו ההכרעה - ובלוק שאינו ברשימה פתוח גם אם גבהיו נופלים בטווח ישן שנשאר |
| **הכול או ריק** | סימון **כל** הבלוקים נשמר כרשימה ריקה, שהיא "כל הגבהים". אחרת היו שני מצבים שנראים זהים ומתנהגים אחרת |
| **אזור לא מפוצל** | אין לו בלוקים לסמן, ולכן הוא ממשיך לקבל טווח מספרי חופשי |
| **קבוצת "היקף"** | הבלוקים והטווח נוסעים יחד ב-`PATCH`, ושליחת אחד מאפסת את השני - הם שני ניסוחים של אותה שאלה |

### 2. ההכרעה ירדה לרמת ה**רצועה**

במפה המפוצלת לגבהים הפקח רואה את האזור חלוק לרצועות ומשחרר את הפ"מ ב**רצועה**
שהוא מתכוון אליה. לכן הרצועה - ולא האזור כולו - היא מה שנשאל: רצועה סגורה
חוסמת, ורצועה פתוחה **באותו אזור** ממשיכה לקבל. `bandRestrictionKind` עונה על
זה, ו-`altBlockAtPoint` על נקודת השחרור מזהה את הרצועה כבר בנקודת הכוונה - כדי
שהחסימה תקרה לפני שהטופס נפתח.

### 3. חלון קופץ במקום שורה בבאנר

התראות אזור סגור/מוגבל עברו מהבאנר שבראש המסך ל**חלון קופץ** מרכזי עם אישור.
הנימוק אינו אסתטי: אלה תגובות ל**פעולה שהפקח הרגע עשה**, ולא מצב שנמשך. שורה
בראש המסך היא מקום שהעין לומדת להתעלם ממנו, ואילו כאן הפקח חייב לדעת שהגרירה
שלו **לא קרתה**. הבאנר חזר להחזיק את התראות ה**זיהוי** בלבד - הן כן מתארות מצב
שנמשך ונקראות תוך כדי עבודה.

| מה | הנוסח |
|---|---|
| שיוך לאזור סגור | "האזור סגור - השיוך נדחה" |
| אזור נסגר מתחת לפ"מ שכבר בו | "אויש אזור סגור" - **חלון אחד** לכל הפ"מים באזור, לא אחד לכל פ"מ |
| שיוך לאזור מוגבל | "שים לב - גררת לאזור מוגבל", עם ה**גבהים הפתוחים** והערת המגבלה |

"הגבהים הפתוחים" הם המחצית השימושית של ההתראה: לא רק "יש מגבלה", אלא **מה כן
אפשר**. תור: מוצג ראש התור בלבד, כדי ששתי גרירות רצופות לא יפתחו שני חלונות זה
על זה.

### קבצים

| קובץ | מה |
|---|---|
| `src/utils/zoneRestriction.ts` | `restriction_range_ids`, `bandRestrictionKind`, `openBands`, `bandLabel`, `restrictedBandIds` |
| `server/db/init.js` | `map_zone_operational_state.restriction_range_ids` |
| `server/routes/maps.js` | הבלוקים כחלק מקבוצת ההיקף ב-`PATCH /operational`, ובשני ה-GET |
| `src/components/views/SectorDashboard.tsx` | איחוד הרשימות, החלון הקופץ, חסימה ברמת הרצועה, `data-zone-band` |
| `e2e/fz-zone-restriction.spec.ts` | הבדיקה המפוצלת (רצועה סגורה חוסמת / פתוחה מקבלת) + מעבר לחלון הקופץ |

### QA

| בדיקה | תוצאה |
|---|---|
| `tsc --noEmit` | ✅ נקי |
| `npm test` (vitest) | ✅ 2203/2203 (58 ל-`zoneRestriction`) |
| `npm run build` | ✅ עבר |
| ה-SQL של הנתיב, מול ה-DB | ✅ 11 מסלולים (מצב לבד, בלוקים לבד, בחירה מרובה, מעבר בלוקים↔טווח, פתיחה) בטרנזקציה שהתגלגלה אחורה |
| `playwright` (`fz-zone-restriction`) | ⚠️ **5/5 עברו** לפני שהמכונה נעמסה; בהרצות שאחרי כן הן נתקעות בטיים-אאוט (בדיקה שלקחה 36 שניות לקחה 7 דקות). לא הצלחתי לאמת מחדש את **החלון הקופץ של אזור מוגבל** - ראה למטה |

### מה **לא** אומת, ובמפורש

החלון הקופץ של "גררת לאזור מוגבל" עבר e2e לפני העומס, ובהרצה מאוחרת הבדיקה
דיווחה שהשיוך **כן** נוצר (כמצופה) אך החלון לא נמצא. אין דרך להכריע בין באג
לבין טיים-אאוט בסביבה שרצה פי 12 לאט - ולכן זה נרשם כאן כפתוח ולא כעובר.
החסימה עצמה (החצי הבטיחותי) אינה תלויה בחלון.

### הערה תפעולית: שני סוכנים על אותו עץ

במהלך העבודה סוכן נוסף ערך וקימט את `SectorDashboard.tsx` באותו worktree
(`6c311d4`, `1797846`), והעבודה שלי נסחפה לתוך קומיט שהודעתו על נושא אחר. בשלב
מסוים היו בעץ שש שורות `console.log('ZDBG'...)` בקוד ייצור. הסוכן השני ניקה
אותן לפני הדחיפה, ואין ZDBG בהיסטוריה שנדחפה - אבל זה בדיוק התרחיש ש-CLAUDE.md
§worktree נפרד לכל סוכן מזהיר מפניו, והוא קרה **שוב**.

---

## 2026-08-27 - אזור סגור חוסם את השיוך; אזור מוגבל מתריע בלבד

**הבקשה:** "אל תאפשר לגרור לאזור סגור ותוציא התראה - ולאזור מוגבל רק תוציא
התראה."

זה הופך את ההכרעה שנרשמה יום קודם ("ההתראה אינה חוסמת"). הצוות הכריע, ובצדק:
שני המצבים היו עד כה אותו דבר בשתי עוצמות, ועכשיו הם **שני כלים**.

| | אזור סגור | אזור מוגבל |
|---|---|---|
| שיוך פ"מ | **נחסם** | מותר |
| התראה | "אזור סגור - השיוך נדחה" | "אויש אזור מוגבל" |
| מי מכריע | המערכת | הפקח |

ההבדל אינו עוצמה אלא **מי מכריע**, ולכן "מוגבל" אינו דרגה חלשה של "סגור" -
הוא הכלי המתאים לאזור שאיושו אפשרי בתנאים.

### איפה נחסם, ולמה שם

| שכבה | תפקיד |
|---|---|
| **`doFzSave`** | ההגנה שאי אפשר לעקוף. כל 26 נקודות הקריאה - גרירה, שידוך בלחיצה, הטופס, תפריט הפין, מסלול הקונפליקט - עוברות בה, והיא מחזירה `false` כשנחסם |
| **נקודות הכוונה** | חמשת המקומות שפותחים את טופס ההקצאה נחסמים מראש כשהאזור סגור לכל גובה אפשרי, כדי שהטופס לא ייפתח לחינם |
| **טופס ההקצאה** | בלוק סגור מוצג ⛔, מקווקו ואינו נבחר. בלעדיו הפקח היה בוחר בלוק, נדחה בשמירה, ומחפש את התקלה במקום הלא נכון |

**מה לא נחסם, בכוונה:** פ"מ שכבר יושב באזור סגור (האזור נסגר מתחתיו) ממשיך
להיות ניתן להזזה, לעדכון הערה ולשינוי סטטוס. `doFzSave` הוא גם מסלול השמירה של
כל אלה, וחסימה גורפת הייתה **מקפיאה** את מי שבתוך האזור - ובכלל לא היה אפשר
לגרור אותו החוצה. הציר הוא "אירוע חדש": ההגבלה נבדקת מול השיוך הקודם.

### אזור שנסגר ויש בו פ"מ

מרגע שהשיוך נחסם, הדרך היחידה שנשארה להיות בתוך אזור סגור היא שהאזור נסגר
מתחתיך - וזה בדיוק המקרה שהיה עובר בשקט. נוסף זיהוי של **שינוי בשלשת ההגבלה**
(מצב + טווח), ולכל פ"מ שנמצא בתוך הסגירה החדשה יוצאת "אויש אזור סגור". לפי
השלשה ולא לפי המצב לבדו: הצרת טווח על אזור שכבר היה סגור סוגרת בלוק נוסף.

### הבאג שה-e2e תפס - והתיקון האמיתי

"סגור" ואחריו מילוי טווח החזירו את האזור ל**מוגבל**. שדות הטווח הם `input`
בלי `value`, ה-`onBlur` שרץ הוא מרינדור קודם, ולכן הוא נשא איתו את המצב שהיה
**לפני** הסגירה. `zoneOpRef` (ref שנקרא בזמן הקריאה) שיפר את זה אבל לא ריפא:
טעינה מחדש של האזורים יכולה לדרוס את העדכון האופטימי בחלון שבין השתיים,
והתקלה נשארה **מתחלפת** - עברה פעם, נפלה פעם. בפקד בטיחותי אין "לפעמים".

התיקון הוא בחוזה ולא בתזמון: **המצב והטווח נכתבים בנפרד, והשרת הוא בעל המצב.**

| מה נשלח | מה קורה |
|---|---|
| `restriction: ''` | האזור נפתח, והטווח מתנקה |
| `restriction: 'closed'\|'restricted'` בלי טווח | המצב נקבע, הטווח נשמר |
| טווח בלי `restriction` | הטווח נקבע, המצב נשמר; אזור פתוח הופך ל-`restriction_if_open` (ברירת מחדל `restricted`) |

הלקוח אינו שולח את המצב הנוכחי לעולם, ולכן state ישן בו אינו יכול להוריד אזור
סגור ל"מוגבל" - לא בגלל תזמון טוב יותר, אלא כי אין לו מה לדרוס. בנוסף,
`saveZoneOperational` מיישב את **תשובת** ה-PATCH לתוך ה-state ולא נשאר על
הניחוש האופטימי.

### ניקוי ה-e2e - כשל שהיה שקט

ה-`finally` היה `.catch(() => {})`, ובפועל נמצאו ב-DB אזור שנשאר **סגור**
ושיוכי בדיקה שלא נמחקו. אזור סגור שנשכח אינו "לכלוך": מרגע שהשיוך נחסם, הוא
**מונע** מהפקח לשייך אליו. הניקוי עכשיו נבדק ומכשיל את הבדיקה אם לא הצליח.

### קבצים

| קובץ | מה |
|---|---|
| `src/utils/zoneRestriction.ts` | `closedForAllBands` - "האם בכלל לפתוח טופס" |
| `server/routes/maps.js` | `PATCH /operational`: מצב וטווח בנפרד, `restriction_if_open` |
| `src/components/views/SectorDashboard.tsx` | החסימה ב-`doFzSave`, חמש נקודות הכוונה, סימון הבלוקים בטופס, התראת הדחייה, זיהוי אזור שנסגר עם פ"מ בתוכו |
| `src/components/admin/managers.tsx` | תווית `zone_closed_blocked` |
| `e2e/fz-zone-restriction.spec.ts` | הבדיקה ההופכית (סגור חוסם) + חדשה (מוגבל מותר), וניקוי שנבדק |
| `src/i18n/registry/ctrl.json` | 2 מפתחות (he+en) |

### QA

| בדיקה | תוצאה |
|---|---|
| `tsc --noEmit` | ✅ נקי |
| `npm test` (vitest) | ✅ 2185/2185 (6 חדשות ל-`closedForAllBands`) |
| `npm run build` | ✅ עבר |
| `playwright` (`fz-zone-restriction`) | ✅ 4/4, וגם 8/8 ב-`--repeat-each=2` - הרצה כפולה בכוונה, כי התקלה שתוקנה הייתה מתחלפת |
| ה-SQL של הנתיב, מול ה-DB | ✅ 12 מסלולים (מצב לבד, טווח לבד, פתיחה, `if_open`, הערה, בלוקים) בטרנזקציה שהתגלגלה אחורה |

---

## 2026-08-26 - אזור סגור / אזור מוגבל, ותפריט האזור בלחיצה על הקו

**הבקשה:** "באזורים על מפה רוצה פונקציה של אזור מוגבל / סגור. בלחיצה על קו של
אזור לפתוח תפריט על אזור - עם סגור - מוגבל והערה וטווח גבהים. במידה ומקצה פמ
לאזור כזה להוציא התראה שאויש אזור סגור. יש להתחשב כמובן בגובה האזור אם האזור
פוצל."

| | |
|---|---|
| **הפקד הוא הקו** | לחיצה על **קו** האזור פותחת את תפריט האזור (`zoneAtEdgeOnly`). פנים האזור נשאר יעד שחרור של פ"מ, ולכן לחיצה בתוכו אינה פותחת תפריט. הקליק הימני הקיים נשאר למי שעובד בעכבר - ב-Cintiq אין לחצן ימני בעט ואין אחד באצבע |
| **מנגנון אחד לשני המקרים** | להגבלה יש **טווח גבהים** (`restriction_alt_min/max`, רום טיסה), וטווח ריק = כל הגבהים. כך אזור **לא מפוצל** נסגר "מ-100 עד 140" בלי שיהיו לו בלוקים, ובאזור **מפוצל** נגיעה על שם בלוק ממלאת את הטווח שלו. ההכרעה מול הפ"מ היא חפיפת טווחים - ולא שני מקומות שיכולים לסתור זה את זה |
| **ההגבלה לא חוסמת** | ההתראה יוצאת, השיוך נשמר. לפעמים מאיישים אזור סגור ביודעין (חילוץ, יעוט), והמערכת אינה אמורה למנוע מהפקח לרשום את המציאות - רק לוודא שהיא נראית |
| **ברירת המחדל הבטוחה** | אין בלוק ואין גובה בפ"מ = **מתריעים**. אותה הכרעה של `zoneBlockOccupancy`: התראה מיותרת היא רעש, התראה שלא נשמעה היא פ"מ באזור סגור |

### שלושה דברים שנמצאו תוך כדי, ותוקנו

| מה | למה זה היה חשוב |
|---|---|
| **`map_zones` לא נמשכת בפולינג** | סגירה שנקבעה בעמדה אחת לא הייתה מגיעה לשאר עד רענון ידני - כלומר פקח שני משחרר פ"מ לאזור סגור, וההתראה אצלו לא יוצאת כלל. נוסף נתיב **קל** `GET /api/map-zones/operational?map_id=` (רק שורת המצב, בלי הפוליגונים) שנמשך כל 5 שניות. המשיכה של `GET /api/map-zones` המלא הייתה עשרות ק"ב לעמדה כל טיק, עבור שדה שמשתנה פעם במשמרת |
| **אזור מוגבל נעלם עם מתג הצגת האזורים** | מצב בטיחותי אינו דבר שמתג תצוגה מכבה. `zoneOverlayShown` מצייר אזור מוגבל תמיד - וגם הקו, שדרכו נפתח התפריט, נשאר שם כדי לגלות למה |
| **הטווח דרס את הסגירה** (נתפס ב-e2e) | שדות הטווח הם `input` בלי `value`, ולכן ה-`onBlur` שרץ הוא מרינדור קודם - ואיתו ה-`restriction` שהוא שולח. "סגור" ואז מילוי טווח החזירו את האזור ל"מוגבל". נפתר ב-`zoneOpRef`, שנקרא בזמן ה**קריאה** ולא בזמן היצירה |

### קבצים

| קובץ | מה |
|---|---|
| `src/utils/zoneRestriction.ts` | **חדש** - ההכרעה, טהורה. `assignmentRestriction`, `bandRestricted`, `altRangesOverlap`, `restrictionRangeLabel` |
| `src/utils/zoneHit.ts` | `zoneAtEdgeOnly` - הקו בלבד, עם שובר שוויון בין אזורים צמודים |
| `server/db/init.js` | `map_zone_operational_state`: `restriction`, `restriction_alt_min`, `restriction_alt_max` |
| `server/routes/maps.js` | `GET /api/map-zones/operational` (חדש); `PATCH /:id/operational` מקבל את קבוצת ההגבלה; ה-`GET` המלא מחזיר אותה |
| `src/components/views/SectorDashboard.tsx` | התפריט, הצביעה על המפה, הפולינג, ההתראה |
| `src/components/admin/managers.tsx` | תוויות התחקיר `zone_closed_manned` / `zone_restricted_manned` |
| `src/types/index.ts`, `src/types/ground.ts` | שדות ההגבלה על `MapZone` |
| `src/i18n/registry/ctrl.json` | 16 מפתחות (he+en) |

### QA

| בדיקה | תוצאה |
|---|---|
| `tsc --noEmit` | ✅ נקי |
| `npm test` (vitest) | ✅ 2179/2179 (מהם 34 חדשות ל-`zoneRestriction`, 6 ל-`zoneAtEdgeOnly`) |
| `npm run build` | ✅ עבר |
| `playwright` (`fz-zone-restriction`) | ✅ 3/3 |
| מיגרציה + SQL של הנתיב, מול ה-DB | ✅ הוחלה; ארבעת המסלולים (סגירה טווחית, עדכון שדה אחר, סגירה גורפת, פתיחה) נבדקו בטרנזקציה שהתגלגלה אחורה |

ה-e2e הוא זה שתפס את באג הטווח, ולא `tsc`/unit: הוא היחיד שעובר את המסלול
האמיתי - נגיעה על הקו, התפריט, השמירה, ומה שבאמת יושב בשרת אחריה. הוא גם
**משחזר את עצמו**: מצב האזור מוחזר ל"פתוח" ב-`finally`, וגם נפילה באמצע אינה
משאירה אזור סגור ב-DB.

---

## 2026-08-24 - קונטיינר: בורר המיקום מקופל מאחורי משולש

**הבקשה:** "רציתי שפה אחרי בחירת הצג קונטיינר יהיה משולש בצד שמאל שפותח את
תפריט המיקום ולא ישר מציג".

ארבע הסכמות תפסו מקום קבוע בתפריט "תצוגה" גם למי שקבע מיקום פעם אחת ולא נגע
בו שוב. עכשיו הן מאחורי משולש בקצה השורה (`◂` סגור / `▾` פתוח), שמופיע רק
כשהקונטיינר פעיל - כשהוא כבוי אין מה למקם.

| | |
|---|---|
| **`stopPropagation` על המשולש** | בלעדיו לחיצה עליו הייתה נספרת גם כלחיצה על השורה - כלומר **סוגרת את הקונטיינר** ומסגירה את התפריט, באותה נשימה |
| **קיפול בסגירה** | `useEffect` על `showWindowContainer` מאפס את הבורר, אחרת הוא נפתח מעצמו בהדלקה הבאה - וזו בדיוק ההתנהגות שהוסרה |
| **נשמר בין פתיחות** | פקח שפתח את הבורר מוצא אותו פתוח בפתיחה הבאה של התפריט באותו סשן. הדרישה היא שלא ייפתח **כברירת מחדל**, לא שייסגר אחרי כל בחירה |

### QA

| בדיקה | תוצאה |
|---|---|
| `tsc --noEmit` | ✅ נקי |
| `npm test` (vitest) | ✅ 2117/2117 |
| `npm run build` | ✅ עבר |
| `playwright` (קונטיינר + שחזור) | ✅ 10/10 |

הבדיקה החדשה מוודאת את מה שקל לשבור: שהמשולש פותח את הבורר **בלי** לסגור את
הקונטיינר, ושהוא מקופל בפתיחה הראשונה.

---

## 2026-08-21 - קונטיינר: בורר מיקום ויזואלי, וחלונות אחד ליד השני

**הבקשה:** בתפריט התצוגה, ליד "קונטיינר חלונות", שייפתחו אופציות **ויזואליות**
לאיפה לשים את הקונטיינר. ובנוסף: "אם אני מגדיל מעבר לגודל הזה - שים את
הריבועים אחד ליד השני".

### בורר המיקום

ארבע סכמות קטנות של שורת העמדה, עם עמודת הקונטיינר מודגשת. ויזואלי ולא רשימת
שמות, כי "בין הפ"מים לעזרים" מחייב את הפקח לבנות את המסך בראש - והתמונה עונה
על זה במבט אחד.

```
[▓][נק][מפה][פ"מ][עזר]   [נק][מפה][▓][פ"מ][עזר]
[נק][מפה][פ"מ][▓][עזר]   [נק][מפה][פ"מ][עזר][▓]
```

עמודות השורה קיבלו ערכים **אי-זוגיים** (נקודות=1|3 · מפה=2 · פ"מים=5 · עזרים=7),
והזוגיים נשארו פנויים לקונטיינר (0/4/6/8). כך הוא נכנס בין כל שתי עמודות בלי
לגעת ב-`order` של אף אחת מהן.

הספליטר עובר לצד הפונה למרכז המסך: במיקום `left` הוא על הקצה הימני של
הקונטיינר וגרירה ימינה מרחיבה - אחרת הידית בורחת מהיד.

### אחד ליד השני

מעל `DOCK_COL_WIDTH` (240) לכל עמודה, הפריסה הופכת לרשת והחלונות עומדים זה
לצד זה (עד 4 עמודות). `DOCK_MAX_WIDTH` הועלה ל-900, כי עכשיו יש טעם בקונטיינר רחב.

**למה CSS Grid ולא `flex-wrap`:** המימוש הראשון חישב רוחב עמודה מ-`calc((100% -
gap) / cols)` - ושתי משבצות של 283px לא נכנסו ל-569px הפנויים ונשברו שורה.
עיגול תת-פיקסלי מול `padding` ו-`border` הוא קרב שאי אפשר לנצח בחישוב ידני;
`grid` מחלק את הרוחב בעצמו ותמיד מדויק.

**סמן ההכנסה עבר להיות הדגשה בתוך המשבצת** (`boxShadow: inset`) ולא אלמנט
נפרד: ברשת אלמנט נפרד תופס תא ומזיז את כל הפריסה בדיוק כשהפקח מכוון למקום.

`dockInsertIndexGrid` מחשב את מקום ההכנסה לפי סדר קריאה (שורה, ואז מרכז
המשבצת ב-X); בעמודה אחת נשמרת התנהגות המשבצת-האמצע הקודמת.

### QA

| בדיקה | תוצאה |
|---|---|
| `tsc --noEmit` | ✅ נקי |
| `npm test` (vitest) | ✅ 2117/2117 |
| `npm run build` | ✅ עבר |
| `playwright` (קונטיינר + שחזור) | ✅ 9/9 |

> **הערה על חבילת הבדיקות:** `src/i18n/i18n-guard.test.ts` נופל לסירוגין
> ב**הרצה מלאה** על `Test timed out in 5000ms` - הוא סורק את כל עץ הרכיבים
> מהדיסק, ותחת עומס חורג מברירת המחדל של vitest. לבד הוא עובר תמיד, ואין קשר
> לתוכן. אותו דפוס כמו `airPictureMaps.test.js`. שווה `testTimeout` ייעודי.

---

## 2026-08-21 - קונטיינר: החלון לא נעלם, והחלונות נארזים למעלה

**מהשטח:** "אחרי שלחצתי על מחיקת לוח הודעות מהקונטיינר לא מצליח להחזיר אותו
ע"י לחיצה בתפריט תצוגה" + "כל דבר בקונטיינר להצמיד למעלה".

### התקלה: עגינה דרסה את המיקום הצף

ידית הגרירה של החלון הפעילה **שתי** גרירות במקביל - זו של החלון וזו של
הקונטיינר. עד שהחלון הגיע לקונטיינר, הגרירה שלו כבר עדכנה את `pos` לנקודת
השחרור: בתוך העמודה, בקצה הימני של המסך.

התוצאה: תפריט התצוגה **כן** החזיר את החלון - אבל הוא חזר לצוף מעבר לקצה
המסך. ב-24" (`--s=1.65`) הקונטיינר יושב בקצה, ורוחב הלוח 196 יחידות, ולכן
מה שחזר היה פס דק מחוץ לשדה הראייה. למפעיל זה נראה בדיוק כמו חלון שנמחק,
והוא חיפש אותו בתפריט במקום בשוליים.

| | |
|---|---|
| **התיקון** | ה-hook מצלם את המיקום הצף ב-`pointerdown`, ומשחזר אותו כשהגרירה מסתיימת בעגינה. שחרור **החוצה** ממשיך לנחות בנקודת השחרור, אבל עובר `clampToViewport` |
| **ה-API** | `onUndock` הפך ל-`setFloatingPos`, ונוסף `floatingPos` (קריאת המיקום הנוכחי). שם מדויק: הפונקציה ממקמת את החלון, לא "מגיבה לשחרור" |

### הפריסה: אריזה למעלה במקום מתיחה

`FitScaleBox` קיבל מצב שלישי - `mode="width"`: התאמה ל**רוחב** בלבד, והגובה
נגזר מאותו מקדם. המשבצות נערמות מלמעלה ולא נמתחות לגובה העמודה, ולכן חלון
בודד נשאר בגודלו במקום להתנפח על כל הקונטיינר. כשהכל לא נכנס - העמודה נגללת.

בנוסף: חלון **חדש** שמשוחרר בשטח הריק נכנס ל**ראש** הרשימה ולא לתחתיתה
(`dockHitTest` מדווח `overEmpty`). שחרור מדויק **על** משבצת עדיין מכבד את
המקום שכוון אליו.

### מלכודת שנמצאה בדרך: הבדיקה עצמה הפילה את הלוגין

ה-e2e נפל ב-5 מתוך 7 בדיקות - וכולן על **מסך הכניסה**, לא על הקונטיינר.
הסיבה: יירוט `**/api/workstation-presets**` תפס גם את הרשימה שממנה ניזון
**בורר העמדות**, וכל תפיסה היא הליכה חוזרת לשרת דרך Playwright. הצטברות
ההשהיות חרגה מה-15 שניות שהלוגין ממתין.

התיקון: יירוט `/api/workstation-presets/<id>/config` בלבד - `myPresetConfig`
הוא `livePresetConfig ?? הרשימה`, ולכן זה מספיק, והלוגין לא נוגע בו.
אחרי השינוי: 7/7 ירוקות, בלי ניסיונות חוזרים, 11-19 שניות לבדיקה.

> הלקח: כשבדיקה נופלת בשלב שאינו נושא הבדיקה - לחשוד קודם **בבדיקה**.
> `retries` היה מסתיר את זה ומשאיר חבילה איטית ושברירית.

### QA

| בדיקה | תוצאה |
|---|---|
| `tsc --noEmit` | ✅ נקי |
| `npm test` (vitest) | ✅ 2105/2105 |
| `npm run build` | ✅ עבר |
| `playwright` (קונטיינר + שחזור) | ✅ 7/7, בלי ניסיונות חוזרים |

---

## 2026-08-21 - קונטיינר החלונות: עמודה שאליה דוחפים חלונות צפים

**הבקשה:** פיצ'ר "קונטיינר" שאפשר להציג או להסתיר במצב תצוגה בעמדה. כשהוא
מוצג נפתח חלון בצד ימין - בין העזרים לפ"מים - ואפשר לדחוף אליו את כל החלונות
הצפים. חלון שנגרר לשם נשאר עד שגוררים אותו החוצה; אפשר להחליף סדר בגרירה;
והגודל של כולם נקבע פרופורציונלית לפי גודל הקונטיינר.

### ההרכבה

החלון נשאר **במקומו בעץ הרכיבים** - עם ה-props, ה-state וההיסטוריה שלו - ורק
היעד ב-DOM מתחלף (`createPortal`). זו הסיבה שחלון שהיה באמצע עריכה לא מאבד את
מה שהוקלד בו כשמעגנים אותו, ושלא נוצרה "גרסה מעוגנת" נפרדת לתחזוקה.

```
┌ פ"מים (4) ┐ ┌ קונטיינר (5) ┐ ┌ עזרים (6) ┐
│           │ │ ┌──────────┐ │ │           │
│           │ │ │ פתקית    │ │ │           │  גובה הקונטיינר
│           │ │ ├──────────┤ │ │           │  מתחלק שווה בשווה,
│           │ │ │ הודעות   │ │ │           │  וכל חלון מוקטן
│           │ │ └──────────┘ │ │           │  כיחידה אחת
└───────────┘ └──────────────┘ └───────────┘
```

| מודול | תפקיד |
|---|---|
| [`src/utils/windowDock.ts`](src/utils/windowDock.ts) | המודל: מי מעוגן, באיזה סדר, מה הרוחב. pub/sub + `localStorage` פר-עמדה |
| [`src/hooks/useDockableWindow.ts`](src/hooks/useDockableWindow.ts) | ה-hook שהופך חלון צף לבר-עגינה בשלושה שינויים, ו-`DockableWindow` לחלונות שנולדים ב-`map()` |
| [`src/components/shared/WindowContainer.tsx`](src/components/shared/WindowContainer.tsx) | העמודה עצמה: משבצות, סמן הכנסה, סידור מחדש, ספליטר רוחב |

**החלונות שחוברו:** לוח הודעות, פתקיות, חלונות נתונים, תלת מימד, מזג אוויר,
תמונה אווירית, דסק חופשי, מסלולים בשימוש, כניסת רכב.

**למה `presetId` ו"פעיל" חיים במודול ולא ב-props:** החלונות מפוזרים על פני
עשרה קבצים ועומק עץ שונה. `setDockPreset` נקרא פעם אחת מהעמדה, ו-`setDockEnabled`
נקרא מהקונטיינר ב-mount/unmount - ולכן כשהקונטיינר נסגר **כל** החלונות חוזרים
לצוף מעצמם, בלי שאף חלון יידע מה מצב המתג.

### שלוש מלכודות שנסגרו בדרך

1. **`registerDockZone` בלי מערך תלויות** - הניקוי רץ לפני כל רינדור, מאפס את
   אזור השחרור, מודיע למנויים וגורר רינדור נוסף. לולאה אינסופית.
2. **`dockLoad` בלי מטמון** - נקרא ברינדור של כל חלון בר-עגינה, ובגרירה יש
   רינדור לכל תזוזת מצביע: `JSON.parse` עשרות פעמים בשנייה.
3. **קיזוז בגרירה כלפי מטה** - ה-index נספר כשהחלון הנגרר עדיין תופס משבצת,
   ובלי הקיזוז שחרור בדיוק מתחת למקום הנוכחי היה מזיז אותו משבצת אחת למטה.

### QA

| בדיקה | תוצאה |
|---|---|
| `tsc --noEmit` | ✅ נקי |
| `npm test` (vitest) | ✅ 2105/2105 (109 קבצים), מהם 16 חדשות ל-`windowDock` |
| `npm run build` | ✅ עבר |
| `npx playwright test e2e/window-container.spec.ts` | ✅ 4/4 - פתיחה ומיקום בין הפ"מים לעזרים, גרירה פנימה והחוצה, סגירה ופתיחה מחדש, חלוקה שווה והחלפת סדר |

ה-e2e מזריק `show_window_container` ל**תשובת ה-API** (route interception) ולא
למאגר - הבדיקה רצה מול ה-DB החי בלי לשנות בו עמדה ובלי להשאיר שאריות.

---

## 2026-08-21 - דסק משימה: אזור המפה הוא סביבת עבודה שלמה

**הבקשה:** באזור שהוקצה למפה צריך את **כל** מה שיש סביב מפה בעמדה רגילה -
פ"ממים, נקודות העברה, סרגל שמאלי וסרגל תחתון. ואם יש כמה אזורי מפה, לכל אחד
הסרגלים והרבדים שלו.

### ההרכבה

בעמדה רגילה המפה במרכז והשאר בשוליים; בדסק השוליים אינם שייכים לאף מפה
מסוימת, ולכן הכל נכנס **לתוך האזור**:

```
┌─ אזור מפה ──────────────────────────────────┐
│ ┌ נקודות ┐ ┌─ מפה + סרגל אנכי ─┐ ┌ פ"ממים ┐ │
│ │ העברה  │ │                    │ │        │ │
│ │        │ │  [סרגל תחתון]      │ │        │ │
│ └────────┘ └────────────────────┘ └────────┘ │
└──────────────────────────────────────────────┘
```

ההרכבה נעשית **סביב** `renderMapPanel` ולא בתוכו: הפאנל נשאר "המפה בלבד"
וממלא את מה שנותר, וכך אותו רכיב משרת את העמדה הרגילה ואת הדסק.

### הסרגל התחתון והרבדים - פר-מפה

הסרגל היה יחיד מעל אזור המפה, ומצבו (הצג אזורים, סינון, צבע לפי, קווים,
אזורים מחוברים) ב-state גלובלי. עכשיו הוא מרונדר **בתוך** הפאנל, ומצבו מגיע
מ-`cfg.view` - ולכן הסרגל של מפה 1 אינו מדליק אזורים על מפה 2. ההצללה של
שמות המשתנים בראש גוף הפאנל היא מה שהפך את כל 12 השימושים לפר-מפה בלי לגעת
בכל אחד בנפרד.

> **שינוי התנהגות בדו-מפה:** הסרגל התחתון והנ"צ עברו מפינת אזור המפה לפינת
> **הפאנל**. במפה יחידה הפאנל ממלא את האזור ולכן המיקום זהה; בדו-מפה לכל מפה
> יש עכשיו סרגל משלה (שתיהן חולקות מצב, כפי שהיה).

### כשאין מקום, המפה מנצחת

שתי עמודות באזור צר חונקות את המפה עד שאין מה לראות. לכן העמודות יורדות
לפסי-צד לפי רוחב האזור (`ResizeObserver`): מתחת ל-784px נכבית עמודת הפ"ממים
(הרשימה זמינה גם כאזור נפרד בפריסה), ומתחת ל-544px גם נקודות ההעברה. בחירה
מפורשת של המפעיל גוברת על האוטומט.

עמודת הפ"ממים אינה מוצגת כשלמפה כבר מקושר חלון פ"ממים **עצמאי** בפריסת הדסק -
אחרת אותה רשימה הייתה מופיעה פעמיים.

### QA

tsc נקי · 2,088 בדיקות יחידה · build · e2e: שלוש בדיקות הדסק (סביבת העבודה
המלאה - שתי העמודות, שני הסרגלים והנ"צ - נבדקות בתוך האזור), ובעמדה הרגילה
`cursor-geo`, `dashboard-baseline` ו-`ctrl-draw-canvas` עוברות.

---

## 2026-08-20 - דסק משימה: נקודות העברה, סרגל הכלים ותצוגת הפ"מ פר-חלון

**הבקשה:** בחלון המפה של הדסק חסרות נקודות ההעברה, וצריך את סרגל הכלים כמו
במפה רגילה - למשל "איך מוצג הפ"מ על המפה".

שלוש חוליות שהיו קשורות ל**עמדה** ולא ל**חלון**, ולכן נשרו בדסק:

| מה | למה נשבר | התיקון |
|---|---|---|
| נקודות ההעברה | הסינון רץ מול `allSectors` = `relevant_sectors` של העמדה. בעמדת דסק הוא **ריק** - הסקטורים שייכים לחלון המפה - ולכן הרשימה יצאה ריקה תמיד והפאנל לא הופיע | הבריכה היא כלל הסקטורים (`allSectorsFull`), והסינון לפי `transfer_points` של החלון. `applyFixedTPoints` מקבל את ההיתר מבחוץ במקום לגזור אותו מהעמדה |
| סרגל הכלים המלא | הוא מותנה ב-`isFlightZonesMode`, דגל של **העמדה**. לעמדת דסק אין כזה | `flight_zones_mode` הפך להגדרה **פר-חלון מפה**, ועובר דרך `cfg.fzMode` (שכבר היה פר-cfg) |
| "איך מוצג הפ"מ" | `fzPinDisplay` נקרא מה-preset ישירות בתוך גוף הפאנל | `cfg.pinDisplay` - פר-מפה, עם override חי מסרגל הכלים |

הפרדה זו נכונה גם עקרונית: בעמדה רגילה יש מפה אחת ולכן "הגדרת העמדה" ו"הגדרת
המפה" הן אותו דבר; בדסק כל מפה יכולה לשמש למשהו אחר.

### מה **לא** היה תקול

"האזורים לא יושבים במיקום על המפה" - נבדק מול מפה אמיתית (מרחבי 305, 25
אזורים, מעוגנת) באותה מפה בשתי העמדות:

| | שכבת האזורים | הנדרש לפי המכל |
|---|---|---|
| עמדה רגילה | `left 0, top 145.842, 554×338.315` | `0, 145.842, 554×338.315` |
| חלון בדסק | `left 149.93, top 0, 966.141×590` | `149.93, 0, 966.141×590` |

ונקודות הפוליגון הראשון זהות בשתיהן. הסחיפה תוקנה ב-ResizeObserver של
הרשומה הקודמת; מה שדווח היה הבנייה שלפניה.

### QA

tsc נקי · 2,070 בדיקות יחידה · build · ארבע בדיקות e2e (המפה בפריסה, קטגוריות
הניהול, עיגון + נ"צ + נקודות העברה בחלון, וסרגל הכלים המלא במצב אזורי טיסה).
## 2026-08-20 - פאנל השכבות נגרר ונסגר, התלת מימד נפתח מלא, והודעה ארוכה נכנסת לכפתור

**הבקשה (פקח):** (1) יכולת לסגור ולגרור את חלון סרגל הכלים. (2) התלת מימד ייפתח
תמיד בחלון מלא, וכשהופכים אותו לקטן - ברבע התחתון-שמאלי, כשכולו מוצג על המפה.
(3) ההודעות לא יחרגו מגודל החלון; אם צריך - להקטין פונט בהתאם.

### 1. פאנל השכבות - נגרר בעט ובאצבע, ונסגר מתוכו

| | |
|---|---|
| **מה היה** | הפאנל נעוץ בפינה העליונה של אזור המפה. כשהמידע שהפקח צריך יושב מתחתיו (קצה מסלול, נקודה, אזור), הדרך היחידה לפנות את הפינה הייתה לסגור אותו מתפריט "תצוגה" - כלומר לוותר גם על מתגי השכבות, פקדי הזום, כפתור התלת מימד וכפתור הציור שבתוכו |
| **מה יש** | הכותרת היא **ידית גרירה** מלאת-רוחב, ובה ✕ לסגירה ו-↩ לחזרה לפינה (גם לחיצה כפולה על הכותרת) |

**גרירה בעט ובאצבע** דרך [`startPointerDrag`](src/utils/pointerDrag.ts) - חלוקה
ב-`--s`, `setPointerCapture` ו-`touchAction:'none'` (CLAUDE.md §גרירה). **לא**
`useDragPosition`: הוא מחזיר קואורדינטות **מסך** לחלון `position:fixed`, ואילו
הפאנל הוא `position:absolute` **בתוך** אזור המפה, כלומר מיקומו נמדד מולו.
הגרירה חסומה כך ש-70px מהפאנל תמיד נשארים בתוך שטח המפה, והגובה הפנוי (`layersAvailH`)
נגזר מחדש מהקצה העליון בפועל - אחרת פאנל שנגרר מטה היה נחתך בתחתית המפה.

**ה-✕ מעדכן את אותו מתג של התפריט** (`onCloseLayersPanel` → `setShowGroundLayers`).
פאנל שנסגר בלי לעדכן אותו היה משאיר בתפריט "✓ מוצג" מול מסך ריק.

### 2. התלת מימד - נפתח מלא, ומוקטן לרבע התחתון-שמאלי של המפה

| | |
|---|---|
| **מה היה** | מצב התצוגה נשמר בסשן, ולכן פתיחה חוזרת החזירה את החלונית הקטנה שהושארה בפינה. החלון הקטן נפתח בפינת מסך קבועה (`insetInlineStart: 28, top: 92`) - `position:fixed`, כלומר מול **החלון** ולא מול המפה, ולכן נחת חלקית מחוץ לשטח המפה ועל פאנל השכבות |
| **מה יש** | לחיצה על "תלת מימד" מאפסת את המצב ל**מלא** (`toggle3D`). מעבר למצב "חלון" ממקם אותו ברבע (חצי רוחב × חצי גובה) של הפינה התחתונה-שמאלית של שטח המפה, **כולו בתוכה** |

הגאומטריה ב-[`smallWinInArea`](src/components/ground/pattern3dPrefs.ts) - פונקציה
טהורה, 8 בדיקות. הקלט בפיקסלים פיזיים והפלט ביחידות מוגדלות; "תחתית" היא התחתית
ה**נראית** (אזור המפה עלול לגלוש מתחת לקצה החלון, ואז ידית ה-⇲ נופלת מהמסך);
מינימום הקריאות (`MIN_WIN_W/H`) גובר על גודל המפה, ואז החלון נצמד לקצה במקום
לצאת ממנו. **"שמאל" הוא פיזי ואינו מתהפך עם השפה** - המפה היא מרחב ולא זרימת
טקסט - אבל הייצוג לוגי: `x` נשמר כהיסט מקצה ה-inline-start, כי כך מעוגן
`Pattern3DWindow`.

### 3. הודעות - מוקטנות עד שהן נכנסות, ולא נשפכות החוצה

| | |
|---|---|
| **מה היה** | תא הודעה בגובה קבוע (38px בסקייל 1) עם פונט קבוע (12px). "מרחב 305 סגור בגלל תקלת חשמל ביבא" פשוט חרג ממנו |
| **מה יש** | טקסט ההודעה עובר ב-[`FitText`](src/components/shared/FitText.tsx) ומוקטן עד שהוא נכנס לתא (רצפה 6px). הטקסט המלא נשאר גם ב-`title` |

**למה לא חיתוך:** `text-overflow: ellipsis` **מסתיר** מידע תפעולי, וגלילה בתוך
כפתור דורשת פעולה כדי לקרוא הודעה שאמורה להיקרא במבט. אותו שיקול שהוליד את
`FitScaleBox` לרצועת המסלולים - וזה גם למה `FitText` הוא רכיב נפרד ולא שימוש בו:
שם התוכן הוא `width:max-content` (שורה אחת מוקטנת לרוחב), וכאן הטקסט **נשבר
לשורות** ורק אז מוקטן, עד שגם הגובה נכנס.

**המדידה אימפרטיבית, בלי state:** חיפוש בינארי (~4 מדידות) בתוך `useLayoutEffect`
שכותב `fontSize` ישירות ל-DOM. דרך `useState` זו לולאת מדידה→רנדר→מדידה שצריך
להגן עליה במונים ובאפסילון, כפי ש-`FitScaleBox` נאלץ.

**תוך כדי - הלוח עצמו חרג מהמסך ב-24":** `maxHeight: '78vh'` יושב תחת
`#root { zoom: var(--s) }`, והזום מכפיל גם יחידות חלון - ב-24" (`--s=1.65`) הגובה
המרבי יצא 129% מהמסך האמיתי, כלומר ההודעות התחתונות וידית ה-⇲ נפלו מתחת לקצה.
מחולק עכשיו: `calc(78vh / var(--s, 1))`. **בפיתוח ב-15.6" זה בלתי נראה** (`--s=1`) -
בדיוק המלכודת ש-`/ui-adapt` מתעדת.

### QA

| בדיקה | תוצאה |
|---|---|
| `tsc --noEmit` | ✅ נקי |
| `vitest run` | ✅ 2,085 בדיקות (108 קבצים) אחרי rebase על main, כולל 8 חדשות ל-`smallWinInArea` ו-9 ל-`FitText` |
| `npm run build` | ✅ עובר |
| שומר i18n | ✅ שלושת המפתחות החדשים (`ground.layersClose/layersDragHint/layersResetPos`) ב-registry, he+en |
| מדידת ההקטנה בדפדפן | ✅ ההודעה מהצילום נכנסת לתא 88×38 ב-9px, 3 שורות; הודעה ארוכה יורדת לרצפה (7px) ונכנסת; מילה אחת ארוכה מהתא נשברת |

---

## 2026-08-20 - דסק משימה: האזורים נסחפים מהמפה, ואין נ"צ בחלון

**הבקשה:** בדסק משימה האזורים "זזים" ואינם מעוגנים למפה, ואין את תצוגת הנ"צ
שיש במפה הרגילה.

### 1. גבולות התמונה התיישנו

כל שכבות המפה - אזורים, פ"ממים, נקודות העברה, תמונ"א - ממוקמות לפי
`imgBounds`: המלבן שבו התמונה **מצוירת** בתוך המכל (`objectFit: contain`
ממרכז אותה ומשאיר שוליים). הגבולות נגזרים מגודל המכל **ברגע החישוב**, ולכן
לכל מפה יש ResizeObserver שמחשב אותם מחדש. לחלונות המפה של הדסק לא הוספתי
כזה - והם דווקא אלה שמשנים גודל הכי הרבה: ספליטר בין אזורי הפריסה, פתיחת
סרגל העזרים, שינוי גודל חלון. התוצאה: הגבולות נשארו של הגודל הקודם, והאזורים
נסחפו ביחס לתמונה.

התיקון הוא אותה זרימה של map1/map2, רק שמספר המפות נקבע בזמן ריצה ולכן
ה-observers נפרסים לפי רשימת החלונות.

### 2. נ"צ - הבאדג' עבר מפינת האזור לפינת הפאנל

הבאדג' היה מרונדר בפינת **אזור המפה** ומסונן ב-`!isMissionDeskMode`. בדסק
פינת האזור אינה שייכת לאף מפה מסוימת - יש שם כמה מפות - ולכן הבאדג' עבר
**לתוך** `renderMapPanel`, ו-`mapHoverCoord` נושא `panKey` כדי שרק החלון
שמעליו המצביע יציג את הקריאה. במפה יחידה הפאנל ממלא את האזור, ולכן המיקום
זהה למה שהיה; בדו-מפה הקריאה עברה לפינת המפה שמעליה המצביע - שיפור, ושינוי
התנהגות שכדאי לדעת עליו.

`dmContextAtPoint` - שכבר ידע לפתור *איזו* מפה תחת המצביע בדו-מפה - קיבל ענף
לחלונות הדסק ומחזיר גם את מפתח החלון.

### QA

הבדיקה `e2e/mission-desk-maps.spec.ts` בונה מפה משלה (תמונה, עיגון ואזור),
ומודדת את הסחיפה: את מלבן התמונה **המצוירת** מול מה ששכבת האזורים מצהירה
עליו. אימות שהיא באמת תופסת את הבאג - ניטרול זמני של ה-ResizeObserver:

| | סחיפה אחרי שינוי יחס המכל |
|---|---|
| בלי התיקון | 95.25px ❌ |
| עם התיקון | < 1.5px ✅ |

בנוסף: הנ"צ מופיע בפינת החלון בריחוף, `cursor-geo.spec.ts` (5 בדיקות הדיוק
של הנ"צ בעמדה הרגילה) עובר, tsc נקי, 2,068 בדיקות יחידה, build.

> **מלכודת שהתגלתה:** מלבן ה-`<img>` **אינו** מלבן התמונה. האלמנט ממלא את
> המכל ו-`objectFit:contain` ממרכז בתוכו את הפיקסלים - בדיקה שמשווה שכבה
> ל-`img.boundingBox()` תיכשל על 13px שהם התנהגות תקינה.

---

## 2026-08-19 - דסק משימה מרובה-מפות: חלון מפה, נקודות העברה ופ"ממים פר-מפה

**הבקשה:** עמדה מסוג דסק משימה שאחד מרכיביה הוא **מפה**. בהגדרת העמדה חובה
לבחור לה מפה; נקודות ההעברה שנבחרות יושבות **בתוך** חלון המפה; לכל חלון מפה
חלון פ"ממים משלו; הדברים הגנריים (בד"חים, עזרים) בחלון כללי בצד; ומסך הניהול
מקובץ לקטגוריות מכווצות, כי דסק יכול להחזיק הרבה מפות והרבה הגדרות.

### 1. חלון מפה הוא שירות של הדסק - והתוכן מגיע מהעמדה

הדסק מגדיר **שיש כאן מפה**, לא *איזו* מפה: אותו דסק משרת עמדות שמסתכלות על
מפות שונות. לכן `mission_desk_services.service_type` קיבל שני ערכים חדשים -
`map` ו-`strips` - וההגדרה עצמה (מפה, נקודות העברה, מפות סקטור) יושבת
**פר-עמדה** ב-`workstation_presets.mission_desk_map_config`, ממופתח לפי
`service_id`.

הקנבס (`MissionDeskBody`) אינו יודע דבר על מפות: הוא מקצה לשירות אזור וקורא
ל-`renderHostService(svc)` שהעמדה מספקת. כך **אותה** מפה של עמדת הבקר, על כל
שכבותיה, יושבת בתוך פריסת הדסק - ולא רכיב מפה שני לתחזק.

### 2. שני חילוצים ב-SectorDashboard, בלי שינוי התנהגות

| חילוץ | מה הוא משרת |
|---|---|
| `renderMapPanel(cfg)` (1,785 ש') | מפה יחידה, מפה כפולה, **וכל חלון מפה בדסק** |
| `renderStripsPanel()` (239 ש') | סרגל הפ"ממים של העמדה, **וכל חלון פ"ממים בדסק** |

שניהם הועברו **כמות שהם** מתוך ה-JSX (ולכן ההזחה נשארה של JSX): העברה מכנית,
בלי שינוי התנהגות - זה בדיוק ה-`<MapPanel>` ש-[DUAL_MAP_PLAN.md](DUAL_MAP_PLAN.md)
תכנן. הבסיס כבר היה שם: הבלוק היה `.map(cfg => …)` שכל משתניו מוצללים מ-`cfg`.

**ה-state של חלון מפה** יושב ברשומה ממופתחת (`mdSlots`) ולא בזוג hooks לכל מפה
כמו `map1*`/`map2*` - מספר החלונות נקבע בזמן ריצה, ואי אפשר לפרוס hooks לפי
מספר משתנה. מפתח החלון הוא מזהה **השירות** ולא מזהה המפה, כי שני חלונות יכולים
להציג את אותה מפה וצריכים זום, הזזה וסקטור נבחר נפרדים.

### 3. נקודות ההעברה בתוך החלון, העזרים בחוץ

נקודת העברה שייכת למפה. בעמדת בקר יש מפה אחת ולכן פאנל צד גלובלי מספיק; בדסק
יש כמה מפות על אותו מסך, ולכן הפאנל (`DraggableNeighborPanel` - **אותו** רכיב)
נפרס בתוך חלון המפה שלו, מתקפל. הדברים הגנריים - בד"חים, עזרים, קישורים, מצבי
בסיס - נשארו בסרגל העזרים הימני, כמו בכל עמדה.

### 4. מסך הניהול מקובץ - וב"מ מכווץ

[`AdminSection`](src/components/admin/AdminSection.tsx) הוא רכיב אחד לשני
השימושים: שש קטגוריות בטופס העמדה, וקבוצה לכל חלון מפה בדסק. זהות העמדה (שם,
סוג, דסק) נשארת פתוחה - היא קובעת מה שאר הטופס בכלל מציג.

שני פרטים שלא מובנים מאליהם: התוכן נשאר **mounted** גם כשהוא מכווץ (בוני
שאילתות ועורכי עמודות מחזיקים state מקומי שהיה נמחק בכל כיווץ), ומקטע שכל תוכנו
מותנה בסוג העמדה ויצא ריק - מוסתר לגמרי, כי כותרת שנפתחת לכלום גרועה מכותרת
שאינה קיימת.

### 5. חלון מפה בלי מפה חוסם שמירה

`mdMissingMapServices` בודק שלכל שירות `map` בדסק נבחרה מפה בהגדרת העמדה.
חלון בלי מפה הוא אזור ריק על מסך העמדה שהמפעיל אינו יכול לתקן בעצמו - ולכן
חסימה ולא אזהרה.

### QA

| בדיקה | תוצאה |
|---|---|
| `tsc --noEmit` | ✅ נקי |
| `vitest run` (107 קבצים) | ✅ 2,065 עוברות (מהן 12 חדשות ל-`missionDesk.ts`) |
| `npm run build` | ✅ |
| API מקצה לקצה (סוגי שירות חדשים, round-trip של `mission_desk_map_config` ב-POST/PUT/GET, דחיית סוג לא מוכר) | ✅ |
| e2e `mission-desk-maps.spec.ts` - המפה נטענת בתוך אזור הדסק, ממלאת את האזור שהוקצה לה, חלון הפ"ממים לצידה, סרגל העזרים בצד, בלי שגיאות ריצה | ✅ |
| e2e - מסך הניהול: קטגוריות מכווצות ב"מ, פתיחה חושפת תוכן, קבוצת מפה פר-חלון | ✅ |

> **מלכודת שהתגלתה בדרך:** שרת ה-vite של ה-e2e (`--port 5199`) שנשאר מהרצה
> קודמת **מגיש קוד ישן** - הבדיקה נופלת על שינוי שכבר בוצע. לפני הרצת e2e
> ב-worktree: להרוג את המאזין ב-5199.

---

## 2026-08-19 - "הצג מסך לדוגמה" בהגדרת עמדה, ושיכפול מפה מעוגנת ללא עזרים

**הבקשה:** (1) בהגדרת עמדה - "הצג מסך לדוגמה" על כל המסך. (2) יכולת שיכפול
מפה מעוגנת ללא עזרים.

### 1. מסך לדוגמה - המסך האמיתי של העמדה, על כל המסך

| | |
|---|---|
| **מה היה** | מי שהגדיר עמדה במסך הניהול לא ראה מה יצא. כדי לבדוק שינוי היה צריך לצאת, להתחבר לעמדה, ולחזור |
| **מה יש** | כפתור **🖥 הצג מסך לדוגמה** בטופס העמדה פורס את המסך **האמיתי** שלה על כל המסך, בזמן אמת, קריאה בלבד. Esc / ✕ סוגרים וחוזרים לטופס |

**בלי רכיב תצוגה חדש (DRY).** המסגרת היא אותה `?peek=<presetId>` של סרגל
ההצצה בעמדה: הריבוע והמסך המלא הם **אותו רכיב**, שחולץ ל-
[`src/components/shared/StationScreenFrame.tsx`](src/components/shared/StationScreenFrame.tsx)
מתוך `StationPeekBar`. הדמיה עם נתוני דוגמה הייתה רכיב תצוגה שני לתחזק, והייתה
מציגה מה שהמפתח חושב שהעמדה מראה - לא מה שהיא מראה.

**מה מוצג זה מה שנשמר.** המסגרת קוראת את הגדרות העמדה מהשרת, ולכן שינוי שעדיין
בטופס אינו בתוכה. במקום להשאיר את המגדיר בהנחה שהוא רואה את השינוי שלו - אזהרה
בכותרת כשהטופס "מלוכלך", וכפתור מושבת בעמדה חדשה שטרם נשמרה (אין לה מה להציג).

**קריאה בלבד בשתי שכבות** (כמו בסרגל): `pointer-events: none` על המסגרת +
`installPeekWriteGuard` שחוסם כל כתיבה ל-API במסמך ה-peek.

**תוך כדי:** המסגרת המשותפת **ממרכזת** את המסך בקופסה. קודם הוא הוצמד לפינה,
וכל קופסה שאינה 16:9 (חלון מוגדל, מסך מלא) קיבלה שוליים בצד אחד.

### 2. שיכפול מפה - עותק נקי

| | |
|---|---|
| **מה היה** | למפות לא היה שיכפול כלל. מפה חדשה על אותו רקע = העלאה מחדש של התמונה **וכיול מחדש** של שתי נקודות העיגון |
| **מה יש** | `POST /api/maps/:id/duplicate` + כפתור **⧉ שכפל** בשורת המפה בניהול, עם שם מוצע שניתן לשנות לפני היצירה |

**מה עובר לעותק:** התמונה, שמונת ערכי העיגון, ובסיס האב. **מה לא עובר
("ללא עזרים"):** אזורי המפה, בלוקי הגובה שלהם, נקודות ההעברה ותת-המפות. גם
הקשר לאב (`parent_map_id`/`parent_rect`) נשמט - תת-מפה משוכפלת היא מפה
**עצמאית**, ולא סקטור נוסף שיסתנכרן חזרה מהאב דרך `sync-zones-from-parent`
ויחזיר בדלת האחורית בדיוק את האזורים שלא רצו.

**שמות:** `"<שם> (העתק)"`, וכשתפוס `(העתק 2)`... הנקטעים ל-100 התווים של
העמודה (`maps.name` הוא `VARCHAR(100)` - בלי הקטיעה שם באורך מלא היה נופל
ב-22001). שם מפורש שכבר תפוס מוחזר 409 כמו ב-`POST /api/maps`.

**בלי מיגרציה** - אין טבלה ואין עמודה חדשה, רק שורת `maps` נוספת.

**QA:** `tsc` נקי · build נקי · 2035/2035 בדיקות + 12 חדשות ב-
[`server/routes/maps.duplicate.test.js`](server/routes/maps.duplicate.test.js)
(Postgres אמיתי ב-PGlite: העותק נושא תמונה ועיגון, ואפס אזורים ואפס נקודות) ·
e2e חדש [`e2e/admin-station-preview.spec.ts`](e2e/admin-station-preview.spec.ts)
מול ה-DB האמיתי: המסך לדוגמה נפרס ומצטלם ([`admin-station-sample-screen.png`](e2e/__screenshots__/admin-station-sample-screen.png)),
והשיכפול דרך הדפדפן יוצר עותק בלי אזורים ומנקה אחריו.

---

## 2026-08-18 - מיזוג מנוע ה-polling המאוחד, וקבלה-למפה אטומית

מיזוג `feature/transfer-accept-states` (9.7, 5 קומיטים) ל-main אחרי 40 יום של
סחיפה. חמישה קונפליקטים, ושתי הכרעות שכדאי לזכור:

| קונפליקט | ההכרעה |
|---|---|
| `transfers.js` - הענף הזריק inline את לוגיקת הקבלה, ש-main כבר חילץ בינתיים ל-`acceptTransferTx` | **נלקח main.** שמירת שני הצדדים הייתה מריצה מיזוג-אחים ומחיקת סטריפ **פעמיים** באותה טרנזקציה |
| ארבעה קבצי רכיב | שורות `import` בלבד - שני הצדדים נשמרו |

**מה הענף באמת הוסיף שחסר ב-main:** `POST /api/transfers/:id/accept-to-map` רץ
ב-`pool.query` בלי טרנזקציה ובלי מיזוג אחים. כלומר פ"מ מפוצל שנקלט **למפה** לא
התאחד עם אחיו שכבר יושב בעמדה, ואם עדכון סטטוס ההעברה נכשל - הסטריפ עבר אך
ההעברה נשארה `pending`, והפ"מ הופיע **בשתי עמדות**. עכשיו הנתיב עטוף
`BEGIN/COMMIT` וממזג כמו הקבלה הרגילה.

**איחוד:** אלגוריתם מיזוג-האחים היה קיים פעמיים - inline ב-`acceptTransferTx`
ובפונקציה `mergeWithSiblingIfAny` של הענף, בקוד זהה. אוחד לעותק אחד ששני מסלולי
הקבלה קוראים לו, כדי שלא יסטו זה מזה בשקט (זו הייתה כוונת הקומיט
"share sibling-merge between accept and accept-to-map").

**מנוע ה-polling:** נכנס כפי שהוא. 21 טיימרי הרשת שהענף היגר ב-SectorDashboard
עברו auto-merge נקי; חמישה `setInterval` שנוספו ל-main מאז נשארו כפי שהם (אחד
מהם שעון UI ולא polling), ולכן האימוץ חלקי ואין כפל-סקירה של אותו endpoint.

**QA:** `tsc` נקי · 2021 בדיקות עברו, 0 נכשלו · `vite build` נקי.
`server/routes/airPictureMaps.test.js` נפל ב-`afterAll` (timeout 10ש' בסגירת
שרת הבדיקה) תחת עומס הסוויטה המלאה, ועובר 7/7 כשהוא רץ לבדו - שביר קיים,
בקובץ שהמיזוג אינו נוגע בו.

**כיסוי חדש:** נתיב הקבלה - המנגנון הקריטי במערכת - היה עד כה **בלי אף בדיקה**.
נוספו 10 בדיקות ב-[server/routes/transfers.test.js](server/routes/transfers.test.js)
על `mergeWithSiblingIfAny` ו-`acceptTransferTx` (מיזוג חלקי, מיזוג שמשלים מצבה,
קבלה בלי אח, קבלה למוד טבלה, העברה שאינה קיימת), ובראשן נעילה על כך שהמיזוג רץ
**פעם אחת**. הבדיקה אומתה במוטציה: הזרקת קריאה כפולה מפילה אותה ב-`expected 2 to be 1`.

---
## 2026-08-16 - בלוקי גבהים עוברים בירושה לתת-מפות

**הבקשה:** להגדיר ב-DB שני בלוקי גבהים (גבוה 150-400, נמוך 100-140) לכל אזורי
מפת "מרחבי 305 סקטור", ואז - "תוריש לילדים".

| | |
|---|---|
| **מה היה** | סנכרון אזור-אב → אזור-ילד בתת-מפה העביר **שם, צבע ופוליגון בלבד**. אזור בתת-מפה ("אזורי ים", "אזורי יבשה") הופיע בלי אף בלוק גובה, ואי-אפשר היה להציב בו פ"מ לפי גובהו |
| **מה יש** | [`server/utils/zoneAltInherit.js`](server/utils/zoneAltInherit.js) מחיל את בלוקי האב על כל צאצאיו, לכל עומק (אזורי קרב → מרחבי 305 → אזורי ים/יבשה) |
| **בלי מיגרציה** | אין טבלה חדשה ואין עמודה חדשה - רק שורות `zone_altitude_ranges` נוספות |

**שני מצבים, ובכוונה לא אחד:**

| מסלול | מצב | מה קורה לילד |
|---|---|---|
| עריכת בלוק על האב (`POST/PUT/DELETE /api/zone-altitude-ranges`) | `mirror` | מראה מלאה - כולל מחיקת בלוק שאין לאב |
| סנכרון שם/צבע/פוליגון (`PUT /api/map-zones/:id`, `sync-zones-from-parent`) ויצירת אזור-ילד (`POST /api/map-zones` עם `parent_zone_id`) | `fill` | מילוי בלבד - ילד עם בלוקים משלו לא נדרס |

ההפרדה נחוצה: שינוי **שם** של אזור לא אמור למחוק חלוקת גבהים שנקבעה ידנית
בתת-מפה, בעוד מחיקת בלוק על האב כן צריכה להתגלגל למטה.

**התאמה לפי שם, עדכון במקום.** `strip_zone_assignments.altitude_range_id` הוא
`ON DELETE SET NULL` - מחיקה והכנסה מחדש היו מפילות **כל פ"מ מוצב** מהבלוק שלו
בכל עדכון של האב. לכן בלוק-אב מותאם לבלוק-ילד לפי השם, והשורה הקיימת מתעדכנת
ושומרת על ה-`id` שלה.

**נתונים שהוגדרו:** 48 אזורים (25 במפה 24 "מרחבי 305 סקטור", 10 ב-74 "אזורי ים",
13 ב-75 "אזורי יבשה") קיבלו גבוה 150-400 + נמוך 100-140. אזור `61 צפון` שהחזיק
200-300 / 100-190 עודכן במקום ולא נמחק.

**QA:** `tsc` נקי · 1948/1948 בדיקות (16 חדשות ב-`zoneAltInherit.test.js`) ·
אימות end-to-end מול ה-DB האמיתי עם ה-router האמיתי (עץ מפות זמני שנמחק בסוף):
10/10 - הוספה מתגלגלת לילד ולנכד, עדכון שומר על ה-`id`, מחיקה מתגלגלת, שינוי
שם/צבע לא דורס בלוק ייחודי של הילד, ואזור-ילד חדש נולד עם בלוקי האב.
---

## 2026-08-16 - ביטול פעולה (CTRL+Z) לכל המערכת

**הבקשה:** "אני רוצה פיצ'ר לכל המערכת של CTRL+Z שמחזיר לאחור כמה פעולות אחרונות.
כל פעולה חזרה אחורה תגרור התראה לפני אישור ואחרי אישור זה יתבצע. לכן יש לשמור
היסטוריה אחורה של עד 5 דק' על כל פעולה / יישות."

שלוש החלטות אפיון נסגרו מול המשתמש לפני המימוש: המחסנית היא **הפעולות שלי
מהעמדה שלי** · **העברות עמדה ו-GAPI חסומות לביטול** · **הגדרות תצוגה אישיות
אינן נכללות**. הפירוט המלא ב-[UNDO_SPEC.md](UNDO_SPEC.md).

| | |
|---|---|
| **מה היה** | פעולה שבוצעה בטעות (מחיקת פ"מ, גרירה לאזור הלא נכון) הייתה בלתי הפיכה. זה מה שהסדק הפיזי גם כן לא ידע לעשות |
| **מה יש** | CTRL+Z בכל מסך → חלון אישור שמראה מה מתבטל, מתי נעשה, ומי נגע בזה מאז → ביצוע. עד 5 דקות אחורה, כמה פעולות ברצף |

### למה טריגר ב-DB ולא קוד ב-endpoints

יש **~340 endpoints שכותבים**. "לכל פעולה פעולה הפוכה" היה מחייב לגעת בכולם,
ולהשאיר את מי שנשכח בלי ביטול **ובלי שאיש ידע**. הטריגר הגנרי תופס גם את מה
ש-endpoint אינו יודע שהוא עשה - `ON DELETE CASCADE`, עדכון גורף, טריגר אחר -
ומחיל את עצמו על **טבלה חדשה** בעלייה הבאה בלי שאיש יזכור. אותו שיקול שהוביל
ליירוט fetch יחיד בלקוח (אימות/סביבה/נתק) ולטבלת ה-RULES הגלובלית באימות.

### הרכיבים

| שכבה | קובץ |
|---|---|
| יומן + טריגר גנרי | `server/db/undoJournal.js` (בתבנית `versionedTables.js`), מותקן ב-`init.js` וב-`envs.js` |
| הקשר הפעולה | `server/db/action-context.js` + הזרקת `SET LOCAL app.action_id` ב-`pool.js` |
| פתיחת פעולה | `server/middleware/actionContext.js` (אחרי הקשר הסביבה, לפני כל ה-routers) |
| תוויות והיפוך | `server/undo/labels.js`, `server/undo/revert.js` |
| API | `server/routes/undo.js` - `stack` · `next` (כולל התנגשות) · `POST :id` |
| לקוח | `src/utils/undoStation.ts` (יירוט `X-Station`), `src/utils/undoApi.ts`, `src/components/shared/UndoManager.tsx` |

### מלכודת ה-pooler, שוב

`SET LOCAL` שדולף בגלל reassignment של `client.query` כבר שבר כאן את
`search_path` פעם אחת. אותו מנגנון עם מזהה פעולה היה מצרף כתיבה של בקשה אחרת
לפעולה ישנה - וביטול היה מחזיר לאחור שינוי שאיש לא ביקש. לכן **עטיפה אחת בלבד**
(`wrapClient`), היא מציבה את המזהה **תמיד** (ערך אמיתי או ריק), וה-connection
שלה מושמד בשחרור. הנתיב החם (`pool.query` בלי טרנזקציה מפורשת) אינו עושה
reassignment ולכן אינו משלם על כך.

### מה נשמר מהתנגשות

`after` ביומן הוא צילום השורה **אחרי** שטריגר הגרסה העלה את `rev`. לכן כל נגיעה
מאוחרת יותר, גם מעמדה אחרת, מזיזה את `rev` ונתפסת. ביטול מול התנגשות **נעצר**
ומחזיר 409; דריסה מודעת דורשת `force` ונרשמת ב-`activity_log`.

**QA:** `tsc` נקי · `vite build` נקי · **1992/1992 בדיקות (58 חדשות)**, מהן
19 אינטגרציה ו-13 HTTP מקצה-לקצה מול **Postgres אמיתי** (PGlite): יצירה→ביטול,
עדכון→שחזור ערכים, מחיקה→שחזור עם אותו מזהה, שני ביטולים ברצף, ביטול כפול
נחסם, בידוד בין עמדות, זיהוי שלושת סוגי ההתנגשות, טבלה חסומה, וטבלה בלי מפתח
ראשי. **טרם הורץ מול Neon** - ראה הערה למטה.

> ⚠️ **התקנת הטריגרים ב-DB המשותף** מתבצעת ב-`initDb()`, כלומר בעליית השרת
> הראשונה של הענף הזה. הטריגר אינרטי בלי `app.action_id`, ולכן שאר העצים
> אינם מושפעים - אבל זו כן נגיעה בסכמה המשותפת, ולכן היא נעשית ביודעין.

---

## 2026-08-15 - תצוגת פ"מ נבחרת פר תא בחלון סטריפים

**הבקשה:** "לכל חלון סטריפים רוצה יכולת לבחור תצוגת פ"מ סטריפ אזרחי שונה".
הובהר מול המשתמש: הבחירה היא **פר תא**, וחלה על כל פ"מ שמוצג בו - "אזרחי" מתאר
את **סגנון התצוגה** ולא את סוג הפ"מ.

| | |
|---|---|
| **מה היה** | כל תאי החלון ציירו את הפ"מ בתצוגת ה**עמדה** (`classic_strip_table_id`, יום/לילה). תא CLEARANCE ותא העברה נראו זהים |
| **מה יש** | לתא יש `strip_table_id` אופציונלי - בורר ב"הגדרות תא" מתוך `classic_strip_tables`. ריק = תצוגת העמדה, בדיוק כמו קודם |
| **בלי מיגרציה** | השדה יושב ב-`layout_json` (JSONB) של `strip_window_layouts`, וה-route שומר את העץ כמו שהוא - אין עמודה חדשה ואין שינוי בשרת |

**נפילה חזרה:** תצוגה שנבחרה לתא ואחר כך נמחקה בניהול אינה מרוקנת אותו -
`swResolveStripTable` מחזיר את תצוגת העמדה, והפאנל מציג אזהרה כדי שהמנהל יידע.
השוואת ה-id מספרית, כי הערך חוזר מ-JSONB ולעתים כמחרוזת.

**QA:** `tsc` נקי · 1928/1928 בדיקות (6 חדשות) · `vite build` נקי · round-trip
מול ה-DB האמיתי על שורה זמנית שנמחקה בסוף: התא שבחר שמר את הבחירה, והתא שלא
בחר חזר `undefined` (כלומר יורש).

---

## 2026-08-14 - מפות מעוגנות ב-ATSIM, שיתופן לעמדה, ותיקון פיגור הרקע

**הבקשה:** "רוצה ש-ATSIM תוסיף יכולת להוסיף מפה עם עיגון כמו בהוספת מפה.
ואפשר לשתף את המפות על SKYKING בלי יכולת לעדכן את DB SKYKING אם יעשה שינוי.
יש גם בעיה בשינוי מיקום וזום - לוקח הרבה זמן למפה לזוז וקודם הרכיבים האוויריים
זזים ואז המפה."

שני ריפוז: `atsim@7ba469f` ו-`skyboard@814514f` (v1.0.71).

### 1. עיגון בשתי נקודות במקום גבולות מוקלדים

| | |
|---|---|
| **מה היה** | `ATSIM_MAP_BOUNDS=latMin,latMax,lonMin,lonMax` ב-`.env`, תמונה אחת, בלי UI |
| **הבעיה** | אלו גבולות **קצוות התמונה** - נתון שאיש אינו יודע. שגיאה בו מציבה כל מטוס במקום הלא נכון בלי שום סימן חיצוני (ה-README של ATSIM כבר הזהיר מכך) |
| **התיקון** | `anchorsToBounds` - שתי נקודות מזוהות (אחוזי תמונה + נ"צ), והגבולות נגזרים באקסטרפולציה. **אותו מודל של `anchor1_x_img`** בטבלת `maps`, ולכן מפה עוברת בין המערכות בלי תרגום |
| **אחסון** | `atsim/mapStore.js` בתבנית `store.js`, בהבדל אחד: **הבייטים לעולם אינם במטמון**. מפת שדה היא 8 מגה-בייט, והרשימה נקראת בכל טעינת FRONT |

### 2. שיתוף לעמדה - בלי נגיעה ב-DB

הדרישה המפורשת. לכן **אין טבלה, אין מיגרציה, ואין נתיב שבו המאגר כותב**:
המפה נמשכת `pull-through` דרך אותו ריליי, אותו `base_url` ואותו אסימון של
התמונ"א, ומוגשת ממטמון בזיכרון התהליך בלבד.
המחיר מפורש: מאגר לא זמין = אין מפה. עדיף מעותק ב-DB שהתיישן בשקט.
ב-ATSIM הנתיבים יושבים תחת `/air-picture/*` ולא תחת `/api/*` - זו ההבחנה
שכבר קיימת שם בין הפן של העמדה לפן של המאגר, ומכאן שאין בהם כתיבה כלל.

**`MapGeoAnchor.projection`:** מפה שצולמה ממפת רשת היא במרקטור, ושם מעלת רוחב
מתארכת. קריאה ליניארית שלה מזיזה מטוס בקילומטרים באמצע התמונה - סטייה שאינה
נראית על המסך. ברירת המחדל היא ההתנהגות ההיסטורית ו-`buildGeoAnchor` אינו
מייצר את השדה, ולכן **כל מפה קיימת מתנהגת בדיוק כמו קודם** (יש בדיקה על כך).

### 3. "קודם הרכיבים האוויריים זזים ואז המפה" - שני באגים

| | |
|---|---|
| **תמונה מקומית** | `transform-origin: cx% cy%` + `scale(z)` **אינה** הטרנספורמציה ש-`zoomBounds` מבצעת. הן מסכימות רק ב-`center=0.5`; בכל pan אחר הרקע זז ב-`δ(z-1)` בעוד המטוסים זזים ב-`δz` - בזום ×2 **חצי מהמהירות**. בנוסף האחוזים נמדדו ביחס ל-`#stage` ולא לתמונה, ובלטרבוקס זה היסט נוסף |
| **רקע Google** | לא הוזז **בכלל** בגרירה (`transform = ''`), והוחלף רק בשחרור אחרי round-trip לשרת ולגוגל. הרקע קפא והמטוסים החליקו עליו |
| **התיקון** | `bgPlacement` גוזר `translate+scale` מ**אותם bounds** שמזינים את המטוסים. מכיוון שהוא מקבל את מה שהתמונה *באמת* מכסה, אריח ישן ממוקם נכון בדיוק כמו טרי - הרקע נע עם היד, והבקשה החדשה רק מחדדת אותו. השהיית הרשת נעלמת |
| **בונוס** | הבקשה מושהית, ההחלפה אחרי `decode` (בלי הבהוב לבן), וה-`blob` הקודם משוחרר - הייתה דליפת תמונה שלמה בכל זום ובכל pan |
| **באג שהתיקון חשף** | `panCenter` נירמל את הגרירה לפי הקנבס ולא לפי מלבן ההכלה של התמונה. במפה בלטרבוקס המפה זזה **לאט מהיד** - נמדד 39px על גרירה של 60px |

**QA:** ATSIM 193 בדיקות (67 ב-`geo`, כולל בדיקות-שלילה שנכשלות אם השיטה
הישנה תחזור) · SKY-KING `tsc` נקי, 1818 בדיקות (34 חדשות), `vite build` נקי ·
בדיקת הריליי מרימה מאגר ATSIM **אמיתי** ומנטרת את `pool.query` כך שכל נגיעה
ב-DB מפילה אותה · smoke בדפדפן (Playwright): הרקע נמדד **1:1 עם המצביע** -
‎-120.0px על גרירה של ‎-120px, מול ‎-60px שהיו נדרשים בשיטה הישנה.

**נותר ידוע:** `zoomBounds` מצמצם תמיד במרחב מרקטור, גם למפה ליניארית, ולכן
ב-pan אנכי על מפה כזו יש סטייה של ~1.4% מהיד (0.85px על 60px). הרקע והמטוסים
**נעולים זה לזה** בכל מקרה, ולכן זה קוסמטי.

---

## 2026-08-14 - מטופס חברי העמדה ישר למסך הטעינה (בלי לחזור לבחירת עמדה)

**הבקשה:** "אחרי מסך הזה בכניסה לעבור ישר למסך טעינה, לא לחזור למסך בחירת עמדה."

**מה קרה בפועל:** טופס חברי העמדה נפתח **מעל** רשימת בחירת העמדה. ברגע שהבקר
אישר, הטופס נסגר והרשימה נחשפה שוב לכל אורך הכניסה לשרת (קביעת סביבה +
`POST /workstations/login`) - הוא ראה את עצמו "חוזר אחורה" בדיוק אחרי שאישר,
ורק אז עלה מסך הטעינה של העמדה.

| | |
|---|---|
| **[`StationLoadingScreen`](src/components/shared/StationLoadingScreen.tsx)** | מסך הטעינה הופרד מ-`SectorDashboard` לרכיב משותף, ומוצג **פעמיים ברצף** באותה עלייה: במסך הכניסה מרגע אישור הטופס, ובעמדה עד שהמידע והמפות הגיעו. אותו רכיב = מסך אחד רציף לעין |
| **שלבי הטעינה** | מוגדרים ברכיב (כניסה לעמדה · טעינת נתוני שדה · עליית מפות ואזורים), והמציג רק מסמן אילו הסתיימו. קודם הם היו מחרוזות עברית מקודדות ב-`SectorDashboard`; עכשיו ב-registry (`shared.loadingStep*`) |
| **[`App.tsx`](src/App.tsx)** | `handlePresetLogin` סוגר את מודל בחירת העמדה ומעלה את מסך הטעינה **לפני** הבקשות. בסיס האב נפתר פעם אחת בראש הפונקציה (במקום שוב אחרי ההתחברות) כי גם מסך הטעינה מציג את הסמל. כישלון כניסה מסיר את המסך ומחזיר את מסך הכניסה עם השגיאה |
| **[`utils/themeMode.ts`](src/utils/themeMode.ts)** | `readStoredThemeMode()` - מקור אמת יחיד לתמה השמורה, כדי שמסך הטעינה של הכניסה ייצבע בתמה שהעמדה תיפתח בה ולא יבזיק בצבע אחר. `SectorDashboard` צורך אותו במקום לקרוא ל-`localStorage` בעצמו |

**QA:** `tsc` נקי · `vite build` נקי · 1788 בדיקות יחידה עוברות (2 נפילות
timeout ב-`server/auth/localCredentials` - scrypt איטי תחת עומס מקבילי, עוברות
בהרצה נפרדת; לא קשורות לשינוי) · e2e חדש
[`login-loading-screen.spec.ts`](e2e/login-loading-screen.spec.ts) תופס את המצב
הראשון שמופיע אחרי סגירת הטופס. **בקרה שלילית:** עם הקוד הישן הבדיקה מחזירה
`picker` במקום `loader`.

---

## 2026-08-12 - קיבוע עמודות בכל טבלת בן, גם העתידיות

**הבקשה:** "תעשה יכולת במסך ניהול לקבע שדות שאם יש סקרול הן תמיד יראו כמו
שעשינו בפ"מ - זה רלוונטי לכל טבלה שקיימת ושתהיה בעתיד."

**למה זו לא קוסמטיקה:** טבלת נקודות המכוון היא 15 עמודות וטבלת המטוסים 13 -
שתיהן נגללות אופקית. בגלילה עמודות הזיהוי (שם מטרה / מספר זנב) יוצאות מהמסך,
והבקר נשאר מול מספרים בלי לדעת **של מי** הם. זו טעות שקטה: אין הודעה, אין
אינדיקציה, רק החלטה על סמך שורה לא נכונה.

**בניהול:** כפתור 📌 לכל עמודה בבוחר של טבלת הבן - אותה שפה בדיוק כמו הקיבוע
שברמת הפ"מ (העמודות עד כאן, כולל, נשארות גלויות). נשמר ב-`frozenColumns` על
עמודת הטבלה במוד הטבלה, וב-`tableFrozenColumns` על התא בפ"מ הקלאסי.

**גנרי לכל טבלה:** `defaultFrozen` נוסף ל-`SubTableDef` ברישום, ו-
`subTableFrozenCount` מכריע - מה שהוגדר בניהול גובר, ובלעדיו ברירת המחדל של
הטבלה. טבלת בן שתתווסף בעתיד מקבלת קיבוע מרגע שהיא נרשמת, בלי נגיעה בקוד
התצוגה. הקיבוע חל בשלושת המקומות שמרנדרים טבלת בן: השורה הנפרסת בעמדה, חלון
העריכה הצף, והתא בפ"מ הקלאסי.

**ההיסטים מחושבים מהרוחב המוצהר ברישום ולא ממדידת DOM.** הטבלה מרונדרת בתוך
לולאה על הפ"מים (שורה נפרסת לכל פ"מ פתוח), ושם אי-אפשר לקרוא hook שימדוד -
זו הפרת כללי ה-hooks. במקום זה העמודות **המקובעות בלבד** מקבלות את הרוחב
המוצהר ב-`width` קשיח, ולכן ההיסט הוא סכום פשוט ומדויק: בלי מדידה, בלי ריצוד,
ובלי תלות באורך התוכן. שאר העמודות נשארות נגזרות-תוכן.

`insetInlineStart` ולא `right`: הקיבוע נשאר בקצה **המוביל** גם באנגלית (LTR),
שם הוא משמאל. לתא מקובע יש רקע אטום - תא דביק שקוף היה מראה את מה שנגלל תחתיו.

**QA:** בדיקות להכרעת הקיבוע (מוגדר מול ברירת מחדל, 0 מפורש, חיתוך לקונפיג ישן
שמקבע עמודה שכבר אינה שם) ולחישוב ההיסטים. 1828 בכל החבילה · tsc נקי · build עובר.

---

## 2026-08-13 - קו הציור על המפה חזר לעובי שנבחר (רגרסיה מ---s)

**הבקשה:** "ציור על מפה נהיה קו עבה פתאום, פעם עבד טוב."

**הרגרסיה:** [`80a3273`](80a3273) (2026-08-08) תיקן את **היסט המלבן/העיגול**
במסך 24" והחליף את מדידת אזור המפה מ-`getBoundingClientRect()` ל-`clientWidth`.
המדידה נכונה לשכבת ה**צורות** (SVG ב-`width:100%` מפרש קואורדינטות בפיקסלי
פריסה), אבל היא הוחלה גם על ה-**bitmap** של הקנבס - ושם היא שגויה: `#root` יושב
תחת `zoom: var(--s)`, ולכן bitmap בגודל הפריסה נמתח בהצגה פי --s.

| | |
|---|---|
| **מה נראה** | קו העט עבה ומטושטש פי `--s` - 1.65 בעמדת 24", 1.22 ב-18", 1.05 ב-16". במסך 15.6" (`--s=1`) לא היה שינוי כלל, ולכן זה לא נתפס בפיתוח |
| **נמדד בדפדפן** | משטח 600 פריסה = 990 על המסך; עט `lineWidth:1.5` תפס **2.475** פיקסל מסך עם bitmap לפי `clientWidth`, ו-**1.5** בדיוק עם bitmap לפי גודל המסך |
| **התיקון** | [`bitmapPx` / `syncCanvasBitmap`](src/utils/mapDrawing.ts) - ה-bitmap נבנה בפיקסלי **מסך** (`clientWidth × --s`): פיקסל קנבס = פיקסל על הזכוכית. שכבת הצורות ממשיכה לקבל פיקסלי פריסה, ולכן תיקון ההיסט מ-80a3273 נשמר |
| **DRY** | אותו helper בשני מנועי הציור - הרכיב המשותף ([MapDrawLayer](src/components/map/MapDrawLayer.tsx), עמדת השדה) ומנוע עמדת הבקר ([SectorDashboard](src/components/views/SectorDashboard.tsx)). `draw` בעמדת הבקר עבר ל-`applyStrokeStyle` המשותף במקום לשכפל עובי/צבע/מחיקה |
| **מדידה עצמית** | כל קנבס נמדד מ**עצמו** ולא מ-`#map-area`: בדו-מפה כל קנבס תופס חצי מהאזור, ומדידת האזור כולו נתנה יחס גובה-רוחב שגוי ל-bitmap - קו אופקי ואנכי בעובי שונה |
| **קביעת גודל אחת** | הוסרה קביעת גודל שנייה (`window.resize` לפי `parentElement`) שכתבה ל-`canvas.width` במידה אחרת ו**מחקה** את ציור מפה 1 בכל שינוי גודל חלון. הכל דרך ה-`ResizeObserver` היחיד |
| **קנבס מפה 2** | נרשם ל-`ResizeObserver` ברגע שהוא נכנס ל-DOM (`WeakSet` נגד רישום חוזר ולולאה) - קודם הוא נשאר עם bitmap ברירת המחדל 300×150 מתוח על חצי מסך עד שינוי גודל החלון |

**QA:** `tsc` נקי · 18 בדיקות יחידה ל-`mapDrawing` (6 חדשות) · `vite build` נקי ·
e2e: `map-draw` 18/18 (3 חדשות - עובי הקו על המסך ב-`--s` 1 / 1.22 / 1.65),
ו-`ctrl-draw-canvas` חדש שנכנס לעמדת בקר **חיה** ומודד את הקנבס בשני הסקיילים.
**בקרה שלילית:** עם הקוד הישן הבדיקות נופלות בדיוק ביחס `--s` (1.22 ו-1.65
במתקן; 360px חוסר ברוחב ה-bitmap בעמדה החיה).

> נשארו במדידת פריסה (מחוץ להיקף הדיווח): קנבס הדסק החופשי (`swDoRedraw`) וקנבס
> החתימה - שם הקו מטושטש/עבה באותו יחס.

---

## 2026-08-13 - החלק המפוצל נוחת ליד המקור: באזור שלו, בשורה שמתחתיו, בלי קונפליקט

**הבקשה:** "כשמפצל פ"מ על מפה עם אזורים צריך שפ"מ המפוצל יופיע באותו אזור ליד
(בלי קונפליקט). כשמפצל פ"מ במוד טבלה צריך שתיווצר שורה של הפ"מ המפוצל מתחת לפ"מ
שממנו פוצל (בלי קונפליקט)."

**מה קרה בפועל:** אחרי פיצול, החלק **נעלם מהמפה** - והופיע בתחתית הטבלה, מנותק
מהמבנה שממנו יצא. ואם כן הופיע באזור, שני החלקים הבהבו מיד באדום זה על זה.

| | |
|---|---|
| **למה נעלם מהמפה** | הלקוח קרא מהתשובה שדה בשם `newStripId`, בעוד ש-`POST /api/strips/partial-create` מחזיר `partialStripId`. הערך יצא `undefined`, תנאי ההעתקה נכשל בשקט, ו**הקצאת האזור לא נוצרה כלל**. הפ"מ נוצר - אבל בלי אזור אין לו פין על המפה |
| **למה בתחתית הטבלה** | סדר הטבלה הוא `tableRowOrder`, וסנכרון הסדר מוסיף כל פ"מ **חדש** בסוף הרשימה. החלק אכן חדש - ולכן נחת הכי רחוק שאפשר מהמבנה שממנו פוצל |
| **למה קונפליקט** | גלאי הקונפליקט משווים גובה מול גובה. שני חלקים של אותו מבנה נמצאים **תמיד** באותו גובה ובאותו אזור - ולכן כל פיצול הדליק לעצמו התראה. שלושה גלאים + שני דיאלוגי תיאום סבלו מכך |
| **[`formationSplit.ts`](src/utils/formationSplit.ts)** | מודול טהור אחד לשלושת הצדדים: `isSameFormation` (אחים לפי שורש `parent_strip_id`), `insertAfter` (מיקום השורה), `splitPinPosition` (מיקום הפין). 24 בדיקות |
| **מיקום הפין** | סריקה בטבעות סביב המקור, 12 כיוונים בכל טבעת, ועצירה בנקודה הראשונה שגם **בתוך הפוליגון** של האזור וגם רחוקה מספיק מכל פין אחר. הקוד הקודם הוסיף `+7` ל-X בלבד - קו ישר שדחף החוצה מכל אזור שגבולו מימין |
| **אזור לפני רווח** | כשאין נקודה שעונה על שניהם מוחזרת נקודה בתוך האזור גם אם צפופה. פ"מ מחוץ לאזור שלו הוא מידע **שגוי**; צפיפות היא אי-נוחות שהבקר פותר בגרירה אחת |
| **שתי מפות** | הקצאות המפה השנייה יושבות ב-state נפרד (`map2Assignments`), ולכן חיפוש רק ב-`stripZoneAssignments` פספס פיצול שנעשה עליה. גם "מי כבר תפוס" נמדד רק מול פינים של **אותה** מפה - אחוזים של מפה אחרת אינם אותו מרחב |
| **מסלול אחד לכל התצוגות** | מפה, טבלה, קלאסי ומגדל מגיעים כולם ל-`handleSplitPartial` דרך `sectorSplitModal`. התיקון חל על ארבעתם בלי שכפול |

> **החרגת האחים אינה גורפת.** `formationRootId` נופל ל-id של הפ"מ עצמו כשאין
> `parent_strip_id`, ופ"מ מול עצמו אינו "אח" - אחרת כל פ"מ היה מסתיר את
> הקונפליקטים של עצמו. שני פ"ממים שלא פוצלו מעולם נשארים שני שורשים שונים.

**QA:** 1,708 בדיקות יחידה ירוקות (24 חדשות ל-`formationSplit`) · `tsc` נקי ·
build עובר. שני קבצי בדיקה נכשלים בטעינה מסיבה שקדמה לשינוי ואינה קשורה אליו -
`@electric-sql/pglite` (תלות של המאגר המקומי) אינה מותקנת ב-`node_modules`.

---

## 2026-08-12 - שדות טבלת המטוסים נערכים בתא, ולא "לקריאה בלבד"

**הבקשה:** "יכולת לעדכן את השדות האלו - בניהול מערכת כתוב לקריאה בלבד" (על
תקלה, מהות התקלה ופירוט התקלה בבורר של מוד הטבלה).

**מה היה שגוי בהחלטה הקודמת:** סימנתי את **כל** טבלת המטוסים `readOnly`, בנימוק
ששורה היא רשומת DB ולא איבר במערך שנשמר בכתיבה אחת. הנימוק נכון לגבי **אופן
השמירה** - אבל ממנו לא נובע שאי אפשר לערוך. במיוחד התקלה: היא **דיווח של הבקר
בעמדה**, יש לה כבר מסלול כתיבה משלה, וזה בדיוק השדה שאמור להתעדכן חי.

| | |
|---|---|
| **מה השתנה בהגדרה** | `readOnly` הוחלף ב-`rowWrite: 'strip-field' \| 'aircraft-row'` - הצהרה על **איך** נשמר תא, ולא על האם. נקודות המכוון נשארות `strip-field` (המערך **הוא** השדה) |
| **מה נערך** | תקלה במתג · מהות התקלה · פירוט התקלה · מספר זנב · טייס · נווט · סגול 1/2 · דת"ק · כיפה |
| **מה לא** | `idx` (המפתח), חימושים ומערכות - הם שיטוח של טבלאות בן שלמות לטקסט אחד, ואי אפשר לפרק הקלדה חזרה לשורות. הבורר מציג להם "לקריאה בלבד" **מפני שאין לאן לכתוב**, ולא כברירת מחדל גורפת |
| **מהות התקלה מהתפריט** | `datalist` מ-`/api/fault-types` (התפריט שמנהל מנהל המערכת) - מנחה אך לא חוסם, כך שמהות שטרם נרשמה עדיין ניתנת לדיווח |
| **שני מסלולים** | `aircraftRowWrite` ([stripAircraft.ts](src/types/stripAircraft.ts)) מחזיר נתיב+גוף: התקלה ל-`/fault` (שלושת השדות יחד), השאר ל-`PUT /strip-aircraft/:stripId/:idx` בעדכון חלקי. שמירה פר-שורה ולא פר-מערך - אחרת עמדה אחת הייתה דורסת שורה שעמדה אחרת עדכנה באותו רגע |

> **הקלדת מהות/פירוט מדליקה את הדגל.** השרת מתעלם משניהם כשהדגל כבוי, ולכן
> בלעדיה מה שהוקלד היה נעלם בשקט. כיבוי נעשה במתג בלבד - ניקוי הטקסט אינו מכבה,
> כי "יש תקלה, פרטים בהמשך" הוא מצב לגיטימי.

**QA:** 1631 בדיקות ירוקות (5 חדשות לשמירת תא) · `tsc` נקי · smoke מול שרת חי
שהריץ בדיוק את מה שהתא שולח: שם טייס נשמר · הקלדת מהות על שורה שדגלה כבוי
הדליקה אותו · פירוט לא איבד את המהות · עריכת דת"ק לא נגעה בתקלה · כיבוי הדגל
ניקה מהות ופירוט והדת"ק שרד.

---

## 2026-08-12 - קבלה אוטומטית של פ"מ בנקודת מעבר (תרגול תהליך ההעברה ב-MVP)

**הבקשה:** "לטובת שלב ה-MVP שאין עמדה מקבלת אלא רק עמדה אחת, אבל עדיין רוצים
לתרגל את התהליך - נגדיר נקודות מעבר שעושות 'קבל פ"מ' כאילו מישהו קיבל ידנית.
במסך הניהול נגדיר על נקודת המעבר 'קבלה אוטומטית': 'מיידית' או 'לאחר הזמן
שמוקצה לפ"מ להגיע'."

**הבעיה:** עם עמדה אחת אין מי שילחץ "קבל", וכל העברה נשארת `pending` לנצח -
כך שאי אפשר לתרגל את מחצית התהליך שאחרי השליחה.

| | |
|---|---|
| **ההגדרה** | `sectors.auto_accept_mode` (`off` / `immediate` / `eta`) - יושבת על **נקודת המעבר** ולכן חלה על כל תת-נקודותיה ועל כל עמדה ששולחת אליה. נערכת בלשונית "נקודות העברה" במסך הניהול, ומופיעה כתגית ברשימת הנקודות |
| **מתי מבשילה** | [`server/utils/autoAccept.js`](server/utils/autoAccept.js) - מודול **טהור** (בלי DB) עם 11 בדיקות. `eta` נספר מ-`eta_set_at` ולא מיצירת ההעברה, כי הבקר יכול לעדכן את הזמן אחרי השליחה והמונה על הכרטיס מתחיל מחדש |
| **DRY - אותה קבלה בדיוק** | הליבה חולצה מה-route ל-`acceptTransferTx(client, id, receivingPresetId)`, והקבלה האוטומטית קוראת לה. מימוש שני היה מתפצל בשקט ממיזוג אחים אחרי פיצול ומרישום `strip_table_assignments` |
| **בשרת ולא בלקוח** | הקבלה חייבת לקרות גם כשאיש אינו צופה במסך, ובלי לשכפל את ההחלטה לשלוש התצוגות (מפה/קלאסי/מגדל). סבב כל 5ש' מ-`server.js` דרך `forEachEnvironment`, עם `inFlight` נגד חפיפה |
| **בלי קבלה כפולה** | "תפיסת" השורה בתוך הטרנזקציה (`UPDATE ... WHERE status IN ('pending','acknowledged')`) - קבלה/דחייה/ביטול ידניים שקדמו מנצחים, וסבב מקביל מקבל 0 שורות ומדלג |
| **fail closed** | `normalizeAutoAcceptMode` מחזיר `off` לכל ערך לא מוכר. פ"מ שנקלט מעצמו בטעות חמור מפ"מ שממתין |
| **בלי זמן מוקצה** | `eta` בלי `eta_minutes` נקלט מיד - אחרת פ"מ שנשלח בלי ETA היה נתקע לנצח **דווקא** בנקודה שהוגדרה אוטומטית |
| **יומן** | `activity_log.transfer_accepted` עם `details.auto = true` (אין `req.user` - המערכת קיבלה, לא אדם) |

**QA:** `tsc` נקי · 1,637 בדיקות יחידה (86 קבצים) ירוקות כולל שומר ה-i18n · build
עובר · `node --check` על כל קובץ `.js` שהשתנה · smoke מול ה-DB האמיתי (11 בדיקות):
נקודה כבויה נשארת `pending`, `immediate` נקלטת ומעדכנת את הפ"מ ואת היומן, סבב חוזר
לא מקבל שוב, `eta` לא נקלטת לפני הזמן ונקלטת אחריו, ו-`eta` בלי זמן מוקצה נקלטת מיד.

**לא נכלל:** נקודות מעבר פרוביזוריות והעברות קלאסיות עמדה-לעמדה - אין להן
`to_sector_id`, ולכן ההגדרה שיושבת על נקודת המעבר אינה חלה עליהן.

---

## 2026-08-13 - טבלת בן: מה שהוגדר לעריכה בניהול פתוח בעמדה, בלי מתג נעילה

**הבקשה:** "בטבלת נק מכוון ומטוסים כשפתחתי שדה לעדכון זה לא נותן לעדכן בטבלה
שמתחת לפמ" - ואחר כך, על התיקון הראשון: "לא צריך מתג נעילה - צריך שיהיה פתוח
תמיד אם הגדרתי ככה בהגדרות טבלה בניהול."

**האבחנה:** ההגדרה נשמרה כשורה (`fault_type: "keyboard"`, `air_verified:
"toggle"` וכו' במוד "בתחק חזית"), אבל בעמדה עמד שער **שני** - נעילת כתיבה
פר-עמודה (`tableEditableCols`). התא נראה "לא נותן לעדכן" כי `editable` היה
`false`, וה-✏️ ששחרר אותו ישב בכותרת הטבלה **הראשית** - עמודה שיכולה להיות
מגוללת הרחק מהטבלה שנפרסה מתחת לפ"מ, בלי רמז לקשר ביניהן.

| | |
|---|---|
| **הכרעה** | ההגדרה בניהול היא **מקור האמת היחיד**. מי שלא רוצה שדה פתוח - לא מגדיר אותו לעריכה, ולא נועל אותו שוב בעמדה |
| **מה הוסר** | תנאי `editableHere` בתא של טבלת הבן, וה-✏️ בכותרת עבור עמודת טבלת בן (הוא הפך למתג שאינו שולט בכלום) |
| **מה נשאר** | `tableEditableCols` לעמודות פ"מ **רגילות** - שם הנעילה מגנה על כתיבה ישירה לשדה הפ"מ, וההתנהגות לא שונתה |
| **צד הדגל** | עמודת דגל (תקלה / עצור תקיפה) נפתחת אף היא: היא מרונדרת כ-checkbox במקום כ-✓ סטטי |

> **דרך צדדית שננטשה:** התיקון הראשון הוסיף מתג נעילה על הטבלה הפרוסה עצמה, כדי
> שלפחות יהיה נראה. המשתמש פסל - נעילה שנייה על גבי הגדרה שכבר נקבעה בניהול היא
> צעד מיותר, לא בעיית גילוי. המתג הוסר לגמרי.

**QA:** `tsc` נקי · `vite build` נקי · 1667 בדיקות יחידה ירוקות · **e2e חדש**
([`e2e/sub-table-cell-edit.spec.ts`](e2e/sub-table-cell-edit.spec.ts)) שרץ על
עמדה **קיימת** ומוד טבלה **קיים** ("בת"ק עזה" / "בתחק חזית") ולא על מתקן סינתטי:
פורס את טבלת המטוסים, מוודא שאין מתג נעילה, שהתא מוצג פתוח (`…` ולא `–`), נפתח
בלחיצה, והערך נכתב בפועל ל-`strip_aircraft`.

**לא נכלל:** בדיקת e2e לטבלת נקודות המכוון עצמה (במוד הקיים כל שדותיה הפתוחים
הם דגלים, ואין בה תא טקסט לבדוק).

---

## 2026-08-12 - תקלה מדווחת מהמפה, נוסעת עם המטוס בפיצול, ומסומנת בחמישה מסכים

**הבקשה:** "בעדכון פ"מ על מפה (בשלוש נקודותיו) להוסיף אופציה של תקלה... בשמירה
יסומן ליד הפ"מ שיש מטוס אחד ממנו בתקלה. בפיצול מבנה - התקלה עוברת עם המטוס...
לא מופיע על 2 הפ"ממים רק איפה שהמטוס נמצא. צריך להדגיש תקלה גם בנקודת מעבר,
בטבלה, במוד אזרחי ובנקודת הצטרפות במגדל."

**האבחנה:** תשתית התקלה כבר הייתה קיימת מקצה לקצה (עמודות ב-`strip_aircraft`,
מסלול `/fault`, תפריט מהויות, `utils/faults.ts`, `AircraftFaultFields`), אבל
שלושה דברים חסרו: אין דרך לדווח תקלה **מהמפה**, התקלה **נכפלה** בפיצול מבנה,
והיא נחשפה **רק** מ-`strips.js` ולכן לא הגיעה לשלושה מסכים.

| | |
|---|---|
| **דיווח מהמפה** | מקטע תקלה בתפריט ה-⋮ של הפ"מ ([`SectorDashboard`](src/components/views/SectorDashboard.tsx), `fzPinMenu`): כפתור לכל מטוס במבנה → מהות מתפריט המהויות → פירוט חופשי בכתב יד ([`HandwritingOverlay`](src/components/shared/HandwritingOverlay.tsx)) או במקלדת וירטואלית. נפרס **בתוך** התפריט - דיווח תקלה הוא פעולה של שניות באמצע בקרה, וחלון מודאלי נוסף היה עולה יותר מהסדק הפיזי |
| **מספרי המטוסים** | נגזרים מ-`aircraft_indices` במבנה מפוצל ומ-`numberOfFormation` במבנה שלם, כדי שלא יוצע לדווח על מטוס שכבר עבר לפ"מ אחר. כפתורים ולא `select` - בעט ובאצבע בחירה בלחיצה אחת מהירה מפתיחת רשימה נגללת |
| **הכפילות בפיצול** | `partial-create` **משאיר** את שורות המטוסים גם על המקור (הן דרושות למיזוג חזרה), ולכן תקלה של מטוס שעבר הופיעה על שני הפ"ממים. הפתרון אינו מחיקה אלא סינון: `aircraft_indices @> to_jsonb(sa.idx)` ב-[`server/db/aircraftFaults.js`](server/db/aircraftFaults.js) (חדש), החל גם על טבלת המטוסים `strip.aircraft` |
| **התקלה נוסעת** | שלושת מסלולי הפיצול והמיזוג (`partial-create`, `ground-single-transfer`, `merge-partial`) העתיקו `datk, kipa` בלבד, ולכן הפ"מ הבן נולד "תקין". עכשיו הם נושאים גם את שלוש עמודות התקלה |
| **תג משותף** | [`FaultBadge`](src/components/shared/FaultBadge.tsx) (חדש) - "⚠ 2" עם HINT של המהות והפירוט. מוצג ליד האו"ק במפה, בתצוגה הטבלאית, בנקודת המעבר, במוד האזרחי ובנקודת ההצטרפות. רכיב אחד ולא חמישה סימונים - ו-⚠ הוא ערוץ **נוסף** על האדום (FAA HF-STD-001, עיוורון צבעים) |
| **החשיפה בשרת** | `aircraft_faults` נחשף גם מ-`transfers.js`, `civilian.js` ו-`joiningPoints.js`. `faultRedFor` עבר ל-`utils/faults.ts` (ומיוצא מחדש) כדי שלא יהיו שני גוונים של אדום תקלה באותו מסך |

> **למה במיזוג התקלה דורסת ב-`ON CONFLICT` והדת"ק לא:** בפיצול נשארת על הפ"מ
> המקורי שורה ישנה לכל מטוס שיצא. `DO NOTHING` גורף היה מותיר אותה, ובולע בשקט
> תקלה שנרשמה על המטוס בזמן שהיה אצל הפ"מ הבן.

**QA:** 1633 בדיקות יחידה ירוקות (14 חדשות: `formatFaultNumbers`, `faultRedFor`,
ובלם רגרסיה על הסינון ב-`aircraftFaults.test.js`) · `tsc` נקי · `vite build` נקי ·
`node --check` על ששת קבצי ה-`.js` שהשתנו · smoke מול שרת חי ו-DB אמיתי: פ"מ בן
4 מטוסים, תקלה למטוס 3, פיצול ל-3+4 - התקלה עברה לבן ונעלמה מהאב, טבלאות המטוסים
התחלקו 2/2, ומיזוג חזרה החזיר את התקלה **והפירוט** למבנה המאוחד.

**לא נכלל:** בדיקת e2e ויזואלית של התג בחמשת המסכים. שדות הזהות של המטוס (מספר
זנב, שמות צוות) עדיין **אינם** נוסעים בפיצול - פער קיים שלא הורחב כאן, כי הבקשה
הייתה על התקלה בלבד.

---

## 2026-08-12 - טבלת המטוסים נרשמת כטבלת בן: נבחרת ב"הוסף טבלה" ונפרסת בעמדה

**הבקשה:** "לא רואה את הטבלה במסך ניהול במוד טבלאי."

**האבחנה:** הטבלה הייתה קיימת ב-DB ובחוזה, אבל **לא הייתה רשומה** ב-
[`src/types/subTables.ts`](src/types/subTables.ts) - הרישום שממנו נגזרים תפריט
"הוסף טבלה" בהגדרת מוד הטבלה, בוחר העמודות המקונן והרינדור. עד כה הייתה שם
רשומה אחת בלבד (נקודות מכוון), ולכן טבלת המטוסים פשוט לא הופיעה בתפריט.

| | |
|---|---|
| **הרישום** | רשומה שנייה ב-`STRIP_SUB_TABLES`. 13 עמודות ב-[`src/types/stripAircraft.ts`](src/types/stripAircraft.ts) (חדש): מספר, מספר זנב, טייס, נווט, סגול 1/2, חימושים, מערכות, דת"ק, כיפה, ותקלה (דגל, מהות, פירוט). ברירת מחדל: ארבע עמודות זיהוי |
| **הנתון** | `GET /api/strips/global` מחזיר את הטבלה **מקוננת** על הפ"מ תחת `aircraft` (תת-שאילתה, כמו `aircraft_faults`; החימושים והמערכות מקוננים בתוך שורת המטוס). כך המנגנון הגנרי מוצא אותה באותו `stripField` שבו הוא מוצא את `targets` - בלי צינור נתונים משלה, ובלי לגעת בפ"מ הקלאסי |
| **קריאה בלבד** | `readOnly` ברישום: שורת מטוס היא רשומת DB עם מפתח משלה (`strip_id, idx`) ולא איבר במערך שנשמר בכתיבה אחת. עריכה בתא הייתה מנסה לשמור מערך שלם למקום שאינו קיים |
| **הכללה של הרינדור** | שלושת מקומות הקריאה קראו `toAimPoints(strip[stripField])` - כלומר כל טבלה חדשה הייתה נקראת כנקודות מכוון והשדות שלה נזרקים. עכשיו `toRows` הוא חלק מהגדרת הטבלה ו-`subTableRows(def, strip)` הוא השער היחיד. גם הודעת "אין שורות" והכפתור "פתח עורך נקודות מכוון" חדלו להיות גלובליים |
| **חימושים ומערכות** | משוטחים לטקסט בתא ("פצצה x2", "מכ\"ם (לא שמיש)") - תא בטבלה אינו פורס טבלה שלישית. כמות 1 ומערכת שמישה אינן מוצגות, כי הן הצפוי |

> **מלכודת שנתפסה רק ב-smoke:** גרשיים אחוריים בהערת SQL בתוך template literal
> סגרו את המחרוזת והפילו את השרת בטעינה. `tsc` ובדיקות היחידה עברו - הן אינן
> טוענות את `server/routes/strips.js`. מאז מורץ `node --check` על כל קובץ `.js`
> שהשתנה, לצד ה-smoke מול שרת חי.

**QA:** 1619 בדיקות יחידה ירוקות (9 חדשות לנרמול השורה ולרישום) · `tsc` נקי ·
שומר ה-i18n ירוק (13 מפתחות חדשים ב-registry, he+en) · smoke מול שרת חי: פ"מ עם
מס"מ 2, זהות וצוות, תקלה, חימוש ומערכת - `strip.aircraft` חזר מקונן מלא, ושורות
התצוגה יצאו בדיוק כפי שהטבלה תרנדר אותן.

**לא נכלל:** בדיקת e2e ויזואלית של הטבלה הפרוסה בעמדה.

---

## 2026-08-12 - ה-X לא סגר, ההזחה הייתה בצד הלא נכון, והנ"צ חזר לשדות

**ארבעה דיווחים מהמסך:**

**1. ה-X בחלון הצף לא סגר אותו.** הכפתור יושב **בתוך** ידית הגרירה, וזו קוראת
`setPointerCapture` על ה-pointerdown - כך שהמצביע נתפס לגרירה והלחיצה לא הגיעה
ל-`onClick` כלל. `stopPropagation` על ה-pointerdown של הכפתור מחזיר לו את הקליק.
זו אותה מלכודת של גרירה בעט/מגע, רק מהצד השני: ה-capture שמציל את הגרירה הוא
שבלע את הכפתור.

**2. ההזחה הייתה בצד ההפוך.** בסבב הקודם פירשתי "מוזחת שמאלה" כ-`marginInlineEnd`,
וזה דחף את הטבלה מהקצה השמאלי במקום להתחיל אותה בפנים. הכוונה הייתה שהטבלה
**תתחיל בקו של האו"ק**. הפעם ההזחה אינה בפיקסלים אלא ב-**`colSpan`**: תא ריק
שמכסה בדיוק את העמודות שלפני האו"ק (שתי ידיות + `callSignIdx`), ולכן היישור
מדויק בכל רוחב עמודות ובכל קונפיגורציה של מוד טבלה - ולא הערכה שתישבר.

**3. מסגרת אחת ולא שתיים.** קודם היו שתיים: קופסה סביב הטבלה, וקו מקווקו מתחת
לפ"מ. עכשיו מסגרת **אחת** מקיפה את שניהם: גג על שורת הפ"מ, דופן על התא הדביק
(ולכן גלויה גם בגלילה לצדדים), רצפה מתחת לטבלה האחרונה - ו**אין** קו בין הפ"מ
לטבלה שלו, כי קו כזה חצה את המסגרת לשתיים ובדיוק את זה הוא היה אמור למנוע.

**4. נ"צ בשדות נפרדים** ([CoordFields](src/components/strips/AimPointsTable.tsx)),
כמו בעיגון מפה: `[N▾][DDMM].[mmmm] / [E▾][DDDMM].[mmmm]`. שדה חופשי אחד מחייב
את הפקח לזכור כמה ספרות בכל מקטע ואיפה הנקודה - הקלדה שקל לטעות בה בעט ולא
לשים לב. לכל מקטע `maxLength` משלו, חצי הכדור מרשימה, ו**מעבר אוטומטי** לשדה
הבא כשהנוכחי מתמלא, כדי שההקלדה תישאר רציפה כמו בשדה אחד. `joinCoord` מחזיר
מחרוזת ריקה כל עוד לא הושלמו כל המקטעים - שורה שנפתחה ולא מולאה לא נשמרת עם
נ"צ שבור למחצה, ובדיקה נועלת שמה שנשמר תמיד עובר את הוולידציה.

**QA:** 67 בדיקות למודל (בהן פירוק/הרכבה של הנ"צ, נ"צ חלקי, מקטע לא שלם) ·
1617 בכל החבילה · tsc נקי · build עובר.

---

## 2026-08-12 - טבלת המטוסים תחת הפ"מ: זהות המטוס וצוות האוויר, בשני ה-DB

**הבקשה:** "הוסף טבלת מטוסים תחת פ"מ ב-DB של GAPI וגם של SKY-KING. כמות שורות
לפי המס"מ. השדות: מספר זנב, שם טייס, שם נווט, סגול 1, סגול 2, טבלת חימושים,
טבלת מערכות. שדות רק של SKY-KING: תקלה כן/לא, מהות תקלה מתפריט שמנוהל ע"י מנהל
מערכת, פירוט התקלה."

**מה כבר היה:** טבלת המטוסים **קיימת** משני צדדיה - `strip_aircraft` ב-SKY-KING
(עם `strip_aircraft_armaments` ו-`strip_aircraft_systems` תחתיה) ו-`aircraft[]`
מקונן ב-sortie של החוזה. גם שלושת שדות התקלה כבר קיימים ופנימיים ל-SKY-KING
(ראה הרשומה שמתחת), וכמות השורות כבר נגזרת מהמס"מ ב-`ensure`. **מה שחסר היה
חמשת שדות הזהות והצוות** - וזו העבודה כאן.

| | |
|---|---|
| **DB (SKY-KING)** | חמש עמודות ל-`strip_aircraft`: `tail_number`, `pilot_name`, `navigator_name`, `sagol_1`, `sagol_2`. כולן VARCHAR ולא מספריות - מספר זנב וסגול נכתבים כפי שהם מוכתבים בקשר, ומרגע שהם מספר האפסים המובילים נבלעים (`"077"` הופך ל-77) |
| **DB (GAPI)** | אותם חמישה שדות ב-`aircraft[]` של ה-sortie, דו-כיווניים (⇄). [GAPI-CONTRACT.md](GAPI-CONTRACT.md) §6.1.2 - סעיף חדש שמתאר את טבלת המטוסים כטבלת בן, במקביל ל-§6.1.1 של נקודות המכוון |
| **מקור אמת יחיד** | `AIRCRAFT_FIELDS` ב-[`server/gapi/entities.js`](server/gapi/entities.js). הכניסה (`sync.js`), היציאה (`outbox.js`) ומסלול העדכון בעמדה נגזרים ממנו - שדה מטוס חדש נוסף במקום אחד ולא בארבעה |
| **עדכון חלקי** | `PUT /api/strip-aircraft/:stripId/:idx` כותב **רק** עמודות שהופיעו ב-body. בלי זה כל לקוח ותיק ששולח `{datk, kipa}` בלבד היה מוחק בשקט מספרי זנב ושמות צוות שהוזנו במסך אחר. אותה סמנטיקה הוחלה על `bulk-import` |
| **התקלה נשארת של SKY-KING** | `AIRCRAFT_INTERNAL_COLUMNS` (`has_fault`, `fault_type`, `fault_details`, `greens`, `flight_status`) אינם ברשימת השדות התפעוליים: לא יוצאים ל-GAPI, ו-upsert נכנס - שהוא replace-set שמוחק מטוסים שאינם ברשימה - **לא נוגע בהם** |

**מה תוקן אגב כך: טבלת המטוסים לא יצאה ל-GAPI כלל.** `hasAircraft` שימש רק
בכיוון הנכנס, בעוד `buildOutboundEvent` בנה את ה-payload משורת ה-`strips` בלבד -
כלומר הסימון ⇄ בחוזה היה נכון רק בחצי. עכשיו `drain` טוען את המטוסים
(תת-שאילתות לחימושים/מערכות, לא JOIN - היה מכפיל את שורת המטוס), ומסלול העדכון
בעמדה קורא ל-`captureChange` כדי שהעריכה תגיע ל-GAPI.

> **מלכודת שנמנעה:** שורות המטוסים נוצרות ב-`ensure` ולא נולדות עם הפ"מ, ולכן
> `loadAircraft` מחזיר `null` (ולא `[]`) כשאין שורות. `[]` היה מכריז בפני GAPI
> "לפ"מ הזה אין מטוסים" ומוחק שם את הטבלה בכל עריכת או"ק של פ"מ שטרם נבנתה לו
> טבלת מטוסים ב-SKY-KING.

**QA:** 1610 בדיקות יחידה ירוקות (מתוכן 44 של GAPI, כולל בדיקות חדשות לכניסה
וליציאה של טבלת המטוסים) · `tsc` נקי · המיגרציה הורצה מול ה-DB בפועל וחמש
העמודות אומתו ב-`information_schema` · smoke מקצה-לקצה מול שרת חי: מס"מ 4 גרר
ארבע שורות, כתיבת זהות וצוות, `PUT {datk,kipa}` בלבד **לא** מחק אותם, מסלול
התקלה ומסלול הזהות לא דורסים זה את זה, ומחיקת הפ"מ ניקתה ב-CASCADE.

**לא נכלל:** ממשק משתמש. הבקשה הייתה ברמת ה-DB, וחמשת השדות זמינים היום דרך
ה-API ודרך הייבוא הסיטוני - אין להם עדיין תא בטבלת העמדה.

---

## 2026-08-11 - תקלה במטוס: דגל, מהות ופירוט - והשרשור לרמת הפ"מ

**הבקשה:** "שדות נוספים לפ"מ ב-DB של SKY-KING (לא דרך ה-GAPI): תקלה (דגל כן/לא -
יופיע באדום), מהות התקלה מתפריט שמנוהל במסך ניהול מערכת, ופירוט חופשי. זה **ברמת
מטוס** בפ"מ. ברמת פ"מ - שרשור כל התקלות של המטוסים לשדה שיכתוב 'תקלה למספר X',
וב-HINT המהות והפירוט."

**ההחלטה המרכזית: התקלה יושבת על המטוס, לא על הפ"מ.** שלוש עמודות נוספו ל-
`strip_aircraft` (`has_fault`, `fault_type`, `fault_details`) - לצד הדת"ק והכיפה,
שגם הם פר-מטוס. הפ"מ אינו נושא עמודת תקלה כלל: `GET /api/strips/global` מחשב
`aircraft_faults` בתת-שאילתה (ולא JOIN - היה מכפיל את שורת הפ"מ), והתצוגה מרכיבה
ממנו את שדה **"תקלות"**.

| | |
|---|---|
| **התפריט** | טבלת `fault_types` (קונפיג, `public` בלבד) - מנוהלת בטאב "חימושים/מערכות/תקלות" במסך ניהול מערכת |
| **למה שם ולא FK** | `strip_aircraft` משוכפלת לכל סכמת `env_NN` ו-`fault_types` לא; FK חוצה-סכמות היה נשבר בסביבות התרגול. מחיקת מהות אינה מוחקת תקלה קיימת, והבורר עדיין מציג ערך שנמחק |
| **מסלול כתיבה נפרד** | `PUT /api/strip-aircraft/:id/:idx/fault` ולא הרחבה של מסלול הדת"ק/כיפה - כך עדכון חנייה בעמדה אחת לא דורס תקלה שנרשמה בעמדה אחרת באותו רגע |
| **כיבוי הדגל מנקה** | גם בשרת וגם בלקוח: "אין תקלה" חייב להיות אין תקלה, אחרת מהות ופירוט ישנים היו צצים שוב בהדלקה הבאה |
| **הרשאות** | סימון תקלה = `USER` (דיווח תפעולי של הבקר/הפקח); עריכת התפריט = `STAFF` (מסך ניהול). שני כללים מפורשים ב-`middleware/auth.js` |

**התצוגה:** [`src/utils/faults.ts`](src/utils/faults.ts) מרכיב `"תקלה למספר 2, תקלה
למספר 4"` לשדה ו-`"מספר 2: מנוע - רעש חריג"` ל-HINT (המחרוזות מה-registry, ולכן
עובדות גם באנגלית). השדה `faults` נוסף לקטלוג שדות הסטריפ הקלאסי ולבונה
השאילתות (יחד עם `has_fault` בוליאני, לצביעה וסינון), ומוצג באדום בשלושת מסלולי
הרינדור של הכרטיס הקלאסי, בכרטיס הפ"מ ובפאנל הפ"מ של המגדל.

**רכיב אחד לעריכה** ([`AircraftFaultFields`](src/components/shared/AircraftFaultFields.tsx)):
אותם שלושה שדות בחלון פרטי הפ"מ (שורה לכל מטוס במבנה) ובשורת המטוס בעמדת המגדל -
לא שני מימושים.

**QA:** 1369/1369 unit (6 חדשות ב-`faults.test.ts`, 2 בכללי ההרשאות, 1 בסיווג
הטבלה) · tsc נקי · build · smoke מול ה-DB האמיתי: 15/15 (יצירת מהות, 401 בלי
אסימון, שמירה, שרשור ב-`aircraft_faults`, עדכון דת"ק שאינו מוחק תקלה, כיבוי
שמנקה, וניקוי אחרי הבדיקה).

---

## 2026-08-12 - טבלת בן: העמודות נבחרות ידנית, ומסגרת מקיפה בעמדה

**חמש בקשות מהצוות, בעקבות שימוש בפועל:**

| # | מה היה | מה תוקן |
|---|---|---|
| 1 | בחירת טבלה הוסיפה 4 עמודות מעצמה | הטבלה נפתחת **ריקה**; עמודות רק ב"הוסף שדה", כמו ברמת הפ"מ |
| 2 | "הוסף שדה" בחר את השדה הפנוי הבא | נוסף "— בחר שדה —"; **המקנפג בוחר**, והמערכת לא מנחשת |
| 3 | שדה פ"מ חדש נחת **אחרי** הטבלאות | נכנס בסוף שדות הפ"מ, לפני הטבלאות |
| 4 | הטבלה הוזחה ימינה, בלי מסגרת | מוזחת **שמאלה**, ומסגרת מקיפה את הפ"מ ואת טבלאותיו |
| 5 | פורמט הנ"צ לא הודגם בשדה | הדגמה בשדה, וסימון אדום גלי על נ"צ פגום גם בטבלה |

**למה 1+2 חשובים יותר משהם נראים:** מערכת שבוחרת "את הבא בתור" מייצרת עמודות
שאיש לא ביקש, והמקנפג מגלה אותן רק בעמדה - ואז מתקן אחת-אחת. עמודה בלי שדה
נבחר פשוט לא מוצגת, ומסומנת בעורך באזהרה כתומה כדי שלא תישכח.

**3 - למה זו תקלה ולא נוחות:** טבלת בן נפרסת כשורה שלמה מתחת לפ"מ, ולכן מקומה
תמיד אחרון. בלי `insertBeforeTables` כל שדה שנוסף אחרי שהוגדרה טבלה היה נוחת
מעברה, והמקנפג היה נאלץ לגרור אותו חזרה **בכל פעם**.

**4 - המסגרת:** גג בצבע הטבלה על שורת הפ"מ, רצפה מתחת לטבלה האחרונה שנפרסה,
ורקע משותף - כך שהפ"מ וטבלאותיו הם **גוש אחד** מוקף, והגוש הבא מופרד ממנו
בבירור. הטבלה עצמה קופסה מעוגלת מוזחת `marginInlineEnd` (שמאלה ב-RTL) עם פס
זיהוי עבה על אותו צד.

**5 - הפורמט לא השתנה,** הוא היה מאז ההתחלה `N/S`+4+`.`+4+`/`+`E/W`+5+`.`+4.
מה שחסר היה **הדרכה**: `COORD_PLACEHOLDER` מדגים אותו בכל שדה נ"צ (מספרים
אמיתיים ולא תבנית ריקה - כך רואים כמה ספרות בכל מקטע), ובדיקה נועלת שההדגמה
עוברת את הוולידציה עצמה, כדי ששתיהן לא ייפרדו.

**QA:** 1556 בדיקות ירוקות · tsc נקי · build עובר.

---

## 2026-08-12 - טבלת הבן נפרסת מ-+ ליד הפ"מ, ולא נדחסת לתא

**הבקשה:** "בתצוגה טבלאית בעמדה, אם מוגדרת טבלת בן - צריך שיהיה + ליד הפ"מ,
ובלחיצה עליו זה פותח את השורה של הטבלה. UX: צריך שיהיה ברור שזה חלק מהפ"מ
והפרדה בין השאר. ניתן לפתוח כמה טבלאות לכמה פ"מים בו-זמנית."

**מה היה לא בסדר:** הטבלה רונדרה **בתוך תא** של עמודה. עמודה בטבלת פ"מים היא
צרה, וטבלה בת 4-15 עמודות בתוכה יצאה דחוסה עד לבלתי-קריאה - במיוחד כשלפ"מ יש
כמה נ"צי תקיפה.

**התיקון - שורה נפרסת (expander row):**

| | |
|---|---|
| **ה-+** | בראש שורת הפ"מ, ליד ידית הגרירה - **צמוד לפ"מ**, ולא תלוי במקום שאליו נגררה עמודת הטבלה בהגדרת המוד. מופיע רק כשהמוד מגדיר טבלת בן |
| **התא של העמודה** | הפך למתג קומפקטי: `+ 3 שורות`, ו-⛔ כשיש נ"צ עם "עצור תקיפה". גם הוא פותח |
| **השורה הנפרסת** | ברוחב הטבלה, עם כל העמודות שנבחרו - ועריכה בתא כשהיא מאופשרת |
| **בו-זמנית** | `Set` ולא ערך יחיד: הבקר משווה בין פ"מים, ופתיחת אחת לא סוגרת אחרות |

**איך "ברור שזה חלק מהפ"מ" נפתר ויזואלית:** הגבול התחתון העבה של שורת הפ"מ
**עובר לשורה הנפרסת**, ובמקומו נשאר קו מקווקו בצבע הטבלה - כך הפ"מ והטבלאות שלו
נקראים כגוש אחד, והגוש הבא מופרד ממנו בבירור. בנוסף: פס אנכי בצבע הטבלה בצד
הפ"מ, הזחה של 26px, רקע נבדל, וכותרת שנושאת את **שם הפ"מ** ("של קרן 21") - כי
שורה ברוחב מלא בין פ"מים עלולה להיקרא כשייכת לשכן.

**ניגודיות:** צבע הזיהוי היה תורכיז בהיר אחד לכל התמות, שעל הרקע הבהיר יורד ל-
~1.7:1 - מתחת לסף 3:1 של WCAG 1.4.11. הוחלף ב-`subTableAccent(themeMode)`:
`#0e7490` באור, `#22d3ee` בכהה ובכחול. נבדק בבדיקה.

**QA:** 1555 בדיקות ירוקות · tsc נקי · build עובר.

---

## 2026-08-12 - טבלת נקודות מכוון גם בפ"מ הקלאסי, ולא רק שורת תקציר

**המשך ישיר** לשינוי הקודם: במוד הטבלה נקודות המכוון כבר נפרסו לטבלה, אבל בפ"מ
הקלאסי נשארה רק שורת תקציר - כי תא בכרטיס קלאסי היה מסוגל להחזיק **ערך אחד**.

**התיקון:** ל-`SGCell` נוסף `tableColumns`. כשה-`fieldKey` של תא הוא מפתח של
טבלת בן (`subTables.ts`), התא **נפרס לטבלה** - שורה לכל נ"צ - ובפאנל מאפייני התא
נפתח **בוחר העמודות שלה**, אותו רכיב `SubTableColumnsEditor` שמשרת את מוד הטבלה.
שני מפתחות בקטלוג הקלאסי, לשני מצבי שימוש:

| מפתח | מה מציג | מתי |
|---|---|---|
| `aim_points` | **טבלה** - שורה לכל נ"צ, רק העמודות שנבחרו לתא | תא רחב מספיק |
| `aim_points_summary` | **שורת תקציר** אחת | תא צר |

**קריאה בלבד בכוונה:** הכרטיס הקלאסי נמוך (48px כברירת מחדל) והעריכה בו היא
פר-שורה ולא פר-תא, ולכן טבלה נערכת שם הייתה לוחמת במנגנון הקיים. העריכה נשארת
במוד הטבלה ובחלון העורך המשותף. כותרות העמודות מוצגות רק כשיש יותר משורה אחת -
בשורה בודדת הן בזבוז גובה בכרטיס נמוך. שורה עם **עצור תקיפה** נצבעת אדום גם כאן.

**QA:** 58 בדיקות למודל ולרישום · 1552 בדיקות בכל החבילה · tsc נקי · build עובר.

---

## 2026-08-11 - נקודות מכוון היא **טבלת בן** של הפ"מ, ולא 12 שדות בבורר

**הדיווח (עם צילום של בורר השדות):** "את כל זה אני צריך בהגדרת טבלה - שיהיה כמו
הוסף שדה, הוסף טבלה, ויפתח תפריט מרשימת הטבלאות. ובאותה טבלה יהיה לי בוחר שדות
כמו שיש ברמת הפ"מ."

**מה היה שבור בגישה הקודמת.** 15 שדות נקודת מכוון נדחפו לבורר השדות של הפ"מ,
ומשם נבעו שתי תקלות: הבורר הוצף ב-12 שורות שאינן שדות פ"מ, ו**תא יחיד ניסה
לייצג מערך שלם** - ולכן 11 השדות הפרטניים נאלצו להיות קריאה-בלבד (עריכה בתא
הייתה מתרגמת ל"איזו מ-N השורות?").

**התיקון - טבלת בן היא סוג עמודה, לא אוסף שדות** ([subTables.ts](src/types/subTables.ts)):

| | לפני | אחרי |
|---|---|---|
| הוספה | 12 שורות בבורר שדות הפ"מ | כפתור **"הוסף טבלה"** → תפריט טבלאות הבן |
| בחירת עמודות | לא הייתה | **בוחר שדות משלה**, באותה שפה של בורר הפ"מ (שדה, כותרת, מצב עריכה, גרירה) |
| התא בעמדה | ערך אחד מכווץ | **טבלה קטנה**: שורה לכל נ"צ, רק העמודות שנבחרו |
| עריכה | קריאה בלבד | **עריכה בתא** - כל תא הוא בדיוק שורה אחת × שדה אחד |

השדות ירדו מ-`STRIP_FIELD_DEFS`; בפ"מ הקלאסי נשאר רק התקציר המצרפי (תא שם מציג
ערך אחד), ובבונה השאילתות נשארו כולם - סינון לפי דגל או נ"צ הוא בדיוק מה שרוצים.

**ארבעה דגלי אישור לתקיפה** נוספו לטבלה: מאומת אווירי, רשאי לצאת כיוון, עצור
תקיפה, מאומת קרקעי. הם **בוליאניים ולא מחרוזות** - `"false"` הוא מחרוזת truthy,
ותקלה כזו הייתה מאשרת תקיפה במקום לעצור אותה. `toAimFlag` קורא גם `1`/`"כן"`/
`true`, וכל ערך שלא הוכרע נקרא `false`: **ערך שלא הובן אינו אישור**. `עצור תקיפה`
צובע את השורה באדום ופותח את שורת התקציר, כי הוא הדגל היחיד שאומר *לא לתקוף*.

**GAPI:** מבנה טבלת הבן תועד מפורשות ב-GAPI-CONTRACT.md §6.1.1 - מקוננת בתוך
ה-sortie כמו `aircraft[]`, בלי `entity` משלה, replace-set בכל upsert. ב-DB של
SKY-KING היא נשארת JSONB תחת הפ"מ (החלטת הצוות): שמירה אחת של כל הטבלה, בלי
מיגרציה ובלי שורות יתומות.

**QA:** 57 בדיקות למודל ולרישום (דגלים, ברירת מחדל בטוחה, ירידה מבורר הפ"מ,
עריכה במתג מול מקלדת) + 40 GAPI (15 השדות הלוך-חזור עם דגלים בוליאניים).
כל החבילה: 1551 בדיקות ירוקות · tsc נקי · build עובר.

---

## 2026-08-11 - תיקון **נתונים**: עוגן מפת אזורי הקרב היה מוסט ב-5 ק"מ

**הבקשה:** "תסתכל על קו החוף של המפה של 305 וקו החוף של WINDY ולפי זה ליישר."

**איך נמדד - בלי לנחש:** מפת 305 (#24) היא **חיתוך** של מפת אזורי הקרב (#17),
והגזירה שלה מדויקת עד הספרה השביעית - כלומר ההפרש מגיע מהעוגן של מפת האם.
כדי להפריד ים מיבשה אצל Windy נטענה שכבת **`sst`** (טמפרטורת ים), שנצבעת
**רק מעל מים** ולכן נותנת מסכה חדה לגמרי; מולה הים התכלת של הסדק.

| ציר | מה נמדד | תרגום |
|---|---|---|
| **אורך** - חוף ישראל, נתניה↔עזה, 9 שורות | Windy מזרחה ב-9px, **קבוע** לאורך כל החוף | 5.2 ק"מ ≈ 2.8 מייל ימי |
| **רוחב** - חוף סיני (מזרח-מערב), 11 עמודות | Windy צפונה ב-4px | 2.4 ק"מ ≈ 1.3 מייל ימי |

ההפרש **קבוע ולא משתנה לאורך החוף** - ולכן זו הזזה של נקודות העוגן ולא עיוות
היטל. מי שסימן את שתי צמתי הגרטיקולה סטה ב-1.3% מרוחב הגיליון.

**התיקון:** `Δx = -1.312%`, `Δy = +0.444%` על העוגן של #17, ו**גזירה מחדש** של
ארבע מפות הבת (24, 25, 74, 75) דרך שרשרת החיתוכים - אחרת הן היו נשארות על
המיפוי הישן. עודכן בטרנזקציה אחת, עם גיבוי ו-SQL לשחזור.

**אימות:** ההפרש בשני הצירים יורד לאפס על מפת האם, ועל מפת 305 עצמה הוא ירד
מ-~5 ק"מ ל-**~1.2 ק"מ** (0.65 מייל ימי) - רצפת המדידה, שנובעת מרוחב רצועת
המים הטריטוריאליים המצוירת על הסדק.

⚠️ **תופעת לוואי מכוונת:** 19 אזורים (מתוך 124) שמורים כ-`polygon_geo` ולא
כפיקסלים, ולכן הם **זזו על המסך** 5.2 ק"מ מערבה ו-2.4 ק"מ דרומה - 17 במפה 17
ו-2 במפה 24. אם ה-geo שלהם נגזר בזמנו מהציור, הם צריכים בדיקה חוזרת.

**מה זה שיפר מעבר למז"א:** אותו עוגן משרת את **התמונ"א**, את נ"צ תחת הסמן ואת
האזורים הגיאוגרפיים - כולם מדויקים יותר ב-5 ק"מ.

**שחזור** (הערכים שהיו לפני התיקון; `scripts/` חסום ב-gitignore ולכן ה-SQL חי כאן):

```sql
UPDATE maps SET anchor1_x_img=3.0965686,   anchor1_y_img=1.4285816,   anchor2_x_img=94.05679,   anchor2_y_img=95.741745  WHERE id=17;
UPDATE maps SET anchor1_x_img=-32.526276,  anchor1_y_img=-90.19221,   anchor2_x_img=101.792366, anchor2_y_img=209.10773  WHERE id=24;
UPDATE maps SET anchor1_x_img=-121.345024, anchor1_y_img=-3.6344688,  anchor2_x_img=100.90386,  anchor2_y_img=214.44281  WHERE id=25;
UPDATE maps SET anchor1_x_img=-53.965603,  anchor1_y_img=-112.7952,   anchor2_x_img=157.7881,   anchor2_y_img=259.8557   WHERE id=74;
UPDATE maps SET anchor1_x_img=-264.56412,  anchor1_y_img=-217.37732,  anchor2_x_img=127.85036,  anchor2_y_img=295.81104  WHERE id=75;
```

---

## 2026-08-11 - חומרת הודעה בלוח ההודעות: רגיל / חמור / קריטי

**הבקשה:** "לחלון הודעות תוסיף חומרת הודעה: רגיל - צובע בירוק, חמור - באדום,
קריטי - אדום מהבהב. יכול להגדיר לכל הודעה את החומרה. אם אלו הודעות קבועות אז
גם בהגדרת עמדה והודעות קבועות."

**מה נעשה:** לכל הודעה בלוח ההודעות (`SignalBoard`) נוספה **חומרה**. עד היום
כל הודעה פעילה נצבעה ירוק אחיד, ולכן על מסך עם עשרות סימנים דלוקים לא היה
הבדל ויזואלי בין "מסלול 09 בשימוש" ל"תקלה חמורה במכ"ם" - שניהם ריבוע ירוק
זהה, והפקח נדרש לקרוא כל אחד.

| רובד | מה נוסף |
|---|---|
| **DB** | `workstation_signals.severity VARCHAR(8) NOT NULL DEFAULT 'normal'` (`ADD COLUMN IF NOT EXISTS` - שלוש ההודעות הקיימות ב-DB נפלו ל'רגיל') |
| **שרת** | `POST/PUT /api/signals` מקבלים `severity`, `GET /api/signals/incoming` מחזיר אותו לנמען. ערך לא מוכר עובר `normSeverity` ונשמר כ-'normal' |
| **מקור אמת** | [`src/utils/signalSeverity.ts`](src/utils/signalSeverity.ts) - הצבעים במקום אחד, לשימוש הלוח וההגדרות כאחד |
| **לוח ההודעות** | הכפתור נצבע לפי החומרה כשהוא דלוק; כבוי נשאר אפור. נקודת חיווי בפינה מראה באיזו חומרה יידלק ופותחת בורר של שלוש אפשרויות. גם ההודעות **הנכנסות** מעמדות אחרות נצבעות לפי החומרה שנקבעה בעמדת המקור |
| **הגדרת עמדה** | במאגר ההודעות הקבועות נוספה חומרה לכל הודעה. הודעה קבועה שנפרסת לכפתור נולדת עם החומרה שהוגדרה לה |

**ההבהוב של "קריטי" אינו מוריד ל-`opacity: 0`.** `@keyframes signal-critical-blink`
ב-[App.css](src/App.css) מחליף בין אדום בהיר לאדום כהה, כך שהכיתוב קריא לאורך
כל המחזור - הבהוב בעמדת פיקוח נועד למשוך תשומת לב ואסור שימנע קריאה. `steps(1)`
ולא דהייה רכה: זו התראה, לא אנימציה. יש בדיקה שנכשלת אם מישהו יחזיר `opacity: 0`.

**תאימות לאחור:** הודעה בלי `severity` (רשומה ישנה, לקוח ישן, פריט מאגר שנשמר
כמחרוזת) נופלת ל'רגיל' בכל שלושת הרבדים - DB, שרת ולקוח - ולכן המסך ממשיך
להתנהג בדיוק כמו קודם עד שנקבעת חומרה.

**QA:** `tsc` נקי · 1511 בדיקות עוברות (9 חדשות ל-`signalSeverity`) · `vite build`
נקי · מיגרציה הוחלה על ה-DB המשותף ואומתה · round-trip מול ה-DB בשאילתות של
ה-routes עצמן: `critical` נשמר, `"URGENT"` נפל ל-`normal`, INSERT ישן בלי
העמודה נפל ל-`normal`, ו-`incoming` מחזיר את החומרה לנמען.

---

## 2026-08-11 - תיקון: Windy חוסם זום מעל 11, ומפת שדה קיבלה מז"א פי 4 רחב מדי

**הדיווח:** "בעמדת 305 זה היה כמעט בדר - כמה מיילים בצד. במפה שדה תעופה
מעוגנת ללא מפה - כמו הקפה תל נוף - זה לא היה מעוגן טוב, צריך יותר זום אין."

**האבחון:** שדה "בח"א 8 - הקפה" (#81) מעוגן כך שהקנבס הסכמטי מכסה **8.35 ק"מ**,
כלומר 78.8 px/km בשרטוט. הרכבה של אותו מקרה בדפדפן, עם סרגל 5 ק"מ לפי קנה
המידה של השרטוט, הראתה ש-Windy מרנדר ב-15.9 px/km - **פי 4 רחב מדי**.
החישוב ביקש `zoom=13`; קנה המידה בפועל התאים ל-`zoom=11`.

**השורש - תקרת זום ב-Windy עצמו.** נטענו הפריימים בזום 9,10,11,12,13,14,15
ונבדקו: **11 עד 15 מרנדרים תמונה זהה** - אותן ערים באותם פיקסלים בדיוק. נבדק
גם בנקודת הקצה השנייה (`embed.html`) ובשכבת מכ"ם - אותה תקרה. Windy פשוט
מתעלם מכל זום מעל 11.

זה גם מסביר למה עמדת 305 נראתה "כמעט": מפה מרחבית דורשת זום 9-10, מתחת
לתקרה, ולכן שם ההתלכדות עבדה.

**התיקון:** `MAX_ZOOM = 11` ב-[fit.ts](src/weather/fit.ts). מכיוון ש-`scaleX/scaleY`
נגזרים מהזום שנבחר, קיבוע התקרה מגדיל את הסקייל אוטומטית וההתלכדות נשמרת בכל
קנה מידה. **המחיר הוא חדות ולא דיוק** - בקנה מידה של שדה תעופה אריחי Windy
נמתחים פי ~5, והכיתוב שלו גדל בהתאם. מעבר לתקרה אין מידע נוסף להציג ממילא:
מודל המז"א עצמו הוא ברזולוציה של קילומטרים.

**תיקון נלווה - שוליים קבועים סביב האזור הנראה.** ה-overscan היחסי (×1.5) לבדו
לא הספיק בסקייל גדול: האזור הנראה מתכווץ ל-129px, ו-50% ממנו דקים מסרגל הזמן
של Windy (עובי קבוע). נוסף `CHROME_MARGIN_PX = 70` - המסגרת היא הגדולה מבין
`visible × 1.5` ל-`visible + 140`.

**מה שנשאר ואינו ניתן לתיקון בקוד:** ההפרש של כמה מיילים בעמדת 305 מקורו
ב**עוגן של המפה עצמה** - נמדד שם אי-עקביות של 1.4% בין יחס התמונה לפריסה
הגיאוגרפית, שהם ~3.8 ק"מ על גיליון של 269 ק"מ. שיפור דורש מיקום מחדש של שתי
נקודות העוגן, לא שינוי קוד.

---

## 2026-08-11 - תיקון: שכבת המז"א לא התלכדה עם מפת השדה

**הדיווח:** "מפת מז"א לא מעוגנת למפה שנמצאת בפועל בעמדה."

**האבחון - הרכבה של המפה האמיתית מה-DB מול Windy בדפדפן:** מתחת היה סדק
"אזורי קרב" של **ישראל**, ומעליו Windy הציג את **דלתת הנילוס** - קהיר, פורט
סעיד, אל עריש. מדידת ההיסט נתנה 270px אופקית ו-200px אנכית, וזה בדיוק
`(frameW × scaleX − bounds.width) / 2` ו-`(frameH × scaleY − bounds.height) / 2`.

**השורש - `inset: 0` + `margin: auto` על אלמנט שגדול ממכולתו.** הכלל הזה ממרכז
רק אלמנט **קטן** מהמכולה; כשהוא גדול, המרג'ינים היו יוצאים שליליים, והספסיפיקציה
(CSS 2.1 §10.3.7) מורה לאפס אחד מהם ולהצמיד לקצה - השמאלי ב-LTR והימני ב-RTL.
מסגרת ה-Windy גדולה בכוונה פי `OVERSCAN` (1.5), כדי שסרגל הזמן והלוגו ייחתכו -
ולכן **המירכוז נכשל בכל מפה, בשקט**. שום דבר לא נראה שבור: המפה עלתה, השכבה
עלתה, פשוט בנקודה הלא נכונה על הגלובוס.

**התיקון:** מרכוז בחשבון מפורש בפיקסלים - `left: (bounds.width − frameW) / 2`
ו-`top: (bounds.height − frameH) / 2` (ערכים שליליים, זו הטבעת שנחתכת). אריתמטיקה
מפורשת במקום כלל CSS עם מקרה קצה.

**אימות:** אותה הרכבה חזרה - תל-אביב, חיפה, ירושלים, אשדוד, באר שבע ואילת
יושבים על מקומם בסדק, וקו החוף של Windy רץ על קו החוף של המפה.
**ואז על כל המפות:** יחס התמונה מול הפריסה הגיאוגרפית של העוגן נמדד בכל שש
המפות המעוגנות ויצא 0.965-0.988 - כלומר המתיחה הלא-אחידה היא עד 3.5% (בלתי
מורגשת) וחסם העיוות לא נכנס לפעולה באף אחת.

**בדיקת רגרסיה** ([WeatherLayer.test.tsx](src/weather/WeatherLayer.test.tsx)):
נאסר `margin:auto`, ונדרש שמרכז המסגרת יתלכד עם מרכז אזור התצוגה. תוך כדי
התברר שבדיקה קיימת הייתה **חלשה** - ה-regex שלה תפס את מידות המכולה במקום את
מידות ה-iframe, ולכן הייתה עוברת גם עם הבאג. חודדה.

---

## 2026-08-11 - תצוגת מז"א על המפה (Windy)

**הבקשה:** "רוצה להציג מידע על מפה מאתר WINDY - יהיה כפתור תצוגה 'הצג מז"א'
בסרגל הכלים, ואז ייפתחו אופציות WINDY כמו בתמונה" (התפריט המהיר של Windy).
**ההכרעה של הצוות:** על מפה או שדה תעופה - שכבה **מעוגנת לפי העיגון**;
במוד טבלה או אחר - חלון צף נגרר עם יכולת הגדלה. השכבות: העשר מהתמונה + תעופתיות.

**שלוש הכרעות שנבדקו מראש ולא הונחו:**

| הכרעה | מה נבדק | התוצאה |
|---|---|---|
| **iframe ולא אריחי מפה** | ה-CSP של העמדה ([securityHeaders.js](server/middleware/securityHeaders.js)) | `frame-src 'self' https: http:` מתיר מסגרת כמו שהיא, בעוד `img-src 'self' data: blob:` **חוסם** אריחים חיצוניים. שכבת אריחים הייתה דורשת לפתוח `img-src` **ו-`connect-src`** לשרתי Windy - הרחבת משטח תקיפה עבור תצוגה. ההטמעה עובדת בלי מפתח API ובלי תלות npm חדשה |
| **מזהי השכבות** | הורדת ה-bundle של `embed.windy.com` ו-grep | כל 15 המזהים קיימים (`radar`, `satellite`, `hurricanes`, `rainAccu`, `thunder`, `cbase`, `deg0`, `cape`...). לא נוחשו |
| **ארגז החול לא שובר את Windy** | Playwright מול windy.com, עם `sandbox` ובלעדיו | צילומי המסך **זהים בבתים**, אפס שגיאות קונסולה. ה-sandbox נשאר, והוא מה שמונע ממסגרת חיצונית לנווט את העמדה כולה |

**המלכודת שנתפסה במדידה ולא בקוד:** ברירת המחדל הראשונה למיזוג הייתה `screen`,
מתוך הנחה שהוא יעלים את רקע המפה של Windy וישאיר רק את צבעי המז"א. הרכבה של
מסגרת Windy מעל "סדק" סינתטי בארבעה מצבי מיזוג הראתה שההנחה שגויה: הרקע של
Windy הוא **אפור בהיר (~`#8a8a8a`)** ולא כמעט-שחור, ולכן `screen` הלבין את המפה
עד שכיתוב המסלול ותוויות האזורים נבלעו - ו-`multiply` חנק את המז"א עצמו.
ברירת המחדל תוקנה ל-`normal` בבהירות 0.45 (בתמה בהירה: `multiply`).

**ההתאמה הגיאוגרפית** ([fit.ts](src/weather/fit.ts)): מפת השדה היא תמונה עם
מיפוי לינארי לפי שני עוגנים, ומפת Windy היא מרקטור עם רמות זום שלמות. הפתרון -
זום שלם קרוב לקנה המידה + תיקון השארית ב-CSS `scale`, ותיקון `1/cos(φ)` לציר
הרוחב. אומת בדפדפן: המרחק תל-אביב↔ירושלים יצא ~410px מול 413px חזויים.

**למה השכבה יושבת בתוך ה-transform של המפה** (כמו `AirPictureLayer`): פאן וזום
הם `translate/scale` על אותה שכבה, ולכן המז"א זז עם המפה **בלי טעינה מחדש** של
ה-iframe. שכבה מחוץ ל-transform הייתה מחשבת מרכז חדש בכל תזוזה וטוענת את Windy
עשרות פעמים בגרירה אחת. התפריט עצמו דווקא **חייב** לשבת בחוץ - `fixed` בתוך
שכבה עם transform מתנהג כמו `absolute`, והתפריט היה נגרר ומתקרב עם זום המפה.

**נגיעות בקוד קיים:** `SectorDashboard` (פריט בתפריט תצוגה, שכבה בשכבת המפה,
תפריט/חלון), `GroundView` (שכבה + מתג בפאנל השכבות), `helpTopics` +
`registry/help.json` (נושא עזרה), `registry.ts` + `registry/weather.json`.

**QA:** `tsc` נקי · 1,398 בדיקות עוברות (38 חדשות: גאומטריית ההתאמה, בניית
הכתובת, ההעדפות, ורינדור השכבה והתפריט) · `npm run build` עובר · אימות חי
בדפדפן מול windy.com.
**מה לא נבדק:** התצוגה בתוך העמדה עצמה על מפת שדה אמיתית - נדרש מבט אנושי.
## 2026-08-11 - הקפה תלת מימדית בעמדת המגדל

**הבקשה:** להציג את ההקפה, בלוקי הגבהים והמטוסים **במרחב** - כדי שהשאלה "מי מעל
מי, ומי נכנס לאן" תיענה במבט אחד ולא בראש של הפקח.

**הכרעה ראשונה - SVG עם היטל משלנו, לא three.js.** הסצנה היא קו הקפה, ערימת
לוחות וכמה מטוסים - **עשרות פוליגונים**. אלגוריתם הצייר (מיון לפי עומק, ציור
מהרחוק לקרוב) מספיק לחלוטין, וסדר האלמנטים ב-SVG מיישם אותו בחינם. התמורה: אפס
תלות חדשה לשרשרת האספקה, `<text>` מקורי (חד, `tr()`, מותאם תמה), ובדיקות vitest
על פונקציות טהורות במקום WebGL headless.

**הכרעה שנייה - היטל אורתוגרפי ולא פרספקטיבי.** בפרספקטיבה אותו מרווח גבהים
נראה גדול מקדימה וקטן מאחור, וזו בדיוק הטעות שמחקר הנדסת אנוש של ATC מזהיר
מפניה. אורתוגרפי שומר קנה מידה קבוע: שני בלוקים במרחק 1000 רגל נראים תמיד באותו
מרחק. מאותו מקור מגיעים גם ארבעת רמזי העומק, שאינם קישוט אלא **תנאי לתקינות
התצוגה**: צל ההקפה על הקרקע, קווי הורדה אנכיים, רשת קרקע וציר גבהים עם תוויות
ברגל. וכלל-על: כל מספר שקריטי לפעולה (גובה, או"ק) מופיע כטקסט - לעולם לא רק
כמיקום במרחב.

**המסגור מחושב פעם אחת** מכדור חוסם ולא מחדש בכל סיבוב. ההיטל אורתונורמלי (שלוש
שורות אורתונורמליות - נבדק), ולכן רדיוס שנמדד **במרחב** חוסם את ההיטל בכל זווית.
בלי זה הסצנה "נושמת" בכל תזוזה של היד וזה בלתי נסבל למעקב.

**סטייה מהאפיון - סימן ה-`near`.** האפיון נתן `near = z·sin(tilt) − ry·cos(tilt)`.
זה סותר את נוסחת ה-`y` שבאותו סעיף: `y = ry·sin − z·cos` מתארת מצלמה שיושבת
**בדרום** (וזו היחידה שמקיימת גם את הזהות "tilt=90 = מבט-על מדויק"), ואצלה מה
שנמצא נמוך במסך הוא הקרוב לצופה - כמו במפה שמוטה קדימה. הסימן המקורי היה ממיין
הפוך ומצייר את הרחוק **מעל** הקרוב, וזו טעות שנראית סבירה לחלוטין על המסך.
מומש `near = z·sin + ry·cos`, עם בדיקה שמנסחת את הטיעון הפיזי במפורש.

**DRY - מטוס חייב לשבת באותו מקום בשתי התצוגות.** לוגיקת המיקום על הצלע חולצה
מ-`PatternAircraftLayer` ל-`placePatternAircraft()` המיוצאת, ושתי התצוגות צורכות
אותה יחד עם `legPoint`; הבלוקים מגיעים מ-`buildBlocks`/`formationsInBlocks`/
`conflictBlocks` של הטבלה. שכפול היה מייצר תצוגה שמראה מטוס בצלע אחת ובטבלה
בשנייה - הרסני יותר מאשר לא להציג אותו כלל. גם ב-`GroundView` הופק מקור אחד
(`patternAircraftRows`) לשכבה השטוחה ולסצנה.

**DB - שתי תוספות עמודה בלבד, אין טבלה חדשה:**

| עמודה | למה |
|---|---|
| `airfield_patterns.downwind_alt_ft` / `base_alt_ft` | גבהי ההקפה על **ההקפה** ולא על השדה: לשדה מסלול לכל כיוון והקפת ימין/שמאל, ולכל אחת גובה משלה. NULL → 3000/1500 **בקוד**, כדי שברירת המחדל תשתנה בלי מיגרציה ו"לא הוגדר" יישאר מובחן |
| `airfields.elev_ft` | בלוקי ההצטרפות הם גובה **מוחלט** וגבהי ההקפה הם **מעל פני השדה**. בלי גובה השדה אי אפשר לשים את שניהם על אותו ציר |

שני ה-`PUT` מעדכנים את העמודות **רק כשהן נשלחו** (`CASE WHEN`), אחרת שמירה ממסך
שאינו מכיר אותן הייתה מאפסת את ההגדרה בשקט.

**גבולות מכוונים:** התצלום האווירי **אינו** מוטה (עיוות ראסטר הופך תוויות לבלתי
קריאות); אין עריכה מהתלת מימד - הזזת מטוס בין בלוקים נשארת בטבלה, מקום אחד
לפעולה; אין פרספקטיבה, תאורה או אנימציית מעבר.

**קבצים:** [pattern3d.ts](src/utils/pattern3d.ts) (ליבה טהורה) ·
[Pattern3DScene.tsx](src/components/ground/Pattern3DScene.tsx) ·
[Pattern3DControls.tsx](src/components/ground/Pattern3DControls.tsx) (חלון צף,
`windowFrame('view')`, גרירה ב-Pointer Events) · `GroundView` (כפתור בשורת פקדי
הזום - זו בקרת **מבט**, אחות של הזום, ולא שכבת תוכן) · `PatternsSection` +
`ManagementPage` (שדות הקלט) · מרחב שמות `pattern3d`.

**שלושה תיקונים שהגיעו מ*הסתכלות* על רינדור, לא מהנחה** (הסצנה רונדרה ל-PNG
בארבע זוויות מצלמה):

| מה נראה | התיקון |
|---|---|
| ב-55° הקרקע ממלאת את המסגרת כרקע, וההפרש בין עם-הרוח לבסיס נדחס (`cos 55° = 0.57`) | `DEFAULT_CAMERA.tilt` 55 -> **30** (`cos = 0.87`): הקרקע נקראת כמישור וההקפה מרחפת מעליה |
| "בסיס 33" ו"פיינל 33" נערמו מתחת לתיבות המטוסים בתחתית ההקפה - **בדיוק במקום שבו קוראים סדר נחיתה** | ההזחה נמדדת ב-`k` (יחידות הטקסט) ולא ביחידות עולם, ולכן היא גדלה יחד עם הכתב; `LABEL_CLEAR` גדול מחצי תיבת המטוס, ו-`opacity: 0.7` - **זהות המטוס גוברת על שם הצלע** |
| חץ הצפון היה קו ונקודה ולא נקרא כחץ | ראש חץ + האות `צ`/`N` בקצה (מפתח `northLetter`). ההתקצרות עם ההטיה **נשמרה** - זה רמז העומק. במבט-על עם `yaw=0` ההיטל מתנוון לאורך אפס, ואז מוצגת האות בלבד |

**חפיפת בלוקים במבט-על - נבדק ונמצא דטרמיניסטי.** ב-`tilt=90` כל בלוקי הנקודה
נופלים על אותה נקודת מסך (נכון גאומטרית - הטבלה היא המקום לקרוא אותם). הסדר
**אינו** תלוי בסדר המערך: העומק במבט-על הוא `near = z`, ולכן הבלוק הגבוה תמיד
אחרון בציור - למרות ש-`buildBlocks` מחזיר מהגבוה למטה, כלומר בסדר ההפוך. נעול
בבדיקה.

**QA:** 1398/1398 unit (54 חדשות: 40 ב-`pattern3d.test.ts` - זהויות ההיטל, מבחן
ה"נשימה", עקביות `altOnLeg` מול הקו המצויר; 14 ב-`Pattern3DScene.test.tsx` -
מאויש מול פנוי, קונפליקט, קו חיבור שלא מנוחש, אי-חפיפת תוויות, סדר הציור
במבט-על) · tsc · build.

---

## 2026-08-11 - טבלת נקודות מכוון (העברת מטרה לתקיפה) בפ"מ וב-GAPI

**הבקשה:** "רוצה להוסיף ל-GAPI ולפ"מ שדות של העברת מטרה לתקיפה. זה נקרא טבלת
נקודות מכוון. כל שורה זה נ"צ לתקיפה" - ואחריה 11 שדות: שם מטרה, שם נקודת מכוון,
נ"צ (17 ספרות), גובה ברגליים, HD, AN, AN מזערי, מרעום, הערה, חימוש, כמות פצצות.

**מה כבר היה שם.** הפ"מ נשא `strips.targets` (JSONB) עם **שני** השדות הראשונים
בדיוק - `{name, aim_point}` - ומוצג בפאנל הפרטים, בתצוגה הקלאסית, במוד הטבלה
ובייבוא מקובץ. לכן זו לא טבלה חדשה אלא **הרחבה** של הקיימת: שורה ישנה נקראת
כמות שהיא, אין מיגרציה, והנתונים הקיימים לא זזים.

**מקור אמת יחיד** ([aimPoints.ts](src/types/aimPoints.ts)): 11 השדות מוגדרים פעם
אחת, ומהם **נגזרים** קטלוג העמודות של מוד הטבלה, קטלוג הפ"מ הקלאסי, בונה
השאילתות, עורך הטבלה, הייבוא מקובץ ומיפוי GAPI. שדה שיתווסף בעתיד יופיע בכל
הבוררים מאליו - ובדיקה נועלת את זה (אין מפתח כפול בשלושת הקטלוגים, וכל 12 השדות
נמצאים בכולם).

**איפה מקנפגים:** לפי בחירת הצוות, השדות נכנסו לבורר העמודות של **מוד הטבלה**
(`שדה מפ"מ`), ולא לחלון ייעודי שנבנה מראש. השדה המצרפי `aim_points` מציג את כל
נ"צי התקיפה ובעריכה פותח את עורך הטבלה; 11 השדות הפרטניים הם עמודות **קריאה
בלבד** - שורת נ"צ היא יחידה אחת, ופיצולה ל-11 תאים נערכים היה מאלץ עריכה של
אותו מערך מכמה מקומות במקביל.

**עורך אחד ולא שניים** ([AimPointsTable.tsx](src/components/strips/AimPointsTable.tsx)):
פאנל פרטי הפ"מ צר (230px) מכדי להכיל 11 עמודות, ולכן שם מוצג תקציר וכפתור פותח
את אותו חלון עריכה שנפתח ממוד הטבלה. מסגרת כתומה (חלון עריכה), נגרר ב-Pointer
Events דרך `useDragPosition`, portal ל-body עם `zoom: var(--s)` ידני, ושלוש
התמות. תפריט החימושים נטען פעם אחת מ-`default_armament_names` ומשותף לכל המופעים -
במוד טבלה יש מופע לכל שורת פ"מ, ובקשה לכל אחד הייתה מציפה את השרת.

**נ"צ:** `NDDMM.mmmm/EDDDMM.mmmm`, 17 ספרות. הפקח מעתיק ממסמך, ולכן הדבקה של 17
ספרות רצופות מסודרת לפורמט ב-blur; דקות ≥ 60 ומעלות מחוץ לתחום מסומנות באדום בלי
לחסום הקלדה. **מרעום** מוקלד ב**שניות** (0.02) ומוצג לצידו ב-מ"ש (20) כדי שלא
יתפרש כ-0.02 מ"ש.

**הפער שהתגלה ב-GAPI:** `SORTIE_OP_FIELDS` ב-[hooks.js](server/gapi/hooks.js) קובע
אילו עריכות מצדיקות דחיפה החוצה - ו-`targets` לא היה שם. בלי התיקון הבקר היה עורך
נ"צ תקיפה, השינוי היה נשמר ב-SKYKING, ו**לא** היה מגיע ל-GAPI: בלי שגיאה ובלי סימן.
בצד GAPI נוסף `aim_points` כשדה תפעולי דו-כיווני (`strips.targets`, JSONB), ומבנה
השורה תועד ב-GAPI-CONTRACT.md §6.1.1.

**QA:** 47 בדיקות למודל (תאימות לאחור לשורה בת שני שדות, נ"צ, מרעום, ייבוא/ייצוא,
שילוב בשלושת הקטלוגים) + 6 בדיקות GAPI (הלוך-חזור של 11 השדות, מערך ריק,
`bodyTouchesOperational`). כל החבילה: 1400 בדיקות ירוקות · `tsc` נקי · build עובר.

**חלוקה לענפים:** ה-UI וה-DB בקו הראשי (`feature/aim-points`); המיפוי והחוזה של
GAPI בענף `feature/gapi-integration` שבו הוא חי.

---

## 2026-08-08 - חיווי הנתק הבהב כל שתי שניות בלי שהיה נתק

**הבקשה:** "למה יש כל הזמן ניתוקים רגעיים לשרת? זה מקפיץ הודעה מתישה."

**האבחון: לא היו ניתוקים.** האפליקציה ייצרה אותם בעצמה. `apiFetch` הכריז נתק על
**כל** כישלון בודד, בלי סף ובלי לסווג את סוג הכישלון - וארבעה מקורות נפרדים
התנקזו לשם:

| מקור | למה זה לא נתק |
|---|---|
| **ביטול יזום** של ה-poller (`AbortError`) | ה-poller של התמונ"א קוצב לעצמו 1800ms, מבטל את הבקשה הקודמת בכל טיק ומבטל בסגירת הפאנל. הוא **יודע** שביטול אינו תקלה (`ac.signal.aborted`), אבל השכבה שמתחתיו לא הכירה את ההבחנה |
| **503 של ריליי התמונ"א** | זו התשובה כשהתמונ"א **כבויה** - מצב תקין |
| **502 של ריליי התמונ"א** | ההערה בשרת אומרת מפורשות "התקלה במאגר החיצוני, לא ב-SKY-KING" - והלקוח סתר אותה |
| **8 שניות מול תור של 6 חיבורים** | SectorDashboard מריץ ~40 pollers; HTTP/1.1 מגביל ל-6 חיבורים למקור, ובקשה יכולה לחצות את התקרה בזמן שהשרת בריא (אותו מנגנון של #028) |

הקצב: כל 2 שניות. כל הבהוב הדליק בועית ענבר פועמת ואז **6 שניות** של "הקשר חזר".

**התיקון - שלוש שכבות:**

1. **סיווג במקום ספירה עיוורת** ([apiFetch.ts](src/offline/apiFetch.ts)): ביטול של
   הקורא ניטרלי ונזרק אליו חזרה כמו שהוא (תשובה מהטמון במקומו הייתה נקראת אצל
   ה-poller כדגימה חדשה); תקרת הזמן שלנו מוסבת ל-`TimeoutError` כדי שתהיה נבדלת
   מ-`AbortError`; **תשובת 5xx = השרת ענה**, ולכן `markReachable` ולא נתק.
2. **סף שלושה כשלים רצופים** ([netStatus.ts](src/offline/netStatus.ts)): בנתק אמיתי
   כל הבקשות נופלות והסף נחצה בפחות משנייה; בקשה בודדת שנפלה מתוך 40 לא מחשיכה
   עמדה. השחזור נשאר מיידי, והנתק מתוארך לכשל **הראשון** ברצף.
   `/api/air-picture/` עוקף את השכבה כולה - הוא ריליי למאגר חיצוני עם חיווי משלו
   (ובדרך נחסכה כתיבת מאות KB ל-IndexedDB כל שתי שניות).
3. **החיווי לפי "המידע אינו חי" ולא "אין קשר"** ([ConnectionBanner](src/components/shared/ConnectionBanner.tsx)):
   שרת שעונה 5xx לאורך זמן מקפיא את התמונה בדיוק כמו כבל מנותק, ולכן `stale`
   עולה גם כשגיל המידע חוצה 30 שניות בזמן שהשרת "עונה". "הקשר חזר" מוצג רק אחרי
   נתק בן 4 שניות ומעלה (או כשיש פעולות ב-outbox) - הודעה בת 6 שניות על אירוע בן
   שנייה היא רעש, לא בשורה.

**תופעת לוואי שנסגרה:** כתיבה שנכשלה ב-5xx הוצגה כ"נחסמה כי אין קשר" (הודעה
שגויה - השרת ענה), וכתיבה **פרטית** נכנסה ל-outbox ושודרה שוב מאוחר יותר, כלומר
שוכפלה. עכשיו תשובת 5xx חוזרת לקורא כמות שהיא.

**QA:** 1318/1318 unit (12 בדיקות חדשות ב-`apiFetch.test.ts`: ביטול, תקרת זמן,
סף, תיארוך הנתק, עקיפת התמונ"א, 5xx בכתיבה) · tsc · build.

---

## 2026-08-08 - הצורה נחתה במקום הלא נכון בעמדת שדה

**הבקשה:** "ציור עיגול ומלבן בעמדת שדה תעופה לא מצוייר מהמקום הנכון."

**האבחון:** לא ניחוש - מדידה בעמדת שדה חיה (e2e שנכנס לעמדה, גורר בעט וקורא את
ה-`boundingBox` של הצורה שרונדרה). המספרים חשפו שתי תקלות **נפרדות** שהסתירו זו
את זו:

| מה נמדד | ערך |
|---|---|
| `canvas.width` (ה-bitmap) | 689 |
| הקופסה של הקנבס על המסך | **1055** |
| שרשרת האבות של הקנבס | העמודה המרכזית - **לא** שכבת תוכן המפה |
| היסט הצורה בפועל | **210px** |

1. **המשטח הורכב מחוץ לשכבת המפה.** ה-`</div>` שנשא את ההערה "end mapInnerRef"
   אינו זה שסוגר אותה בפועל (קובץ JSX בן 5,200 שורות), ולכן `<MapDrawSurface>`
   נחת כאח של מכולת המפה ולא כילד שלה. תוצאה: הקנבס נמתח על כל העמודה המרכזית
   ו**לא נע עם הזום/פאן של המפה**. תוקן - המשטח הועבר פנימה, והאימות הוא
   שרשרת האבות בפועל ב-DOM, לא ההזחה בקוד.
2. **המנוע מדד את ההורה במקום את עצמו.** הקנבס ושכבת הצורות שניהם `width:100%`,
   כלומר שניהם נמדדים מול **בלוק ההכלה** (האב הממוקם הקרוב) - ולא בהכרח מול
   `parentElement`. כשההורה הישיר הוא `position:static` השניים נפרדים: ה-bitmap
   נבנה ב-689 ואילו ה-SVG פירש את אותם מספרים בקנה 1055, והצורה נחתה מוסטת
   ומוקטנת ב-35%. המנוע מודד עכשיו את **הקנבס עצמו** - המדד היחיד שמובטח
   שיסכים עם שכבת הצורות בכל מקום שבו הרכיב יורכב.

**למה זה חמק:** כל בדיקות המיקום עד כה בדקו את **קו העט** - פיקסלים על ה-bitmap,
שם המיפוי `client → bitmap` עקבי בפני עצמו ולכן נכון גם כשהקנה שגוי. הצורה עוברת
מסלול אחר לגמרי (שבר → SVG), ואף בדיקה לא מדדה **איפה היא נוחתת**. נוספו חמש:
מטריצת 15.6"/24" × זום מפה 100%/x2, ובדיקה ייעודית שההורה הישיר `static` ובגודל
אחר. אומת אדום-ירוק: בלי התיקון ההיסט 50.75px, איתו < 6px.

**QA:** מדידה בעמדת שדה חיה - ההיסט ירד מ-210px ל-0.8px (חצי עובי הקו) ·
`tsc` נקי · `vite build` נקי · `e2e/map-draw` 15/15.

---

## 2026-08-08 - שתי תקלות בסרגל הציור

**הבקשה:** "לא ניתן לגרור את חלון הציור" · "ציור של מלבן ועיגול לא מתחיל
ממיקום העכבר".

| תקלה | סיבה | תיקון |
|---|---|---|
| הסרגל לא נגרר | **לא היה מימוש גרירה בכלל** - רק `stopPropagation` על ה-pointerdown. הסרגל מכסה את המפה, והפקח לא יכול היה לראות מה שמתחתיו | ידית ⠿ דרך [`useDragPosition`](src/hooks/useDragPosition.ts) המשותף, שכבר פותר את חלוקת ה---s, את `setPointerCapture` ואת `touchAction` |
| מלבן/עיגול בהיסט | `SectorDashboard` מדד את `#map-area` ב-`getBoundingClientRect()` - **פיקסלים ויזואליים**, שכוללים את `zoom: var(--s)` - בעוד ששכבת הצורות (SVG ב-`width:100%`) מפרשת אותם כפיקסלי **פריסה** | `clientWidth`/`clientHeight`, שאינם מושפעים מ-zoom של אב |

**למה זה לא נתפס קודם:** ההיסט הוא **פי `--s`**, כלומר הוא **לא קיים כלל
במסך 15.6"** (`--s = 1`) וגדל ל-65% במסך 24" - ובתוך זה הוא גדל ככל
שמתרחקים מפינת המפה. הרכיב המשותף (`MapDrawLayer`) עשה את זה נכון מלכתחילה
עם `parent.clientWidth`, ולכן מתקן הבדיקה שלו עבר; רק מנוע הציור הוותיק של
עמדת הבקר החזיק את היחידות השגויות.

**שחזור לפני תיקון:** מדדתי במתקן הבדיקה של הרכיב המשותף וקיבלתי היסט של
פחות מ-1.2px גם ב-`--s=1.65` - כלומר התקלה **אינה** שם. זה מה שהוביל למנוע
הנפרד של `SectorDashboard`.

**QA:** `tsc` נקי · 1,299 בדיקות · `vite build` נקי · `e2e/map-draw.spec.ts`
**10/10** כולל בדיקה חדשה שגוררת את הסרגל ב**עט, באצבע ובעכבר** בנפרד -
המלכודת האמיתית היא שבאצבע הוא לא זז בכלל.

**לא אומת ויזואלית בעמדה האמיתית:** תיקון היחידות נבדק בקריאה ובבנייה, אבל
לא הורץ מול עמדת בקר חיה במסך 24".

---

## 2026-08-07 - ATSIM הופרד לריפו נפרד + חילול תרחישים ורקע Google Maps

**הבקשה:** "אני רוצה שזה יהיה נפרד מ-SKYKING" · "בסימולטור יהיה יכולת לחולל
תרחיש אוטומטי - משתמש יבחר כמות ופוליגון וזה יעשה רנדומלי" · "יכולת להוסיף
מפה מגוגל MAP".

**הריפו החדש:** [github.com/levori119/atsim](https://github.com/levori119/atsim)
(פרטי), מקומית `../atsim`. `atsim/` נמחק מ-SKY-KING יחד עם `npm run atsim`
ושורות ה-`.gitignore`. הקומיט המקורי (`e73b938`) נשאר בהיסטוריה — לא נכתבה
היסטוריה מחדש, כי היא כבר נדחפה.

| החלטה | למה |
|---|---|
| ריפו נפרד ולא submodule/monorepo | ATSIM אינו חלק מ-SKY-KING אלא **מערכת עמיתה**. submodule היה מחזיר בדיוק את הצימוד שההפרדה נועדה לבטל, ו-registry פרטי אינו אופציה ברשת מבודדת |
| `listen.js` הועתק ולא יובא | 12 שורות מול תלות בין-ריפואית שנשברת בכל שינוי מבנה |
| **החוזה בשני עותקים** + `AIR_TRAFFIC_API_VERSION` | משמעת אנושית אינה מנגנון. המאגר מחזיר `v` בכל סנאפשוט, והעמדה מתריעה על MAJOR שונה — **בדיקת זמן ריצה**, שהיא היחידה שעובדת בין ריפואים. אי-התאמה אינה חוסמת תצוגה: מסך ריק בלי הסבר גרוע יותר |
| חילול **דטרמיניסטי לפי seed**, לא `Math.random` | "התקלה קרתה בתרחיש 4821" הוא דיווח שאפשר לעבוד איתו; "היו מטוסים אקראיים" הוא לא. וגם: בדיקות בלי mock ל-RNG |
| דגימת דחייה עם גבול ניסיונות | פוליגון קעור וצר (פרוזדור, רצועת חוף) תופס אחוז זעום מהתיבה החוסמת, ובלי גבול הלולאה הייתה תולה את השרת |
| Google Maps **דרך השרת** ולא מהדפדפן | מפתח שמוטמע בעמוד הוא מפתח שדולף. בלי מפתח או בלי רשת — נפילה חזרה לרשת נ"צ, כי הכלי אמור לעבוד גם מנותק |
| **Web Mercator** (`geo.js`) במקום מתיחה ליניארית | ברגע שיש תמונת רקע אמיתית, מעלת רוחב אינה קבועה. מטוס שהוצב במתיחה ליניארית היה יושב עשרות מיילים מהמקום שבו הוא נראה. ה-bbox האפקטיבי נגזר **מהתמונה** ולא להפך |

**באג שנתפס באימות:** ה-`seed` שנגזר מהקלט הוחזר כמחרוזת base36,
`generateScenario` דחה אותו (`Number('abc') = NaN`) ונפל ל-seed קבוע `1` —
**כל התרחישים נבנו מאותה סדרה**. התגלה כי הפלט הראה `seed: 1` לכל בקשה.
הוחלף ב-`seedFrom` מספרי, עם ארבע בדיקות.

**QA:** ב-ATSIM — 67 בדיקות עוברות, ואומת מול השרת החי: חילול 15 מטוסים בגזרה
משולשת, **אפס מחוץ לגזרה**, ושתי גזרות שונות מייצרות תרחישים שונים.
ב-SKY-KING — `tsc` נקי, 1,286 בדיקות ב-69 קבצים (‏28 עברו לריפו החדש),
והריליי `SKY-KING → ATSIM` אומת מול המאגר במיקומו החדש (`v: "1.0"`, 15 מטוסים).

**מגבלה ידועה:** החילול מבטיח שכל **נקודות הדרך** בגזרה. בפוליגון קעור קטע ישר
בין שתי נקודות פנימיות עלול לחתוך החוצה.

---

## 2026-08-07 - תמונ"א על הדסק בעמדה (שלבים 2-9)

**הבקשה:** "תמשיך הכל" - מהמאגר (ATSIM) אל **העמדה עצמה**.

| החלטה | למה |
|---|---|
| הסנאפשוט ב-store **מחוץ ל-React** (`useSyncExternalStore`) | אילו ישב ב-`useState` של `SectorDashboard` (17,894 שורות), כל דגימה הייתה מרנדרת את כל מסך הבקר 30 פעם בדקה. בין הרשת לפיקסל אין אף `setState` ברכיב-אב |
| **קנבס ולא SVG** | 300 מטוסים ב-SVG = ~1,500 צמתי DOM שמשתנים כל 2 שניות. השכבות הקיימות (אזורים, הקפות) הן SVG בצדק - הן סטטיות ומעטות. זו בחירה לפי סוג המידע, לא סטייה מהתבנית |
| **מטמון ביטמאפ לתוויות** (LRU 400) | `fillText` ~10-20µs × 600 קריאות לפריים = יותר מכל התקציב. התווית מצוירת רק כשהטקסט משתנה - לכל היותר פעם ב-2 שניות |
| הקנבס **בתוך** ה-transform של המפה, עם צפיפות שמוכפלת ב-`mapZoom` | **שינוי מהתכנון המקורי.** התכנון היה להוציא אותו החוצה ולחשב פאן/זום ידנית, אבל שכבה עם `transform` היא הקשר ערימה סגור - ומחוץ לה אי אפשר לשבת **בין** תמונת המפה ליישויות SKY-KING. הכפלת הצפיפות פותרת את הטשטוש בלי המתמטיקה, ומייתרת גם את ה-lerp על ה-transition |
| `setTimeout` שרשרתי ולא `setInterval` | רשת שמשיבה ב-3ש' מול קצב של 2ש' הייתה בונה תור בקשות חופפות שגדל בלי גבול |
| poller **יחיד לעמדה** עם ספירת מנויים | דו-מפה מציגה את אותה תמונה. אין שום סיבה למשוך אותה פעמיים |
| הקאפ (300) **אחרון** בשרשרת, לפי מרחק ממרכז המפה | מטוס שהפקח סינן החוצה לא תופס מקום בתקרה ודוחק מטוס רלוונטי. וחיתוך "300 הראשונים" היה מסתיר דווקא את המטוס שמעל השדה |
| ב-`stale`/`down` המטוסים **נשארים** עם חיווי גיל | תמונה שנעלמת פתאום נקראת כ"אין תנועה". תמונה ישנה **שמסומנת ככזו** עדיפה על היעלמות שקטה |
| בהירות ברירת מחדל 0.45, `zIndex` מתחת לשכבות SKY-KING, `pointerEvents:'none'` | התמונ"א היא **מודעות מצבית ולא כלי עבודה**. הפ"מים חייבים להישאר הבולטים והלחיצים. זה גם מה שהאפיון ביקש |
| ההעדפות ב-`sessionStorage` ולא ב-DB | אותה תבנית של `data_windows`. כתיבה בכל הזזת סליידר הייתה עשרות `UPDATE` בדקה - ובעיקר, הייתה משנה את ההגדרה לכל מי שיעלה בעמדה אחריו |
| שמירה **חסומה** כשתמונ"א דלוקה ואף מפה לא מעוגנת | עמדה כזו מבטיחה מידע ולא נותנת אותו. פקח שרואה מפה ריקה יסיק "אין תנועה" במקום "התצוגה מנוטרלת". בדו-מפה שרק אחת מעוגנת - אזהרה בלבד, כי יש תמונה אמיתית על מפה אחת |
| `air_picture_config` **גלובלית**, לא פר-סביבה | החלטת הצוות: מאגר אחד לכל הסביבות בשלב זה. רשומה ב-`IGNORED_EXACT` כמו `gapi_env_config` |

**ממצא אבטחה שנסגר תוך כדי:** `PUT /api/air-picture/config` נפל לברירת המחדל
`USER` בטבלת המדיניות, כלומר **כל בקר מזוהה יכול היה להפנות את התמונ"א התפעולית
למאגר אחר**. התגלה באימות בפועל (ה-probe שציפיתי שיחזיר 403 החזיר 200 - ובדרך
גם דרס את הקונפיג). הועבר ל-`ADMIN`, ו-`admin-config` ל-`STAFF`.

**שכבה משותפת אחת** ל-`SectorDashboard` ול-`GroundView` - אותה פונקציונליות,
אותה לוגיקה, אותו עיצוב. אין חריג.

**QA:** `npx tsc --noEmit` נקי · `npm test` - 1,314 עברו, 70 קבצים (33 חדשות
ב-`track.test.ts` וב-`prefs.test.ts`) · `npx vite build` נקי ·
**אומת קצה-לקצה מול DB חי ומול ATSIM**: המיגרציה הוחלה (שתי עמודות + טבלה,
`TIMESTAMPTZ`), הקונפיג הופנה למאגר, והריליי `SKY-KING → ATSIM` החזיר 5 מטוסים
עם נ"צ, גובה וסיווג. `GET /config` אומת שאינו מחזיר את טוקן המאגר.

**מה לא נבדק:** אין עדיין e2e של Playwright על השכבה עצמה, ולא נמדד תקציב
הציור (3ms) מול 300 מטוסים אמיתיים - שער הביצועים של §6 באפיון. השכבה כוללת
הורדת-רמה אוטומטית, אבל הסף עצמו טרם נמדד בעמדה.

---

## 2026-08-07 - ATSIM: מאגר תמונ"א (שלבים 0-1 של "תמונ"א על הדסק")

**הבקשה:** "תריץ את מאגר תמונ"א, ויחד עם השרת הם לא ב-RAILWAY ולא ב-NEON."
לפני כן נקבעה השפה: "תמונה אווירית זה מטוס פיזית בשמיים, ופ"מ זה הרישום שלו."

| החלטה | למה |
|---|---|
| החוזה ב-`shared/airTrafficApi.js` ולא בתוך `atsim/` | שלושה צרכנים צריכים אותו - המאגר, ה-FRONT שלו, ובעתיד שכבת התצוגה בעמדה. אותה תבנית של `shared/sanitizeHtml.js` |
| **מטוס ≠ פ"מ** - `id` יציב **נפרד** מ-`cs` | שם הפ"מ משתנה תוך כדי טיסה, המטוס הפיזי לא. מזהה שנגזר מהשם היה "מקפיץ" את המטוס בכל שינוי רישום ומאפס את המטמון |
| **חישוב על-פי-קריאה ולא טיקים** | אפס CPU במנוחה · בלי drift · עמיד לאתחול (המיקום נגזר מהזמן) · "הרצה בשעה מסוימת" מגיעה בחינם · נבדק ב-vitest בלי שרת ובלי שעון |
| משך צלע לפי **ממוצע** המהירויות בקצותיה | בהאצה ליניארית המרחק הוא v_ממוצע × T. עם v0 בלבד המטוס היה מפספס את נקודת הדרך בכל צלע שבה המהירות משתנה |
| **קובץ מקומי, לא Neon ולא Railway** | המשך ישיר של עקרון הבידוד: מאגר שיושב על אותו DB מנוהל היה נופל יחד איתו. באותו יום עצמו מכסת Neon חרגה והפילה את העמדה - בדיוק התרחיש |
| `ETag` על התוכן בלבד, בלי `t`/`seq` | אחרת כל דגימה היא מטען מלא גם כשאיש לא זז. בתנועה חיה זה ממילא 200 - החיסכון הוא רק בתמונה קפואה, וכך זה אמור להיות |
| שכפול יוצר עותק **עצור** | שכפול הוא פעולת עריכה. תרחיש שמתחיל לרוץ מעצמו היה מזריק מטוסים לתמונה בלי שמישהו ביקש |
| כתיבה אטומית (tmp + rename) | כתיבה שנקטעת משאירה JSON חתוך, וההפעלה הבאה נופלת לזרע - כלומר **כל התרחישים נמחקים בשקט** |

**באג שנתפס:** `Number(null)` הוא `0` ולא `NaN`, ולכן תרחיש **עצור**
(`startAt: null`) נקרא כ"הומרא ב-1970" ומסלול מעגלי גלש חזרה לתוך התמונה -
תרחיש שאיש לא הריץ שידר מטוסים לעמדה. ה-`/api/health` חשף את הסתירה
(`running: 0` לצד `tracks: 5`). תוקן עם בדיקת רגרסיה.
התגלה רק מפני שרסטארט נכשל ב-`EADDRINUSE` ונבדק בטעות הקוד הישן -
`server/listen.js` דיווח על הכשל בקול במקום "listening" על שרת מת.

**QA:** `npx tsc --noEmit` נקי · `npm test` - 1,280 עברו, 68 קבצים
(30 חדשות ב-`atsim/sim.test.js`) · אימות ידני מול השרת החי: הרצה/עצירה מגדרת
בפועל (5 מטוסים → עוצרים תרחיש → 2 → מריצים → 5), דגימות ברווח 4 שניות מראות
תנועה וטיפוס עקביים, `ETag` מחזיר `304` בגוף ריק בתמונה קפואה, וה-FRONT נטען
ב-Playwright בלי שגיאת קונסולה.

**מה עוד לא נעשה:** העמדה אינה צורכת את זה. שכבת הקנבס, הקאפ של 300 בצד
הלקוח, וההגדרה בניהול עמדה הם שלבים 2-6 ב-[AIR_PICTURE_SPEC.md](AIR_PICTURE_SPEC.md).

---

## 2026-08-07 - סרגל הציור בעמדת השדה (רכיב משותף עם עמדת המפה)

**הבקשה:** "בעמדת שדה תעופה שים את סרגל הכלים של ציור שיש בעמדת מפה."

| החלטה | למה |
|---|---|
| **חילוץ** הסרגל לרכיב משותף (`components/map/MapDrawLayer.tsx`) ולא שכפול לעמדת השדה | סרגל ציור שני היה מתפצל מהראשון בשינוי הבא. עקרון הרכיבים המשותפים: רכיב זהה = אותה פונקציונליות, אותה לוגיקה, אותו עיצוב בשתי העמדות |
| הלוגיקה הטהורה ל-`utils/mapDrawing.ts`, התצוגה ב-`MapDrawLayer` | ההמרות שבר↔פיקסל, עובי המחק ויצירת הצורה ניתנות לבדיקה בלי DOM. עמדת המפה עברה לייבא אותן במקום להחזיק עותק פרטי |
| עמדת המפה מחוברת **רק לסרגל**, ומריצה מנוע ציור משלה | הציור שם מסונכרן בין חברי העמדה (`collab-state`) ומזין את זיהוי הכתב שמניח פ"מ על המפה. לגרור את זה לעמדת השדה היה מייבא מנגנון שלם שאין לו שם משמעות |
| הייחודי לעמדת המפה נכנס דרך פתחים (`tools` / `toolsExtra` / children) | כלי האו"ק, בדיקת MyScript ושיתוף העמדה נשארים שם - בלי `if (isGroundMode)` בתוך הרכיב המשותף |
| הקנבס **בתוך** שכבת התוכן של המפה | כך הציור נע ומתקרב עם המפה. בחוץ הוא היה נשאר תלוי במסך והסימון היה מצביע על המקום הלא נכון ברגע שהפקח מזיז את המפה |
| כפתור ה-✏ **בפאנל השכבות** ליד פקדי הזום, **עם כיתוב** | הוא ישב תחילה כריבוע 20px בלי כיתוב בפינה השמאלית-תחתונה - ולא נמצא בעין ("לא מוצא את זה"). זו הפינה שבה יושב ה-✏ גם בעמדת המפה, וזה המקום שבו מחפשים כלי מפה. כשהפאנל סגור (מתפריט "תצוגה") הכפתור צף באותה פינה, כדי שהציור לא ייעלם עם הפאנל |
| הסרגל מותאם לשלוש התמות ולסקייל גודל המסך | הסרגל הישן היה כהה קשיח. ocean היא תמה **כהה** - הניגודיות של הכותרת/התוויות/הכפתורים נמדדת בבדיקה, לא מונחת |

**הגרירה:** Pointer Events + `touchAction:'none'` + `setPointerCapture` (בתוך
try/catch) ובלי `preventDefault` על ה-pointerdown. ה-e2e מדמה עט, אצבע ועכבר
בנפרד - זו הדרך היחידה לוודא שהציור לא "עובד רק בעכבר".

**תיקון (אותו יום): הקנבס כיסה את הפקדים.** מהשטח: "לחצתי על ציור, לא מצליח
ללחוץ על שאר הכפתורים... וזה מצייר על החלון POPUP של פקד הצייר". השורש הוא
**קונטקסט ערימה**, לא z-index: בעמדת המפה לשכבת התוכן **תמיד** יש `transform`
(= קונטקסט ערימה סגור), ולכן הקנבס שבתוכה לעולם אינו מדורג מול הפאנלים שמחוצה
לה. בעמדת השדה ה-`transform` **מוסר בזום 100%** (`el.style.transform = ''`),
שכבת התוכן נשארה בלי z-index, והקנבס (z=200) דלף לקונטקסט של מכולת המפה - שם
הוא גבוה מסרגל הציור, מפאנל השכבות ומכפתור ה-✏, כיסה אותם, ולכן בלע את
הלחיצות ואת משיכות העט. התיקון: שכבת התוכן מקבלת `zIndex: 0` + `isolation:
'isolate'` וסוגרת את עצמה - כל מה שבתוכה (סטריפים z=30, תפריטים z=100, הציור
z=200) נשאר מתחת לפקדים **בלי קשר ל-z-index שלו**. השומר ב-e2e בודק את
המבנה ולא את המספר: `document.elementFromPoint` מעל כפתור פקד חייב להחזיר את
הכפתור ולא `canvas`, ומשיכת עט על גוף הסרגל אינה מציירת דבר. אומת אדום-ירוק
(נכשל בלי התיקון).
**תופעת לוואי מוכרת:** מודאל "הוראות הסעה" (`position:fixed`, z=9999) יושב
בתוך שכבת התוכן ולכן נערם עכשיו מתחת לפאנלי המפה. זה כבר היה המצב בכל זום ≠
100% (ל-`transform` יש אותה השפעה), ולכן התיקון עשה את ההתנהגות **עקבית** ולא
חדשה.

**QA:** `npx tsc --noEmit` נקי · `npm test` - 1,228 עברו (24 חדשות: 12 ב-
`mapDrawing.test.ts`, 12 ב-`MapDrawLayer.test.tsx`) · `npx playwright test
e2e/map-draw.spec.ts` - 8 עברו: ציור בעט/אצבע/עכבר, הקנבס שקוף לאירועים כשהציור
כבוי, הקו נוחת במקום הנכון תחת זום מפה x2 + מסך 24" (`--s=1.65`), הציור נשאר
מעוגן אחרי שינוי גודל, "נקה" מוחק, ומלבן נשמר כצורה · `npx vite build` נקי.
**לא אומת מול DB חי**: מכסת התעבורה של Neon חרגה, ולכן ה-e2e של העמדה עצמה
(כניסה לעמדת שדה אמיתית) לא רץ - האימות נעשה על מתקן בדיקה שמשחזר את מבנה
המפה (`e2e/fixtures/map-draw.tsx`).

---

## 2026-08-07 - אמצעי נחיתה על המסלול (ILS / LOC / GS / VOR / TACAN)

**הבקשה:** "בהגדרת מסלול בשדה תעופה להגדיר לו אמצעי נחיתה ILS, LOC, GS, VOR,
TACAN. בשדה התעופה עצמו על מסלול להגדיר סטטוס אמצעים - תקין / לא שמיש / אחזקה /
תקין מוחרג והערה. על המסלול עצמו של נחיתה לסמן את אמצעי הנחיתה ובצבע את הסטטוס -
ירוק שמיש, אדום עם X לא שמיש, אדום ללא איקס אחזקה, כתום שמיש מוחרג - וב-HINT
להציג הערת החרגה. לשים עם כיוון המסלול בין הזברה למספר."

| החלטה | למה |
|---|---|
| אמצעי שייך ל**קצה** ולא למסלול (`aids_a`/`aids_b`, `end_side`) | ה-ILS של 27 וה-ILS של 09 הם התקנות נפרדות עם סטטוס נפרד. הטופס כבר מפוצל "צד A"/"צד B" (מרחקים מוצהרים), והסימון ממילא מצויר בקצה הרלוונטי |
| **הגדרה** על `airfield_runways` (קונפיג) · **סטטוס** ב-`runway_aid_status` (תפעולית) | בדיוק ההפרדה של התאורות וה-NOTAMים: ההגדרה משותפת לכל הסביבות, המצב החי מבודד פר סביבת תרגול |
| `end_side` = מיקום ('a'/'b') ולא שם הכיוון | במסלול מקושר בשני שדות שמות הכיוונים אינם בהכרח זהים; המיקום הוא מה שניתן למפות בוודאות (`matchEndSlot`, כמו `shorten_end` ב-NOTAM) |
| הקריאה דרך `resolveAidStatus` ולא `SELECT` בראוט | ILS תקול הוא תקול לכל מי שרואה את המסלול הפיזי. השומר (`runwayStateGuard.test.js`) מכשיל קריאה ישירה מהטבלה |
| **ההגדרה קובעת מה מוצג** | אמצעי מוגדר בלי דיווח = תקין; שורת סטטוס לאמצעי שהוסר מההגדרה נמחקת בעדכון המסלול. אחרת "ILS לא שמיש" היה קם לתחייה ביום שבו מגדירים מחדש ILS באותו קצה |
| ההערה נשמרת רק ב"תקין מוחרג" | הערת החרגה שנשארת תלויה על אמצעי שחזר להיות תקין היא מידע שגוי, לא מידע ישן |
| גודל הכיתוב נגזר משלושה חסמים ולא קבוע | רוחב המסלול ("TACAN" ארוך מ-"GS"), הרצועה שנותרה בין הזברה למספר, ולעולם לא בגודל **מספר הכיוון** - הוא הזהות של המסלול |
| רק אמצעי **עם הערה** לוכד מצביע במפה | ה-HINT חייב pointer-events, אבל שכבת המסלולים היא `pointer-events:none` כדי שגרירת המפה תעבור דרכה. סימון שקט לא יגנוב תחילת גרירה בעט |

**מיקום הסימון:** בין הזברה למספר, מסובב לכיוון הטיסה מאותו קצה - בדיוק כמו על
המסלול האמיתי: הנוחת חוצה את הסף, קורא את האמצעים ואז את מספר הכיוון. אותו סימון
מופיע בשני המקומות שבהם המסלול משורטט - מפת השדה (`RunwayLayer`) ודיאגרמת
הווידג'ט בסרגל העזרים - ומשתמש באותה לוגיקה (`runwayAids.ts`), לא בשכפול.

**שלושה תיקונים אחרי המשוב הראשון:**

| מה | למה |
|---|---|
| חלון הסטטוס קיבל **מסגרת וכותרת** (גבול 2px, סרגל כותרת, צל) | נפתח כקופסה חשופה שנראתה כמו שכבה תלושה מהמסך ולא כחלון |
| נפתח **ממורכז מעל שרטוט המסלולים** (portal ל-body, zIndex 8950) במקום בצד | הפקח קובע סטטוס **מול המסלול שהוא רואה**. בנוסף, `insetInlineEnd` הצמיד אותו ב-RTL לצד השמאלי - ההפך מהסרגל שממנו הוא נפתח |
| **מסלול סגור אינו צובע את האמצעים באדום** (במפה ובווידג'ט) | היה `isClosed ? אדום : צבע הסטטוס` - וכך ILS **תקין** במסלול סגור הוצג אדום. "ILS אדום" פירושו שה-ILS תקול; הסגירה כבר מסומנת ב-X הגדול ובמסגרת |
| **משבצת חצי-שקופה מאחורי כל סימון במפה** (מילוי `#020617` ב-0.62 + מסגרת בצבע הסטטוס) | על תצלום שדה אמיתי כיתוב "עירום" נבלע במיסעה. גובה המשבצת נחסם ב-`lineHeight` שחוזר מ-`aidLabels` ורוחבה ברוחב המסלול, כך שמשבצות סמוכות אינן נוגעות ואינן חורגות מהאספלט בשום זום |

**QA:** `npx tsc --noEmit` נקי · `npm test` - 1,210 עברו (45 חדשות: 17 ב-
`runwayAids.test.ts`, 8 ב-`runwayShape.test.ts` לגאומטריית הסימון, 7 ב-
`RunwayLayer.test.tsx` על ה-SVG המרונדר בפועל - כולל נעילת הכלל שמסלול סגור אינו
משנה את צבע האמצעים) · `npx vite build` נקי.
**לא אומת מול DB חי**: מכסת התעבורה של Neon חרגה בזמן הפיתוח, ולכן המיגרציה
(שתי עמודות JSONB + טבלה) ושני ה-endpoints לא הורצו מול הסכמה בפועל.

---

## 2026-08-05 - כל גרירה עובדת בעט ובאצבע (Pointer Events)

**הבקשה:** "לא מצליח לגרור עם טאץ', דסק חופשי. צריך להכניס ל-CLAUDE.md שצריך
לתמוך בטאץ' עם עט / יד בכל הגרירות (חלונות / ספליטרים וכו')." ואז: "תתקן הכל".

עמדת היעד היא **מסך מגע** (Wacom Cintiq 24 Touch + Pro Pen 3), אבל 24 ידיות
גרירה מומשו ב-`onMouseDown` + `mousemove`/`mouseup`. **אירועי mouse אינם נשלחים
באצבע** - ולכן כל אחת מהן פשוט לא נגררה בעמדה. הדסק החופשי היה רק המקרה שנתקלו בו.

| החלטה | למה |
|---|---|
| `utils/pointerDrag.ts` - `startPointerDrag` + `DRAG_HANDLE_STYLE` | ארבע מלכודות חוזרות בכל ידית (touchAction, pointer capture, סקייל, preventDefault). כל מימוש מחדש שכח לפחות אחת - לכן מימוש **אחד** לכל המערכת |
| `touchAction:'none'` על ה**ידית** ולא על המיכל | על המיכל זה הורג את גלילת המגע של המפה. בעורך האדמין הוא מוחל רק על קווי המסלול ורק במצב "ציור סקטור" |
| ההיסט שמוחזר ל-`onMove` כבר **מחולק ב---s** | `clientX` בפיקסלים אמיתיים מול `left/top` ביחידות מוגדלות. בלי זה החלון זז פי 1.65 מהיד בקינטיק 24 - באג שהיה קיים גם בעכבר |
| `closest('button, input, ...')` כיציאה מוקדמת | ה-pointer capture על שורת הכותרת בולע את ה-click של כפתוריה. אותה מלכודת שתוקנה בחלונות הנתונים |
| הוסרו `preventDefault` מ-pointerdown | בעט הוא מבטל את אירועי העכבר התואמים; סימון טקסט נמנע ב-`userSelect` |
| במפות ובסרגלים: המרה ישירה mouse→pointer, בלי capture ידני | הדפדפן קובע **implicit capture** למגע, וה-move/up כבר מרוכזים באלמנט אחד. שינוי מינימלי = סיכון מינימלי |

**תוקנו אגב כך שלושה באגי סקייל** (התבטאו רק ב-`--s` ≠ 1): פאנל התחזית קרא
`getBoundingClientRect` מול `left/top`, גובה סטריפ/פאנל/כותרת בעורכי האדמין,
וסרגל הגבהים ב-`BlockVisualPainter` (מיפוי `clientY` ל-`RULER_H`).

**קבצים:** `src/utils/pointerDrag.ts` (חדש), `CLAUDE.md` (עקרון UX #9 + §גרירה -
מגע ועט + Do NOT), `src/components/views/SectorDashboard.tsx` (7 ידיות),
`src/components/admin/managers.tsx` (5 + שני effects), `src/components/admin/ManagementPage.tsx` (4),
`src/components/map/MapZoneEditor.tsx` (3), `src/components/views/GroundView.tsx` (2),
`src/components/classic/ClassicViews.tsx` (2), `src/components/blocks/BlockVisualPainter.tsx` (1).

**QA:** `tsc` נקי · 1162 unit ב-62 קבצים עוברות · build עובר · סריקה חוזרת מוודאת
שלא נותרה גרירה מבוססת-mouse (`onMouseDown` שנותרו הם כפתורים עם `preventDefault`
לשמירת פוקוס, `stopPropagation`, ורקע מודאל).
**❌ לא אומת בעמדה** - חובה בדיקה ידנית בעט ובאצבע על ה-Cintiq.

---

## 2026-08-06 - נקודות הצטרפות: הטבלה יוצאת מהמפה, והגרירות עוברות ל-Pointer

**הבקשה:** "יכולת לגרור ולשנות מיקום נקודת הצטרפות · שבלוק הגבהים ייפתח
למטה/למעלה ושלא יוסתר ע"י קצוות המסך גם לא בצדדים · כשלוחץ על ה-+ לא פותח
לרשימת מטוסים · לא מצליח לגרור להקפה / לשנות סטטוס."

ארבע התקלות, ומה עמד מאחורי כל אחת:

| תקלה | הסיבה | התיקון |
|---|---|---|
| הסמן לא נגרר | הסמן היה `<button>`, וה-`onPointerDown` בדק `closest('button')` כדי לא לחטוף לחיצות - ולכן על הסמן עצמו הגרירה **לעולם** לא התחילה | הסמן הוא `div role="button"` עם גרירת Pointer וסף תזוזה 6px: מתחת לסף זו לחיצה שפורסת את הטבלה, מעליו זו גרירה |
| הטבלה נחתכה בקצוות | היא ישבה **בתוך** שכבת המפה, שממוזערת ומוזזת עם הזום והפאן | `JoiningPointOverlay` - חלון צף (portal ל-body) שנפתח **מעל הסמן כשיש מקום ואחרת מתחתיו**, מוצמד לגבולות המסך בארבעת הכיוונים, נגרר בכותרת, ומצמיד את עצמו מחדש כשגובהו משתנה (ResizeObserver) |
| `+` נפתח לרשימה ריקה | `strip_aircraft` נוצרות רק כשנוגעים במטוס הבודד, ולכן פ"מ שהגיע בהעברה יכול להיות **בלי שורות בכלל** | `formationAircraft` - נפילה-לאחור למספר המטוסים במבנה. הפריסה היא של המבנה, לא של מה שנשמר ב-DB |
| אי אפשר לגרור להקפה / לשנות סטטוס | הכפתורים ישבו בתוך שורות המטוס שלא נפרסו; והגרירה מומשה ב-HTML5 `draggable`, ש**אינו עובד באצבע** | הגרירה עברה ל-Pointer Events עם `setPointerCapture` + `touchAction:'none'` וצל גרירה, לפי CLAUDE.md §גרירה |

> **מגבלה ידועה:** גרירת פ"מ מ**רשימת הפ"ממים שבצד** אל בלוק גובה עדיין נשענת
> על HTML5 drag - כי כך מוגדר המקור בכל GroundView. בעט זה עובד, באצבע לא.
> מעבר מלא של רשימת הפ"ממים ל-Pointer Events הוא שינוי רוחבי בפני עצמו.

**קבצים:** `src/components/ground/JoiningPointOverlay.tsx` (חדש),
`JoiningPointPanel.tsx`, `src/utils/joiningPoints.ts` (+`formationAircraft`),
`src/components/views/GroundView.tsx`, `src/i18n/registry/joining.json`.

**QA:** 1174/1174 unit (7 חדשות ל-`formationAircraft`) · `tsc` נקי · build.
**❌ לא אומת ויזואלית** - חובה בדיקת משתמש בעמדה.

---

## 2026-08-05 - נקודות הצטרפות (STAR): ישות חדשה בעמדת שדה

**הבקשה:** "רוצה להגדיר ישות חדשה - נקודות הצטרפות. דומה לנקודות העברה אך תצוגה
שונה. רלוונטי רק לעמדה מסוג שדה." (+ בהמשך: "להוסיף יכולת לגרור מרשימת פ"מ בצד
לטבלת גבהים").

נקודת הצטרפות היא נקודת כניסה לשדה. היא **חולקת את מנגנון ההעברות** - הקבלה היא
אותו `POST /api/transfers/:id/accept` - אבל **התצוגה שונה**: במקום פאנל מוסר/מקבל,
הנקודה נפרסת ל**טבלת בלוקי גבהים** ופ"מ יושב בבלוק לפי גובהו, כמו על הסדק.

| החלטה | למה |
|---|---|
| הנקודה שייכת ל**שדה** ולא לעמדה | הלקח מקישורי המסלולים: הצמדת הגדרה לעמדה אפשרה לשתי עמדות באותו שדה לחלוק על מה שקיים בשדה. לעמדה נשארת **דריסת תצוגה** בלבד (מיקום/פרוס) |
| הגובה נכתב ל-`strips.alt` ולא לטבלה חדשה | שני מקורות אמת לגובה של אותו פ"מ הם באג שמחכה לקרות. `joining_point_strips` מחזיק **שיוך ותיאום** בלבד |
| `joining_point_aircraft` ממופתח ב-`(strip_id, idx)` ולא בנקודה | מטוס בהקפה **עוזב** את טבלת ההצטרפות אבל נשאר על ההקפה - המצב חייב לשרוד את היציאה מהנקודה. לכן גם `joining_point_id` הוא SET NULL |
| `flight_status` על `strip_aircraft` | "זה עובר לסטטוס מטוס" - ירוקים / אישור לנחות / נחיתה נשארים נכונים גם מחוץ לנקודה |
| הפרש הגבהים אינו קבוע לאורך הטווח | 1000 רגל בין 4000 ל-7000 ו-500 בין 7000 ל-10000. `buildBlocks` מחלקת לקטעים; קטע שלא כוסה נופל להפרש ברירת המחדל |
| חפיפת טווחים **נחסמת** בשמירה | בלוק חסר או כפול בטבלה הוא טעות **בטיחותית** (מטוס שאינו נראה), לא אי-נוחות |
| חלוקת הנתיבים = חלוקת ההרשאות | `/api/joining-points*` = הגדרה (ראש צוות/מנהל) · `/api/joining-point-strips|aircraft` = מצב חי (כל מזוהה) |

**נמצא ותוקן בבדיקת ה-smoke** (`tsc` + unit לא יכלו לתפוס): `s.note` שאינה קיימת
(העמודה היא `notes`), `details` שנכתב כמחרוזת לעמודת JSONB, ותנאי `in_pattern`
גורף שהציג בעמדה מטוסים בהקפות של **שדות אחרים**. בנוסף הזהות ביומן הביקורת הועברה
מגוף הבקשה ל-`req.user` (SK-18).

**מ-`/atc-ux`:** הקונפליקט מסומן **גם ב-⚠ ולא בצבע בלבד** (צבע לעולם אינו הערוץ
היחיד), והסרת פ"מ מהנקודה מחייבת אישור.

**קבצים:** `server/db/init.js` (5 טבלאות + עמודה), `server/db/env-tables.js`,
`server/routes/joiningPoints.js` (11 routes), `server/app.js`, `server/middleware/auth.js`,
`src/utils/joiningPoints.ts`, `src/components/ground/JoiningPointPanel.tsx`,
`src/components/ground/PatternAircraftLayer.tsx`, `src/components/admin/JoiningPointsSection.tsx`,
`src/components/views/GroundView.tsx`, `src/components/views/SectorDashboard.tsx`,
`src/components/admin/ManagementPage.tsx`, `src/components/admin/managers.tsx`,
`src/i18n/registry/joining.json`, `src/types/ground.ts`.

**QA:** 37 unit חדשות (`joiningPoints.test.ts`) · smoke API 13/13 מול DB אמיתי
(כולל חסימת חפיפה, דריסת עמדה, כתיבת הגובה ל-`strips.alt`, דחיית סטטוס לא מוכר)
· `tsc` נקי · build · i18n guard נקי.
**❌ לא אומת ויזואלית** - טבלת הבלוקים, הגרירות והמפה מחייבות בדיקת משתמש בעמדה.

---

## 2026-08-08 - חלונות הנתונים הם שירות של **כל** עמדה, עם מתג בסרגל העליון

**הבקשה:** "רוצה את השירות הזה לכל עמדה. לכן בסרגל עליון בהגדרות עמדה שים
אופציה 'הצג כמות מטוסים'. בהגדרת עמדה אפשרות להגדיר מראש."

| | |
|---|---|
| השכבה עלתה מ-GroundView ל-SectorDashboard | היא הייתה מותקנת בתוך עמדת השדה, ולכן לא הייתה קיימת בשום עמדה אחרת. עכשיו היא מרונדרת ברמת המסך וצפה מעל כל תצוגה - בקר, קלאסי, אזרחי, דסק משימה |
| `workstation_presets.show_data_windows` | ברירת המחדל שהמנהל קובע לעמדה |
| מתג בסרגל העליון (הגדרות עמדה) | "הצג כמות מטוסים". ההחלטה של הפקח גוברת עד סוף הסשן; בלי נגיעה, ברירת המחדל של העמדה קובעת |
| עורך החלונות בניהול | יצא מהענף של עמדת שדה ועבר למקטע משותף לכל סוגי העמדות |

### באג שנתפס בבדיקה, לא בקוד

החלונות היו ב-`z-index: 9000` (שכבת ההודעות) בעוד תפריטי הסרגל העליון ב-3000.
כלומר **חלון צף כיסה בדיוק את התפריט שממנו מכבים אותו** - הבדיקה ניסתה ללחוץ
על המתג ופגעה בחלון. הורד ל-2500: מעל תוכן המפה, מתחת לתפריטים.

**QA:** 1307/1307 unit · build · e2e 7/7 - בדיקה חדשה מקימה חלון על עמדת **בקר**
(לא שדה), מוודאת שהוא עולה, ומכבה אותו מהמתג בסרגל העליון.

---

## 2026-08-07 - חלון במצב "מספר בלבד" - חצי גובה

מונה נקי הוא מספר אחד, ולא היה צריך את הגובה שנועד לרשימה. הגוף במצב `count`
הוא עכשיו **חצי** מגובה מצבי הרשימה (66 מול 132), כך שנכנסים יותר מונים על
המפה בלי להסתיר אותה. הרוחב נשאר קבוע ואחיד, ושתי תצוגות הרשימה נשארו זהות -
כלומר שני גבהים בלבד בכל המערכת, ולא גובה שמשתנה עם התוכן.

**QA:** e2e 6/6 - הבדיקה מודדת ומאמתת: רוחב זהה בכל המצבים, הפרש של 66 פיקסלים
בין מונה לרשימה, ושתי תצוגות הרשימה באותו גודל בדיוק.

---

## 2026-08-05 - "נמצא בעמדה" הוא מצב אמיתי: מי מחזיק את הפ"מ עכשיו

**הבקשה:** "כל גרירה של פ"מ לדסק או חיבור לאזור יוסיפו את שם העמדה שעשתה את זה
לשדה 'נמצא בעמדה'. יכול להיות בכמה עמדות. ברגע שהפ"מ לא בדסק (בנקודת העברה או
חזר לחלון פ"מים) - להסיר."

עד עכשיו השדה קרא את `workstation_preset_name` - **עמדה אחת**, והיא נגזרת מ-
`strips.workstation_preset_id` שנושאת גם **יעד העברה**. כלומר פ"מ שממתין בנקודת
העברה נראה כאילו הוא כבר "נמצא" אצל המקבל, ופ"מ שיושב בדסקים של שתי עמדות נראה
כאילו הוא רק באחת.

| מקור | מה הוא אומר |
|---|---|
| `strip_table_assignments` | הפ"מ נגרר לדסק של העמדה. כבר היה קיים, רב-עמדתי, ונמחק ביציאה מהדסק |
| `strip_zone_assignments.preset_id` | **חדש** - העמדה שחיברה את הפ"מ לאזור. הטבלה לא ידעה מי עשה זאת |

`GET /strips/global` מחזיר `at_preset_names` - איחוד השניים, וממנו השאילתא עובדת.
ההסרה מגיעה בחינם: היא כבר מוחקת את שורות השיוך.

**עדכון עמדה משתמש ב-COALESCE** ולא בדריסה: עדכון שמגיע ממסלול שלא מוסר עמדה
(עריכת הערה על האזור, למשל) לא ימחק את מי שמחזיק את הפ"מ.

**QA:** 1124/1124 unit (3 חדשות לריבוי עמדות) · build · e2e 6/6 - הבדיקה החדשה
מריצה את מחזור החיים מול ה-DB: פ"מ חדש (ריק) ← דסק א' ← + דסק ב' ← גריעת א' ←
גריעת ב' (ריק) ← חיבור לאזור ← ניתוק. תוך כדי התברר שחלק האזור **דולג בשקט**
כי `/map-zones` דורש `map_id` - תוקן כדי שהבדיקה באמת תבדוק אותו.

---

## 2026-08-05 - שורת הפ"מ בחלון: או"ק/טייסת (מצבה), וגודל חלון קבוע

**הבקשה:** "פה להציג או"ק/טייסת (מספר מטוסים). אם זה מפוצל אז או"ק+מספר במבנה /
טייסת. את הגודל של החלונות שיהיה קבוע."

| | |
|---|---|
| `dwStripLabel` | פ"מ רגיל: `כסף/117 (2)` · פ"מ **מפוצל**: `כסף2/117` - המספר במבנה יושב על האו"ק, ובלי ספירה (המספר במבנה כבר אומר על אילו מטוסים מדובר) |
| גודל קבוע | 230px רוחב וגוף בגובה קבוע ל**כל** חלון ב**כל** מצב תצוגה. מעבר בין מספר / או"קים / פ"מים כבר לא משנה את הגודל, ולוח החלונות נשאר יציב - הרשימות גוללות בפנים |

**QA:** 1104/1104 unit (7 חדשות ל-`dwStripLabel`) · build · e2e 5/5 - הבדיקה
מודדת את גודל החלון בשלושת המצבים ומוודאת שהוא זהה, ומאמתת את מבנה השורה.

---

## 2026-08-05 - חלון הנתונים בעמדה: הכפתורים לא נלחצו, ואי אפשר היה לראות את הפ"מים

**הדיווח:** "הנקודה בעמדה עצמה לא ניתן לפתוח עדכון שאילתא או להרחיב להציג את הפ"מים."

### השורש: pointer capture בלע את הלחיצות

הכותרת של החלון היא ידית הגרירה, ולכן `onPointerDown` שלה קורא ל-`setPointerCapture`.
הכפתורים (הרחבה, הסתרה) הם **ילדים** של הכותרת - האירוע בועבע אליה, הכותרת תפסה את
המצביע, ו-`click` על הכפתור **לא נוצר כלל**. כלומר לא "חסר פיצ'ר": שום כפתור בחלון
לא היה לחיץ. התיקון: `pointerdown` שמקורו בכפתור אינו מתחיל גרירה.

זה גם מסביר למה זה לא נתפס קודם - ה-e2e בדק שהחלון **מרונדר** עם הספירה הנכונה,
לא שאפשר להפעיל אותו. הבדיקה החדשה לוחצת על הכפתורים בפועל.

### מה נוסף

| | |
|---|---|
| מצב תצוגה שלישי `count_strips` | שורת פ"מ: או"ק · מצבה · גובה/משימה · דקות לנחיתה. שורה ולא כרטיס מלא, כי החלון צף מעל המפה |
| דפדוף מחזורי | מספר ← או"קים ← פ"מים ← מספר (`dwNextMode`) |
| עריכת שאילתא מהעמדה | ✎ פותח את `QueryBuilder` במודל. השינוי נשמר **בסשן בלבד**, החלון מסומן ✎, וכפתור מחזיר להגדרת העמדה |
| דריסת שאילתא **מוצהרת** | רשומת הסשן היא העתק מלא של החלון, ולכן קיום `query` בה אינו מעיד על דריסה. בלי דגל `edited` מפורש, כל **הזזה** של חלון הייתה מקפיאה את שאילתת המנהל - שינוי הגדרה בניהול לא היה מגיע לפקחים שהזיזו את החלון |
| המודל ב-portal ל-body | מחוץ ל-`#root` אין `zoom`, ולכן הוא מוחזר ידנית ויחידות ה-vw/vh מחולקות ב---s (מלכודת מוכרת, ראה `/ui-adapt`) |

**QA:** 1097/1097 unit · build · e2e 5/5 - הבדיקה החדשה מדפדפת בין מצבי התצוגה,
מוודאת שהמשימה והדקות לנחיתה מופיעות בשורת הפ"מ, ופותחת את עורך השאילתא מהעמדה.

---

## 2026-08-05 - "נמצא בעמדה" בשאילתא: בחירה מתפריט, לא הקלדת שם

**הבקשה:** "בשאילתא (גם שאילתא כללית) להוסיף נמצא בעמדה, ואז צריך תפריט עמדות."

עד עכשיו הדרך היחידה לסנן לפי עמדה מחזיקה הייתה השדה `הועבר לעמדה` עם **הקלדת
שם חופשית** - שנשברת בכל שינוי שם עמדה, ולא אומרת למשתמש אילו עמדות בכלל קיימות.

| שינוי | פירוט |
|---|---|
| שדה `at_preset` - **נמצא בעמדה** | `ftype: 'preset_select'` - אותה רשימת סימון שכבר שימשה את "נוצר ע\"י עמדה" |
| נפילה-לאחור למזהה | פ"מ שמגיע בלי `workstation_preset_name` נפתר דרך `ctx.presetNamesById`, אחרת התנאי היה יוצא ריק בלי שום סימן |
| `לא אחד מ` על שדות עמדה | בורר האופרטור היה **מוסתר** לשדות עמדה, ולכן `not_in` לא היה נגיש כלל |
| רשימת העמדות משותפת | `usePresetNames` טוען אותה פעם אחת ומשתף בין כל בוני השאילתות, במקום לחווט prop דרך כל מסך. ההגדרה שנמסרת כ-prop עדיין גוברת |
| אופרטור שמור שאינו ברשימת הסוג | מוצג כמו שהוא במקום בורר ריק - שאילתא ותיקה ממשיכה להראות מה היא באמת עושה |

`הועבר לעמדה` נשאר (סומן "טקסט") כדי לא לשבור שאילתות שמורות.

**QA:** 1085/1085 unit (7 חדשות ל"נמצא בעמדה") · build · e2e 4/4 - כולל בדיקה
שפותחת את עורך העמדה בניהול, בוחרת את השדה ומוודאת שמופיעה רשימת עמדות לסימון.

---

## 2026-08-05 - חלונות נתונים בעמדת שדה: "מגדיר בשאילתא מה אני רוצה לראות"

**הבקשה:** "רוצה לשים סטטיסטיקות בשדות תעופה - אגדיר בעמדה אילו חלונות של נתונים
רוצה לראות. דוגמה: מסוקים שאמורים לנחות אצלי, באוויר, ונמצאים בעמדת 'אזורי מסוקים'.
דוגמה נוספת: מטוסי קרב מהבסיס שלי, באוויר, זמן נחיתה משוער פחות מ-X דקות מעכשיו."

### מה **לא** נבנה מחדש

מנוע השאילתות כבר קיים ומשמש את סינון הפ"מים ואת תאי חלון הפ"מים
([queryBuilder.ts](src/utils/queryBuilder.ts) + [QueryBuilder.tsx](src/components/query/QueryBuilder.tsx)).
החלונות נשענים עליו כמו שהוא - אין בונה שאילתות שני ואין מנוע סינון שני.

### א. שדות זמן בשאילתא ([queryBuilder.ts](src/utils/queryBuilder.ts))

`takeoff_time` היה מוגדר כשדה **טקסט**, כלומר `gt`/`lt` עליו הריצו
`parseFloat('2026-08-05T…')` = **2026**. השוואה חסרת משמעות. עכשיו יש `ftype: 'time'`,
וההשוואה עליו היא **בדקות מעכשיו**: `פחות מ` · `יותר מ` · `שווה ל` · `לא שווה ל` · `כבר עבר`.
אופרטור טקסט על שדה זמן (`מכיל`) עדיין עובד - שאילתות ותיקות לא נשברו.

| החלטה | למה |
|---|---|
| `ctx.now` נמסר מבחוץ | "פחות מ-15 דקות" נכון רק ביחס לרגע החישוב. השכבה מרעננת כל 15 שניות, והבדיקות שולטות בזמן ולא תלויות בשעון ההרצה |
| נחיתה שזמנה **עבר** נכללת ב"פחות מ-X" | פ"מ מאחר שעדיין באוויר הוא בדיוק מי שצריך להישאר מול העיניים, לא להיעלם מהחלון |
| פ"מ **בלי** זמן לא מתאים לאף תנאי זמן - גם לא ל"לא שווה" | "אין נתון" אינו "לא מתקיים". אחרת כל פ"מ בלי ETA היה מציף את החלון |
| `נוחת אצלי` / `ממריא אצלי` נגזרים מ-`parent_base_id` של העמדה | אותה הגדרת חלון עובדת בכל שדה בלי לשכפל אותה לכל בסיס |

### ב. זמן נחיתה מתוכנן - שדה חדש (`strips.planned_landing_time`)

**לא היה כזה במערכת.** מה שכן היה - `strip_transfers.eta_minutes` - הוא ספירה לאחור
**לנקודת העברה**, לכל העברה בנפרד, ולא לנחיתה. השדה החדש הוא `TIMESTAMPTZ` (לא שעה
כטקסט), עובר בכל מסלולי הפ"מ: יצירה, עדכון, ייבוא אקסל, פיצול מלא ופיצול חלקי,
`/strips/global`, כרטיסי ההעברה ו-`waiting-strips`. בכרטיס הפ"מ הוא נערך לצד זמן
ההמראה - **אותו רכיב** (`StripTimeField`), במקום שכפול הבלוק.

### ג. החלונות עצמם

- **הגדרה בעמדה** (`workstation_presets.data_windows`, JSONB) - מסך הניהול, עמדת שדה.
- **שינוי בסשן** - הפקח מזיז/מסתיר/מרחיב, נשמר ב-`sessionStorage` בלבד. חלון שהמנהל
  מחק נעלם גם אם נשאר בסשן, אחרת שארית סשן הייתה מחזירה לחיים חלון שהוסר בכוונה.
- **תצוגה** - מספר בלבד כברירת מחדל, מתג להרחבה למספר + או"קים.
- **גרירה** - `setPointerCapture` + `touchAction:none` (עט Cintiq), והקואורדינטה
  מחולקת ב---s: `clientX` בפיקסלים לא-מוגדלים מול `left/top` תחת `zoom` - בלי החלוקה
  החלון בורח מהעט ב-18"/24".

### QA

| בדיקה | תוצאה |
|---|---|
| `tsc --noEmit` | נקי |
| `vitest run` | 1040/1040 (26 ב-queryBuilder כולל 11 חדשות, 16 ב-dataWindows) |
| `vite build` | עבר |
| מיגרציה מול Neon בפועל | העמודה קיימת ב-8 הסכמות (public + 7 סביבות), `timestamp with time zone` |
| כתיבה/קריאה אמיתית | 9 דקות נכתבו, 9.0 חזרו - בלי הסטת אזור זמן |
| `e2e/data-windows.spec.ts` | 3/3 - API (יצירה/קריאה/עדכון/ניקוי), שמירת הגדרה בעמדה, ורינדור החלון מעל מפת השדה עם הספירה הנכונה |

---

## 2026-08-05 - הסנכרון בין מסלולים מקושרים עבר מ"העתקה בכתיבה" ל"פתרון בקריאה". ו-GRF נכנס פנימה

**הבקשה:** "תמליץ איך לעשות את הסנכרון מסלולים, NOTAM ו-GRF במסלולים מקושרים. **כי אם לא איפסתי את זה אז זה לא מתעדכן**" - ואחריה: "צריך לוודא שאם מקשרים מסלולים חדשים משדה אחר, גם החדשים מאותחלים."

**מה נמצא (האבחנה של הפקח הייתה מדויקת):** הסנכרון שנבנה קודם באותו יום היה **copy-on-write** - הוא רץ רק ברגע הכתיבה. לכן:
- מידע שכבר היה במסלול **לפני** שהקישור נוצר לא זז לעולם. הדרך היחידה הייתה למחוק ולהזין מחדש - בדיוק ה"איפוס" שדווח.
- קישור חדש לקבוצה קיימת נולד **ריק**.
- ביטול קישור השאיר עותקים שאיש לא ידע שהם עותקים.
- **GRF לא היה מסונכרן בכלל.**

**מה נעשה:** אין יותר עותקים. השורה נשמרת היכן שנכתבה, וכל **קריאה** מרכיבה את מצב הקבוצה וממפה אותו לשדה ששואל ([runwayState.js](server/utils/runwayState.js)).

| | לפני | אחרי |
|---|---|---|
| מתי הסנכרון קורה | בכתיבה | בקריאה |
| מידע קיים + קישור חדש | לא זז | מופיע **מיד** |
| ביטול קישור | עותקים נשארים | מפריד מיד |
| GRF | לא מסונכרן | מסונכרן, הדיווח האחרון לכל קצה |
| מחיקה בצד אחד | מחיקת כל העותקים לפי `link_uid` | יש רק שורה אחת - נמחקת לשניהם |

**מה שומר על זה שלא יישבר:**
- `id` המקורי חוזר בקריאה, ולכן PUT/DELETE של הלקוח עובדים בלי שינוי וחלים על שני הצדדים; `runway_id` ממופה למקומי, ולכן הלקוח (שמפתח הכל לפי `runway_id`) לא נגע כלל.
- שדות `source_airfield_name`/`is_linked` בכל שורה - הפקח רואה **מי** סגר את המסלול שלו ולא מנחש.
- **בדיקת שומר** ([runwayStateGuard.test.js](server/utils/runwayStateGuard.test.js)): נכשלת על `SELECT ... FROM` ישיר מטבלאות המצב בקובץ ראוט - קריאה כזו מפספסת **בשקט** סגירה שנרשמה בשדה המקושר. כתיבה מותרת (היא תמיד מקומית). 24 קבצי ראוט נסרקים.
- מיגרציה חד-פעמית שמוחקת את העותקים שהמימוש הקודם יצר (לפי `link_uid`), אחרת האיחוד היה מציג אותם פעמיים.

**גם [base.js](server/routes/base.js) עבר**: הצובר של NOTAMים בסטטוס בסיס היה `JOIN` ישיר, כלומר סגירה משדה מקושר לא הופיעה שם.

**QA:** 1162/1162 בדיקות יחידה ✅ (16 חדשות לכללי ההכרעה + שומר) · tsc ובילד נקיים ✅ · **8/8 e2e** ([linked-runway-sync](e2e/linked-runway-sync.spec.ts)), ובהן שתי הבדיקות שמשחזרות בדיוק את מה שדווח: **"מידע שנרשם לפני הקישור מופיע בצד השני מיד עם יצירת הקישור, בלי לגעת בו"** ו**"צירוף שדה שלישי לקבוצה קיימת - הוא מקבל את המצב מיד"**.

---

## 2026-08-05 - מסלול מקושר = אותו מסלול פיזי: סגירה, תאורות והמסלולים שבשימוש מסונכרנים. וכיוון אחד למסלול

**שתי בקשות שנוגעות באותו מקום:**
1. "ברגע שקישרתי מסלולים זה משפיע בהתאמה - תאורות, סגירת מסלול, הכל מסונכרן - גם טבלת מסלולי המראה/נחיתה."
2. "אל תאפשר המראות ונחיתות על מסלולים מנוגדים - ברגע שלחצתי על מסלול מסוים להמראה/נחיתה, המסלול ההפוך הופך לכתום. כשלחצתי על הכתום הוא הופך ירוק והשני כתום."

### א. כיוון אחד למסלול ([runwayEnds.ts](src/utils/runwayEnds.ts))

הפאנל "מסלולים בשימוש" החזיק **שתי רשימות חופשיות** (המראה, נחיתה) ולכן איפשר לסמן גם 15L וגם 33R - שני כיוונים מנוגדים על אותו אספלט. עכשיו: כיוון אחד למסלול, **חוצה את שתי השורות**. הקצה הנגדי מסומן **כתום** (לא אפור-כבוי, כי זו לא בחירה פנויה; ולא אדום-חסום, כי הלחיצה **מותרת**), ולחיצה עליו **מחליפה כיוון** - זו הפעולה התכופה בשדה, לא שגיאה שצריך לחסום.

### ב. מצב משותף למסלולים מקושרים ([linkedRunways.js](server/utils/linkedRunways.js))

הגשר קיים מאתמול: לכל מסלול המראה יש **מסלול ראי** ב"מסלולי הסעה", והקישור הוא בין מסלולי ההסעה. לכן `מסלול -> הראי שלו -> קבוצת הקישור -> הראי השכן -> המסלול שלו`.

| מה מסונכרן | איך |
|---|---|
| **סגירה / קיצור** (`runway_notams`) | העותקים נושאים `link_uid` משותף - עדכון ומחיקה חלים על כולם, אחרת "פתיחת המסלול" בצד אחד הייתה משאירה אותו סגור בצד השני |
| **תאורות** (`runway_lighting`) | upsert לכל המקושרים - אותן נורות פיזיות |
| **מסלולים בשימוש** (`runway_end_use`, **טבלה חדשה**) | היה **מצב סשן בלקוח בלבד**: לא נשמר, לא נראה בעמדה שכנה, וממילא לא היה מה לסנכרן. עכשיו מצב של השדה, שנטען בפולינג כמו הסגירות |

**המלכודת שהוכתבה מהנתונים:** שמות הקצוות שונים בין השדות ('15L' מול '15'), ולעיתים גם **הסדר הפוך** (`heading_a='18'` אצל אחד, `'36'` אצל השני - קיים בפועל ב-DB בין 'בחא 8 אווירי' ל'בחא 8 - הקפה'). התאמה לפי מיקום הייתה סוגרת/מפעילה את **הקצה ההפוך**, ולכן `matchEndName` מתאים לפי **המספר** (ואות הצד L/R/C אינה חלק מההשוואה). ב-NOTAM של קיצור, ששמור לפי מיקום ('a'/'b'), `matchEndSlot` ממיר - וכשאין התאמה העותק **אינו נוצר**: עדיף בלי קיצור מאשר קיצור בקצה ההפוך.

**שינוי התנהגות שיש לדעת עליו:** המסלולים שבשימוש הפכו למצב **משותף** - שתי עמדות של אותו שדה רואות עכשיו את אותה טבלה (לפני כן לכל עמדה הייתה תצוגה משלה). זו המשמעות של "מצב השדה", וזה תנאי לסנכרון בין שדות.

**QA:** 1012/1012 בדיקות יחידה ✅ (15 ב-`runwayEnds.test.ts` + 13 ב-`linkedRunways.test.js`, TDD) · tsc נקי ✅ · build נקי ✅ · **e2e:** [linked-runway-sync](e2e/linked-runway-sync.spec.ts) 5/5 - סגירה ופתיחה בשני הצדדים, קיצור שעובר לקצה הנכון כשהסדר הפוך, תאורות, מסלול בשימוש עם התאמת שם והורדת הכיוון הנגדי, **ובלי קישור אין סנכרון** · [runway-direction](e2e/runway-direction.spec.ts) 1/1 בממשק אמיתי · 19/19 בכל ספקי השדה יחד ✅ · המיגרציה הורצה מול Neon.

---

## 2026-08-05 - שדה סכמטי (בלי מפת רקע) לא נטען בעמדה: אין תמונה -> אין גבולות -> אין שכבות

**הבקשה:** "אם רק ציירתי מפה סכמטית בלי מפת רקע הוא לא טוען אותה. ראה 'תל נוף הקפה'."

**מה נמצא:** כל שכבה ב-[GroundView](src/components/views/GroundView.tsx) ממוקמת לפי `imgBounds` - גבולות **התמונה המרונדרת** (פיצוי על letterboxing של `object-fit: contain`). `updateImgBounds` החזירה `null` כשאין `img.naturalWidth`, וכל שכבה מותנית ב-`imgBounds &&` - ולכן שדה בלי מפת רקע רינדר **הודעת "אין מפה מוגדרת" בלבד**, למרות שהמסלולים, ההקפות והאלמנטים קיימים ב-DB. בפועל: עמדת **"תל נוף הקפה"** (`preset 197`, tower) יושבת על שדה `בחא 8 - הקפה` עם `map_id = NULL`, 4 מסלולים ו-6 הקפות - ומסך ריק.

**למה זה לא נתפס קודם:** בעמדת הניהול **כן** יש משטח לשדה בלי מפה (`airfield-blank-canvas`, 4:3) - אחרת אי אפשר היה לצייר מלכתחילה. הצד שמציג נשאר מאחור, וזה נראה כמו "לא נשמר" ולא כמו "לא מרונדר".

**מה נעשה:** [schematicCanvas.ts](src/utils/schematicCanvas.ts) - קבוע יחס אחד (`SCHEMATIC_ASPECT` = 4:3) ונוסחת `contain` אחת לשני הצדדים. הקואורדינטות נשמרות ב**אחוזים**, ולכן יחס שונה בין הציור לתצוגה היה מעוות את השרטוט - ולכן גם עמדת הניהול עברה לצרוך את אותו קבוע במקום `'4 / 3'` מוטבע. ב-GroundView: `updateImgBounds` נופלת ליחס הסכמטי כשאין תמונה, ה-`ResizeObserver` צופה ב**מכולה** ולא בתמונה (בלי מפה אין `img` שיימדד), ומרונדר משטח רשת באותו מקום. ההודעה "אין מפה מוגדרת" נשארה - אבל רק כשאין **גם** מפה וגם שרטוט (`hasSchematicContent`).

**QA:** 984/984 בדיקות יחידה ✅ (7 חדשות ב-`schematicCanvas.test.ts`, TDD) · tsc נקי ✅ · build נקי ✅ · **e2e חדשה** ([schematic-airfield.spec.ts](e2e/schematic-airfield.spec.ts)): שדה בלי `map_id` + עמדת מגדל עליו -> המשטח מרונדר, יחסו 4:3, שכבת המסלולים מעליו בשטח אמיתי, וההודעה "אין מפה מוגדרת" אינה מוצגת ✅ · אומת גם בצילום מסך מהאפליקציה: המסלול 31/13 משורטט על הרשת.

---

## 2026-08-05 - מסלול המראה נכנס אוטומטית ל"מסלולי הסעה", ונעול לעריכה שם

**הבקשה:** "בניהול שדה תעופה - כשיצרתי מסלול במסלולים, זה צריך להיכנס אוטומטית לקבוצת 'מסלולי הסעה' ולמלא את הנתונים אוטומטית ולכתוב בהערה שזה הגיע בעקבות יישות 'מסלולים'. לא ניתן במסלולי הסעה לעדכן מסלולים אלו, רק מסלולים שנוצרו שם."

**מה נמצא:** מסלול המראה מוגדר **פעמיים**: ביישות **"מסלולים"** (`airfield_runways` - כיוונים, אורך, מרחקי הכרזה) וב**"מסלולי הסעה"** (`airfield_routes` - השרטוט על המפה, שאליו נקשרים קישורי מסלולים, `active-takeoffs` ו-`runway-conflict`). שתי ההגדרות היו ידניות ובלתי תלויות, ולכן יכלו לסתור זו את זו בשקט: שם או קצה שהשתנו במקום אחד נשארו ישנים בשני.

**מה נעשה:** ההגדרה השנייה היא מעכשיו **ראי** של הראשונה.

| שכבה | שינוי |
|---|---|
| [runwayRoute.js](server/utils/runwayRoute.js) *(חדש)* | גזירת כל שדות המסלול ממסלול ההמראה: שם (או הקצוות כשאין), `is_runway`, קצוות, שרטוט מ-`start/end_*_pct`, קטגוריה, צבע אחיד, והערת מקור. `matchesRunway` **מאמץ** מסלול קיים במקום ליצור כפילות |
| [airfield.js](server/routes/airfield.js) | POST/PUT של מסלול מסנכרנים את הראי **באותה טרנזקציה**; PUT/DELETE של מסלול ראי מוחזרים **409 `route_from_runway`**; שכפול שדה מסב את `source_runway_id` למסלול של העותק |
| [init.js](server/db/init.js) | `airfield_routes.source_runway_id` (FK CASCADE) + אינדקס חלקי, והשלמה אידמפוטנטית לכל המסלולים הקיימים בעליית השרת |
| [ManagementPage.tsx](src/components/admin/ManagementPage.tsx) | שורת ראי מציגה 🔒 "ממסלולים" **במקום** כל כפתורי הפעולה, וגרירת המסלול במפה מבוטלת עבורה |

**החלטות שנשענות על הנתונים:**
- **אימוץ ולא כפילות.** בשדות הקיימים כבר היו מסלולי הסעה שמייצגים מסלולי המראה. ההשלמה מאמצת מסלול מתאים (אותו שם, או **שני** הקצוות - קצה בודד תואם גם בין מסלולים שונים) ורק אחרת יוצרת. בפועל: **8 נוצרו, 1 אומץ**, אפס כפילויות.
- **השרטוט נדרס רק כשליישות יש קואורדינטות.** מסלול המראה שהוגדר בלי מיקום על המפה לא ימחק שרטוט שכבר צויר ידנית.
- **אין כפתור שנחסם.** כפתור שמחזיר שגיאה הוא הבטחה שבורה; השורה הנעולה פשוט אינה מציגה פעולות, וה-409 בשרת הוא הגבול האמיתי.
- **`source_runway_id` בשכפול שדה.** מסלולי ההסעה מועתקים **לפני** שמסלולי ההמראה של העותק נוצרים, ולכן הם הצביעו על מסלול השדה **המקורי** - ומחיקה שם הייתה מוחקת מסלול בעותק. המיפוי מוסב אחרי לולאת המסלולים. מכוסה בבדיקה.

**QA:** 977/977 בדיקות יחידה ✅ (17 חדשות ב-`runwayRoute.test.js`, TDD - נפלו לפני, עברו אחרי) · tsc נקי ✅ · build נקי ✅ · **7/7 e2e חדשות** ([runway-route-mirror.spec.ts](e2e/runway-route-mirror.spec.ts)): יצירה ממלאת את כל השדות + הערת מקור, עדכון מתגלגל, עריכה ומחיקה נחסמות (וגם **הנתון לא זז**), מסלול שנוצר ידנית **כן** נערך, מחיקת המסלול מוחקת את הראי, שכפול שדה מצביע נכון, והשורה בממשק נעולה ובלי כפתורים · 5/5 e2e קיימות ✅ · ההגירה הורצה מול Neon.

---

## 2026-08-04 - קישורי מסלולים: הקישור הוא בין **שדות תעופה**, לא בין עמדות

**הבקשה:** "בניהול - בבניית שדה תעופה - קישורי מסלולים זה בין שדות תעופה ולא בין עמדות. צריך להחליף את השדה."

**מה נמצא:** חבר בקישור היה `(preset_id, route_id)` - עמדה + מסלול. זו טעות באפיון ולא רק בתווית: מסלול שייך ל**שדה** (`airfield_routes.airfield_id`), ועמדה רק **רואה** אותו דרך השדה שלה. מהצמדת העמדה נבע קישור חלקי מסוג שקט - עמדה אחת בשדה "מקושרת" ושכנתה באותו שדה לא - וגם אפשרות לרשום עמדה של שדה א' מול מסלול של שדה ב'. בפועל הנתון היחיד שהמערכת צורכת הוא `route_id` (`GET /api/active-takeoffs` מצטרף לקבוצה לפי מסלול בלבד; `preset_id` לא נקרא בשום מקום).

**מה נעשה:** החבר הוא **מסלול**, והשדה **נגזר ממנו** ואינו נשמר בנפרד - כך אין שדה שסותר את המסלול.

| שכבה | שינוי |
|---|---|
| [routeLinks.ts](src/utils/routeLinks.ts) | `LinkMember` = `route_id` (+ `airfield_id`/`airfield_name` לתצוגה בלבד), מפתח הכפילות עבר ל-`route_id`, `groupSummary` מונה שדות במקום עמדות |
| [RouteLinksSection.tsx](src/components/admin/RouteLinksSection.tsx) | הבורר הראשון הוא **שדה תעופה** (`data-testid="route-link-airfield"`) והשני מציג רק את מסלולי אותו שדה. שם השדה בשבב מחושב מהמסלול, לא ממה שנשמר |
| [airfield.js](server/routes/airfield.js) | `loadLinkGroups` מצטרף ל-`airfields` דרך המסלול ומחזיר `airfield_name`; `replaceMembers` כותב `route_id` בלבד; ולידציה = **שני מסלולים שונים**; פילטר `?preset_id=` הוסר (לא היו לו צרכנים) |
| [init.js](server/db/init.js) | `preset_id` → nullable, דדופ לפי `(group_id, route_id)`, `UNIQUE(group_id, route_id)` במקום השלשה, וה-FK על `preset_id` מוסר |
| [foreign-keys.js](server/db/foreign-keys.js) | הרשומה הוסרה **עם נימוק**: ב-CASCADE מחיקת עמדה הייתה מוחקת חברים בקישור ושוברת אותו בשקט. העמודה נשארת כהיסטוריה |
| [links.json](src/i18n/registry/links.json) | `selectStation`→`selectAirfield`, `noRoutesForStation`→`noRoutesForAirfield`, `{{presets}}`→`{{airfields}}`, וכל הטקסטים (he+en) |

**המלכודת שנתפסה באימות:** המיגרציה רצה בהתחלה על `public` בלבד - ו-`env_21` (סביבת תרגול קיימת) נשארה עם `preset_id NOT NULL`. הסנכרון ב-[envs.js](server/db/envs.js) רק **מוסיף** טבלאות/עמודות/FK; הוא אינו יודע להסיר `NOT NULL` או להחליף אילוץ ייחודי. כל שמירת קישור בתרגול הייתה נופלת. המיגרציה עודכנה ללולאה על `public` + כל סכמות `env_*`.

**עוד תיקון שנדרש בדרך:** [admin-airfield-fixes.spec.ts](e2e/admin-airfield-fixes.spec.ts) הקימה נתונים דרך `request` **בלי אסימון** - מאז שכבת האימות (SK-01) כל הקריאות חזרו 401, ההקמה נכשלה בשקט וכל 5 הבדיקות נפלו (גם לפני השינוי הזה). נוסף [`apiAuthHeaders()`](e2e/helpers.ts) שמזדהה דרך אותו שער כמו המשתמש (`/api/auth/mirage-login`), והספק עובר ל-`APIRequestContext` מאומת.

**QA:** 960/960 בדיקות יחידה ✅ (23 ב-`routeLinks.test.ts` נכתבו מחדש למודל השדות, TDD - נפלו לפני, עברו אחרי) · tsc נקי ✅ · build נקי ✅ · **5/5 e2e** ✅ כולל קישור שנוצר דרך ה-UI בין **שלושה שדות** ובדיקה שהבורר מציע שדות ולא עמדות · המיגרציה הורצה מול Neon: `public` + `env_21` שתיהן `preset_id` nullable, `uq_route_link_members_group_route` קיים, 0 FK על `preset_id` ✅ · אומת מראש ש-0 שורות נמחקות בדדופ ו-0 קבוצות יורדות מתחת לשני מסלולים ✅ · הקבוצה הקיימת ב-DB מוצגת אחרי השינוי כ-"בחא 8 קרקעי / 33" ↔ "בחא 8 אווירי / 33" - כלומר הנתונים ממילא כבר קישרו בין שדות, והעמדה הייתה רעש.

---

## 2026-08-04 - ה-CSP הפיל ארבעה חלקים בייצור. הכלל: `default-src` הוא ברירת המחדל של כל הנחיה שלא נכתבה

**הבקשה:** ארבע תקלות שדווחו יחד, **כולן רק בייצור, ב-dev הכל תקין** - (1) המפה בהגדרת שדה תעופה מוצגת מכווצת/שבורה (2) בעמדת השדה המפה לא נטענת והטוגלים בסרגל התצוגה לא מגיבים ל-ON/OFF (3) עמדות נוספות שמוצגות לא נטענות (4) אייקוני מסך הטעינה לא מסתובבים.

**מה נמצא:** מקור אחד לכל הארבע - מדיניות ה-CSP שנוספה יום קודם ב-[42a06d5](../../commit/42a06d5). היא הכילה `default-src 'self'` **בלבד**, מתוך הנחה שהשמטת `script-src`/`img-src`/`style-src` משאירה אותם פתוחים. ההפך הוא הנכון: **הנחיה שלא נכתבה יורשת את `default-src`**. ההערה שנשארה בקובץ אף תיעדה את ההנחיה השגויה במפורש ("script-src מלא היה מפיל את סקריפט ה-boot") - ההשמטה לא הצילה אותו, היא רק הסתירה את הסיבה.

מה נחסם בפועל, ומה המפעיל ראה:

| הנחיה שירשה `'self'` | מה נחסם | הסימפטום |
|---|---|---|
| `img-src` | תמונות `data:` | `maps.image_data` נשמר ב-DB כ-data URL. התמונה נשברה בהגדרת השדה (#1); בעמדת השדה `onLoad` לא נורה, `imgBounds` נשאר `null`, וכל רובד ב-[GroundView.tsx:2255+](src/components/views/GroundView.tsx#L2255) מותנה בו - **לכן הטוגלים "לא הגיבו"** (#2) |
| `style-src` | תגי `<style>` מוטבעים | ה-keyframes של [RotatingEmblems.tsx:112](src/components/shared/RotatingEmblems.tsx#L112) לא הוחלו - האייקונים לא הסתובבו (#4). אותו דבר ל-GroundView (הבהוב אלמנטים, טבעת קונפליקט) ול-ClockWidget |
| `frame-ancestors 'none'` | מסגור **גם מאותו מקור** | [StationPeekBar.tsx:286](src/components/shared/StationPeekBar.tsx#L286) מציג כל עמדה נצפית כ-iframe של האפליקציה עצמה (`/?peek=<id>`) - הסרגל נשאר ריק (#3) |
| `script-src` | הסקריפט המוטבע ב-`index.html` | `--s` נשאר על 1 ו-`data-screen` לא נקבע - כל עמדת 18"/24" נפתחה בסקייל של 15.6" |
| `script-src` | `public/driver.html` | סקריפט מוטבע בן ~980 שורות + 23 מטפלי `onclick` - **אפליקציית הנהג עלתה ריקה**. לא דווח, כנראה לא נבדקה מאז |

**למה ב-dev זה לא נראה:** ב-dev הלקוח מוגש מ-vite ([server/app.js:145](server/app.js#L145) מפנה ל-:5000), ולכן **אינו עובר ב-middleware הזה כלל**. מדיניות ה-CSP קיימת רק בנתיב שאף אחד לא בודק בו יום-יום.

**מה נעשה:**
1. **סקריפט ה-boot הוצא ל-[public/boot.js](public/boot.js)** ו-`index.html` טוען אותו כקובץ. כך `script-src 'self'` נשאר **בלי `'unsafe-inline'`** - זו ההנחיה שנושאת את כל ערך ה-CSP מול XSS, ולא רצינו לפתוח אותה כדי לתקן תצוגה.
2. **[securityHeaders.js](server/middleware/securityHeaders.js) נכתב מחדש** עם הנחיה מפורשת לכל סוג משאב, וכל אחת נושאת בהערה את יכולת המוצר שהיא מאפשרת: `img-src data: blob:` · `style-src 'unsafe-inline'` (הלקוח מסגנן ב-`style={{...}}` במאות מופעים - אין לזה מסלול אחר) · `worker-src blob:` (tesseract) · `'wasm-unsafe-eval'` (pdfjs/tesseract) · `frame-src` פתוח (מצלמות בכתובת שהמפעיל מגדיר) · `frame-ancestors 'self'` + `X-Frame-Options: SAMEORIGIN`.
3. **`/driver` מקבל CSP מקל משלו** (`DRIVER_CSP`, [driver.js:18](server/routes/driver.js#L18)) במקום לרפקטר 980 שורות ו-23 מטפלים בדף שאי אפשר לבדוק בלי GPS ומכשיר. הנזק תחום - הדף אינו קורא מידע עמדות ואסימון הנהג מוגבל לנתיבי `/api/driver`. **חוב פתוח:** חילוץ הסקריפט ומעבר ל-`addEventListener`.

**QA:** אומת בדפדפן אמיתי (Playwright) מול `dist` שנבנה, עם אותו middleware של הייצור - לפני: `script-src-elem inline` + `img-src data` + `style-src-elem inline` נחסמו; אחרי: **0 הפרות CSP**, תמונת `data:` נטענת ✅ · `<style>` מוחל ✅ · `data-screen` נקבע ✅ · iframe מאותו מקור נטען ✅ · הסקריפט של דף הנהג רץ ✅. בנוסף: 960/960 בדיקות יחידה ✅ · tsc נקי ✅ · build נקי ✅ · vite ב-dev מגיש את `/boot.js` (200) ✅.

**נוספה [securityHeaders.test.js](server/middleware/securityHeaders.test.js)** - 12 בדיקות שקושרות כל הנחיה ליכולת המוצר שהיא מחזיקה, כדי שהצמצום הבא ייפול בבדיקה ולא אצל המפעיל. כולל הצד השני: `script-src` בעמדה **חייב** להישאר בלי `'unsafe-inline'`.

**הלקח:** CSP הוא הקוד היחיד שרץ רק בייצור. שינוי בו מחייב בדיקה בבילד פרודקשן עם קונסולה פתוחה - `npm run dev` לא ייגע בו.

---

## 2026-08-04 - MIRAGE_SERVICE_TOKEN: ההתחברות הייתה שבורה מאחורי שני health ירוקים

**הבקשה:** "MIRAGE_SERVICE_TOKEN במיראז - מה אני צריך להגדיר ואיפה".

**מה נמצא:** המשתנה לא היה מוגדר ב**אף אחד** משני השירותים, ושניהם רצים עם `NODE_ENV=production` מה-Dockerfile המשותף ([Dockerfile:21](Dockerfile#L21), ושירות המיראז מריץ `node mirage/server.js` מאותה תמונה). לכן [mirage/app.js:159-168](mirage/app.js#L159-L168) החזיר `503 service_token_required` ב-`/api/authorize`, ו**אף אחד לא היה יכול להתחבר למערכת**. אומת מקצה לקצה: `POST /api/auth/mirage-login` → `502 {"error":"mirage_unavailable","reason":"service_token_required"}`.

זה נמצא מיד אחרי שהפריסה מהתקרית הקודמת עלתה יפה - שני השירותים החזירו `/api/health` תקין, ה-API היה מוגן כראוי, וההתחברות בכל זאת שבורה. **הבריאות של שני התהליכים אינה אומרת דבר על הערוץ שביניהם.**

**מה נעשה:** נוצר סוד אחד (48 בתים אקראיים) והוצב בשני השירותים באותה ריצה. הזהות אומתה בהשוואת SHA-256 של שני הערכים, בלי שהערך יודפס. אחרי פריסה מחדש של שניהם - ההתחברות חזרה.

**התיעוד תוקן.** [DEPLOY.md](DEPLOY.md) תיאר את המשתנה כ"חובה למיראז'", וזה נקרא כאילו מדובר במקום אחד. נוספה מטריצת חמשת המצבים (בשניהם / ערכים שונים / רק ב-MIRAGE / רק ב-SKY KING / חסר) עם קוד השגיאה של כל אחד - `service_token_required` ו-`bad_service_token` מצביעים על צדדים שונים של הבעיה - וההבהרה שמשתנה חדש נכנס לתוקף רק בפריסה, ולכן צריך לפרוס את **שני** השירותים.

**QA:** אומת מול הייצור - מיראז דורש אסימון (`401` בלי כותרת) ✅ · התחברות עם אישורים שגויים מקבלת **דחייה אמיתית** `401 bad_credentials` ולא כשל ערוץ ✅ · אכיפת SK-01 מהסבב הקודם נשמרה: 3/3 נתיבים תפעוליים `401` ✅ · 3/3 נתיבים ציבוריים `200` ✅ · שני ה-`/api/health` תקינים ✅.

---

## 2026-08-04 - תקרית פריסה: שעה של API פתוח, מאחורי health ירוק

**הבקשה:** "יש תקלה ב-DEPLOY" - הקונטיינר בלולאת קריסה עם `[auth] AUTH_SECRET חסר או קצר מ-32 תווים`.

**מה קרה:** `AUTH_SECRET` לא הוגדר מעולם ב-Variables של שירות `SKY KING` ב-Railway, בעוד `NODE_ENV=production` מגיע מ-[Dockerfile:21](Dockerfile#L21). [server.js:19-28](server.js#L19-L28) יוצא ב-`exit(1)` - התנהגות מכוונת ונכונה. Railway מיצה את 10 הניסיונות שב-`railway.json` וויתר.

**החלק המסוכן לא היה הקריסה אלא מה שקרה אחריה.** פריסה כושלת ב-Railway **אינה מורידה את השירות** - היא משאירה את התמונה הקודמת מגישה. הפריסה שנפלה היא בדיוק זו שנשאה את קומיט האבטחה 42a06d5, ולכן במשך כשעה הייצור הגיש גרסה מלפני SK-01: `/api/strips`, `/api/crew-members` ו-`/api/sectors` החזירו **200 לבקשה אנונימית מהאינטרנט הפתוח**. `/api/health` דיווח `{"phase":"ready"}` כל אותו זמן, האתר עבד, ולא היה שום סימן חיצוני. `uptimeMs` היה הרמז היחיד: 49 דקות בזמן שהפריסה החדשה בת דקות.

`/api/_diag/env` היה חשוף גם הוא, אך הגרסה שרצה מסננת `user:pass` מה-`DATABASE_URL` ומחזירה host בלבד - לא דלפו אישורי גישה.

**מה נעשה:** הוגדר `AUTH_SECRET` (48 בתים אקראיים, 64 תווים) בשירות, והופעלה פריסה חדשה. אומת אחריה: חמישה נתיבים תפעוליים מחזירים **401** בלי אסימון, ארבעה נתיבים ציבוריים ממשיכים להחזיר **200**.

**התיעוד תוקן במקום שבו הוא נכשל.** [DEPLOY.md](DEPLOY.md) כבר תיאר את `AUTH_SECRET` כחסם עלייה - זה לא היה הפער. הפער היה ש**`/api/health` תואר כאימות הפריסה**, והוא ירוק גם כשהקוד החדש לא עלה. נוספו לטבלת התקלות שורת לולאת-הקריסה, והאזהרה שאימות פריסה מחייב בדיקה שהקוד **החדש** חי (`curl` שחייב להחזיר 401), לא שהשרת חי.

**QA:** אומת מול הייצור אחרי הפריסה - 5/5 נתיבים תפעוליים מוגנים, 4/4 ציבוריים תקינים, `phase=ready`, ואזהרת המפתחות הזרים נעלמה מלוג העלייה ✅.

---

## 2026-08-04 - 9 המפתחות הזרים החסומים בדיפלוי: 8 היו נתונים, אחד היה הצהרה שגויה

**הבקשה:** "יש תקלה ב-DEPLOY" - הלוג מדווח בכל עלייה `[DB] 9 מפתחות זרים לא ניתנים להוספה - יש שורות יתומות`.

**מה נמצא:** מאחורי דיווח אחד הסתתרו **שתי תקלות שונות**, ואחת מהן הייתה מלכודת.

- **8 FK - שורות יתומות אמיתיות.** 385 שורות שכולן ילדים של הורה מחוק: `bdh_items` של מסמכי בד"ח 5 ו-9 שאינם קיימים, `sector_neighbors` ו-`sub_sectors` של סקטורי ה-seed המקורי 1 ו-2 שנמחקו מזמן, וילדים של פ"מים מחוקים (`strip_aircraft`, `strip_serial_selections`, `strip_transfers`, `strip_table_assignments`) - 352 מהן בסביבת התרגול `env_21`. נמחקו.
- **FK אחד - הצהרה שגויה, וניקוי היה שובר ייצור.** `strips.held_by_workstation` הוצהר מול `workstations(id)`. מדידה מול הנתונים: **0 מ-31 הערכים תואמים ל-`workstations.id`, ו-31 מ-31 תואמים ל-`workstation_presets.id`**. הסמנטיקה של העמודה נדדה במעבר לעמדות-preset: [transfers.js:317](server/routes/transfers.js#L317) ו-[transfers.js:357](server/routes/transfers.js#L357) כותבים לשם `assignedPresetId`, ו-[workstations.js:348-349](server/routes/workstations.js#L348-L349) משווה במפורש גם ל-UUID של עמדה **וגם** ל-`String(presetId)` - הקוד עצמו יודע שיש שתי צורות. איפוס 31 השורות ל-`NULL` היה מוחק מצב תפעולי חי, ואז ה-FK היה נוצר ו**מפיל כל קבלת העברה**.

**מה נעשה:**

- **ההצהרה השגויה הוסרה בשני המקומות** - מ-`FOREIGN_KEYS` ומה-`REFERENCES workstations(id)` שבתוך ה-`CREATE TABLE` ב-[init.js:70](server/db/init.js#L70). הסרה מהרשימה בלבד לא הייתה מספיקה: בסביבה **חדשה** ה-`CREATE TABLE` רץ על טבלה ריקה, ה-FK כן נוצר, וקבלת ההעברה הראשונה נופלת. זה היה באג רדום לכל התקנה נקייה.
- **[scripts/db-orphans.mjs](scripts/db-orphans.mjs)** - כלי קבוע. סורק את כל הסכמות, מצליב כל FK מוצהר שאינו קיים מול הנתונים, ומדפיס כמה יתומות ואילו ערכים (`--sample`). מדיניות הניקוי נגזרת מכלל המחיקה המוצהר: `CASCADE` -> מחיקה, אחר -> `SET NULL`, ועמודה `NOT NULL` שאינה `CASCADE` מדווחת ואינה נוגעת בעצמה. `--fix` רץ בטרנזקציה אחת לסכמה.
- **בדיקת אבטחה פלייקית תוקנה** (התגלתה באימות). `auth.test.js` שיבש אסימון בהיפוך התו **האחרון** של החתימה. חתימת HMAC-SHA256 היא 32 בתים = 43 תווי base64url, והתו האחרון נושא 4 ביטים משמעותיים בלבד - `A`↔`B` נבלע בביטים המבוזבזים ומפענח לאותם בתים, כך שהאסימון נשאר תקף. נמדד: **6.5% מההרצות** (1 מכל 16 - בדיוק 16 הערכים החוקיים שנצפו בתו האחרון: `0 4 8 A E I M Q U Y c g k o s w`). השיבוש הועבר לתו הראשון של החתימה, שנושא 6 ביטים מלאים.

**החלטות:**

- **לא לנקות לפני שבודקים לאן העמודה מצביעה.** "FK חסום = נתונים מלוכלכים" היא ההנחה שהייתה שוברת כאן את הייצור. `--sample` הוא ברירת המחדל של הכלי מסיבה זו, ו-`--fix` דורש דגל מפורש.
- **31 השורות של `held_by_workstation` לא נגעו.** הן מצב תפעולי תקף - מי מחזיק את הפ"מ עכשיו.
- **`sub_sectors` נמחקו בלי חשש** כי [seed.js:49-65](server/db/seed.js#L49-L65) מחזיר אותן בעלייה הבאה עם סקטורים תקינים (`CENTER`/`SOUTH`), שכן הזריעה מותנית ב"הטבלה ריקה" ופותרת מזהי סקטור לפי שם.

**QA:** vitest **938/938** ✅ (הריצה הראשונה הראתה 937/938 - הכשל היה הבדיקה הפלייקית לעיל, לא רגרסיה) · השיטה החדשה לשיבוש אסימון: **0 מתוך 5000** נותרו תקפים ✅ · `ensureForeignKeys` על `public` בדיוק כמו בדיפלוי: **נוספו 8, קיימים 128, נכשלו 0** ✅ · **136/136 FK מוצהרים קיימים ב-`public`** ✅ · בידוד סביבות התרגול נמדד אחרי השינוי: **0 FK מסביבה אל טבלה תפעולית ב-`public`** ✅.

---

## 2026-08-09 - הסרה מטבלת ההצטרפות מורידה את המטוסים מההקפה

**הבקשה:** "הטבלאות ריקות אבל המטוסים עדיין על ההקפה בתצוגה. צריך להיות שאם אני מנקה את הטבלה זה מעלים מההקפה."

**מה היה:** `DELETE /api/joining-point-strips/:pointId/:stripId` מחק **רק** מ-`joining_point_strips`. שורות `joining_point_aircraft` עם `pattern_id` נשארו יתומות, והמטוסים המשיכו להיות מצוירים על ההקפה מול טבלה ריקה - בדיוק המצב שבצילום מהשטח.

**מה נעשה:** המחיקה עברה לטרנזקציה שמנקה גם את המיקום על ההקפה, בתנאי שהפ"מ אינו נמצא בשום נקודת הצטרפות אחרת.

**החלטות:**

- **הטבלה היא מקור האמת.** יצא ממנה = ירד מההקפה. המיקום על ההקפה נגזר מהשיוך לנקודה, ולא ישות עצמאית שממשיכה לחיות אחריו.
- **התנאי `NOT EXISTS` נשאר** אף שהוספה לנקודה ממילא מסירה מכל נקודה אחרת (`joiningPoints.js:330`, ולכן פ"מ נמצא בנקודה אחת לכל היותר). הוא עולה כלום ומגן על השינוי הזה אם הכלל ההוא ישתנה.
- **הזרימה של "שים בהקפה" לא נגעה.** שם הפ"מ יוצא מהטבלה **בכוונה** כשכל מטוסיו בהקפה, והם צריכים להישאר מצוירים - זה מסלול נפרד בקוד ולא עובר דרך ה-endpoint הזה.

**QA:** `tsc` נקי ✅ · `npm run build` ✅ · vitest ✅ · **smoke חי מול Neon**: פ"מ בטבלה + מטוס על ההקפה -> הסרה מהטבלה -> **אפס מטוסים על ההקפה** ✅.

---

## 2026-08-06 - שמות ההקפה: כיווץ לפי גודל המסך, ומתג הסתרה בסרגל התצוגה

**הבקשות:** "כאשר אני משנה רזולוציה למסך - להקטין את הפונט של טקסטים של ההקפה." · "יכולת להסתיר את שמות ההקפה לפי סרגל התצוגה השמאלי במפה."

**מה נעשה:**

- `MAP_LABEL_SCALE_MAP` ב-[scale.ts](src/utils/scale.ts) + [useMapLabelScale](src/hooks/useMapLabelScale.ts): מכפיל **כיווץ** לתוויות שעל המפה לפי גודל המסך (15.6"=1.0 · 16"=0.92 · 18"=0.84 · 24"=0.7). מוחל על שמות הצלעות ועל שמות אלמנטי ההקפה, וגם על ההזחה שלהם מהקו.
- מתג **"הצג שמות הקפה"** ב"הגדרות תצוגה" של פאנל השכבות במפה, דולק כברירת מחדל.

**החלטות:**

- **כיווץ חלקי ולא `1/--s`.** הזום הגלובלי מגדיל הכל אחיד; לכפתור סרגל זה **טוב** (`TOOLBAR_SCALE_MAP` אף מגדיל אותו מעבר לכך, למגע/עט), אבל לתווית שיושבת על המפה זה רע - היא גדלה עם המסך בעוד ההקפה עצמה נשארת באותו יחס, ובמסך 24" השמות חנקו את השרטוט. ביטול מלא של הזום (`1/--s`) היה מקפיא את התווית בגודל פיזי קבוע והיא הייתה נראית זעירה ב-24"; לכן הכיווץ חלקי - התווית עדיין גדלה עם המסך, רק פחות.
- **`data-screen` הוא מקור האמת, וההחלפה חיה.** ה-hook מאזין דרך `MutationObserver` בדיוק כמו `useToolbarScale`, ולכן שינוי גודל המסך משנה את התוויות **בלי רענון**.
- **המתג להקפות בלבד.** "הצג שמות" הקיים שולט בשמות אלמנטים ונקודות; ערבוב היה מכבה גם אותם.
- **מספר הכיוון על המסלול נשאר דולק תמיד** - הוא הזהות של המסלול, לא תווית עזר, ולכן אינו תלוי במתג.

**QA:** `tsc` נקי ✅ · `npm run build` ✅ · vitest **1174/1174** ✅ (5 חדשות ל-`getMapLabelScale`) · **e2e אמיתי** - [ground-runways-patterns.spec.ts](e2e/ground-runways-patterns.spec.ts): המתג מסתיר את השמות **ומשאיר את ההקפה**, והפונט קטן מונוטונית לאורך ארבעת גדלי המסך כשמחליפים אותם **חי** - **6/6 עברו** ✅.

**מלכודת שנתפסה:** שתי השכבות (מסלולים והקפות) מקבלות `sz` זהה, ולכן ה-prop `showLabels` נחת בטעות על `RunwayLayer` - שגם לו יש prop באותו שם, ולכן ה-tsc לא תפס. הבדיקה תפסה: המתג לא הסתיר דבר.

---

## 2026-08-05 - סגירת מסלול ב-NOTAM מורידה את ההקפה מהתצוגה

**הבקשה:** "אם יש הקפה למסלול מסוים כי בחרתי אותו להמראה או נחיתה, ואז ב-NOTAM סגרתי מסלול, צריך להוריד את ההקפה מתצוגה."

**מה נעשה:** `closedRunwayEnds` ב-[runwayEnds.ts](src/utils/runwayEnds.ts) מחזיר את קצוות המסלולים שיש עליהם NOTAM סגירה, ו-[GroundView](src/components/views/GroundView.tsx) מסנן אותם מרשימת הקצוות הפעילים לפני `activePatterns`.

**החלטות:**

- **ה-NOTAM גובר על הסימון בפאנל.** הסימון "בשימוש" הוא **כוונה תפעולית**; NOTAM סגירה הוא עובדה על האספלט. ההקפה יורדת מיד, בלי לדרוש מהפקח לבטל גם את הסימון - ואם הסגירה מבוטלת, ההקפה חוזרת מעצמה כי הסימון נשמר.
- **הסגירה חלה על שני הקצוות.** אין "כיוון סגור" - המסלול סגור, ולכן שני קצותיו יורדים.
- **מימוש אחד, שני צרכנים.** אותה לוגיקה הייתה כתובה בנפרד בפאנל "מסלולים בשימוש" (`_isEndClosed`); היא הוחלפה בקריאה לאותו עוזר, כך שסגירה נראית זהה בכפתור ובהקפה.

**QA:** `tsc` נקי ✅ · `npm run build` ✅ · vitest **1124/1124** ✅ (11 חדשות ל-`closedRunwayEnds`) · **e2e אמיתי** - [ground-runways-patterns.spec.ts](e2e/ground-runways-patterns.spec.ts): קצה בשימוש -> ההקפה מוצגת · NOTAM סגירה -> יורדת אף שהקצה עדיין מסומן · ביטול הסגירה -> חוזרת - **4/4 עברו** ✅ · רגרסיה: `runways-in-use-panel`, `runway-direction`, `linked-runway-sync` - **10/10** ✅.

---

## 2026-08-05 - פאנל "מסלולים בשימוש": צד שמאל, סדר קנוני, הפרדה בין מסלולים, ולחיצה מהירה

**הבקשות:** "את זה תציג בצד שמאל בעמדה ולא ימין." · "זה קצת עובד לאט הלחיצות." · "להקפיד שבכל העמדות שמקושרות למסלולים שיהיה באותו סדר של מסלולים בתצוגה." · "בין כל זוג מסלולים לשים קו מפריד."

**מה נעשה:**

- **צד שמאל** - `right: 10` -> `left: 10` במיקום הקבוע של הפאנל (הגרירה החופשית לא הושפעה).
- **הלחיצות האטיות - הסיבה האמיתית:** `RwyBtn` הוגדר **בתוך ה-render** של הפאנל. React מזהה רכיב לפי **זהות הטיפוס**, וטיפוס חדש בכל רינדור פירושו `unmount` + `mount` של כל 16 הכפתורים - בעמדה שמתרעננת בפולינג כל 10 שניות זה נראה כמו ממשק שמגיב לאט. הרכיב הוצא לרמת המודול כ-[RunwayUseButton](src/components/views/SectorDashboard.tsx) עטוף ב-`React.memo`, ונוסף `touchAction: 'manipulation'` (מבטל את השהיית 300ms של דאבל-טאפ בעט/מגע).
- **סדר קנוני** - `orderedRunwayGroups` ב-[runwayEnds.ts](src/utils/runwayEnds.ts): הסדר נגזר מ**שם המסלול בלבד** ולכן זהה בכל עמדה. קבוצה = מסלול (שני קצותיו צמודים), בתוך הקבוצה הקצה הנמוך ראשון, והקבוצות לפי הקצה הנמוך ובשוויון לפי הסיומת L < C < R.
- **קו מפריד** בין קבוצה לקבוצה, בשתי השורות.

**החלטות:**

- **הסדר משם המסלול ולא מ-`sort_order`/`id`.** קודם הוא הגיע מסדר ה-DB, ולכן שתי עמדות שמקושרות לאותם מסלולים הציגו אותם בסדר שונה. הפקח משווה בין מסכים - סדר שונה לאותו מידע הוא מלכודת. גזירה מהשם היא היחידה שאינה תלויה בהיסטוריית ההזנה של כל שדה.
- **המפריד בין מסלולים, לא בין קצוות.** שני קצוות של אותו מסלול הם אותו אספלט וחייבים להיקרא כיחידה; ההפרדה הנחוצה היא בין מסלול למסלול.
- **קצה שמופיע בשני מסלולים מוצג פעם אחת.** אחרת קישור בין שדות היה מכפיל כפתורים.

**QA:** `tsc` נקי ✅ · `npm run build` ✅ · vitest **1113/1113** ✅ (9 חדשות ל-`orderedRunwayGroups`) · **e2e אמיתי** - [runways-in-use-panel.spec.ts](e2e/runways-in-use-panel.spec.ts): הפאנל בחצי השמאלי של המסך, מספר המפרידים = מסלולים-1 בכל שורה, שני קצותיו של כל מסלול צמודים והמסלולים ממוינים לפי הקצה הנמוך, והלחיצה משנה מצב תוך פחות משתי שניות **בלי שצומת ה-DOM של הכפתור מוחלף** (סימון `data-probe` ששורד את הרינדור מוכיח שאין הרכבה מחדש) - **4/4 עברו** ✅ · רגרסיה: `runway-direction`, `linked-runway-sync` ו-`ground-runways-patterns` - **13/13** ✅.

---

## 2026-08-05 - ההקפה בעמדה נדלקת עם המסלול הפעיל

**הבקשה:** "ההקפה בעמדה תוצג כאשר בחרתי מסלול המראה פעיל להמראה ונחיתה ברשימת מסלולים בעמדת שדה תעופה."

**מה נעשה:** שכבת ההקפות בעמדת השדה כבר לא נשלטת בצ'קבוקס ידני בלבד - היא מסננת לפי הקצוות שסומנו בפאנל **"מסלולים בשימוש"** (המראה ∪ נחיתה). `activePatterns` ב-[trafficPattern.ts](src/utils/trafficPattern.ts) עושה את ההתאמה, ו-[SectorDashboard](src/components/views/SectorDashboard.tsx) מעביר את `towerTakeoffRunways`/`towerLandingRunways` כ-`activeRunwayIdents`.

**החלטות:**

- **סינון ולא הצגת הכול.** בשדה עם כמה מסלולים יש הקפה לכל קצה; ציור כולן יחד הופך את המפה לרשת קווים חסרת משמעות. ההקפה הרלוונטית היא של המסלול שבשימוש עכשיו - ולכן היא נדלקת עם בחירת המסלול ונכבית איתה.
- **השוואה על הקצה המלא.** `33L` אינו `33` - שני קצוות שונים על אספלט שונה. ההשוואה סובלנית לרווחים ולאותיות בלבד.
- **שכבת ההקפות דולקת כברירת מחדל** עכשיו: כשאין מסלול פעיל היא ממילא ריקה, ולכן צ'קבוקס כבוי היה רק שלב מיותר לפני שרואים משהו.

**QA:** `tsc` נקי ✅ · `npm run build` ✅ · vitest **1104/1104** ✅ (7 חדשות ל-`activePatterns`) · **probe בדפדפן** על עמדת שדה אמיתית: 3 הקפות מרונדרות בדיוק לשלושת הקצוות שבשימוש, ו-4 מסלולים ✅ · **e2e אמיתי** - [ground-runways-patterns.spec.ts](e2e/ground-runways-patterns.spec.ts): כיבוי הקצה -> ההקפה נעלמת, הדלקה -> חוזרת במצב תצוגה בלבד עם חמש צלעות, וכיבוי חוזר מכבה - **3/3 עברו** ✅.

**הערה על הבדיקה:** הגרסה הראשונה זרעה מסלול והקפה משלה וספרה `pattern-shape` **בכללותו**. בשדה אמיתי כבר יש קצוות בשימוש שההקפות שלהם מוצגות בצדק, ולכן הספירה בדקה את הנתונים ולא את הקוד - ונתנה גם false pass וגם false fail. הבדיקה עובדת עכשיו על קצה אמיתי של השדה ומתייחסת ל-`data-pattern-id` שלו בלבד.

---

## 2026-08-03 - הגדרת שדה תעופה: שדה בלי מפה, שם חובה וייחודי, ובורר בסיס קריא

**הבקשות:** "צריך יכולת לייצר שדה תעופה רק מאלמנטים בלי לבחור מפה." · "שם שדה תעופה חייב להיות חובה ולא זהה לשם שדה תעופה אחר." · "בסיס אב בהגדרת שדה תעופה התפריט מופיע ככתב שחור ולא רואים." · "צריך טיפה פחות רחב את המסלולים ושים את שם המסלול על המסלול."

**מה נעשה:**

- **שדה מאלמנטים בלבד.** כל אזור המפה היה מותנה ב-`hasMap` - בלי מפה לא היה **על מה** למקם, כלומר שדה בלי מפה לא היה שמיש כלל. נוסף משטח עבודה: רשת ביחס 4:3 שמוצגת במקום התמונה. `hasMap` הפך ל-`true` כי כל כפתורי המיקום והציור נשענים עליו, ועכשיו יש משטח בכל מקרה; עיגון גיאוגרפי הוא היחיד שבאמת דורש תמונה והוא ממילא מותנה ב-`adminAirfieldMapData`.
- **שם חובה וייחודי.** `validateAirfieldName` ב-[airfield.js](server/routes/airfield.js) נאכף ב-**POST וגם ב-PUT**. קודם ה-409 היה ב-POST בלבד - שינוי שם דרך PUT יכול היה להתנגש בשם קיים בלי שאיש ידע - ושם ריק עבר בשקט (כך נוצר שדה בלי שם כלל ב-DB). ההשוואה על `LOWER(TRIM(name))`. הטופס מציג את השגיאה במקום `alert`, וגם מקרה השם הריק מקבל הודעה.
- **בורר בסיס האב.** הצבע היה `#475569` על רקע `#0f172a` - יחס ניגודיות שלא מאפשר קריאה. הכתב הועבר ל-`#e2e8f0`, וגם ל-`<option>` ניתן צבע ורקע מפורשים (בלעדיהם הדפדפן מרנדר את הרשימה הנפתחת בברירות המחדל של המערכת).
- **המסלול צר יותר, והשם עליו.** הרוחב ירד (`length*0.065`, חסום 2.0-4.6). והכיתוב - שהיה קשור ל"הצג שמות" שכבוי כברירת מחדל ולכן **לא הופיע כלל בעמדה** - דולק תמיד: מספר הכיוון הוא הזהות של המסלול, לא תווית עזר.

**החלטות:**

- **אכיפת השם בשרת ולא רק בטופס.** גם שכפול, גם מיגרציות וגם כל קריאת API אחרת עוברים שם; ולידציה בטופס בלבד הייתה משאירה את הדלת פתוחה - וזה בדיוק מה שקרה.
- **המשטח הריק נמדד ב-ResizeObserver.** בלי `imgBounds` היה `aspect = 1` בעוד המשטח 4:3, והמסלולים וההקפות היו יוצאים מעוותים דווקא בשדה שנבנה מאלמנטים בלבד.
- **fixture נוצר בקוד.** `admin-airfield-fixes.spec.ts` נשען על `e2e/.map-fixture.txt` שאינו ב-repo - הבדיקה נשברה מיד כשהקובץ נמחק. `makeTestMapPng()` ב-helpers מייצר PNG תקין ב-process.

**QA:** `tsc` נקי ✅ · `npm run build` ✅ · vitest **881/881** ✅ · **e2e אמיתי** - [admin-airfield-basics.spec.ts](e2e/admin-airfield-basics.spec.ts): דחיית שם ריק (400), דחיית שם כפול ביצירה **ובשינוי שם** (409) כולל אותיות ורווחים, משטח עבודה בעל גובה אמיתי בשדה בלי מפה שקליק עליו מתורגם לנ"צ, ו**ניגודיות נמדדת** של בורר הבסיס (≥4.5:1) - **4/4 עברו** ✅ · רגרסיה: כל 12 בדיקות ה-e2e של שדות התעופה עברו ✅.

---

## 2026-08-03 - זום אמיתי במפת הניהול, ומסלולי המראה והקפות בעמדת השדה

**הבקשות:** "בהגדרת שדה תעופה ה +- לא עושה זום למפה אלא מגדיל את הרכיבים שצוירו." · "בעמדת שדה תעופה להוסיף יכולת להציג הקפה ומסלולים (בלי מסלולי הסעה). כשמציג מסלולים להציג את זה לא כקו אלא כמסלול."

### הזום שלא זימם

**מה נמדד בדפדפן לפני התיקון:** ב-156% התמונה נשארה **720px** וה-SVG גם, בעוד רוחב ה-layout של המיכל התכווץ מ-722 ל-**461** יחידות מקומיות.

**הסיבה:** `zoom: z` על מיכל שרוחבו נקבע על ידי ההורה **אינו מגדיל דבר**. הוא מבטא מחדש את הפריסה ביחידות מקומיות (רוחב ההורה חלקי z), ולכן תמונה ב-`width:100%` נפרסת בדיוק לאותו גודל פיזי. רק ילדים עם **px קשיח** - סמני האלמנטים - הוכפלו ב-z. זה בדיוק מה שנראה כמו "הכפתורים מגדילים את הרכיבים המצוירים".

**התיקון:** רוחב מפורש `${100 * z}%` על המיכל. באחוזים מקומיים זה יוצא רוחב ההורה במלואו, כלומר פי z פיזית - והתמונה שבתוכו גדלה איתו, בעוד הגלילה של המיכל שמסביב מאפשרת לנוע במפה. **כל חשבון הקואורדינטות נשאר כשהיה**, כי `getBoundingClientRect` חלקי z ממשיך להחזיר יחידות מקומיות. אומת במדידה חוזרת: 726 -> **1134.7** ב-156% (בדיוק ×1.5625), **580.8** ב-80%, ו-`scrollWidth` 1141 מול `clientWidth` 726.

`MapZoneEditor` לא נגוע - הוא משתמש נכון ב-`transform: scale()`.

### מסלול המראה מצויר כמסלול

- [runwayShape.ts](src/utils/runwayShape.ts) + [RunwayLayer.tsx](src/components/map/RunwayLayer.tsx): מיסעה, ספי מסלול, קו מרכז מקווקו ומספר כיוון בכל קצה.
- **הרכיב משותף לניהול ולעמדה.** הקו הירוק במפת הניהול הוחלף באותו ציור; נקודות הקצה נשארו כידיות עריכה מעל. מה שהמנהל מסמן הוא בדיוק מה שהפקח רואה.

### עמדת השדה: שתי שכבות חדשות

- **🛬 מסלולי המראה** - דולקת כברירת מחדל, **נפרדת ממסלולי ההסעה** (הבקשה המפורשת: מסלולים בלי רשת ההסעה). הגאומטריה מ-`airfield_runways`.
- **🔄 הקפות** - כבויה כברירת מחדל, מוזנת מ-`airfield-patterns` דרך `SectorDashboard` ומרונדרת ב-**אותו** `TrafficPatternLayer` ששרטט אותן בניהול, במצב תצוגה בלבד.

**החלטות:**

- **רוחב סכמטי ולא פרופורציה אמיתית.** בגרסה הראשונה הרוחב היה קבוע (2.4% מגובה התמונה) והמסלול עדיין **נקרא כקו** - נתפס בצילום, לא בבדיקות. `derivedRunwayWidth` גוזר מהאורך וחוסם בין 2.6 ל-7: רחב מספיק שהסימונים ייקראו, צר מספיק שלא יבלע את סביבתו.
- **מספר הכיוון מסובב לאורך הציר**, כמו הצביעה על המסלול עצמו - כך שקצה אחד נקרא הפוך מהשני. זו המוסכמה, וכך גם בדוגמה שהגיעה מהשטח.
- **סיבוב יחיד לכיתוב.** `transform-origin` נוסף על גבי `rotate` (שכבר נושא מרכז משלו) הזיז את המספרים מהמקום ולכן הם לא נראו כלל.
- **`airfield_runways` ולא `airfield_routes.is_runway`.** הראשונה היא הישות האמיתית עם שני הקצוות והכיוונים; השנייה היא קו גיאומטרי למנוע הקונפליקטים.
- **ההקפות כבויות כברירת מחדל, המסלולים דולקים.** המסלולים הם המידע הבסיסי של השדה; ההקפה נדרשת לפי הצורך.

**QA:** `tsc` נקי ✅ · `npm run build` ✅ · vitest **880/880** ✅ (19 חדשות לגאומטריית המסלול) · **מדידה בדפדפן** של הזום לפני ואחרי ✅ · **e2e אמיתי** - [ground-runways-patterns.spec.ts](e2e/ground-runways-patterns.spec.ts): כניסה לעמדת שדה אמיתית, המסלול מצויר כמיסעה (יותר מ-4 פוליגונים + קו מרכז) ולא כקו, השכבה נפרדת ממסלולי ההסעה וניתנת לכיבוי, וההקפה נדלקת ומוצגת במצב תצוגה בלבד עם חמש צלעות - **3/3 עברו** ✅.

---

## 2026-08-03 - שדה תעופה: שכפול שנפל, וקישורי מסלולים כרובד עצמאי בין N עמדות

**הבקשה:** "צריך לסדר את נושא שדה תעופה. א. שכפול שדה מעיף את המערכת. ב. להוסיף קטגוריה אחרת של קישור מסלולים בין עמדות ושלא יהיה תחת מסלולי הסעה (כי זה גם רלוונטי למסלולי טיסה). ג. יכולת לקשר בין מסלולים בין יותר מ-2 עמדות."

### א. שכפול שדה - **שני** באגים בלתי תלויים

שוחזר מול ה-DB האמיתי: 3 מתוך 8 השדות נפלו ב-500, השאר עברו. שני גורמים שונים:

1. **`sids`/`stars` הועברו כ**מערך JS** לעמודת JSONB.** `pg` מסדר מערך JS כ**ליטרל מערך של Postgres** (`{"בור","גילה"}`) ולא כ-JSON — ולכן כל שדה עם STAR/SID נפל ב-`invalid input syntax for type json` (כך "כנף 1"). מערך **ריק** עבר בשקט ונשמר כ-`{}` — אובייקט במקום מערך, והלקוח נפל לרשימה ריקה בלי הודעה. תוקן ב-[airfield.js](server/routes/airfield.js) עם `asJson()` שגם **מרפא** ערך משובש שנשמר בעבר, ובנוסף ריפוי חד-פעמי ב-[init.js](server/db/init.js).
2. **sequences שפיגרו אחרי `max(id)`.** `airfield_sectors` (max=11, next=10), `airfield_polygons` (max=4, next=4), `airfield_status_types` (max=6, next=2) — כל אחד מהם הפיל את ה-INSERT ב-`duplicate key ... _pkey`. **זו לא תקלה של השכפול**: גם הוספת סקטור רגילה לשדה הייתה נכשלת. [sequences.js](server/db/sequences.js) חדש סורק ומתקן בכל עלייה, אידמפוטנטית.

**החלטות:**

- **תיקון בעלייה ולא סקריפט ידני.** שחזור dump יקרה שוב; סקריפט חד-פעמי היה משאיר את הבאג לחזור בשקט. הריצה מתקנת רק את מה שמפגר ולא נוגעת בנתונים.
- **⚠ לא `pg_get_serial_sequence`.** הגרסה הראשונה השתמשה בה ו**לא תיקנה כלום** — היא מחזירה NULL כשה-sequence אינו **owned** על ידי העמודה, וזה בדיוק המצב אחרי שחזור dump, כלומר בדיוק הטבלאות השבורות. השם נשלף מברירת המחדל של העמודה (`nextval('...')`).
- **`next == max` נחשב מפגר.** ה-id הזה כבר תפוס.
- **השכפול הושלם.** קודם הועתקו רק נקודות, נתיבים, אלמנטים, פוליגונים, סקטורים וסוגי סטטוס. נוספו **מסלולים** (כולל המרחקים המוצהרים והנ"צ), **מסלולי גלגול**, **הקפות + אלמנטיהן** (עם מיפוי `runway_id` לעותק), **הודעות שדה**, ועמודות שנשמטו: `lat/lng/show_in_driver` בנקודות ו-`is_runway/end_a_name/end_b_name` בנתיבים. שדה מפוצל בלי המסלולים שלו אינו עותק. **GRF/NOTAM/תאורה לא מועתקים בכוונה** — מצב תפעולי חי של אותו רגע, לא הגדרת שדה.
- **`az_*` מדולגות** (AeroZone, ראה CLAUDE.md).

### ב+ג. קישורי מסלולים - רובד עצמאי, קבוצה במקום זוג

- **הוצא מתוך "מסלולי הסעה".** הבלוק ישב **בתוך** סקשן `routes` ולכן נראה כאילו הוא שייך להסעה. אותו מסלול פיזי נראה בשתי עמדות בשמות שונים גם כשהוא **מסלול המראה** — לכן הוא סקשן משלו, [RouteLinksSection.tsx](src/components/admin/RouteLinksSection.tsx), והבורר מציע **כל** סוג מסלול עם אייקון הסוג (🛫 המראה · ✈ מטוסים · 🚗 רכב · 🛤️ כללי).
- **קבוצה במקום זוג.** `route_links` היה זוגי, ולכן קישור בין שלוש עמדות דרש שלושה זוגות נפרדים שכל אחד מהם ניתן היה למחוק לבד ולהשאיר קישור חלקי ושקט. [route_link_groups + route_link_members](server/db/init.js) מחזיקות קבוצה של N חברים (עמדה + מסלול), N>=2 — שתי עמדות הן המקרה הפרטי. הלוגיקה ב-[routeLinks.ts](src/utils/routeLinks.ts).

**החלטות:**

- **הגירה אוטומטית ומסומנת.** הזוגות הקיימים מוגרים לקבוצות בעלייה, עם `migrated_from_link_id` ואינדקס ייחודי חלקי — הרצה חוזרת אינה מכפילה. `route_links` נשארת בסכמה כמקור ההגירה; הקוד קורא רק מהקבוצות.
- **`UNIQUE(group_id, preset_id, route_id)` ולא `(group_id, preset_id)`.** עמדה יכולה לראות שני מסלולים שמקושרים לאותו מסלול פיזי אצל שכנתה, ולכן אותה עמדה מותרת פעמיים עם מסלולים שונים — רק אותו **צמד** נחסם.
- **עריכה = החלפת הרכב.** `PUT` מוחק את החברים ומכניס מחדש בטרנזקציה, במקום diff חלקי שמשאיר מצבי ביניים.
- **`active-takeoffs` עבר לקבוצות:** מסלול מקושר הוא כל מסלול באותה קבוצה, ולא רק בן-הזוג — קבוצה של שלוש עמדות נספרת במלואה.

**QA:** `tsc` נקי ✅ · `npm run build` ✅ · vitest **861/861** ✅ (13 חדשות ל-sequences, 23 לקישורים) · **שחזור מול Neon האמיתי**: לפני התיקון 3/8 שדות נפלו ב-500, אחריו **8/8 עוברים** עם sids/stars, מסלולים והקפות שלמים בעותק ✅ · העלייה דיווחה `סונכרנו 4 sequences שפיגרו אחרי max(id)` ✅ · **smoke חי** על קבוצות הקישור - 7 בדיקות: הגירה מהזוגות, קבוצה של 3, הרחבה ל-4, דחיית קבוצה עם עמדה אחת, חיפוש לפי עמדה, 404, ומחיקה מדורגת ✅ · **e2e אמיתי** - [admin-airfield-fixes.spec.ts](e2e/admin-airfield-fixes.spec.ts): שכפול עם SIDs, שכפול פעמיים ברצף בלי duplicate key, הסקשן **אינו** מקונן בתוך מסלולי הסעה, הבורר מציע מסלול המראה, קישור בין שלוש עמדות, וחסימת שמירה עם עמדה אחת - **4/4 עברו** ✅.

---

## 2026-08-03 - עמדת ניהול: רובד ההקפה ביישות שדות תעופה

**הבקשה (אפיון):** "ביישות שדות תעופה נוסיף רובד של הקפה. תחת ההקפה נבחר בשדה לאיזה מסלול, ואז כפתור 'צייר על מפה'. על המפה תופיע הקפה צפה שניתן לסובב לכיוון הנכון ולהאריך כל צלע ע"י גרירת הפינות. לכל צלע יש שם - אחרי המראה, צולבת, עם הרוח, בסיס, פיינל (כיוון החץ מצביע למסלול וזה הפיינל). יופיעו שמות הצלעות ליד הצלעות עם תוספת שם המסלול. ניתן לעשות שכפל הקפה / שכפל הקפה הפוכה. לכל הקפה בטבלה יכולת להוסיף אלמנט עם שם, ICON וצבע - משויכים רק להקפה הספציפית."

**מה נעשה:**

- **מודל גאומטרי טהור** - [trafficPattern.ts](src/utils/trafficPattern.ts), 52 בדיקות. ההקפה נשמרת כ**פרמטרים** (עוגן=סף המסלול, כיוון, צד, ארבעה אורכי צלעות) ולא כשש נקודות חופשיות.
- **DB** - [airfield_patterns](server/db/init.js) + `airfield_pattern_elements`, מסווגות CONFIG ב-[env-tables.js](server/db/env-tables.js) ועם FK ב-[foreign-keys.js](server/db/foreign-keys.js).
- **API** - 8 endpoints ב-[airfield.js](server/routes/airfield.js), כולל `POST /:id/duplicate`.
- **שכבת מפה משותפת** - [TrafficPatternLayer.tsx](src/components/map/TrafficPatternLayer.tsx): רכיב אחד לתצוגה ולעריכה, כדי שמה שהמנהל משרטט יהיה בדיוק מה שהעמדה תציג.
- **סקשן ניהול** - [PatternsSection.tsx](src/components/admin/PatternsSection.tsx), נטען לטאב שדות התעופה ב-[ManagementPage](src/components/admin/ManagementPage.tsx). כל הטקסטים ב-registry חדש [pattern.json](src/i18n/registry/pattern.json) (he+en).

**החלטות:**

- **פרמטרים, לא נקודות.** הקפה היא צורה מוגדרת: חמש צלעות בזוויות ישרות סביב ציר המסלול. עם שש נקודות חופשיות, גרירת פינה אחת הייתה שוברת את הזוויות וההקפה מפסיקה להיות הקפה, ו"שכפול הפוך" היה צריך לנחש איזו נקודה היא הסף. במודל הפרמטרי גרירת פינה מאריכה **רק** את הצלעות הצמודות, סיבוב הוא שינוי מספר אחד, ו**שכפול הפוך הוא שלוש השמות**: עוגן -> הקצה השני, כיוון +180°, צד מתחלף - וזה בדיוק "אותו צד פיזי, כיוון הפוך" שבאפיון. `points` נשמרות **בנוסף** ל-`geometry` כדי ששכבת תצוגה עתידית תצייר בלי לחשב יחס תמונה.
- **⚠ מרחב איזוטרופי.** שכבת ה-SVG של המפה היא `preserveAspectRatio="none"` - יחידה ב-X אינה שווה ליחידה ב-Y. מלבן אמיתי על הקרקע הוא מלבן ב**פיקסלים**, ולכן חישוב "באחוזים" היה נותן הקפה מעוותת בכל תמונה שאינה ריבועית (הזוויות לא ישרות). כל החישוב עובר למרחב שיחידתו אחוז מ**גובה** התמונה, וכל פונקציה מקבלת `aspect`. אומת בצילום: הצולבת יוצאת ניצבת למסלול בפיקסלים (מכפלה סקלרית 0.003).
- **הגאומטריה חיה בלקוח בלבד.** `POST /:id/duplicate` מקבל את הגאומטריה והשם מהלקוח ולא מחשב שיקוף בשרת - אחרת אותה מתמטיקה הייתה קיימת בשני מקומות ומתפצלת.
- **`fitToMap` - ההצעה חייבת להיראות.** הקפה אמיתית גדולה פי כמה מהמסלול, ומפת שדה היא תרשים של השדה עצמו. ההצעה הראשונה למסלול ארוך יצאה מחוץ לתמונה והמשתמש ראה שני קווים חתוכים (נתפס בצילום, לא בבדיקות). הפונקציה מכווצת את שלוש הצלעות **באותו יחס** עד שההקפה נכנסת - העוגן, הכיוון, הצד ואורך המסלול נשמרים, והמשתמש תמיד יכול להאריך חזרה.
- **תווית מוזחת מהנורמל של הצלע, לא ממרכז ההקפה.** בצלע קצרה (אחרי המראה) האמצע קרוב לפינה, והזחה מהמרכז הדביקה את התווית שלה לתווית הצולבת. גם זה נתפס בצילום בלבד.
- **`runway_id` הוא SET NULL ולא CASCADE.** מחיקת מסלול מנתקת את ההקפה - לא מוחקת שרטוט שהמנהל השקיע בו.
- **שכפול לא מעתיק אלמנטים.** האפיון אומר "מעתיק את השרטוט"; ובשכפול הפוך האלמנטים ממילא היו נשארים במיקום הלא נכון.
- **איפוס מצב עריכה עבר לתחילת `loadAirfieldPoints`.** באג שנתפס בצילום: הטעינה ארוכה (מפה, אלמנטים, פוליגונים, מסלולים), ואיפוס בסופה ביטל עריכה שהמשתמש כבר פתח בינתיים.
- **Pointer Events + `setPointerCapture`** בכל הידיות - עמדת היעד היא Cintiq 24 עם עט.

**QA:** `tsc` נקי ✅ · `npm run build` ✅ · vitest **825/825** ✅ (52 חדשות לגאומטריה) · **SQL מול Neon האמיתי** בטרנזקציה עם ROLLBACK - 13 בדיקות: DDL, round-trip של JSONB, דיוק תת-אחוזי, CASCADE של אלמנטים, SET NULL של מסלול, TIMESTAMPTZ ✅ · **smoke חי על ה-API** - 10 בדיקות כולל 404 ובידוד אלמנטים בין הקפות ✅ · **e2e אמיתי** - [admin-traffic-patterns.spec.ts](e2e/admin-traffic-patterns.spec.ts): הוספה, שיוך למסלול, חמש תוויות עם שם המסלול, סיבוב סביב הסף, גרירת פינה, שמירה ושחזור אחרי רענון, שכפול רגיל (שם ריק) ושכפול הפוך (33 -> 15, אותו צד), ואלמנט שמקונן רק תחת ההקפה שלו - **4/4 עברו** ✅.

**עדיין לא:** ההקפות מוצגות בעמדת הניהול בלבד. `TrafficPatternLayer` נבנה כרכיב משותף כדי שהחיבור ל-GroundView / SectorDashboard יהיה הוספת שכבה, בלי גאומטריה חדשה.

---

## 2026-08-03 - עמדת שדה: נקודות העברה מוצגות רגיל, וגרירה למפת השדה כחץ

**הבקשה:** "בעמדת שדה תעופה - נקודות העברה לא מוצגות טוב. צריכות להיות מוצגות רגיל עם יכולת גרירה למפה כחץ".

**מה היה:** בעמדת שדה (`preset_type = 'ground'`) פאנל נקודות ההעברה **לא רונדר כלל** - התנאי ב-[SectorDashboard](src/components/views/SectorDashboard.tsx) היה `!isClassicMode && !isGroundMode && !isMissionDeskMode`, כך מאז פיצול המונוליט. ההעברה היחידה מהעמדה הייתה דרך מודל ה-SID בהמראה. ב-[GroundView](src/components/views/GroundView.tsx) נשארו שרידי קוד מת לפאנל שמעולם לא רונדר (`transferSectors`, `sidTransferEntries`, `leftDragOver`).

**מה נעשה:**

- **אותו פאנל בדיוק** (DRY): התנאי הוחלף ל-`!isGroundMgmtMode`, כך ש-`ground` מקבל את `DraggableNeighborPanel` המשותף על כל התנהגותו (קונפליקטי גובה, טווחי גובה, קשר, תת-נקודות). רק "ניהול קרקעי" (`ground_mgmt`) נשאר בלי - אין בו פ"ממים.
- **גרירה למפת השדה = חץ.** ענף חדש ב-`handleNeighborDropOnMap`: בעמדת שדה המפה היא תמונת שדה התעופה ולא מפת הסקטורים, ולכן הנקודה נשמרת כ**שבר 0..1 מגבולות התמונה** (`groundTPins`) ומצוירת ב-GroundView דרך `ptPos` - כך היא נשארת צמודה למקומה בזום/פאן/שינוי גודל מסך. תמיד חץ, בלי דיאלוג חץ/פאנל מלא.
- **החץ הוא יעד העברה:** גרירת כרטיס פ"מ (או מטוס בודד) מרשימת הפ"ממים אל החץ או אל כרטיס הנקודה בפאנל → `handleGroundTransfer`. `onStripDrop` של הרכיב המשותף הורחב ב-`aircraftIdx` אופציונלי, ושני מטפלי ה-drop אוחדו ל-`handleStripDropOnPoint` (קודם היו שני עותקים של אותו one-liner).

**החלטות:**

- **state נפרד ולא `neighborPins`.** הפינים של מפת הסקטורים מחזיקים **פיקסלים** (`tpToMapItem` מייצר ערכים > 1.5) ושכבת הרינדור מפרשת ערך ≤ 1.5 כשבר. עמדת שדה עובדת בשברי תמונה - ערבוב היה נותן פינים בפינה.
- **שחרור נספר רק מעל המפה.** `#map-area` בעמדת שדה מכיל גם את רשימת הפ"ממים; בלי בדיקת `#ground-map-area` שחרור על הרשימה היה מציב חץ בפינה שרירותית.
- **z-index 9 במנוחה.** אותו עיקרון כמו במפת הסקטורים: החץ הוא סימון עזר ויושב **מתחת** לנקודות (10), לאלמנטים (12) ולמטוסים (20+) - לעולם לא מסתיר מידע תפעולי. ברחיפה בלבד הוא עולה ל-40, אחרת אלמנט שיושב עליו היה חוסם הזזה והסרה.

**QA:** `tsc` נקי ✅ · `npm run build` ✅ · vitest **816/816** ✅ · **e2e אמיתי** חדש - [ground-transfer-points.spec.ts](e2e/ground-transfer-points.spec.ts): כניסה לעמדת שדה אמיתית מה-DB, הפאנל מוצג עם כרטיסי הנקודות, גרירה למפה מציבה חץ במקום השחרור, ה-z-index במנוחה מתחת ליישויות, וההסרה זמנית - **2/2 עברו** ✅. רגרסיה: [map-transfer-points.spec.ts](e2e/map-transfer-points.spec.ts) - שתי הבדיקות שנוגעות בפאנל ובפינים של עמדת הבקר עברו ✅ (שתי בדיקות עורך המפה בניהול נכשלו מסיבה שאינה קשורה: ה-spec בוחר את העמדה הראשונה עם `map_id` בלי לסנן `preset_type`, וקיבל עמדת שדה שאין בה כפתור "עריכת מפה ונקודות העברה").

---

## 2026-08-02 - טופס חברי העמדה: יציאה בלי לשמור, ותפריט שמות שמסביר את עצמו

**הבקשה:** "לא ניתן לסגור את הטופס רק לאשר (ללא אישור) והוא לא מוצא את המש"קים".

**מה היה:**

1. **אין דרך החוצה.** ב"עדכון חברי העמדה" הכפתור היחיד היה "✅ שמירה". "דלג" קיים רק במסלול הכניסה (`onSkip`), Escape לא נתפס, ולחיצה על הרקע לא סוגרת. מי שפתח את הטופס מהתפריט בטעות היה חייב לשמור כדי לצאת - כלומר לכתוב רשומת משמרת שלא התכוון אליה.
2. **תפריט ריק = שדה תקול.** התפריט רונדר רק כש-`options.length > 0`. לתפקיד שאין לו אנשים במיראז' (בפועל: **אף משתמש לא מסומן `mashak`**) הלחיצה על השדה לא עשתה כלום - בלי הודעה, בלי רמז שהקלדה חופשית חוקית.
3. **תפריט של שדה תחתון נפתח מחוץ למסגרת.** התפריט תמיד `top: 100%`. מש"ק הוא שדה החיפוש **התחתון** בטופס יב"א, וכשהכרטיס מגיע ל-92vh (מסך נמוך / שלושת המושגחים פתוחים) 132 הפיקסלים של הרשימה נפלו אל **מתחת** לקצה הכרטיס הגולל - ההצעות היו שם, אבל לא על המסך.
4. **חיפוש "מכיל בדיוק".** `o.includes(q)` על המחרוזת הגולמית: "לב אורי" לא מצא "אורי לב", "אורן דור" לא מצא "אורן בן דור", ורווח כפול או גרש הפילו את החיפוש לאפס תוצאות.

**מה נעשה** ([StationCrewForm.tsx](src/components/shared/StationCrewForm.tsx)):

- **`onCancel`** (prop אופציונלי) → כפתור "✕ סגור" בכותרת + Escape. מועבר מ-[SectorDashboard](src/components/views/SectorDashboard.tsx) במסלול העדכון בלבד; במסלול הכניסה התפקיד הזה כבר ממולא ע"י "דלג".
- **`matchCrewOptions(options, query)`** - פונקציה טהורה ומיוצאת: נרמול (גרשיים יורדים, רווחים מתכווצים, lowercase) + התאמת **כל מילה שהוקלדה בנפרד**. "לב אורי", "אורן דור", "שיח" - כולם מוצאים.
- **התפריט נפתח תמיד כשהשדה בפוקוס**, עם שתי הודעות נפרדות: רשימה ריקה לגמרי → `crew.noOptionsForRole` ("לא הוגדרו אנשי צוות לתפקיד זה במיראז' - ניתן להקליד שם"); רשימה קיימת בלי התאמה → `crew.noResults`.
- **פתיחה כלפי מעלה** כשאין מקום מתחת: מדידה אחרי הרינדור מול הכרטיס הגולל, שמסומן `data-crew-scroll` (גם ב-[DebriefForm](src/components/shared/DebriefForm.tsx), שמשתמש באותם `CrewFields`).

**החלטות:**

- **מדידה, לא קבוע פיקסלים.** הטופס מוצג תחת `zoom: var(--s)` לפי גודל המסך; חישוב "132px + מרווח" מול `window.innerHeight` היה נכון רק ב-15.6". לכן ה-effect מודד את הרשימה **אחרי** שרונדרה ומשווה ל-rect של הכרטיס.
- **הגבול הוא הכרטיס ולא החלון.** `overflowY: auto` הופך את הכרטיס לאזור גלילה, ותפריט שנפתח מתחת לקצהו אינו נחתך - הוא פשוט **מחוץ לשדה הראייה** עד שגוללים. `closest('[data-crew-scroll]')` נותן את הגבול האמיתי בשני הטפסים בלי לכפול קוד.
- **לחיצה על הרקע אינה סוגרת.** עמדת היעד היא Cintiq עם עט; נגיעה מקרית מחוץ לכרטיס הייתה מוחקת טופס מלא. ✕ ו-Escape מפורשים.
- **Escape מדורג.** תפריט הצעות פתוח בולע את המקש (`stopPropagation` ב-`SearchPicker`) - לחיצה ראשונה סוגרת את התפריט, שנייה את הטופס.
- **התפריט הריק לא מוצף באנשים אחרים.** הפיתוי היה ליפול לרשימת כל המורשים לעמדה כשלתפקיד אין אנשים - אבל בקר אינו מש"ק, וזה היה מייצר רישום שגוי בשקט. במקום זה השדה **אומר** שהתפקיד לא מוגדר במיראז' ומזמין הקלדה חופשית.

**מקור התקלה שדווחה (נתונים, לא קוד):** אף משתמש במיראז' אינו מסומן בתפקיד `mashak` (רק `bakar` / `pakach` / `mefale`), ולכן תפריט המש"קים ריק באמת. מסומן במסך הניהול של מיראז' (`http://localhost:7300`), עמודת "תפקידים".

**QA:** `tsc` נקי ✅ · vitest **751/751** (45 קבצים) ✅ - 10 חדשות ל-`matchCrewOptions` ב-[StationCrewForm.test.ts](src/components/shared/StationCrewForm.test.ts), נכתבו **אדומות לפני המימוש** (10/10 נכשלו) · `npm run build` ✅ · **e2e אמיתי**: [station-crew](e2e/station-crew.spec.ts) "תפריט המשתמש" הורחבה - ✕ סוגר, Escape סוגר, ואז שמירה - עברה ✅. בנוסף אומת בהרצה מול תפריט מדומה (`page.route` על `mirage-crew`): שדה מש"ק ריק הציג את הודעת ההסבר, ובמסך נמוך (460px) תפריט של 40 שמות נפתח **כלפי מעלה** ונשאר בתוך הכרטיס (menu.y=180 < input.y=316) ✅.

---

## 2026-08-02 - שורת "מחושב מהמפה" בעברית מלאה, ומתאר האזורים המחוברים יורד ב"עוזב אזור"

**הבקשות:** (1) "תסדר את הטקסט - תשאיר בעברית כי האנגלית הופכת. תכתוב לדוגמה 350 קשר · 4 מייל +10% נותן XXX". (2) "בעזיבת אזור ביקשתי להעלים מתצוגה את הפוליגון של האזורים המחוברים שרלוונטיים לאותו פ"מ".

**מה היה:**

1. **השורה הורכבה מרסיסי JSX** ב-[TransferFormModal](src/components/dashboard/AdminDashboard.tsx): `tr('dashboard.etaComputedFromMap')` + `"16 NM"` + `tr('shared.knots')` + `+10%`. שתי בעיות: `NM` צמוד למספר בתוך פסקה RTL נשבר לריצת bidi נפרדת והמספר מוצג בנפרד מהמונח; וסדר המילים היה **קבוע בקוד** - תרגום לא יכול לנסח מחדש.
2. **המתאר המקיף** של פ"מ המחובר לכמה אזורים ([SectorDashboard](src/components/views/SectorDashboard.tsx)) צויר לכל שיוך עם 2+ אזורים, בלי תלות בסטטוס - כולל פ"מ ב"עוזב אזור", כלומר סימון תפוסה שכבר אינה קיימת.

**מה נעשה:**

- `tr(key, params)` מקבל הצבות (i18next interpolation, `escapeValue:false` כבר מוגדר). המשפט **השלם** עבר ל-registry: `"מחושב מהמפה: {{speed}} קשר · {{dist}} מייל · +{{pct}}% נותן {{minutes}} דק'"` - עברית בלבד, וסדר המילים ניתן לשינוי בזמן ריצה בלי build.
- `etaFromZone` הפך ל-`"מאזור {{zone}}"` (היה `"מאזור"` + span נפרד).
- בציור המתאר: `if (a.status === 'עוזב אזור') return null`.

**החלטות:**

- **`{{pct}}` ולא "10%" קשיח** - הערך נגזר מ-`ROUTE_FACTOR` ב-[eta.ts](src/utils/eta.ts). מחרוזת קשיחה הייתה משקרת ביום שהמקדם ישתנה.
- **הסינון של "עוזב אזור" יושב ב-`map` ולא ב-`filter`** של `multi`. האינדקס `i` הוא אינדקס הצבע ב-`HULL_PALETTE`; סינון מוקדם היה מזיז את הצבעים של **כל שאר** הפ"ממים ברגע שאחד עוזב, וצבע = זהות במסך תפעולי.
- **שבר עשרוני מתחת ל-10 מייל** - עיגול לשלם הציג "0 מייל נותן 1 דק'" בנקודת העברה קרובה.
- **שומר ה-i18n הורחב**: `\btr\('key'\s*[,)]` - קודם הוא תפס רק קריאה בלי הצבות, וקריאה עם `params` הייתה חומקת מבדיקת המפתחות היתומים.

**QA:** `tsc` נקי ✅ · vitest **741/741** (45 קבצים) ✅ · `npm run build` ✅ · המשפט אומת בהרצה: `מחושב מהמפה: 350 קשר · 16 מייל · +10% נותן 3 דק' · מאזור 81 דרום`. **לא אומת ויזואלית** במפה חיה - היעלמות המתאר ב"עוזב אזור" מחייבת אישור המפעיל.

---

## 2026-08-02 - כניסה לעמדה = הרכב חדש: הטופס נפתח נקי, רק הבקר/פקח שנכנס

**הבקשה:** "בכניסה מחודשת לעמדה מבך נתוני א"צ נוספים צריך להיות ריק למעט הבקר / פקח שעם המשתמש שלו נכנסנו".

**מה היה:** `useSessionRoles` טען את `workstation_session_roles` של העמדה **בשני** המסלולים - גם ב"עדכון חברי העמדה" וגם בעליית עמדה. בכניסה מחודשת הטופס נפתח מלא באנשי המשמרת הקודמת (כולל דגלי "קיים משגיח" ושדות המושגח הפתוחים), והנכנס היה צריך למחוק שדה-שדה. מי שלא מחק - רשם צוות שגוי בשקט, וזה מקור הנתונים של מסך הכשירויות ושל התחקירים.

**מה נעשה:** המצב ההתחלתי הופרד לפי המסלול, ב**נקודה אחת** - `initialSessionRoles(saved, defaultBakar, fresh)`:

- **עליית עמדה** (`mode === 'enter'`) - `fresh`: הטופס נפתח על `EMPTY_SESSION_ROLES` עם `bakar` = המשתמש שאיתו נכנסנו. גם כשבמשמרת הקודמת ישב שם אחר, השדה מציג את הנכנס.
- **עדכון חברי העמדה / תחקיר** - ללא שינוי: ההרכב השמור נטען כמו שהוא, כי הוא ההרכב שיושב בעמדה **עכשיו**. `bakar` נופל למשתמש הנוכחי רק כשהוא ריק.

**החלטות:**

- **הרכיב נשאר משותף אחד.** ההבדל בין המסלולים הוא המצב ההתחלתי בלבד - אותם שדות, אותה לוגיקה, אותו עיצוב (`/ui-adapt`, DRY). ה-flag יורד מ-`mode` שכבר קיים בטופס, ולא מפרמטר חדש שהקורא צריך לזכור להעביר.
- **גם הדגלים מתאפסים,** לא רק השמות. דגל "קיים משגיח" דלוק עם שדה ריק הוא מצב ביניים חסר משמעות, והטופס אמור להיפתח קצר בדיוק כמו בעמדה שמעולם לא נרשמה בה משמרת.
- **קש"פ מתנקה גם הוא.** הוא נשמר באותה שורת משמרת ומתחלף איתה; להשאיר מספר של משמרת קודמת זה בדיוק סוג הנתון השגוי-בשקט שהבקשה באה למנוע. אם בפועל הוא קבוע לעמדה - מקומו בהגדרות העמדה ולא ברשומת המשמרת.
- **במסלול הכניסה כבר לא נשלח `GET`** של חברי העמדה - ממילא כל מה שהוא מחזיר נמחק. הטופס נפתח בלי המתנה לרשת (ה-`loading` יורד מיד), ורשומת ה-DB לא נפגעת: ה-`PUT` שבאישור דורס את השורה כולה.

**QA:** `tsc` נקי ✅ · vitest **741/741** (45 קבצים) ✅ - 7 חדשות ב-[StationCrewForm.test.ts](src/components/shared/StationCrewForm.test.ts), נכתבו **אדומות לפני המימוש** (7/7 נכשלו) · `npm run build` ✅ · **e2e אמיתי** ([station-crew](e2e/station-crew.spec.ts) 4/4): הבדיקה מזהמת בכוונה את `workstation_session_roles` של העמדה בהרכב מלא דרך ה-API, נכנסת לעמדה דרך המסך, וקוראת את הערכים **מה-DOM** - `bakar` = "בודק אוטומטי", כל שאר שדות ה-`data-crew-field` ריקים, ואין שדות מושגח. TDD אמיתי: הבדיקה **נצפתה אדומה** מול הקוד הישן (`bakar` = "בקר קודם") ✅.

**מלכודת שחזרה:** כישלון e2e אחרי שהקוד כבר תוקן - `reuseExistingServer` שימר שרת **vite** ישן מהרצה קודמת שהגיש את המודול הישן. אותו כלל של השרת ב-3001 חל גם על 5199: כישלון שלא מסתדר עם הקוד = להרוג לפי **פורט** ולהריץ מחדש.

---

## 2026-08-02 - אותו פ"מ בשתי עמדות: הערה פר-עמדה + הפ"מ לא נעלם מדסק העמדה השנייה

**הבקשה:** "2 עמדות שונות - שתיהן גוררות את אותו פ"מ לדסק שלהן וכל אחת כותבת הערה אחרת ו/או שמה בנקודת העברה אחרת - זה צריך לאפשר".

**מה היה:** החזקת אותו פ"מ בשני דסקים כבר נתמכה ([strip_table_assignments](server/db/init.js) הוא many-to-many), אבל שתי שכבות מעליה שברו אותה:

1. **ההערה גלובלית.** `strips.notes` היא עמודה אחת. עמדה ב' דרסה את ההערה של עמדה א' בלי אינדיקציה, ותוך מחזור polling אחד גם המסך של א' התעדכן להערה של ב'.
2. **הפ"מ נעלם.** ב-[unifiedStrips.ts](src/utils/unifiedStrips.ts) הענף של `pending_transfer` היה **short-circuit לפני** בדיקת השיוך המפורש. מכיוון ש-`strips.status` גלובלי, ברגע שעמדה א' שמה את הפ"מ בנקודת העברה הוא נעלם לחלוטין מהדסק של עמדה ב' - לא "מסומן בהעברה", נעלם - בדיוק ברגע התפעולי שבו הוא הכי נחוץ לה.

**מה נעשה:** טבלה `strip_station_notes` בעלת PK `(strip_id, preset_id)`, עמודה `station_note` בקטלוג השדות, ותא עריכה בטבלת הדסק. ב-`unifiedStrips` השיוך המפורש נבדק לפני `pending_transfer`. הדסק של כל עמדה עצמאי: העברה של אחת אינה מרוקנת את השנייה.

**החלטות:**

- **טבלה נפרדת, לא עמודה ב-`strip_table_assignments`** - למרות שזו לכאורה השורה הנכונה (אותו PK בדיוק). שלוש סיבות מהקוד: `reset-placement[-preset]` מוחק את הטבלה ההיא בסיטונאות ([strips.js](server/routes/strips.js)) ו"ניקוי הצבות" של מנהל היה מוחק את כל ההערות; הסרת פ"מ מהדסק מוחקת את השורה; ופ"מ שנמצא בדסק דרך **התאמת query** אינו מחזיק שורת שיוך בכלל ולא היה לו למה לתלות הערה. UPSERT על אותה טבלה גם היה הופך כתיבת הערה ל"נעיצה" סמויה של הפ"מ לדסק.
- **ההערה המשותפת נשארה.** `strips.notes` לא נגעה - שתי עמודות נפרדות בטבלה ("הערות" משותפת, "הערת עמדה" פרטית). כל מה שקורא אותה (טבלה, כרטיס, ייצוא, [queryBuilder](src/utils/queryBuilder.ts)) לא נגע.
- **תת-שאילתות סקלריות ב-`/api/strips/global`**, לא JOIN שני. שני JOINים מצטברים היו מכפילים שורות זה מול זה ו-`jsonb_object_agg` היה נופל על `duplicate key`. נמדד: השאילתה החדשה **מהירה ב-7.5%** מהישנה (חציון 78.0ms מול 84.3ms, 87 פ"ממים) - ה-`GROUP BY` שהוסר עלה יותר משתי תת-שאילתות על PK.
- **תיקון הסדר ב-`unifiedStrips` הוא נקודת שינוי אחת** במקום 35 השימושים ב-`pending_transfer` שפזורים ב-SectorDashboard. הקובץ טהור ובר-בדיקה, ולכן התיקון מכוסה ב-5 בדיקות חדשות שנכתבו לפניו (TDD).
- **מה שלא נעשה, במכוון:** שתי עמדות ששמות את אותו פ"מ בשתי נקודות העברה **שונות** עדיין מייצרות תופעת לוואי גלובלית - `POST /api/strips/:id/transfer` כותב `strips.status='pending_transfer'` ו-`workstation_preset_id` על השורה המשותפת. ניתוק זה הוא שינוי ב-state machine (35 שימושים + [queryBuilder](src/utils/queryBuilder.ts) + [ClassicViews](src/components/classic/ClassicViews.tsx)) ומחייב אישור CEO לפי `/transfer-logic`. גם מצב **מפה** נשאר חשוף לדריסה: `strip_zone_assignments` עם `UNIQUE(strip_id)` ו-`handleMove(toMap=true)` שמעביר בעלות.

**QA:** `tsc` נקי ✅ · vitest **734/734** (44 קבצים) ✅ (5 חדשות ב-[unifiedStrips.test.ts](src/utils/unifiedStrips.test.ts) + סיווג הטבלה ב-[env-tables.test.js](server/db/env-tables.test.js), נכתבו אדומות לפני המימוש) · `npm run build` ✅ · בדיקת בידוד הסביבות 8/8 ✅ · **נתונים אמיתיים:** מול השרת החי, שתי עמדות (דסק קמנ"ק 83, דסק קזת"א 49) על פ"מ s1087 - שתי הערות שונות התקיימו במקביל, עדכון אצל אחת לא נגע בשנייה, `strips.notes` המשותפת לא השתנתה, וניקוי אצל אחת השאיר את השנייה ✅. **לא אומת ויזואלית** - התא בטבלה מחייב הוספת העמודה "הערת עמדה" למוד טבלה במסך הניהול, ואישור המפעיל שראה אותו.

**ממצא צדדי (קדם לשינוי):** סנכרון סכמת `env_11` נכשל מאז 23.07.2026 בגלל שורה יתומה אחת ב-`env_11.strip_table_assignments` (`strip_id=1230` שאינו קיים). הסביבה כבר חסרה 3 טבלאות תפעוליות (`map_zone_operational_state`, `station_sessions`, `debriefs`) והטבלה החדשה היא הרביעית. נכשל סגור (`verifyEnvSchema` זורק בכניסה) ולכן אין זליגה, אבל סביבה 11 אינה שמישה עד ניקוי השורה.

---

## 2026-08-02 - זמן אוטומטי לנקודת העברה (טווח מהמפה המעוגנת)

**הבקשה:** "רוצה לעשות זמנים אוטומטיים. מעזיבת פ"מ לנקודת העברה ממפה. אם מפה מעוגנת ופ"מ מבקש לעזוב - קו פחות או יותר ישר לנקודת ההעברה שסימננו, לחשב את הטווח במיילים. מהירות מטוס קרב ~350 קשר, מסוק ותובלה 120 קשר. + עוד 10% כי לא באמת יטוס קו ישר. לפי זה לחשב עוד כמה זמן יגיע ולשים את הזמן הזה (משתמש יכול לעדכן)".

**מה היה:** בטופס ההעברה ([TransferFormModal](src/components/dashboard/AdminDashboard.tsx)) שדה "זמן עד לנקודת העברה" נפתח **תמיד על 0**, והבקר הקליד מספר מהראש בכל העברה. כל הנתונים לחישוב כבר היו במערכת ולא נוצלו: המפה מעוגנת (`maps.anchor1/2_lat/lon`), לנקודת ההעברה יש נ"צ (`map_transfer_points.lat/lon`), ולפ"מ שהונח על המפה יש נ"צ (`strips.map_lat/lon`, נגזר ב-`handleMove`).

**מה נעשה:** בפתיחת טופס ההעברה מחושב הטווח בקו ישר עד נ"צ נקודת ההעברה, ומתורגם לדקות לפי מהירות שיוט. השדה נפתח על הערך המחושב, ומתחתיו שורה שמראה **ממה הוא הורכב** - `32 NM · 350 קשר · +10% · מאזור 61 צפון` - כדי שהבקר יידע מה הוא דורס. אין מפה מעוגנת / הפ"מ לא על המפה / אין נ"צ לנקודה → השדה נפתח על 0, בדיוק כמו קודם.

**החלטות:**

- **הקו יוצא מהאזור, לא מסמל הפ"מ.** בבקשת הצוות: "תיקח קו ישר מהאזור או מהאזורים המחוברים, הקצר ביותר - המקום הכי קרוב לנקודת ההעברה מהפוליגון שבו נמצא הפ"מ". לכן `closestGeoOnPolygon` מחזיר את הנקודה על פוליגון האזור הקרובה ליעד (יעד **בתוך** האזור = טווח 0), והמינימום נלקח על פני האזור הראשי + `extra_zones`. ההבדל תפעולי ולא זניח: על מפה 24 האמיתית, פ"מ באזור "61 צפון" אל נקודת סקטור 2742 - 17.1 NM (3 דק') מהאזור מול 46.1 NM (9 דק') ממרכזו.
- **החישוב הגיאומטרי במישור מקומי סביב היעד** (מייל ימי לציר), לא ישירות על lat/lon: מעלת אורך מתקצרת עם קו הרוחב (~0.85 ב-32°N), וחישוב נאיבי היה מסיט את "הנקודה הקרובה" מזרחה/מערבה על צלעות אלכסוניות.
- **החישוב הוצא לקובץ טהור** [src/utils/eta.ts](src/utils/eta.ts) - לא בתוך רכיב בן 17K שורות. שם יושבים גם פענוח המיקום (נ"צ שמור → פין) ובחירת נקודת ההעברה לפי סקטור/תת-נקודה, כי **שם הבאגים**: מכפלת פיקסלים↔אחוזים, `left/top` של התמונה במכל, והמוסכמה ששכבת הפינים מפרשת ערך ≤1.5 כשבר של המכל ולא כפיקסלים.
- **הפוליגון מומר לנ"צ דרך עוגני המפה** ולא נלקח מ-`map_zones.polygon_geo` - השדה קיים בסכמה אך ריק בפועל (אזור אחד מתוך 25 במפה 24).
- **המהירות לפי הטייסת, דרך `getSquadronAircraftType` הקיים** - אותו מסווג שכבר קובע את אייקון המטוס על המפה. אין טבלת מהירויות שנייה ואין שדה חדש בפ"מ.
- **מהירות לכל סוג פ"מ, לא שני דליים.** אחרי סבב ראשון שבו כל מה שאינו קרב קיבל 120, הצוות נתן את הערכים המדויקים: קרב 350 · מסוק/תובלה/כטמ"מ/GA 120 · **אז"מ 90** · **מרסס 80** · **דאון 50** · **רחפן וטיסן 20** (חסרי משמעות תפעולית, ערך סמלי). הטבלה ב-`STRIP_TYPE_SPEED_KT`, מותאמת בהכלה לפי הסדר.
- **10% מוחלים על המרחק** (`ROUTE_FACTOR`), לא על הזמן - זו אותה תוצאה, אבל זה מה שהבקשה תיארה: המסלול בפועל ארוך מהקו הישר.
- `strip_type` נוסף לשני ה-serializers של הפ"ממים ([strips.js](server/routes/strips.js), [workstations.js](server/routes/workstations.js)) - הוא היה עמודה ב-DB שמעולם לא הוחזרה ללקוח, ובלעדיו **מסוק אזרחי היה מסווג כקרב** (אין לו טייסת).
- הערך הוא ברירת מחדל ולא נעילה. הבקר רואה מספר, לא סימן שאלה - ומשנה אותו כשהוא יודע יותר טוב (עקרון UX 1: פחות צעדים מהסדק).

**QA:** `tsc` נקי ✅ · vitest **727/727** ✅ (מתוכן 46 חדשות ב-[eta.test.ts](src/utils/eta.test.ts) - haversine מול מעלה=60NM, מהירות לכל סוג, תוספת ה-10%, מינימום דקה, פיקסל↔נ"צ עם `left/top`, בחירת תת-נקודה, מוסכמת ה-≤1.5, הנקודה הקרובה בפוליגון לצלע ולא רק לקודקוד, יעד בתוך האזור=0, ו-null בכל מסלול שבו חסר נ"צ) · `npm run build` ✅ · **נתונים אמיתיים:** מול ה-DB החי, מפה 24 המעוגנת - פ"מ "חנית" (טייסת 101 → F-16) אל 3 נקודות ההעברה שלה (6.8/32.9/23.6 NM → 1/6/4 דק'), ו-4 פ"ממים משויכי-אזור שהטווח שלהם נמדד מהפוליגון וקצר עקבית מהמדידה ממרכז האזור ✅.

---

## 2026-08-02 - חלונית הפ"ממים: בורר "לפי אזורים" / "רשימת פ"מ"

**הבקשה:** "בחלון פ"מ שתצוגת מפה עם אזורים תעשה בחירה למעלה האם לקבץ לפי אזורים או רשימת פ"מ רגילה. רשימת פ"מ רגילה הסדר יהיה לפי מי שבאוויר לפי זמן המראה מהמוקדם למאוחר, לאחר מכן מי שעל הקרקע לפי זמן המראה מתוכנן... לא צריך לקבץ, יש הפרדה בצבע - מי שבאוויר כחול".

**מה היה:** בעמדה עם `flight_zones_mode` חלונית הפ"ממים ([SectorDashboard.tsx](src/components/views/SectorDashboard.tsx)) **תמיד** קיבצה לפי אזורים - כותרת צבעונית לכל אזור + "ללא הקצאה", ומי שלא שויך נדחק לתחתית. כשהמפעיל רוצה לראות מי ממריא הלאה (סדר תפעולי לפי זמן), הקיבוץ עמד בדרך: אותה טיסה מופיעה תחת כל אזור ששויכה אליו, וכל אזור מתחיל מנייה חדשה.

**מה נעשה:** מעל הרשימה (רק כשהמפה עם אזורים) שני כפתורים - **לפי אזורים** (ברירת מחדל, ההתנהגות הקיימת) ו**רשימת פ"מ**: רשימה שטוחה בלי כותרות, קודם מי שבאוויר לפי זמן המראה מהמוקדם למאוחר ואחריהם מי שעל הקרקע לפי המראה מתוכנן. הבחירה נשמרת ב-localStorage לפי עמדה (`fz-sidebar-group-<presetId>`), כמו שאר מתגי התצוגה של אזורי הטיסה.

**החלטות:**

- **אין קיבוץ ואין כותרת "באוויר"/"על הקרקע"** - ההפרדה ויזואלית: או"ק של מי שבאוויר כבר מוצג על רקע כחול (`#1d4ed8`), ובמצב רשימה הוא נשאר הסימן היחיד. כותרת נוספת היא רעש (עקרון UX 2).
- **הסדר לא שוכפל.** אותו סדר "באוויר קודם, ואז לפי זמן המראה" הופיע ב-3 מקומות בקובץ; הוצא ל-[src/utils/stripOrder.ts](src/utils/stripOrder.ts) (`compareAirborneThenTakeoff`) ומשמש את שלושתם - הרשימה הרגילה, מצב הטבלה, ושובר-השוויון בתוך אזור בקיבוץ. פ"מ בלי זמן המראה נדחף לסוף **הקבוצה שלו**, לא לתחילתה.
- הבורר צבוע דרך `T.*` + `lightMode` (נכון באור/שחור/כחול), `direction: dir` ובלי מידות פיזיות - לפי /ui-adapt. הטקסטים ב-registry `ctrl` (`fzListByZones`, `fzListFlat`, `fzListViewLabel`).

**QA:** `tsc` נקי ✅ · vitest **681/681** ✅ (מתוכן 6 חדשות ב-[stripOrder.test.ts](src/utils/stripOrder.test.ts) - באוויר לפני קרקע גם כשההמראה מאוחרת, מיון בתוך כל קבוצה, שתי הקבוצות יחד, וחסר/לא-תקין בסוף הקבוצה) · `npm run build` ✅.

---

## 2026-08-02 - הרחבת תחקיר ברשימת התחקירים

**הבקשה:** "ברשימת תחקירים להוסיף הרחבת תחקיר בשביל לראות את כל הנתונים והתמונות".

**מה היה:** טבלת התחקירים ([ManpowerPage.tsx](src/components/manpower/ManpowerPage.tsx)) הציגה שבע עמודות - עמדה, זמן ארוע, סיווג, מהות, מעורבים, נרשם ע"י וזמן רישום. **חברי הצוות, פירוט התחקיר, פירוט האחריות ותמונת העמדה נשמרו ולא היו נראים בשום מקום** - התחקיר נכתב ולא ניתן היה לקרוא אותו במלואו.

**מה נעשה:** לחיצה על שורה (או על ה-▸ שבתחילתה) פותחת מתחתיה פאנל עם כל מה שאינו עמודה: חברי הצוות כצ'יפים לפי סדר התפקידים של `CrewFields`, פירוט התחקיר, פירוט האחריות ותמונת העמדה. לחיצה על התמונה פותחת אותה בגודל מלא; Esc או לחיצה סוגרים.

**החלטות:**

- **התמונה נמשכת רק בפתיחה.** `GET /api/debriefs` מחזיר `has_screenshot` בלבד (dataURL של מסך מלא הוא עשרות KB לשורה), ולכן ההרחבה מושכת את הרשומה המלאה מ-`GET /:id` - פעם אחת לכל תחקיר, ורק אם יש תמונה. שורה עם תמונה מסומנת 📷 עוד לפני הפתיחה, כדי שלא צריך לפתוח כדי לגלות.
- **הפאנל אינו משכפל עמודות.** מעורבים, סיווג וזמנים כבר בשורה שמעל; חזרה עליהם היא רעש (עקרון UX 2 - מינימום רעש). השורה + ההרחבה יחד = כל מה שנרשם בטופס.
- **תווית שורת האב לפי סוג העמדה** - "בקר" ביב"א ו"פקח" במגדל, לפי `preset_role` (שפה משותפת). לשם כך נטענת רשימת העמדות פעם אחת עם הרשימה.
- הפאנל מציג את הצוות מתוך ה-snapshot שבתחקיר (`crew` JSONB) - לא מהעמדה **היום**. זו כל הנקודה של תחקיר.

**QA:** `tsc` נקי ✅ · vitest **678/678** ✅ · `npm run build` ✅ · e2e [manpower.spec.ts](e2e/manpower.spec.ts) - בדיקה חדשה שכותבת תחקיר עם צוות/פירוט/אחריות/תמונה דרך ה-API, פותחת את השורה ומאמתת שכולם מוצגים, שהתמונה נמשכה (`src` הוא `data:image/`), שהלחיצה עליה פותחת גודל מלא ו-Esc סוגר, ושכיווץ מחזיר את השורה למצבה - **3/3 עברו** ✅ · אומת ויזואלית ב-24" (‎--s:1.65‎): פריסת שתי עמודות, התמונה לא חורגת מהמסך בגודל מלא ✅.

---

## 2026-08-02 - ⋮ של פ"מ עבר לצד תגית הסטטוס

**הבקשה:** "את השלוש נקודות שים מצד ימין של הסטטוס (בדרך / באזור וכו')".

**מה היה:** אחרי המעבר מ-`top:-5` ל-`top:100%` (הרשומה מ-2026-08-01) הכפתור אמנם לא כיסה את האו"ק, אבל ישב בשורה שלישית משלו מתחת לתגית הסטטוס - הפין נעשה גבוה, ועל מפה צפופה שלוש השורות של פינים שכנים נדחסות זו בזו.

**התיקון ([SectorDashboard.tsx](src/components/views/SectorDashboard.tsx)):** תגית הסטטוס וה-⋮ יושבים יחד בשורת flex אחת. ה-⋮ ראשון ב-DOM והשורה מקבלת `direction: dir`, ולכן הוא בצד ההתחלה - **ימין בעברית**, שמאל באנגלית (מתמרן עם השפה, כמו כל שאר הרכיבים). הפין חזר לשתי שורות בלבד.

**החלטות:**

- **כשאין תגית סטטוס** (תצוגת אייקון, או סטטוס שאינו אחד מארבעת המוכרים) הכפתור נשאר `absolute` מתחת לתוכן וממורכז - בדיוק כמו קודם. שם אין למה להיצמד, והעוגן על המפה חייב להישאר על הטקסט/אייקון.
- מטא-הסטטוס (`fzStatusMeta`) והסגנון+ההנדלרים של הכפתור (`fzMenuBtnStyle`/`fzMenuBtnProps`) חושבו פעם אחת לפני ה-JSX ומשותפים לשני המיקומים - אותה התנהגות בדיוק, בלי שכפול.

**QA:** `tsc` נקי ✅ · vitest **643/643** ✅ · `npm run build` ✅ · e2e [fz-pin-menu.spec.ts](e2e/fz-pin-menu.spec.ts) עודכן לדרישה החדשה ומודד על המסך: חפיפה אנכית עם התגית ומרכוז לגובהה, `menu.left ≥ tag.right` ב-RTL (ומראה הפוכה ב-LTR), ואי-כיסוי של האו"ק שמעל ✅.

---

## 2026-08-02 - הערות והצעות מהשטח + חלון עזרה לעמדה (מחלון "אודות")

**הצורך:** שני דברים שחסרו בחלון שנפתח מסמל המערכת: (1) למפעיל בשטח אין דרך להעביר הצעה או הערה למפתחים בלי לתפוס מישהו בטלפון; (2) עמדה נבנית לפי תצורה, ולמפעיל חדש אין מי שיסביר לו מה כל כפתור עושה **בעמדה שלו**.

**מה נעשה:**

- **הערות והצעות** - סעיף חדש בחלון האודות עם כפתור `+` שפותח טופס: שם מלא (ממולא מראש משם המפעיל המחובר), טלפון, יחידה, נושא ופירוט. נשמר בטבלה חדשה `suggestions` ([server/routes/suggestions.js](server/routes/suggestions.js)) יחד עם העמדה ששלחה. **התאריך והשעה נרשמים בשרת** (`created_at DEFAULT NOW()`, TIMESTAMPTZ) ולא נשלחים מהלקוח - כך אין תלות בשעון התחנה.
- **רשימה למנהל המערכת הטכני** - טאב "הערות והצעות" במסך הניהול (admin בלבד): כל ההצעות מהחדשה לישנה, עם שולח/טלפון/יחידה/עמדה/תאריך-שעה, סינון לפי סטטוס (חדשה · בטיפול · בוצעה · נדחתה), הערת מנהל ומחיקה. תוכן ההצעה עצמה אינו ניתן לעריכה - רק הטיפול בה.
- **חלון עזרה** - כפתור "עזרה" בפוטר של חלון האודות פותח הסבר **ממוספר** על כל חלק בעמדה. הסינון הוא לב העניין: [src/utils/helpTopics.ts](src/utils/helpTopics.ts) מחזיק נושא לכל רכיב + תנאי תצוגה **זהה לתנאי שמרנדר אותו** ב-SectorDashboard, ולכן כפתור שלא נבחר בבניית העמדה (דש בורד, ספרורים, כל המכלול, שכבות/רכבים בעמדת שדה, שידוך בלחיצה) גם לא מקבל סעיף. הספרור רץ אחרי הסינון, ויש חיפוש חופשי.
- **הרחבה (באותו יום, לבקשת המשתמש): כיסוי מלא של המסך.** העזרה עברה למבנה **דו-שכבתי** - נושא ממוספר `n` והכפתורים שבתוכו `n.m`: כל תפריט (משתמש · יצירה · תצוגה · הגדרות עמדה · שם העמדה) מפרט את הכפתורים שבו ומה כל אחד עושה, ונוספו חלון הפ"ממים (כולל כפתור השאילתה על שלושת מצביו), חלון נקודות ההעברה (מוסר/מקבל, אשר · קלטתי · דחה עם סיבה), חלון העזרים (בלוקים, מדיניות, מסלולים, נתיבי הסעה, ATIS, קשרים, מצבי בסיס, רשימת תיוג, בד"ח), המפה (אזורים, בלוקי גובה, פינים, סימון התנגשות, נקודות העברה, תפריט אזור בלחיצה ימנית), סרגל הכלים של המפה (בהירות, זום, מפה עיוורת, ציור, סגירות, צורת הפין וגודלו) ו**מונחים** (פ"מ, נקודת העברה, מוסר, מקבל, נקודה זמנית, אזור, בלוק, ספרור, בד"ח, מז"א, עומס, איחוד עמדות). בעמדה שנבדקה: **104 סעיפים**. הפריטים מקופלים כברירת מחדל עם "פתח הכל", והחיפוש **שומר את המספור המקורי** כדי ש"סעיף 13.8" יישאר אותו סעיף.

**החלטות:**

- `suggestions` מסווגת **קונפיג** ב-[env-tables.js](server/db/env-tables.js) ולא תפעולית: הצעה שנשלחה מתוך סביבת תרגול חייבת להגיע לאותה רשימה של מנהל המערכת, ולא להימחק עם שחרור הסביבה.
- שני הרכיבים החדשים ([SuggestionForm.tsx](src/components/shared/SuggestionForm.tsx), [HelpModal.tsx](src/components/shared/HelpModal.tsx)) שואבים פלטה מ-`crewPalette` הקיים - אין פלטה רביעית במערכת, והם נכונים בשלוש התמות (נבדק ויזואלית באור/שחור/כחול). `vh` מחולק ב-`var(--s,1)` לפי /ui-adapt.
- כל הטקסטים ב-registry החדש `suggest`/`help` - ניתנים לעריכה ממסך ניהול התרגומים בלי build.

**QA:** `tsc` נקי ✅ · vitest **678/678** ✅ (12 ל-`helpTopics` - כולל "כל נושא **וכל כפתור** נגישים באיזשהו סוג עמדה", "כפתור שלא הוגדר לעמדה לא מקבל סעיף" ו"פריט בתוך תפריט נעלם לפי אותו תנאי שמרנדר אותו") · `npm run build` ✅ · אימות API מול ה-DB האמיתי: POST/GET/PATCH/DELETE, ולידציה (400 בלי שדות חובה), סטטוס לא חוקי נדחה, `created_at` timestamptz, הטבלה ב-public בלבד ✅ · e2e חדש [about-suggestions-help.spec.ts](e2e/about-suggestions-help.spec.ts) - **4 בדיקות**: הטופס נשמר ל-DB עם חותמת זמן; העזרה ממוספרת ברצף, המונה כולל את סעיפי המשנה, "פתח הכל" חושף בדיוק את כל הכפתורים, החיפוש מוצא גם כפתור בתוך תפריט, וההתאמה לסרגל נבדקת אחד-לאחד; ההצעה מופיעה למנהל ברשימה וניתן לשנות סטטוס; אנימציית הלחיצה רצה לפני שהחלון נפתח ומסתיימת מעצמה, לכל סעיף יש שורת מיקום, ו"הצג לי" מציב טבעת **על הכפתור האמיתי** (נמדד: `ring.x = target.x - 6`, `ring.width = target.width + 12`) וחוזר לעזרה ✅ · אימות ויזואלי בשלוש התמות, ומדידת אי-גלישה של תווית ה"הצג לי" ב-15.6" וב-24" אמיתי (2560px, ‎--s=1.65‎) ✅.

- **הרחבה שנייה (לבקשת המשתמש): אנימציה בלחיצה + "איפה זה על המסך".**
  - לחיצה על סמל המערכת מפעילה **אנימציית פינג מכ"ם** ([SkyKingLogo.tsx](src/components/shared/SkyKingLogo.tsx), prop `launching`): שני גלים שנפתחים מהסמל החוצה + קפיצת סקייל. הגלים יושבים **מחוץ ל-SVG** ב-`absolute` - בתוכו הם נחתכו בגבול ה-viewBox ולא נראו. החלון נפתח **אחרי** 380ms, אחרת הוא ממורכז ומכסה את הסמל והאנימציה לא נראית כלל.
  - לכל נושא בעזרה נוסף **מיקום** (`whereKey`, מוצג עם 📍): "בקצה הימני של הסרגל העליון", "בפינה השמאלית העליונה של המפה", "בצד שמאל של המסך, לצד המפה"...
  - **"הצג לי"** ([HelpSpotlight.tsx](src/components/shared/HelpSpotlight.tsx)): החלון מתפנה, המסך מוחשך, והרכיב האמיתי מקבל טבעת ציאן פועמת + תווית עם שמו ומיקומו וכפתור "חזרה לעזרה". העוגן הוא `data-help="<topicId>"` על הרכיבים עצמם - מקור אמת יחיד, ובדיקת יחידה **סטטית על המקור** מוודאת שכל נושא-מסך נושא עוגן ושאין עוגן יתום (שינוי מזהה היה מבטל את ההצבעה בשקט).
  - שתי מלכודות סקייל שנתפסו במדידה: `innerWidth/Height` חייבים חלוקה ב---s בדיוק כמו הקואורדינטות, ו-`boxSizing: border-box` לתווית - בלעדיו ה-padding הוסיף 32px לרוחב והתווית גלשה מקצה המסך כשהרכיב בצד ימין.

**פתוח:** התהליך המבצעי שיתווסף לחלון העזרה - המשתמש ימסור את תוכנו בהמשך.

---

## 2026-08-02 - שידוך פ"מ בלחיצה (חלופה לגרירה) במסך אזורי הטיסה

**הצורך:** על עמדת ה-Cintiq, גרירת פ"מ מהסרגל אל אזור בעט היא פעולה ארוכה ומועדת להחלקה. הבקשה: מתג בהגדרות עמדה שמאפשר את אותה פעולה בשתי לחיצות.

**מה נעשה** ([SectorDashboard.tsx](src/components/views/SectorDashboard.tsx)):

- מתג **"שידוך בלחיצה"** בתפריט `⚙ הגדרות עמדה`, מוצג רק במצב אזורי טיסה. מותמד ב-`localStorage` תחת `fz-pair-mode-<presetId>` (מתג תצוגה מקומי לעמדה, בלי DB).
- כשהמתג דלוק: לחיצה על פ"מ בסרגל **או** על פין במפה בוחרת אותו (מסגרת ציאן פועמת + שורת הנחיה). לחיצה שנייה על היעד שקולה לשחרור גרירה שם:
  | יעד | תוצאה |
  |------|--------|
  | אזור במפה (פנים הפוליגון **או קו המתאר**) | שיוך לאזור בנקודת הלחיצה |
  | נקודת העברה (סמן מלא/חץ במפה, כרטיס שכן בפאנל) | סימון "עוזב אזור" + פתיחת טופס ההעברה |
  | חלון הפ"ממים (הסרגל) | ניתוק מכל האזורים ששויכו + חזרה לרשימה |
- `Esc` או לחיצה על שורת ההנחיה מבטלים את הבחירה. הגרירה הישנה **לא השתנתה** - שתי הדרכים חיות זו לצד זו, וכשהמתג כבוי ההתנהגות זהה לקודם (כולל מחזור הסטטוס בלחיצה על פין).

**DRY - שלוש כפילויות שנסגרו תוך כדי:**

- `FZ_SECTOR_SELECTOR` + `fzSectorIdOf` + `fzTransferToSector`: זיהוי "נקודת העברה מתחת לנקודה" והטיפול בה היו משוכפלים **חמש פעמים** (שלוש ב-`handleFzPinPointerUp`, שתיים ב-`handleFzMapDrop`) עם אותו גוף בדיוק. עכשיו מקור אמת אחד, שמשרת גם את הגרירה, גם את הדגשת היעד וגם את השידוך בלחיצה. `[data-transfer-sector]` נשמר בסלקטור אף שאינו מרונדר כרגע - הוא נבדק בקוד הקיים.
- גיאומטריית הפגיעה עברה ל-[src/utils/zoneHit.ts](src/utils/zoneHit.ts) (`pointInPolygon`, `distToSegment`, `zoneAtPoint`, `zoneAtPointOrEdge`) עם 18 בדיקות יחידה. `fzPointInPolygon` הפנימי הוסר, `fzGetZoneAtPoint` רק עוטף. הגרסה עם ה-`tol` היא זו שמאפשרת "או על הקו" - הסובלנות נמדדת ב**אחוזי מפה** ולכן אינה משתנה עם זום המפה או עם סקייל המסך.

**איפה הלחיצה נתפסת:** לא בשכבת ה-overlay - היא הייתה חוסמת את גרירת המפה. הלחיצה על המפה נתפסת בסוף `startMapPanDrag` (תזוזה < 8px = לחיצה, אחרת גרירת מפה כרגיל), ולכן היא נהנית מהכלל הקיים "יישות תופסת את הלחיצה שלה, השאר נופל לשכבת הגרירה". יעדים שמחוץ למפה נתפסים ב-listener אחד ברמת `document` ב-capture, שמזהה את היעד ב-`elementsFromPoint` (ולא ב-`ev.target`) - כי `setPointerCapture` של היישויות מזיז את יעד ה-click.

**סמן ייעודי על המפה** (בקשת המשך): בעמדה יש שלוש משמעויות לחיצה, ולכן שלוש **צורות** סמן שנקראות בזווית העין - יד (grab) לוקחת יישות, כוונת **עגולה** מזיזה את המפה, ו**ריבוע** משדך. שני מצבים ב-[pairCursor.ts](src/utils/pairCursor.ts): ריבוע חלול אפור = "המצב דלוק, בחר פ"מ"; ריבוע ציאן מלא עם כוונת = "נבחר פ"מ, הלחיצה הבאה משייכת כאן". שניהם עם קו מתאר שחור, לקריאות גם על מפה בהירה. אותה תבנית של סמן הפאן: SVG במקום אחד → `encodeURIComponent` → משתנה CSS על ה-body → כלל ב-[App.css](src/App.css). הסמן חל על `[data-map-panel]` בלבד (מחוץ למפה הסמן רגיל), ופקדי המפה נשארים `pointer`. **מלכודת specificity:** הסלקטור של השידוך (0-2-1) חזק מזה של `body.map-panning` (0-1-1), ולכן הריבוע היה נשאר תקוע גם בזמן גרירת מפה - נדרש כלל מפורש `body.map-panning.fz-pairing` (0-3-1) שמחזיר את כוונת הפאן.

**QA:** `tsc` נקי ✅ · vitest **654/654** ✅ (13 חדשות ב-[pairCursor.test.ts](src/utils/pairCursor.test.ts): data-URI תקין, נקודת אחיזה במרכז, `<rect>` ולא עיגול, ושני המצבים נבדלים זה מזה ומסמן הפאן) · `npm run build` ✅ · e2e חדש [fz-click-pairing.spec.ts](e2e/fz-click-pairing.spec.ts) - שתי בדיקות מול ה-API האמיתי: (1) הדלקת המתג → **`getComputedStyle` על לוח המפה מחזיר את סמן ה-idle** → בחירת פ"מ → **הסמן מתחלף ל-armed** → לחיצה במרכז אזור → **השיוך נוצר ב-DB עם ה-zone_id הנכון** → לחיצה על חלון הפ"ממים → **השיוך נמחק**; (2) לחיצה על נקודת העברה פותחת את טופס ההעברה (הטופס נפתח לפני כל כתיבה, ולכן הבדיקה חסרת תופעות לוואי) ✅ · רגרסיה: `map-pan-drag` 4/4 (כולל "לחיצה על פ"מ לא גוררת את המפה"), `fz-pin-menu`, `map-transfer-points` ✅.

**פתוח:** במצב שידוך, לחיצה על פ"מ בסרגל תופסת את הלחיצה ולא פותחת את כרטיס הפ"מ - זו המשמעות של מצב. אם יתברר שצריך את שניהם, המקום הוא לחיצה כפולה לפתיחה.

---

## 2026-08-01 - הסרת בורר גודל המסך ממסך הכניסה

**מה נעשה:** בורר גודל המסך (15.6"/16"/18"/24") ירד מ-`WorkstationLogin` ([src/App.tsx](src/App.tsx)) יחד עם ה-state, האזהרה "נא לבחור גודל מסך" ושלושת מפתחות ה-i18n (`login.screenSize*` ב-he/en).

**למה זה בטוח:** סקריפט ה-boot ב-[index.html](index.html#L14-L22) כבר מחשב את הסקייל לבד - קורא `bt-screenSize` מה-localStorage, ואם אין ערך מזהה את האלכסון בפועל (`screen.width/height × devicePixelRatio`) ומכוון `--s` ו-`data-screen`. הבורר היה שכבה ידנית מעל מנגנון קיים, וצעד מיותר לפני כל כניסה לעמדה. `SCREEN_SCALE_MAP` ב-[src/config.ts](src/config.ts) נשאר (מפת הערכים עצמה), רק הייבוא ב-App.tsx ירד.

**e2e:** ~20 בדיקות לחצו על הכפתור כדי לקבע `--s`. נוסף `setScreenSize(page, size?)` ל-[e2e/helpers.ts](e2e/helpers.ts) - `addInitScript` שכותב את `bt-screenSize` **לפני** ה-`goto`, כך שסקריפט ה-boot קורא אותו בטעינה (וגם בכל reload אחריו). כל אתרי הלחיצה הוחלפו בקריאה הזו; `loginToWorkstation` משתמש באותה פונקציה, ולכן הבדיקות הרגישות לסקייל (גרירת מפה ב-24", fit-scale) שומרות על `--s=1.65` דטרמיניסטי במקום להסתמך על גודל המסך של מכונת ההרצה.

**QA:** `tsc` נקי ✅ · vitest **612/612** ✅ · `npm run build` ✅ · e2e של מסך הכניסה (`leo-logo` - "מסך התחברות") עבר בלי הכפתור ✅ · אומת בדפדפן ש-`setScreenSize('24"')` → `--s=1.65` ו-`data-screen=24` עוד לפני רינדור האפליקציה ✅.

**פתוח:** אין יותר דרך ידנית לעקוף זיהוי שגוי של גודל מסך (אין בורר במסך אחר). אם יידרש - המקום הנכון הוא הגדרות העמדה, לא מסך הכניסה.

---

## 2026-08-01 - זליגת תרגול→אמת באזורי מפה, ושרידות ל-failover של ה-DB

שני הממצאים שנפתחו בעבודת העמידות בנתק (ראה הרשומה הבאה), סגורים.

### 1. מצב תפעולי של אזור זלג מתרגול לאמת

**מה היה:** [maps.js](server/routes/maps.js) כתב את `active_alt_range_ids` ו-`limitation_note` ישירות ל-`map_zones`. הטבלה הזו מסווגת **קונפיגורציה** ולכן יושבת ב-`public` בלבד ומשותפת לכל 50 הסביבות - אבל שני השדות האלה נקבעים **חי בעמדה**, בקליק ימני. כלומר בקר בסביבת **תרגול** שהגביל גובה באזור שינה את האזור **האמיתי**. הסיווג ב-`env-tables.js` הוא ברמת **טבלה** ולא ברמת עמודה, ולכן `checkTableClassification` - שנועד בדיוק למנוע זליגה כזו - לא יכול היה לתפוס אותה.

**התיקון:** טבלה תפעולית `map_zone_operational_state` (`zone_id` PK → `map_zones` CASCADE). ההגדרה של האזור נשארת קונפיג, המצב החי מבודד פר-סביבה. זו בדיוק התבנית הקיימת של `blocks` (תוכן תפעולי) מול `block_spaces`/`block_tables` (מבנה קונפיג).

**חוזה ה-API לא השתנה:** `GET /api/map-zones` משרג את שני השדות חזרה לתוך האזור ב-`LEFT JOIN`, ולכן **אפס שינוי בלקוח**. העמודות נמנות במפורש ולא `z.*` - העמודות הישנות נשארו באותו שם, ו-`z.*` יחד עם ה-COALESCE היה מייצר שם עמודה כפול שמי מנצח בו תלוי בדרייבר. במסלול בטיחותי לא נשענים על זה.

**העמודות הישנות לא נמחקו** (אין DROP בלי אישור): הן נשארו deprecated לקריאה בלבד כגיבוי היסטורי, ו-`initDb` מעביר מהן פעם אחת את המצב הקיים (`ON CONFLICT DO NOTHING`). בפועל היו 0 שורות להעביר.

**TDD:** הבדיקה נכתבה ל-[env-isolation.integration.test.js](server/db/env-isolation.integration.test.js) ו**נצפתה אדומה** (`relation "public.map_zone_operational_state" does not exist`) לפני המימוש. היא יוצרת מפה ואזור **ייעודיים** ולא נוגעת בנתונים אמיתיים - הלקח מ-`map_transfer_points` שנדרסו בעבודה אמיתית (ראה למטה בקובץ).

### 2. שרידות ל-failover של ה-DB + readiness

**`GET /api/ready`** ([server/app.js](server/app.js)) - מוכנות אמיתית: 503 כל עוד `phase !== 'ready'` או שה-DB לא מגיב תוך 3ש'. `GET /api/health` נשאר **liveness** בכוונה (לא נוגע ב-DB, 200 בזמן עלייה) כדי ש-Railway לא יהרוג קונטיינר באמצע `initDb`. שני endpoints שונים כי הם עונים על שתי שאלות שונות, וערבוב שלהם הוא בדיוק מה שגורם ל-restart loop.

**שרידות ב-[pool.js](server/db/pool.js)** - כשה-DB עובר failover כל ה-connections מתים בבת אחת, ובלי טיפול כל העמדות מקבלות 500 יחד. עכשיו `SELECT` משודר שוב (2 ניסיונות, 120/400ms) על שגיאת connection (`57P01`, `08006`, `ECONNRESET`...).

> ⚠️ **כתיבה לעולם אינה משודרת שוב.** כשה-connection מת אי אפשר לדעת אם השאילתה הספיקה להתבצע בצד השרת, ושידור חוזר של `INSERT` היה משכפל פ"מ או העברת עמדה. `isReadOnlySql` הוא **fail closed**: רק `SELECT` מובהק, וגם CTE שמכיל `INSERT`/`DELETE` נפסל.

**מה שנשאר דורש תשתית ולא קוד** - standby ל-DB, שני מופעי אפליקציה מאחורי LB, יתירות רשת. מתועד ב-[ARCHITECTURE.md](ARCHITECTURE.md#יתירות-ו-failover) עם סדר עדיפות מומלץ.

**QA:** `tsc` נקי ✅ · vitest **612/612** ✅ (12 חדשות לסיווג השגיאות/שאילתות + בדיקת בידוד האזורים) · `npm run build` ✅ · **smoke מול Neon אמיתי** (7 בדיקות): עדכון חלקי משמר את השדה השני, GET מחזיר את שניהם משורגים, **העמודות הישנות נשארו נקיות**, אזור לא קיים → 404, גוף ריק → 400, ומחיקת מפה גוררת CASCADE ✅ · **smoke למוכנות:** בזמן עלייה `health=200` (liveness) מול `ready=503 {reason:"booting"}`, ואחרי העלייה שניהם 200 עם `dbLatencyMs` ✅

> **לקח על הרצת smoke:** ההרצה הראשונה בדקה בטעות את **הקוד הישן** - פורט 3001 היה תפוס משרת שנשאר מהרצת e2e (`reuseExistingServer`), ו-`node server.js` החדש נפל בשקט. זה נתפס רק כי צעד 5 בבדיקה הראה שהעמודות הישנות עדיין נכתבות. **בבדיקת smoke יש לוודא שהשרת שנבדק הוא זה שהורם** - להרוג לפי פורט (לא לפי שם התהליך, יש סוכנים מקבילים על פורטים אחרים).

---

## 2026-08-01 - עמידות בנתק: העמדה ממשיכה לעבוד כשכבל הרשת מנותק

**הבקשה:** "DB GAPI מעביר ל-DB SKY-KING שמעביר למערכת... שניהם יושבים בנתיב רקיע (רשת סודי ביותר ללא חיבור לאינטרנט)... נתק נוסף אפשרי בין DB SKY-KING לmחשב עצמו (כאילו ניתקתי את הכבל רשת). אני רוצה שגם במקרה קיצון זה נמשיך לעבוד על המערכת עם המידע שקיים באותו רגע - כמובן ללא שיתוף מידע בין העמדות. סוג של CASH מקומי."

**נתק GAPI↔DB - כבר עבד.** הבדיקה העלתה ש[server/gapi/sync.js](server/gapi/sync.js) כותב אירוע נכנס **ל-DB של SKY-KING** ולא מעביר אותו לאפליקציה, והאפליקציה קוראת אך ורק מ-DB SKY-KING. בתוספת אידמפוטנטיות (`event_id`+`version`), outbox עם `MAX_ATTEMPTS=12` ו-reconcile תקופתי - זה store-and-forward תקני. **לא נגענו.**

**נתק DB↔עמדה - לא עבד, ונכשל מסוכן.** שלושה ממצאים:
1. ברירת המחדל בהפצה הייתה **לקוח דק** - גם ה-HTML/JS נטענו מהשרת. כבל מנותק = אין אפליקציה לטעון.
2. [electron-main.cjs](electron-main.cjs) היה מתוכנן להיעצר: `did-fail-load` → מסך "אין חיבור לשרת" שמכסה את האפליקציה.
3. **הכי חמור:** אפס תשתית offline (`serviceWorker`/`indexedDB`/`navigator.onLine` = 0 מופעים), ושישה polls **מחקו מצב** בכישלון - `catch { setActiveTakeoffs([]) }` ודומיו. תוך 5 שניות מהנתק הבאנר הציג "אין המראות פעילות". זה **מידע שגוי אקטיבי**, גרוע ממסך שקרס: בקר לא מבחין שהוא לא יודע.

**חמש שכבות:**

**1. האפליקציה על העמדה** - [electron/stationServer.cjs](electron/stationServer.cjs): שרת סטטי זעיר שמגיש את `dist/` מהדיסק ומפרוקסס `/api` + `/driver` לשרת האמיתי. **למה שרת מקומי ולא `file://`:** תחת `file://` ה-origin הוא `null` ו-IndexedDB נחסם - וה-cache שלנו יושב עליו; הפרוקסי גם שומר את הבקשות same-origin ולכן `API_URL='/api'` ממשיך לעבוד **בלי לגעת במאות אתרי ה-fetch**. מאזין ל-127.0.0.1 בלבד, עם הגנת path traversal ו-502 מהיר במקום תקיעה עד timeout של TCP. מצב `bundled` מזוהה מהאריזה (יש `dist` ואין `server.js`) ולא מדגל שנשכח.

**2. cache מקומי** - [src/offline/](src/offline/): כל GET נשמר ב-IndexedDB ומוגש בנתק עם `x-skyking-from-cache` + חותמת. **IndexedDB ולא localStorage** - כתבי יד ותמונות מפה חורגים מ-5MB, ו-localStorage סינכרוני וחוסם את ה-UI thread.

**3. יירוט אחד במקום מאות עריכות** - [apiFetch.ts](src/offline/apiFetch.ts) עוטף את `fetch` הגלובלי. helper שקוראים לו ידנית היה מחייב לגעת בכל אתר קריאה ולהשאיר את מי שנשכח בלי הגנה - כלומר בדיוק ההתנהגות שבאנו לסגור. נעטף **אחרי** מתווך הסביבה כדי שהבקשה תישא `X-Env`.

**⚠️ ה-cache משויך לסביבה.** בלי זה תרגול ואמת היו חולקים רשומות - בדיוק הזליגה שמנגנון הסכמות בשרת בא למנוע. גם פריט outbox נושא את הסביבה שבה נרשם ונשלח אליה, גם אם המפעיל עבר בינתיים.

**4. כישלון רשת לא מוחק מצב** - תוקנו 6 אתרים: [SectorDashboard](src/components/views/SectorDashboard.tsx) (המראות פעילות, קונפליקטי מסלול, מפת השדה), [DraggablePanels](src/components/transfers/DraggablePanels.tsx) (טווחי גובה של השכן), [ManagementPage](src/components/admin/ManagementPage.tsx), [managers](src/components/admin/managers.tsx). האתר ב-`SectorDashboard:1785` **הושאר** - הוא `catch` סביב `JSON.parse` של localStorage, לא רשת.

**5. חיווי ומדיניות כתיבה** - [ConnectionBanner](src/components/shared/ConnectionBanner.tsx) מעל כל מסך: שעת המידע, שעון גיל **מתקתק**, "שיתוף בין עמדות מושבת", מונה פעולות ממתינות וחיווי נתק שו"ב. קורא את התמה מ-`body` ולכן לא דורש prop דרך ארבעה ענפי ניתוב. כתיבה: פרטית (כתב יד, העדפות, יומן ביקורת) → outbox; **משותפת → נחסמת** ב-503 `OFFLINE_SHARED_WRITE`. ברירת המחדל ב-[policy.ts](src/offline/policy.ts) היא `shared` (fail closed) - נתיב חדש שנשכח לא נשלח בשקט אחרי נתק ארוך. **למה חסימה ולא מיזוג:** אם עמדה A "קיבלה" פ"מ בנתק ובמקביל B העבירה אותו ל-C - אין רזולוציה נכונה, יש רק ניחוש.

**באג שנמצא בדרך (פתוח):** [maps.js:189](server/routes/maps.js#L189) כותב מצב תפעולי (`active_alt_range_ids`, `limitation_note`) ל-`map_zones` שמסווגת **קונפיגורציה** - כלומר עמדה בסביבת תרגול שמגבילה גובה **כותבת לאמת**. הסיווג ב-`env-tables.js` הוא ברמת טבלה ולא עמודה, ולכן הזליגה חומקת ממנו. נרשם כחוב טכני #9 ב-[ARCHITECTURE.md](ARCHITECTURE.md).

**מלכודת /ui-adapt שנתפסה במדידה:** רקע הבאנר הוא **צבע סטטוס קבוע** (ענבר `#f59e0b`) ולא משטח של התמה - ולכן גם הטקסט עליו חייב להיות קבוע. הגרסה הראשונה גזרה את הצבע מהתמה ("כהה→טקסט בהיר"), ונמדד `#f8fafc` על ענבר = **2.18:1**, מתחת לסף, דווקא בתמה הכהה שהיא ברירת המחדל בחדר בקרה. התיקון: טקסט כהה (`#1e293b`) בכל שלוש התמות = 6.5:1. הבדיקה **נצפתה אדומה** מול הצבע הישן לפני שתוקנה.

**QA:** `tsc` נקי ✅ · vitest **599/599** ✅ (78 חדשות: מדיניות, יירוט, בידוד סביבות, שרת העמדה כולל path traversal) · `npm run build` ✅ · **ניגודיות נמדדה ב-3 התמות** (dark/light/ocean, סף 4.5:1 של WCAG 1.4.3) ✅ · **בדיקת עשן על ה-build האמיתי**: שרת העמדה הגיש את `dist` (3.6MB bundle) והמשיך להגיש אותו כשה-upstream נפל, וה-`/api` החזיר 502 תוך **2ms** במקום להיתקע ✅ · **e2e אמיתי** ([e2e/offline-resilience.spec.ts](e2e/offline-resilience.spec.ts), 2/2) - `route.abort('connectionfailed')` מדמה שליפת כבל: הבאנר הופיע עם שעון גיל, המסך **לא התרוקן** אחרי מחזור פולינג, `POST /api/transfers` הוחזר 503 עם `OFFLINE_SHARED_WRITE` והודעת החסימה הוצגה, `POST /api/activity-log` הוחזר 202 ונכנס לתור, `GET /api/sectors` הוגש מ-IndexedDB עם `x-skyking-from-cache: 1`, ובחזרת הקשר הופיע "הקשר לשרת חזר" ✅ · **חבילת ה-e2e המלאה: 66 עברו, 3 נכשלו - כולן קיימות מראש ולא רגרסיה.** `prov-drop` ו-`prov-drop-html5` מתועדות ככשל על HEAD נקי (שורות 216, 361 בקובץ זה); `emblem-admin` נבדקה בביסקציה - נכשלת **זהה** גם כשהיירוט מנוטרל, כלומר תלוית מצב DB ולא בשינוי.

---

## 2026-08-01 - ⋮ של פ"מ על המפה: מתחת לתוכן במקום על האו"ק

**הבקשה:** "את השלוש נקודות לפ"מ על מפה תציג מתחת לטקסט / ICON".

**מה היה:** כפתור התפריט של פ"מ על מפת האזורים ישב `top: -5` בפינה העליונה של העוטף - בתצוגות טקסט (מוקטן/כתב-יד) בצד שמאל ובאייקון/מורחב בצד ימין. הקפיצה בין הצדדים נועדה להתחמק מהאו"ק, אבל בכתב-יד ("כידון 4/105", מוטה 3°-) העיגול נחת בדיוק על התו הראשון: הבקר קרא את הפ"מ מבעד לכפתור.

**התיקון ([SectorDashboard.tsx](src/components/views/SectorDashboard.tsx)):** `top: '100%'` + מרכוז (`left: 50%` + `translateX(-50%)`) - מתחת לכל תוכן הפ"מ, בכל ארבע התצוגות (כתב-יד / מוקטן / מורחב / אייקון) ובלי חריגה לתצוגה מסוימת. הפיצול לצדדים ירד: כשהכפתור מתחת אין מה להתחמק ממנו.

**נשאר absolute:** העוטף הוא flex-column הממורכז על נקודת הפ"מ ב-`translate(-50%,-50%)`. כפתור בזרימה היה מגדיל את גובה העוטף ומזיז את הטקסט עצמו כלפי מעלה - כלומר העוגן על המפה כבר לא היה על הטקסט. `position: absolute` משאיר את הגיאומטריה של הפ"מ כפי שהיא. המידות ממשיכות להתחלק ב-`mapZoom` כך שהכפתור לא גדל עם זום המפה.

**QA:** `tsc` נקי ✅ · vitest 514/514 ✅ · **e2e אמיתי** ([e2e/fz-pin-menu.spec.ts](e2e/fz-pin-menu.spec.ts)) - נמדדה גיאומטריה על המסך: ראש הכפתור מתחת לתחתית התוכן וממורכז מולו. TDD: הבדיקה **נצפתה אדומה** מול הקוד הישן (ראש הכפתור 435.9 מול תחתית תוכן 485.0 - חפיפה של ~49px) ועברה אחרי התיקון ✅ · ללא רגרסיה ב-[map-pan-drag](e2e/map-pan-drag.spec.ts) 4/4 ו-[map-transfer-points](e2e/map-transfer-points.spec.ts) ✅.

---

## 2026-08-02 - תפקידים מקצועיים במיראז' (בקר/פקח/מש"ק/מפעיל), יחידות, זמני עמדה ומסך כ"א ותחקירים

**הבקשות:** "במיראז תוסיף תפקידים - בקר, מש"ק מפעיל לכל איש צוות... לבקר, משגיח בקר, אחורי - מתפריט בקרים. מש"ק ומשגיח מש"קים מתפריט מש"ק. למפעיל ומשגיח מפעיל מתפריט מפעיל. הכל מהמיראז וכמובן הם צריכים להיות מורשים לעמדה." · "במיראז תוסיף תפקיד פקח" · "בדף תחקיר ביחידות נוספות תוסיף את שמות היחידות מתפריט יחידות שתוסיף במסך ניהול טכני (ולא עמדות)" · "יש למשוך זמן כניסה לעמדה וזמן סיום עמדה... במיראז נוסיף תפקיד חדש 'כח אדם'... כפתור 'כ"א ותחקירים'... 'תחקירים', 'כשירויות'".

**תפקיד מקצועי ≠ הרשאה — שני צירים:** `roles` (admin/team_lead/manpower/user) הוא ציר ההרשאה, ו-`roles.length > 0` הוא התנאי לגישה לאפליקציה. אילו "בקר" היה נכנס לאותה רשימה, סימון תפקיד מקצועי היה **מעניק גישה למערכת** - ובנוסף אדם יכול להיות גם admin וגם בקר. לכן `positions` הוא מערך נפרד ברשומת האפליקציה במיראז'.

**"כח אדם" היא הרשאה נוספת ולא חלופית:** במסך הניהול של המיראז' התפקיד נבחר מ-`<select>` יחיד ונשמר כ-`roles: [role]` — כלומר בחירת "כח אדם" **מחקה** את "מנהל". התיקון: `manpower` יצאה מהבורר והפכה לתיבת סימון נפרדת, וה-roles נבנה כ-`[baseRole, ...(manpower ? ['manpower'] : [])]`. צד ה-SKY-KING כבר היה נכון (`is_admin`/`is_team_lead`/`is_manpower` נגזרים בנפרד זה מזה) — הבאג היה רק ב-UI של המיראז'.

**בקר ופקח הם שני תפקידים, לא אותו תא:** בטופס חברי העמדה `bakar` הוא אותה **עמודה** ביב"א ובמגדל (רק התווית מתחלפת) - אבל ב**מיראז'** אלו שני מקצועות נפרדים: מי שמוסמך לעמדת יב"א אינו בהכרח מוסמך למגדל. לכן `positions` כולל `bakar` **ו-**`pakach`, ושורת האב, המושגח והאחורי נשאבים מהתפריט לפי `preset_role` (`headPosition`).

**מי שלא הוגדרו לו תפקידים מופיע בכל התפריטים:** `positions` ריק פירושו "לא הוגדר", לא "אף תפקיד". בלי הכלל הזה כל התפריטים בעמדה היו נפתחים ריקים עד שמישהו יעבור על כל המשתמשים במיראז' - כשל תפעולי, לא נתון חסר.

**הסינון הראשון הוא הרשאת העמדה:** `GET /api/auth/mirage-crew?presetId=N` מריץ בדיוק את אותו כלל של `mirage-eligible` (`isEligibleForPreset`, שהוצא לפונקציה משותפת) ורק אז מקבץ לפי תפקיד. הוא מחזיר **שמות בלבד** - אין כאן הזדהות ואין הרשאה, ולכן במכוון אין מספרים אישיים, בניגוד ל-`mirage-eligible` שבו המספר דרוש להזדהות מחדש.

**יחידות ≠ עמדות:** רשימת יב"א/מגדלים במעורבי התחקיר נגזרה מ-`workstation_presets`, וזו הייתה טעות מודל: עמדה היא תצורת תצוגה במערכת, יחידה היא גוף בשטח. יש יחידות בלי עמדה, ועמדה אחת יכולה לשרת כמה יחידות - הרשימה הייתה גם חסרה וגם הציגה שמות טכניים למי שכותב תחקיר. נוספה טבלת `units` (קונפיג) ולשונית "יחידות" במסך הניהול.

**זמני עמדה במקטעים, לא משמרת אחת:** `station_sessions` נסגרת ונפתחת מחדש בכל אירוע שמשנה מי יושב על העמדה - החלפת משתמש, עדכון חברי העמדה - ונסגרת ביציאה. משמרת אחת ארוכה הייתה זוקפת את **כל** השעות למי שישב בסוף, וזה בדיוק מה שמסך הכשירויות אמור למדוד. אינדקס UNIQUE **חלקי** (`WHERE exited_at IS NULL`) מבטיח מקטע פתוח יחיד לעמדה, כך שרענון לשונית לא מייצר כפילות. הסגירה ביציאה נשלחת עם `keepalive: true` - בלי זה הבקשה נקטעת עם פריקת הדף שמגיעה מיד אחריה.

**הגרף בכשירויות:** SVG מקומי, בלי ספריית תרשימים. סדרה יחידה (שעות לפי תקופה) → גוון אחד ובלי מקרא, קצוות מעוגלים מעוגנים לבסיס, סרגל רצסיבי, ותווית ערך ב-hover בלבד ולא על כל עמודה. הטבלה היא תצוגת ברירת המחדל ולכן תמיד קיימת חלופה טקסטואלית לקריאה.

**מידור - מה כן ומה טרם:** מסך התחקירים והכשירויות מציג רק עמדות שהמשתמש מורשה להן לפי מיראז' (`approved_workstations`; ריק = הכל), עם חיווי גלוי. זהו מנגנון ההרשאה **הקיים**; מודל המידור הייעודי טרם אופיין ("יאופיין בהמשך"), וכשיאופיין יש להחליף את הסינון בנקודה אחת.

**עוגן לבדיקות:** לכל שדה שם בטופס חברי העמדה נוסף `data-crew-field`. הסדר משתנה לפי סוג העמדה (במגדל אין מפעיל/מש"ק), ולכן בדיקה שנשענת על אינדקס נשברת - כפי שקרה בפועל בהרצה הראשונה.

**QA:** `tsc` נקי ✅ · vitest **728/728** ✅ (כולל שומר ה-i18n, בידוד סכמות מול Neon ו-4 בדיקות חדשות למיראז': positions מוחזר ב-authorize בלי לגעת ב-roles, פקח נפרד מבקר, admin+manpower ו-team_lead+manpower חיים יחד, ותפקיד לא מוכר נזרק) · `npm run build` ✅ · **e2e אמיתי**: [station-crew](e2e/station-crew.spec.ts) 3/3 - ארבעה אנשים במיראז' (בקר/פקח/מש"ק/מפעיל), והבדיקה נכנסת **לשתי עמדות בפועל** (יב"א ומגדל, שמותיהן נשלפים מה-API ולא מקובעים) ומוודאת שכל אחד מופיע **רק** בשדות שלו - כולל שבמגדל שורת האב מציגה את הפקח ולא את הבקר; [manpower](e2e/manpower.spec.ts) 4/4 - משמרת נפתחת בכניסה ונסגרת ביציאה **מול ה-API האמיתי**, תפקיד "כח אדם" פותח את שני המסכים כולל הגרף, ומשתמש **מנהל + כ"א** רואה גם את כפתורי הניהול וגם את כפתור הכ"א; [emblems](e2e/emblems.spec.ts) 3/3 ללא רגרסיה.

**מלכודת שנתפסה שלוש פעמים בהרצה:** בדיקות נכשלו מול **שרת ישן** ש-`reuseExistingServer` שימר מהרצה קודמת. פעם אחת ה-routes החדשים לא היו בו (404 של express חוזר כ-HTML), ופעם אחת `MIRAGE_POSITIONS` הישן סינן החוצה את `pakach` - כך שהוא נראה כמו "בלי תפקידים" ונפל לכלל "מופיע בכל התפריטים". **כישלון e2e אחרי שינוי בשרת = לבדוק קודם שהשרת אותחל מחדש** (הריגה לפי פורט 3001, לא לפי שם תהליך).

**מה לא נעשה:** מודל המידור הייעודי לעמדות (יאופיין בהמשך) · מודל כ"א המלא (הוגדר במפורש כשלב הבא) · קיבוץ בתחקירים לפי טקסט חופשי - נמנע בכוונה, קיבוץ לפי פירוט מייצר קבוצה לכל שורה.

---

## 2026-08-01 - חברי העמדה: טופס אחד לעליית עמדה ולעדכון + "צור תחקיר"

**הבקשה:** "בעמדה על שם המשתמש שפותחים תפריט צריך להוסיף - עדכון חברי העמדה. יפתח הטופס כניסה לעמדה... להוסיף שדה בקר (אוטומטית זה יהיה לפי משתמש עמדה אך ניתן לעדכן לשם אחר לפי רשימת הבקרים שבמיראז - לעשות תפריט חיפוש עם הקלדה מהירה)... סדר התצוגה צריך להיות בקר → אחורי → מושגח → מפעיל → מפעיל מושגח → מש"ק → קש"פ... איפה שיש מושגח לסמן בדגל 'קיים משגיח' ורק אז לפתוח את השדה בצד (שלא יתפוס מקום בפתיחה ראשונה)... בעמדה מסוג מגדל צריך לעדכן את הטופס גם בעליית עמדה וגם בעדכון חברי עמדה - קש"פ, פקח, אחורי, מושגח... עוד להוסיף לתפריט 'צור תחקיר'". ובהמשך: "תיישר את המשגיחים - וגם למש"ק יכול להיות משגיח".

**מה היה:** טופס "כניסה לעמדה" היה JSX מוטבע ב-`App.tsx` עם שלושה שדות קשיחים (קש"פ / מפעיל / אחורי), ונפתח **רק** בעליית עמדה. בתוך העמדה לא הייתה שום דרך לעדכן מי יושב עליה - מה שקורה בכל חילוף משמרת.

**רכיב אחד לשני המסלולים:** [StationCrewForm.tsx](src/components/shared/StationCrewForm.tsx) - אותם שדות, אותה לוגיקה, אותו עיצוב, גם בעליית עמדה (`App.tsx`) וגם ב"עדכון חברי העמדה" (תפריט המשתמש ב-`SectorDashboard`). מה שמשתנה בין המסלולים הוא הכותרת, תווית האישור ונוכחות "דלג" - לא מבנה הטופס. ה-JSX המוטבע ב-`App.tsx` הוסר, וגם שתי המחרוזות של state (`roleForm`/`roleFormLoading`) - הטופס טוען את הנתונים בעצמו.

**בקר = פקח, תא אחד:** בעמדת מגדל התווית היא "פקח" ובעמדת יב"א "בקר", אבל זה **אותו נתון** ולכן אותה עמודה (`bakar`). עמודה כפולה הייתה מייצרת שאלה "איזה מהם מלא" בכל קריאה.

**"מושגח" נפתח בצד, ומיושר:** הסדר שנמסר (בקר → אחורי → מושגח → ...) רומז על שורות נפרדות, ו"לפתוח את השדה בצד" רומז על צמידות; הוסכם על צמידות לשורת האב. הגרסה הראשונה שמה את תווית "מושגח" **מעל** השדה הצמוד - מה שדחף אותו מטה ושתי התיבות יצאו לא מיושרות. התיקון: שורת תוויות אחת ושורת שדות אחת, שני חצאים `flex:1` - התווית של המושגח יושבת באותו גובה כמו תווית האב. משגיח נוסף למש"ק (`mashak_mushgach`) באותה תבנית.

**הדגל נשמר בנפרד מהשם:** `has_mushgach` / `has_mefale_mushgach` / `has_mashak_mushgach` הם עמודות משלהם - אחרת "קיים משגיח" היה נופל בכל רענון עד שיוקלד שם. בשמירה, דגל כבוי **מנקה** את השם, כך ש"אין משגיח" הוא מצב אחד ולא שניים.

**חיפוש מהיר:** `SearchPicker` - שדה טקסט חופשי עם רשימה מסוננת בהקלדה (↑/↓/Enter/Esc), על `GET /api/auth/mirage-crew` (חדש). ה-endpoint מחזיר **שמות בלבד** ולא מספרים אישיים: אין כאן הזדהות ואין הרשאה, בניגוד ל-`mirage-eligible` שבו המספר דרוש להזדהות מחדש. מיראז' שלא זמין → נפילה שקטה לטקסט חופשי, בלי לחסום כניסה לעמדה.

**התחקיר - [DebriefForm.tsx](src/components/shared/DebriefForm.tsx):** משתמש ב-`CrewFields` שהוצא מ-`StationCrewForm`, ולכן חברי הצוות בתחקיר הם **אותם שדות בדיוק** (חיפוש מהיר, דגלים, סדר) בלי שכפול. סיווג התחקיר נשמר כ**קוד** (`critical`/`severe`/...) ולא כטקסט - התוויות ניתנות לעריכה בזמן ריצה ממסך ניהול התרגומים, ושמירת טקסט הייתה מנתקת תחקירים ישנים מהסיווג שלהם. רשימות ה"מעורבים" נגזרות מהנתונים **החיים** בעמדה (טייסת/או"ק מהפ"מים הפעילים, יב"א/מגדלים מ-`workstation_presets` לפי `preset_role`) - אפס ניהול נוסף, תמיד מעודכן.

**הצילום קודם לטופס:** [stationSnapshot.ts](src/utils/stationSnapshot.ts) מצלם דרך `html-to-image` (DOM→canvas) **לפני** שהטופס נפתח - אחרת הטופס היה מכסה את העמדה בתמונה. `getDisplayMedia` נפסל: הוא פותח דיאלוג בחירת מסך בכל צילום, ובאמצע אירוע זה צעד מיותר. `pixelRatio: 0.5` + `skipFonts` (רבע מנפח ה-base64), ו-`data-nosnapshot` מסנן החוצה שכבות שלא אמורות להיכנס - רשת ביטחון לצילום חוזר. כישלון צילום אינו חריג: התחקיר נשמר בלי תמונה.

**DB:** `workstation_session_roles` הורחבה ב-8 עמודות (ALTER אידמפוטנטי, בלי לגעת ב-CREATE הקיים); טבלת `debriefs` חדשה עם `TIMESTAMPTZ` בלבד. שתיהן **תפעוליות** - נוספו ל-`OPERATIONAL_TABLES` (בלי זה סביבת תרגול הייתה נופלת בשקט ל-`public` וכותבת לאמת), ו-`debriefs.preset_id` נוסף ל-`foreign-keys.js`.

**QA:** `tsc` נקי ✅ · vitest **559/559** ✅ (כולל שומר ה-i18n ובידוד סכמות הסביבה מול Neon) · `npm run build` ✅ · **e2e אמיתי** ([e2e/station-crew.spec.ts](e2e/station-crew.spec.ts), 2/2): הטופס נפתח בעליית עמדה בסדר הנכון ו**בלי** שדות מושגח, קש"פ מבקש מספר קשר פנים, סימון הדגל פותח את שדה המושגח ו**שתי התיבות נמדדו מיושרות** (הפרש ≤ 1px), ביטול הדגל מסתיר אותו; בעמדה החיה - "עדכון חברי העמדה" ו"צור תחקיר" בתפריט, שמירה עוברת, וטופס התחקיר מוצג עם כל השדות **ועם תמונת העמדה בפועל** ✅ · ללא רגרסיה ב-[emblems](e2e/emblems.spec.ts) 3/3 (אותו מסלול כניסה).

**מה לא נעשה:** כרטיס העמדה בדש בורד הניהול ממשיך להציג רק קש"פ/מפעיל/אחורי - הבקר והמשגיחים לא נוספו לו (מחוץ לתכולה שהתבקשה, והכרטיס צפוף). אין עדיין מסך לצפייה בתחקירים שנשמרו - רק יצירה; `GET /api/debriefs` קיים ומוכן לצרכן.

---

## 2026-08-01 - קטגוריית המסלולים בחלון העזרים: הקטנה במקום הסתרה + הגדלה בלחיצה

**הבקשה:** "את קטגוריית הצגת המסלולים בבסיס אווירי - לא להציג את הכל במידה וחורג מהגודל ב"מ של חלון העזרים - יש להקטין (לא להסתיר, רק להקטין) לרוחב ב"מ. ובלחיצה על החלון - להגדיל אותו לגודל של שישית מסך, ומצב התצוגה ימלא את כולו (המסלולים, הפונטים והכל)."

**מה היה:** רצועת המסלולים הייתה `overflowX: 'auto'` - 4 מסלולים (56px + 8px מרווח ≈ 248px) בחלון עזרים של 220px, כלומר המסלול הרביעי **נחתך** וחייב גלילה. בעמדת בקרה מסלול שיוצא מהמבט הוא כשל בטיחותי, לא אי-נוחות.

**רכיב אחד לשתי התצוגות:** [FitScaleBox.tsx](src/components/shared/FitScaleBox.tsx) - `mode='shrink'` מקטין עד שנכנס לרוחב, `mode='fill'` מגדיל עד שממלא את השטח. רצועת המסלולים עצמה (`stripBody`) מוגדרת **פעם אחת** ומרונדרת בשתי התצוגות - אין שכפול JSX, וכל שינוי עתידי במסלול חל בשתיהן.

**למה `zoom` ולא `transform: scale()`:** `transform` לא משנה פריסה - הייתי צריך לחשב את גובה המיכל ידנית מהמדידה, והטקסט היה נמתח (מטושטש). `zoom` היא תכונת פריסה: המיכל מתכווץ מעצמו והפונטים מרונדרים מחדש בגודל החדש. זה גם המנגנון שכל ה-UI כבר נשען עליו (`#root { zoom: var(--s) }`).

**המדידה יחסית בכוונה:** `getBoundingClientRect` מחזיר מידות **אחרי** כל הזומים (הגלובלי + המקומי), ולכן `סקייל חדש = סקייל נוכחי × (רוחב פנוי / רוחב תוכן)` תקף בכל `--s` בלי לקרוא את הזום או להמיר יחידות - בדיוק המלכודת שהפילה את גרירת המפה. ההתכנסות מוגנת מריצוד: סף שינוי 1% + מכסת 12 התאמות ברצף שמתאפסת אחרי 400ms שקט.

**החלון המוגדל:** `createPortal` ל-`body` (כמו פאנל "מסלולים בשימוש") כדי לצוף מעל כל המסכים - ולכן `zoom: var(--s)` ידני ו-`calc(34vw / var(--s,1)) × calc(50vh / var(--s,1))` ≈ שישית מסך (בלי החלוקה ב---s החלון היה יוצא פי 1.65 מהמסך ב-24"). z-index 8901 (מעל "מסלולים בשימוש" 8900, מתחת להודעות 9000). נסגר ב-✕, ב-Esc או בלחיצה נוספת על הרצועה. הכפתורים שבתוך הרצועה (NOTAM/GRF/תאורה) כבר עושים `stopPropagation` - ולכן לחיצה עליהם לא פותחת את החלון המוגדל.

**גרירת החלון המוגדל (בקשה משלימה):** הידית היא אייקון ⠿ בכותרת - שטח מגע 34px לעט/אצבע, לא כל שורת הכותרת (שם יושבים ✕ ו-↩ ולחיצה עליהם לא אמורה לגרור). הגרירה עצמה ב-hook משותף חדש, [useDragPosition.ts](src/hooks/useDragPosition.ts), ש**פאנל "מסלולים בשימוש" עבר אליו גם הוא** - הייתה שם גרסה כמעט זהה, בלי `touch-action: none` (הדפדפן תפס את התנועה כגלילה במגע) ובלי `setPointerCapture` (הגרירה נקטעה מעל ה-iframe של סרגל ההצצה), ועם קפיצה לנקודת המצביע בגרירה הראשונה. ה-hook מחלק כל קואורדינטה ב---s (אותה מלכודת של גרירת המפה), קורא את מיקום ההתחלה מה-DOM, וחוסם גרירה אל מחוץ למסך. ↩ מחזיר את החלון למרכז.

**שורת הכותרת של הקטגוריה:** שלושת כפתורי הריכוז (NOTAMs / GRF / NOTAM כללי) היו בשורה אחת וחרגו מרוחב עמודת העזרים. עכשיו הם אחד מתחת לשני ברוחב מלא, מתחת לשורת הכותרת (השם + ▼). אגב כך `📋 NOTAM כללי` הקשיח הוחלף ב-`tr('ctrl.generalNotam')`.

**QA:** `tsc` נקי ✅ · vitest **559/559** ✅ · **e2e אמיתי** ([e2e/fit-scale.spec.ts](e2e/fit-scale.spec.ts), 6/6 - **גם ב-15.6" וגם ב-24"**): כל 4 המסלולים גלויים ובתוך גבולות החלון (אף אחד לא נחתך), הרצועה מנצלת מעל 80% מהרוחב, בחלון המוגדל התוכן ממלא מעל 85% מציר אחד ו**הטקסט עצמו** גדל פי 1.5 לפחות (נמדד לפי הגובה שרונדר בפועל - `getComputedStyle().fontSize` מחזיר את הערך המוצהר ומתעלם מה-zoom), והגרירה זזה **1:1** עם המצביע כשידית הגרירה 34px ו-`touch-action: none` ✅ · ללא רגרסיה ב-[dashboard-baseline](e2e/dashboard-baseline.spec.ts) 2/2 ו-[layout-rtl](e2e/layout-rtl.spec.ts) 1/1.

**מגבלה ידועה:** ה-DB המקומי לא מכיל מסלולים לאף בסיס, ולכן האימות הוויזואלי נעשה על מתקן בדיקה ([e2e/fixtures/fit-scale.tsx](e2e/fixtures/fit-scale.tsx)) שמשחזר את הרצועה ומריץ את **הרכיב האמיתי** - לא על מסך המגדל עם נתוני אמת.

---

## 2026-08-01 - גרירת מפה בלחיצה על שטח ריק (סמן מטרה)

**הבקשה:** "לחיצה על מפה כשלא במוד ציור ולא על יישות פ"מ / נקודת העברה / כל יישות אחרת בעתיד שניתן ללחוץ עליה - גוררת את המפה. הסימבול הופך לסימבול של מטרה (שונה מסמל היד של גרירת פ"מ), וכל עוד העט/היד/העכבר לא מורם המפה זזה איתו."

**מה היה:** הזזת המפה הייתה רק בארבעה חצים בסרגל הזום, 50 פיקסלים בכל לחיצה. על מפה מוגדלת זו עבודה של עשרות הקשות.

**איך מבדילים "שטח ריק" מ"יישות" - בלי לגעת באף יישות:** הכלל הוא `e.target === e.currentTarget` על **שכבת התוכן** של המפה. תמונת המפה היא `pointerEvents:'none'`, ולכן לחיצה על שטח ריק מגיעה לשכבה עצמה כ-target; לחיצה על פ"מ / נקודת העברה / סמן שכן מגיעה עם ה-target שלהם ופשוט לא עונה לתנאי. **יישות שתתווסף בעתיד לא צריכה לדעת דבר על הגרירה** - זו בדיוק הדרישה. מתחת לשכבת התוכן נוספה שכבת גרירה נוספת שתופסת את השוליים שמחוץ לשכבה כשזום המפה קטן מ-100%. במוד ציור קנבס הציור (zIndex 200) יושב מעל ותופס את העט קודם, ובנוסף `shouldStartMapPan` חוסם במפורש.

**⚠️ הממצא המשמעותי - סקייל גודל המסך:** `#root { zoom: var(--s) }` (1.00 ל-15.6" ועד **1.65 ל-24"**, עמדת היעד). `clientX` נמדד אחרי ה-zoom ואילו ה-`translate` נמדד בפיקסלים המקומיים שלפניו - כך שגרירה נאיבית הייתה מזיזה את המפה **פי 1.65** מהעט. הסקייל נמדד בפועל מהאלמנט (`measureCssZoom`, יחס `getBoundingClientRect().width / offsetWidth`) ולא נקרא מה-CSS, והדלתא מחולקת בו.

**ביצועים:** בזמן התנועה נכתב ה-`transform` **ישירות ל-DOM** של שכבות המפה (תוכן, קנבס, צורות) וה-`transition` מנוטרל; ה-state מתעדכן פעם אחת בהרמת העט. המסך מרנדר אלפי אלמנטים - `setState` בכל frame היה מקרטע. מכיוון שהמסך מרונדר מחדש כל סבב פולינג, הפאן החי נשמר גם ב-ref שממנו הרינדור קורא, אחרת רינדור באמצע הגרירה היה מחזיר את המפה אחורה.

**סמן המטרה:** SVG של כוונת (עיגול + 4 שנתות, קו מתאר שחור לקריאות על מפה בהירה), מוגדר פעם אחת ב-[mapPan.ts](src/utils/mapPan.ts) ומוזרק כמשתנה CSS ל-`body.map-panning` ב-[App.css](src/App.css). ה-`!important` ו-`*` נחוצים כי במהלך הגרירה המצביע עובר מעל יישויות עם סמן משלהן. **בלי `preventDefault`** על ה-pointerdown: בעט/מגע הוא מבטל את אירועי העכבר התואמים ותפריטים שנסגרים ב-`mousedown` היו נתקעים פתוחים - סימון הטקסט נמנע ב-`user-select` במקום.

**QA:** `tsc` נקי ✅ · vitest **520/520** ✅ (19 חדשות ב-[mapPan.test.ts](src/utils/mapPan.test.ts), TDD - נכתבו ונצפו אדומות לפני המימוש) · `vite build` ✅ · **e2e אמיתי** ([e2e/map-pan-drag.spec.ts](e2e/map-pan-drag.spec.ts), 4/4): תזוזה ויזואלית **1:1** של תמונת המפה מול תזוזת המצביע - **גם ב-15.6" וגם ב-24"** (החלון מוגדל באותו יחס, אחרת לוח המפה נדחס ל-83px ולא נבדק כלום); סמן המטרה פעיל בזמן הלחיצה; המפה לא קופצת חזרה בהרמה; **לחיצה על פ"מ אמיתי שהוצב ל-DB לא מזיזה את המפה** (הבדיקה מציבה אותו ומנקה אחריה); שכבות ה-hit-test במקומן ✅ · ללא רגרסיה ב-e2e של המפה ([map-transfer-points](e2e/map-transfer-points.spec.ts) 4/4, [dashboard-baseline](e2e/dashboard-baseline.spec.ts) 2/2).

**מה לא נגעתי:** מפת המגדל (GroundView) - שם יש zoom/pan משלה (מקלדת + גלגלת) ו-`groundMapDragRef` שהוכרז ומעולם לא חובר. אותה התנהגות שם היא שינוי נפרד.

**מגבלה ידועה:** אין חסם לגרירה - אפשר לגרור את המפה עד מחוץ ללוח, בדיוק כמו חצי ההזזה הקיימים. כפתור האיפוס (⟲) מחזיר לזום 100% ולמרכז.

---

## 2026-08-01 - סימן היצרן (LEO²) בכל המסכים

**הבקשה:** "תוסיף את הלוגו של החברה גם בכל המסכים שייראה נורמלי - לא גדול ולא קטן ושלא יתפוס מקום."

**רכיב אחד לכל המסכים:** [LeoLogo.tsx](src/components/shared/LeoLogo.tsx) - SVG מוטבע, פרמטרים `height` / `themeMode` / `opacity` בלבד. אין וריאנט למסך: אותה צורה בקצה הסרגל של עמדת הבקר (17px, אחרי השעון), בסרגל של דסק המשימה (17px), בכותרת מסך הניהול (19px) ובפוטר של מסך ההתחברות (24px, מעל מספר הגרסה). הוא נכנס בקצה השורה הקיימת ולא פותח שורה או אזור חדש - הסרגל לא נשבר ושדות ההזדהות לא נדחפים מטה.

**מסך הטעינה (בקשה נוספת באותו יום):** הסימן נוסף גם לתחתית מסך הטעינה - זה שמציג את סמלי בסיס האב ומיח"ה המסתובבים - בגובה 30px, `position: absolute` ולא פריט flex, כדי שלא יזוז עם מספר שלבי הטעינה. יש לו **אנימציית הרכבה** חד-פעמית (`animateIn`): האותיות עולות, הכנף נפרשת (`scaleX` מהשורש החוצה), הקשת מטפסת והנקודה התכולה "נוחתת" בקצה עם overshoot - מוטיב של מגע מכ"ם, באותה שפה של מסך הטעינה. הרצף אורך ~1.1ש' מתחילת ההשהיה (0.3ש'), מכוון בכוונה כך שגם טעינה מהירה תספיק להשלים אותו. ה-CSS מוזרק ב-`<style>` מקושר ל-`useId` כמו ב-`RotatingEmblems`, ומכובה תחת `prefers-reduced-motion: reduce`. **בסרגלים התפעוליים הסימן נשאר סטטי** - תנועה מתמדת בכרום של עמדת בקרה היא הסחה (אותו עיקרון כמו `RotatingEmblems variant='topbar'`).

**למה SVG מוטבע ולא קובץ תמונה:** נשאר חד בכל אחד מארבעה גדלי המסך (`--s`) ובכל גובה, נטען עם ה-bundle ולא מתחרה במטח קריאות ה-API של עליית הדשבורד (אותו שיקול כמו הסמלים המובנים ב-`RotatingEmblems`), ומקבל צבע לפי התמה במקום להיות תמונה שטוחה בצבע אחד. יושב ב-`#root` ולכן מתכווץ/גדל עם `--s` בלי טיפול ידני.

**מלכודת שנתפסה - 'ocean' היא תמה כהה:** `/ui-adapt` מכליל "אור/כחול = רקע בהיר", אבל בפועל `T.surface` של ocean ב-SectorDashboard הוא `#05404e` ו-`panel` ב-`mdTheme` הוא `#123a5c` - שניהם כהים. הגרסה הראשונה גזרה "נייבי לכל מה שאינו dark" והסימן יצא נייבי על טורקיז כהה, יחס ניגודיות ~1.3:1, כלומר בלתי נראה. התיקון: רק `light` מקבל נייבי; `dark` ו-`ocean` מקבלים את גרסת ה-reversed. **הבדיקה מודדת ולא מניחה** - `e2e/leo-logo.spec.ts` שולף את צבע הרקע בפועל של הסרגל ואת ה-fill של הסימן ומחשב יחס WCAG, בשלוש התמות.

**QA:** `tsc --noEmit` נקי ✅ · `npm run build` נקי ✅ · vitest **520/520** ✅ · e2e [leo-logo.spec.ts](e2e/leo-logo.spec.ts) **9/9** ✅ - נוכחות + גובה בטווח + רוחב < 5% מהחלון בכל מסך · ניגודיות ≥ 3:1 בשלוש התמות · במסך הטעינה: עיגון לתחתית מתחת לשלבי הטעינה ומרכוז אופקי, האנימציה מתחילה נסתרת ומסתיימת מלאה (מדידת `opacity` אחרי `pause()` + `currentTime` - בלי תלות בזמן אמיתי), ותחת `prefers-reduced-motion` אין ולו אנימציה אחת רצה והסימן מלא. אימות ויזואלי: פילמסטריפ פריימים של רצף ההרכבה + צילומי סרגל בשלוש התמות.

---

## 2026-07-31 - ברירת המחדל של פ"מ על מפה: כתב יד בגודל 12

**הבקשה:** "ברירת מחדל לפ"מ על מפה - כתב יד וגודל 12."

**מה היה:** מפת האזורים נפתחה בתצוגת **מורחב** (`strip`) בגודל **7** - הגודל המינימלי בסקאלה (7-22). בקר שרוצה את תצוגת ה**כתב יד** (שמדמה כתיבת צ'ינו על הסדק: `או"ק(מס' מטוסים)/טייסת`) נדרש בכל כניסה לפתוח את בורר סוג התצוגה ולהקיש A+ חמש פעמים - שני ה-state-ים הם `useState` בלבד ולא נשמרים בין סשנים.

**מה השתנה:** ברירת המחדל היא `handwrite` בגודל `12`, בכל שכבות ההגדרה - סטייט ריצה ב-[SectorDashboard.tsx](src/components/views/SectorDashboard.tsx) (`fzPinFontSize`, ה-fallback של `fz_pin_display`), ברירת המחדל של העמודה ב-[init.js](server/db/init.js), ה-fallback ב-POST/PUT של [workstations.js](server/routes/workstations.js) וטופס העמדה ב-[ManagementPage.tsx](src/components/admin/ManagementPage.tsx).

**מיגרציה חד-פעמית ולמה היא בטוחה:** 22 מתוך 23 העמדות ב-DB היו ב-`'strip'` - כלומר ברירת המחדל הישנה, לא בחירה מודעת (בורר האדמין הציע רק "סטריפ"/"אייקון" ונפתח על "סטריפ"). לכן שינוי ה-fallback בקוד לבדו לא היה משנה כלום על המסך. ה-UPDATE ל-`handwrite` רץ ב-`initDb` ומסומן ע"י ה-`DEFAULT` **הישן** של העמודה: ברגע שהוא מוחלף ל-`handwrite`, התנאי לא מתקיים שוב - כך שאדמין שיבחר "מורחב" אחרי המעבר לא ייסחף חזרה באתחול הבא. העמדה היחידה שנבחרה במפורש (`מרחבי 304` = `icon`) לא נגעה.

**בורר האדמין הושלם ל-4 מצבים** (כתב יד / מורחב / מוקטן / אייקון), במקביל לבורר שבסרגל המפה - אחרת ערך `handwrite` לא היה מיוצג בטופס וכפתור לא היה נדלק. התוויות עברו ל-`tr('ctrl.pin*')` במקום מחרוזות קשיחות.

**QA:** `tsc -p tsconfig.build.json --noEmit` נקי ✅ · vitest **501/501** ✅ · תנאי המיגרציה נבדק מול ה-DB בקריאה בלבד: `column_default = 'strip'::character varying` (יפעל באתחול הבא), 22 שורות `strip` + שורה אחת `icon` שנשמרת ✅.

---

## 2026-07-31 - תצוגת עמדות אחרות בעמדה: ריבועים חיים בתחתית המסך

**הבקשה:** "יכולת להציג עמדות שונות בעמדה שבה אני נמצא - כל עמדה כזו רק אם יש לי הרשאה לצפות בה. יוצגו למטה בריבועים קטנים ON TOP עם משולש להקטנה/הגדלה, מתעדכנים בזמ"א, ובלחיצה יגדלו ל-2/3 מסך לקריאה בלבד. אין אישור לעמדה - הריבוע לא מוצג."

**ההכרעה הארכיטקטונית - למה מסגרת (iframe) ולא instance שני:** הוחלט שהריבוע יציג את **המסך האמיתי** של העמדה, מכל סוג. כל סוגי העמדות עולים דרך `SectorDashboard` - קובץ של 16,764 שורות עם 88 `useEffect`, 24 `setInterval`, 29 מאזיני `window` ו-19 נגיעות ב-`document.body` (תמה, מסך מלא, קיצורי מקלדת). רינדור instance שני שלו **באותו מסמך** היה מתנגש על כל הגלובלים האלה, כלומר סיכון ישיר ליציבות של העמדה החיה. לכן כל ריבוע הוא `<iframe src="/?peek=<presetId>">` של האפליקציה עצמה, מוקטן ב-`transform: scale()` מגודל לוגי 1600×900: נאמנות מלאה לכל סוגי העמדות, עדכון בזמן אמת "בחינם" מהפולינג הקיים, אפס שכפול קוד ואפס סיכון לעמדה החיה.

**ההרשאה - אין טבלת הרשאות חדשה:** המנגנון כבר היה קיים. המיראז' מחזיר `apps['SKY-KING'].workstations`, [server/routes/mirage.js](server/routes/mirage.js) מפענח לרשימת `preset_id`, והיא יושבת ב-`session.crewMember.approved_workstations` (רשימה ריקה = בלי הגבלה). זהו בדיוק הסינון שכבר עושים בבחירת עמדה במסך הכניסה: **מי שרשאי להיכנס לעמדה רשאי לצפות בה**. עמדה בלי הרשאה - הריבוע לא מרונדר כלל, לא מוצג נעול.

**קריאה בלבד - שתי שכבות ולא אחת:** `pointer-events: none` על המסגרת מונע אינטראקציה, אבל עמדה מריצה גם כתיבות אוטומטיות (למשל רישום איש הצוות הפעיל, `PUT /api/preset-active-crew`). לכן במסמך ה-peek נחסמת **כל** בקשה שאינה GET, בעטיפת `window.fetch` אחת (`installPeekWriteGuard`) - במקום לבדוק כל אחד מ-149 אתרי הכתיבה. באותה תבנית `installPeekPollThrottle` מכפיל מרווחי פולינג ≥2ש' פי-3, כדי שארבעה ריבועים חיים לא יכפילו פי-חמישה את העומס על השרת; טיימרים מהירים (שעון העמדה) נשארים מדויקים - שעה מיושנת בעמדה נצפית היא מידע מטעה.

**גארד נגד קינון:** עמדה שמוצגת בריבוע לא מציגה סרגל עמדות משלה (`IS_PEEK_FRAME`), אחרת היה נוצר עץ מסגרות אינסופי. השרת גם דוחה עמדה שמציגה את עצמה (400 `self_view_not_allowed`).

**מה נוסף:** טבלה `preset_view_stations` (+ סיווג `CONFIG_TABLES` + 2 FK ב-`foreign-keys.js`) · 5 endpoints ב-[workstations.js](server/routes/workstations.js) (13→18) · [src/utils/stationPeek.ts](src/utils/stationPeek.ts) (לוגיקה טהורה) · [src/components/shared/StationPeekBar.tsx](src/components/shared/StationPeekBar.tsx) (רכיב משותף לכל סוגי העמדות) · מצב `?peek=` ב-[App.tsx](src/App.tsx) שבונה סשן מה-URL ולא מ-sessionStorage (הוא משותף בין הטאב למסגרות שבתוכו - קריאה ממנו הייתה מציגה את העמדה של הצופה) · סקשן "עמדות לצפייה" בטופס העמדה בניהול (גרירה/עריכה/מחיקה) · מתג בתפריט "תצוגה" · `buildRelevantSectors` חולץ ל-[session.ts](src/utils/session.ts) כי נדרש בשני מסלולי הכניסה.

**QA:** `tsc --noEmit` נקי ✅ · vitest **501/501** ✅ (47 חדשות ב-`stationPeek.test.ts` ו-`session.test.ts`, TDD - נכתבו לפני המימוש ונצפו אדומות) · `vite build` ✅ · **e2e אמיתי** ([e2e/station-peek.spec.ts](e2e/station-peek.spec.ts)): הריבוע עולה עם מסגרת חיה של העמדה הנצפית, הגדלה ל-2/3 מסך, Esc, כיווץ - וצילומי מסך ב-`e2e/__screenshots__/station-peek-*.png` ✅ · **אימות ה-read-only בפועל:** מסגרת peek נצפתה 12 שניות ולא שלחה ולו בקשה אחת שאינה GET ✅ · smoke test מלא ל-CRUD מול ה-DB (self-view→400, upsert בכפילות, סידור מחדש, מחיקה) ✅.

**מגבלה ידועה:** כל ריבוע הוא instance מלא של האפליקציה. המיתון (ריבועים נטענים רק כשהסרגל פתוח, פולינג ×3, הגדלה אחת בכל רגע) נבנה סביב 3-4 ריבועים; רשימה ארוכה משמעותית תדרוש מדידה על חומרת העמדה.

---

## 2026-07-31 - זיהוי קולי נסגר אחרי שנייה בעמדה: מעבר לתמלול מקומי (whisper.cpp + ivrit-ai)

**התקלה:** "כשאני לוחץ על המיקרופון לדבר הוא נסגר אוטומטית אחרי שנייה - ב-DEV עובד טוב."

**הסיבה:** ה-Web Speech API של Chromium (`webkitSpeechRecognition`) לא מזהה מקומית - הוא שולח את האודיו לשירות הזיהוי הענני של גוגל, והמפתחות אליו קומפלו **רק לתוך Chrome עצמו**. בכל מעטפת Chromium אחרת, כולל Electron, הרצף הוא `start()` → `onstart` → כשנייה → `onerror {error:'network'}` → `onend`. ה-`onend` עשה `setVoiceListening(false)`, ולכן המיקרופון "נסגר מעצמו". באג ידוע ופתוח ב-Electron ([#46143](https://github.com/electron/electron/issues/46143), [#7749](https://github.com/electron/electron/issues/7749)); `GOOGLE_API_KEY` **לא** עוזר - הוא מחובר רק לגיאולוקציה. ב-DEV זה עבד כי שם רץ Chrome אמיתי.

**תיקון מיידי (גלוי לבקר):** השגיאה נבלעה לגמרי (`recog.onerror = () => setVoiceListening(false)`). מעכשיו כל קוד שגיאה מתורגם להודעה על המסך - הרשאת מיקרופון, אין מיקרופון, לא נקלט דיבור, מנוע חסר, חריגת זמן.

**התיקון המלא - תמלול מקומי בעמדה:** [whisper.cpp](https://github.com/ggml-org/whisper.cpp) v1.9.1 (בינארי Windows מוכן - אין קומפילציה מול ה-ABI של Electron) + מודל עברית [`ivrit-ai/whisper-large-v3-turbo-ggml`](https://huggingface.co/ivrit-ai/whisper-large-v3-turbo-ggml) (Apache-2.0), מקוונטז ל-q5_0: **1549MB → 547MB**. **האודיו לא עוזב את העמדה** - שיפור מול כל חלופת ענן, ורלוונטי ישירות לסקר האבטחה.

**מה לא השתנה:** כל הפרסור העברי (`parseVoiceCommand`, `parseDestCommand`, `findSectorByVoice`, `locateZoneInText`) נשאר כפי שהיה. הוחלף **מקור התמלול** בלבד, מאחורי הפשטה ב-[src/utils/speech.ts](src/utils/speech.ts) שבוחרת מנוע לפי הסביבה: דפדפן → Web Speech (התנהגות זהה להיום), עמדה → whisper מקומי.

**זרימה:** `getUserMedia` → `MediaRecorder` + `AnalyserNode` (עצירה אוטומטית אחרי 1.2 שנ' שקט) → `OfflineAudioContext` ל-16kHz מונו → [wav.ts](src/utils/wav.ts) → base64 → `contextBridge` ([electron-preload.cjs](electron-preload.cjs), מתודה אחת לערוץ) → IPC → [electron/whisper.cjs](electron/whisper.cjs). **base64 ולא באפר גולמי** כי טבלת הטיפוסים של `contextBridge` לא מציינת `ArrayBuffer` במפורש; המחיר זניח (~128KB לקליפ של 3 שניות).

**כיול השהיה - הממצא המשמעותי:** whisper מעבד תמיד חלון של 30 שניות, גם על פקודה בת 2. מדידה על i7-1355U: **15.8 שניות** לאמירה של 2.5 שניות (6.3x מזמן אמת) - לא מבצעי. הכיוונון (`-bo 1 -bs 1 -fa` + חלון אודיו **דינמי** לפי אורך ההקלטה) הוריד ל-**3.2 שניות** (1.26x). ⚠️ `-ac 128` נבדק ו**שובר** את המודל ללולאת חזרות ("בנונה 120, בנונה 120, ...") ודווקא איטי יותר - הרצפה של 256 מכוסה בבדיקה.

**מה נשקל ונדחה:** `ivrit-ai/whisper-v2-d4-ggml` (3.09GB, גדול מ-turbo = איטי יותר); STT בענן (מוציא אודיו מבצעי החוצה); הרצת העמדה על Chrome kiosk במקום Electron (מאבד את מסך "אין חיבור" והמעטפת).

**אבטחה:** נוסף `setPermissionRequestHandler`/`setPermissionCheckHandler` - ההרשאות (כולל מיקרופון) ניתנות **רק** ל-origin של האפליקציה במקום ברירת המחדל של Electron שמאשרת הכל; כל קריאת IPC מאמתת את מקור השולח. `files` ב-electron-builder הוא whitelist - הקבצים החדשים נוספו אליו במפורש.

**QA:** `tsc -p tsconfig.build.json` נקי ✅ · vitest **489/489** ✅ (42 חדשות: [wav.test.ts](src/utils/wav.test.ts) 17, [speech.test.ts](src/utils/speech.test.ts) 13, [electron/whisper.test.js](electron/whisper.test.js) 20 - כולל כללי העצירה האוטומטית, שהם הלוגיקה שקובעת אם פקודה תיחתך באמצע) · `vite build` ✅ · **מבחן קצה-לקצה אמיתי של המנוע** - WAV שיוצר ב-TTS של Windows הורץ דרך `transcribeWav` בפועל: תמלול חזר, המספרים נקלטו נכון, זמנים נמדדו ✅ · הרצת העמדה ב-Electron מדפיסה `[stt] מנוע התמלול מוכן` ✅.

**מה עדיין לא נבדק:** דיוק התמלול על **עברית מדוברת אמיתית** - במכונה לא מותקן קול עברי ל-TTS, ולכן האימות נעשה על אודיו אנגלי (המודל תעתק אותו לעברית והמספרים עברו נכון). נדרשת אמירה של בקר בעמדה. וכן מדידת ההשהיה על חומרת העמדה עצמה - 3.2 שניות נמדדו על מעבד נייד 15W.

---

## 2026-07-31 - עמדת דסק משימה: מה שהוגדר בניהול לא הוצג (עזרים, דש בורד, מצבי בסיס)

**הבקשה:** "בעמדה מסוג דסק משימה - אם בחרתי בניהול להציג עומסים או דש בורד מנהל וכו', וגם דברים של חלון ימני (עזרים) - להציג כמו כל עמדה." (למעט פ"ממים ונקודות העברה.)

**הסיבה:** עמדת `mission_desk` נותבה ב-[App.tsx](src/App.tsx) למסך נפרד (`MissionDeskView`) שהכיל רק את קנבס הדסק + פס עליון מינימלי. כל מה שמונע מהגדרות העמדה - חלון העזרים הימני, כפתור דש בורד מנהל (`show_dashboard`), מצבי בסיס, לחץ/מז"א/ATIS/NOTAM, מד עומס, ספרורים - חי ב-`SectorDashboard` בלבד. עורך העמדה בניהול כן מציג את ההגדרות האלה לעמדת דסק, כך שהמשתמש בחר אותן והן פשוט לא הוצגו.

**התיקון (DRY, בלי שכפול):** קנבס הדסק חולץ ל-[MissionDeskBody.tsx](src/components/missiondesk/MissionDeskBody.tsx), ועמדת דסק משימה רצה מעכשיו **דרך `SectorDashboard` כמו כל עמדה** - הדגל `isMissionDeskMode` מחליף את המפה/סטריפים/טבלה בקנבס הדסק במרכז. כך העמדה יורשת אוטומטית את כל ה-chrome של העמדה (וגם פיצ'רים עתידיים) במקום להעתיק אותו. `MissionDeskView` נשאר סביב אותו קנבס למצב ההגדרה בניהול (`adminMode`).

**מה הוסתר בדסק (פ"ממים / נקודות העברה):** פאנל נקודות ההעברה, סרגל הפ"ממים, תפריט "יצירה" (נקודה זמנית), פצל/אחד, "כל המכלול", זיהוי קולי, תצוגת בלוקים, בחירת מפה/טבלה בתפריט התצוגה, ואיחוד/פיצול עמדה (שם העמדה מוצג כצ'יפ). כותרת המסך מציגה `🗂 <שם הדסק>` במקום "סקייבורד". טעינת מפת ברירת המחדל מדולגת (אין מפה בדסק) ושלב המפה במסך הטעינה מסומן כהושלם, כדי שמסך הטעינה לא ייתקע.

**QA:** `tsc -p tsconfig.json` נקי ✅ · vitest 395/395 ✅ · `vite build` ✅ · **e2e דסק משימה 5/5** ✅ - כולל בדיקה חדשה שמגדירה דרך ה-API עמדת דסק עם `show_dashboard` וקבוצת עזרים, ומאמתת שבמסך העמדה מופיעים פריט העזרים, כפתור דש בורד וצ'יפ הלחץ, ובדיקת רגרסיה שסרגל הפ"ממים מוסתר ופאנל נקודות ההעברה לא קיים. (`e2e/prov-drop.spec.ts` נכשל גם על HEAD נקי - כשל קיים, לא רגרסיה.)

---

## 2026-07-31 - שמות אזורים הוצגו הפוכים על המפה בעמדה

**התקלה:** אזור בשם `61 צפון` נראה נכון בניהול אזורים, אבל על המפה בעמדה הוצג `צפון 61`.

**הסיבה:** שם שמתחיל בספרה מתחיל בתו "חלש" באלגוריתם הדו-כיווני של יוניקוד, ולכן סדר התצוגה שלו נקבע לפי **כיוון הבסיס של ההקשר**: RTL ← `61 צפון` (כפי שהוזן), LTR ← `צפון 61` (הפוך). אזור המפה בעמדה יושב בתוך המיכל שסומן `dir="ltr"` ב-[SectorDashboard.tsx](src/components/views/SectorDashboard.tsx) (המיכל המבני שמקבע את סדר העמודות: נקודות משמאל, עזרים מימין); הפאנלים שבתוכו מחזירים `direction: dir` לעצמם - `#map-area` לא. אותה תקלה הייתה קופצת בכל המסך גם כשה-UI מוחלף לאנגלית (שורש LTR). אומת ב-Chromium: אותו `<text>` בדיוק מרונדר `61 צפון` תחת בסיס RTL ו-`צפון 61` תחת בסיס LTR.

**התיקון:** [src/utils/bidi.ts](src/utils/bidi.ts) - `bidiAuto` עוטף מחרוזת שהוזנה על ידי המשתמש ב-FSI/PDI (`U+2068`/`U+2069`), התקן היוניקודי של `dir="auto"`: כיוון הבסיס נקבע לפי האות החזקה הראשונה **במחרוזת עצמה**, והיא מבודדת מהטקסט שסביבה. הופעל על תוויות שמות האזורים במפה: תווית האזור, פיצול לפי גבהים, מפה עיוורת, תווית האזור שעל סיכת הפ"מ, שם סגירה, וההערה/מגבלה המוצגות באותה תווית - וגם על תווית האזור בעורך האזורים בניהול, שהייתה נשברת באותו אופן במצב אנגלית.

**למה לא להחזיר `direction: rtl` ל-`#map-area`:** המיכל LTR נבחר במכוון (סדר העמודות), ושינוי כיוון על מיכל בן ~5,000 שורות משפיע על סדר flex, יישור טקסט ומאפיינים לוגיים בכל מה שבתוכו. בידוד ברמת המחרוזת הוא נקודתי, ובנוסף פותר גם את מצב האנגלית - שם `direction: rtl` קשיח דווקא היה שובר שם לטיני (`61 North` היה מוצג `North 61`).

**QA:** `tsc -p tsconfig.build.json` נקי ✅ · vitest 395/395 ✅ (כולל 7 חדשות ב-[bidi.test.ts](src/utils/bidi.test.ts)) · `vite build` ✅ · **אימות רינדור אמיתי ב-Chromium (Playwright, `getStartPositionOfChar` על סדר הגליפים בפועל):** בתוך מיכל `dir="ltr"` ותחת שורש RTL **וגם** LTR - `61 צפון` נקרא כפי שהוזן ✅, `61 North` נשמר ✅, ו-`getComputedTextLength`/מרכז ה-BBox זהים לפני ואחרי העטיפה (תווי הבידוד אינם משנים מרכוז) ✅.

---

## 2026-07-31 - תיקון מחוון המקלדת: מעבר לעברית לא זוהה

**התלונה:** "מעבר מעברית לאנגלית אין בעיה, מעבר מאנגלית לעברית מציג את המצב הנכון רק אחרי שהקלדתי תו עברי אחד."

**הסיבה:** `navigator.keyboard.getLayoutMap()` - באג ידוע ב-Chromium ([340949926](https://issues.chromium.org/issues/340949926)): עבור פריסה **לא-לטינית** ה-API מחזיר את מפת ה-US במקום את הפריסה בפועל. כלומר קריאת "לטינית" ממנו לא נושאת שום מידע - היא נראית זהה בפריסה עברית ובאנגלית. זה גם מסביר את האסימטריה שדווחה: אחרי החלפת פריסה המצב המוקלד אופס, ה-API ענה "אנגלית" תמיד, ולכן הכיוון עברית←אנגלית "עבד" במקרה והכיוון ההפוך הציג ערך שגוי.

**מה נעשה:** ה-API הוסר. **תשובה שגויה בביטחון גרועה מ"לא ידוע".** במקומו:
- **זיהוי החלפת הפריסה עצמה** - Alt+Shift "נקי" (בלי מקש נוסף בין הלחיצה לשחרור) או Win+Space **הופכים** את המצב מיד, בלי להקליד. בעמדה מותקנות שתי פריסות ולכן החלפה = היפוך. מצב לא ידוע נשאר לא ידוע - לא ממציאים.
- **הפרדה מקיצורי מקלדת** - דגל `dirty` נדלק כשנלחץ מקש רגיל בזמן שמקש צירוף לחוץ, ולכן Shift+אות (אות גדולה) ו-Alt+Tab אינם נחשבים החלפת פריסה. הבדיקה בשחרור נעשית **לפני** איפוס הדגלים ולכן מזוהה בדיוק פעם אחת.
- **"?" מפורש כשלא יודעים** - בעליית המסך, ובחזרה לפוקוס (הפריסה יכלה להשתנות מסרגל השפה של Windows מחוץ לאפליקציה).
- **הרמז "הקש תו לזיהוי" מוצג בשורה ולא כ-tooltip** - מסך המגע של העמדה (Cintiq) לא יודע hover.

**מה שנשאר:** התו שהוקלד בפועל (keydown + beforeinput) הוא עדיין האמת היחידה, וכל תו מתקן מיד ניחוש שגוי של ההיפוך (למשל אם מותקנות יותר משתי פריסות).

**מה זה לא פותר:** לפני שהוקלד תו ראשון אין דרך בדפדפן לדעת את הפריסה. פתרון מלא מחייב קריאה ל-Win32 `GetKeyboardLayout` מתהליך ה-main של Electron - נשקל ולא מומש: הוא מוסיף interop נייטיבי, polling ומשטח תקיפה נוסף בזמן שהמערכת בסקר אבטחה.

**QA:** `tsc` נקי ✅ · vitest 395/395 ✅ · `vite build` ✅ · **e2e 9/9** ✅ - כולל בדיקה ייעודית לבאג (Alt+Shift מחליף מיד בשני הכיוונים), ושלוש בדיקות רגרסיה: Shift+אות לא מחליף, Alt+Tab לא מחליף, ו-Alt+Shift ממצב לא ידוע לא ממציא מצב.

---

## 2026-07-31 - מחוון מצב מקלדת ליד שדה הסיסמה ב-LOGIN

**מה נעשה:** מסך ה-LOGIN רץ במסך מלא (Cintiq/Electron) ולכן מחוון השפה של Windows מוסתר; בשדה סיסמה התווים מוסתרים, ולכן הקלדה בפריסה הלא נכונה התגלתה רק אחרי "מספר אישי או סיסמה שגויים". נוסף מחוון קבוע מתחת לשדה הסיסמה: `⌨️ מקלדת: עברית/אנגלית` + התראת CAPS LOCK.
- [src/hooks/useKeyboardLanguage.ts](src/hooks/useKeyboardLanguage.ts) - הזיהוי.
- [src/components/shared/KeyboardLangIndicator.tsx](src/components/shared/KeyboardLangIndicator.tsx) - התצוגה. רכיב משותף (DRY) עם `dark` לרקע כהה, כדי שכל שדה סיסמה עתידי ישתמש בו ולא ישכפל לוגיקה.
- [src/App.tsx](src/App.tsx) - שובץ מתחת לשדה הסיסמה של הזדהות מיראז'.
- i18n: `login.keyboardLabel/keyboardHe/keyboardEn/keyboardOther/keyboardUnknownHint/capsLockOn` ב-[he.json](src/i18n/locales/he.json)+[en.json](src/i18n/locales/en.json).

**איך מזהים - שני מקורות, המוקלד גובר:** (1) **תו שהוקלד בפועל** - `keydown` (`e.key`) **וגם** `beforeinput` עם `inputType='insertText'` (`e.data`). ה-`beforeinput` אינו כפילות: יש מסלולי קלט שמייצרים טקסט בלי `keydown` (IME/insertText) - זה התגלה ב-e2e כשהקלדת תו עברי לא זוהתה כלל. הדבקה (`insertFromPaste`) לא נספרת - היא לא מעידה על מצב המקלדת. (2) **`navigator.keyboard.getLayoutMap()`** (Keyboard Map API) - נבדקים 6 מקשי אותיות; פריסה עברית מחזירה אותיות עבריות. נותן תשובה כבר לפני התו הראשון, אך קיים רק ב-Chromium/Electron ובהקשר מאובטח - בפיירפוקס/ספארי המצב נשאר "?" עד ההקלדה הראשונה (עם tooltip שמסביר). מניעת מרוץ: ה-API כותב רק ל-`probed`, ההקלדה ל-`typed`, ו-`typed` גובר; הוא מאופס ב-keyup של Shift/Alt/Meta/Ctrl (מקשי החלפת פריסה) ובחזרה לפוקוס, ואז הפריסה נבדקת מחדש.

**עיצוב:** עברית = ענבר (מצב "שים לב" - סיסמאות לרוב לטיניות), אנגלית = נייטרלי, CAPS LOCK = צ'יפ אדום נפרד. גדלים ב-px ולכן נסקלים עם `zoom: var(--s)` הגלובלי.

**QA:** `tsc -p tsconfig.build.json` נקי ✅ · vitest 388/388 ✅ (כולל i18n guard) · `vite build` ✅ · **e2e** [e2e/keyboard-lang-indicator.spec.ts](e2e/keyboard-lang-indicator.spec.ts) 5/5 ✅ - הצגה ליד שדה הסיסמה, מעבר אנגלית↔עברית לפי הקלדה אמיתית, תרגום המחוון עם המסך, צ'יפ CAPS LOCK, וצילומי שני המצבים (`login-keyboard-he.png`/`login-keyboard-en.png`) לבדיקת עין.

---

## 2026-07-30 - הפרדה ויזואלית בין פיתוח לפרודקשן (רקע ורוד ב-DEV)

**מה נעשה:** הרצה מקומית צובעת את רקע העמוד בוורוד (`#ec4899`) ומקיפה את החלון במסגרת ורודה - בשלוש האפליקציות.
- **SKY-KING** - [src/index.tsx](src/index.tsx) מוסיף `dev-mode` ל-`body` כש-`import.meta.env.DEV` דולק; [src/App.css](src/App.css) מגדיר את הרקע ואת המסגרת (`body::after`). הסימון יושב על `body` ולכן חל על **כל** המסכים (כניסה, CTRL, מגדל, דסק, ניהול) בלי שכפול ובלי לגעת ברכיב כלשהו.
- **מיראז'** - [mirage/admin.html](mirage/admin.html). מוגש כ-HTML סטטי בלי שלב build, ולכן הזיהוי לפי מארח מקומי (`localhost`/`127.0.0.1`/`::1`).
- **GAPI** (repo נפרד, `../gapi`) - `src/main.tsx` + `src/styles.css`, אותו מנגנון ואותו ורוד.

**למה `import.meta.env.DEV` ולא בדיקת `hostname`:** אפליקציית ה-Electron הארוזה טוענת `http://localhost:PORT` ([electron-main.cjs](electron-main.cjs)) - בדיקת מארח הייתה צובעת ורוד דווקא את **עמדת העבודה החיה**. Vite מקפל את התנאי בזמן build, ולכן חבילת הפרודקשן לא מכילה אותו כלל.

**החלטות עיצוב:** הוורוד קבוע בכל התמות (צבע סטטוס, כמו הכתום של סביבת תרגול ב-[EnvironmentBadge.tsx](src/components/shared/EnvironmentBadge.tsx)). המסגרת יושבת על `body` - מחוץ ל-`#root` ולזום שלו - עם `position: fixed` ו-`pointer-events: none`, ועוביה מוכפל ב-`--s` כדי שתיראה זהה בכל גדלי המסך. היא נדרשת כי מסכי העבודה פרושים על `100vh` ומכסים את רקע העמוד. הוורוד נשאר **מסביב** לתוכן ולא מתחתיו: תאי טבלה שקופים במיראז' וב-GAPI נשענו על רקע ה-`body` והפכו לטקסט אפור על ורוד - שם הוחזר רקע אטום.

**QA:** `tsc --noEmit` נקי בשתי האפליקציות ✅ · vitest 326/326 ✅ · `vite build` ✅ · **הוכחה שפרודקשן לא ייצבע:** `dev-mode` מופיע 0 פעמים בחבילות ה-JS הבנויות (רק כלל CSS מת), ובמיראז' בדיקה שלילית בפועל - `127.0.0.1` ← ורוד, `127.0.0.2` ← לא ✅ · **אימות ויזואלי:** צילום שלוש האפליקציות + כניסה מלאה לעמדת בקר דרך ה-e2e helper; `elementFromPoint` ב-3 נקודות באזור העבודה החזיר 0 חשיפות של `body` (הוורוד לא דולף לתוכן) ✅.

**כיבוי הסימון ל-e2e:** `localStorage['bt-dev-marker'] = 'off'`, מוגדר ב-[playwright.config.ts](playwright.config.ts) דרך `storageState`. Playwright מריץ תמיד את שרת ה-vite (כלומר תמיד DEV), ובלי הדגל כל תמונת ייחוס ב-`e2e/__screenshots__/` הייתה נושאת את המסגרת הוורודה - התמונות אמורות לתעד את המוצר, לא את סביבת ההרצה. **בשונה מדגל `bt-kiosk` אין כאן ערך `'on'`:** התנאי החיצוני נשאר `import.meta.env.DEV` בלבד, כך ש-Vite ממשיך למחוק את הבלוק כולו מ-bundle הפרודקשן ולא נפתחת דרך לצבוע עמדה חיה. אומת: 0 מופעים של `dev-mode`/`bt-dev-marker` ב-JS הבנוי; בפיתוח בלי דגל ← ורוד, עם `'off'` ← נקי; 25/25 e2e ✅ והתמונות נוצרו מחדש בלי המסגרת.

---

## 2026-07-30 - הזדהות מול מיראז' בלבד (הסרת משתמשים מקומיים מ-LOGIN וממסך הניהול)

**מה נעשה:** משתמשי SKY-KING מנוהלים **רק** במיראז' - כדי שהמערכת תהיה תקנית (מקור זהות אחד).
- **מסך LOGIN** ([src/App.tsx](src/App.tsx)) - הוסרו ה-checkbox "הזדהות דרך מיראז'", ה-state `authSource` (כולל `localStorage['bt-authSource']`), רשימת אנשי הצוות (חיפוש + dropdown) והקריאה ל-`GET /api/crew-members`. נשאר מסלול אחד: מספר אישי + סיסמה מול מיראז'.
- **מסך ניהול** ([src/components/admin/ManagementPage.tsx](src/components/admin/ManagementPage.tsx)) - הוסר טאב "אנשי צוות" על כל תוכנו (טופס משתמש, עמדות מאושרות, רשימה, `saveCrewMember`/`editCrewMember`/`deleteCrewMember`) ומהניווט. `adminOnlyTabs` = `strips`, `serials`, `translations`.
- **e2e** - הבדיקות נכנסו עד היום דרך רשימת המשתמשים; עכשיו [e2e/helpers.ts](e2e/helpers.ts) מזדהה דרך מיראז' (`identifyViaMirage`) עם משתמש בדיקות שנוצר בהרצה (`ensureMirageE2EUser`, idempotent). [playwright.config.ts](playwright.config.ts) מריץ גם את שרת מיראז' (`MIRAGE_DATA_FILE=e2e/.mirage-e2e.json` - אחסון מבודד, `data.json` לא נוגעים בו) ואינו מקבע יותר `bt-authSource`. `mirage/server.js` מכבד `MIRAGE_DATA_FILE`. בחירת העמדה בהלפר מדלגת על שאריות בדיקות (שם שמתחיל ב-`__`).

**למה:** מקור זהות יחיד (מיראז') = תקן. אין שתי רשימות משתמשים שיכולות להתפצל, ואין דלת עוקפת שמתעלמת מהרשאות מיראז'.

**מה לא השתנה:** טבלת `crew_members` ו-`server/routes/crew.js` נשארו - כניסת מיראז' מאחדת לפיהן העדפות אישיות לפי `personal_id`, ואיש צוות שלא קיים בהן נכנס כמשתמש וירטואלי (`id: null`). החלפת איש צוות בעמדה ממשיכה דרך `MirageCrewSwap`.

**השלכה תפעולית:** שירות מיראז' הוא **תלות קשיחה** לכניסה. שירות שנפל = אין כניסה לעמדה (`502 mirage_unavailable`).

**QA:** `tsc --noEmit` נקי ✅ · vitest 326/326 ✅ · e2e Playwright - i18n-login 5/5, dashboard-baseline 2/2, translations-admin 3/3, mission-desk 4/4 (כולל מסך הניהול), emblems 3/3 ✅ (הזדהות מיראז' אמיתית מקצה לקצה: שרת מיראז' → `POST /api/auth/mirage-login` → דשבורד).

---

## 2026-07-30 - סמל אחיד: favicon, אייקון עמדה ואייקון התראות ממקור אחד

**מה נעשה:** ה-favicon של האתר עדיין היה **הלוגו של Vite** - ברירת מחדל שנשארה מההתקנה הראשונית והוצגה בלשונית הדפדפן בכל העמדות.
- [public/favicon.svg](public/favicon.svg) הוחלף בסמל SKY KING והוא כעת **מקור האמת היחיד לכל האייקונים**: favicon בדפדפן (בשימוש ישיר כ-SVG), אייקון אפליקציית העמדה, ואייקון ההתראות. `build/icon.svg` נמחק - היה כפילות של אותו ציור.
- [scripts/build-icon.mjs](scripts/build-icon.mjs) קורא מ-`public/favicon.svg` ומייצר **שניים**: `build/icon.png` (1024) ו-`public/favicon.png` (192).
- **באג שנחשף אגב:** [ClockWidget.tsx](src/ClockWidget.tsx) הפנה התראות דפדפן ל-`/favicon.ico` - **קובץ שמעולם לא היה קיים בפרויקט**, כלומר תזכורות יצאו בלי אייקון. הופנה ל-`/favicon.png` (ל-`Notification.icon` אין תמיכה ב-SVG, ולכן נדרש ה-PNG).

**DRY:** ציור אחד, שלושה צרכנים. שינוי בסמל = `npm run icon:build` ותו לא.

**QA:** `tsc -p tsconfig.build.json` ✅ · ה-SVG אומת ברינדור Chromium (256/48/32/16) ✅ · הפניית ה-favicon ב-`index.html` כבר מצביעה ל-`/favicon.svg` - לא נדרש שינוי ✅.

---

## 2026-07-30 - אייקון לאפליקציית העמדה

**מה נעשה:** עד עכשיו העמדה הארוזה קיבלה את אייקון ברירת המחדל של Electron (הבנייה הדפיסה `default Electron icon is used`).
- [build/icon.svg](build/icon.svg) - מקור האמת. נגזר מלוגו מסך הכניסה ([src/App.tsx](src/App.tsx)) בגרסה **סטטית ומעובה**: קווי הרשת והמעגל הפנימי הוסרו, עובי הקווים הוכפל, האלומה הפכה לגזרה מלאה. הסיבה: אייקון נקרא גם ב-16x16 בשורת המשימות, ושם קו של 0.5 יחידות נעלם לגמרי.
- [scripts/build-icon.mjs](scripts/build-icon.mjs) (`npm run icon:build`) - מרנדר ל-`build/icon.png` בגודל 1024x1024 דרך Chromium של Playwright (כבר devDependency; אין בפרויקט ספריית רסטר). דגל `--preview <dir>` מייצר 256/48/32/16 לבדיקת קריאות. ה-SVG מוטמע inline ולא כ-`<img src="file://">` - דף `setContent` יושב על origin של about:blank ו-Chromium חוסם ממנו משאבי file:// (התוצאה הייתה צילום של "תמונה שבורה").
- `icon: build/icon.png` בשתי קונפיגורציות האריזה; electron-builder בונה מזה את ה-.ico. בפיתוח החלון טוען את אותו PNG ישירות (מוגן ב-`existsSync`, כי בגרסה הארוזה `build/` לא נארז).

**QA:** נבנה מתקין מלא - השורה `default Electron icon is used` **נעלמה** מהלוג ✅ · האייקון **חולץ מה-exe הבנוי ומהמתקין** ואומת ויזואלית ✅ · נבדקה קריאות ב-256/32/16 ✅ · הרצה מול Railway אחרי השינוי ב-BrowserWindow ✅.

**פתוח:** אין חתימה דיגיטלית - SmartScreen יציג "מפרסם לא ידוע" בהתקנה. נדרשת תעודת Code Signing.

---

## 2026-07-30 - עמדת Electron כלקוח דק מול Railway

**מה נעשה:** [electron-main.cjs](electron-main.cjs) הפך מ"מריץ שרת בתוך העמדה" ל**לקוח דק** שטוען את האפליקציה מהענן:
- **יעד הטעינה** נפתר לפי קדימויות: `SKYKING_STATION_URL` → `config.json`: `"mode":"local"` (שרת מקומי, legacy) → `config.json`: `"APP_URL"` → ברירת מחדל (פיתוח `localhost:5000`, הפצה `https://sky-king.up.railway.app/`). בהתקנה חדשה נוצר `config.json` עם `APP_URL`, כך שאפשר להפנות עמדה לכתובת אחרת בלי לבנות מחדש. **בהפצה כבר לא נדרש `DATABASE_URL`** בעמדה.
- שם משתנה הסביבה הוא `SKYKING_STATION_URL` ולא `SKYKING_URL` - האחרון כבר תפוס: מיראז' משתמש בו ככתובת ה-API של SKY-KING ([mirage/app.js:19](mirage/app.js#L19)).
- **חוסן רשת** (קריטי בעמדה בלי שורת כתובת): מסך מצב מקומי חדש [electron-status.html](electron-status.html) - "מתחבר לשרת" בעלייה, ו"אין חיבור לשרת" עם סיבה בעברית וספירה לאחור בכשל. ניסיון חוזר ב-backoff 2/4/8/16/30 שניות. מטופלים גם `did-fail-load`, נפילת renderer, **וגם סטטוס HTTP ≥400** בכתובת היעד - זה לא `did-fail-load`, ובלי זה עמדה הייתה מציגה עמוד 502 של Railway בזמן פריסה מחדש.
- **נעילת ניווט:** קישורים מחוץ ל-origin של האפליקציה (מפות Google מ-[GroundVehiclePanel](src/components/ground/GroundVehiclePanel.tsx#L252)) נפתחים בדפדפן המערכת דרך `shell.openExternal`; העמדה נשארת נעולה על האפליקציה. pinch-zoom מנוטרל (מסך מגע Cintiq).
- **קיצורי תחזוקה:** `F5`/`Ctrl+R` טעינה מחדש (עובד גם ממסך "אין חיבור"), `Ctrl+Shift+I` כלי פיתוח, בנוסף ל-`F11` ו-`SKYKING_WINDOWED=1` הקיימים.
- **אריזה ייעודית** [electron-builder.railway.json](electron-builder.railway.json) + `npm run electron:build:railway`: ארוזים רק `electron-main.cjs`, `electron-status.html`, `package.json` - בלי `dist/`, `server.js` ו-`node_modules` (asar של 18KB). פלט ל-`release-station/` (נוסף ל-.gitignore). סקריפטים חדשים: `electron:railway`, `electron:railway:windowed`.

**למה:** העמדה צריכה להציג את המערכת שרצה ב-Railway, לא להריץ עותק משלה. כך אין DB להגדיר בכל עמדה, ואין פער גרסאות בין העמדות לענן.

**DRY:** לא נוצר entry שני ל-Electron - אותו `electron-main.cjs` ואותו חלון kiosk משרתים את שני המצבים; מצב השרת המקומי נשמר כענף `mode:"local"`.

**QA:** **הרצה אמיתית מול Railway** - `npm run electron:railway` הדפיס `[window] mode=remote url=https://sky-king.up.railway.app/ kiosk=true frame=false` + `[load]` מוצלח ✅ · **הבנייה הארוזה אומתה** (`electron-builder --dir`, asar = 3 קבצים בלבד) והורצה: העמדה עלתה, יצרה `config.json` עם `APP_URL`, ו**צולמה עם מסך הכניסה האמיתי מ-Railway** ✅ · **נתיב הכשל אומת** - כתובת שגויה החזירה 404 והופעלו 4 ניסיונות חוזרים בהפרשים 2/4/8/16 שניות ✅ · שני מצבי מסך המצב צולמו (Playwright) - RTL, ספירה לאחור, קיצורים ✅.

---

## 2026-07-30 — מסך מלא בעליית עמדה (kiosk) בפרודקשן

**מה נעשה:** עליית עמדה בבניית פרודקשן פותחת את המסך במלואו — בלי שורת כתובת ובלי טאבים, כמו `F11`:
- כלי חדש [src/utils/kiosk.ts](src/utils/kiosk.ts) — `enterKioskFullscreen()` (אידמפוטנטי, לא זורק), `isKioskEnabled()`, `isFullscreen()`. תמיד על `document.documentElement` ולא על אלמנט פנימי, כדי ש-portals שמרונדרים ל-`body` (מודלים, מקלדת וירטואלית) יישארו גלויים.
- נקרא מתוך ה-click של הכניסה לעמדה ב-`WorkstationLogin` ([src/App.tsx](src/App.tsx)) — לפני כל `await`, כי Fullscreen API דורש user gesture תקף וחלון ההרשאה נסגר אחרי קריאות רשת. שני מסלולי הכניסה מכוסים (אישור טופס התפקידים + "דלג").
- דגל עקיפה ב-localStorage `bt-kiosk`: `off` מבטל בעמדה מסוימת גם בפרודקשן, `on` מפעיל בפיתוח לצורך אימות. ברירת המחדל נגזרת מסוג הבנייה (`import.meta.env.PROD`) — בפיתוח ובבדיקות כבוי, כדי לא להפריע.
- **Electron — kiosk מלא** ([electron-main.cjs](electron-main.cjs)): חלון העמדה נפתח `fullscreen: true` + `frame: false` (בלי X/מקסום/מיזעור) + `kiosk: true` (נעילת מסך מלא), **גם בפיתוח וגם בגרסה הארוזה** — מה שנבדק הוא מה שרץ בעמדה. `F11` משחרר/מחזיר את הנעילה (`setKiosk`) ו-`SKYKING_WINDOWED=1` מריץ בחלון רגיל — שסתומי מילוט לתחזוקה. בעלייה נרשם `[window] kiosk=… fullscreen=… frame=…` לתמיכה בשטח.
- **`npm run electron:dev` תוקן** — עבר ל-[scripts/electron-dev.mjs](scripts/electron-dev.mjs) שמנקה `ELECTRON_RUN_AS_NODE`. טרמינלים מוטמעים (VS Code/Claude Code) מגדירים אותו =1, ואז הבינארי של Electron רץ כ-Node, `app` יוצא undefined והריצה מתה מיד ב-`Cannot read properties of undefined (reading 'isPackaged')` — **בלי שום חלון**. זה הסביר "לא עובד" בהרצה מקומית.

**למה:** העמדה היא מחליף של הסדק הפיזי — סרגלי הדפדפן גוזלים שטח תצוגה ומזמינים ניווט מקרי בחדר הבקרה.

**DRY:** לא היה קוד fullscreen בפרויקט. הכלי מרוכז במודול אחד ומשרת את כל סוגי העמדות (CTRL/TWR/mission_desk) בנקודה אחת — מסך הכניסה המשותף.

**QA:** **Electron אומת בהרצה אמיתית** — `npm run electron:dev` הדפיס `[window] kiosk=true fullscreen=true frame=false` ✅ · TDD — 16 unit tests ([src/utils/kiosk.test.ts](src/utils/kiosk.test.ts)) נכתבו ונכשלו לפני המימוש ✅ · `tsc -p tsconfig.build.json` + `vite build` ✅ · 313 unit tests ✅ · **אימות בדפדפן אמיתי (Playwright, [e2e/kiosk-fullscreen.spec.ts](e2e/kiosk-fullscreen.spec.ts))** — זרימת כניסה מלאה: עם הדגל `document.fullscreenElement === <html>`, בלי הדגל נשארים בחלון רגיל ✅ · אומת ב-bundle שנבנה שברירת המחדל בפרודקשן היא "דלוק" ✅ · **חבילת ה-e2e המלאה:** 9 כשלונות **גם בלי** השינוי (הורצה פעמיים — עם הבדיקה החדשה ובלעדיה, אותו מספר; 6 מהן קבועות: `prov-drop`, `prov-drop-html5`, `provisional-point-ui`, `translations-admin`×3, והיתר מתחלפות = flakiness תלוי-DB). לא רגרסיה מהשינוי הזה, אך **פתוח לטיפול נפרד**.

---

## 2026-07-29 — אזורים מרובי-גבהים: בורר גובה לפמ, מגבלת אזור, חריגה מבלוק, פיצול תצוגה

**מה נעשה:** הרחבת מנגנון האזורים (Flight Zones) בעמדת CTRL ([SectorDashboard.tsx](src/components/views/SectorDashboard.tsx)):
- **בורר גובה לפמ** — בתפריט ה-⋮ של הפמ, בחירת הבלוק (הגובה בעל השם) מתוך `zone_altitude_ranges`. בהצבה נבחר אוטומטית הבלוק שטווחו מכיל את `strip.alt` (אחרת הראשון).
- **פיצול תצוגה לגבהים** — טוגל גלובלי "פצל לגבהים"; אזור עם ≥2 גבהים מחולק לרצועות אופקיות (צפון=גבוה→דרום=נמוך, Sutherland–Hodgman), כל רצועה מתויגת "\<אזור\> \<שם הגובה\>". רינדור משותף `renderZoneLabels` לשני מסלולי האזור (legacy + geo).
- **מגבלת אזור (קליק ימני)** — תפריט אזור: בחירת הבלוקים הפעילים (`active_alt_range_ids`) + טקסט מגבלה חופשי (`limitation_note`), מוצגים בקטן ליד שם האזור. מצב תפעולי **משותף בין העמדות** דרך `PATCH /api/map-zones/:id/operational` (ללא child-sync).
- **התראת חריגה מבלוק** — אייקון ⛔ על פמ שגובהו מחוץ לכל בלוק מוגדר, או שהבלוק שלו אינו פעיל לאחר מגבלה.
- **HINT** — בריחוף מעל אזור, tooltip עם רשימת הגבהים (🔒 = מוגבל).

**DB:** מיגרציה — `map_zones.active_alt_range_ids JSONB` + `limitation_note TEXT` (idempotent ב-[init.js](server/db/init.js), מתועד ב-[data-model.md](data-model.md)).

**DRY:** נשען על התשתית הקיימת — `zone_altitude_ranges`, `strip_zone_assignments.altitude_range_id`. מסך הניהול (הגדרת גבהים) לא שונה. תפריט הגובה/האזור בסגנון תפריט הפמ הקיים.

**QA:** `tsc` + `vite build` + 297 unit tests ✅ · i18n-guard ✅ · **DB** — מיגרציה + round-trip מול Neon אמיתי ✅ · **גאומטריית הפיצול** — אומתה ע"י שחזור האלגוריתם ✅ · **⚠️ טרם אומת ויזואלית על העמדה** — ממתין לאישור משתמש (הנחות: יחידות `strip.alt` מול הטווחים, כיוון צפון=למעלה).

---

## 2026-07-26 — RotatingEmblems: סמלי בסיס אב + מיח"ה במסך הטעינה ובסרגל העליון

**מה נעשה:** רכיב תצוגה משותף חדש [src/components/shared/RotatingEmblems.tsx](src/components/shared/RotatingEmblems.tsx) — סמל בסיס האב + סמל מיח"ה מסתובבים. `variant='loader'` (הקפה/סיבוב רציפים, החליף את לוגו הראדאר במסך הטעינה של SectorDashboard) ו-`variant='topbar'` (סיבוב כניסה חד-פעמי בעליית המערכת, נוסף לסרגל העליון של SectorDashboard ו-MissionDeskView). מותאם תמה + סקייל + `prefers-reduced-motion`.
- **סמלים אמיתיים** (Wikimedia/ויקיפדיה) הוטמעו ב-[src/assets/emblems/files/](src/assets/emblems/files/) — 8 בסיסי חה"א (רמת דוד, חצור, רמון, חצרים, תל נוף, עובדה, נבטים, פלמחים) + סמל חה"א (מיח"ה). registry לפי **שם הבסיס** (עמודת `code` ריקה) ב-[emblems.tsx](src/assets/emblems/emblems.tsx). מקורות+רישוי: [SOURCES.md](src/assets/emblems/SOURCES.md) (רובם CC BY-SA; רמון+נבטים שימוש הוגן; חה"א PD). fallback: placeholder מצויר.
- בסיס האב נפתר בכניסה מ-`preset.parent_base_id` מול רשימת `aviation_bases` ונשמר ב-`session.parentBase` ([src/App.tsx](src/App.tsx), [src/types/index.ts](src/types/index.ts)). בלי בסיס אב → מיח"ה בלבד.
- מחרוזת "מיח"ה" דרך i18n (`shared.micha`, he+en).

**למה:** זיהוי ויזואלי מיידי של הבסיס שהעמדה משרתת + מערך הבקרה (שקיפות/התמצאות).

**DRY:** `parent_base_id` כבר היה קיים מקצה-לקצה (טופס אדמין ניהול עמדות → שרת → מיראז'). **לא** נדרשה מיגרציה — מיגרציה שהתחילה נכתבה ובוטלה אחרי שזוהתה העמודה הקיימת (init.js:642, וה-FK מופל בכוונה ב-init.js:660 לצימוד רופף). הפיצ'ר רק **צורך** את הקיים.

**QA:** `tsc -p tsconfig.build.json` + `vite build` ✅ (exit 0) · **אימות ויזואלי (Playwright, `e2e/emblems.spec.ts`)** — 4 מקרים צולמו באפליקציה החיה: loader עם/בלי בסיס אב, topbar עם/בלי בסיס אב ✅.

---

## 2026-07-08 — DRY בהעברות (ממצאי code-review C+D)

**מה נעשה:** ריכוז מנגנון קריאת ההעברות ב-[server/routes/transfers.js](server/routes/transfers.js):
- חולץ `transferSelect(where, order)` — מקור-אמת יחיד לרשימת ה-SELECT + ה-JOINs (superset). 6 ה-GET (`incoming/outgoing` לפי sector/workstation + `classic-incoming/outgoing`) עברו להשתמש בו; ה-`WHERE` של כל endpoint נשמר **מילה-במילה** — אין שינוי בלוגיקת ניתוב/סטטוס/state-machine.
- חולץ `restoreStripToSender(db, stripId, fromWorkstationId)` — משותף ל-`reject` ו-`cancel` (הוסר ~14 שורות כפילות).

**למה:** 6 עותקים של אותה רשימת SELECT היו מקור לבאגי "העברה שלא מופיעה" (עמודות חסרות בחלק מה-endpoints). כעת עמודה חדשה נוגעת במקום אחד.

**מה לא נגעתי (דחוי לאישור CEO / worktrees פתוחים):** איחוד `accept`+`accept-to-map` (שינוי flow); מיזוג עמודות ניתוב `*_workstation_id`/`*_preset_id` (ממצא B — מיגרציה); מנוע polling מאוחד (ממצא A).

**QA:** `node --check` ✅ · import נקי ✅ · smoke read-only מול DB אמיתי — כל 6 השאילתות המורכבות רצות ללא שגיאה (LIMIT 0, אימות עמודות+JOINs) ✅.

### מנוע polling מאוחד (ממצא A — Phase 1, תשתית מבודדת)

**מה נעשה:** נוצר [src/hooks/usePollingRegistry.ts](src/hooks/usePollingRegistry.ts) — מנוע יחיד שמקבץ את כל משימות ה-poll לטיימר אחד (base-tick 1s), משהה כשהטאב מוסתר (Page Visibility), מריץ ריענון מיידי בחזרה, ומדלג על משימה שריצתה הקודמת עדיין באוויר (מונע הצטברות מול Neon). `usePolling(id, fn, ms)` שומר את ה-callback ב-ref (פותר stale-closure). **טרם מחובר** ל-SectorDashboard (עבודה מקבילית פעילה על הקובץ) — תשתית בלבד.

**רקע:** היום ~30 `setInterval` נפרדים (ראה ARCHITECTURE — חוב טכני #2 / בעיה #114 באקסל).

**אדופטר ראשון:** [SignalBoard.tsx](src/components/shared/SignalBoard.tsx) — הטיימר המקומי (`setInterval(load, 6000)`) הוחלף ב-`usePolling('signalboard-<presetId>', load, 6000, { immediate:false })` + effect נפרד לריענון מיידי במאונט/שינוי catalog (שימור סמנטיקה מדויק). נבחר קובץ מבודד בכוונה — **לא** SectorDashboard (עבודה מקבילית).

**QA:** 6 בדיקות vitest חדשות (batching→טיימר יחיד, תזמון, immediate, unregister, idempotent, in-flight skip) ✅ · `tsc --noEmit` נקי ✅ · כלל הסוויטה 126/126 ✅ · `npm run build` עובר (519 modules) ✅.

**Phase 2 (רכיבים מבודדים) — בוצע:** הוגרו 4 טיימרים נוספים ל-`usePolling`:
- [GroundVehiclePanel.tsx](src/components/ground/GroundVehiclePanel.tsx) — `gvp-requests` (5ש') + `gvp-gps` (5ש', `enabled` רק כשיש רכב פעיל/ממתין).
- [AdminDashboard.tsx](src/components/dashboard/AdminDashboard.tsx) — `admin-strips-global` (2ש') + `admin-dash-meta` (10ש').
- כל אחד: `doFetch` הורם ל-`useCallback`, effect נפרד לריענון מיידי, פולינג חוזר דרך המנוע. משמר-התנהגות. QA: `tsc` נקי · 126/126 · build ✅.

**Phase 3 (SectorDashboard) — בוצע:** כל **21** טיימרי-הרשת הוגרו ל-`pollingRegistry.register/unregister` (החלפה 1:1 של `setInterval`/`clearInterval` בתוך אותם effects — סגירה טרייה בכל ריצה, בלי שינוי deps או סמנטיקה). ids: `sd-main-data`, `sd-slow-data`, `sd-position-merges`, `sd-sticky-notes`, `sd-serials`, `sd-peer-messages`, `sd-bdh-alerts`, `sd-air-defense-alerts`, `sd-map2-transfers`, `sd-base-pressure`, `sd-parent-base-status`, `sd-base-statuses`, `sd-workgroup-mazaa`, `sd-tower-base-statuses`, `sd-ground-airfield`, `sd-strip-zone-assignments`, `sd-airfields-config`, `sd-active-takeoffs`, `sd-runway-conflicts`, `sd-preset-config`, `sd-collab-sync`.

**שיקול תכנוני:** ל-callbacks יציבים משתמשים ב-hook `usePolling`; לפולים שמוגדרים **בתוך** effect וסוגרים על state — ב-API האימפרטיבי `pollingRegistry`, שהוא תחליף מדויק ל-`setInterval` ולא מצריך הרמה ל-`useCallback` (שהיה משנה תלויות ומסכן רגרסיות).

**סה"כ:** 26 טיימרי-רשת → **מנוע יחיד**. טיימרי שעון-UI טהורים (ClockWidget, `rwNow`/`nowMs`) נשארו `setInterval` — אינם polling רשת.

**QA Phase 3:** `tsc --noEmit` נקי · 126/126 · build ✅ · 21 register = 21 unregister, כל ה-ids ייחודיים.

### ⛔ ממצא B (מיזוג עמודות ניתוב) — נבדק ו**נדחה**. אין לבצע.

הסקירה הראשונית סימנה את `to_workstation_id`/`to_preset_id` ככפילות. **בדיקה מול ה-DB הוכיחה שזה שגוי:**

| בדיקה | תוצאה (216 שורות) |
|---|---|
| `to_preset_id IS NULL AND to_workstation_id IS NOT NULL` | 170 (העברות סקטור) |
| `to_preset_id IS NOT NULL` | **0** |
| שתי העמודות סותרות זו את זו | 0 |
| `to_sector_id` מלא | 216 |

ה-**NULL של `to_preset_id` נושא משמעות** — הוא מבדיל בין העברת סקטור להעברה ישירה לעמדה:
- `accept()` — `if (to_preset_id) {...} else { סטריפ מקבל sector_id / on_map / x / y }`
- `classic-incoming` — הענף `t.to_preset_id IS NULL AND t.to_sector_id = ANY(...)`

Backfill מסוג `to_preset_id = COALESCE(to_preset_id, to_workstation_id)` היה מסיט **170 העברות סקטור לענף השגוי** ב-accept. **לא בוצע.**

**עיצוב יעד נכון (לעתיד, דרך `/migrate` + כיסוי בדיקות, אחרי מיזוג ה-worktrees):** להפוך את סוג הניתוב למפורש — עמודה `routing_kind` (`'sector'`/`'preset'`) + עמודת יעד אחת — במקום להסתמך על NULL-ness מרומז. expand → migrate → contract.

### ממצא D — הושלם (אושר ע"י CEO)

**1. אטומיות.** `accept-to-map` ביצע **שני UPDATE ללא טרנזקציה**: אם השני נכשל, הסטריפ עובר אך ההעברה נשארת `pending` → הפ"מ מופיע **בשתי העמדות**. עוטף כעת ב-`BEGIN/COMMIT/ROLLBACK` + `client.release()`.

**2. איחוד לוגיקת המיזוג.** לוגיקת מיזוג-האחים של פ"מ מפוצל חולצה ל-`mergeWithSiblingIfAny(client, stripId, assignedPresetId)`, ומשמשת כעת את **`accept` וגם את `accept-to-map`** (קודם הייתה מוטמעת ב-accept בלבד). ב-`accept-to-map`, אם התרחש מיזוג — הפ"מ **המאוחד** הוא זה שמונח על המפה ב-(x,y).

**3. `strip_table_assignments`.** `accept-to-map` מציב `in_table=true` וכעת גם רושם את השורה המתאימה (כשלא היה מיזוג), בדיוק כמו `accept`.

**הערות:**
- אין FK על `strip_transfers` → מחיקת הסטריפ הממוזג אינה מדרדרת, ושורת ההעברה נשארת לעדכון ל-`accepted`. אומת מול `pg_constraint`.
- הלקוח לא צורך את `mergedIntoId` (גם לא ב-`accept`) — הרענון מגיע מה-poll `sd-main-data`. ההבהוב עשוי לכוון לסטריפ שמוזג; קוויזם קיים, כעת סימטרי בשני הנתיבים.

**QA:** `node --check` ✅ · import נקי ✅ · connect/release מאוזנים ✅ · כל SQL של ה-helper (sibling lookup / merge UPDATE / DELETE / upsert) אומת מול הסכימה החיה בתוך טרנזקציה שגולגלה לאחור ✅.

---

## מצב נקודת פתיחה (Baseline)

**תאריך:** 2026-06-21

### גדלי קבצים
| קובץ | שורות | הערה |
|---|---|---|
| `server.js` | 8,075 | מונוליט — DB + 355+ routes |
| `src/App.tsx` | 41,625 | מונוליט — כל ה-frontend |
| `src/mockData.ts` | 23 | mock data (לא בשימוש פעיל) |
| `src/ClockWidget.tsx` | קיים | כבר הופרד |
| `src/VirtualKeyboard.tsx` | קיים | כבר הופרד |

### Tech Stack
- Frontend: React 18 + TypeScript + Vite + Tailwind CSS + Framer Motion
- Backend: Node.js ESM + Express 5
- DB: PostgreSQL / Neon (`pg` pool, `DATABASE_URL` מ-env)
- Desktop: Electron (`electron-main.cjs`)

---

## QA Baseline Report — מה המערכת עושה

**תאריך:** 2026-06-21 | **סטטוס:** ✅ הושלם

### ממצא חשוב: WebSocket כבר קיים!
ה-agent גילה `emit` calls בserver.js — כלומר WebSocket **כבר מיושם** (לפחות חלקית).
צריך לבדוק מה בדיוק מכוסה ומה עוד חסר.

---

### מבנה server.js

#### DB — 50+ טבלאות (לפי סדר יצירה ב-initDb):

| קבוצה | טבלאות |
|---|---|
| **Core** | `strips`, `strip_aircraft`, `strip_aircraft_armaments`, `strip_aircraft_systems` |
| **Transfers** | `strip_transfers`, `sectors`, `sector_neighbors`, `sub_sectors` |
| **Workstations** | `workstations`, `workstation_presets`, `crew_members`, `crew_member_workstations` |
| **Filters & Display** | `workstation_personal_filters`, `table_modes`, `classic_strip_tables` |
| **Airfield Ground** | `airfields`, `airfield_points`, `aviation_bases`, `airfield_routes`, `airfield_runways`, `airfield_taxiways`, `airfield_elements`, `airfield_polygons`, `airfield_sectors`, `airfield_atis`, `runway_grf`, `runway_notams`, `runway_lighting` |
| **Blocks & Zones** | `block_spaces`, `block_tables`, `blocks`, `map_zones`, `zone_altitude_ranges`, `strip_zone_assignments` |
| **Collaboration** | `work_groups`, `work_group_members`, `work_group_notes`, `sticky_notes`, `workstation_collab_state`, `workstation_messages` |
| **Admin** | `serials`, `strip_serial_selections`, `bdh_documents`, `bdh_items`, `aid_groups`, `base_statuses`, `activity_log` |
| **Driver/Vehicle** | `vehicle_requests`, `vehicle_gps`, `vehicle_messages`, `base_routes`, `element_nav_routes`, `route_links` |
| **Misc** | `learned_digits`, `preset_links`, `default_armament_names`, `default_system_names`, `closures`, `maps`, `preset_active_crew` |

#### Helper functions (ללא endpoint):
- `initDb()` — אתחול DB (שורות 21–1282)
- `cleanupExpiredStrips()` — ניקוי שעתי של סטריפים ישנים
- `mirrorClassicPartnerLinks()` — סנכרון קישורי שותף
- `bearingDeg()`, `turnLabel()`, `haversineM()`, `pctToGeo()` — geo utils
- `astarPath()` — A* pathfinding ברשת מסלולי שדה

---

### מבנה App.tsx (41,625 שורות)

| רכיב | שורות | תפקיד | שיתוף |
|---|---|---|---|
| Aircraft Icon System | 22–58 | SVG icons לפי טייסת | שניהם |
| ConfirmModal | 62–99 | דיאלוג אישור עם מקלדת | גלובלי |
| CrewMember / Session types | 102–128 | TypeScript interfaces | גלובלי |
| Query Builder DSL | 130–320 | מנוע סינון מורכב (AND/OR/NOT) | CTRL + admin |
| WorkstationLogin | 341–941 | מסך כניסה + בחירת עמדה | כולם |
| Digit Learning (OCR) | 945–987 | async helpers ל-Tesseract | לפי בקר |
| MapZoneEditor | 1,010–2,223 | עריכת אזורי מפה + גיאו-קיבוע | admin |
| MapsManager | 2,226–2,478 | ניהול מפות (upload/delete) | admin |
| LearnDigitsOverlay | 2,481–2,606 | canvas לאימון OCR | לפי בקר |
| HandwritingOverlay | 2,607–2,922 | כתב יד לגובה (Tesseract) | שניהם |
| OutgoingTransferCard | 3,013–3,099 | כרטיס העברה יוצאת | CTRL |
| IncomingTransferCard | 3,102–3,221 | כרטיס העברה נכנסת + countdown | שניהם |
| DraggableNeighborPanel | 3,224–3,789 | פאנל נקודת העברה (מוסר/מקבל) | שניהם |
| ContextMenu | 4,042–4,122 | תפריט קליק ימני להעברה | שניהם |
| DraggableMapMarker | 4,125–4,708 | marker ניתן לגרירה על מפה | CTRL |
| DraggableIncomingTransfer | 4,709–4,848 | כרטיס העברה נכנסת צף | CTRL |
| Strip (Core) | 4,849–5,716 | רכיב הסטריפ המרכזי | שניהם |
| OnScreenKeyboard | 5,741–5,806 | מקלדת וירטואלית לטאבלט | שניהם |
| TableHandwritingCanvas | 5,807–5,960 | canvas OCR לתצוגת טבלה | TWR |
| BlockMiniView | 5,961–6,098 | תצוגת בלוקי גובה | שניהם |
| GroundVehiclePanel | 6,099–7,013 | ניהול כלי רכב + מז"א (פטריוט, יבה) | admin/tactical |
| Ground constants & icons | 7,014–7,291 | SVG icons לגבולות שדה | TWR |
| ClassicBoardController | ~20K–30K | עמדת CTRL — מפה + סקטורים | CTRL |
| ClassicBoardTable | ~30K–40K | עמדת TWR — טבלה + העברות | TWR |
| App (root) | ~40K–41,625 | routing בין מסכים | גלובלי |

#### ממצאים קריטיים ל-refactor:
1. **אין State Management חיצוני** — הכל ב-local state, prop drilling סיכון
2. **fetch() מפוזר** בכל הרכיבים — אין centralized API layer
3. **אין הפרדה** בין CTRL לTWR בקוד — אותו קובץ, תלוי ב-session
4. **Ground Vehicle Panel** כולל ניהול מז"א — מורכב יותר ממה שנראה

---

### API Routes — קטלוג לפי קבוצה

| קבוצה | Routes | טבלות עיקריות |
|---|---|---|
| strips | ~25 | strips, strip_aircraft, armaments, systems |
| transfers | ~15 | strip_transfers |
| sectors | ~10 | sectors, sector_neighbors, sub_sectors |
| workstations | ~15 | workstation_presets, crew_members |
| maps | ~15 | maps, map_zones, zone_altitude_ranges |
| airfield/ground | ~40 | airfields, runways, elements, routes, polygons |
| blocks | ~10 | block_spaces, block_tables, blocks |
| collaboration | ~15 | sticky_notes, work_groups, collab_state |
| admin | ~20 | serials, BDH, aid_groups, activity_log |
| driver/vehicle | ~15 | base_routes, vehicle_*, element_nav |
| base_statuses | ~8 | base_statuses, atis |
| civilian | ~5 | civilian_strip_assignments |
| misc | ~10 | preset_links, contacts, table_modes |
| **סה"כ** | **~198 קבוצות, 355+ endpoints** | |

---

## תוכנית ארגון מחדש

### server.js → מבנה חדש

```
server/
  db/
    pool.js          ← Pool יחיד (DATABASE_URL)
    init.js          ← initDb() בלבד (CREATE TABLE)
    seed.js          ← seed data (INSERT ... ON CONFLICT DO NOTHING)
  routes/
    strips.js        ← /api/strips/*, /api/strip-aircraft/*, formations
    transfers.js     ← /api/transfers/*, /api/strips/:id/transfer*
    sectors.js       ← /api/sectors/*, /api/sub-sectors/*
    workstations.js  ← /api/workstation-presets/*, /api/crew-members/*
    maps.js          ← /api/maps/*, /api/map-zones/*, /api/zone-*
    airfield.js      ← /api/airfields/*, /api/airfield-*/*, /api/runway-*
    blocks.js        ← /api/block-*/*, /api/closures/*
    collaboration.js ← /api/sticky-notes/*, /api/work-groups/*, /api/collab-*
    admin.js         ← /api/serials/*, /api/bdh/*, /api/aid-groups/*, /api/table-modes/*
    base.js          ← /api/base-statuses/*, /api/aviation-bases/*, /api/workstation-contacts/*
    driver.js        ← /api/base-routes/*, /api/route-*/*, /driver
    activity.js      ← /api/activity-log
    strips-classic.js← /api/classic-strip-*/*, /api/strip-window-*/*, /api/strip-table-*
    civilian.js      ← /api/civ-strips/*, /api/civilian-*
  utils/
    geo.js           ← bearingDeg, haversineM, pctToGeo, astarPath
  app.js             ← express setup + כל router mounts
server.js            ← נשאר כ-entry point קצר (import app + listen)
```

### App.tsx → מבנה חדש

```
src/
  types/
    index.ts         ← כל ה-interfaces (Strip, Transfer, CrewMember...)
  components/
    shared/
      ConfirmModal.tsx
      AircraftIcon.tsx
      BlockMiniView.tsx
    login/
      WorkstationLogin.tsx
      HandwritingCalibration.tsx
    strips/
      Strip.tsx
      StripCard.tsx
    transfers/
      OutgoingTransferCard.tsx
      IncomingTransferCard.tsx
      DraggableNeighborPanel.tsx
      TransferContextMenu.tsx
    map/
      MapZoneEditor.tsx
      MapsManager.tsx
      DraggableMapMarker.tsx
    ground/
      GroundVehiclePanel.tsx
      AirfieldIcons.tsx
    input/
      HandwritingOverlay.tsx
      TableHandwritingCanvas.tsx
    views/
      ClassicBoardController.tsx  ← CTRL
      ClassicBoardTable.tsx       ← TWR
    admin/
      (AdminPanel components)
  utils/
    queryBuilder.ts  ← Query DSL logic
    handwriting.ts   ← OCR helpers
    geo.ts           ← geo calibration
  App.tsx            ← routing בלבד
```

---

## לוג שינויים

### #001 — יצירת קובץ לוג + QA Baseline
**תאריך:** 2026-06-21
**קבצים שהשתנו:** `REFACTOR_LOG.md` (חדש)
**מה נעשה:** תיעוד מצב נקודת פתיחה + ניתוח מלא של server.js ו-App.tsx
**QA לפני:** N/A
**QA אחרי:** N/A
**הערות:** WebSocket לא קיים בפועל — רק REST+polling. "emit" בדוח הQA היה שגיאה.

---

### #002 — פיצול server.js
**תאריך:** 2026-06-21
**קבצים שנוצרו:**
- `server/db/pool.js` — Pool יחיד
- `server/db/init.js` — initDb() + cleanupExpiredStrips()
- `server/db/seed.js` — seedDb() — נתוני אתחול בנפרד מסכמה
- `server/routes/crew.js` — 16 routes
- `server/routes/strips.js` — 45 routes
- `server/routes/transfers.js` — 16 routes
- `server/routes/sectors.js` — 16 routes
- `server/routes/workstations.js` — 13 routes
- `server/routes/maps.js` — 26 routes
- `server/routes/blocks.js` — 15 routes
- `server/routes/airfield.js` — 74 routes
- `server/routes/base.js` — 18 routes
- `server/routes/collaboration.js` — 27 routes
- `server/routes/admin.js` — 42 routes
- `server/routes/classic.js` — 15 routes
- `server/routes/civilian.js` — 6 routes
- `server/routes/driver.js` — 20 routes
- `server/app.js` — express setup + router mounts
- `server.js` — entry point קצר (19 שורות במקום 8,075)

**QA לפני:** server.js = 8,075 שורות, 353 routes
**QA אחרי:** ✅ 353/353 routes נשמרו בקבצים החדשים (ספירה מדויקת)
**הערות:**
- גילוי: `system_defaults` CREATE TABLE חסרה ב-initDb המקורי — נוספה ל-init.js
- גילוי: `airfield_polygon_statuses` CREATE TABLE חסרה גם — טבלה קיימת ב-DB מהיסטוריה
- seed data הופרדה מהסכמה לראשונה — init.js = schema בלבד, seed.js = נתונים

---

### #005 — Phase 2B: Extract utilities from App.tsx
**תאריך:** 2026-06-21
**קבצים שנוצרו:**
- `src/utils/session.ts` — getSession / saveSession / clearSession
- `src/utils/handwriting.ts` — compareImages (OCR similarity)
- `src/utils/notes.ts` — parseNoteValue / serializeNoteValue
- `src/utils/geo.ts` — geoToImagePct / imagePctToGeo / fmtDms + MapGeoAnchor

**QA:** ✅ כל הfunctions pure — אין side effects, אין JSX
**הערה:** App.tsx לא שונה — אלו עותקים עצמאיים. בשלב הבא App.tsx ישתמש ב-imports.

---

### #006 — Phase 2B: הרחבת src/types/index.ts
**תאריך:** 2026-06-21
**שינויים:**
- נוספו: `MapGeoAnchor`, `MapZone`, `ZoneAltRange`, `StripZoneAssignment`
- נוספו: `AircraftPos`, `GroundAircraftRow`
- נוספו: `Strip` (full interface), `Transfer` (full interface)

**QA:** ✅ 204 שורות, TypeScript valid

---

### #007 — Phase 2C: Extract leaf components
**תאריך:** 2026-06-21
**קבצים שנוצרו:**
- `src/components/shared/ContextMenu.tsx` — right-click menu להעברה
- `src/components/shared/OnScreenKeyboard.tsx` — מקלדת וירטואלית לטאבלט

**QA:** ✅ רכיבים עצמאיים עם props מוגדרים, ללא תלויות ב-App state

---

### #008 — Phase 3: ARCHITECTURE.md
**תאריך:** 2026-06-21
**קובץ שנוצר:** `ARCHITECTURE.md`
**תוכן:**
- מבנה מלא של Frontend/Backend/DB/Electron
- זרימת נתונים (Strip lifecycle, Transfer flow)
- ארכיטקטורת עמדות (CTRL vs TWR)
- DB Schema — יחסי ליבה
- מצב סנכרון (polling → WebSocket מטרה)
- מבנה מסכים (ASCII diagrams)
- workflow map עם סקילים

---

### #009 — Phase 4: Fix missing DB tables
**תאריך:** 2026-06-21
**שינוי:** `server/db/init.js` — נוספו CREATE TABLE:
- `airfield_polygons` — גיאומטריית polygon לשדה תעופה
- `airfield_sectors` — סקטורי תנועה קרקעית
- `airfield_status_types` — סוגי סטטוס תפעולי לשדה
- `airfield_polygon_statuses` — סטטוס + GRF + RVR לפי polygon
- `system_defaults` — ברירות מחדל מערכת (כבר תוקן בשלב #002)

**בעיה שנמצאה:** הטבלאות האלו קיימות ב-Neon אבל **לא היו** ב-initDb() המקורי.
→ על DB חדש (fresh) הייתה מתרחשת שגיאה בכל route שמשתמש בהן.
**QA לפני:** ❌ 4 טבלאות חסרות — Fresh DB היה נכשל
**QA אחרי:** ✅ כל 5 הטבלאות קיימות ב-init.js

---

### #010 — Phase 5: QA Final
**תאריך:** 2026-06-21
**תוצאות:**
| בדיקה | תוצאה |
|---|---|
| server.js שורות | 20 (היה 8,075) |
| Routes 353/353 | ✅ |
| server/db/*.js syntax | ✅ |
| server/routes/*.js syntax (14 קבצים) | ✅ |
| src/types/index.ts | ✅ 204 שורות |
| src/utils/*.ts (5 קבצים) | ✅ |
| src/components/shared/*.tsx (3 קבצים) | ✅ |
| DB tables (5 חסרות תוקנו) | ✅ |
| ARCHITECTURE.md | ✅ |

**App.tsx:** עדיין 41,625 שורות — foundation הונח, views split בפגישה הבאה
**אין regressions:** server.js המקורי שועתק 1:1 (353 routes, כל הלוגיקה)

---

### #003 — פיצול App.tsx — foundation
**תאריך:** 2026-06-21
**קבצים שנוצרו:**
- `src/types/index.ts` — כל ה-TypeScript interfaces (~80 שורות)
- `src/utils/aircraft.ts` — icon system לפי טייסת (~57 שורות)
- `src/utils/queryBuilder.ts` — Query DSL (AND/OR/NOT filter engine) (~218 שורות)
- `src/components/shared/ConfirmModal.tsx` — דיאלוג אישור גלובלי (~88 שורות)

**QA לפני:** App.tsx = 41,625 שורות (1 קובץ)
**QA אחרי:** ✅ App.tsx לא שונה — קבצים חדשים הם עותקים עצמאיים
**הערות:** שלב 1 של פיצול App.tsx. המשך (split לviews ו-components) בפגישה הבאה.

---

### #004 — QA סופי
**תאריך:** 2026-06-21
**בדיקות שבוצעו:**
- ✅ `node --check server.js` — OK
- ✅ `node --check server/app.js` — OK
- ✅ `node --check server/routes/*.js` — כל 14 קבצים OK
- ✅ 353/353 routes נשמרו
- ✅ `system_defaults` CREATE TABLE נוספה ל-init.js
- ✅ seed data הופרדה ל-seed.js
- ✅ App.tsx לא נשבר
**סטטוס:** ✅ אין regressions ידועים

---

### #011 — Phase 2D: חיבור App.tsx למודולים המחולצים (WIRING)
**תאריך:** 2026-06-21
**מה נעשה:** App.tsx עכשיו **מייבא בפועל** את הקבצים המחולצים והוסרו ההגדרות הכפולות.

**מיפוי coupling (קריטי):** נמצא שכל הרכיבים ב-App.tsx הם **top-level consts** עם props —
אין צימוד ל-App state. לכן פיצול הוא מכני ובטוח.

**מודולים שחוברו:**
- `./types` — AircraftIconType, CrewMember, WorkstationSession, Q* types
- `./config` — API_URL, SCREEN_SCALE_MAP (חדש)
- `./utils/scale` — scale, sc (חדש)
- `./utils/aircraft` — 4 icon functions
- `./utils/queryBuilder` — 11 items (Q_FIELDS verified identical to original via diff)
- `./utils/session` — getSession/saveSession/clearSession
- `./utils/handwriting` — compareImages
- `./utils/notes` — parseNoteValue/serializeNoteValue
- `./components/shared/ConfirmModal` — ConfirmModal + customConfirm (כיחידה, _showConfirm)
- `./components/shared/ContextMenu`
- `./components/shared/OnScreenKeyboard`

**QA לפני (baseline):**
- ✅ `tsc --noEmit` נקי
- ✅ `vite build` — 472 modules, bundle 2,699.49 kB

**QA אחרי (כל batch אומת):**
- ✅ `tsc --noEmit` נקי
- ✅ `vite build` — bundle 2,699.55 kB (הפרש 0.06 kB = רק שינוי imports)
- ✅ App.tsx: 41,625 → 41,131 שורות (494 הוסרו, מקור אמת יחיד)

**שיטת אימות:** queryBuilder אומת byte-level מול git HEAD (Q_FIELDS keys + לוגיקת
evalQLeaf/getQFieldValue זהים). bundle size כמעט זהה = הוכחת שקילות פונקציונלית.

**בעיה שנמצאה ותוקנה:** import של MapGeoAnchor התנגש עם הגדרה מקומית ב-App → הוסר מה-import.

---

### #012 — Architecture review
**תאריך:** 2026-06-21
**מה נעשה:** ARCHITECTURE.md עודכן לשקף את ה-wiring בפועל (סימון ✅ wired לכל מודול).
**ממצא ארכיטקטוני:** המבנה תקין. ה-keystone (API_URL, sc, types, shared utils) במקום —
זה פותח את חילוץ ה-views הגדולים בלי תלות מעגלית.

---

### #013 — Phase 2E: חילוץ views + ground layer (סשן 2)
**תאריך:** 2026-06-21
**שיטה:** bottom-up — קודם helpers/types, אז leaf components, אז views. build-verify אחרי כל שלב.

**מודולים שנוצרו וחוברו:**
- `src/utils/strips.ts` — getFormationDisplayName, getTransferLabel, getTransferSq, normalizeAlt, parseAltToFeet, computeBlockDeviation (אומת byte-level מול git)
- `src/utils/digits.ts` — getLearnedDigits, saveLearnedDigit, clearLearnedDigits, getDigitsCount
- `src/utils/geo.ts` — חובר בפועל (geoToImagePct, imagePctToGeo, fmtDms, buildGeoAnchor as getAnchorFromMapData)
- `src/types/ground.ts` — AircraftPos, GroundAircraftRow, MapZone, ZoneAltRange, StripZoneAssignment, GroundStatusKey, VectorLine, VectorData
- `src/components/shared/HandwritingOverlay.tsx` — OCR כתב יד
- `src/components/strips/Strip.tsx` — רכיב הסטריפ המרכזי (868 שורות, כולל _activeStripDetailsCloser singleton)
- `src/components/transfers/TransferCards.tsx` — TransferStripEditor, OutgoingTransferCard, IncomingTransferCard
- `src/components/map/MapZoneEditor.tsx` — עורך אזורי מפה (1,225 שורות)
- `src/components/ground/groundShared.tsx` — constants (מז"א, ground statuses), GroundMarkerSVG, renderGroundSvgIcon, getElemDisplayStateOpts, normalizeAircraftPositions, ptLineDist, dpSimplify, toEmbedUrl
- `src/components/views/GroundView.tsx` — עמדת מגדל TWR (4,812 שורות)

**QA לכל שלב:** ✅ tsc --noEmit נקי + ✅ vite build
**bundle:** יציב 2,699.55 kB לאורך כל החילוצים = הוכחת שקילות פונקציונלית מלאה.
**App.tsx:** 41,131 → **33,176 שורות** (~8,000 הוסרו לקבצים מודולריים).

**באגים שנתפסו ע"י tsc ותוקנו תוך כדי:**
- MapGeoAnchor import התנגש עם local def
- _activeStripDetailsCloser (singleton) — הועבר ל-Strip.tsx
- clampMenuPos — נוסף ל-imports של Strip
- MapZone.polygon — types/index.ts הגדיר string, אבל ה-runtime shape הוא array → הוגדר נכון ב-types/ground.ts
- GROUND_SVG_ICON_KEYS / AIR_DEFENSE_STATUSES — export פוספס בגלל type annotation, תוקן

---

### #014 — Phase 2F: פיצול App.tsx מלא (סשן 3) ✅ הושלם
**תאריך:** 2026-06-22
**מה נעשה:** כל הרכיבים חולצו מ-App.tsx, כולל שני הענקים. App.tsx ירד מ-41,625 ל-**728 שורות**.

**שיטה:** bottom-up מלא — types/consts → leaf components → mid components → giants. build-verify אחרי כל batch.

**מודולים חדשים (38 סה"כ):**
- **types/**: `ground.ts`, `stripGrid.ts`, `stripFields.ts` (+ index.ts מורחב)
- **utils/**: `strips.ts`, `digits.ts`, `stripGrid.ts`, `stripWindow.tsx` (+ geo/scale/session/notes/handwriting/aircraft/queryBuilder)
- **components/shared/**: HandwritingOverlay, LearnDigitsOverlay, Modals, ContextMenu, OnScreenKeyboard, ConfirmModal
- **components/strips/**: Strip
- **components/transfers/**: TransferCards, DraggablePanels
- **components/map/**: MapZoneEditor, MapsManager
- **components/ground/**: groundShared, GroundVehiclePanel
- **components/blocks/**: BlockMiniView, BlockVisualPainter
- **components/query/**: QueryBuilder
- **components/classic/**: ClassicViews
- **components/dashboard/**: AdminDashboard
- **components/admin/**: managers (12 admin managers), ManagementPage
- **components/views/**: GroundView, VerticalView, SectorDashboard

**הענקים שחולצו:**
- SectorDashboard — 14,535 שורות → `components/views/SectorDashboard.tsx`
- ManagementPage — 7,446 שורות → `components/admin/ManagementPage.tsx`
- admin managers — 3,165 שורות → `components/admin/managers.tsx`

**QA (כל batch):** ✅ tsc --noEmit + ✅ vite build. **bundle יציב 2,699.5 kB לכל אורך** = שקילות פונקציונלית מלאה, אפס regressions.

**App.tsx הסופי (728 שורות):** רק `WorkstationLogin` (מסך כניסה) + `App` (routing) + 13 imports נקיים.

**באגים ש-tsc/build תפסו ותוקנו תוך כדי:** ~15 (חוסר types/helpers משותפים, exports שפוספסו בגלל type annotation, ReactDOM→createPortal, name shadows). כולם ב-compile — אפס הגיעו ל-runtime.

---

### #015 — תשתית בדיקות (vitest) + בדיקות יחידה
**תאריך:** 2026-06-22
**מה נעשה:** הוספת vitest + בדיקות יחידה ל-utils הטהורים שחולצו בריפקטור.
- `package.json`: נוסף `npm test` (vitest run) + `npm run test:watch`
- קבצי בדיקה: `src/utils/{strips,queryBuilder,geo,notes,aircraft}.test.ts`
- כיסוי: normalizeAlt, parseAltToFeet, getFormationDisplayName, computeBlockDeviation,
  evaluateQuery/evalQLeaf/getQFieldValue, geo round-trip + fmtDms, notes round-trip, aircraft mapping

**QA:** ✅ **68/68 בדיקות עוברות** + tsc --noEmit נקי
**ערך:** רשת ביטחון ראשונה למערכת + אימות שחילוץ ה-utils בריפקטור היה נכון (התנהגות זהה למקור).

---

### #016 — תיקוני runtime (התגלו בהרצה בפועל) + ביצועים
**תאריך:** 2026-06-22
**הקשר:** אחרי שהמשתמש הריץ את האפליקציה לראשונה — באגים שרק runtime חושף (tsc/build לא תופסים).

1. **API 404** — `app.js` מן routers ב-`/api` בעוד הם מגדירים נתיב מלא `/api/*` → `/api/api/*`. תוקן: mount ב-root.
2. **runway-conflicts/active-takeoffs קרסו** — `jsonb_array_*` על `aircraft_positions` שאינו מערך (44 rows עם `{}`). הוקשח עם `jsonb_typeof()='array'`.
3. **התראות בד"ח לא הופצו** — `bdh_alerts.created_at` היה `timestamp` ללא tz; בשרת מקומי (UTC+3) הוסט 3ש' והסינון `created_at >= sessionStart` זרק כל התראה. הומר ל-`timestamptz`.
4. **ביצועים — האטת polling אגרסיבי:** loadData (strips/global+transfers) 1500→**5000ms**; workstation-messages 3000→**6000ms**; collab sync 3000→**6000ms**. (1000ms נשאר — countdown מקומי.)
5. **הקשחת pg Pool** — `max:12`, `idleTimeoutMillis:30000`, `connectionTimeoutMillis:10000` + `pool.on('error')`. מונע תקיעה כשה-pool מתמלא ומטפל ב-connections מתים של Neon.
6. **`npm run dev` cross-platform** — `concurrently` (היה `&` שלא עובד ב-Windows).

**QA מקיף:**
- ✅ tsc | ✅ 68/68 בדיקות | ✅ build (2,699 kB)
- ✅ Smoke test 22 endpoints → 200 (2 דרשו פרמטר = ולידציה תקינה)
- ✅ עומס 60 בקשות מקבילות → 60×200, ללא תקיעת pool

**לקח:** ה-QA gate צריך לכלול **smoke test runtime** — באגי routing/SQL/tz נתפסים רק בריצה.

**דפוס פתוח:** עוד עמודות `timestamp` ללא tz (`sticky_notes`, `work_group_notes`, `strips`...) — עלולות להיות מוסטות 3ש'. מומלץ audit + המרה ל-`timestamptz`.

---

### #017 — מצבי קבלה (קבל/אשר/דחה+הערה) + תצוגת נקודת העברה (שלמה/חץ)

**תאריך:** 2026-07-08

**מה נעשה:**
- **State machine של העברות** הורחב: `pending → acknowledged → accepted` / `rejected`.
  - Route חדש `POST /api/transfers/:id/acknowledge` — "אשר": המקבל אישר קבלה, הסטריפ נשאר אצל המוסר עד "קבל" סופי.
  - `POST /api/transfers/:id/reject` מקבל `note` (חובה) → נשמר ב-`reject_note`, ושולח פופאפ למוסר דרך `workstation-messages` (reuse).
  - Route חדש `POST /api/transfers/:id/dismiss` — המוסר מסלק כרטיס דחייה (כתום).
  - כל שאילתות ה-GET עודכנו: `status IN ('pending','acknowledged')` (בצד המוסר גם `rejected`) כדי ש-acknowledged/rejected לא ייעלמו.
- **UI** (`TransferCards.tsx`): כפתור **אשר**, תיבת דחייה עם הערת חובה, צביעה אצל המוסר (אושר=ירוק, נדחה=כתום, קונפליקט=אדום). `IncomingTransferCard.onReject` → `(id, note)`.
- **תצוגת נקודת העברה**: החץ (`neighborPin`) הוקטן בחצי (28×36→14×18). מתג המרה במקום שלם↔חץ (▽/△). ברירת מחדל פר-נקודה `sub_sectors.display_mode` ("הגדרת עמדה") מוחלת אוטומטית בגרירת שכן.
- **DB:** `strip_transfers.reject_note TEXT`, `sub_sectors.display_mode VARCHAR(10) DEFAULT 'full'` (ב-`init.js`).

**קבצים:** `server/routes/transfers.js`, `server/routes/sectors.js`, `server/db/init.js`, `src/components/transfers/TransferCards.tsx`, `src/components/transfers/DraggablePanels.tsx`, `src/components/views/SectorDashboard.tsx`, `data-model.md`, `SERVICES.md`, `SHARED_LANGUAGE.md`.

**Event Log:** `transfer_acknowledged`, `transfer_rejected` (עם note).

**QA:** tsc נקי · vite build ✅ · 120/120 בדיקות · syntax Backend תקין. **Runtime ויזואלי — ממתין לאימות משתמש** (דורש 2 עמדות + מפה + restart לשרת להחלת המיגרציה).

---

### #018 — דסק משימה כללי (General Mission Desk) — מודול חדש

**תאריך:** 2026-07-21 · **Branch:** `feature/general-mission-desk` · **מקור:** אפיון דסק משימה כללי.pdf

**מה נעשה:**
- **סוג עמדה חדש** `preset_type='mission_desk'` — דסק גנרי לרישום, נבנה במסך הניהול מ-3 סוגי שירותים:
  - **מסך ניהול אמצעים** (`ButtonsBoard`) — כפתורים נוצרים בעמדה בקליק ימני, מיקום חופשי בגרירה (Pointer Events, מותאם Cintiq), מצבים עם צבע לכל מצב, טקסט חופשי, פונט/גודל/מודגש, וטריגר **התראה מתפרצת** לעמדות אחרות (reuse `workstation-messages` + toast).
  - **טקסט חופשי** (`InkPad`) — דיו על canvas בקואורדינטות יחסיות, שורות הפרדה + כותרת לפי config, undo/פלנלית. ללא OCR.
  - **טבלה חכמה** (`SmartTable`) — עמודות (טקסט/מספר/V-X/תפריט), עמודות חישוב (פרסר נוסחאות ללא eval), עיצוב מותנה פר-שורה (+הבהוב), שורת סיכום (סכום/כמות/ממוצע/מינ/מקס), שורות התחלתיות והוספה בעמדה.
- **פריסת דסק** — עץ BSP (`layout_json`, אותה תבנית כמו חלון סטריפים); באדמין: פיצול אזורים + גרירת שירות לאזור (`MissionDeskAdmin`, קובץ נפרד — managers.tsx כבר ענק).
- **שיתוף שירות בין עמדות** — `mission_desk_sharing` JSONB בעמדה; כתיבת state עושה **fan-out בשרת** לעמדות המשותפות; polling (5ש') מקבל. grace 8ש' לכתיבה מקומית מונע דריסה בזמן עריכה.
- **DB:** `mission_desks`, `mission_desk_services`, `mission_desk_service_state` (UNIQUE(service,preset), TIMESTAMPTZ) + `workstation_presets.mission_desk_id/mission_desk_sharing`.
- **i18n:** קבוצת registry חדשה `missiondesk` (he+en מלא); dispatch ב-`App.tsx` (ללא נגיעה ב-SectorDashboard).

**קבצים:** `server/routes/missionDesks.js` (חדש), `server/app.js`, `server/routes/workstations.js`, `server/db/init.js`, `src/types/missionDesk.ts`, `src/utils/missionDesk.ts` (+29 בדיקות), `src/components/missiondesk/*` (5 קבצים), `src/components/admin/MissionDeskAdmin.tsx`, `ManagementPage.tsx` (tab + עורך עמדה), `App.tsx`, `src/i18n/registry/missiondesk.json`, `e2e/mission-desk.spec.ts` (חדש), `data-model.md`.

**Event Log:** `mission_desk_button_state_changed`, `mission_desk_row_added`.

**QA (עבר במלואו, כולל runtime):** tsc נקי · 167/167 unit (TDD — בדיקות נכשלות commit קודם למימוש) · vite build ✅ · smoke API מלא מול Neon (כולל fan-out שיתוף) · **2 בדיקות Playwright חדשות שרצו בפועל**: עמדה (יצירת כפתור בקליק ימני → החלפת מצב → **התראה מתפרצת נצפתה בעמדה שנייה חיה בדף נפרד** → סיכום טבלה חי → ציור דיו נשמר ל-DB → כניסה מחדש עם שרידות → activity_log) + אדמין (יצירת דסק+שירות+פריסה+שמירה). צילומי מסך אומתו ויזואלית.

**ידוע/נדחה:** אין גרירת מחיצות לשינוי יחסי פיצול באדמין (50/50 קבוע) · בחירת פונט מוגבלת ל-3 · עיכוב סנכרון עד ~8ש' בשירות שנערך במקביל בשתי עמדות.

---

### #019 — דסק משימה: אמצעים קבועים, מגע, גודל כפתור, פלנלית לפי מיקום

**תאריך:** 2026-07-21 · המשך #018 לפי משוב CEO.

**מה נעשה:**
- **אמצעים קבועים ונתוני טבלה בהגדרת עמדה** — כפתור "📌 פתח דסק להגדרה" בעורך העמדה פותח את הדסק האמיתי (`MissionDeskView adminMode`) מעל המסך; כפתורים/שורות שנוצרים שם מסומנים `fixed` ונשמרים ישירות ל-state של העמדה. קבוע (📌) בעמדה: מופעל/נגרר/טקסט חופשי — לא נמחק ולא נערך. במצב הגדרה לא נורות התראות אמת ולא נצרכות הודעות העמדה (seen).
- **מסך מגע** — פעולות גלויות ב"מסך ניהול אמצעים": ➕ צור כפתור + ✏️ מצב עריכה (לחיצה על כפתור פותחת עורך); קליק ימני נשאר כקיצור לעכבר בלבד. מחיקה גם מתוך העורך.
- **גודל כפתור** — רוחב/גובה בפיקסלים בעורך הכפתור (ריק = אוטומטי), לפי סעיף האפיון "גודל שונה לכל כפתור".
- **פלנלית** — כלי "מחק במיקום" (מחיקת strokes בגרירת הסמן, מרחק נקודה-מקטע — `eraseStrokesAt`, מכוסה 4 בדיקות) לצד "ניקוי מלא".
- **תיקונים בדרך:** רענון רשימת ה-presets ב-App בכל מעבר עמוד + בהתחברות (עמדת דסק שנוצרה כשהדף פתוח עלתה כמוד טבלאי); אייקון 🗂 בתפריט הצד; שגיאות שרת ביצירת/שמירת דסק כבר לא נבלעות.

**קבצים:** `types/missionDesk.ts` (w/h/fixed), `utils/missionDesk.ts` (+eraseStrokesAt), `missiondesk/ButtonsBoard/InkPad/SmartTable/MissionDeskView`, `admin/MissionDeskAdmin.tsx` (+MissionDeskConfigOverlay), `ManagementPage.tsx`, `App.tsx`, `registry/missiondesk.json`, `e2e/mission-desk.spec.ts` (4 בדיקות).

**QA:** tsc נקי · 171/171 unit (TDD ל-eraser) · build ✅ · **4/4 e2e בפועל** כולל: יצירת אמצעי דרך ➕ (מגע), התראה מתפרצת בין 2 דפים חיים, רגרסיית רשימה מיושנת, ותצוגת הגדרה מלאה (פתיחה מעורך העמדה → יצירת קבוע → `fixed:true` ב-DB).

---

### #020 — מיראז' (דמו) — מערכת ניהול משתמשים והרשאות + בחירת מקור הזדהות ב-LOGIN

**תאריך:** 2026-07-22

**מה נעשה:**
- **אפליקציית מיראז' נפרדת** (`mirage/`) — Express עצמאי (פורט 7300, `npm run mirage`): `POST /api/authorize` מקבל `{app, personalNumber}` ומחזיר `{authorized, roles, user}` (`admin`/`team_lead`/`user`); CRUD משתמשים + מסך ניהול (`/`, RTL, dark); נתונים ב-`mirage/data.json`.
- **מתווך ב-SKY-KING** — `server/routes/mirage.js`: `POST /api/auth/mirage-login` שולח למיראז' (`MIRAGE_URL`), ממפה roles → `is_admin`/`is_team_lead`, מאחד עם איש צוות קיים לפי `personal_id` (שומר עמדות מאושרות); אין תואם → משתמש וירטואלי (`id:null`). 403 אין הרשאה · 502 מיראז' לא זמין · timeout 4s.
- **מסך LOGIN** — checkbox "הזדהות דרך מיראז'" (**ברירת מחדל: מסומן**, נשמר ב-`localStorage['bt-authSource']`). מסומן → קלט מספר אישי במקום חיפוש איש צוות; לא מסומן → זרימת משתמשי המערכת ללא שינוי. כל הטקסטים דרך i18n (he+en).
- **e2e** — `playwright.config.ts` מקבע `bt-authSource=internal` דרך `storageState` (הבדיקות בודקות את הזרימה הפנימית).

**קבצים:** `mirage/` (app.js, server.js, admin.html, data.json, mirage.test.js), `server/routes/mirage.js`, `server/app.js`, `src/App.tsx`, `src/i18n/locales/he.json+en.json`, `playwright.config.ts`, `package.json` (סקריפט `mirage`).

**QA:** TDD — 11 בדיקות vitest למיראז' (נכתבו קודם, red→green) · tsc נקי · 185/185 unit · build ✅ · smoke מלא: מיראז' חי (authorize מורשה/לא מורשה/לא מוכר) + מתווך מול DB אמיתי (איחוד איש צוות קיים, משתמש וירטואלי, 403, 502 כשמיראז' כבוי) · e2e dashboard-baseline 2/2 ✅.

---

### #021 — מיראז': הרשאת עמדות למשתמש (בחירה מרובה מהאפליקציה + הזנה ידנית)

**תאריך:** 2026-07-22 · המשך #020.

**מה נעשה:**
- **פורמט מורחב במיראז'** — רשומת אפליקציה: `{roles, workstations}` (הפורמט הישן, מערך roles, נתמך לאחור). `workstations`: `{id, name}` מהאפליקציה או `{name}` ידני.
- **authorize** מחזיר גם `workstations` (ריק = אין הגבלה).
- **`GET /api/workstation-options`** במיראז' — מושך שמות עמדות חיים מ-SKY-KING (`SKYKING_URL`); לא זמין → `available:false` והמסך עובר להזנה ידנית.
- **מסך הניהול** — בחירה מרובה (checkboxes) של עמדות מהאפליקציה + שדה הזנה ידנית (מופרד בפסיקים) + עריכת משתמש קיים (כפתור "עריכה" → PUT).
- **המתווך ב-SKY-KING** — מפענח את `workstations` מול `workstation_presets`: עם `id` = השוואת ID טכני, בלי = השוואת טקסט (trim); התוצאה → `approved_workstations`. ריק = לפי ה-DB; לא זוהה כלל → `[-1]` (שום עמדה, מוצג "אין עמדות מאושרות").

**קבצים:** `mirage/app.js`, `mirage/admin.html`, `mirage/mirage.test.js` (+5 בדיקות TDD), `server/routes/mirage.js`.

**QA:** TDD red→green · 190/190 unit · smoke מקצה לקצה מול DB אמיתי: הגבלה לפי ID (29 ✓), לפי טקסט `בת"ק אריק` (26 ✓), משולב `[29,26]` ✓, עמדה לא קיימת → `[-1]` ✓, בלי הגבלה → נשאר לפי DB `[2]` ✓. הערה: curl בעברית מ-git-bash שולח קידוד שגוי — אימות טקסט נעשה ב-node fetch (כמו דפדפן).

---

### #022 — תיקון: בכניסת מיראז' מקור העמדות הוא מיראז' בלבד

**תאריך:** 2026-07-22 · תקלה מדווחת: בכניסה דרך מיראז' הוצגו העמדות המוגדרות למשתמש ב-SKY-KING.

**סיבת שורש (שני פערים):**
1. משתמש קיים בלי הגבלת עמדות במיראז' נפל לרשימת `crew_member_workstations` של SKY-KING — במקום "ריק = כל העמדות" כפי שמוצג במסך הניהול של מיראז'.
2. רכיב בחירת העמדה ב-LOGIN מתעלם מההגבלה עבור admin — ולכן admin ממיראז' ראה את כל העמדות גם עם הגבלה.

**התיקון:**
- שרת: בכניסת מיראז' `approved_workstations` = הגבלת מיראז' בלבד (`mirageApproved || []`) — גם למשתמש קיים.
- קליינט: `auth_source:'mirage'` על איש הצוות + ה-activity-log; הסינון חל גם על admin כשמקורו מיראז'.

**חריג מפורש (רכיב משותף):** בחירת עמדה ב-LOGIN — בכניסה **פנימית** admin רואה הכל (התנהגות קיימת, לא שונתה); בכניסת **מיראז'** ההגבלה חלה על כולם, כולל admin.

**QA:** tsc · 190/190 unit · אימות בדפדפן אמיתי (Playwright מול vite+API+מיראז' חיים): הגבלה → עמדה אחת בלבד ✓ · בלי הגבלה → כל 23 העמדות (לא ה-DB) ✓ · admin עם הגבלה → העמדה המוגבלת בלבד ✓.

---

### #023 — החלפת איש צוות בכניסת מיראז': סינון לפי הרשאת עמדה + הזדהות מחדש

**תאריך:** 2026-07-22 · המשך #020–#022.

**מה נעשה:**
- **שרת** — `GET /api/auth/mirage-eligible?presetId=N`: המורשים לעמדה לפי מיראז' (roles באפליקציה + הגבלת עמדות ריקה או שהעמדה בה, לפי id/שם). `POST /api/auth/mirage-login` מקבל `presetId` אופציונלי ואוכף את הרשאת העמדה (403 `workstation_not_permitted`).
- **רכיב משותף** `MirageCrewSwap` (DRY — ב-SectorDashboard ‏וב-MissionDeskView): רשימה מסוננת ממיראז' בלבד; בחירת איש → הקלדת מ.א. → מיראז' מאשר (כולל בדיקת התאמה בין המ.א. לאיש שנבחר) → ההחלפה. תמיכה בשתי תמות (בהיר במודל הבקר, כהה בתפריט הדסק).
- הכניסה הפנימית — זרימת ההחלפה הקיימת ללא שינוי. `crew_swap` ב-activity-log מתעד `auth_source`.

**קבצים:** `server/routes/mirage.js`, `src/components/shared/MirageCrewSwap.tsx` (חדש), `SectorDashboard.tsx`, `MissionDeskView.tsx`, `App.tsx`, `he.json+en.json`.

**QA:** tsc · 190/190 unit · אימות דפדפן מלא: eligible מסונן נכון (מוגבל-לעמדה-אחרת לא מופיע, ללא-הרשאת-אפליקציה לא מופיעה) · מ.א. שגוי → שגיאת אי-התאמה · מ.א. נכון → החלפה ליוחאי ✓ · hint מוצג ✓ · e2e mission-desk ‏4/4 (אין רגרסיה במצב פנימי).

---

### #024 — סביבות תרגול (סימולציה): סכמה לכל סביבה, בורר ב-LOGIN, באדג' בסרגל

**תאריך:** 2026-07-23 · פיצ'ר חדש (בסגנון גלקסיה).

**מה נעשה:**
- **מודל:** 50 סביבות עבודה. טסות (1-10) → סכמת `public` הקיימת (מידע טס משותף: פ"מ/סגירות/ספרורים זהים); תרגול (11-50) → סכמת PostgreSQL נפרדת לכל אחת (`env_11`…`env_50`) עם עותק של הטבלאות **התפעוליות** בלבד. קונפיגורציה (סקטורים/מפות/עמדות/משתמשים) משותפת ל-public.
- **בידוד ללא נגיעה ב-353 routes:** `pool.js` עוטף את ה-pool וקורא את סכמת הסביבה מ-`AsyncLocalStorage` (`env-context.js`) — סביבה נקבעת פעם אחת ב-middleware מכותרת `X-Env`, ו-`pool.query` מכוון אוטומטית (`SET LOCAL search_path` בטרנזקציה לסביבות תרגול; מסלול מהיר ל-public).
- **סיווג טבלאות סגור** (`env-tables.js`) — מקור אמת יחיד; `checkTableClassification` מפיל את ה-boot אם טבלה ב-public אינה מסווגת (מונע זליגת תרגול↔אמת). אומת: 95 טבלאות → 39 תפעוליות, 50 קונפיג, 6 מוחרגות.
- **יצירת סכמה** (`envs.js`) — עצלה בכניסה ראשונה (~15ש', כ-DDL אחד ב-round-trip בודד אחרי שאיטרציה פר-טבלה נמשכה >100ש'), סנכרון ב-boot, איפוס.
- **Frontend:** בורר סביבה ב-LOGIN (טסות/תרגול, ברירת מחדל 1) · `installEnvFetchInterceptor` מזריק `X-Env` לכל fetch · `EnvironmentBadge` משותף בסרגל של SectorDashboard ו-MissionDeskView (תרגול בכתום-אזהרה) · i18n מלא (registry/env.json).

**באג קריטי שנתפס ותוקן (destroy-on-release):** מול ה-pooler של Neon (pgbouncer), החלפת `client.query` להזרקת `SET LOCAL` גורמת ל-search_path לדלוף לרמת ה-server connection — connection ששב ל-pool ומשרת בקשה "טסה" קרא/כתב בטעות לסכמת תרגול (אומת: 14/14 connections מזוהמים). התיקון: connection ששירת סביבת תרגול דרך `pool.connect()` **מושמד** בשחרור (`release(err)`), לא חוזר ל-pool. אומת: 0/16.

**קבצים:** חדשים — `server/db/env-context.js`, `env-tables.js`, `envs.js`, `server/middleware/environment.js`, `server/routes/environments.js`, `src/utils/environment.ts`, `src/components/shared/EnvironmentBadge.tsx`, `src/i18n/registry/env.json`. שונו — `server/db/pool.js`, `server.js`, `server/app.js`, `App.tsx`, `SectorDashboard.tsx`, `MissionDeskView.tsx`, `index.tsx`, `types/index.ts`, `i18n/registry.ts`, `data-model.md`.

**QA:** tsc נקי · 287/287 unit · build נקי · **7/7 אינטגרציה מול Neon** (בידוד כתיבה, קונפיג משותף, שכפול FK מלא, אכיפת FK חוצה-סכמה, טרנזקציה מפורשת + 20 קריאות public נקיות — רגרסיית הדליפה) · smoke HTTP: `/api/environments`→200 (50), `X-Env:99`→400, enter env 25→200 (~14ש', schema_created=true), בידוד כתיבה מעל HTTP. ⚠️ **לא אומת ויזואלית בדפדפן** (LOGIN/סרגל) — ממתין לבדיקת משתמש.

---

### #024 — מיראז': סיסמה חזקה לפי התקן לכל משתמש

**תאריך:** 2026-07-23 · המשך #020–#023.

**מה נעשה:**
- **`mirage/password.js`** — מדיניות לפי NIST SP 800-63B: ‏12-64 תווים, אות גדולה+קטנה+ספרה+תו מיוחד, בלי מספר אישי/שם, בלי סיסמאות נפוצות. אחסון scrypt (node:crypto, בלי תלות חדשה) עם salt פר-משתמש; השוואה ב-`timingSafeEqual`.
- **authorize** דורש סיסמה; משתמש לא קיים וסיסמה שגויה מחזירים אותה תשובה (`bad_credentials` — בלי חשיפת קיום משתמש); משתמש ותיק בלי סיסמה → `password_not_set`. **הגבלת ניסיונות:** 5 כישלונות → חסימת דקה (429 `rate_limited`).
- **CRUD** — יצירה דורשת סיסמה תקנית (400 `weak_password` + פירוט קודים); עריכה מחליפה סיסמה (ריק = ללא שינוי); ה-hash לעולם לא נחשף (`hasPassword` בלבד).
- **מיגרציה** — משתמשי data.json קיבלו hash של סיסמת הדמו `Demo!Mirage#26`; ‏pg store משלים hash-ים חסרים מ-data.json ב-boot (idempotent); משתמש pg-בלבד בלי סיסמה מסומן ⚠ במסך הניהול.
- **SKY-KING** — המתווך מעביר סיסמה וממפה 401/429; שדה סיסמה ב-LOGIN וב-`MirageCrewSwap` (החלפת איש צוות); הודעות שגיאה ב-i18n he+en.

**קבצים:** `mirage/password.js` (חדש), `mirage/app.js`, `mirage/store.js`, `mirage/admin.html`, `mirage/data.json`, `mirage/mirage.test.js`, `server/routes/mirage.js`, `src/App.tsx`, `src/components/shared/MirageCrewSwap.tsx`, `he.json+en.json`.

**QA:** TDD — ‏25 בדיקות מיראז' (מדיניות, hash/verify, authorize, rate limit, CRUD, אי-חשיפת hash בקובץ) · tsc נקי · 322/322 unit · e2e API על פורטים זמניים (3197/7391, בלי לגעת בסביבה הרצה): נכונה→200, שגויה→401, חסרה→400, אחרי 5 כישלונות→429 ✓.

---

### #025 — תיקון: מסך הניהול נכנס תמיד לסביבה 1

**תאריך:** 2026-07-30 · המשך #024 (סביבות תרגול).

**הבאג:** בחירת סביבה 11/15 ב-LOGIN וכניסה ל**ניהול** פתחה את המסך על הסביבה הקודמת (ברירת מחדל 1). רק כניסה לעמדה עם אותה סביבה "תיקנה" זאת - כי `bt-env` נשמר ב-sessionStorage ושרד גם יציאה, ולכן הכניסה הבאה לניהול כבר הייתה נכונה.

**השורש:** רק מסלול עליית העמדה (`handlePresetLogin`) קרא ל-`setCurrentEnv(selectedEnv)`. כפתורי **ניהול מערכת / ניהול עמדות / תחקיר** קראו ל-`onManagement`/`setShowLoginDebrief` ישירות - בלי לקבוע את הסביבה, ולכן כל בקשותיהם נשאו `X-Env` ישן.

**מה נעשה:**
- **`enterEnvironment(env, apiUrl)`** ב-`src/utils/environment.ts` - נקודת כניסה אחת לכל מסלולי הכניסה מ-LOGIN: מנרמלת, קובעת את הסביבה לפני כל fetch, ובסביבת תרגול ממתינה ל-`POST /environments/:env/enter` (יצירת הסכמה) ומחזירה false בכישלון.
- **`App.tsx`** - שלושת המסלולים (עמדה / ניהול / תחקיר) עוברים דרכה. `handlePresetLogin` הוחלף לקריאה אחת במקום הענף הכפול.
- **`ManagementPage`** - נוסף `EnvironmentBadge` (הרכיב המשותף) לכותרת. בטיחות ATC: במסך שעורך נתונים חייבים לראות אם זו סביבת תרגול או אמת.

**קבצים:** `src/utils/environment.ts`, `src/utils/environment.test.ts`, `src/App.tsx`, `src/components/admin/ManagementPage.tsx`.

**QA:** TDD - 4 בדיקות חדשות ל-`enterEnvironment` (טסה לא חוסמת, תרגול ממתין, כישלון→false, סביבה לא חוקית לא משנה מצב) נכשלו לפני המימוש ועברו אחריו · tsc נקי · 326/326 unit · build נקי · smoke HTTP: `POST /environments/15/enter`→`{ok:true}`, `GET /strips/global` עם `X-Env:1`→87 שורות מול `X-Env:15`→3 (בידוד מאושר).

---

### #026 — רשימת תיוג: קטגוריה שנייה של מסמכים, על אותו מנגנון של בד"ח

**תאריך:** 2026-07-30 · הרחבה של שירות #11 (התראות בד"ח).

**הצורך:** רשימות תיוג תפעוליות שמתנהלות בדיוק כמו בד"ח (יצירה בניהול, סעיפים וכותרות, שיוך לעמדות), אך בעמדה הן קטגוריה נפרדת מעל בד"ח, ובפתיחה **אין** בחירת פ"מ ומספר מטוס.

**מה נעשה (DRY - בלי שכפול):**
- **DB:** עמודה אחת - `bdh_documents.kind VARCHAR(20) NOT NULL DEFAULT 'bdh'` (`'bdh'` | `'checklist'`). אין טבלאות חדשות: הסעיפים (`bdh_items`), השיוך לעמדות (`workstation_bdh`) וההתראות (`bdh_alerts`) משרתים את שני הסוגים.
- **`src/utils/bdhDocs.ts`** (חדש) - מקור אמת יחיד לסיווג: `docKind` / `isChecklistDoc` / `filterDocsByKind` / `normalizeDocKind`. כל ערך שאינו `'checklist'` (כולל מסמכים היסטוריים בלי הערך) הוא בד"ח.
- **שרת:** `GET /api/bdh?kind=` מסנן (בלי הפרמטר - הכל), `POST /api/bdh` מקבל `kind`. שאר ה-endpoints ללא שינוי.
- **ניהול:** טאב **רשימת תיוג** משתמש **באותו קוד** של טאב בד"ח (`activeTab === 'bdh' || 'checklists'`), עם `kind` נגזר מהטאב - אותו עורך סעיפים, אותה גרירה, אותו שיוך לעמדות.
- **עמדה:** מסמכי העמדה מפוצלים לשתי קטגוריות; שתיהן מרונדרות מ-`renderDocCategorySection` **אחד** (חילוץ ה-JSX ששימש את בד"ח), רשימת תיוג מעל בד"ח. הקטגוריות שבפנים **מקופלות**: פתיחת הקטגוריה הראשית מציגה רק את שמותיהן (ומונה מסמכים), לחיצה פותחת, וחיפוש פותח הכל. ה-viewer משותף - ברשימת תיוג מוסתרת כל שורת הפעולה (פ"מ, מספר מטוס ו"הפץ"); הפצת התראה נשארת של בד"ח בלבד.

**קבצים:** `src/utils/bdhDocs.ts` + `bdhDocs.test.ts` (חדשים), `server/db/init.js`, `server/routes/admin.js`, `src/components/admin/ManagementPage.tsx`, `src/components/views/SectorDashboard.tsx`, `src/i18n/registry/ctrl.json`, `src/i18n/registry/admin.json`, `data-model.md`, `SERVICES.md`, `SHARED_LANGUAGE.md`.

**QA:** TDD - 5 בדיקות סיווג · tsc נקי · 331/331 unit (כולל שומר i18n) · build נקי · smoke API על פורט זמני 3009 מול Neon: יצירת רשימת תיוג→`kind=checklist`, `?kind=checklist`=1 / `?kind=bdh`=4 / ללא פרמטר=5 (סכום מדויק), 4 המסמכים הישנים קיבלו `kind='bdh'`, שיוך לעמדה נקלט וגם ב-`bdh-preset-assignments`, `PUT` לא משנה `kind`, ניקוי מלא (המצב חזר ל-4 מסמכים ולשיוך המקורי).

### #027 — סמלי יב"א 506/509 האמיתיים ב-registry הסמלים

**תאריך:** 2026-07-30 · השלמה של `RotatingEmblems` (#025).

**הצורך:** סמל מיח"ה מוצג בכל עמדה לצד סמל הבסיס, אבל שתי יחידות הבקרה שמופיעות ב-`aviation_bases` לא היו מיוצגות נכון: `509` לא היה ב-registry כלל (העמדות שלו קיבלו את ה-placeholder המצויר), ו-`506` הסתמך על `506.jpg` - צילום סיכה 200px על רקע לבן, שסומן ב-`SOURCES.md` להחלפה.

**מה נעשה:**
- `files/509.webp` + `files/506.webp` - סמלי היחידות מוויקיפדיה (CC BY-SA 3.0, דובר צה"ל · ויקימדיה ישראל), 350px עם אלפא, במקום `506.jpg` שנמחק. הרישום ב-`BASE_EMBLEMS` הוא `'506'`/`'509'` - **בדיוק** כפי ש-`aviation_bases.name` מחזיק אותם.
- **למה WebP ולא PNG:** אלה צילומי סמל רקום עשירי-פרטים; PNG שוקל ~280KB לכל אחד לעומת ~50KB ב-WebP q0.9 באותה רזולוציה, ואיכות הצפייה בגדלים 13-92px זהה. Vite מייבא `.webp` ללא הגדרה.
- ההמרה נעשתה ב-Chromium דרך Playwright - אותה גישה כמו `scripts/build-icon.mjs` (אין ספריית רסטר בפרויקט).
- תוקן תיאור מיושן: `MichaEmblem` הוא סמל מערך הבקרה האווירית (מיח"ה 517), לא "סמל חה"א הכללי" (`emblems.tsx`, `SERVICES.md`).

**קבצים:** `src/assets/emblems/emblems.tsx`, `src/assets/emblems/SOURCES.md`, `files/506.webp`, `files/509.webp` (חדשים), `files/506.jpg` (נמחק), `SERVICES.md`.

**QA:** tsc נקי · `vite build` נקי (שני ה-assets נפלטו: 44.67KB + 53.15KB) · e2e `emblems.spec.ts` 3/3 ✅ · **אימות ויזואלי באפליקציה החיה:** כניסה לעמדות "מרחבי 305" (בסיס אב 509) ו"מרחבי 304" (506) - ה-DOM טוען `509.webp`/`506.webp` לצד `micha.png`, והסמלים נראים נכון בסרגל העליון (צילום מוגדל).

### #028 — ניהול סמלים ממסך הניהול: בוחרים תמונה ביישות "בסיסים"

**תאריך:** 2026-07-31 · הרחבה של `RotatingEmblems` (#025, #027).

**הצורך:** עד עכשיו סמל בסיס היה **קובץ בקוד** — בסיס חדש או סמל שהתחלף חייבו commit, build והפצה. הדרישה: לבחור תמונה ביישות "בסיסים" במסך הניהול.

**מה נעשה:**
- **DB:** `aviation_bases.emblem_data TEXT` (data URL) + טבלה `system_emblems(key, image_data, updated_at TIMESTAMPTZ)` לסמלים שאינם של בסיס (כרגע `micha`). שתיהן קונפיגורציה (public בלבד) ב-`env-tables.js`.
- **למה לא מפתח ב-`system_defaults`:** `GET /api/defaults` נטען בכל עמדה — תמונה בתוכו הייתה מנפחת כל טעינת דשבורד.
- **שרת:** `server/routes/emblem.js` (6 endpoints). GET מגיש תמונה **בינארית** עם ETag ו-`no-cache`, כך שה-`<img>` נטען ישירות מה-URL ומוטמן; 404 = אין סמל. הכתיבה עוברת דרך שער יחיד — `server/utils/emblemImage.js` — שמאמת סוג (**בלי SVG**: הוא מוגש מאותו origin ויכול להריץ סקריפט) ותקרה של 2MB.
- **`GET /api/aviation-bases`** מחזיר `has_emblem` בלבד ולא את התמונה — הרשימה נטענת בכל כניסה לעמדה.
- **לקוח:** `src/utils/emblemUpload.ts` מכווץ כל תמונה שנבחרת ל-350px (הגודל של הסמלים המובנים) → ~50KB לשורה. `EmblemPicker` הוא רכיב אחד שמשרת גם את סמל הבסיס וגם את סמל מיח"ה.
- **עמדה:** `RotatingEmblems` מנסה קודם את ה-DB ואז נופל לסמל המובנה ול-placeholder. הבדיקה נעשית פעם אחת לכל URL לכל טעינת עמוד (מטמון ברמת המודול) — **בכוונה בלי דגל בסשן**, כדי שסמל שהוחלף בניהול ייתפס בטעינה הבאה ולא ימתין לכניסה מחדש. אחרי שינוי בניהול המטמון מתאפס (`resetEmblemProbes`), כך שכניסה לעמדה באותו session רואה את הסמל החדש.
- **בדיקה שנתקעת ← סמל מובנה אחרי 4 שניות:** הרצת ה-e2e המלאה (26 בדיקות) חשפה מקרה שבו בדיקת הסמל לא הסתיימה והסרגל נשאר עם **חלל ריק**. חלל ריק בעמדה גרוע מסמל ברירת מחדל, ולכן יש סף זמן: אם התמונה מה-DB תגיע מאוחר יותר, היא עדיין נכנסת במקומה.

**באג קיים שנחשף אגב כך:** `coord_n`/`coord_e` היו `VARCHAR(10)`, והמסך שולח נ"צ עשרוני מלא (`30.611944444444444` = 18 תווים) — **כל שמירת בסיס עם שניות בנ"צ נכשלה ב-500** ("שגיאה בשמירה"), הרבה לפני הפיצ'ר הזה. העמודות הורחבו ל-`VARCHAR(20)` והמסך מעגל ל-6 ספרות (~10 ס"מ).

**קבצים:** `server/routes/emblem.js`, `server/utils/emblemImage.js` + test (חדשים), `src/utils/emblemUpload.ts` + test (חדש), `src/components/admin/EmblemPicker.tsx` (חדש), `e2e/emblem-admin.spec.ts` (חדש), `server/app.js`, `server/routes/base.js`, `server/db/init.js`, `server/db/env-tables.js`, `src/components/shared/RotatingEmblems.tsx`, `src/assets/emblems/emblems.tsx`, `src/components/admin/ManagementPage.tsx`, `src/i18n/registry/admin.json`, `data-model.md`, `SERVICES.md`.

**QA:** TDD (הבדיקות נכתבו לפני המימוש ונצפו אדומות) · tsc נקי · 375/375 unit · build נקי · **smoke API מול Neon (25 בדיקות):** העלאה/קריאה/הסרה, ETag→304, `has_emblem` מתעדכן, SVG/URL חיצוני/חריגת גודל נדחים ב-400, בסיס לא קיים→404, ו-`GET /api/defaults` נשאר 90 תווים · **e2e מקצה לקצה** (`emblem-admin.spec.ts`, 10/10 בשתי חזרות): העלאת תמונה בטאב "בסיסים" → ממוזער בטבלה → **התמונה מוצגת בעמדה במקום הסמל המובנה**; ולסמל מיח"ה גם המסלול ההפוך — הסרה מחזירה את הסמל המובנה. ה-DB הוחזר למצבו (אין סמלים שמורים; העמדות ממשיכות להציג את הסמלים המובנים).

### #029 — "שגיאה בכניסה" ב-DEV: לוג האזנה שקרי ב-Express 5 + הודעת שגיאה שהסתירה שרת מת

**תאריך:** 2026-07-31 · תחקור תקלה שדווחה ממסך ה-LOGIN.

**התסמין:** הזדהות מול מיראז' ב-DEV החזירה "שגיאה בכניסה" — נוסח שמשמעו למשתמש "הסיסמה שלי שגויה".

**מה קרה בפועל:** שרת ה-API (3001) לא רץ. הדפדפן פנה ל-`/api/auth/mirage-login`, פרוקסי Vite ניסה `localhost:3001`, קיבל ECONNREFUSED והחזיר **500 text/plain**. `handleMirageLogin` מיפה 401/403/429/502 להודעות מדויקות, וכל השאר נפל ל-`else` הגנרי. המיראז' עצמו (7300) היה תקין לגמרי.

**שורש הבעיה — `app.listen` של Express 5 משקר:** `application.js` רושם את ה-callback שלך גם כמאזין ל-`'error'` (`server.once('error', done)`). לכן bind כושל מפעיל דווקא את ה-callback ה"מוצלח": השרת מדפיס `SKY-KING API listening on 0.0.0.0:3001` בזמן ש-`server.address()` הוא `null`. גרוע מזה — עצם קיום המאזין ל-`'error'` מונע קריסה, אז התהליך נשאר **חי, שקט וללא פורט**. אומת אמפירית: `net.createServer` לא מתנהג כך, `app.listen` של Express 5.2.1 כן.

**מה נעשה:**
- `server/listen.js` (חדש) — `listen(app, port, host)` שנשען על אירוע `'listening'` (אמין) ומחזיר Promise: resolve רק כשבאמת מאזינים, reject אחרת. משמש גם את `server.js` וגם את `mirage/server.js` (עזר רשת טהור; שניהם נארזים לאותו image). `server.js` ומיראז' יוצאים עכשיו ב-`exit 1` עם סיבה במקום להדפיס "listening" ולהיתקע.
- `src/utils/mirageAuthError.ts` (חדש) — מיפוי סטטוס→מפתח i18n, **משותף** ל-`App.tsx` ול-`MirageCrewSwap` שהחזיקו עד כה שתי העתקות שהתחילו להיפרד (רק אחת מהן טיפלה ב-`workstation_not_permitted`). מחזיר מפתח ולא טקסט, כך שהבדיקה רצה בלי i18n.
- **`5xx` הופרד מ"שגיאה בכניסה"** → `login.errorServerDown` ("השרת אינו זמין"). בעמדה מבצעית ההבדל בין "טעיתי בסיסמה" ל"המערכת למטה" הוא ההבדל בין ניסיון נוסף לקריאה לתמיכה. `502` נשאר ייעודי: השרת חי אבל *המיראז'* לא ענה.
- מיראז' נופל ל-IPv4 אם למארח אין IPv6 (`EAFNOSUPPORT`/`EADDRNOTAVAIL`/`EINVAL`) במקום לא לעלות; `EADDRINUSE` נכשל בקול. הלוג מדווח את הכתובת שנתפסה בפועל (`server.address()`), לא את זו שביקשנו.

**קבצים:** `server/listen.js` + test (חדשים), `src/utils/mirageAuthError.ts` + test (חדשים), `server.js`, `mirage/server.js`, `src/App.tsx`, `src/components/shared/MirageCrewSwap.tsx`, `src/i18n/locales/he.json`, `src/i18n/locales/en.json`, `SERVICES.md`.

**QA:** TDD (שתי חבילות הבדיקות נכתבו לפני המימוש ונצפו אדומות) · tsc נקי · 388/388 unit · build נקי · **אימות התנהגות בפועל:** פורט תפוס → `exit 1` + סיבה, בלי שורת "מאזין" (לשני השרתים); פורט פנוי → מיראז' עולה dual-stack ונענה מ-`127.0.0.1` ומ-`[::1]`; `POST /api/auth/mirage-login` עם סיסמה שגויה מחזיר `401 bad_credentials` דרך 3001 וגם דרך פרוקסי Vite.

### #029 — תיקון: מסך הטעינה עלה בלי הסמלים המסתובבים

**תאריך:** 2026-07-31 · תיקון ל-#028.

**התופעה (דיווח מהשטח):** אחרי בחירת עמדה, במקום שני הסמלים המסתובבים היה **חלל ריק**. ובסרגל העליון הוצגו הסמלים המובנים ולא אלה שהועלו בניהול.

**המדידה:** ציר זמן של ה-DOM מרגע הכניסה הראה שמסך הטעינה חי 1.0–3.9 שניות **בלי אף `<img>` של סמל**, והסמלים הופיעו רק ב-t+5.2 שניות — ואז דווקא המובנים.

**השורש:** #028 בדק תחילה אם קיים סמל ב-DB (`new Image()`), ורק לפי התשובה צייר. הבקשה הזו יוצאת בדיוק כשהדשבורד פותח את מטח קריאות ה-API שלו, ו-HTTP/1.1 מגביל ל-6 חיבורים למקור — כך שהיא נתקעה בתור מעבר לסף ה-4 שניות שהוגדר, ונפלה לסמל המובנה.

**התיקון (הסדר התהפך):**
- **הסמל המובנה מצויר מיד**, ותמונת ה-DB מחליפה אותו ב-`onLoad`. אין יותר רגע ריק - הבדיקה בוטלה, כי ה-`<img>` **הוא** הבדיקה. `onError` (אין סמל ב-DB) מסיר את התמונה מה-DOM ומשאיר את המובנה.
- `warmEmblems` ([src/utils/emblemSource.ts](src/utils/emblemSource.ts)) נקרא ב-`App` מיד אחרי הכניסה, כשהדפדפן עוד פנוי - לכן התמונה כבר במטמון כשהמסך עולה.
- ההגשה עברה מ-`no-cache` ל-`max-age=60`: בלי הטמנה אמיתית כל טעינת מסך שלחה בקשה שמתחרה באותו מטח.

**מדידה אחרי:** בפריים הראשון שבו מסך הטעינה מופיע, **שני הסמלים מה-DB כבר טעונים ומוצגים**.

**באג בבדיקה עצמה שתוקן אגב כך:** ה-`afterEach` של `emblem-admin.spec.ts` **מחק** את סמל מיח"ה ואת סמל בסיס 509 - כלומר הרצת הבדיקה מחקה סמלים אמיתיים שהועלו בניהול, בלי דרך לשחזר. עכשיו הבדיקה מצלמת את הסמלים הקיימים ב-`beforeEach` ומחזירה אותם ב-`afterEach`.

**קבצים:** `src/utils/emblemSource.ts` (חדש), `src/components/shared/RotatingEmblems.tsx`, `src/App.tsx`, `server/routes/emblem.js`, `src/components/admin/ManagementPage.tsx`, `e2e/emblem-admin.spec.ts`, `e2e/emblems.spec.ts`.

**QA:** tsc נקי · 388/388 unit · build נקי · e2e 5/5 (`emblems` + `emblem-admin`) · אימות ויזואלי של מסך הטעינה + ציר זמן DOM לפני/אחרי.

---

### #030 — מסכים גדולים מ-15.6": סרגל עורך אזורי המפה יצא מחוץ למסך + כפתורים קטנים מדי לעט

**תאריך:** 2026-07-31

**התופעה (דיווח מהשטח):** "מה שקיים היום לא טוב במסכים גדולים מ-15.6". במסך 24" הכפתורים בסרגל עורך אזורי המפה קטנים מדי.

**המדידה:** צילום המסך בפועל (Playwright, 1920x1080, כניסה אמיתית → ניהול → מפות → אזורים) הראה שב-24" **הכותרת כולה, ובתוכה סרגל הכלים, נמצאת מחוץ למסך**. הקופסה הפנימית התחילה ב-y ≈ 288- פיקסלים.

**השורש:** `#root { zoom: var(--s) }` מכפיל **כל** אורך שמחושב בתוכו, כולל יחידות חלון. חלון העורך מומד `96vw x 93vh`, ולכן ב-24" (`--s = 1.65`) הוא נמתח פי 1.65 מהמסך; מכיוון שהוא ממורכז אנכית, העודף נגזר חצי-חצי - והכותרת (הסרגל) נעלמה מעל הקצה העליון. ב-15.6" (`--s = 1`) הבעיה לא מתקיימת, ולכן היא לא נראתה בפיתוח.

**התיקון:**
- **גלישה:** יחידות החלון של החלון והרקע מחולקות בסקייל - `calc(100vw / var(--s,1))`, `calc(96vw / var(--s,1))` וכו'. התוכן ממשיך לגדול עם הזום הגלובלי, אבל החלון יושב בדיוק על המסך האמיתי בכל גודל.
- **גודל הכפתורים:** מכפיל ייעודי לכפתורי סרגל בלבד מעל הזום הגלובלי - 15.6"=x1.0 · 16"=x1.1 · 18"=x1.2 · 24"=x1.4. הזום הגלובלי מגדיל הכל אחיד, ולכן כפתור 28px נשאר קטן *ביחס* למסך; בסרגל צפוף זה לא מספיק לעט/מגע על Cintiq 24. שאר הכותרת (שם המסך, תוויות מצב) לא הוגדל.

**קבצים:** `src/utils/scale.ts` (`TOOLBAR_SCALE_MAP`, `getToolbarScale`, `tbPx`, `readToolbarScale`), `src/hooks/useToolbarScale.ts` (חדש), `src/components/map/MapZoneEditor.tsx`, `src/utils/scale.test.ts` (חדש).

**QA:** TDD (7 בדיקות אדומות → ירוקות) · tsc נקי · 501/501 unit · build נקי · אימות ויזואלי ומדידה ב-4 גדלי מסך: הכותרת יושבת ב-y≈28 בכולם; רוחב כפתור 28px → 31/34/39; pan, zoom, איפוס ודיוק לחיצה (סטייה 0-1px) זהים ב-15.6" וב-24".

**פתוח:** אותה מלכודת `vw/vh` תחת `zoom` עשויה לחול על מודאלים נוספים שממודדים ביחידות חלון - לא נסרקו בשינוי זה.

---

### #031 — נקודות העברה קבועות על המפה (הגדרה חד-פעמית בניהול עמדה)

**תאריך:** 2026-07-31

**הצורך (מהשטח):** "בניהול עמדה עם מפה אני רוצה לעשות עריכת מפה ולהגדיר באופן קבוע נקודות העברה על מפה".

**המצב שהיה:** נקודת ההעברה נגררה למפה **ידנית בכל משמרת** ולא נשמרה בשום מקום - `neighborPins`/`neighborMarkers` היו `useState` בלבד ב-`SectorDashboard`. עורך המפה היה נגיש רק מ"ניהול מפות" ותמך באזורים/עוגנים/סקטורים בלבד.

**התיקון:**
- **DB:** טבלה `map_transfer_points`. `preset_id = NULL` = ברירת מחדל למפה (חלה על כל עמדה שמשתמשת בה); `preset_id` מלא = דריסה של עמדה מסוימת. `sub_label` מבדיל בין הנקודה השלמה לתת-נקודה. המיקום ב**אחוזי תמונה** + נ"צ (`lat`/`lon`) כשהמפה מכוילת. אינדקס ייחודי על `(map_id, COALESCE(preset_id,0), sector_id, COALESCE(sub_label,''))` שמאפשר UPSERT.
- **API:** `GET/POST/DELETE /api/map-transfer-points`. ה-GET מחזיר את התמונה ה**אפקטיבית** - השרת ממזג את ברירת המחדל של המפה עם דריסות העמדה (`is_override`).
- **עורך:** מצב חדש "🔀 נקודות העברה" ב-`MapZoneEditor` (הרחבה, לא רכיב חדש - DRY): בחירת נקודה מהקטלוג → לחיצה על המפה למיקום, גרירה להזזה, מתג חץ/פאנל מלא, ומתג היקף (ברירת מחדל למפה / ייחודי לעמדה).
- **ניהול עמדה:** כפתור "🗺 עריכת מפה ונקודות העברה" מתחת לבורר המפה, שפותח את אותו עורך בהקשר העמדה (הקטלוג מצטמצם לנקודות ההעברה שלה).
- **זמן ריצה:** הנקודות נזרעות למפה אוטומטית בכניסה לעמדה (פעם אחת לכל עמדה+מפה, כולל מפה 2). הזזה/הסרה במשמרת נשארת **זמנית**; כפתור ⟲ בפאנל נקודות ההעברה מחזיר למיקום הקבוע.

**קבצים:** `server/db/init.js`, `server/db/env-tables.js` (סיווג CONFIG), `server/db/foreign-keys.js` (3 FK), `server/routes/maps.js`, `src/components/map/MapZoneEditor.tsx`, `src/components/admin/ManagementPage.tsx`, `src/components/views/SectorDashboard.tsx`, `src/i18n/registry/{map,admin,ctrl}.json`, `e2e/map-transfer-points.spec.ts` (חדש).

**QA:** tsc נקי · 501/501 unit · build נקי · smoke מול ה-DB האמיתי (UPSERT שומר על אותו `id`, דריסה גוברת על ברירת המחדל, מחיקת דריסה מחזירה לברירת המחדל, round-trip של תת-נקודה בעברית ושל נ"צ, אין אזהרות FK/סיווג טבלאות ב-boot) · 2 בדיקות e2e דרך המסך האמיתי: מיקום נשמר ל-DB מתוך העורך שבהגדרת העמדה, והנקודה נטענת אוטומטית למפת העמדה בכניסה (נבדק לפי `.neighbor-pin-drop-zone[data-pin-sector]`, לא לפי טקסט בלבד).

**פתוח:** אין עדיין דרך למקם נקודות ישירות בהקשר מפה 2 מתוך העורך (העורך נפתח על מפה 1 של העמדה); נקודות מפה 2 מוגדרות דרך "ניהול מפות" על אותה מפה.

---

### #032 — חץ נקודת ההעברה הסתיר את הפ"מים על המפה

**תאריך:** 2026-07-31

**התופעה (דיווח מהשטח):** "כל נקודות העברה חץ על מפה מופיע ON TOP מעל כל היישויות - הן צריכות להיות מתחת כל היישויות למעט סימון של אזורים / אזורים מחוברים".

**השורש:** חץ נקודת ההעברה נוצר עם `zIndex: 80` - מעל **כל** שכבות המפה. הוא סימון עזר, לא יישות, ולכן הסתיר מידע תפעולי (פ"מ שנפל מתחתיו נעלם).

**סדר השכבות על המפה (אחרי התיקון):**

| z | שכבה |
|---|---|
| 1 · 2 · 3 | אזורים · מתאר עיוורת · אזורים מחוברים |
| 12 | סגירות מרחב |
| **20** | **חץ נקודת העברה** |
| 43 · 45 | קווי חיבור · פ"מ במצב אזורי טיסה |
| 50 | פ"מ על המפה · נקודת העברה שלמה (פאנל) |
| 60 · 76 | כפתורי פ"מ · חצים מקווקוים |
| 190 · 250 · 500 | פיני אזורים · נקודה זמנית · כפתורי פיצול/מיזוג |

**התיקון:** `zIndex: 80` → `20` בשכבת הפינים ב-`SectorDashboard.tsx`, עם תיעוד סדר השכבות בקוד. הנקודה השלמה (`DraggableMapMarker`) נשארה ב-50: היא כרטיס-יישות ולא סימון.

**קבצים:** `src/components/views/SectorDashboard.tsx`, `e2e/map-transfer-points.spec.ts`.

**QA:** TDD - הבדיקה ממקמת חץ **בדיוק מתחת לפ"מ** שעל המפה ושואלת את הדפדפן מי העליון (`elementFromPoint`). נכשלה על הערך הישן ("החץ מסתיר את הפ"מ") ועברה אחרי התיקון, כולל אימות שהחץ עדיין **מעל** שכבת האזורים. tsc נקי · 501/501 unit · 4/4 e2e.

**לקח (חשוב):** הבדיקה כתבה לנקודה קיימת ב-DB, וכתיבה כזו היא UPSERT - כלומר **דריסת מיקום שהוגדר בעבודה אמיתית**. שורה אחת (`רפיח למרכז / צפון`, מפה 24) נדרסה בפועל לפני שהתגלה. ה-afterEach שוכתב כדי **לצלם את השורות לפני ולהחזירן אחרי** (התבנית של `emblem-admin.spec`), ואומת שהרצה מלאה משאירה את הנתונים זהים בייט-בייט.

---

### #033 — חבילת התקנה רזה של העמדה (בלי מנוע התמלול המקומי)

**תאריך:** 2026-08-01

**הצורך:** המתקין של העמדה שוקל **583 MB**, כי מודל העברית של whisper (`ggml-model.bin`) נארז לתוכו. לשליחת העמדה למחשב אחר זה כבד מדי, ולא בכל עמדה צריך זיהוי קולי.

**מה נעשה:** קונפיגורציית בנייה שנייה - `electron-builder.railway-lite.json` - זהה ל-`electron-builder.railway.json` פרט לשלושה דברים:

| | מלא | רזה |
|---|---|---|
| `extraResources` (whisper) | נארז | **הוסר** |
| `directories.output` | `release-station/` | `release-station-lite/` |
| `artifactName` | ברירת מחדל | `SKY-KING-Station-Lite-Setup-${version}.exe` |

**תוצאה:** 583 MB → **82 MB** (פי 7.1 קטן יותר).

**למה אותו `appId` ואותו `productName`:** זו אותה עמדה, רק אריזה אחרת - שתי החבילות מתקינות לאותו מקום ומחליפות זו את זו. מתקינים אחת בלבד.

**התנהגות בלי מנוע התמלול:** `sttStatus()` מחזיר `stt-missing-binary`, והמסך מתרגם אותו ל-`ctrl.voiceErrSttMissing` ("מנוע התמלול לא מותקן בעמדה"). שאר העמדה זהה. אפשר להוסיף תמלול לעמדה רזה בדיעבד בלי התקנה מחדש - להעתיק את תיקיית whisper ולהצביע עליה ב-`WHISPER_DIR` שב-`config.json`.

**קבצים:** `electron-builder.railway-lite.json` (חדש), `package.json` (סקריפט `electron:build:railway:lite`), `.gitignore`.

**QA:** הבנייה עברה (exit 0). אומת ש-`resources/` של החבילה הרזה מכיל `app.asar` + `elevate.exe` **בלבד** (בלי `whisper/`), ושה-asar עדיין מכיל את `electron/whisper.cjs` - כלומר הקוד קיים ורק המשאבים חסרים, מה שמייצר את הודעת "לא מותקן" במקום קריסה. 20/20 יוניט של `electron/whisper.test.js`. **הרצת החבילה הארוזה לא נבדקה מהסביבה הזו**: גם החבילה המלאה שכבר בשימוש נסגרת מיד כשמריצים אותה מה-shell, כלומר מגבלת סביבה ולא ממצא על החבילה הרזה.

---

### #034 — דסק משימה: הפריסה בעמדה יצאה הפוכה (מראה) ממסך ההגדרה

**תאריך:** 2026-08-01

**התופעה (דיווח מהשטח):** "דסק משימה מופיע הפוך בעמדה ממה שיש בניהול המבצעי". אותו דסק בדיוק: במסך ההגדרה סדר האזורים ימין→שמאל (אמצעים · תמונה · טקסט קבוע), ובעמדה שמאל→ימין. גם עמודות הטבלה החכמה יצאו הפוכות.

**השורש:** קנבס הדסק (`MissionDeskBody`) מרנדר את עץ הפריסה ב-`display:flex` ולכן **סדר האזורים נקבע ע"י הכיווניות שהוא יורש**:

| הקשר | ההורה | הכיווניות בפועל |
|---|---|---|
| מצב הגדרה (`MissionDeskView`) + עורך הפריסה (`MissionDeskAdmin`) | שורש המסמך | `rtl` |
| עמדה (`SectorDashboard`) | המְכל המבני `dir="ltr"` (`SectorDashboard.tsx:8094`) | `ltr` |

המְכל ה-LTR הזה קיים בכוונה - סדר העמודות של עמדת הבקר (נקודות העברה משמאל … עזרים מימין) תוכנן ל-LTR - אבל הדסק נכנס לתוכו וספג ממנו מראה.

**התיקון:** הקנבס מקבע את כיווניותו בעצמו לפי שפת המערכת (`i18n.dir()`) במקום לרשת אותה מההורה. כך אותה פריסה נראית זהה בשני ההקשרים, ובאנגלית שניהם מתהפכים יחד.

בנוסף, חישוב גרירת הספליטרים עבר מ-`document.documentElement.dir` ל-`dir` המקומי של הקנבס - אותה מלכודת בדיוק (הקנבס היה LTR בעמדה בזמן שהמסמך RTL, ולכן הגרירה יצאה הפוכה שם).

**קבצים:** `src/components/missiondesk/MissionDeskBody.tsx`, `e2e/mission-desk.spec.ts`.

**QA:** TDD - בדיקת רגרסיה חדשה מקימה דסק עם פיצול אופקי של שני אזורים, נכנסת לעמדה, ובודקת ש-`getComputedStyle(canvas).direction === 'rtl'` ושהאזור הראשון בפריסה נמצא **מימין** לשני. נכשלה לפני התיקון ועברה אחריו. tsc נקי · 612/612 unit · 6/6 e2e של דסק משימה · build עבר.

---

### #035 — מתקין macOS לעמדה (DMG), ותיקון קונפיג שבור של העמדה העצמאית

**תאריך:** 2026-08-02

**הבקשה:** חבילת התקנה ל-mac, כמו זו שנבנתה ל-Windows.

**המחסום:** electron-builder **חוסם** בניית יעדי macOS ממכונת Windows (`Build for macOS is supported only on macOS`), ויצירת DMG נשענת על `hdiutil` של מערכת ההפעלה. לכן אין דרך לייצר את הקובץ מהמכונה הזו - הבנייה עברה ל-GitHub Actions על runner של mac (הריפו ציבורי, דקות ה-mac חינם).

**מה נבנה:**
- `.github/workflows/build-mac.yml` - `macos-latest` → `npm ci` → `electron-builder --mac` על תצורת ה-Lite → DMG + ZIP ל-`x64` ול-`arm64` כ-artifact. הפעלה ידנית, או אוטומטית בדחיפה שנוגעת בקבצי האריזה.
- `scripts/mac-adhoc-sign.cjs` - hook `afterPack` שחותם את ה-`.app` חתימת ad-hoc. **בלעדיו המתקין חסר תועלת ב-Apple Silicon:** electron-builder שובר את חתימת Electron המקורית באריזה (שינוי שם הבינארי, הזרקת `app.asar`, עריכת `Info.plist`), ומק arm64 מסרב להריץ בינארי שחתימתו אינה תקפה ("האפליקציה פגומה"). `identity: null` לבדו רק מדלג על החתימה ולא מחזיר אותה.
- `electron-builder.railway-lite.json` - בלוק `mac`: יעדי dmg+zip לשתי הארכיטקטורות, `artifactName` הכולל `${arch}` (בלי זה שתי הארכיטקטורות דורסות זו את זו באותה ריצה), `identity: null`, ו-`NSMicrophoneUsageDescription` ב-`extendInfo` - בלעדיו macOS דוחה את בקשת ההקלטה של `getUserMedia`.

**ממצא צדדי:** `electron-builder.station.json` (העמדה העצמאית לרשת מבודדת) **נכשל בוולידציה** מאז שנוסף לו מפתח `_comment` - electron-builder אוסר מפתחות לא מוכרים, גם ברמה העליונה. כלומר `npm run electron:build:station` לא רץ מעולם בהצלחה. המפתח הוסר וההסבר עבר ל-SERVICES.md; הקונפיג עובר עכשיו ואורז.

**מגבלה מתועדת:** תמלול מקומי (whisper) לא נכלל בתצורה זו, ו-`vendor/whisper/` מכיל בינארי Windows בלבד - תמלול במק ידרוש `whisper-cli` + `.dylib`. בנוסף, חתימת ad-hoc אינה עוקפת Gatekeeper בקובץ שהורד: פתיחה ראשונה בקליק ימני → Open, או `xattr -dr com.apple.quarantine`.

**קבצים:** `.github/workflows/build-mac.yml`, `scripts/mac-adhoc-sign.cjs`, `electron-builder.railway-lite.json`, `electron-builder.station.json`, `package.json`, `.gitignore`, `README.md`, `DEV_GUIDE.md`, `SERVICES.md`.

**QA:** `electron-builder --mac --dir` על Windows עובר ולידציית סכמה ונעצר **רק** בחסימת הפלטפורמה (כלומר הקונפיג תקין) · אריזת Windows של אותה תצורה רצה עם ה-hook החדש ללא שגיאה · ה-hook נבדק ב-Node ויוצא מיד ב-`win32` בלי לקרוא ל-`codesign` · `electron-builder.station.json` עבר מכשל ולידציה לאריזה מוצלחת · tsc נקי · 654/654 unit.

**הריצה הראשונה על mac אמיתי** ([run 30734168995](https://github.com/levori119/skyboard/actions/runs/30734168995)) אישרה את החתימה - `valid on disk` + `satisfies its Designated Requirement` - ובנתה בפועל DMG ו-ZIP ל-`arm64` ול-`x64`, אבל **נכשלה בסוף**: electron-builder מנסה ב-CI לפרסם GitHub Release מיוזמתו ונופל על `GitHub Personal Access Token is not set` *אחרי* שהתוצרים כבר קיימים - וכשהשלב נכשל, שלב ההעלאה לא רץ ולא נשאר מה להוריד. התיקון: `--publish never` (גם ב-workflow וגם ב-npm script).

---

### #036 — בורר העמדה במסך הכניסה: קיבוץ לפי בסיס אב, והאחרון שעודכן ראשון

**תאריך:** 2026-08-02

**הבקשה:** במסך הכניסה, אחרי בחירת המשתמש, לקבץ את רשימת העמדות לקטגוריות לפי בסיס אב (הכל סגור, פתיחת קטגוריה חושפת את עמדותיה); להציג את העמדות מהאחרון שעודכן/נוצר עם חותמת הזמן לצידן; ובבסיס אב יחיד - לא להציג כותרת קטגוריה.

**הבעיה:** הרשימה הייתה `<select>` שטוח ממוין אלפביתית (`ORDER BY name`) - 23 עמדות מ-5 בסיסים בערבוביה, בלי שום רמז מתי כל עמדה הוגדרה. הבקר סורק את כל הרשימה כדי למצוא את העמדה שלו.

**המחסום ב-DB:** ל-`workstation_presets` לא הייתה עמודת `updated_at` כלל, ולכן "האחרון שעודכן" לא היה ניתן לחישוב. נוספה `updated_at TIMESTAMPTZ` עם backfill מ-`created_at` (בלי ה-backfill כל 23 העמדות הוותיקות היו נוחתות בתחתית הרשימה), ו-`DEFAULT NOW()` נקבע רק **אחרי** המילוי. ה-PUT ועדכון ספי העומס דורסים אותה ב-`NOW()`.

**מה נבנה:**
- `src/utils/presetGroups.ts` - הלוגיקה הטהורה (קיבוץ, מיון, `formatStationTime`), נפרדת מה-UI כדי שתיבדק בלי DOM ותשמש גם מסכים נוספים. בסיס אב שנמחק (מזהה בלי שם) מאוחד ל"ללא בסיס" ולא מציג מזהה גולמי; קבוצת "ללא בסיס אב" תמיד אחרונה - היא סל שאריות, לא בסיס.
- `src/components/shared/StationPicker.tsx` - האקורדיון עצמו. הפלטה מגיעה מ-`crewPalette` של `StationCrewForm` (הטופס שנפתח מיד אחרי הבחירה) כדי ששני השלבים ייראו כרצף אחד בכל תמה.
- `GET /api/workstation-presets` - `LEFT JOIN aviation_bases` מוסיף `parent_base_name` (מייתר טעינה נפרדת של טבלת הבסיסים אצל כל צרכן), והסדר שונה ל-`COALESCE(updated_at, created_at) DESC`.
- `pickWorkstation()` ב-`e2e/helpers.ts` - החליף שישה שכפולים של נהיגה ב-`<select>` בקבצי ה-e2e.

**RTL:** חותמת הזמן עטופה ב-`dir="ltr"` - "28/06 14:32" הוא רצף תווים חלשים, ובהקשר עברי האלגוריתם הדו-כיווני הפך אותו ל-"14:32 28/06". חץ הקטגוריה הסגורה מסובב ב-180° בעברית כדי להצביע לכיוון הטקסט.

**קבצים:** `src/utils/presetGroups.ts` (+`.test.ts`), `src/components/shared/StationPicker.tsx`, `src/App.tsx`, `server/routes/workstations.js`, `server/db/init.js`, `src/i18n/registry/shared.json`, `e2e/helpers.ts`, `e2e/station-picker.spec.ts`, `e2e/{emblem-admin,emblems,leo-logo,station-crew,mission-desk}.spec.ts`, `data-model.md`.

**QA:** 22 בדיקות יחידה חדשות (TDD - נכתבו לפני המימוש) · 677/677 unit · tsc נקי · אימות מול ה-DB האמיתי: המיגרציה idempotent, 23 עמדות ב-6 קבוצות, 0 בלי חותמת, 0 `parent_base_id` יתום · אימות API חי: POST → PUT מקדם את `updated_at` והעמדה קופצת לראש הרשימה → DELETE · 2 בדיקות e2e חדשות מול נתונים אמיתיים (קטגוריות סגורות בפתיחה, המונה תואם, מיון יורד בכל קטגוריה, חותמת זמן לכל עמדה) · צילומי מסך בעברית ובאנגלית.

**תיקון בעקבות משוב (אותו יום):** התצוגה הבחינה בין "נוצר" ל"עודכן" לפי הפרש החותמות. הבקשה הייתה **תמיד זמן העדכון האחרון** - לעמדה שלא שונתה מאז שנוצרה שתי החותמות שוות ממילא (backfill המיגרציה), ולכן ההבחנה רק הוסיפה רעש. `isUpdatedStamp` והמפתח `shared.stationCreatedAt` הוסרו, ובדיקת ה-e2e מוודאת עכשיו במפורש ש"נוצר" **אינו** מופיע.

---

### #037 — סקטורים: ניהול בעורך האזורים, בחירה פר-מפה בעמדה, ומיקוד מהמפה

**תאריך:** 2026-08-02

**הבקשה:** (1) בניהול מערכת, במסך האזורים — להציג את הסקטורים הקיימים בתפריט הצד, עם עדכון ומחיקה. (2) בהגדרת עמדה — להגדיר אם רוצים לראות סקטורים ואילו, מתוך הקיימים במפה הנבחרת; בעמדה עם שתי מפות, לכל מפה בנפרד. (3) בעמדה — הרשימה בפינה הימנית העליונה של כל מפה, לחיצה מציגה את הסקטור הנבחר, ובאותה רשימה כפתור "מפה מלאה".

**מה כבר היה:** "מצב סקטור" בעורך האזורים ידע **ליצור** מפת סקטור (גרירת מלבן → crop → `maps` חדשה עם `parent_map_id` + `parent_rect` + העתקת האזורים שבתחום). מה שלא היה: שום מקום לראות את הסקטורים שנוצרו, לתקן אותם או למחוק אותם — וגם לא שימוש כלשהו בהם בעמדה.

**ההחלטה המרכזית — סקטור בעמדה הוא תצוגה, לא מפה אחרת.** לחיצה על סקטור **אינה** מחליפה את המפה למפת-הבת אלא ממקדת את אותה מפה (זום+פאן) על `parent_rect`. מפת הבת היא crop של אותם פיקסלים, ולכן התוצאה החזותית זהה — אבל הפ"ממים, האזורים ונקודות ההעברה שעל המפה נשארים חיים, בעוד שמעבר למפת הבת (שהאזורים בה הם עותקים נפרדים) היה מנתק אותם. "מפה מלאה" הוא בדיוק הצד השני של אותה פעולה: חזרה לזום 1.

**מה נבנה:**
- `src/utils/sectorFocus.ts` — הלוגיקה הטהורה: `parseParentRect` (JSONB או מחרוזת ותיקה, ממוין x1<x2) ו-`sectorFocusView`. שכבות המפה עוברות `translate(pan) scale(zoom)` עם `transform-origin: center`, ולכן `pan = (מרכז הפאנל - מרכז הסקטור) * zoom`; גודל הפאנל נגזר מ-`imgBounds` (`panelW = width + 2*left`) ולא ממדידת DOM נוספת. הזום נחסם ב-3 — תקרת כפתורי הזום — אחרת לחיצה על + אחרי מיקוד הייתה **מקטינה** את המפה.
- `PATCH /api/maps/:id` — עדכון **חלקי** (שם / `image_data` / `parent_rect`), כך ששינוי שם לא מוחק את התמונה. שם כפול → 409, שם ריק → 400.
- `POST /api/maps/:id/sync-zones-from-parent` הורחב ליצור אזור-בת לאזור שנכנס לתחום רק עכשיו. בלי זה **הרחבת** תחום סקטור הייתה משאירה את האזורים החדשים מחוץ למפת הסקטור (הלולאה דילגה על אזור בלי עותק קיים).
- `MapZoneEditor` — פאנל "סקטורים במפה זו" בתפריט הצד: בחירה מדגישה את התחום על המפה (ענבר, נבדל מהטורקיז של המלבן הנגרר), שינוי שם inline, **תיחום מחדש** (גרירת מלבן חדש → crop → PATCH → סנכרון אזורים), ומחיקה עם אישור.
- `ManagementPage` — `renderSectorPicker`, **אותו רכיב** למפה 1 ולמפה 2 (DRY), עם הגדרה נפרדת לחלוטין לכל מפה. הסקטורים המוצעים הם מפות-הבת של אותה מפה בלבד, כך שעמדה לא יכולה לבחור סקטור ששייך למפה אחרת.
- `SectorDashboard` — הרשימה בפינה של כל פאנל מפה, מצב פר-מפה (`activeSectorByMap` לפי מפתח הפאנל) כדי ששתי המפות לא ידרסו זו את בחירת זו. "מפה מלאה" מופיע רק כשסקטור נבחר, וכפתור ה"איפוס" שבסרגל הזום קורא לאותה פונקציה — אחרת הרשימה הייתה ממשיכה לסמן סקטור שכבר לא בפוקוס.

**DB:** 4 עמודות ב-`workstation_presets` — `sector_maps_enabled`/`sector_map_ids` (מפה 1) ו-`map2_sector_maps_enabled`/`map2_sector_map_ids` (מפה 2). הסדר במערך = סדר הרשימה על המפה.

**RTL:** הרשימה ממוקמת ב-`right` **פיזי** (מול סרגל הזום שב-`left` הפיזי) ולא ב-`insetInlineEnd`. המפה היא מרחב גאוגרפי, לא זרימת טקסט — צד ימין של המפה נשאר ימין גם באנגלית.

**קבצים:** `src/utils/sectorFocus.ts` (+`.test.ts`), `src/components/map/MapZoneEditor.tsx`, `src/components/admin/ManagementPage.tsx`, `src/components/views/SectorDashboard.tsx`, `server/routes/maps.js`, `server/routes/workstations.js`, `server/db/init.js`, `src/i18n/registry/{map,admin,ctrl}.json`, `data-model.md`, `SERVICES.md`.

**QA:** 10 בדיקות יחידה חדשות · 773/773 unit (כולל שומר ה-i18n) · tsc נקי · build נקי · המיגרציה הורצה מול ה-DB האמיתי ואומתה ב-`information_schema` · smoke API חי מול נתונים אמיתיים: יצירת מפת אב+סקטור → `GET /maps` מחזיר `parent_map_id`+`parent_rect` → PATCH שם (התמונה והתחום נשמרו) → PATCH תיחום מחדש → 409/400/404 בשגיאות → sync-zones → ניקוי · round-trip של הגדרות העמדה: POST → GET config → PUT → GET config, עם אימות ששתי המפות נשמרות בנפרד.

**לא נבדק:** הרינדור בפועל של שלושת המסכים (פאנל הסקטורים בעורך, הבורר בהגדרת עמדה, הרשימה בעמדה) לא נראה על מסך — אומת רק דרך tsc/build/בדיקות. אין בדיקת e2e לרשימה שבעמדה.

---

### #038 — בסיס אב כציר הרשאה: ראש צוות מנהל מכלול, וכל תוכן ה-admin מקובץ לפיו

**תאריך:** 2026-08-02

**הבקשה:** (1) במסך הניהול — לקבץ את העמדות לפי בסיס אב. (2) מי שנכנס כראש צוות יראה רק את העמדות של המכלולים שיש לו הרשאה אליהם (מהמיראז'). (3) למפות — להוסיף גם כן בסיס אב, להציג רק לפי הרשאות ולקבץ לפיו. (4) אותו דבר לעזרים ולבלוקים.

**מה כבר היה:** `workstation_presets.parent_base_id` שימש לתצוגה בלבד (סמל הבסיס, וקיבוץ בורר העמדה במסך הכניסה — #036). המיראז' אישר **עמדות**, וההגבלה נאכפה רק על הכניסה לעמדה ועל סרגל התצוגה. מסך הניהול היה גלובלי לחלוטין: ראש צוות ראה את כל העמדות, המפות, העזרים והבלוקים של כל הבסיסים. רשימת העמדות שם קובצה לפי תפקיד (יב"א/מגדל/כללי) ולא לפי בסיס.

**ההחלטה המרכזית — בסיס האב הוא ציר הרשאה, ואישור לעמדה אחת פותח את כל המכלול.** המיראז' מאשר עמדות, לא בסיסים; `allowedBaseKeys()` ממפה את העמדות המאושרות לבסיסי האב שלהן, וממנה והלאה **הבסיס** הוא יחידת ההרשאה. הסיבה: ראש צוות מנהל מכלול, לא עמדה בודדת — אם הוא מורשה לעמדה בבסיס, אין היגיון שיערוך אותה אבל לא את המפה או טבלת הבלוקים שהיא משתמשת בהן. הכלל השני: **תוכן בלי בסיס אב (`NULL`) גלוי לכולם** — סל התוכן המשותף, לא תוכן מסווג. כלל אחד לכל סוגי הפריטים, כדי שלא ייווצר תוכן יתום שאיש לא רואה ולא יכול לשייך.

**נקודת האכיפה — אחת, לא בכל טאב.** `ManagementPage` טוען את הרשימות המלאות (`allPresets`/`allMaps`/`allBlockSpaces`/`allBlockTables`) וגוזר מהן ב-`useMemo` רשימות מסוננות באותם שמות שהקוד כבר השתמש בהם. כך **כל** הצרכנים — AidsManager, WorkGroupsManager, בחירת העמדות לבלוק, טאב הקשרים, בורר המפה בהגדרת עמדה — הוגבלו בלי לגעת באף אחד מהם. `assignableBases` מגביל בנפרד לאילו בסיסים מותר לשייך תוכן **חדש**.

**מה נבנה:**
- `src/utils/presetGroups.ts` הורחב: `baseKeyOf`, `allowedBaseKeys`, `isBaseAllowed`, `filterByAllowedBases`, `groupItemsByBase`. נשמרה מוסכמת המיראז' — רשימת אישורים ריקה = **אין הגבלה** (`null`), `[-1]` = הגבלה שאף עמדה בה לא זוהתה. הקיבוץ הכללי ממוין **לפי שם** ולא לפי עדכניות: מסך ניהול הוא רשימת הגדרות שמחפשים בה לפי שם, בשונה מבורר הכניסה התפעולי.
- `src/components/admin/BaseGroupList.tsx` — רכיב הקיבוץ המשותף לארבעת הטאבים (במקום ארבעה מימושים), עם `renderItems` שמאפשר גם קיבוץ-משנה בתוך הבסיס. קבוצה יחידה → אין כותרת. הכותרות **פתוחות כברירת מחדל**, בשונה מבורר הכניסה שסגור: שם המטרה היא מסך קצר, כאן זה משטח עריכה והסתרה רק מוסיפה קליקים. כולל `ParentBaseSelect` — בורר בסיס אב אחיד לכל הטפסים.
- **עמדות:** קיבוץ ראשי לפי בסיס אב, ובתוכו הקיבוץ הקיים לפי תפקיד. אותה היררכיה בדיוק כמו במסך המשתמשים של המיראז' (`mirage/admin.html`), כך שמי שמגדיר שם הרשאות רואה את אותו סדר גם כאן.
- **מפות:** `MapsManager` מקבל `bases`/`assignableBases`/`allowedBases`; בורר בסיס בטופס ההעלאה ובכל שורת מפה (שמירה מיידית ב-PATCH), הרשימה מקובצת ומסוננת. בלי הפרופס (פתיחה כמודל עצמאי) ההתנהגות לא השתנתה.
- **עזרים:** רשימת העמדות מקובצת, "קשר לקבוצה קיימת" מסונן, וקבוצה חדשה **יורשת את בסיס האב של העמדה שנבחרה** — שיוך נכון בלי שדה נוסף למלא.
- **בלוקים:** בסיס אב גם למרחב וגם לטבלה (החלטת המשתמש). המרחבים מקובצים, והטבלאות מקובצות לפי בסיס ובתוכו לפי הקטגוריה הקיימת (`categoriesOf` מחושב פר-בסיס במקום גלובלית).
- מצב ריק מובחן: ראש צוות בלי אף בסיס מורשה מקבל "אין עמדות בהיקף הניהול שלך" ולא "לא הוגדרו עמדות" — הסיבה היא הרשאה, לא היעדר הגדרה.

**DB:** `parent_base_id INTEGER` (ללא FK אכיף, כמו בעמדה) ב-`maps`, `aid_groups`, `block_spaces`, `block_tables` — כולן טבלאות **קונפיג**, קיימות ב-`public` בלבד, ולכן אין השלכת סביבות תרגול.

**API:** `parent_base_id` נוסף ל-`POST/PATCH /api/maps`, `POST/PUT /api/aid-groups`, `POST/PUT /api/block-spaces`, `POST/PUT /api/block-tables`. `PUT /api/aid-groups/:id` מעדכן את הבסיס **רק כשהשדה נשלח** — אחרת שינוי שם היה מנתק קבוצה מהבסיס שלה. שכפול טבלת בלוקים משמר את הבסיס; שכפול קבוצת עזרים לעמדה אחרת מקבל את הבסיס של **עמדת היעד**, כי העותק חי מעכשיו אצלה.

**RTL:** כל המרווחים והגבולות שנגעתי בהם הועברו ל-logical properties (`borderInlineStart`, `paddingInlineStart`, `textAlign:'start'`).

**קבצים:** `src/utils/presetGroups.ts` (+`.test.ts`), `src/components/admin/BaseGroupList.tsx` (חדש), `src/components/admin/ManagementPage.tsx`, `src/components/admin/managers.tsx`, `src/components/map/MapsManager.tsx`, `server/routes/{maps,admin,blocks}.js`, `server/db/init.js`, `src/i18n/registry/admin.json`, `data-model.md`, `SERVICES.md`.

**QA:** 16 בדיקות יחידה חדשות · 773/773 unit (כולל שומר ה-i18n) · tsc נקי · build נקי · המיגרציה הורצה מול ה-DB האמיתי ואומתה ב-`information_schema` · smoke API חי מול נתונים אמיתיים על ארבעת סוגי התוכן: POST עם בסיס → GET מחזיר אותו → ניקוי שיוך (`null`) → שיוך מחדש → עדכון שם בלבד לא נוגע בבסיס → שכפול טבלת בלוקים משמר בסיס → ניקוי כל רשומות הבדיקה.

**לא נבדק:** הרינדור בפועל של ארבעת הטאבים לא נראה על מסך — אומת דרך tsc/build/בדיקות ו-smoke API בלבד. אין בדיקת e2e לסינון ההרשאות של ראש צוות (נדרש משתמש מיראז' עם הגבלת עמדות מול DB בדיקות).

---

### #039 — זיהוי: פ"מ באזור (התמונ"א פוגשת את הפ"ממים)

**תאריך:** 2026-08-12

**הבקשה:** במוד מפה מעוגנת עם אזורים, וכשתמונ"א דולקת — לזהות שהאזור שהוקצה לפ"מ מכיל **את הרכיב האווירי שלו בלבד**: הוא לא חורג, לא עוזב, ואף אחד לא נכנס אליו. השידוך בין הפ"מ לרכיב הוא לפי **או"ק דומה אך לא זהה** ("בננה" מול "בננה1"), בהתאמה של 80% מהאותיות בסדר. שלוש התראות: `פ"מ XXX חורג מהאזור`, `או"ק XXX נכנס לאזור שבו נמצא YYY ללא תיאום`, וחריגת בלוק גובה — שנחשבת "כאילו חרג מהאזור".

**מה כבר היה:** כמעט כל התשתית. ההקצאה לאזור (`strip_zone_assignments` + `extra_zones`) בגרירה ובשיבוץ, בלוקי הגובה (`zone_altitude_ranges`) וה-`fzPinExceedance`, גאומטריית הפוליגון (`src/utils/zoneHit.ts`), והתמונ"א שכבר משליכה כל מטוס ל**אותו מרחב אחוזי-מפה** שבו נשמרים הפוליגונים (`track.place`). גם ארבעת הסטטוסים (`בדרך לאזור`/`באזור`/`עוזב אזור`/`כניסה`) כבר היו — אבל **נקבעו ידנית** בתפריט ה-⋮ של הפין. מה שחסר היה הגשר: מי מחליט שהמטוס הזה הוא הפ"מ הזה.

**ההחלטה המרכזית — או"ק דומה נמדד ב-LCS מנורמל לשם ה*ארוך*, ועל הא-ב בלבד.** "כמה מהאותיות משותפות **ובסדר**" הוא בדיוק תת-סדרה משותפת ארוכה ביותר; מה שנשאר להכריע הוא המכנה. נרמול לפי השם הקצר היה מתאים "א" ל"אבגד" ב-100% ומייצר שידוכי שווא בשדה עמוס, ולכן לפי הארוך. **הספרות אינן נכנסות להשוואה** (הבהרת המשתמש): הספרה היא מספר סידורי בתוך המבנה ולא שם, ולכן "בננה", "בננה1" ו"בננה12" הם אותו שם בדיוק. בלי ההבחנה הזו סיומת דו-ספרתית הייתה יורדת ל-0.67 ונופלת מהסף — כלומר פ"מ שלא היה מזוהה כלל.

**ומה שההחלטה הזו שברה, ותוקן באותה נשימה:** משעה שהאותיות אינן מבדילות, שני פ"מים נפרדים בשם "בננה1" ו"בננה2" מקבלים **שניהם** ציון 1 מול הרכיב "בננה1", והשידוך היה נקבע לפי סדר הרשימה. לכן השידוך נושא **שובר שוויון** נפרד (`fullCallsignSimilarity`) שבו הספרה כן נספרת. הפרדת התפקידים היא העיקר: הסף עונה על "האם זה בכלל אותו שם", ושובר השוויון על "ומי מבין הדומים". או"ק שכולו ספרות נופל לצורה המלאה — אחרי הסרת הספרות לא היה נשאר ממנו דבר, והוא לא היה משתדך לעולם.

**ההחלטה השנייה — חיץ *ומעליו* זמן החזקה, ולא בדיקה נקודתית.** התמונ"א מגיעה כל 2 שניות ומרעידה בשבריר אחוז. מטוס שטס לאורך גבול אזור היה מייצר סדרת "חורג/חזר" — וההתראה שמצלצלת לשווא היא ההתראה שהבקר לומד להתעלם ממנה, כלומר גרועה מאין התראה. לכן רצועת היסטרזיס (0.3% מפה סביב הקו, 100 רגל סביב הבלוק) **ובנוסף** 5 שניות החזקה לפני שכל שינוי מתקבל. הסימטריה חשובה: כדי להיכנס צריך להיות בפנים מעבר לחיץ, וכדי לצאת — בחוץ מעבר לחיץ.

**ההחלטה השלישית — כותב אחד.** הסטטוס משותף לכל מי שרואה את הפ"מ, אבל אילו כל עמדה שרואה את האזור הייתה כותבת אותו, אותה כתיבה הייתה נשלחת חמש פעמים בכל טיק. רק העמדה שב-`preset_id` של ההקצאה כותבת. הכתיבה עוברת ב-`PATCH .../status` ולא ב-POST הקיים, שהוא upsert מלא: הוא היה מחזיר גם את ההערה, התיאום והמיקום כפי שהעמדה הכירה אותם ברענון האחרון — ודורס עריכה שנעשתה בינתיים בעמדה אחרת.

**מה שלא מרנדר:** ה-hook **אינו נרשם** ל-`airPictureStore`. מנוי היה מרנדר את `SectorDashboard` (18,700 שורות) בכל דגימה, 30 פעם בדקה — בדיוק מה ש-AIR_PICTURE_SPEC §5.2 נבנה כדי למנוע. במקום זה טיימר של שנייה קורא את הסנאפשוט **מתוך עצמו**, ו-`setState` נקרא רק כשחתימת ההתראות משתנה — כמה פעמים במשמרת.

**מטריצת המקרים שנקבעה מראש** (ולא תוקנה תוך כדי):
- **אובדן מגע אינו חריגה.** רכיב שנחת או יצא מכיסוי המאגר פשוט אינו ניתן לבדיקה; התראה כאן הייתה מדווחת חריגה בכל נחיתה.
- **מבנה.** "בננה1" ו"בננה2" שניהם שייכים לפ"מ "בננה". בלי זה השני היה מדווח כרכיב זר **באזור של עצמו**. הפ"מ "באזור" רק כשכל חבריו בפנים, וחבר שיצא מייצר חריגה.
- **פ"מ שטרם נכנס** מעולם אינו "חורג" — הוא "בדרך לאזור".
- **הפרדה אנכית מלאה** משתיקה את התראת הכניסה ללא תיאום (החלטת המשתמש), וכך גם `is_coordinated`.
- **תמונה שאינה `live` מקפיאה** את הזיהוי במקום להסיק ממנה — חישוב-חשבון על דגימה ישנה היה מטיס את המטוס אל מחוץ לאזור ומדווח חריגה שלא קרתה.
- **דו-מפה:** אותה הקצאה משתקפת גם על מפת הבן; ההתראות מאוחדות לפי זהות **תפעולית** (סוג · פ"מ · הזר) ולא לפי מזהה אזור, אחרת אותו אירוע היה מדווח פעמיים.

**מה נבנה:**
- `src/airPicture/zoneWatch.ts` (חדש) — המנוע. טהור לחלוטין, הזמן נכנס כפרמטר, ולכן כל המטריצה למעלה נבדקת ב-vitest בלי להריץ עמדה. המצב נבנה מחדש בכל טיק ונושא רק מפתחות חיים, כך שמשמרת של 12 שעות אינה מנפחת אותו.
- `src/airPicture/useZoneWatch.ts` (חדש) — ההרצה בעמדה: טיק של שנייה, הקפאה על תמונה לא-חיה, איחוד דו-מפה, חלון חסימת כתיבה חוזרת של 20 שניות (גדול ממחזור הרענון של 5 שניות, ובכל זאת מתקן מעצמו סטטוס שנדרס ידנית).
- `SectorDashboard` — באנר התראות בראש המפה (שורה להתראה, ✕ שמשתיק עד שהיא חולפת) + טבעת `fzring-conflict` על הפין. הטבעת ממשיכה להבהב גם אחרי השתקת הבאנר: המצב עצמו לא חלף.
- **תקרת הבאנר: שלוש שורות, והשאר בגלילה** (תיקון אחרי שנראה על מסך). רכיב זר שנכנס לאזור עמוס מייצר שורה לכל פ"מ שנמצא בו, והבאנר גדל עד שכיסה את חצי המפה העליון — כלומר בדיוק את האזורים שעליהם הוא מתריע. השורה **בגובה קבוע ובשורה אחת** (`ellipsis`, הטקסט המלא ב-`title`) ולא `flexWrap`: אחרת "שלוש התראות" אינו מספר קבוע — טקסט ארוך שנשבר לשתי שורות היה מכווץ את הרשימה הנראית לשתיים, ודווקא במסך צר שבו המקום יקר מכל. נוספה **לשונית מניין** שאומרת כמה יש בסך הכל, כי התראה רביעית שמסתתרת מתחת לקיפול בלי שדבר מרמז על קיומה היא בדיוק כשל ההתראות שהפיצ'ר בא למנוע. הגלילה ב-`overscrollBehavior: 'contain'` (לא מתגלגלת למפה) ו-`touchAction: 'pan-y'` (אצבע ועט ב-Cintiq, לא רק גלגל עכבר).
- `PATCH /api/strip-zone-assignments/:strip_id/status` — שדה אחד, 404 על רשומה שנותקה (ולא יצירה מחדש).
- קבוצת i18n חדשה `zoneWatch` — שלוש ההתראות בעברית ובאנגלית, כמשפט שלם עם `{{param}}` ולא כרסיסי JSX.

**קבצים:** `src/airPicture/zoneWatch.ts` (+`.test.ts`), `src/airPicture/useZoneWatch.ts`, `src/components/views/SectorDashboard.tsx`, `server/routes/maps.js`, `src/i18n/registry/zoneWatch.json`, `src/i18n/registry.ts`, `SERVICES.md`, `data-model.md`, `AIR_PICTURE_SPEC.md`.

**QA:** 50 בדיקות יחידה חדשות · 1675/1676 unit ירוקות · tsc נקי · build נקי · שומר ה-i18n ירוק.

**על הכישלון היחיד — `env-isolation.integration`:** הוא נכשל גם על העץ הראשי הלא-משונה, כלומר **קדם לשינוי הזה ואינו נובע ממנו** (אומת בהרצה נפרדת של אותו קובץ על הבסיס: `1 failed | 7 passed`). הוא גם עבר באחת ההרצות ונפל באחרת בלי שינוי קוד ביניהן, ולכן זו תלות בהשהיית Neon — ה-timeout הוא 30 שניות והרצת הקובץ נמשכת ~110 שניות. טיפול נפרד: להעלות את ה-timeout או להוציא את הקובץ מהסוויטה השוטפת. **שים לב למלכודת שבדרך:** `vitest ... | tail -6` מחזיר את קוד היציאה של `tail` ולא של vitest, ולכן `exit 0` שם אינו אומר דבר — הקובע הוא הפלט.

**מה כן נראה על מסך:** הבאנר, במוד אזורים עם תמונ"א חיה — ומשם הגיעה תקרת שלוש השורות (למעלה). **מה שעדיין לא:** הטבעת על הפין, נראות הבאנר תחת שלוש התמות ובסקייל 24", זמן התגובה בפועל מרגע החציה, וכתיבת הסטטוס מול DB חי.

---

## (היסטורי) הצעד הבא שתוכנן — שני הענקים הנותרים

### למה ManagementPage + SectorDashboard נדחו

שניהם **consumers בתחתית הקובץ** שצורכים עשרות sub-components המוגדרים מעליהם ב-App:
- **ManagementPage** (~7,400 שורות) צורך ~17 רכיבים: AidsManager, BlockVisualPainter, ClassicPartnersAndPointsEditor, ClassicStripCard, ClosuresManager, DefaultNamesManager, MapsManager, QueryBuilder, SerialsAdminTab, SettingsModal, StripGridEditor, StripWindowAdmin, TableModesManager, WorkGroupsManager, ...
- **SectorDashboard** (~14,500 שורות) צורך עוד יותר (Strip✅, transfer cards✅, DraggableNeighborPanel, BlockMiniView, ...)

**כלל:** אי אפשר לחלץ consumer לפני שכל ה-sub-components שלו importable.
לכן הסדר חייב להיות bottom-up:
1. חלץ את רכיבי ה-admin הבודדים → `src/components/admin/` (CrewManager, PresetsManager, AidsManager, TableModesManager, SerialsAdminTab, DebriefingTab, ClosuresManager, StripWindowAdmin, DefaultNamesManager, BlockVisualPainter, QueryBuilder, SettingsModal, ...)
2. חלץ רכיבי sector נותרים → DraggableNeighborPanel, BlockMiniView, TableHandwritingCanvas, GroundVehiclePanel, StickyNotesLayer
3. אז `ManagementPage` → `src/components/admin/ManagementPage.tsx`
4. ולבסוף `SectorDashboard` → `src/components/views/SectorDashboard.tsx`

**אזהרה קריטית:** כל שלב = build-verify מיד. לא להשאיר את ה-build שבור בין שלבים.

---

## (ישן) הצעד הבא — חילוץ ה-views הגדולים

**הענקים שנותרו ב-App.tsx:**
| view | שורות (~) | תפקיד |
|---|---|---|
| `SectorDashboard` | ~14,500 | עמדת CTRL הראשית |
| `ManagementPage` | ~7,400 | מסך admin |
| `GroundView` | ~4,800 | עמדת TWR (מגרש) |
| `Strip` | ~870 | רכיב הסטריפ המרכזי |
| `MapZoneEditor` | ~1,200 | עורך אזורי מפה |

**סדר מומלץ (מהקטן לגדול, כל אחד עם build verify):**
1. `Strip` → `src/components/strips/Strip.tsx` (תלוי: sc✅, customConfirm✅, parseNoteValue✅, HandwritingOverlay, BlockMiniView)
2. רכיבי transfer (OutgoingTransferCard, IncomingTransferCard) → `src/components/transfers/`
3. `MapZoneEditor` → `src/components/map/`
4. `GroundView` → `src/components/views/GroundView.tsx`
5. `ManagementPage` → `src/components/admin/`
6. `SectorDashboard` → `src/components/views/SectorDashboard.tsx` (אחרון — הכי מורכב)

**כלל זהב:** כל חילוץ = build verify מיד אחריו. אם build נשבר — לחזור אחורה.
**אזהרה:** SectorDashboard מחייב שכל הרכיבים שהוא משתמש בהם כבר חולצו תחילה.

---

## User Stories

---

## סטטוס כללי

| שלב | סטטוס | תאריך |
|---|---|---|
| #045 עליית ה-DB, התלת מימד, פאנל השכבות וסרגל העמדות (v1.0.104) | ✅ הושלם | 2026-08-19 |
| #037 סקטורים: ניהול בעורך, בחירה פר-מפה, מיקוד מהמפה | ✅ הושלם | 2026-08-02 |
| #018 דסק משימה כללי (מודול חדש) | ✅ הושלם | 2026-07-21 |
| קובץ לוג + Baseline | ✅ הושלם | 2026-06-21 |
| QA Baseline | ✅ הושלם | 2026-06-21 |
| פיצול server.js — DB layer + routes | ✅ הושלם | 2026-06-21 |
| QA אחרי server.js — 353/353 routes | ✅ הושלם | 2026-06-21 |
| פיצול App.tsx — utilities + types + shared (חולץ) | ✅ הושלם | 2026-06-21 |
| פיצול App.tsx — WIRING (App מייבא בפועל) + build verify | ✅ הושלם | 2026-06-21 |
| פיצול App.tsx — Strip + transfers + MapZoneEditor + GroundView + ground layer | ✅ הושלם | 2026-06-21 |
| פיצול App.tsx — כל ה-sub-components + ManagementPage + SectorDashboard | ✅ הושלם | 2026-06-22 |
| **App.tsx: 41,625 → 728 שורות (98.3% חולץ ל-38 מודולים)** | ✅ הושלם | 2026-06-22 |
| QA סופי | ✅ הושלם | 2026-06-21 |
| User Stories | ✅ הושלם | 2026-06-21 |
| ARCHITECTURE.md | ✅ הושלם | 2026-06-21 |
| תיקון DB tables חסרות | ✅ הושלם | 2026-06-21 |
| **GAPI (GALAXY API) — אינטגרציה דו-כיוונית עם מערכת השו"ב** | ✅ הושלם | 2026-07-30 |

---

### #040 - פקדים בסטריפ האזרחי: כפתור, שדה, דגל ושני תפריטים

**תאריך:** 2026-08-15

**הבקשה:** (אפיון "עדכון פיצר פמ סטריפים אזרחי") להוסיף לסטריפ האזרחי **פקדים** שהמנהל מגדיר והפקח משנה בלחיצה - כפתור מחזורי, שדה מקלדת/כתב יד, דגל, תפריט בחירה בודדת ותפריט בחירה מרובה. לכל פקד: עיצוב מותנה, ערך ב"מ, והיקף - פנימי לחלון (מתאפס בחלון אחר) או גלובלי לפ"מ. בניהול - כמה פקדים במשבצת, מסודרים בגרירה.

**האפיון המלא, כולל מטריצת 24 מקרים:** [CIV_STRIP_CONTROLS.md](CIV_STRIP_CONTROLS.md). שלוש הכרעות נקבעו עם הצוות **לפני** התכנון: הכרטיס האזרחי מקבל עורך פריסה כמו הקלאסי; "חלון" = הלוח האזרחי של עמדה; ופקד גלובלי הוא שדה לכל דבר בכל המערכת.

**מה כבר היה, ולכן לא נבנה מחדש:** עץ הפריסה `layout_json` (SGCell/SGSplit) ועורכו `StripGridEditor`, התנאים `conditions_json` מול ה-Query Builder, ו-`strips.custom_fields` שהוא שדה **פנימי** במפת הפ"מ - GAPI אינו כותב אליו ו-upsert נכנס אינו נוגע בו. גם תבנית **גריד** כבר הייתה קיימת (`mode='civil'`), ולכן **לא נוסף מוד תבנית חדש** - רק `workstation_presets.civilian_strip_table_id` שקובע איזו תבנית הלוח האזרחי מצייר.

**ההחלטה המרכזית - הכרטיס האזרחי לא קיבל מנוע רינדור משלו.** הוא מצייר דרך `ClassicStripCard` עם `layoutJson`, ולכן התאים, התנאים, גובה הסטריפ והפקדים הם התנהגות אחת בשני המסכים. הכרטיס הקבוע הישן נשאר כנפילה לאחור לעמדה בלי תבנית, כך שאף לוח קיים לא השתנה בלי שהמנהל ביקש.

**ההחלטה השנייה - "אין NULL", והערך האפקטיבי נפתר בקריאה.** ערך שמור → ב"מ → אפס-הסוג (`''` / `FALSE` / `[]`). ה-ב"מ אינו מחומר, ולכן שינוי שלו בניהול משנה מיד כל פ"מ שלא נגעו בו. **ההבחנה שהצילה את הדגל:** `undefined`/`null` הם "לא נקבע מעולם" ורק הם נופלים ל-ב"מ; ערך שנוקה **במפורש** (`false`, `''`, `[]`) הוא ערך לכל דבר. בלי ההפרדה הזו דגל שהפקח כיבה, ושה-ב"מ שלו TRUE, היה נדלק מחדש בכל רענון - שקר תפעולי שהיה מתגלה רק בשדה.

**ההחלטה השלישית - השאילתא פותרת את אותו ב"מ.** פקד גלובלי נחשף כשדה `ctl__<key>`, ו-`getQFieldValue` מריץ עליו את **אותה** `resolveControlValue`. בלי זה פ"מ שמציג במסך TRUE (ברירת מחדל) היה נבדק בשאילתא כ-false, והסינון היה סותר את מה שהעין רואה. פקד **פנימי ללוח** אינו נכנס לרשימת השדות במכוון: ערכו חסר משמעות מחוץ ללוח שלו.

**כתיבה נקודתית למפתח.** הערך הגלובלי נכתב ב-`jsonb_set` על המפתח בלבד ולא בכתיבה מחדש של כל `custom_fields` - שתי עמדות שמשנות שני פקדים שונים על אותו פ"מ באותו רגע לא דורסות זו את זו. הכתיבה בעמדה אופטימית, אבל **נכשלת בקול**: כשהשרת לא ענה הערך חוזר לקדמותו.

**גרירה.** סידור הפקדים במשבצת ב-Pointer Events + `touchAction:'none'` + `setPointerCapture`, והשורה שמתחת למצביע נמצאת ב-`elementFromPoint`. משטח כתב היד (`InkPad`) נפתח בחצי מסך - **מחולק ב---s** בגלל ה-`zoom` של ה-root, אחרת "חצי מסך" היה יוצא 82% - וקנבס ריק נשמר כערך ריק ולא כתמונה לבנה.

**קבצים:** `src/types/stripControls.ts`, `src/utils/stripControls.ts`, `src/components/classic/StripControl.tsx`, `src/components/shared/InkPad.tsx`, `src/components/admin/StripControlsEditor.tsx` (חדשים) · `ClassicViews.tsx`, `managers.tsx`, `ManagementPage.tsx`, `SectorDashboard.tsx`, `queryBuilder.ts`, `QueryBuilder.tsx`, `stripGrid.ts` · `server/routes/stripControls.js` (חדש), `civilian.js`, `workstations.js`, `middleware/auth.js`, `db/init.js`, `db/env-tables.js`, `db/foreign-keys.js`.

**QA:** 46 בדיקות יחידה חדשות (38 ללוגיקת הפקדים, 8 לשאילתות) · 1907/1907 unit · tsc נקי · build נקי · המיגרציה הורצה מול ה-DB האמיתי ואומתה ב-`information_schema` (`updated_at` הוא TIMESTAMPTZ) · smoke API חי מול נתונים אמיתיים: כתיבת ערך פנימי → קריאתו לפי לוח → ערך גלובלי → **מפתח שני שאינו דורס את הראשון** → מפתח לא חוקי נדחה ב-400 → `GET /api/civ-strips` מחזיר את שני המחסנים → ניקוי כל רשומות הבדיקה.

**לא נבדק:** הרינדור בפועל לא נראה על מסך - אומת דרך tsc/build/בדיקות ו-smoke API בלבד. אין בדיקת e2e ללחיצה על פקד בעמדה ולסידור בגרירה בעורך.

**חוב שהתגלה ולא נגעתי בו:** הלוח האזרחי גורר כרטיסים בין העמודות ב-HTML5 `draggable` (מאז שנבנה), שאינו נשלח באצבע - כלומר הגרירה הזו לא עובדת על מסך המגע. זה חוב קיים שאינו נובע מהפיצ'ר, ותיקונו הוא שינוי נפרד.

---

### #041 - "רענן הגדרות" עובר לתפריט התצוגה, ומרענן באמת

**תאריך:** 2026-08-16

**הבקשה:** להעביר את "רענן הגדרות" לתפריט **תצוגה**, וש**לחיצה עליו תרענן את העמדה - בעיקר את הגדרות התצוגה - בלי להפיל אותה**.

**מה הפריט עשה קודם:** משך *רק* את `workstation-presets/:id/config`. זו הייתה פעולה כמעט חסרת ערך, כי אותה תצורה נמשכת ממילא בפולינג כל 10 שניות. כל **שאר** הגדרות התצוגה - מודי הטבלה (הגדרת העמודות), המפה, אזוריה ושיבוצי הפ"ממים שעליה - נטענות **רק בכניסה לעמדה**, ולכן שינוי שנעשה בניהול חייב יציאה וכניסה כדי להיראות. `refreshWorkstation` מושך עכשיו את כולן במקביל, ובנוסף את `loadSlowData` (קטלוג השדות והפקדים, טבלאות הקלאסי, הבלוקים) ואת `loadData`.

**"בלי להפיל" - זה העיקר, וזה מה שקבע את המימוש.** אין `location.reload()` ואין remount: המשמרת, החלונות הצפים, הפתקים והציור נשארים במקומם. ובמכוון **לא** נגענו במה שהמפעיל בחר בעצמו - מפה/טבלה/בלוקים, איזו מפה ואיזה מוד טבלה. לכן הרענון מושך את המפה **הנוכחית** (`currentMapId`) ולא קורא ל-`loadDefaultMap`, שהיה מחזיר את העמדה למפת ברירת המחדל; ומרענן את **רשימת** מודי הטבלה בלי להריץ מחדש את האפקט שמאלץ `table_mode_id` מהתצורה - אלה בדיוק ה"הפלות" שהפיצ'ר נועד למנוע.

**משוב:** טוסט מותאם-תמה. כשל של אחד המקורות אינו מרוקן את המסך (מידע ישן עדיף על מסך ריק) אבל מדווח למפעיל שזה מה שהוא רואה.

**קבצים:** `src/components/views/SectorDashboard.tsx` (הפריט עבר לתפריט התצוגה, `refreshWorkstation`, `configRefreshTick`, טוסט) · `src/utils/helpTopics.ts` + `src/i18n/registry/help.json` (סעיף העזרה עבר מ-`settingsMenu` ל-`viewMenu`) · `src/i18n/registry/ctrl.json` (4 מפתחות) · `e2e/view-menu-refresh.spec.ts` (חדש).

**QA:** 1932/1932 unit · tsc נקי · build נקי · e2e חדש **עבר מול הנתונים האמיתיים**: הפריט בתפריט התצוגה ולא בהגדרות; אחרי לחיצה - הטוסט עולה, לא חוזרים ללוגין, אין מסך טעינה, וסימון שהוטבע על אלמנט בסרגל **שרד** (הוכחה שלא היה reload ולא remount).

**לא נבדק:** לא נבדק בעין ששינוי שנעשה בניהול (עמודת טבלה, אזור במפה) אכן מופיע מיד אחרי לחיצה - נבדקה משיכת המקורות ואי-הפלת העמדה, לא ההשתקפות הוויזואלית של שינוי ניהולי.

---

### #042 — זיהוי פ"מ באזור: הבלוק ברום טיסה, התמונ"א ברגל

**תאריך:** 2026-08-16

**הדיווח:** "זה לא עובד אם בחרתי בלוק גבהים לפ"מ ולא רק אזור." עם אזור בלבד — הזיהוי עבד; ברגע שנבחר בלוק — הפ"מ לא הגיע ל"באזור" אף פעם, וישבה עליו התראת חריגת גובה קבועה.

**מה באמת קרה — שתי יחידות שנפגשו בלי שאיש הכריז על כך.** לא ניחשתי, שאילתתי את ה-DB החי:

| מקור | ערכים בפועל | יחידה |
|---|---|---|
| `zone_altitude_ranges` (כל 96 השורות) | 100-400 · "נמוך" 100-140 · "גבוה" 150-400 | **רום טיסה** |
| `strips.alt` | `FL235`, `090`, `310`, `370` | **רום טיסה** |
| `AirTrack.alt` (תמונ"א) | 12000, 25000 | **רגל** |

כלומר **כל** המערכת מדברת רום טיסה, והתמונ"א היא החריג היחיד — כי כך המאגר החיצוני מוסר אותה. מנוע הזיהוי הוא הצומת הראשון שבו השתיים נפגשות, והוא השווה 12,000 רגל מול "140". התוצאה: כל מטוס תמיד מחוץ לבלוק.

**למה זה לא נתפס קודם:** ההשוואה הקיימת (`fzPinExceedance`) מודדת `strips.alt` מול הבלוק — רום טיסה מול רום טיסה, עקבי לחלוטין. הבאג נולד בדיוק כשנכנס הצרכן השלישי. **ו-`data-model.md` אמר במפורש "טווח הגובה (רגל)"** — תיעוד שגוי שקיבעתי כנכון ובנה עליו את המנוע. זה התיקון האמיתי כאן: המסמך תוקן לפי הנתונים, לא להפך.

**התיקון:** `blockAltFeet` ב-`zoneWatch.ts` — ממיר את **הבלוק** לרגל ולא את המטוס. הכיוון מכוון: המנוע כולו ברגל (`ALT_BUFFER_FT`), לתמונ"א אין שגיאת עיגול, ולבלוק — שגובהו תמיד כפולה של 100 — אין מה לאבד. ערך מעל FL600 (60,000 רגל, מעל כל מה שטס באזור) נקרא כרגל ואינו מומר פעמיים, כך ששורה חריגה לא תיהפך למיליון רגל.

**קבעתי את זה בבדיקות** ולא רק בהערה: 9 בדיקות חדשות, ובהן אחת שמריצה את הבאג עצמו — בלוק שנשלח למנוע כרום טיסה מייצר `alt-deviation`, ואותו בלוק אחרי המרה מייצר "באזור". נפילה של הבדיקה הזו תסמן מיד שהיחידה חזרה להתערבב.

**קבצים:** `src/airPicture/zoneWatch.ts` (+`.test.ts`), `src/components/views/SectorDashboard.tsx`, `data-model.md` (שתי הופעות), `SERVICES.md`, `AIR_PICTURE_SPEC.md`.

**QA:** 59 בדיקות ב-`zoneWatch.test.ts` (מ-50) · tsc נקי · build נקי · הסוויטה המלאה ירוקה.

**לא נבדק:** התיקון לא נראה על מסך מול תמונ"א חיה — האימות הוא ברמת המנוע ומול הנתונים ב-DB. יש לאמת בעמדה שפ"מ עם בלוק "נמוך" אכן מגיע ל"באזור".

---

### #043 — זיהוי חריגה מאזור: "מחק הכל", הגדרות עמדה, והדגשה כשהתמונה כבויה

**תאריך:** 2026-08-17

**הבקשה:** (1) "מחק הכל" בבאנר ההתראות. (2) תת-תפריט "הגדרות חריגה מאזור" בהגדרות עמדה: התראות כן/לא, והצג התראות גם אם התמונה האווירית כבויה כן/לא. (3) אם כן — להדגיש את הרכיב האווירי שחורג, כשהתמונה כבויה.

**ההחלטה הראשונה — שק JSONB אחד ולא עמודה לכל מתג.** `workstation_presets` נשמרת דרך INSERT ו-UPDATE שהם **רשימות פוזיציוניות בנות 66 פרמטרים**. כל מתג בוליאני חדש שם הוא הזדמנות לשגיאת היסט שקטה — ואכן, בעריכה הראשונה נוספו ה-placeholders בלי שם העמודה וה-INSERT נשבר (57 עמודות מול 58 placeholders). לכן `zone_watch_settings JSONB`: מתג חדש בעתיד לא ידרוש נגיעה ב-SQL בכלל. **הלקח המעשי:** אחרי עריכה כזו לא מספיק שהקובץ מתקמפל — ספרתי עמודות מול placeholders מול אורך מערך הפרמטרים, בשלושת ה-UPDATE ובכל ה-INSERT.

**ההחלטה השנייה — ברירות מחדל שמשמרות התנהגות.** `alerts` דולק, `whenPictureOff` כבוי, ושניהם נקראים מ**מפתח חסר** ולא מערך שנכתב. לכן שלוש העמדות שכבר במוד אזורים (`{}` ב-DB) מתנהגות בדיוק כפי שהתנהגו — בלי מיגרציית נתונים ובלי לגעת בהן.

**ההחלטה השלישית — מי דוגם כשהתמונה כבויה.** זו הייתה המלכודת האמיתית: `AirPictureLayer` אינו מרונדר כשהתמונה כבויה, ו**הוא** זה שקורא ל-`joinAirPicture`. כלומר "התראות גם בלי תמונה" היה יושב על חנות ריקה ולא מתריע לעולם — פיצ'ר שנראה עובד ואינו עובד. לכן `useZoneWatch` מצטרף ל-poller בעצמו; ההצטרפות סופרת מנויים, ולכן כשהשכבה כן רצה אין תעבורה כפולה.

**ההחלטה הרביעית — מדגישים את החורג, לא מציירים תמונה מוקטנת.** הפקח שכיבה את התמונה עשה זאת בכוונה: היא מרעישה את המפה. לכן במצב הזה מצויר **רק** מי שמאחורי התראה חיה — טבעת פועמת, או"ק, וגובה במאות רגל (אותו פורמט של תווית התמונ"א). כשהתמונה דולקת ההדגשה נעלמת: כל המטוסים מצוירים ממילא, וסימון נוסף עליהם הוא רעש ולא מידע.

**פרטים שנפלו לתוך המימוש:**
- **`offenders` בחתימה נפרדת מההתראות.** המיקום זז כל שנייה גם כשרשימת ההתראות זהה; חתימה משותפת הייתה מקפיאה את הסימון על המפה במקום הראשון שבו נצפה. העיגול לשתי ספרות אחרי הנקודה הוא מה שמונע רינדור על רעש של אלפית אחוז.
- **`trackId` נוסף ל-`ZoneAlert`** (רק ב-`intruder`), כדי שהעמדה תדע **איזה** רכיב להדגיש בלי לפרק את מחרוזת ה-`key`.
- **"מחק הכל" אינו דגל גורף** אלא הוספה של המפתחות החיים לרשימת המבוטלים. אירוע חדש יופיע שוב, והטבעת על הפין ממשיכה להבהב — השתקת טקסט אינה שינוי מצב.
- **ההדגשה בקואורדינטות פיקסלים ולא ב-`viewBox` של 100x100.** תמונת מפה אינה ריבועית, ומתיחה לא-אחידה הייתה הופכת את עיגול הסימון לאליפסה.

**מה שהשומרים תפסו — שניים:**
1. `tr('zoneWatch.optOn')`/`optOff` נכתבו בקוד לפני שנרשמו ב-registry. `i18n-guard` נכשל על "מפתחות יתומים" — בדיוק מה שהוא נבנה בשבילו.
2. **`signalSeverity.test.ts` נפל — ולא בגלל באג אצלי, אלא כי חשף באג בו עצמו.** הבדיקה מוודאת שהבהוב "קריטי" אינו יורד ל-`opacity 0`, וחותכת את הקטע ב-`indexOf('}\n}')`. אבל `App.css` נשמר ב-**CRLF**, ולכן הסמן הזה לא נמצא בו כלל, ה-`indexOf` החזיר -1, וה-`slice` סרק **2,794 תווים של CSS זר** במקום את 215 התווים של הקטע. היא עברה עד היום רק במקרה — כי לא היה `opacity:0` בטווח הרחב הזה. ה-`stroke-opacity: 0.35` שהוספתי מכיל את `opacity:0` כתת-מחרוזת, והפיל אותה. תיקנתי **את שני הצדדים**: ה-CSS שלי עבר ל-`.35` (אותו ערך, בלי תת-המחרוזת), והבדיקה מנרמלת סופי שורה לפני החיתוך — כך שהיא סוף סוף בודקת את מה שהיא מצהירה עליו.

**קבצים:** `server/db/init.js`, `server/routes/workstations.js`, `src/components/admin/ManagementPage.tsx`, `src/components/views/SectorDashboard.tsx`, `src/airPicture/useZoneWatch.ts`, `src/airPicture/zoneWatch.ts`, `src/i18n/registry/zoneWatch.json`, `src/App.css`, `src/utils/signalSeverity.test.ts`, `SERVICES.md`, `data-model.md`, `AIR_PICTURE_SPEC.md`.

**QA:** tsc נקי · הסוויטה המלאה ירוקה · build נקי · `i18n-guard` ירוק · העמודה הוחלה על ה-DB המשותף ב-`ADD COLUMN IF NOT EXISTS` ואומתה בשאילתה (שלוש העמדות במוד אזורים מחזיקות `{}`).

**לא נבדק:** שום דבר מזה לא נראה על מסך — לא תת-התפריט בניהול, לא "מחק הכל", ולא ההדגשה מול תמונ"א חיה עם התמונה כבויה. האימות הוא ברמת הטיפוסים, הבדיקות, וסכמת ה-DB.

---

### #044 — תמונ"א ומז"א: כפתור הצגה ישיר על המפה, לא צ'קבוקס בטופס

**תאריך:** 2026-08-17

**הבקשה:** "בחלון תמונ"א / מז"א אני רוצה כפתור פיזי להצגה על מפה, ולא להדליק/לכבות רק בטופס."

**מה היה:** בסרגל המפה של עמדת הבקר ישבו שני כפתורים — `✈` ו-`🌦` — ושניהם **פתחו טופס**. ההדלקה והכיבוי של השכבה על המפה היו צ'קבוקס **בתוך** הטופס. כלומר הפעולה שחוזרת עשרות פעמים במשמרת ("תראה לי את התמונה / תוריד אותה, היא מכסה לי את האזור") עלתה שלוש נגיעות: כפתור → צ'קבוקס → סגירה. בעמדת המגדל זה כבר היה נכון — שם הצ'קבוקס יושב בפאנל השכבות ולידו ⚙ — כך שהעמדות גם לא התנהגו אותו דבר.

**מה נעשה:** כל שכבה היא עכשיו **זוג כפתורים מוצמד** ברוחב הסרגל (20px):

| | |
|---|---|
| הכפתור העליון (`✈` / `🌦`) | **מדליק ומכבה את השכבה על המפה** — נגיעה אחת. הרקע מסמן דלוק/כבוי |
| הרצועה התחתונה (`⚙` / `☰`) | פותחת את ההגדרות / תפריט השכבות |

**מה נשמר במכוון:** צבע האייקון של התמונ"א ממשיך לסמן את **מצב החיבור** (ירוק/אדום/כתום) ולא את מצב הכפתור — פקח שמסתכל על תמונה חייב לדעת אם היא עדכנית, וזו שאלה נפרדת מ"האם היא מוצגת". גם ה-✓ הקטן בפינה נשאר. ו-`toggleWeather` ממשיך להדליק את השכבה בפתיחה ראשונה של התפריט, כי "הצג מז"א" צריך להציג מז"א.

**קבצים:** `src/components/views/SectorDashboard.tsx`, `src/i18n/registry/airPicture.json`.

**QA:** tsc נקי · 1995/1995 unit ירוקות · הועלה בפועל (`:5002` + API `:3003`) ונטען בלי שגיאות.

**לא נבדק:** הזוג לא נלחץ ביד על מסך — לא אומת שהרצועה בת 10px נוחה לעט ולאצבע ב-Cintiq. זו המידה היחידה כאן שראויה לבדיקה פיזית.

### #045 — ארבע פעמים "לחצתי ולא קרה כלום": עליית ה-DB, התלת מימד, פאנל השכבות וסרגל העמדות

**תאריך:** 2026-08-19 · **גרסה:** v1.0.104

**הבקשה:** "יש תקלה - תצוגת עמדות אחרות ותצוגת תלת מימד לא עובד" (ב-Railway).

**מה נמצא — ארבע תקלות נפרדות, לא אחת:**

**1. השרת בייצור לא סיים לעלות.** `/api/health` החזיר `phase:"failed"` כבר ~4.5 שעות:
`[environments] טבלאות לא מסווגות: undo_actions, undo_journal`. פיצ'ר ה-CTRL+Z (רשומת 2026-08-16) יצר
שתי טבלאות ולא סיווג אותן, ו-`checkTableClassification` — שנועד בדיוק לזה — הפיל את השרשרת.
המחיר לא היה רק הודעה בלוג: `syncAllEnvSchemas` ו**כל העבודות המחזוריות** רצות *אחריה*
ב-`server.js`, ולכן קבלה אוטומטית בנקודת מעבר (סבב 5 שניות), ניקוי פ"מים שפגו וניקוי נקודות
העברה זמניות — **לא רצו כלל בייצור**. תוך כדי האימות התגלו עוד שלוש: `reader_docs`,
`reader_bookmarks`, `reader_quick_items`, שענף "חלון קריאה" יצר ב-DB המשותף. סווגו גם הן,
באותו סיווג שבענף שלהן. אימות: 127 טבלאות ב-public, **0 לא מסווגות**, ו-boot מלא
(`checkTableClassification 139ms · syncAllEnvSchemas 13.9s · ה-DB מוכן`).

**2. התלת מימד: הכפתור נדלק ולא קרה כלום.** התנאי היה
`show3D && (p3d.mode !== 'overlay' || imgBounds)`, ו-`overlay` הוא ברירת המחדל. ב**שדה בלי
מפת רקע** (`airfieldMapSrc` ריק) `imgBounds` יכול להישאר `null` **לצמיתות**: האפקט שמתקין את
המדידה תלוי ב-`[updateImgBounds, airfieldMapSrc]`, וכשאין מפה אף אחד מהם אינו משתנה שוב.
בשדה עם מפה זה תיקן את עצמו כשה-`src` הגיע — ולכן זה עבד מקומית ונכשל בייצור. תוקן בשניים:
`shouldRenderPattern3D(show3D)` (המתג לבדו מחליט; הסצנה צריכה **יחס**, ו-`boundsAspect(null)`
מחזיר 1), ונפילה אחורה של המדידה גם ל-`mapRef` כדי שלא יישאר מסלול ל-`null` קבוע.

**3. פאנל השכבות חתך את השורות התחתונות של עצמו.** בלי `maxHeight` ועם `overflow:hidden`,
פקדי הזום, כפתור התלת מימד וכפתור הציור נדחפו מחוץ לאזור המפה. נמדד: ב-18" הכפתור ישב ב-
y=897 לוגי — מתחת לגבול התצוגה; ב-15.6" `elementFromPoint` על מרכזו החזיר
`BUTTON[rwy-takeoff-33R]` — פאנל "מסלולים בשימוש" (z=8900, portal ל-body) בלע את הקליק.
הגובה נחסם עכשיו ל**חיתוך של אזור המפה עם החלון**, **בחלוקה ב-`--s`** (מלכודת 3 ב-CLAUDE.md:
`getBoundingClientRect` מחזיר פיקסלים פיזיים ו-`maxHeight` נמדד ביחידות לוגיות — בלי החלוקה
החסימה יוצאת גדולה פי 1.65 ופשוט לא נכנסת לתוקף), עם `RUNWAY_PANEL_RESERVE` לפאנל הצף.
אחרי התיקון `elementFromPoint` מחזיר `BUTTON[pattern-3d-toggle]`.

**4. סרגל תצוגת העמדות נעלם בשקט.** `shown.length === 0 → return null`, בלי הבדל בין "לא
הוגדרו עמדות" לבין "אין הרשאה". שתי הודעות חדשות, כי הפעולה המתקנת שונה.

**קבצים:** `server/db/env-tables.js` (+`.test.js`), `src/utils/pattern3d.ts` (+`.test.ts`),
`src/components/views/GroundView.tsx`, `src/components/shared/StationPeekBar.tsx`,
`src/i18n/registry/ctrl.json`, `e2e/helpers.ts`, `e2e/station-peek.spec.ts`.

**תקלה שנגרמה תוך כדי, ותוקנה:** `e2e/station-peek.spec.ts` מחקה את העמדות-לצפייה של
`usable[0]` בלי לשחזר. `GET /api/workstation-presets` ממוין **עדכני-ראשון**, ולכן זו בדיוק
העמדה שנערכה זה עתה — וההגדרה האמיתית של המשתמש נמחקה על ה-DB החי (ה-`.env` המקומי מצביע על
אותו Neon של Railway). נוספו `clearViewStations`/`restoreViewStations`: צילום לפני, שחזור
ב-`finally`. אומת round-trip על עמדת `__` זמנית — התוכן (יעד, תווית, סדר) חוזר זהה.

**QA:** tsc נקי · 2055/2055 unit ירוקות · build עובר · Playwright מול ה-DB האמיתי בארבעה
גדלי מסך · אימות בייצור אחרי `railway up`: `{"ok":true,"phase":"ready","bootMs":124771}`
ו-bundle `index-D5V9Os8K.js` נושא `1.0.104`.

**לא נבדק:** התלת מימד לא נלחץ ביד על ה-Cintiq — האימות היה בדפדפן. וגם: לא נמצא הסבר
לכך שאזור המפה נמתח מ---206 עד 1206 פיקסלים פיזיים בחלון של 1080 ב-24" (הפאנל נחסם נכון
ביחס לחלון, אבל הגלישה עצמה של אזור המפה נשארה כפי שהיא ולא נגעתי בה).
